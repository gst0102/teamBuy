# teamBuy 项目通用 Codex 规则

本文件是 teamBuy 全项目的通用规则。它只描述当前仍适用的工程约束；历史记录和阶段性方案统一放在 `docs/archive/`，不具有指令优先级。

## 1. 生效范围与文档优先级

- 系统、开发者和用户当前回合的明确要求优先于仓库文档。
- 本文件适用于整个仓库。
- `automation/ascript/AGENTS.override.md` 只在 `automation/ascript/` 及其子目录生效，用于补充和覆盖 AScript 专项规则。
- `docs/CURRENT.md` 是当前状态入口，`docs/decisions-active.md` 是当前决策入口；开发前先读这两个文件和本文件。
- `docs/archive/` 仅用于追溯历史，不得把其中的阶段目标、旧测试门禁或旧交接结论当成当前规则。

## 2. 开始工作与变更纪律

- 开始任务先执行 `git status --short --branch` 和 `git diff --stat`，识别已有改动归属。
- 不覆盖、不回滚、不重置用户或其他 Codex 的未提交改动；禁止使用 `git reset --hard`、`git checkout --`。
- 代码和文档修改前先检查相关文件与调用关系，优先采用最小生产改动。
- 关键配置、计数含义、分支条件、保护逻辑和失败处理必须有说明性注释，写清用途与停止/继续条件；修改行为时同步更新注释。
- 普通文本文件使用 `apply_patch` 修改；删除只能针对已确认的单个明确路径，禁止批量删除目录或文件。
- 不把未验证的推断写成已完成事实；报告要区分代码存在、静态检查通过、运行验证通过和用户验收通过。
- 不把密钥、Token、AppSecret、私钥、session 或生产环境变量写入前端、日志、文档或 Git。
- 用户只要求诊断时不顺带实现；用户要求实现时才修改代码，并运行最接近目标的现有检查。

## 3. 当前产品与架构基线

- 正式产品名为“资料整理助手”。稳定基座负责企业微信通信、会话归档、身份、会话管理、合规、支付权益、资料库和展示页；具体内容能力通过 Skill 扩展。
- 所有文字来源统一进入 `ContentObject -> content-to-note -> UserNote`；来源可以是企业微信消息、微信笔记、聊天记录、链接、手动文字或后续 OCR。
- 资料卡按类型处理：链接/文章、普通文字、房源、团购商品、图片/OCR；统一流程是收藏、编辑、整理、生成，不复制出平行内容系统。
- 用户身份唯一锚点是小程序微信 `openid`；企业微信身份只能作为来源映射，不能仅凭昵称、手机号或文本相似度自动认领。
- 企业微信会话归档采用共享核心：单一回调、单一 Finance SDK Worker、单一企业级游标和统一解密层；teamBuy 作为项目消费者使用独立 token、业务状态和路由配置。
- 媒体必须登记原始哈希和处理后哈希，按 `mediaType + hash` 去重；业务上传统一走 `AppService.process_and_store_media()`。
- `showcase-builder` 是可视化配置工具，不是由 AI 全权生成的展示页；AI 输出必须经过结构化校验，不能直接写库、扣费或执行关键业务动作。

## 4. 小程序与分享基线

- 未登录用户可以浏览公开内容；只有新建、上传、保存、发布、个人工作台等需要归属的动作才触发登录，并回到原动作。
- 列表先展示元数据，媒体、画布和非关键统计后台补齐；资料/合集列表元数据缓存基线为 60 秒，分类缓存基线为 10 分钟，并隔离环境、接口和用户身份。
- 客户手机号、微信号、身份和雷达详情只允许短时进程内缓存，不写入小程序持久化存储；退出登录要清理用户资源缓存。
- 所有资料、合集、名片和服务方案的分享快照统一走 `miniprogram/plugins/share-snapshot/index.js`，必须绑定当前版本，不能把列表页或默认截图当成分享封面。
- 页面核心尺寸默认使用 `rpx`；按钮、标签、操作区使用 flex 控制上下左右居中；参考图是验收标准，功能可用不等于 UI 验收通过。
- 小程序预览、体验版上传和审核默认由用户在微信开发者工具中手动完成；Codex 不自行反复调用上传 CLI。

## 5. 测试与部署基线

- 后端测试统一使用仓库根目录 `.venv313` 的 Python 3.13：
  `PYTHONPATH=.:backend ./.venv313/bin/python -m pytest backend/tests`
- 仅 `py_compile`、`node --check` 或 JSON 解析不能替代后端测试；交付报告必须说明命令、通过数量和未覆盖项。
- 生产部署前检查磁盘和 Docker 状态，备份 Compose 与本次变更源码；不得覆盖生产 `.env`、secrets、媒体目录和运行态数据。
- 生产部署按 `docs/archive/deploy` 中的历史经验和 `/Users/yiyi/Desktop/Desktop/myprojects/cloud_tencent/cloud_tencent.md` 执行，部署后检查容器、数据库、内网健康检查、端口映射和公网健康检查。
- 不执行 `docker system prune -a`、镜像/卷批量清理或其他不可逆清理，除非用户明确确认。

## 6. 文档维护

- 新的当前事实写入 `docs/CURRENT.md`；已经确认且可复用的决策写入 `docs/decisions-active.md`。
- 阶段性交接、旧方案、旧测试报告和问题复盘放入 `docs/archive/`，必须标明历史性质。
- 不再新增平行的长期规则文件；需要 AScript 专项规则时只修改 `automation/ascript/AGENTS.override.md`。
