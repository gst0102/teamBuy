# teamBuy 项目级 Codex 工作规则

本文件是 teamBuy 项目的 Codex 工作总纲。

所有架构设计、功能开发、测试验收、Bug 修复、上线检查，默认都应遵循本文件规则。

## 0. 项目知识库与 Codex 启动必读

聊天记录是临时记忆，仓库文档才是长期记忆。每个新 Codex 会话开始前，必须优先读取以下文件，再决定是否开发：

```text
AGENTS.md
docs/project-memory.md
docs/decisions.md
docs/pitfalls.md
docs/dev-log.md
docs/handoff-latest.md
```

启动后先执行：

```text
git status --short --branch
git diff --stat
```

在输出接手理解前，不要直接改代码。接手理解至少包含：

- 项目目标
- 当前代码状态
- 已确认的重要决策
- 当前风险
- 下一步建议执行顺序

每次阶段性结束或上下文变长时，必须更新：

```text
docs/dev-log.md
docs/decisions.md
docs/pitfalls.md
docs/handoff-latest.md
```

固定提示词模板保存在：

```text
docs/prompts/codex-start.md
docs/prompts/codex-handoff.md
```

## 0.1 长期架构总纲

当前正式产品名是“资料整理助手”。完整架构计划固定保存在：

```text
docs/stage2-docs/08-plugin-architecture.md
```

后续架构设计和开发默认遵循以下规则：

- 系统分为稳定基座和可插拔 Skill。稳定基座负责企业微信通信、会话内容存档、用户身份识别、会话管理、合规、支付、笔记库和展示页基础能力；Skill 只负责具体内容处理能力。
- 企业微信入口默认采用混合驱动模式：快捷指令 / 菜单点按优先，规则匹配其次，AI 意图识别只做兜底。AI 只能返回固定枚举和置信度，不能直接写库、扣费、发通知或执行业务动作；低置信度和非法输出必须返回确认菜单。
- 文字类来源统一抽象为 `ContentObject`，并统一进入 `content-to-note`。微信笔记、聊天记录、链接文章、手动文字和后续 OCR 只是不同 Input Adapter，不拆成多个重复的文字整理 Skill。
- 资料卡采用多类型信息结构：统一流程是“收藏 -> 编辑 -> 整理 -> 生成”，但数据结构必须分型。URL/公众号文章是链接卡/阅读卡，普通文字是文本卡，房源是字段卡，团购是商品卡，图片/截图后续是 OCR 卡。第一版 typed card 架构见 `docs/stage2-docs/12-typed-content-card-architecture.md`。
- 用户身份唯一锚点是小程序微信 `openid`。企业微信客服、会话内容存档、OCR、手动录入、链接收藏等多途径来源最终都必须归属到 `openid` 对应的内部用户；`userId` 只是后端内部主键。企业微信 `external_userid` 只允许作为系统内部来源映射到 `openid/userId`，P0 不做面向用户的绑定管理、解绑或改绑功能。
- `note-to-comic-image` 是独立渲染型 Skill；`showcase-builder` 是小程序可视化配置工具，不是 AI 自动全权生成展示页。
- 支付属于 `billing-core` 基座能力，只控制权益和额度，不和内容整理 Skill 耦合。

### 0.1.1 共享企业微信会话存档平台

企业微信会话内容存档是企业级全局消息流，不能由多个产品项目各自调用 Finance SDK。多个项目和未来产品统一接入 `platform/wecom-archive-core/`：

- 共享核心是唯一回调入口、唯一 Finance SDK Worker、唯一企业级 `seq` 游标和唯一解密层；
- 项目通过路由注册接收消息，使用独立项目 token、独立业务数据库和独立业务处理状态；
- 项目处于 `WECOM_ARCHIVE_SOURCE=shared` 时，禁止启动本地 Finance SDK 拉取 worker；
- `sdkfileid` 不得跨项目暴露，媒体必须通过共享核心的项目鉴权媒体代理读取；
- 新项目默认不匹配任何群，只有显式配置 `chatIds` 或 `allowAllChats=true` 才能接收消息；
- 新项目在核心开始拉取后加入时，必须使用核心管理员 replay 接口补投历史事件；不能重新推进全局游标；
- 企业微信后台只配置共享核心的事件回调地址，项目旧回调仅保留迁移兼容，不得并行作为主入口。
- 共享核心的加密原始归档只作为中转数据，默认从入库时间起保留 24 小时；项目已成功 ACK 的事件允许清理，仍有未成功投递的事件不得清理。

### 0.1.2 媒体资产哈希与去重

- 所有进入资料库的图片媒体必须登记 `originalSha256` 和处理后 `storageSha256`，并按 `mediaType + hash` 复用已有 `MediaAsset`；数据库唯一索引是最终去重边界。
- `MediaAssetRef` 是资料、笔记或附件与媒体资产之间的关系记录，多个业务对象引用同一张图是合法的，不等于重复保存图片二进制。
- 业务上传入口必须复用 `AppService.process_and_store_media()`；`MediaStorageService.store_bytes()` 只是底层存储能力，不得在业务接口里绕过媒体资产登记。

## 0.2 小程序上传约定

小程序预览、上传体验版和提交审核默认由用户在微信开发者工具中手动完成。

Codex 不要每轮反复尝试调用微信开发者工具 CLI 上传或预览小程序；除非用户本轮明确要求，并确认开发者工具服务端口已经开启。Codex 默认只负责代码实现、静态检查、JSON 校验、后端测试，并在需要上传时提醒用户自行上传。

## 0.2.1 UI 参考稿与实现一致性要求

凡是用户已经提供页面参考图、高保真稿、标准模板图或 Codex 自己输出过 UI 参考图的功能，开发时必须把该参考稿视为验收标准之一。

禁止出现以下情况：

- 文档和计划里写的是一套页面，实际开发只做成简化功能版。
- 参考图里有头像、统计卡、列表、轨迹、底部操作等关键模块，实际页面缺失或布局完全不同。
- 只验证接口和数据，不验证页面是否贴近参考图。
- 用“功能能跑”替代 UI 验收。

开发完成前必须做：

- 对照参考图逐项检查页面结构、关键模块、文案、按钮和空态。
- 明确标注哪些地方与参考图一致，哪些因数据或小程序限制做了替代方案。
- 如果因为工期只做功能版，必须在交付说明里明确写“当前不是参考图版本”，不能让用户误以为已经按图完成。
- 涉及头像、图片等外部资源时，必须考虑真机小程序域名限制和无图兜底，不能让页面出现空白头像或破图。

验收时如果页面与参考图明显不一致，即使接口和自动化测试通过，也不得判定为“通过”。

## 0.2.2 UI 文本居中与按钮排版硬规则

小程序页面里的按钮、状态标签、胶囊标签、宫格入口、底部操作条、卡片右侧操作区，默认必须做到文字上下居中、左右居中。

禁止反复出现以下问题：

- 只靠 `line-height` 硬凑垂直居中，真机上文字偏上或偏下。
- 按钮没有 `display:flex; align-items:center; justify-content:center;`。
- 标签宽度过窄导致“已发布”这类短文案被拆成两行。
- 操作按钮常驻过宽，挤压左侧标题、摘要、标签和封面。
- 默认 button 样式导致文字偏移、边框或内边距不可控。

开发要求：

- 小程序全局居中基线统一放在 `miniprogram/app.wxss`，页面里不要各自重新发明按钮/标签居中规则。
- 所有自定义按钮和标签优先使用 flex 居中。
- 状态标签必须 `white-space: nowrap`，必要时设置 `min-width`。
- 固定操作区必须给出明确宽度或 grid/flex 约束，不能让按钮自由撑开。
- 列表卡片右侧操作区只能放紧凑主按钮和轻量更多按钮，不能让两个按钮各占半张卡或上下堆成大块。
- 小按钮、胶囊和状态标签必须在真机截图里检查文字上下左右居中。
- 如果使用原生 `button`，必须重置 `margin/padding/line-height`，并用 flex 重新控制排版。

## 0.2.3 小程序尺寸单位硬规则

小程序页面样式默认必须使用 `rpx`，尤其是字号、按钮高度、头像尺寸、卡片间距、统计数字、图标宫格和底部安全区相关尺寸。

开发要求：

- 页面级 WXSS 不要混用 `px` 和 `rpx` 做核心布局尺寸，除非是 `1px` 边框或第三方组件限制。
- 统计数字、胶囊数字、角标数字必须显式设置 `font-size: xxrpx`，不能依赖浏览器或小程序默认字号。
- 头像、按钮、图标宫格和操作入口必须用 `rpx` 固定视觉比例，并用 flex 居中。
- iPad、大屏手机和普通手机都必须检查数字和按钮不会变形、偏小或错位。

## 0.2.4 小程序首屏与缓存性能硬规则

资料整理助手的“我的”、资料库、客户雷达等高频页面，默认遵循“先展示可用信息，后台补齐重资源”的读取策略：

- 列表首屏请求必须区分元数据和媒体资源。禁止为了返回列表而等待所有封面、附件或图片下载、转存和本地缓存；媒体资源应懒加载或后台预热。
- 页面返回时优先使用用户和环境隔离的内存缓存先渲染，再后台刷新网络数据。缓存必须明确 TTL、并发请求合并和失效条件，不能只通过不断延长 TTL 掩盖阻塞链路。
- 缓存 key 至少隔离环境、接口地址和用户身份；客户身份、手机号、微信号等敏感信息不得写入本地持久化缓存。
- 首屏基础内容不得被媒体下载、画布生成、AI 处理或非关键统计接口阻塞；慢请求必须有可见的加载状态，不能让页面长时间空白等待。
- 开发验收必须分别检查冷启动、页面返回和缓存命中三种路径，并记录关键页面从进入到基础内容可见的耗时。发现“读取慢”时，先定位请求瀑布和阻塞任务，再决定缓存或接口修改。
- 个人页只需要资源数量和当前名片摘要时，必须使用轻量摘要接口；禁止调用会逐卡计算统计、客户摘要或下载媒体的全量资料列表接口来构造首屏。
- 分享图是发送动作的附属产物：列表加载不得批量等待分享图生成；名片必须先完成名片专用分享图，才允许触发原生分享，不能用头像或普通资料默认图替代。

## 0.2.5 统一缓存策略基线

- 资料/合集/专题列表元数据默认缓存 60 秒；分类缓存 10 分钟；允许先展示未过期或可接受的陈旧快照，再后台刷新，空数组也是有效快照。
- 静态分享图以后端版本化快照为唯一持久化真源；前端只做页面内存复用和同版本并发合并，不把分享图作为长期本地文件缓存。
- 列表只缓存元数据和封面 URL，不等待附件下载；图片媒体文件最多按封面及每卡少量图片按需缓存，缓存必须同时有 7 天 TTL、数量上限和总字节上限，PDF/视频默认不进入本地媒体文件缓存。
- 客户手机号、微信号、客户身份和雷达详情只允许短时进程内缓存，禁止写入 `wx` 持久化存储；缓存 key 必须隔离环境、API 地址和用户身份，退出登录要清理用户资源缓存。

## 0.3 腾讯云生产部署约定

teamBuy 生产服务器部署方式以以下文档为准：

```text
/Users/yiyi/Desktop/Desktop/myprojects/cloud_tencent/cloud_tencent.md
```

当前生产服务器：

```text
IP: 81.70.84.35
user: ubuntu
project dir: /home/ubuntu/teamBuy
domain: https://teambuy.lifelove.top
ssh key: /Users/yiyi/Desktop/Desktop/vedo-project/vidoekey.pem
```

登录示例：

```text
chmod 600 /Users/yiyi/Desktop/Desktop/vedo-project/vidoekey.pem
ssh -i /Users/yiyi/Desktop/Desktop/vedo-project/vidoekey.pem ubuntu@81.70.84.35
```

部署注意：

- 服务器磁盘可能接近满盘；部署前先检查 `df -h` 和 `docker system df`。
- 不要覆盖生产 `backend/.env`、`backend/secrets/`、媒体目录和运行态数据。
- 改 Docker Compose、Nginx 或后端代码前必须先备份。
- 不要执行 `docker system prune -a`、`docker image prune -a`、`docker volume prune`，除非用户明确确认。
- 本地代码改完不等于线上生效；生产后端需要部署并公网验证，小程序还需要用户在微信开发者工具上传体验版。

### 0.3.1 生产部署快速路径与已验证陷阱（2026-08-27）

- `docker-compose.yml` 中 `${APP_PORT}` 读取的是 Compose 启动环境或项目根目录的 `.env`，不会自动读取 `backend/.env`；如果 Nginx 固定代理 `8004`，部署前必须用 `docker compose port backend 8000` 和 `ss -ltnp` 核对端口，必要时显式执行 `sudo env APP_PORT=8004 docker compose up -d --no-build backend backend-worker archive-worker`。
- `backend/.env` 可能是 `root:root` 且权限为 `600`；不要为了部署修改权限或复制覆盖，Compose 需要读取时使用 `sudo`。
- 部署前先备份 Compose 和本次明确变更的源码到 `/home/ubuntu/teambuy-backups/<timestamp>/`，部署后依次验证容器状态、数据库健康、`127.0.0.1:<app-port>/health` 和公网 `/health`；出现公网 502 时优先检查端口映射，不要先改业务代码。
- `apt-get update` 或 Docker 构建卡住时不要反复等待和重启生产；先保留旧容器，再判断是否可以用已有生产镜像做范围明确的临时热修复，必须给旧镜像打回滚标签，并在网络恢复后补做正常构建。
- 使用 `scp` 上传多个文件时不能把目标写成同一个应用目录后依赖文件名推断路径，容易把文件平铺到错误位置；应逐个上传到明确的远端绝对路径，并在容器内用 `sha256sum` 核对。

## 1. 禁止操作

禁止批量删除文件或目录。

不要使用：

```text
del /s
rd /s
rmdir /s
Remove-Item -Recurse
rm -rf
```

需要删除文件时，只能一次删除一个明确路径的文件。

正确示例：

```text
Remove-Item "C:\path\to\file.txt"
```

如果需要批量删除文件，应停止操作，向用户确认后再继续。

## 2. 角色分工

### 2.1 架构师

架构师负责在开发前把需求拆清楚，形成可执行开发文档。

主要职责：

- 需求拆解
- 系统方案设计
- 企业微信客服接入方案
- 微信笔记聚合规则
- 卡片字段设计
- 接龙规则设计
- 浏览统计规则
- 数据结构设计
- 接口规则设计
- 风险识别
- 任务拆解
- 验收条件草案

架构师输出物建议保存到：

```text
docs/stage2-docs/
```

### 2.2 开发者 / Codex

开发者 / Codex 负责根据开发文档和测试清单实现功能。

主要职责：

- 按开发文档实现功能
- 按测试清单补齐细节
- 不随意扩大修改范围
- 修复 Bug 单中明确的问题
- 更新必要测试
- 运行基础验证命令
- 输出自测报告

开发者输出物建议保存到：

```text
docs/qa/功能名称_Codex自测报告.md
```

### 2.3 AI 测试官 / 验收官

AI 测试官负责把开发文档转化为测试清单、验收标准、Bug 单和回归清单。

主要职责：

- 开发前生成测试用例
- 识别 P0/P1/P2 风险
- 开发后按测试清单验收
- 输出 Bug 修复任务单
- 输出回归测试清单
- 输出上线前检查表
- 修复完成后复测和回归

AI 测试官默认不得直接修改业务代码。

测试官输出物建议保存到：

```text
docs/qa/功能名称_测试清单与验收标准.md
docs/qa/功能名称_验收报告.md
docs/qa/功能名称_复测与回归报告.md
```

### 2.4 用户 / 最终负责人

用户负责最终业务判断和上线确认。

AI 可以辅助测试和验收，但最终上线必须由用户人工确认。

页面图地址为："d:\Desktop\myprojects\teamBuy\docs\png"

## 3. 标准开发 / 测试 / 验收工作流

```text
架构师
  ↓
生成开发文档
  ↓
AI 测试官
  ↓
提前生成测试清单和验收标准
  ↓
开发者 / Codex
  ↓
按照开发文档和测试清单开发
  ↓
Codex 自测
  ↓
AI 测试官验收
  ↓
发现问题
  ↓
生成 Bug 修复任务单
  ↓
开发者 / Codex 修复
  ↓
AI 测试官复测和回归
  ↓
最终人工确认
```

## 4. 阶段 1：架构师生成开发文档

当进入阶段二时，架构师必须读取：

```text
stage1-thinking/05-stage2-input-brief.md
AGENTS.md
```

阶段二开发文档保存到：

```text
docs/stage2-docs/
```

至少包含：

- 产品规划
- 任务拆解
- 技术规格
- 验收标准
- 资源清单
- 数据结构
- Codex 启动任务说明

## 5. 阶段 2：AI 测试官提前生成测试清单

开发前必须优先生成测试清单。

测试清单必须覆盖：

- 企业微信客服回调
- sync_msg 消息拉取
- 微信笔记消息聚合
- 媒体下载和转存
- 导入成功和失败通知
- 首次导入后补绑定
- 卡片草稿生成
- 卡片编辑保存
- 卡片分享查看
- 电话拨号
- 字段复制
- 浏览 PV/UV
- 登录用户浏览列表
- 匿名浏览统计
- 实名接龙
- 手机号和地址附加字段
- 接龙名单管理
- 删除无效接龙
- 标记已跟进
- 素材库搜索分类
- 一键复用
- 数据隔离和回归测试

测试方案保存到：

```text
docs/qa/功能名称_测试清单与验收标准.md
```

## 6. 阶段 3：开发者 / Codex 根据文档和测试清单开发

开发者必须读取：

```text
AGENTS.md
docs/stage2-docs/
docs/qa/功能名称_测试清单与验收标准.md
```

开发要求：

- 优先满足所有 P0 测试项
- 不得只实现“看起来能跑”的版本
- 不得绕过企业微信导入主链路
- 不得把匿名用户展示为实名浏览用户
- 不得让复用卡片覆盖原卡片统计数据
- 不得把密钥、Token、AppSecret 写入前端或 Git
- 开发完成后必须运行已有测试命令
- 没有测试脚本时必须说明未覆盖项

自测报告保存到：

```text
docs/qa/功能名称_Codex自测报告.md
```

## 7. 阶段 4：AI 测试官验收

验收报告必须包含：

- 验收结论：通过 / 不通过 / 需要人工确认
- P0 测试项是否全部覆盖
- 已覆盖测试项
- 未覆盖测试项
- 发现的问题
- Bug 修复任务单
- 回归测试清单
- 上线前必须检查事项

P0 未全部覆盖时，不得给出“通过”。

## 8. 阶段 5：Bug 修复流程

AI 测试官发现问题后，不直接修改业务代码，必须输出 Bug 修复任务单。

Bug 修复任务单必须包含：

- Bug 标题
- 严重级别：P0 / P1 / P2
- 所属模块
- 复现步骤
- 实际结果
- 预期结果
- 可能原因
- 建议修复方向
- 开发 Codex 执行指令
- 修复后必须回归
- 验收标准

## 9. 阶段 6：开发者 / Codex 修复 Bug

开发者修复 Bug 时必须读取：

```text
AGENTS.md
docs/qa/功能名称_验收报告.md
```

修复要求：

- 只修复 Bug 单中列出的问题
- 不扩大修改范围
- 不破坏原有业务规则
- 补充或更新相关测试
- 运行相关测试命令
- 输出修复说明

修复报告保存到：

```text
docs/qa/功能名称_Bug修复报告.md
```

## 10. 阶段 7：AI 测试官复测和回归

复测报告必须包含：

- 原 Bug 是否解决
- 是否引入新问题
- 哪些模块已经回归
- 哪些模块仍未覆盖
- 是否还有 P0 风险
- 是否可以进入最终人工确认

复测报告保存到：

```text
docs/qa/功能名称_复测与回归报告.md
```

## 11. 阶段 8：最终人工确认

只有满足以下条件后，才可以进入最终人工确认：

- P0 测试项全部通过
- 企业微信导入链路可用
- 微信笔记聚合可用
- 卡片生成可用
- 分享查看可用
- 浏览统计可用
- 实名接龙可用
- 素材库复用可用
- 没有阻断性 Bug
- 开发者已输出自测报告
- AI 测试官已输出验收报告
- 如有 Bug，已完成复测和回归

最终是否上线，由用户人工确认。

## 12. P0 风险判定规则

以下情况必须判定为 P0：

- 企业微信回调无法接收
- sync_msg 无法拉取消息
- 微信笔记无法形成卡片草稿
- 图片或视频 media_id 未及时转存导致素材丢失
- 导入成功但用户无法认领
- 导入失败但没有通知用户
- 卡片无法保存
- 卡片无法分享
- 群用户无法打开卡片
- 电话拨号不可用
- 字段复制不可用
- 浏览统计严重错误
- 匿名用户被错误展示为实名浏览用户
- 接龙无法提交
- 接龙名单团长不可见
- 普通用户看到未脱敏昵称
- 删除无效接龙不可用
- 标记已跟进不可用
- 一键复用覆盖原卡片数据
- 密钥、Token、AppSecret 泄露到前端或 Git

P0 问题未解决前，不得进入上线确认。

## 13. 推荐目录结构

```text
项目根目录/
├─ AGENTS.md
├─ skills/
│  ├─ wecom-import-parser/
│  │  └─ SKILL.md
│  ├─ card-generation/
│  │  └─ SKILL.md
│  ├─ miniapp-flow/
│  │  └─ SKILL.md
│  └─ qa-acceptance/
│     └─ SKILL.md
├─ stage1-thinking/
│  ├─ 01-discussion-summary.md
│  ├─ 02-decision-log.md
│  ├─ 03-mvp-scope.md
│  ├─ 04-risk-list.md
│  ├─ 05-stage2-input-brief.md
│  └─ 06-project-skill.md
├─ docs/
│  ├─ stage2-docs/
│  └─ qa/
├─ miniprogram/
└─ backend/
```

## 14. 最终原则

架构师负责设计怎么做。

AI 测试官负责提前定义怎么验收。

开发者 / Codex 负责按照设计和验收标准实现。

AI 测试官负责发现问题、输出 Bug 单、复测和回归。

用户负责最终业务判断和上线确认。

测试不是开发完成后的临时动作，而是开发前就必须定义好的交付标准。

## 15. 分享静态图插件标准

资料、电子名片、合集以及后续可分享实体，统一使用：

- 生成时机：发布成功后或首次进入发送入口时准备分享快照；发送动作只读取已保存的静态图，不在 `onShareAppMessage` 中实时绘制。
- 版本一致性：分享图必须绑定资料 `revision`，或合集 `snapshotVersion + updatedAt`。版本不一致时禁止复用旧图，必须重新生成。
- 存储方式：分享图上传到后端媒体存储，快照元数据保存在实体上；不把客户联系方式等业务数据写入小程序本地存储。
- 更新规则：新图上传并保存成功后才切换为当前图；旧图从切换成功的 `retiredAt` 起保留 30 天，再由后台 worker 删除媒体和历史记录。
- 失败规则：新图生成、上传或版本保存失败时，不删除当前图、不发送错误旧图，并保留可重试状态。
- 安全规则：清理只允许删除本系统媒体地址，必须保护所有实体的当前图；资料和合集没有当前快照时不得静默退回头像、封面或通用默认图。
- 无图规则：没有真实封面时必须生成对应类型的信息视觉区；房源展示房屋信息图，商品展示商品信息图，服务展示服务信息图，普通资料展示文档信息图。不得用不相关 stock 图或伪造真实内容。
- 实现入口：前端复用 `miniprogram/plugins/share-snapshot/index.js`，后端复用分享快照接口、版本校验和清理任务，不要为每一种资料重复实现一套发送图生命周期。

### 15.1 分享链路对抗式审查门禁

每次修改分享、缩略图或原生发送链路，必须在代码完成后进行一次针对最终 `onShareAppMessage` 返回值的对抗式审查。只通过 `node --check`、JSON 解析、后端 pytest 或 Canvas 函数检查，不得判定发送链路完成。

- 必测反例：非名片、有真实主图、当前 `shareSnapshot` 缺失/过期/生成失败时，分别验证产品约定是“立即原图直出”还是“明确等待”；不能把“跳过 Canvas”误称为“已经直出”。
- 若采用原图直出，最终返回对象必须包含已登记且允许公开的原图 `imageUrl`，并指向当前资料详情页；不能返回资料库、首页或“准备中”页面作为分享兜底。
- 若无真实主图，必须明确验证是否允许无图原生分享；无图时不得伪造默认图，也不得让微信因缺少 `imageUrl` 而截取资料库页面。
- 必测正常/异常组合至少覆盖：名片、房源、商品、普通资料、图片资料、合集、无图、旧 revision、失败重试、快速连续点击、未发布和非当前用户。
- 每个场景都要检查四个结果：按钮状态、`onShareAppMessage` 是否允许触发、返回的 `imageUrl`、返回的 `path`。出现“无 `imageUrl` + 非目标页面 path”即为 P0。
- 交付报告必须区分“静态检查通过”“插件单测通过”“最终分享回调通过”“微信开发者工具人工验收通过”；未完成的层级不得写成“功能已完成”。

## 16. 登录按需触发与未登录会话存档

- 小程序首次打开必须允许用户浏览首页和公开内容，不能在 `App.onLaunch`、首页 `onShow` 或首屏数据加载前强制跳转登录。
- 只有用户主动新建/输入资料、上传媒体、保存/发布、查看个人工作台或执行需要账号归属的动作时，才触发登录；登录后必须回到用户刚才发起的目标动作。
- 登录页是业务动作的授权门槛，不是产品首屏；不得把未登录状态误判为首页加载失败，也不得用旧账号数据填充匿名首页。
- 企业微信会话存档与小程序登录解耦：未登录客户消息可以先由共享归档核心和项目业务库保存为 `unclaimed` 待认领资料；只有用户完成明确绑定/认领后，才把资料和企业微信身份映射到该用户的 `openid`。禁止仅凭昵称、手机号或文本相似度自动认领。
- 登录页必须同时提供可见的拒绝/退出路径：有页面栈时显示返回入口，并始终提供“暂不登录，先浏览首页”；拒绝授权只能进入公开首页，不能循环弹登录或回到原受保护动作。
- 头像资料入口必须使用直接可见的 `button open-type="chooseAvatar"`，点击区域要覆盖头像和操作文案；选择成功、取消或失败都要有可感知反馈，不能只放一个透明头像占位让用户猜测可点击。
