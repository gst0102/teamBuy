# 用户内容安全与违禁词配置 v1：Codex 自测报告

## 结论

后端文字规则、内容判定、审核队列、PC 管理入口和高暴露内容门禁已完成并部署生产。未上传小程序体验版，生产当前没有导入正式违禁词。

## 已覆盖

- 规则创建、更新、启停、有效期校验、作用域和重复规则校验。
- NFKC、大小写、全角、零宽字符、空白和标点归一化；原文不被替换。
- `block`、`review`、`warn` 的优先级和稳定错误码。
- 判定绑定内容指纹、内容 revision、规则版本；人工放行不会错误放行修改后的内容。
- 资料、合集、名片、商机、供需卡、微信群资源、活码、互助任务/提交/评论和公开读取门禁。
- PC 内容安全规则、试测、审核队列、放行/拦截和统计接口；普通用户响应不返回具体命中词。
- 互助评论/提交进入复核后的审核放行与拦截状态恢复。

## 已执行验证

```text
PYTHONPATH=.:backend ./.venv312/bin/pytest -q backend/tests
358 passed in 15.60s

专项 backend/tests/test_content_safety.py
4 passed

Python py_compile：通过
PC 内嵌 JavaScript 语法检查：通过
git diff --check：通过
应用路由导入：通过，318 routes
```

## 对抗式检查结果

- 已修复字段作用域 `user_note.body` 无法命中的问题。
- 已避免公开 allow 列表请求重复写审核记录。
- 已验证审核放行跨生命周期 revision 仍能生效，但内容指纹变化会重新审核。
- 已补上历史公开活码解析门禁，避免规则新增后旧活码继续导流。
- 已检查规则命中响应、队列摘要和审计详情，不向普通用户返回具体词、手机号、微信号或完整正文。

## 未覆盖与人工门禁

- 图片、二维码、OCR、外链语义和平台规则批量导入尚未实现。
- 未预置任何规则；正式规则来源、法务口径、申诉流程和留存期限仍需运营确认。
- 必须由用户在微信开发者工具完成人工验收；生产规则仍需管理员先试测、再人工确认后录入。
