# 互帮互助页面 Codex 自测报告

**日期：2026-08-31**

## 结论

页面代码和普通分包路由已完成静态自测；当前属于“页面交互实现”，不代表后端任务、积分流水、真实媒体上传和微信真机链路已经完成。

## 已验证

- 新增普通分包路由在 `miniprogram/app.json` 中声明，所有页面的 `.js/.json/.wxml/.wxss` 文件齐全。
- 新增 JavaScript 通过 `node --check`。
- `app.json` 通过 JSON 解析。
- WXML 标签结构检查通过。
- 内容块演示适配器可生成文字块、图片块，并正确计算普通任务执行者收益。
- `git diff --check` 通过。
- 未执行微信开发者工具上传、预览或体验版发布。

## 尚未覆盖

- 微信开发者工具真实编译、主包/分包大小和依赖分析。
- 真实小程序 `navigateToMiniProgram` 短链接跳转。
- 真实后端任务接口、图片媒体资产登记、积分流水和跨用户数据隔离。
- 真机状态栏、安全区、横屏和图片上传权限验收。

## 交付边界

当前页面使用 `subpackages/my-tools` 和 `subpackages/my-tools-mutual-help` 两个普通分包；本地演示数据存储在小程序本地缓存中，后续接入后端前不得把该适配器视为生产结算逻辑。

## 第二轮调整自测（2026-08-31）

- 首页顶栏已去掉重叠的两个模块标题；其他模块页移除右侧并列标题。
- 本地演示积分使用 v2 命名空间，旧的 0 分测试缓存不会覆盖首次 100 分。
- 四个页面均接入共享主题偏好，浅色 / 深色切换后重新进入其他页面仍保持选择。
- 首页任务大厅接入持久化 1x / 2x 列数选择，默认 1x；2x 使用双列网格。
- 普通任务分类更新，并在普通任务卡片显示实际分类文案。
- 新增静态回归：主题接线、导航无 `rightText`、1x/2x 样式标记、分类选项和 WXML 标签栈检查均通过。

## 第三轮调整自测（2026-08-31）

- 发布普通任务的任务内容和验收标准改为有序文字/图片内容段，模拟添加“文字 → 图片 → 文字”、编辑文字段和删除图片段均通过。
- 图片操作按钮统一压缩为横向紧凑按钮；积分卡拆分为余额主信息、排序提示和规则按钮，规则按钮固定小尺寸。
- 演示积分通过种子版本初始化为 100 分；模拟旧缓存首次读取为 100，显式保存 0 分后仍保留 0，避免把真实的 0 分误恢复。
- 相关 JavaScript、WXML 结构、内容块顺序、样式标记和 `git diff --check` 通过。
- 未执行微信开发者工具真实编译、真机图片选择、后端媒体上传、生产部署或体验版上传。

## 第四轮调整自测（2026-08-31）

- 发布页图片段缩略图使用 `aspectFit`；任务详情和提交材料图片均不再使用 `aspectFill` 裁切。
- 详情内容图片使用 `widthFix` 原比例展示；点击图片调用 `wx.previewImage`，图库包含任务内容图和验收图。
- 远程图片保存流程模拟“下载 → 保存到相册”通过；相册权限拒绝分支有设置引导。
- 相关 JavaScript、WXML 结构、图片预览/保存调用检查通过；未执行微信开发者工具真机验收。

## 第五轮调整自测（2026-08-31）

- 小程序任务详情增加卡片式目标入口，卡片与底部按钮复用同一跳转处理。
- 目标短链接仍只在点击时传给 `wx.navigateToMiniProgram`；页面不会把卡片点击直接当成任务完成或积分结算。
- 相关 JavaScript、WXML 结构、深色卡片样式和跳转入口检查通过；未执行真实微信设备跳转验收。

## 第六轮调整自测（2026-08-31）

- 积分卡“规则”按钮位于卡片右上角，字号和固定尺寸样式已检查；深色模式选择器存在对应覆盖。
- 发布成功后的跳转路径包含刚创建的任务 ID，任务详情包含 `open-type="share"` 按钮；`onShareAppMessage` 返回当前任务标题和当前任务详情路径。
- 目标小程序入口仍是详情页内卡片，未新增重复大厅卡片；点击目标卡和底部按钮共用 `wx.navigateToMiniProgram({ shortLink })`。
- 带 ID 的未知任务不再回退到首条演示任务，避免跨设备分享时静默串任务。
- `node --check`、JSON 解析、WXML 结构检查、分享回调 mock 和 `git diff --check` 均通过；未执行微信开发者工具真实分享、跨设备任务查询、目标小程序真机跳转或生产部署。

## 第七轮调整自测（2026-08-31）

- 顶部“任务”Tab 已接入发布者任务管理列表；新增发布者管理详情页，发布成功后的跳转已改为管理详情。
- 执行者详情已移除模块内 Tab；发布者预览执行者详情时只读，不能打开目标小程序或提交自己的任务。
- 任务大厅已过滤当前用户已提交、已结束和自己发布的任务；发布者管理页显示状态、进度、待验收空态和暂停/恢复入口。
- 主要卡片、表单字段、详情区块和管理进度区的分隔线已统一为 `2rpx`，深色模式覆盖边界颜色。
- `node --check`、JSON 解析、6 个 WXML 标签结构检查、路由/角色分流/过滤/样式静态检查和 `git diff --check` 通过。
- 未执行微信开发者工具真实编译、真机分享、跨设备任务查询、提交验收、目标小程序跳转或生产部署。

## 第八轮调整自测（2026-08-31）

- 任务分享已接入统一分享快照插件：发布者管理详情和执行者详情均生成固定 `750×600` JPG，并通过现有 `/api/uploads/share-snapshot` 上传。
- 任务分享图包含任务类型、标题、执行者奖励、剩余次数和截止信息；普通任务存在内容图片时使用图片优先布局。
- 两个页面均挂载 `shareCardCanvas`，分享按钮在 `shareCardReady` 前禁用；`onShareAppMessage` 通过任务分享构造器返回执行者详情 `path` 和真实 `imageUrl`，没有图片时返回 `null`。
- 任务快照保存了样式版本、源版本和指纹；同一设备本地任务可复用未过期快照，版本变化会重新生成。
- `node --check`、任务分享源/回调 mock、WXML 标签结构和 `git diff --check` 通过。
- 未执行微信开发者工具真实编译、真实聊天转发、跨设备任务查询、后端任务接口、目标小程序真机跳转或生产部署。

## 第九轮调整自测（2026-08-31）

- 任务管理列表卡片按分享卡的信息层级重排，卡片外部新增独立“分享任务”和“删除任务”操作区；详情页不再放日常分享按钮。
- 列表按任务 ID 读取任务自己的 `shareSnapshot`，分享按钮在专用 `imageUrl`准备完成前保持禁用，避免多任务列表串用最后一张分享图。
- 删除任务采用 `status=deleted` 软删除，任务大厅和管理列表隐藏；`getTasks()` 显式空数组不再重新注入演示任务。
- 发布者管理页和执行者页均可打开目标小程序；`isOwner` 只继续限制发布者提交/结算，不限制目标跳转。失败回调保留错误日志。
- 已通过互帮互助 `.js`、分享插件和分享渲染器 `node --check`，多任务分享回调按 ID 模拟、缺失快照阻断、空数组删除持久化和 WXML 结构检查。
- 未执行微信开发者工具真实编译、真实聊天转发、目标小程序真机跳转、后端任务接口或生产部署。

## 第十轮调整自测（2026-08-31）

- 任务管理列表优先使用任务专用 5:4 分享图作为封面；原始长图不再作为固定矮框的首选，详情页继续 `widthFix`、原生放大滑动和保存相册。
- 首页移除重复的“我的工具”眉标题，并增加大厅/任务 Tab 职责提示；保留大厅排除当前用户自己发布任务的角色规则。
- 普通任务提交记录改为任务级聚合，发布者管理详情展示实际待验收材料并可通过验收；小程序任务明确提交完成后即时结算；普通任务提交超过 5 天可自动通过。
- 结算 mock 覆盖普通任务人工验收、小程序任务即时结算、普通任务 5 天自动通过、重复结算幂等和余额不足保护；相关 JavaScript、JSON、WXML 结构及 `git diff --check` 通过。
- 未执行微信开发者工具真实编译、双设备任务共享、目标小程序真机跳转、生产后端积分流水、图片域名白名单或体验版上传。

## 第十一轮调整自测：羊毛任务与评论（2026-08-31）

- 发布页新增羊毛任务类型和查看策略：免费查看/付积分解锁、固定完成奖励（允许 0）和可选打赏；羊毛仍复用普通任务的内容段、验收标准、次数和入口。
- 执行者详情增加解锁卡、评论摘要、值得做/一般/不建议、评论举报、失效退款申请和完成后打赏；未解锁时不展示完整任务内容、入口、验收标准和提交按钮。
- 普通任务与羊毛任务共用评论区；发布者管理详情展示评论及待处理举报/退款数量；首页新增羊毛筛选，自己的未删除任务即使暂停也保留展示。
- wool.js mock 已验证一次性解锁不重复扣分、积分转移、评论种子合并、重复举报去重、打赏转移和退款申请去重；分享源能输出羊毛专用事实。
- node --check、JSON 解析、WXML 结构检查和 git diff --check 通过；未执行微信开发者工具真实编译、后端接口、PC 审核、跨设备分享或生产部署。

## 第十二轮调整自测：羊毛改为非验收型福利（2026-08-31）

- 羊毛发布页改为“标题 + 羊毛分类 + 羊毛说明/入口 + 查看方式 + 可选打赏”，移除验收标准和固定完成奖励；普通任务仍保留独立验收标准。
- 羊毛详情改为“摘要/评论 → 免费查看或一次性解锁 → 福利说明/使用入口 → 可选打赏”，不再生成提交材料、发布者验收、5 天自动通过或固定完成奖励。
- 旧羊毛数据即使带有 `acceptanceCriteriaBlocks`、`completionRewardPoints` 或旧奖励字段，也会在读取和装饰时按新规则忽略；普通任务和小程序任务的既有结算流程保持不变。
- 羊毛可选次数按成功查看/解锁名额处理：免费羊毛首次进入、付费羊毛首次解锁各消耗一次，同一用户重复进入/解锁保持幂等。
- 已通过任务分享源、发布校验、羊毛解锁/名额/积分流水、羊毛无提交结算保护、普通任务验收保留、JavaScript 语法、JSON 解析、WXML 标签结构和 `git diff --check` 检查。
- 未执行微信开发者工具真实编译、真实账号/双设备、目标小程序跳转、PC 审核、服务端任务接口或生产部署。

## 第十三轮调整自测：互助积分充值与 PC 工作台（2026-09-01）

- 后端增加互助积分账户、积分流水、充值订单和活动统计事件；首次建立账户发放 100 分，充值通过服务端订单和支付回调入账。
- 小程序本地增加充值页和套餐选择；充值默认开启/可见，提现默认关闭/隐藏，未来保留 300 分的提现规则只作为说明。
- PC 业务工作台增加“互助积分”子 Tab，展示今日、近 7 日和总量的发布任务、完成任务、充值笔数，并提供充值/提现的启用与显示开关。
- `./.venv312/bin/pytest -q backend/tests`：317 passed；专项测试 2 passed；Python 编译、相关 JS 语法、JSON 解析、页面内嵌 JS 语法和 `git diff --check` 通过。
- 生产已部署并验证：API、两个 Worker、PostgreSQL healthy；本机 8004 和公网 `/health` 正常；互助四张表存在；管理 GET/PUT 返回预期开关；固定活码仍为 200、无 `Location` 且内层二维码图片可访问。
- 未创建生产测试充值订单，未调用生产 test-confirm；小程序体验版仍由用户在微信开发者工具手动上传，任务真实结算和跨设备互助任务仍未迁移为服务端账本。
