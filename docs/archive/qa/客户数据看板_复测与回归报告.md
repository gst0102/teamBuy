# 客户数据看板复测与回归报告

更新时间：2026-06-21

## 1. 复测结论

**需要人工确认**

原 P0 问题“生产后端未部署经营看板接口”已解决。生产 `/api/dashboard/business` 不再是路由级 404，已用服务器演示用户写入真实测试数据并验证经营看板汇总、展示页事件和订单数据。

仍需人工确认的是：用户需要在微信开发者工具上传包含最新小程序代码的体验版，然后真机确认“访客线索 -> 经营看板”页面视觉、Tab 切换和跳转。

## 2. 原 Bug 复测

| Bug | 原问题 | 当前结果 | 结论 |
| --- | --- | --- | --- |
| Bug-01 | 生产 `/api/dashboard/business` 路由级 404，真机经营看板空面板 | 已部署生产后端；`user_test` 返回业务级“用户不存在”；演示用户返回经营看板数据 | 已解决 |

## 3. 线上回归结果

| 回归项 | 结果 |
| --- | --- |
| 生产健康接口 `/health` | 通过，返回 200 |
| 经营看板接口 `/api/dashboard/business?ownerUserId=user_test` | 通过，返回业务级“用户不存在” |
| 服务器演示用户经营看板 | 通过，打开 2、访客 2、看资料 1、咨询 2、订单 1 |
| 展示页列表和 analytics | 通过，1 个已发布展示页，5 条真实展示页事件 |
| 商家订单列表 | 通过，1 条待处理接龙 |
| 后端容器状态 | 通过，`teambuy-backend-1` 正常运行 |

## 4. 演示数据说明

本轮演示数据不是前端 mock。

它通过服务器接口写入真实后端数据，归属于独立演示用户 `user_836a4a8986`，包含：

- 4 条资料。
- 1 个已发布展示页。
- 5 条展示页行为事件。
- 留资、预约和商品接龙动作。
- 1 条待处理接龙订单。

这组数据用于验证经营看板真实聚合口径，不会写入用户真实账号。

## 5. 仍未覆盖

- 小程序体验版真机视觉。
- 经营看板四个 Tab 真机切换。
- 详情页按钮跳转到展示页、待联系、商家订单和客户库。
- 真实客户访问展示页后的线上数据变化。

## 6. 是否还有 P0 风险

当前没有已知后端 P0 阻断风险。

上线前仍必须由用户完成小程序体验版上传和真机确认；未确认前，验收结论保持“需要人工确认”。
