# 当前项目 验收报告 m1

报告日期：2026-06-09  
角色：AI 测试官 / 验收官  
验收对象：teamBuy / 资料整理助手 v0.1 MVP 当前开发结果  
验收依据：

- `AGENTS.md`
- `docs/stage2-docs/`
- `docs/qa/MVP_测试清单与验收标准.md`
- `docs/qa/阶段三MVP_Codex自测报告.md`
- `docs/qa/悦享互动宝_v0.1_UI产品化改版_Codex自测报告.md`
- `docs/qa/企业微信真实联调记录.md`

## 1. 验收结论

验收结论：不通过

说明：

- 本地 mock 链路、后端自动化测试、小程序静态检查已通过，说明当前代码具备较完整的本地演示和开发验证基础。
- 但 v0.1 P0 主链路要求真实企业微信客服回调、`sync_msg`、媒体下载转存、小程序登录、分享查看、拨号、复制、实名接龙等关键能力全部可验收。
- 当前真实 `sync_msg` 仍返回 `48002 api forbidden`，真实企业微信导入主链路未跑通。
- 小程序尚未在微信开发者工具或真机完成系统性人工验收，电话拨号、剪贴板复制、原生分享、tabBar 视觉和页面交互仍不能判定通过。
- P0 未全部覆盖时，按照项目规则不得给出“通过”。

当前可判定：

- 本地后端自动化回归：通过。
- 小程序静态语法和配置检查：通过。
- 本地 mock MVP 闭环：部分通过，仍需小程序人工验收补证。
- 真实企业微信导入闭环：不通过。
- 上线状态：不得进入最终上线确认。

## 2. 本次实际执行的回归验证

| 验证项 | 命令 / 方法 | 结果 |
|---|---|---|
| 后端单元与 API 回归 | `cd backend && pytest` | 50 passed |
| 后端编译检查 | `cd backend && python -m compileall app` | 通过 |
| 小程序 JS 语法检查 | `node --check` 扫描 `miniprogram/**/*.js` | 通过 |
| 小程序 JSON 解析检查 | 解析 `miniprogram/**/*.json` | 通过 |
| 密钥关键词扫描 | 扫描 `backend`、`miniprogram`、`docs`，排除 `docs/png` 和 Markdown 正文 | 仅命中 `.env.example` 占位值与后端环境变量读取代码，未发现真实密钥硬编码 |
| Git 状态检查 | `git status --short --branch` | `main...origin/main [ahead 6]`，仅 `docs/png/` 未跟踪 |

## 3. P0 测试项覆盖检查

| P0 项 | 覆盖状态 | 证据 | 验收判断 |
|---|---|---|---|
| 企业微信客服回调 GET | 已覆盖 | 真实 HTTPS 健康检查记录、后端测试 `test_wecom_callback_get_verify` | 通过 |
| 企业微信客服回调 POST | 部分覆盖 | 后端 mock/JSON/后台 real-sync 入队测试已覆盖；真实 XML 字段映射仍需账号实测 | 需要人工确认 |
| `sync_msg` 消息拉取 | 未通过 | 真实联调返回 `48002 api forbidden` | 不通过 |
| 微信笔记消息聚合 | 已覆盖 | 聚合测试、mock 导入、60 秒窗口测试 | 通过 |
| 媒体下载和转存 | 部分覆盖 | mock/local/object storage 单测与失败重试队列已覆盖；真实企业微信 media_id 端到端未通过 | 需要人工确认 |
| 导入成功通知 | 已覆盖 | mock 通知抽象和查询接口、自测报告记录 | 通过 |
| 导入失败通知 | 部分覆盖 | 抽象已实现；真实失败通知需企业微信链路实测 | 需要人工确认 |
| 待认领导入 | 已覆盖 | 后端测试和小程序发给客服页入口 | 通过 |
| 小程序登录 | 部分覆盖 | 当前为 mock 登录，真实微信 code 换 openid 未接入 | 不通过 |
| 卡片草稿生成 | 已覆盖 | 后端测试覆盖微信笔记、链接、手动创建草稿 | 通过 |
| 卡片编辑保存 | 部分覆盖 | API 与页面已实现，后端测试覆盖发布流；小程序页面人工验收未完成 | 需要人工确认 |
| 卡片分享查看 | 部分覆盖 | 查看页与 `onShareAppMessage` 存在；真实群分享打开未验收 | 需要人工确认 |
| 电话拨号 | 部分覆盖 | 小程序代码存在 `wx.makePhoneCall`；未在开发者工具/真机确认 | 需要人工确认 |
| 字段复制 | 部分覆盖 | 小程序代码存在 `wx.setClipboardData`；未在开发者工具/真机确认 | 需要人工确认 |
| 登录浏览用户统计 | 已覆盖 | `test_anonymous_and_logged_in_view_stats_are_isolated` | 通过 |
| 匿名浏览统计 | 已覆盖 | 匿名 PV 与登录用户列表隔离测试已覆盖 | 通过 |
| 实名接龙 | 部分覆盖 | 后端接龙和必填校验已覆盖；小程序提交流程未人工验收 | 需要人工确认 |
| 手机号和地址附加字段 | 部分覆盖 | 编辑页配置项、后端必填校验部分覆盖；真机表单未验收 | 需要人工确认 |
| 接龙名单管理 | 部分覆盖 | 后端可查、组件可展示；小程序管理页未人工验收 | 需要人工确认 |
| 删除无效接龙 | 已覆盖 | `test_owner_can_delete_and_follow_relay` | 通过 |
| 标记已跟进 | 已覆盖 | `test_owner_can_delete_and_follow_relay` | 通过 |
| 素材库搜索分类 | 已覆盖 | 后端分类接口测试、小程序前端聚合筛选静态检查通过 | 通过 |
| 一键复用 | 已覆盖 | `test_duplicate_card_keeps_stats_isolated` | 通过 |
| 数据隔离和回归测试 | 部分覆盖 | 浏览、接龙、复用隔离有测试；真实多用户小程序环境未验收 | 需要人工确认 |
| 密钥、Token、AppSecret 不泄露 | 已覆盖 | 本次关键词扫描未发现真实密钥硬编码 | 通过 |

P0 覆盖结论：P0 未全部覆盖，且存在真实 `sync_msg` 阻断项。因此本轮验收不通过。

## 4. 未覆盖或异常项

| 编号 | 模块 | 问题 | 影响 | 优先级 |
|---|---|---|---|---|
| GAP-001 | 企业微信真实导入 | 真实 `sync_msg` 返回 `48002 api forbidden` | 微信笔记、链接、图片、视频无法通过真实企业微信客服形成卡片草稿 | P0 |
| GAP-002 | 小程序登录 | 当前仍是 mock 登录，未接入真实微信 code 换 openid | 真实用户身份、实名接龙、登录浏览用户统计无法上线闭环 | P0 |
| GAP-003 | 媒体转存 | 真实企业微信 media_id 下载和对象存储端到端未验收 | 图片/视频素材存在真实环境丢失风险 | P0 |
| GAP-004 | 小程序人工验收 | 微信开发者工具/真机未完成系统验收 | 拨号、复制、分享、tabBar、表单交互无法判定通过 | P0 |
| GAP-005 | 分享统计 | 当前分享入口是原生分享和占位提示，未接入分享次数统计 | 不阻断 v0.1 核心，但不可宣传分享数据能力 | P2 |
| GAP-006 | 访问记录时间维度 | “今天 / 近7天 / 近30天”为前端聚合展示，后端无真实分时段统计接口 | 不可作为真实分日统计验收依据 | P1 |
| GAP-007 | 标签管理 | 当前为轻量新增/删除/筛选，不支持重命名、排序、批量打标 | 符合 v0.1 边界，不作为阻断 | P2 |

## 5. Bug 修复任务单

### BUG-P0-001：真实 `sync_msg` 返回 48002，企业微信导入主链路不可用

| 字段 | 内容 |
|---|---|
| 严重级别 | P0 |
| 所属模块 | 企业微信导入 |
| 复现步骤 | 1. 在线上服务器配置 `WECOM_USE_MOCK=false`。2. 调用 `POST /api/wecom/real-sync?max_pages=1`。3. 查看企业微信接口返回。 |
| 实际结果 | 企业微信返回 `errcode=48002`、`errmsg=api forbidden`，来源 IP 为 `81.70.84.35`。 |
| 预期结果 | `sync_msg` 能成功拉取客服消息，并进入 normalizer -> 幂等过滤 -> 媒体转存 -> 导入批次和卡片草稿生成链路。 |
| 可能原因 | 企业微信后台权限或配置未满足：Secret 非微信客服 Secret、未开启 API 管理微信客服帐号、客服账号未授权 API 管理、可信 IP 未包含服务器 IP、企业主体/API 权限不足。 |
| 建议修复方向 | 先按企业微信后台配置逐项排查，不优先改代码；确认配置无误后，用真实文本消息、图片消息、链接消息分别验证。必要时在后端记录企业微信请求参数摘要和响应日志，避免泄露密钥。 |
| 开发 Codex 执行指令 | 请基于 `docs/qa/企业微信真实联调记录.md` 和 `docs/qa/企业微信客服配置清单.md` 排查 `48002 api forbidden`。不要改动非企业微信导入相关逻辑。若确认需要代码增强，只补充配置检查、错误日志和联调脚本，并更新测试。 |
| 修复后必须回归 | TC-001、TC-002、TC-003、TC-004、TC-007、TC-008、TC-009、TC-013、TC-014、TC-015 |
| 验收标准 | 真实企业微信发送文本、图片、链接后，`/api/wecom/real-sync` 成功，`/api/imports/pending` 出现可认领导入，失败场景有通知记录。 |

### BUG-P0-002：小程序仍为 mock 登录，真实微信登录未形成上线闭环

| 字段 | 内容 |
|---|---|
| 严重级别 | P0 |
| 所属模块 | 小程序登录 / 用户身份 |
| 复现步骤 | 1. 打开 `miniprogram/pages/login/index`。2. 查看登录实现和页面文案。3. 执行登录。 |
| 实际结果 | 当前阶段使用 mock 登录，未执行微信 `wx.login` code 换 openid。 |
| 预期结果 | 上线环境中应通过微信登录建立稳定用户身份，浏览用户、实名接龙、导入认领、卡片归属均绑定真实 openid。 |
| 可能原因 | v0.1 本地开发阶段保留 mock 登录，真实小程序 AppID 和后端登录凭证尚未补齐。 |
| 建议修复方向 | 保留本地 mock 开关，但新增真实微信登录接口和环境配置；上线模式禁用 mock 登录。 |
| 开发 Codex 执行指令 | 请实现小程序真实微信登录链路：前端调用 `wx.login`，后端用 AppID/Secret 换取 openid 并建立用户。真实密钥只能放环境变量，不得写入前端或 Git。保留 mock 登录用于本地开发，并补测试。 |
| 修复后必须回归 | TC-015、TC-016、TC-017、TC-023、TC-026、TC-029、TC-030、TC-038、TC-039 |
| 验收标准 | 微信开发者工具或真机登录后，用户 openid 可稳定绑定；接龙必须登录；登录浏览用户只展示真实登录用户。 |

### BUG-P0-003：真实 media_id 下载与对象存储端到端未验收

| 字段 | 内容 |
|---|---|
| 严重级别 | P0 |
| 所属模块 | 媒体下载和转存 |
| 复现步骤 | 1. 通过真实企业微信客服发送图片或视频。2. 触发 `sync_msg`。3. 查看生成卡片媒体 URL 和 `/api/wecom/media-retries`。 |
| 实际结果 | 当前因真实 `sync_msg` 阻塞，真实 media_id 下载和对象存储上传未完成端到端验证。 |
| 预期结果 | 图片/视频 media_id 应及时下载并转存为持久 URL；失败时写入补偿队列且不导致导入崩溃。 |
| 可能原因 | 真实企业微信权限未通、对象存储线上配置未完成或未验证。 |
| 建议修复方向 | 在 `sync_msg` 修复后，优先用图片、视频消息验证本地 `/media` 或 COS/S3 存储模式；确认失败重试可恢复。 |
| 开发 Codex 执行指令 | 请在真实 `sync_msg` 可用后，针对图片和视频 media_id 完成端到端联调。若对象存储未配置，先明确使用 local 存储验收，再切 COS/S3。补充媒体失败重试验收记录。 |
| 修复后必须回归 | TC-007、TC-008、TC-009、TC-013、TC-014 |
| 验收标准 | 真实图片和视频导入后，卡片封面和媒体列表使用持久 URL；失败媒体进入 retry 队列，重试成功后可复用转存结果。 |

### BUG-P0-004：小程序关键交互缺少开发者工具/真机验收记录

| 字段 | 内容 |
|---|---|
| 严重级别 | P0 |
| 所属模块 | 小程序端验收 |
| 复现步骤 | 1. 用微信开发者工具打开 `miniprogram/`。2. 依次操作登录、tabBar、发给客服、资源库、手动添加、编辑发布、查看、拨号、复制、接龙、管理、一键复用。3. 记录结果。 |
| 实际结果 | 自测报告明确列出“仍需人工验收”，本轮未提供开发者工具或真机验收记录。 |
| 预期结果 | 所有 P0 小程序交互均有可复核的人工验收记录，包含失败项和截图/录屏说明。 |
| 可能原因 | 当前主要完成静态检查和后端回归，尚未进入小程序运行环境验收。 |
| 建议修复方向 | 开发 Codex 或测试官使用微信开发者工具完成全链路人工验收；发现 UI/交互问题后再开具体 Bug 单。 |
| 开发 Codex 执行指令 | 请启动微信开发者工具或由人工测试执行小程序验收，按回归清单逐项记录结果。只修复验收中发现的明确问题，不扩大功能范围。 |
| 修复后必须回归 | TC-015 至 TC-024、TC-026 至 TC-037 |
| 验收标准 | 电话拨号、字段复制、分享打开、实名接龙、接龙管理、一键复用在开发者工具或真机均有通过记录。 |

## 6. 回归测试清单

### 6.1 企业微信导入回归

| 编号 | 回归项 | 必须条件 |
|---|---|---|
| REG-WECOM-001 | 回调 GET 验证 | 线上 HTTPS callback URL 返回企业微信要求响应 |
| REG-WECOM-002 | 回调 POST 接收 | 真实事件可快速返回并创建后台同步任务 |
| REG-WECOM-003 | `sync_msg` 拉取 | 文本消息真实拉取成功 |
| REG-WECOM-004 | 微信笔记聚合 | 同一用户 60 秒窗口消息聚合为一个导入批次 |
| REG-WECOM-005 | 图片/视频转存 | media_id 转为持久 URL，失败写入 retry |
| REG-WECOM-006 | 导入通知 | 成功和失败都有客服通知记录 |
| REG-WECOM-007 | 待认领 | 导入成功后小程序可认领 |

### 6.2 小程序主链路回归

| 编号 | 回归项 | 必须条件 |
|---|---|---|
| REG-MP-001 | 登录 | mock / 真实登录模式明确，线上使用真实登录 |
| REG-MP-002 | tabBar | 首页、资源库、发给客服、访问记录、我的可切换 |
| REG-MP-003 | 手动添加资源 | 创建草稿后进入编辑页 |
| REG-MP-004 | 标签管理 | 新增、删除、筛选、创建和编辑页保存标签 |
| REG-MP-005 | 卡片编辑发布 | 字段保存成功，发布后可打开查看页 |
| REG-MP-006 | 卡片查看 | 封面、详情、来源、统计、接龙区展示正确 |
| REG-MP-007 | 电话拨号 | 调用 `wx.makePhoneCall` 成功或有明确失败提示 |
| REG-MP-008 | 字段复制 | 调用 `wx.setClipboardData` 成功 |
| REG-MP-009 | 分享查看 | 群分享路径可打开对应卡片 |

### 6.3 浏览统计和接龙回归

| 编号 | 回归项 | 必须条件 |
|---|---|---|
| REG-STATS-001 | 登录用户浏览 | 登录用户进入浏览用户列表 |
| REG-STATS-002 | 匿名浏览 | 匿名只计入 PV/UV，不展示身份 |
| REG-RELAY-001 | 未登录接龙限制 | 未登录用户不能提交接龙 |
| REG-RELAY-002 | 登录接龙 | 登录用户可提交实名接龙 |
| REG-RELAY-003 | 手机号/地址必填 | 团长开启后校验生效 |
| REG-RELAY-004 | 普通用户脱敏 | 普通用户看到脱敏昵称，不看到手机号和地址 |
| REG-RELAY-005 | 团长名单管理 | 团长可看完整名单、删除无效、标记已跟进 |

### 6.4 复用和数据隔离回归

| 编号 | 回归项 | 必须条件 |
|---|---|---|
| REG-REUSE-001 | 一键复用生成新卡 | 新卡 ID 不同，字段继承 |
| REG-REUSE-002 | 统计隔离 | 新卡不继承旧卡浏览和接龙数据 |
| REG-REUSE-003 | 原卡不受影响 | 复用失败不得修改原卡 |
| REG-SEC-001 | 密钥扫描 | 前端和 Git 不包含真实密钥 |
| REG-SEC-002 | 范围控制 | 无支付、收款、订单、分账入口 |

## 7. 上线前必须检查事项

| 检查项 | 当前状态 | 是否阻断上线 |
|---|---|---|
| 企业微信客服 API 管理模式已开启 | 未确认 | 是 |
| 企业微信 callback URL 已配置并真实通过 | GET 公网已通过，POST 真实事件待确认 | 是 |
| `sync_msg` 真实拉取通过 | 当前 `48002 api forbidden` | 是 |
| 真实 media_id 下载和转存通过 | 未完成 | 是 |
| 小程序真实微信登录通过 | 未完成 | 是 |
| 小程序开发者工具/真机完整验收 | 未完成 | 是 |
| 电话拨号和字段复制真机可用 | 未完成 | 是 |
| 分享到微信群并打开卡片 | 未完成 | 是 |
| 浏览统计区分登录和匿名 | 自动化通过，需真机补验 | 是 |
| 实名接龙和名单管理 | 后端通过，需真机补验 | 是 |
| 一键复用数据隔离 | 自动化通过 | 否 |
| 对象存储线上配置 | 未确认 | 是 |
| 线上环境变量配置完整 | 部分已配置，需复核 | 是 |
| 前端和 Git 无真实密钥 | 本轮扫描未发现真实密钥 | 否 |
| 无支付、订单、分账等越界功能 | 文档和当前范围符合 | 否 |
| 开发者自测报告 | 已有 | 否 |
| AI 测试官验收报告 | 本文件 | 否 |
| 用户最终人工确认 | 未完成 | 是 |

## 8. 结论与下一步

本轮验收不能进入上线确认。建议下一步顺序：

1. 优先修复企业微信 `sync_msg` 的 `48002 api forbidden` 权限问题，打通真实导入主链路。
2. 接入或明确排期真实微信登录；上线模式不得只依赖 mock 登录。
3. 在真实 `sync_msg` 可用后，立即补测图片、视频 media_id 下载和转存。
4. 使用微信开发者工具或真机完成小程序 P0 人工验收，并形成补充验收记录。
5. 完成以上阻断项后，再由 AI 测试官执行复测与回归，输出复测报告。
