# teamBuy 阶段性交接归档

## 2026-09-08 最新交接：互帮互助任务公众号文章入口修复（本地完成）

### 本轮完成

- 任务入口识别 `mp.weixin.qq.com` 公众号文章，执行者详情和发布者管理详情优先调用 `wx.openOfficialAccountArticle`。
- 官方打开能力不可用或失败时复制链接；小程序短链、自有 H5 和外部普通网页继续沿用各自的原生跳转/复制规则。

### 验证与真实边界

- 公众号文章、自有 H5、京东网页、小程序短链的入口解析断言通过。
- 全量小程序 JS 语法检查和 JSON 解析通过；后端相关回归测试 19 passed。
- 尚未上传微信体验版，暂未完成真机公众号文章跳转和失败兜底验收。

### 下一步

1. 在微信开发者工具重新编译并上传体验版。
2. 真机验证公众号文章成功打开、API 不可用/失败时复制、普通外链复制、小程序短链原生跳转。

## 2026-09-08 最新交接：普通任务分享与详情流程修复（本地完成）

### 本轮完成

- 修复普通任务分享图：后端现在只把可读取的托管媒体作为任务主图；外链/临时/失效图片自动走无图视觉区，不再阻断分享快照。
- 发布者管理详情调整为“任务入口 → 任务说明 → 完成标准 → 参与记录/验收 → 任务评论”；入口不再位于长内容底部。
- 新增后端接口回归用例，覆盖“公众号外链图片 + 公众号任务入口”场景。

### 验证与真实边界

- `.venv313/bin/python -m pytest -q backend/tests/test_share_card_renderer.py backend/tests/test_mutual_help_points.py backend/tests/test_my_tools_cross_device_and_gates.py`：28 passed。
- 全量小程序 JS `node --check`、84 个小程序 JSON 解析、Python 编译与 `git diff --check` 通过。
- 本轮没有部署生产，也没有上传微信体验版；后端修复需部署后才会影响线上分享图，小程序详情顺序需重新编译上传体验版。
- 公众号网页因当前未配置为 web-view 业务域名，仍提供复制到浏览器；若要内嵌跳转，需要微信后台配置并进行真机验收。

### 下一步

1. 部署后重新打开任务管理页，确认原“分享暂时失败”的普通任务变为可分享；旧失败状态需要重新准备一次分享图。
2. 上传小程序体验版，确认发布者管理详情顺序和“任务入口”位置。
3. 在真机分别验收公众号文章、淘宝/京东/拼多多网页：可内嵌的进入 web-view，不可内嵌的显示复制到浏览器。

## 2026-09-01 最新交接：活码方案设计与群资源 Tab 数据问题确认

### 已确认问题

- 当前“找群”和“我的发布”内容一样是代码层面的真实问题：`miniprogram/pages/group-resource-library/index.js` 的 `loadState()` 将同一份 `decorated` 同时赋给 `groups` 与 `myGroups`，Tab 只改变显示模式，没有改变数据来源。
- 当前群列表还通过用户隔离的 `wx.setStorageSync` 读取，实际上不存在跨用户公共群资源池；因此不能仅在前端把同一数组过滤两次后宣称“找群”和“我的发布”已经分离。

### 本轮设计产物

- 活码三状态效果图已生成并在会话中展示：无活码时直接“上传群二维码”，上传页生成固定入口，已有活码时提供保存、更新和剩余 2 天提醒。
- 已按“上传二维码 → 系统校验 → 直接生成活码”简化创建流程，不要求用户填写名称；名称只作为系统元数据自动生成，不能成为发布前的额外步骤。
- “找群 / 我的发布”统一双屏效果图已生成并在会话中展示：找群面向公共有效资源发现，我的发布面向个人资源管理，两个 Tab 不共用同一份展示数据。
- [我的活码上传创建与到期更新流程 v2](https://www.figma.com/board/dbIw1eYe5fA0TALU44v84G) 已更新，覆盖首次上传、二维码校验、固定入口使用、到期提醒和原链接不变的更新闭环。

### 下一轮实现边界

- 群资源库必须拆成公共有效资源查询与我的发布查询；本地无服务端公共数据时，“找群”应展示空态，不能复制“我的发布”内容。
- “找群”只展示可公开查看的有效群资源，支持搜索/筛选和查看二维码扣 30 分；“我的发布”展示当前用户自己的有效、待确认、已过期记录，支持状态管理、更新二维码和删除，不显示消费动作。
- 活码每条记录必须有 owner、固定入口 token、当前二维码媒体、版本和 expiresAt；更新只替换当前二维码并保留固定入口，不删除后重建。
- 到期提醒由服务端定时任务按 `expiresAt - 2 天` 幂等发送；用户必须主动授权，通知只引导用户上传新二维码。
- 本轮没有修改代码、没有部署生产、没有上传微信体验版；效果图和流程图只是设计确认稿。

## 2026-09-01 最新交接：微信群工具改为群资源库三 Tab 主页面

### 本轮已完成

- “我的”页的“微信群工具”现在直接进入群资源库主页面，不再经过“群资源库 / 我的活码”中转页。
- 群资源库主页面新增同级 Tab：“找群 / 我的发布 / 我的活码”。默认进入“找群”，只展示当前未过期的群资源；“我的发布”保留用户自己的已过期资源，便于后续更新。
- 群资源列表增加“当前有效”数量和“有效至”日期；过期群不再显示查看二维码按钮，避免用户误以为仍可消费积分查看。
- “我的活码”先提供真实的页面层级、绿色免费工具视觉和诚实空态；创建按钮暂时提示功能准备中，没有伪造用户端活码接口。
- 原 `/subpackages/my-tools/index/index` 路径保留兼容，打开后跳转到新的群资源库主页面，旧中转页不再作为标准入口。

### 验证与真实状态

- 已通过全量小程序 JavaScript `node --check`、小程序 JSON 解析、目标页面结构检查和 `git diff --check`。
- 本轮只改小程序入口、群资源库本地页面表现和兼容路由；没有部署生产、没有修改生产后端/数据库/`.env`、没有创建生产测试订单、没有调用 `test-confirm`、没有上传微信体验版。
- 群资源库仍是按用户隔离的 `wx.setStorageSync` 本地 MVP；“找群”尚未具备跨用户公共资源数据，正式开放前必须迁移到服务端实体、重复校验、审核和统一积分账本。
- “我的活码”仍未接入用户所有权、二维码上传/更新、固定入口和服务到期订阅消息；当前页面只是信息架构和视觉预览。

### 用户人工验收

1. 在微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存并重新编译。
2. 从“我的 → 微信群工具”确认直接看到“群资源库”，顶部有“找群 / 我的发布 / 我的活码”三个同级 Tab。
3. 在“找群”确认标题为“当前有效群”、卡片显示“有效至”；切到“我的发布”确认自己的已过期资源仍可见但不能查看二维码。
4. 点击“我的活码”确认页面清爽、使用绿色工具视觉，点击“创建活码”只显示“活码功能正在准备中”，不把它当作已开放功能。

## 2026-09-01 最新交接：微信群工具入口与群资源库规则本地预览

### 本轮已完成

- “我的”页的“微信群工具”宫格现在进入 `/subpackages/my-tools/index/index`，不再显示“即将上线”；互帮互助仍保持直接进入自己的首页。
- 微信群工具复合页现在展示“群资源库”和“我的活码”两个核心入口；群资源库可以进入旧页面，我的活码只作为视觉入口保留，尚未接用户端服务。
- 群资源库用户文案已改为“每天最多发布 1 个、累计最多获得 100 积分、更新不重复奖励、自然到期不扣分”；本地预览增加每日发布和累计奖励上限判断。
- 新工具页采用暖米白、陶土橙与低饱和绿色的轻量视觉，标签只表达“积分”和“免费”，不展示“已上线”等运营状态。

### 验证与真实状态

- 已通过全量小程序 JavaScript `node --check`、79 个 JSON 解析、工具入口路由检查和 `git diff --check`。
- 本轮没有部署生产、没有修改后端/数据库/生产 `.env`、没有创建生产测试订单、没有调用 `test-confirm`、没有上传微信体验版。
- 群资源库仍是本地 `wx.setStorageSync` MVP；每日限制和 100 分累计上限只是本地预览防线，正式开放前必须接入服务端群资源实体、统一积分账本、重复资源校验和幂等流水。
- 活码用户端还未开放：现有生产 live-qr 能力主要是 PC 管理和公开固定入口，后续需要新增用户所有权、上传/更新、下载和到期订阅提醒链路。

### 用户人工验收

1. 在微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存并重新编译。
2. 从“我的”点击“微信群工具”，确认进入“群资源与活码”页面，看到两个入口且没有“已上线/即将上线”状态。
3. 点击“群资源库”，确认旧群资源页面可以正常打开；当天第二次点击添加时应提示每天最多发布 1 个群资源。
4. 不要把当前活码灰色入口当作功能已上线；待服务端用户接口完成后再做上传二维码、下载和剩余 2 天订阅消息验收。

## 2026-09-01 最新交接：积分核心与互帮互助订阅模板已部署生产

### 生产状态

- 本次已将积分核心、互助积分充值兼容改造、互帮互助订阅模板配置读取和授权用途隔离部署到生产；代码基线为本地 `a605373`，生产 API/Worker/归档 Worker 使用同一批热修复源码。
- 当前镜像：`teambuy-backend:20260901-073641-mutual-points-subscription`、`teambuy-backend-worker:20260901-073641-mutual-points-subscription`、`teambuy-archive-worker:20260901-073641-mutual-points-subscription`，Compose 的 `latest` 已同步到这三组镜像。
- 备份目录：`/home/ubuntu/teambuy-backups/20260901-073641-mutual-points-subscription/`；旧 API、两个 Worker 镜像保留 `rollback-20260901-073641` 标签。
- API、后台 Worker、归档 Worker、PostgreSQL 均 healthy；生产端口仍为 `8004 -> 8000`，本机和公网 `/health`、数据库健康检查通过。
- 生产 `.env` 仅追加互帮互助模板 ID、字段映射和任务管理跳转页；原客户提醒模板未替换，未创建测试充值订单，也未调用生产 `test-confirm`。

### 已验证与未完成

- 已验证容器加载 `PointsCoreService`、互帮互助订阅模板配置和新路由；PostgreSQL 已补齐积分核心的账户类型、幂等键、来源字段和索引。
- 小程序前端没有通过服务器部署生效，用户仍需在微信开发者工具重新编译、真机预览并手动上传体验版；Codex 未自动上传。
- 互帮互助任务、评论、羊毛解锁、打赏、提交验收和发布者/执行者正式跨用户结算仍未迁移到服务端；订阅模板暂未连接真实任务事件发送。

### 下一步建议

1. 先由用户上传体验版，验收“我的”入口、充值页面和现有生产支付配置；不要用生产 `test-confirm` 代替真实支付。
2. 实现服务端公共任务引擎，统一接入 `PointsCoreService` 的发布、解锁、提交、验收、退款和打赏幂等事务。
3. 再把小程序互帮互助从本地演示适配器切换到服务端 API，并在真实任务事件稳定后接入两类订阅消息发送。

### 真实风险

- 当前积分核心在应用层仍依赖进程锁和仓储保存，尚未证明多进程/多副本并发安全。
- 生产充值已具备后端接口，但互帮互助完整任务账本和订阅消息发送仍不可按“已上线”对外承诺。

## 2026-09-01 最新交接：任务短链接报错、发布后分享与页面分享配置（本地完成）

### 本轮已完成

- 发布页三个任务类型统一处理短链接：用户只粘贴并点击“确认发布”时，自动把有效输入加入 `taskLinks`；无效格式、类型不匹配、重复和超量仍会阻止发布并提示原因。
- 输入框下方增加常驻“案例链接”提示，说明从右上角“…”复制后粘贴，再点击“添加入口”；不再依赖 placeholder 传达关键步骤。
- 任务管理列表和发布者管理详情的分享按钮已改为微信绿；发布成功直达当前任务管理详情，任务专用分享图 ready 后显示“分享任务到微信群”。执行者任务详情也支持右上角分享和页面内分享按钮。
- 当前实际编译路由中，已有资料/名片/合集/商机/回应包分享回调的页面均补齐 `enableShareAppMessage`；首页、客户雷达、我的工具和互帮互助首页补充安全产品分享回调。客户隐私详情、编辑、登录、支付等页面不开放没有明确公开目标的泛分享。

### 本轮验证与边界

- 已通过全量小程序 JavaScript `node --check`、79 个 JSON 解析、短链接自动收纳/校验 smoke、活动路由 callback/config 一致性检查和 `git diff --check`。
- 未部署生产、未上传微信体验版、未创建生产测试充值订单、未调用 `test-confirm`；生产 API、worker、PostgreSQL、环境变量、secrets 和媒体目录均未修改。
- 当前互帮互助任务与积分仍主要是本地演示数据。分享图上传、好友跨设备查询、目标小程序真机跳转和正式服务端积分账本仍需后续接入/验收。

### 下一步人工验收

1. 在微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存并重新编译。
2. 分别创建小程序任务、羊毛、普通任务：只粘贴链接不点击“添加入口”直接确认发布，确认三类都能正常处理；发布成功后确认管理详情出现微信绿分享按钮。
3. 从任务管理卡片、发布者管理详情、执行者任务详情分别发一条新消息；检查右上角“…”可分享/复制链接，且真实聊天卡的图片是任务分享图、path 指向任务详情。
4. 检查首页、资料、雷达、我的、我的工具和互帮互助首页的右上角分享入口；资料/名片/合集仍需确认快照 ready 后再分享。

## 2026-09-01 最新交接：互帮互助文案收口、分享卡修正与自助完成

### 本轮已完成

- 清理互帮互助首页、详情、管理、发布、充值和“我的”中的内部解释性话术；删除任务大厅下方“默认按发布者当前积分从高到低”“大厅也会展示我发布的任务……”等说明，并移除首页重复的排序提示。
- 分享任务源不再把 `#小程序://...` / 网页地址直接当作卡片正文；按任务类型生成自然摘要，统一营销语为“互助赚积分，让别人帮你完成”，底部为“点开任务，完成后赚积分”。
- 无图信息分享卡把营销语上移到标题区；互帮互助任务使用独立 `mutual_task_share_v2` 版本强制旧任务图重新生成，资料/名片/合集继续使用生产兼容的 `share_card_v10`，互帮互助仍只使用暖米白、陶土橙和焦糖色分支。
- 大厅“积分优先”读取发布者当前互助积分；自己的未删除任务继续显示，并允许发布者进入详情按执行流程自行完成。

### 本轮验证与边界

- 已通过互帮互助 JavaScript、分享插件和渲染器 `node --check`，小程序 JSON 解析，分享源 raw short-link 过滤 smoke，最终 `onShareAppMessage` 的正常/旧快照/结束任务阻断复核，当前积分排序/自助完成行为 smoke，目标文案检索和 `git diff --check`。
- 本轮未部署生产、未上传微信体验版、未创建生产充值订单、未调用 test-confirm；生产充值和 PC 统计保持上一轮已部署状态。
- 当前任务、评论、羊毛和结算仍主要是本地演示；自助完成的正式服务端账本规则仍需人工确认。用户需在微信开发者工具清缓存、重新编译后，验收三处页面及最终聊天卡片。

## 2026-09-01 最新交接：互帮互助三处营销提示暖色统一

### 本轮已完成

- 互帮互助任务大厅积分卡下方新增暖色营销提示区，文案为“互助赚积分，让别人帮你完成”，并补充“完成别人的任务，赚取积分，发布自己的需求”。
- 互帮互助任务详情新增同款提示区；普通/小程序任务增加 `+N 分` 奖励主信息，羊毛仍按解锁/福利语义展示。
- 任务专用分享源增加同一营销语；分享渲染器仅对 `templateKind=mutual_task` 使用暖米白、陶土橙、焦糖色调色板，同时覆盖无图信息卡和有图信息卡，默认资料/名片/合集模板不变。
- 只改本地小程序页面、任务分享和通用分享渲染器；没有修改后端、数据库、生产配置、生产镜像或微信体验版。

### 本轮验证

- 互帮互助目录全部 JavaScript、分享插件和分享渲染器 `node --check` 通过。
- 小程序全部 JSON 解析通过；任务分享源 smoke 通过，确认营销语和 `+4 分` 事实字段存在；目标文件无尾随空格，`git diff --check` 通过。

### 当前真实边界与人工验收

- 当前互帮互助任务仍是本地演示数据，尚未接入服务端跨用户任务、正式积分结算、评论/羊毛账本和任务分享 token。
- 生产充值/统计链路保持上一轮已部署状态；本轮没有部署，也没有创建生产测试充值订单或调用 test-confirm。
- 用户下一步需在微信开发者工具打开正确的 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram` 工程，清缓存、重新编译并手动预览/上传，重点检查任务大厅营销区、任务详情奖励/营销层级，以及最终微信分享卡是否为暖色且仍带正确 `imageUrl` 与任务详情 `path`。

## 2026-08-31 最新交接：互帮互助任务多入口本地实现

### 本轮已完成

- 发布页新增统一任务入口编辑区，兼容历史小程序 `shortLink`；普通任务可以挂小程序链接和 HTTPS 网页链接。
- 执行者详情、发布者管理详情支持三种入口行为：小程序原生跳转；已配置业务域名的网页进入 `web-view`；其他 HTTPS 复制后使用手机浏览器访问。
- 任务大厅和任务管理卡片展示入口摘要；分享源同步识别入口类型，分享回调继续强制绑定任务专用 `imageUrl` 与执行者详情 `path`。
- 目标小程序打开权限对所有用户保留，发布者/执行者差异只用于提交、验收和结算动作。

### 本轮验证

- 互帮互助 JS `node --check`、JSON/路由、WXML 结构、任务入口解析、分享回调反例和 `git diff --check` 已通过。
- 未部署生产，未上传微信体验版；未修改活码 200 HTML、AScript 人数检测关闭配置或生产数据。

### 当前真实边界与人工验收

- 当前任务和分享快照仍是本地演示数据；好友跨设备打开分享链接、任务大厅跨用户可见、后端积分流水和媒体权限尚未接入。
- `web-view` 当前只放行 `teambuy.lifelove.top`。第三方站点需逐个完成微信业务域名配置、HTTPS 与真机验收；未配置外链应验证手机浏览器复制兜底。
- 需在微信真机验证：普通任务添加小程序/网页入口、两类详情打开行为、返回后提交材料、分享卡 `imageUrl`、任务详情路由以及多任务连续分享不会串图。

## 2026-08-31 最新交接：首页积分卡与任务大厅范围修正

### 本轮完成

- 首页积分卡右侧操作区固定放置“规则”和“首页排序”，余额和说明固定在左侧，避免两个控件跑到卡片中间。
- 任务大厅默认改为 2x 双列，布局键升级为 v2；用户仍可切换 1x。
- 取消大厅对当前用户自己发布任务的过滤。自己的任务会正常出现在大厅，可自行点击完成；完成后按提交状态从当前可执行列表移除，发布者管理 Tab仍保留管理记录。

### 本轮验证与边界

- 已通过小程序 JS/JSON/WXML、布局默认值和自己的任务可见逻辑校验；本轮不部署生产、不上传体验版。
- 当前任务仍是本地演示数据；本轮不改变后端跨用户任务查询、积分账本、分享快照和任务结算边界。

## 2026-08-28 最新：分享与列表链路收口完成本地实现，待人工验收

### 本轮已完成

- 资料库和合集的“最近”排序均把 `latestShareAt` 纳入活动时间；发送成功后当前页面立即重排，资料/合集缓存也同步更新，列表中间发送的卡片会到第一位。
- 资料创建、图片入库、编辑、发布、复制、删除和分享快照保存成功后统一失效资料库元数据缓存；后台刷新增加 owner generation，旧响应不会重新写回缓存。
- 合集编辑页同时清理旧缓存键和 `teambuy_showcases_v2` 环境隔离键，避免发布后继续显示旧合集。
- 统一分享插件取消隐式首页回退；没有明确目标 path 或目标为资料库路径时拒绝分享，最终分享仍要求合法 HTTPS/JPG 快照和详情页路径。运行代码检索未发现 `universal-share`、`resource_default`、`original_media` 旧分享分支。

### 本轮验证边界

- 通过：全部小程序 JS `node --check`、全部 JSON 解析、Python 编译、`git diff --check`；缓存失效、旧刷新响应 generation、资料库/合集活动排序、分享 path/imageUrl 门禁 smoke 通过。
- 当前机器没有 pytest 和 Docker 命令，因此本轮没有重新运行后端 pytest，也没有部署；不能把本轮表述为生产已生效。
- 未覆盖：微信开发者工具真实 `onShareAppMessage` 产物、手机聊天气泡最终图片、生产接口和新旧体验包版本一致性。

### 下一步

1. 如需线上生效，按 `docs/deploy/tencent-cloud-real-sync.md` 先备份、核对生产容器版本和端口，再部署本地后端变更；本轮未自动执行。
2. 在微信开发者工具打开正确的 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram` 工程，清缓存、重新编译并上传体验版。
3. 按人工矩阵测试旧资料重发、新资料、图片/无图、名片、合集、从列表中间发送后置顶、返回页面和失败重试；以最终聊天卡片而不是按钮文案判定。

## 2026-08-28 最新：名片与合集模板统一迁移已完成本地实现

### 当前状态

- 名片和合集统一沿用 `share_card_v10`，模板 revision 已进入 fingerprint；旧资料和旧合集不会继续复用旧模板 JPG，重新进入发送入口时惰性生成当前模板。
- 合集首图按可见资料顺序取第一张有效公开图片；owner、编辑、列表和公开展示链路使用同一来源规则；无图使用固定信息模板。
- 后端 owner/public 合集 payload 已补齐媒体首图和分享图 fingerprint；资料详情页、合集详情页在指纹匹配前不会开放原生分享。

### 验证与发布边界

- 已通过本轮 Node 语法检查、Python 编译检查、`git diff --check` 和分享模板 smoke；此前本地后端定向回归为 `184 passed`。
- 当前窗口没有 `backend/.venv313`、pytest 或 Docker 命令，因此本轮最后两处后端/详情页门禁补丁未能重新运行 pytest；不能把它们表述为本轮全量测试通过。
- 本轮未部署生产，也未上传微信体验版。部署后必须在开发者工具清缓存、重新编译上传，使用旧资料重发、新建有图/无图资料、名片和有图/无图合集做人工验收。

## 2026-08-28 14:31 最新：生产后端已切换分享图 v10

- 生产 API、后台 worker、归档 worker 已切换到 `share_card_v10`，三个容器内版本文件哈希一致：`d4b33d68a5b631f23615202027b1d8970a2a76af8db521f37e531ad9fa095090`。
- 部署采用已有镜像热修复，没有触发 `apt-get update` 或 Docker 构建；备份目录为 `/home/ubuntu/teambuy-backups/20260828T063111Z-share-v10`，旧镜像回滚标签为 `rollback-20260828T063111Z`。
- API 继续使用 `8004`，PostgreSQL、`/health`、`/health/db` 以及公网健康检查均通过；未改生产 `.env`、密钥、媒体目录和数据库卷。
- 后端已生效不代表聊天卡片立即变化；必须在微信开发者工具清缓存、重新编译上传当前 v10 前端后，再发送新消息。历史消息不会改变。

## 2026-08-28 最新：分享图 v10 图片优先版式已完成本地实现

- 分享插件已从 `share_card_v9` 升级为 `share_card_v10`；非名片有真实主图时走 `image_first` 大图版式，无主图走 `info_first` 固定信息版式，电子名片保留 `identity_first`。
- 有图版式固定输出 750×600（5:4）JPG，主图占卡片主体；图片资料保留完整画面，房源/商品/服务/链接/合集使用大图封面，所有标题、摘要、字段仍受固定行数和宽度截断保护。
- 后端 `SHARE_SNAPSHOT_STYLE_ID` 已同步为 v10，客户端构建标识为 `20260828-share-card-v10`；旧 v9 快照会自动过期，历史微信消息和历史数据库记录保留。
- 本地已移除旧的小图渲染函数；本轮尚未部署生产、尚未上传微信体验版。待本地测试和对抗式审查通过后，部署后端并在开发者工具重新编译上传，再用新建有图、旧资料重发、无图和名片各发一条新消息验收。

## 2026-08-28 最新：有图优先固定模板与名片重生成修复

- 当前小程序分享链路仍使用 `share_card_v9`；非名片有主图时会将真实主图嵌入固定 750×600（5:4）JPG 模板，无主图才使用类型图标信息卡，名片继续使用 600×480 专用模板。
- `miniprogram/plugins/share-snapshot/index.js` 现在从资料 cover、结构化 cover、内容图片和链接预览图中选择主图；`miniprogram/utils/business-card-share.js` 负责固定图片区域、`aspectFill` 裁切和 JPG 导出。
- 合集列表/编辑页已经把第一条资料图或 banner 传入模板；名片页的分享 key 已包含 fingerprint，旧名片不需要重建，会在重新进入页面时重新准备当前快照。
- 本地验证已通过：小程序 JS/JSON、分享模型、Canvas JPG、分享对抗式 smoke，以及后端分享/媒体定向测试 `6 passed`。系统 Python 和旧 `.venv` 不能运行 pytest，使用 `.venv312` 成功完成测试。
- 后端之前已部署 `/api/uploads/share-snapshot` 和 `share_card_v9`，本轮没有重新部署；前端仍需用户在微信开发者工具重新编译上传，再发送有图资料、无图资料、名片三条新消息验收。

## 2026-08-28 12:39 最新部署：生产已切换分享 JPG 与 v9 快照后端

- 已按热修复策略部署 API、后台 worker、归档 worker；新镜像标签为 `share-v9-20260828T043920Z`，旧镜像回滚标签为 `rollback-20260828T043920Z`。
- 容器内确认 `/api/uploads/share-snapshot`、`process_share_image`、`share_card_v9` 均存在，实际图片处理返回 `image/jpeg` 和 `.jpg`；8004、本机/公网健康检查和数据库连接通过。
- 本次只部署后端，没有自动上传小程序。用户需在微信开发者工具清缓存、重新编译当前工程；进入资料库点击“重试分享图”，成功后再新发一条资料消息验证。
- 部署备份：`/home/ubuntu/teambuy-backups/20260828T043920Z-share-v9`。未改 `.env`、密钥、媒体目录和数据库卷。

## 2026-08-28 11:57 最新排查：新前端全部进入“重试分享图”是生产后端未同步

- 真机截图中的“重试分享图”表示统一 v9 分享图准备失败；为了阻止微信再次截取资料列表，新链路会在没有 ready JPG 时隐藏“发客户”。
- 线上 `teambuy-backend-1` 仍是 2026-08-27 21:17 创建的旧镜像，容器中没有 `/api/uploads/share-snapshot`、`process_share_image` 和 `share_card_v9`。健康检查正常不能证明分享上传链路已经部署。
- count-only 雷达后端已经生效，所以雷达变快与分享图失败是两个独立结果。下一步先部署后端，再清缓存、重新编译并上传小程序，最后新发消息验收 HTTPS JPG。

## 2026-08-28 最新实现：分享链路全量迁移到固定模板 JPG（待部署）

- 当前本地 `share_card_v9` 已取消所有非名片原图直出；资料/图片资料/链接/房源/商品/服务方案/合集统一生成 750×600 固定模板，电子名片生成 600×480 专用模板，标题/摘要/字段均有固定行数和宽度截断。
- Canvas 导出为 JPG，前端调用 `/api/uploads/share-snapshot`，后端 `process_share_image()` 强制输出 `image/jpeg`。普通 `/api/uploads/asset` 仍负责业务图片并保持 WebP，不要把两条接口混用。
- 已清除运行代码中的 `original_media`、`shareDirect`、note-edit 原图兜底；旧 `universal-share` 运行模块已不存在。所有原生分享回调均要求当前快照 ready，否则关闭分享并返回 `null`，不再返回资料库截图路径。
- 已进一步删除名片 `share_card_v4`、图片资料专用旧快照判断、`resource_default` 旧别名和未使用的封面传参；合集公开接口命中旧缓存快照时会清空旧分享图字段，运行代码没有旧分享分支。
- 本地验证：后端定向 4 passed，全量 301 passed；69 个小程序 JSON、全部小程序 JS、分享 smoke、`git diff --check` 通过。
- 本轮没有部署生产、没有上传体验版。下一步按 `/Users/yiyi/Desktop/Desktop/myprojects/cloud_tencent/cloud_tencent.md` 和 `docs/deploy/tencent-cloud-real-sync.md` 先备份并部署后端新路由/图片处理，再在微信开发者工具清缓存、重新编译上传，重点确认新快照 URL 为 HTTPS JPG 且聊天卡不再显示资料库列表。

## 2026-08-28 06:28 最新排查：旧代码已清理，但分享图存在 WebP 兼容风险

- 6:27 生产事件对应 `note_11a9ec9b67`，当前快照是 `share_card_v8 / ready`，实际图片为 600×480 WebP；截图却显示资料库列表，说明问题已不是 v7 旧快照或第二份 library 页面。
- 当前运行源码没有 `universal-share`、旧列表兜底或重复 `pages/library/index`；下一步应为分享快照单独输出 PNG/JPG，并部署后端后用全新资料、全新消息重新验证。
- 本轮未修改后端、未部署、未上传体验版。不要继续删除页面；先修复分享图媒体格式，再核对最终 `imageUrl` 和聊天卡片。

## 2026-08-28 06:01 最新修复：分享图改为 5:4 固定长卡并淘汰 v7

- 生产 06:02（北京时间）的“雪花肥牛”记录来自 `library_send_customer`，当前快照是 `share_card_v7 / ready`、750×460；公网可访问但在微信 5:4 展示框里右侧被裁切，故截图中的服务卡变形是比例问题。
- 小程序无图信息卡现统一导出 750×600，标题/摘要最多两行，超长文本按真实绘制宽度加 `...`，内容区有裁剪保护；样式版本为 `share_card_v8`，旧 v7 快照会自动判定 stale 并重新生成。
- 当前源码的资料库分享回调未再使用 `/pages/library/index` 作为分享目标，旧提示文案也不存在；无图或快照未就绪时入口保持禁用/隐藏，避免微信用列表页截图。
- 本轮未修改后端、未部署、未上传体验版。客户端构建标识为 `20260828-share-card-v8`。下一步在微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存并重新编译；首次点击旧资料的“准备分享图”后，等待按钮变为“发客户”，再新发一条服务和一条普通资料消息验证。

## 2026-08-28 05:50 最新复核：后端分享快照已是新版本

- 生产 05:44、05:51 的 `note_6dee30f9ad` 分享事件均已写入；“雪花肥牛”当前是 `share_card_v7 / ready`，生成时间 05:07，图片 URL 公网 200，内容为新固定模板。
- “你是”资料也为 `share_card_v7 / ready`，当前图片同样是新模板；05:50 附近没有新的快照保存请求，因为客户端复用了已就绪 v7 快照。
- 截图下方旧资料列表卡不是生产当前 v7 图片；当前源码和 `HEAD` 中也不存在“资料分享图准备中，请稍后再试”这句旧文案。可能性收敛为历史聊天卡片、实际体验包不是当前工程，或某个最终分享回调未使用该资料的 v7 `imageUrl`。
- 下一步人工取证只需新建一条全新资料/分享消息，并确认开发者工具的 `onShareAppMessage` 返回对象含本站 HTTPS `imageUrl` 和 `/pages/note-preview/index` 目标路径；不要再重复部署后端。

## 2026-08-28 05:44 最新复核：旧分享文案不在当前代码

- 用户 05:44 截图仍显示“资料分享图准备中，请稍后再试”，但该完整文案在当前小程序运行源码、`HEAD` 和后端源码中均无匹配；当前资料库回调已改为无图返回 `null`，提示文案也不同。
- 当前最可能是旧体验包、微信开发者工具打开了另一份工程，或聊天中仍包含之前发送的历史失败卡片。工程 AppID 应为 `wxf43f7bc098d9858b`，构建标识应为 `20260828-share-radar-v2`。
- 生产后端 count-only 已部署；本次没有再次修改代码或服务器。下一步必须由用户在正确工程中清缓存、重新编译并上传，再用新包测试；若新包仍出现旧完整文案，再从上传记录和客户端请求头继续定位。

## 2026-08-28 05:14 最新部署：生产已切换 count-only 雷达后端

- 生产 API、`backend-worker`、`archive-worker` 已使用同一批 hotfix 镜像；部署文件为 `backend/app/core/schema.sql`、`backend/app/models/domain.py`、`backend/app/services/repository.py`、`backend/app/services/app_service.py`。
- 生产数据库已创建 `customer_radar_summaries`，当前没有投影行，首次有效摘要请求会懒生成；部署后第一次冷请求不保证立即很快，后续命中投影才是 count-only 快路径。
- 生产 `/health` 和 `/health/db` 均通过，8004 映射保持正常；旧镜像回滚标签为 `rollback-20260827T211415Z`。
- 下一步人工验收：微信开发者工具清缓存并重新编译上传 `20260828-share-radar-v2`，分别测试资料/合集/供需等有图、无图分享，以及雷达首次冷启动和第二次热命中。若第二次仍慢，再继续做投影预热/回填，不再把问题归因于前端缓存。

## 2026-08-28 最新清理：旧通用分享链路已全量迁移并删除

- 10 个供需/商机/回应包页面已直接依赖 `miniprogram/plugins/share-snapshot/index.js` 的 `prepareShareCardImage` / `buildShareCardMessage`。
- 旧 `miniprogram/utils/universal-share.js` 已删除；运行代码不再引用 `universalShare*`、`universalShareCanvas` 或 `.universal-share-canvas`。
- 通用分享图未就绪时原生分享入口被隐藏；最终回调必须返回合法 `imageUrl` 和当前目标路径。异步 generation 保护已加入，避免旧页面数据覆盖新分享图。
- 本轮只做本地代码清理与验证，未部署生产、未上传体验版。下一步人工验收需重新编译体验版，覆盖供需/商机/回应包的有图、无图、快速连续进入/退出和生成失败重试。

## 2026-08-28 最新复核：4:05 测试仍命中旧体验版，生产雷达后端也未切换

- 生产 `teambuy-backend-1` 仍运行旧 `app_service.py`：生产哈希 `f0dc2a30c4026e46c0d0014907f874f4f878cf452550c842d29338227ca5d1b7`，本地最新哈希 `b2f828fada4754f3fc5bb93cf80ee94ca87d30fa7ad5a1a32ba216169ec9c2`；生产容器没有 `CustomerRadarSummary/customer_radar_summaries`，所以四个数字仍走旧的历史数据重算。
- 4:05 截图的“资料分享图准备中，请稍后再试”加资料库截图对应旧前端回调/旧体验包，不是当前本地 `HEAD` 的客户详情路径。已将 `miniprogram/app.js` 的构建标识更新为 `20260828-share-radar-v2`，重新编译上传后可通过请求头确认。
- 当前本地分享链路仍保留 `share-snapshot`（资料/合集/名片）和 `universal-share`（供需/商机等活跃页面）；本轮没有安全证据证明它们是死代码，不删除。旧根页面也仍有路由或兼容引用，需单独做依赖图清理。
- 本轮只完成版本核对和构建标识更新；未部署生产、未上传体验版。下一步必须先部署后端计数投影，再由用户重新编译上传标识为 `20260828-share-radar-v2` 的体验版，然后分别测试无图资料、真实图片资料和雷达冷/热首屏。

## 2026-08-28 最新排查：分享封面回退与雷达摘要冷启动瓶颈（本轮未开发）

- 分享截图已定位到资料库最终回调的无图分支：`miniprogram/pages/library/index.js` 在分享图未就绪时返回 `/pages/library/index`，同时省略 `imageUrl`；截图中的“资料分享图准备中，请稍后再试”与该分支文案一致。
- 当前链路是微信原生 `open-type="share"`，但分享封面必须由应用生成/登记并通过 `imageUrl` 返回。无图继续放行会让微信使用资料库页面截图；普通文字最容易触发，其他无主图业务卡、合集及通用供需/商机页面也存在同类风险。
- 雷达客户端已经有短时内存摘要缓存；冷启动仍慢的主要原因是服务端摘要读取历史事件并执行三次客户画像投影，不是单纯页面渲染或缓存问题。下一轮应做 count-only/预计算摘要，并收紧所有分享回调的就绪契约。
- 生产只读核对：`teambuy-backend-1`、两个 worker 均为 Python 3.12.14；宿主机为 Python 3.10.12。生产 `app_service.py` 哈希 `f0dc2a30c4026e46c0d0014907f874f4f878cf452550c842d29338227ca5d1b7`，与本地不同；生产容器未包含本地最新批量事件读取方法，因此本地性能优化尚未完整部署。
- 本轮没有修改业务代码、没有部署、没有上传体验版。下一步需先实现并测试分享回调/摘要优化，再按 `docs/deploy/tencent-cloud-real-sync.md` 第 0 节的“备份、回滚标签、范围明确热修复或显式网络构建”策略部署后端，并由用户重新编译上传体验版。

## 2026-08-27 最新交接：客户雷达状态回退已定位并补齐前端竞态保护

- 生产北京时间 11:33 的跟进动作均返回 200；数据库状态已经持久化，实时客户雷达投影返回 `following=2、abandoned=7`，没有发现后端回滚。
- 根因是小程序雷达旧读取请求晚到后覆盖乐观结果；本地已补“操作开始即提升请求代际并清缓存、晚到响应丢弃、成功后后台强制权威校准、连续操作按代际保护回滚”。
- 客户端构建标识为 `20260827-radar-consistency-1`，通过 `X-TeamBuy-Client-Build` 传递；`1.4.8` 只是 contactPlugin 版本，不是小程序包版本。
- 已通过：小程序 JS `node --check`、后端编译、`git diff --check`、销售 SCRM 测试 17 passed。
- 下一步必须在微信开发者工具重新编译并上传体验版，再做连续放弃/恢复、停留等待、返回雷达、冷启动四项人工回归；上传前继续测试旧体验包没有意义。

## 2026-08-25 最新交接：资料页复制绑定消息显示修复

- 用户真机反馈资料页只显示引导步骤，没有绑定消息和复制按钮。
- 根因是资料页成功拿到 `/api/auth/wecom-bind-intent` 返回值后把 `assistantBindMessage` 清空；已改为使用 `data.bindMessage`。
- 首页和资料页现在都能显示当前账号的完整绑定消息；后端无需再次部署。
- 需要重新编译上传体验版后复测资料页“资料助手”入口。

## 2026-08-25 最新交接：绑定消息改为完整自然语言请求（本地，待体验版上传）

- 复制内容不再是单独的 `绑定资料助手 TB-XXXXXX`，改为一段完整消息：说明刚添加资料助手、请求绑定、绑定后图片/文件/链接会自动进入小程序资料库。
- 后端仍只从消息中的 `TB-XXXXXX` 识别绑定意图，现有会话存档绑定逻辑不变。
- 首页和资料库按钮已统一为“复制绑定消息”；文案不再要求点击动态卡片。
- 已完成 pytest 和 JS 静态检查；下一步仍需上传体验版，在真实企业微信会话中验证整段消息被归档并完成绑定。

## 2026-08-25 最新交接：资料助手绑定回到复制动态口令方案（本地，待体验版上传）

- 用户确认停止折腾动态小程序欢迎卡片，采用最早的稳定方案：添加企业微信后复制当前用户专属绑定口令，再发送给资料助手。
- 首页和资料库现在调用 `/api/auth/wecom-bind-intent`，弹层显示 `绑定资料助手 TB-XXXXXX`，提供“复制绑定口令”按钮。
- 会话存档中的口令识别和绑定逻辑继续复用现有实现；没有修改 `/api/wecom/archive/callback`、生产环境变量或其他项目配置。
- 已完成本地脚本检查、绑定口令/归档绑定测试和 `git diff --check`；相关测试通过。
- 下一步：用户在微信开发者工具重新编译上传体验版，用一个尚未绑定的账号添加企业微信资料助手，复制口令并发送；确认归档收到口令后再发送一条资料，检查是否进入该账号资料库。

## 2026-08-25 最新交接：微信客服回调凭证命名已与会话存档隔离

- 客服 / 外部联系人回调使用 `WECOM_KF_CALLBACK_TOKEN`、`WECOM_KF_ENCODING_AES_KEY`，客服 API Secret 使用 `WECOM_KF_SECRET`。
- 会话存档回调只使用 `WECOM_ARCHIVE_CALLBACK_TOKEN`、`WECOM_ARCHIVE_ENCODING_AES_KEY`，不再复用客服变量。
- 本地企业微信相关测试 54 passed；生产已写入新变量并完成备份，备份目录为 `/home/ubuntu/teamBuy/.deploy-backups/wecom-kf-callback-20260825011733/`。
- API 与同步 worker 已重启，健康检查 200；公网加密回调验证返回 200，解密响应匹配；企业微信后台已保存并配置可信 IP。
- 生产客服 Secret 已迁移到 `WECOM_KF_SECRET`，容器内获取 `access_token` 成功。下一步用当前 `WECOM_OPEN_KFID` 对应客服账号发送一条文字，再发送图片或链接，验证回调、`sync_msg` 和资料入库；第二个客服账号尚未纳入当前单账号同步配置。

## 2026-08-24 最新交接：客户详情定向接口已部署，前端待体验版上传

- 新增后端 `GET /api/scrm/customer-detail`；客户详情页不再从整套雷达聚合结果中自行反查客户，而是读取服务端返回的单条 `customer/timeline/lead`。
- 服务端复用现有客户信息链开关、会员门禁和 owner/requester 权限边界；支付关闭直接读取，支付开启非会员进入会员页，支付开启会员直接查看详情。
- `user:xxx`/`anon:xxx`/原始 ID 在服务端统一匹配；`leadId` 必须与当前 `customerId` 同一客户且属于当前 owner，避免详情串线。
- 本地验证：相关 JS `node --check`、小程序 JSON 解析、`git diff --check`、销售 SCRM 与客户信息链测试 `16 passed`。
- 生产验证：健康检查 200，容器实际注册 `/api/scrm/customer-detail`，未带登录令牌访问返回 401；生产备份为 `/home/ubuntu/teamBuy/.deploy-backups/customer-detail-20260824023638/`。
- 当前未上传微信体验版。用户需在微信开发者工具重新编译上传后，验证：雷达卡进入详情、支付关闭直接详情、支付开启非会员进入会员页、会员直接详情、切换账号不残留、消息入口和旧分享链路不受影响。

## 2026-08-24 最新交接：支付跳转状态复核与客户详情匹配修正（本地，待体验版上传）

- 修复雷达页停留期间运营开关变化导致的旧状态跳转：点击“去支付”前重新读取会员状态。功能关闭回到中性态，免支付模式刷新雷达直接查看，收费非会员才进入 `/pages/membership/index`。
- 修复客户详情误报“这条客户信号已更新”：详情页现在统一匹配 `radarProfiles`、`visitorProfiles`、`opportunityAlerts`，兼容客户身份、访客身份和线索身份字段。
- 后端仍是联系方式和客户详情的最终权限边界；本轮没有修改客户信息链开关、支付开关、客户信息链后端、生产配置或生产数据。
- 验证：全量小程序 JS `node --check`、87 个 JSON 解析、`git diff --check`、客户信息链/销售 SCRM pytest `15 passed`。
- 真机验收前必须在微信开发者工具重新编译并上传本地代码。状态矩阵：客户信息链开+支付开=非会员到会员支付页；客户信息链开+支付关=直接详情；客户信息链关=中性关闭态。若只打开支付而未打开客户信息链，仍会显示“暂未开放”，这是后端组合规则。

## 2026-08-24 最新交接：客户信息链门禁文案不再展示支付提示（本地，待体验版上传）

- 会员页功能关闭态改为“当前由运营后台统一控制，暂不展示客户信息”；免支付态改为“当前可直接查看客户信息”。
- 雷达锁定态删除“需要先完成会员支付”和“去支付”，改为“点击后即可继续查看/查看客户信号”；点击后仍按最新状态直接路由。
- 真实会员开通页的订单、取消和成功反馈未改动；客户信息链门禁相关 JS/WXML 不再向用户解释支付。

## 2026-08-24 最新交接：客户详情身份键兼容修正（本地，待体验版上传）

- 修复详情页在会员状态已通过、接口已解锁但身份键格式不一致时误显示“这条客户信号已更新”。
- 详情页现在兼容 `user:xxx`/`xxx`、`anon:xxx`/`xxx`，并从 `radarProfiles`、`visitorProfiles`、`opportunityAlerts`、`customerTimelines` 统一寻找客户记录。
- 未知记录仍不展示客户信息；后端 owner 鉴权、会员门禁、联系方式安全边界未改变。
- 需在微信开发者工具重新编译后人工验证：点击同一雷达卡应进入客户详情；切换账号、功能关闭、收费非会员仍分别保持原有安全分流。

## 2026-08-21 最新交接：会话归档五步收口已完成本地自动化

- 普通会话存档统一按同一发送人、同一会话、首条消息起 60 秒固定窗口聚合；恰好 60 秒合并，第 61 秒拆分；`msgType=note` 保持原子解析。
- 图片加短说明在无高置信结构化场景信号时以图片为主体，图片进入 `UserNote.media`，说明保存到正文和 `structuredData.caption`；系统转存提示不再污染用户正文。
- 已补共享归档核心 teamBuy 私聊/群聊与 PetLove 群聊/私聊的反向路由隔离测试。
- 验证：`PYTHONPATH=backend .venv312/bin/python -m pytest backend/tests -q` 为 `245 passed`；`PYTHONPATH=platform/wecom-archive-core .venv312/bin/python -m pytest platform/wecom-archive-core/tests -q` 为 `5 passed`。
- 新增自测报告：[docs/qa/会话归档五步收口_Codex自测报告.md](docs/qa/会话归档五步收口_Codex自测报告.md)。
- 未部署生产、未上传微信体验版。下一步由用户在微信开发者工具重新编译上传，按报告中的归档聚合、四类编辑器、三种输入、分享图和缓存安全矩阵人工验收。

## 2026-08-20 最新交接：运营后台导航已收敛为分组入口

- `backend/app/static/ops-admin/index.html` 左侧不再平铺全部功能。
- 本地和生产版本一级入口均保留“总览、客户经营”；其余页面通过“数据分析、群运营、业务工作台、系统与反馈”可展开分组承载。
- 组内点击二级标签仍使用原有 `data-tab` 和接口，未改业务数据、权限或后端路由。
- 本地服务已启动，可访问 [http://127.0.0.1:8000/ops](http://127.0.0.1:8000/ops)；生产已部署完整运营后台，可访问 [https://teambuy.lifelove.top/ops](https://teambuy.lifelove.top/ops)。
- 客户经营相关三个接口线上鉴权验证均返回 200，`customerInfoChainEnabled` 初始为 `false`；进入客户经营页后可通过开关启用。
- API、后台 worker、归档 worker 已重启；生产备份目录为 `/home/ubuntu/teambuy-backups/teambuy-customer-ops-20260820-235617/`。

## 2026-08-15 最新交接：我的页名片入口已改为直接发客户

- `miniprogram/pages/profile/index.wxml`：已发布名片显示原生“发名片”按钮；未发布或未完善名片显示“完善名片/制作名片”。
- `miniprogram/pages/profile/index.js`：从资料列表识别当前名片的 `sourceNoteId + shareState`，已发布时直接生成客户页分享路径并记录分享归因；编辑动作保留在未就绪分支。
- 未新增后端接口，未部署生产，未上传微信体验版。
- 验收重点：已发布名片点击一次后出现微信分享面板；草稿进入编辑器；停止分享不允许直接分享；分享路径和封面不串卡。

## 2026-08-15 最新交接：隐私权限与销售链路高风险审查完成

- `app.json` 已移除未使用的 `permission.scope.userLocation`，保留 `requiredPrivateInfos.chooseLocation`；当前代码没有 `wx.getLocation`，不要在微信隐私指引中编造实时定位用途。
- 已确认剪贴板、选中的文件、选中的照片/视频、相册写入和选中的位置信息与代码调用相符；手机号仍是用户主动编辑/提交，不是 `wx.getPhoneNumber` 自动获取。
- 已审查客户智能缓存隔离、会员校验顺序、客户联系方式脱敏、发布与发客户状态、分享图兜底/就绪门禁、旧 `sales-template-select` 引用、无客户信号时会员推广隐藏和生产域名配置，未发现新的 P0。
- 验证：相关 JS `node --check` 通过；小程序 68 个 JSON 解析通过；`git diff --check` 通过；后端 `228 passed`。
- 未部署生产、未上传微信体验版。用户下一步需在微信后台确认剪贴板与选中位置信息的申报项，确认 request/uploadFile/downloadFile 合法域名，并在开发者工具重新编译后验证生产 `uploadFile` 授权与真实分享封面。

## 2026-08-14 最新交接：合集客户分享封面竞态已修复

- 合集列表页只有营销封面 Canvas 生成完成后才显示原生“发客户”；生成中不可点击，失败可重试，不再把空 `imageUrl` 交给微信。
- 合集编辑页不再直接使用 `bannerUrl` 分享，新增隐藏 Canvas 生成统一客户封面；无主图时使用标题封面兜底，图片上传失败时保留本地临时文件分享。
- 已发布合集编辑后会先清空旧分享封面，重新发布后再生成新版本封面，避免旧内容或编辑页截图进入分享卡。
- 已完成：`node --check`（合集列表/编辑器/分享工具）与 `git diff --check`。未部署、未上传体验版；需微信开发者工具重新编译后验证首次生成、重试、编辑后重新发布和真实微信分享卡。

## 2026-08-14 最新交接：电子名片打开速度与图片入口尺寸已调整

- 名片入口优先读取本地 `businessCardNoteId`、名片详情缓存和资料列表缓存，避免每次连续请求完整资料库。
- 身份编辑区先渲染，精选资料列表异步加载，不再阻塞整个页面。
- 头像预览 148rpx、二维码预览 184rpx；添加/更换按钮收窄为 64rpx，保留中间说明文字宽度。
- 未部署、未上传；需微信开发者工具重新编译后检查缓存命中、首次无缓存和小屏真机布局。

## 2026-08-14 最新交接：五类编辑器底部操作与名片二维码尺寸统一

- 电子名片二维码预览区已放大，二维码“添加/更换”按钮已缩小，头像按钮不受影响。
- 房源、商品、服务、普通文字、链接编辑器底部双按钮改为等宽、对称、flex 居中；左侧浅色，右侧蓝色主按钮。
- 未部署、未上传；需真机确认小程序安全区和按钮长文案表现。

## 2026-08-14 最新交接：电子名片与新建第五入口已按设计稿收口

- `pages/business-card-studio` 已脱离通用结构化模板，围绕“人”重做为实时预览、个人身份、名片表达、联系方式、精选资料和四样式。
- 新建资料页一行5项：房源、商品、服务方案、普通资料、电子名片；再次进入名片优先复用已有主名片。
- 姓名、头像、职位、公司、城市、电话、微信、二维码、邮箱、网址统一读写 `salesProfile`；名片只保存表达、引用和样式。
- 客户页不再生成旧服务范围和默认假内容；精选资料最多3项，跨用户/删除引用自动过滤，打开动作单独记录。
- 定向测试3项、前端 JS、Python 编译与 diff 检查通过。未部署、未上传；仍需开发者工具编译及真机核对一行5入口、长公司名、二维码相册和4种样式。

## 2026-08-14 最新交接：普通手动文字资料已按轻编辑器重做

- `text_note` 不再显示旧“资料详情 + 整理归档 + 合集/标签”拼接页，改为客户效果、标题、正文、选填附件和保存预览。
- 新建草稿标题/摘要/正文为空，旧系统占位词加载时会清理；用户未填写标题不能保存。
- 普通文字客户页改为专业阅读排版，并保留咨询与轻量生成同款。
- 空附件默认折叠，有图片/PDF/链接自动展开；相册入口优先使用 `wx.chooseImage`。
- 定向测试和静态检查通过。未部署、未上传，仍需微信开发者工具编译及安卓/iOS真机视觉、相册、PDF检查。

## 2026-08-14 最新交接：服务/合作编辑器已按轻量设计重做

- `pages/service-offer-studio` 已对齐设计稿信息层级，不再使用旧紫色通用结构化模板。
- 服务新契约为名称、一句话介绍、详细说明、服务范围、价格/合作条件和固定咨询动作；历史复杂字段自动合并到详细说明。
- 空白草稿不写实体占位值；客户页不再展示适合人群、流程、案例和预约等旧服务 DOM，空选填模块自动隐藏。
- 图片/PDF/链接继续作为真实附件展示，点击行为进入客户雷达；客户“咨询详情”点击记录咨询事件。
- 定向后端测试 2 项通过，前端语法与静态反向检查通过。未部署、未上传；仍需微信开发者工具编译并真机核对设计稿尺寸与附件选择。

## 2026-08-13 最新交接：手工链接已接入网页解析与链接卡

- 已修复“添加链接后整理成普通笔记”的根因：新增后端网页元信息解析和 `/api/notes/link-capture`。
- 纯链接会生成 `link/bookmark`，自动读取可用的标题、摘要、封面和来源；抓取失败仍保留链接卡。
- 解析限制为公开 HTTPS 网页，并限制跳转、类型和大小，避免后端 URL 抓取形成 SSRF。
- 定向回归 2 项通过，后端编译、小程序 JS 和 diff 检查通过。
- 已部署测试后端 `/test-api`，公网实测普通网页能生成并读取 link/bookmark；未部署生产 `/api`。
- 小程序前端未由 Codex 上传，需用户在微信开发者工具重新编译/预览或上传体验版后真机验证公众号和普通网页。
- 17:05 真机回归补充：已修复纯链接创建后不跳确认页；公众号大页面改为流式读取前 2MB，用户当时的链接记录已补齐真实标题、摘要和封面。
- 17:13 真机回归补充：旧通用编辑器确认不符合效果图，已新增独立 `link-confirm` 确认页；未整理链接统一进入确认页，已整理文章进入预览页。截图对应测试资料已恢复为 bookmark。
- 所有资料入口审计补充：文字、图片、图文、链接、PDF及企业微信认领现在统一先确认识别；空白业务资料和已确认资料不重复确认。确认页按类型展示，不会把其他资料伪装成链接。
- 17:39 回归补充：结构化编辑器 placeholder 字号/行高已统一收紧；朋友圈按钮改为微信限制说明；资料库 onShow 强制刷新 cards，17:39 的 5 条新建资料已确认都在测试接口列表中。
- 深度编辑器附件补充：四类编辑器默认收起图片/PDF/链接区，仅显示“补充资料”摘要；已有附件自动展开，三类选择、预览/打开/删除及数量限制均保留并已静态验证。

## 2026-08-13 最新交接：首页客户处理与四 Tab 已完成本地前端改造

- 底部已收敛为 `首页 / 资料 / 雷达 / 我的`；合集从资料页“我的合集”进入。
- 首页 Banner 右侧已加入动态雷达；左侧文案统一为“分享到客户”。
- 发出、打开、跟进已改成明显可点击的反馈卡，并分别路由到已发资料、访问客户和待跟进。
- 有客户信号时会出现最高优先级客户卡：锁定态脱敏并引导会员，解锁态提供轨迹和跟进入口。
- 首页“快速开始”及无上下文“生成同款”已删除。
- 微信开发者工具重新打开项目后编译成功，空数据首页和资料页合集入口已在 iPhone 12/13 Pro 模拟器目视确认。
- 未部署、未上传、未碰生产；真实客户信号与会员动态状态仍需测试接口联调。

## 2026-08-12 最新交接：首页销售行动流 UI 已重构并实机编译验收

- 首页已改为：单一动态主动作、发出/打开/跟进反馈链、最多三条最近常用、三个快速创建入口。
- 已移除旧版常驻统计宫格、重复空态、六宫格工具和无信号时的提前付费卡。
- 已在微信开发者工具以正确 AppID 重新打开 `teamBuy/miniprogram` 并编译，iPhone 12/13 Pro 模拟器确认新版首屏生效。
- 空资料时不再把资料总数当作已发出数量，也不点亮流程节点。
- 本轮仅修改本地首页及项目记忆文档；未部署、未上传、未碰生产。
- 当前开发者工具控制台可见客户情报测试接口 404；首页可降级为空状态，但带真实资料/信号/会员的动态状态仍待测试后端联调。

## 2026-08-12 最新交接：销售型 SCRM 五阶段已完成本地开发和对抗式审查

- 产品规则：内容制作与分享免费；客户身份、联系方式、完整轨迹、档案和跟进为 19.9 元/月会员。
- 推广规则：一级直接邀请每笔有效首购/续费奖励 50%（995 分）；无二级 5% 自动奖励；提现手续费当前为 0。
- 已完成：
  - 首页、资料库、客户雷达和我的页结构收口。
  - 会员订单/权益、免费脱敏摘要、付费客户信息链。
  - 两种生成同款模式和来源归因。
  - 一级奖励、退款撤销、提现预占状态。
  - 旧业务看板、线索、客户动作、合集分析的后端付费边界。
- 资源工具区块未改；`docs/prompts/group-qr-upload-codex-prompt.md` 的既有删除也未处理。
- 文档：
  - `docs/stage2-docs/38-sales-scrm-five-stage-development-spec.md`
  - `docs/qa/销售型SCRM五阶段_测试清单与验收标准.md`
  - `docs/qa/销售型SCRM五阶段_对抗式审查与修复报告.md`
- 验证：171 个后端测试通过；相关小程序 JS 和 JSON 静态检查通过；`git diff --check` 通过。
- 未完成且不得误报：
  - 未部署任何环境，未改生产 `/api`。
  - 未接微信真实支付、回调验签、自动续费、企业付款。
  - 未完成微信开发者工具/真机 UI 和分享卡验收。
  - 历史 API 仍以请求用户 ID 表达身份，生产商业化前需要统一 session/token 认证改造。

## 2026-08-12 最新交接：资料整理助手测试登录已恢复

- 小程序 AppID：`wxf43f7bc098d9858b`。
- 当前小程序仍使用测试配置：`/test-api`、`/test-media`、`environmentName=test`。
- 旧服务器目录 `/home/ubuntu/teamBuy` 与旧测试容器已经不存在；历史路径不要继续使用。
- 当前测试部署：
  - 服务器目录：`/srv/teambuy-test/teamBuy`
  - Compose 项目：`teambuy-test`
  - 后端容器：`teambuy-test-backend-test-1`
  - 数据库容器：`teambuy-test-postgres-test-1`
  - 宿主机端口：`8003`
- `backend/.env.test` 已在本地和服务器测试目录配置，文件被 Git 忽略；AppSecret 不写入文档或 Git。
- 公网验证：
  - `https://teambuy.lifelove.top/test-health` 返回 200。
  - `/test-api/auth/wechat-login` 已可到达微信 `jscode2session`；无效诊断 code 返回微信侧 `invalid code`，不再返回 502。
- 测试专用 `backend/Dockerfile.test` 为轻量镜像，暂不安装 `paddlepaddle` / `paddleocr`。登录、资料、资源工具 API 可测试；真实 PaddleOCR 识别不在本镜像验收范围。
- 本轮未改生产 `/api`，未重启其他项目容器。

## 2026-07-05 最新交接：项目先收口，后续以迁移复用为主

- 当前结论：
  - `teamBuy` 项目暂时搁置，不继续作为近期主开发战场。
  - 当前仓库先整理归档，并上传 GitHub 保存阶段成果。
  - 后续更适合把域名、小程序主体、H5 外壳、企业微信接入和配置骨架迁到独立新项目。

- 已新增文档：
  - [project-closeout-20260705.md](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/docs/project-closeout-20260705.md)
  - [config-migration-reference.md](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/docs/config-migration-reference.md)
  - [new-project-migration-checklist.md](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/docs/new-project-migration-checklist.md)

- 当前最重要的可复用配置：
  - 生产域名：`https://teambuy.lifelove.top`
  - 服务器：`81.70.84.35`
  - 历史项目目录：`/home/ubuntu/teamBuy`（2026-08-12 已不存在；当前测试目录见顶部最新交接）
  - 小程序 AppID：`wxf43f7bc098d9858b`
  - 当前小程序工作区配置仍是测试环境：
    - `apiRoutePrefix=/test-api`
    - `mediaRoutePrefix=/test-media`
    - `environmentName=test`

- 当前最值得迁走的能力：
  - 企业微信客服接入骨架
  - 会话内容存档骨架
  - H5 `ticket + web-view` 模式
  - 资源工具 H5 页面骨架
  - 后端 `.env.example` 配置结构

- 当前不建议直接照搬的内容：
  - 本地 `artifacts/` 留档
  - teamBuy 当前资源工具业务字段和历史路由包袱
  - 测试环境 `/test-api`、`/test-media` 的路径约定

- 上传 GitHub 前已补：
  - `.gitignore` 增加 `artifacts/`
  - 单独的迁移配置参考文档，方便新项目直接查

## 2026-07-03 本轮交接：资源工具 H5 本地预览模式 + 首页/我的发布第二轮打磨

- 本轮继续只动测试环境的资源工具 H5，不碰生产。
- 已完成：
  - H5 增加本地预览模式：本地直接打开静态页时，不再卡在“登录状态已过期”。
  - 预览模式内置示例机会、示例供需、示例资料、示例回应包，用于视觉和交互验收。
  - 预览模式下保存、申请、订阅、提交审核、下架等动作只做前端模拟，不调用真实接口。
  - 首页继续强化“雷达感”：
    - 实时刷新胶囊
    - 扫描信号区
    - 匹配分角标
    - 主动作更聚焦
  - 我的发布继续强化“控制台感”：
    - 最近动态 / 优先处理 / 建议动作
    - 统计卡层次更清楚
    - `＋ 新建发布 / 查看历史` 改为统一 CTA，不再出现默认按钮黑边
  - 全局按钮补了按下态、焦点态和轻过渡，减少默认浏览器样式干扰。
- 已验证：
  - 本地预览页：首页已能直接看到示例机会雷达。
  - 本地预览页：“我的发布”已能直接看到控制台页。
  - H5 内联脚本语法通过。
  - `git diff --check -- backend/app/static/h5/resource-tools/index.html` 通过。
  - 已同步测试后端并重启 `teambuy-test-backend-test-1`。
  - 公网测试页包含 `preview-banner`、`mine-console-strip`、`lead-live-pill` 等新标记。
  - 公网 H5 内联脚本解析通过。
- 当前建议下一步：
  - 继续按“更酷一点”的方向打磨首页卡片层次和统计节奏。
  - 接着处理“我的发布”里卡片信息密度和状态胶囊细节。
  - 最后统一供需广场和发布供需页的交互动效、图标和按钮层级。

追加进展：

- 首页机会卡又收了一轮：
  - 城市、行业改成轻胶囊元信息。
  - 时间信息单独放右侧。
  - 操作区前加分隔线，卡片层次更清楚。
- 我的发布单卡改成专用发布卡：
  - 增加发布序号角标。
  - 标题、摘要限制行数。
  - 城市 / 行业 / 类型 / 状态改成四格指标区。
  - 操作区改成更稳定的主次按钮布局。
- 已再次同步测试后端。
- 公网测试页已确认包含：
  - `lead-meta-pill`
  - `lead-card-divider`
  - `mine-publish-card`
  - `mine-publish-title`
  - `mine-publish-metrics`
  - `mine-publish-actions`

继续进展：

- 首页顶部继续往“实时扫描面板”推进：
  - 新增扫描状态、重点城市、当前热点三块轻面板
  - 新增脉冲扫描点
- “收到申请”区域不再只是普通列表卡：
  - 改成工单式申请卡
  - 新增申请人 / 渠道意向 / 建议动作三格信息
  - 操作区继续保留通过 / 拒绝，但语言更贴近控制台
- 已再次同步测试后端。
- 公网测试页已确认包含：
  - `lead-panel-strip`
  - `lead-panel-node`
  - `lead-panel-dot`
  - `mine-application-card`
  - `mine-application-grid`
  - `mine-application-actions`

补充修正：

- 用户反馈在 `?page=opportunities` 看不到扫描面板，以及“收到申请”DOM 没反应。
- 已确认原因：
  - 用户打开的是“我的机会”页，不是首页首屏。
  - 预览模式下原先没有给“收到申请”喂 mock 数据。
  - `通过 / 拒绝` 在预览模式下原先没有模拟反馈。
- 已修正：
  - “我的机会”页顶部同步显示一版扫描面板，并明确文案“机会页同步显示”。
  - 预览模式下补充 `previewReceivedApplications` / `previewMyApplications`。
  - 预览模式下 `通过 / 拒绝` 已可弹出模拟处理反馈。
- 已再次同步测试后端。

继续进展：

- “我申请的合作”已统一成控制台申请卡：
  - 蓝色发起视角
  - 申请去向 / 当前状态 / 建议动作三格信息
  - `查看原需求 / 继续找资源` 操作
- 供需广场顶部已合并成统一产品面板：
  - `market-top-panel`
  - hero、统计、筛选合并
  - 增加轻量运行摘要
- 已再次同步测试后端。

## 2026-07-03 本轮交接：资源工具 H5 补历史页、需求详情和搜索

- 本轮继续在测试环境打磨资源工具 H5。
- 已完成：
  - “我的发布”补历史入口和历史页。
  - 需求类供需补独立详情页样式。
  - 发布供需页资料卡改为紧凑显示，并支持“查看详情”。
  - 供给预览页“会员可查看”等联系要求改为可点击按钮。
  - 供需广场补真实搜索输入框和搜索按钮。
  - 供需广场卡片次操作改为申请合作类动作，避免走错保存链路。
  - 订阅配置和“正在为你盯”改为实时联动。
  - 发布页到预览页的提交链路改为走 `publishDraft` 草稿缓存，避免提交时丢标题。
  - 资源工具导航改为 4 个主入口 + 2 个独立动作入口，不再上下重复：
    - 主入口：`我的机会 / 需求广场 / 已保存 / 订阅`
    - 动作入口：`发布供需 / 我的发布`
  - 发布供需表单改为示例占位，不再默认写死参考内容。
  - 首页首屏继续压缩，强化“找需求 / 找资源”导向。
  - 我的发布页补 `＋ 新建发布 / 查看历史` 两个醒目入口。
- 已验证：
  - H5 内联脚本语法通过。
  - H5 文件 `git diff --check` 通过。
- 下一步适合真机检查：
  - 发布页长标题资料卡是否还会挤压变形。
  - 我的发布历史页是否符合你要的“历史”语义。
  - 供需广场搜索词输入和回车搜索是否符合使用习惯。

## 2026-07-03 本轮交接：资源工具 H5 9 页视觉骨架完成

- 本轮接续上一次 systemError 后的状态，不从头重做。
- 已完成并同步测试环境的 H5 页面：
  - 商机线索首页
  - 我的机会
  - 线索详情
  - 订阅雷达
  - 已保存
  - 供需广场
  - 发布供给
  - 供给预览
  - 我的发布
- 本轮最后补齐：
  - “我的发布”从旧版通用卡片替换为参考图风格页面，包含统计卡、发布状态、发布卡片、收到/发出的合作申请和再发布入口。
  - 发布供给页“下一步”补 `preview-supply` 动作，可进入供给预览。
- 已验证：
  - H5 内联脚本本地解析通过。
  - 9 个页面关键结构标记检查通过。
  - 后端 `compileall` 通过。
  - `git diff --check` 通过。
  - 已同步测试后端并重启 `teambuy-test-backend-test-1`。
  - `https://teambuy.lifelove.top/test-health` 正常。
  - `https://teambuy.lifelove.top/test-api/h5/resource-tools/` 已包含 9 页结构标记，远端内联脚本解析通过。
- 当前状态：
  - 只改测试环境，生产未部署、未改动。
  - H5 资源工具已具备 9 页视觉骨架和主按钮链路，下一步适合真机截图逐页微调。
- 下次继续建议：
  - 用户真机先验收 9 页是否都能进入，重点看“我的发布”和“供给预览”。
  - 如果仍有视觉偏差，按 34 号文档截图逐页微调间距、字号、按钮宽度和空态。

## 2026-07-02 本轮交接：资源工具 H5 体验打磨

- 本轮继续打磨商机发现/资源工具 H5，不涉及生产后端。
- 已完成：
  - 顶部导航改为横向滑动胶囊，窄屏不再把 6 个 tab 挤成一排。
  - 机会/供需列表新增城市、行业、类型、排序筛选。
  - 筛选会带接口参数，并在前端做兜底过滤；排序支持推荐优先和最新优先。
  - 发布供需页“发布方向”改为“我能提供 / 我在寻找”中文胶囊，不再让用户看到 `supply/demand` 内部值。
  - 发布供需页城市、行业、类型、联系要求支持自定义输入 + 常用标签点选。
  - 操作按钮补充居中和宽度约束，减少胶囊变形。
- 用户反馈“之前 4 个页面，现在像一个页面，没法验收”后，已追加修正：
  - 默认首屏改为资源工具入口页。
  - 入口页提供 4 个主页面卡片：机会雷达、供需广场、已保存、订阅雷达。
  - 发布供需、我的发布作为常用动作入口放在下方。
  - 搜索和筛选默认隐藏，进入机会/供需/已保存后才显示。
  - 顶部主导航固定为 4 个主页面 + 刷新，不再横向滚动挤压。
  - 首页标题为“资源工具”，进入模块后标题随页面变化。
- 用户进一步要求尽量 1:1 还原原生小程序页面后，已再次修正：
  - 机会雷达恢复 hero、匹配度圆形、主动推荐、雷达图、推荐机会卡和更多机会卡。
  - 供需广场恢复 hero、发布按钮、四格统计、分类筛选面板和线索卡。
  - 已保存恢复轻量跟进台、状态统计、回应包筛选和跟进卡。
  - 订阅雷达恢复配置分组、标签选择、正在为你盯和保存按钮。
  - H5 不再用通用简化列表承载这 4 页，改为尽量贴近原生页面结构。
- 已验证：
  - H5 内联脚本语法通过。
  - 后端 compileall 通过。
  - H5 文件 `git diff --check` 通过。
  - 已同步测试后端并重启 `teambuy-test-backend-test-1`。
  - 公网测试健康接口正常，公网 H5 页面已包含新筛选和中文发布方向。
- 当前配置：
  - 小程序仍是测试环境：`apiRoutePrefix=/test-api`、`mediaRoutePrefix=/test-media`、`environmentName=test`。
  - 生产未部署、未改动。
- 下次继续建议：
  - 真机看 H5 页面：导航横滑、筛选点击、发布供需点选/自定义输入、关联资料选择。
  - 如果体验确认，再补更细的视觉打磨：列表统计条、供需状态文案、详情页按钮层级和空态引导。

## 2026-07-02 本轮交接：资源工具 H5 化最小闭环

- 用户确认：先按“小程序资源工具入口 -> web-view -> 后端 H5 页面 -> 能识别当前用户 -> 能打开机会/供需列表”开发。
- 本轮完成：
  - 后端新增 H5 短期票据接口 `POST /api/auth/h5-ticket` 和校验接口 `GET /api/auth/h5-session`。
  - 后端新增 `/h5/resource-tools/` 静态 H5 首屏。
  - H5 首屏可通过 `apiPrefix` 区分 `/test-api` 和 `/api`，读取机会列表和供需列表。
  - 小程序新增 `pages/resource-tools-webview/index`。
  - “我的”页资源工具入口改为先签发票据再打开 H5，异常时自动回退原生 `pages/opportunity-radar/index`。
- 已验证：
  - 前端 `node --check` 通过。
  - 后端 compileall 通过。
  - 小程序 JSON 校验通过。
  - 后端 TestClient 烟测通过：mock 登录、签票、验票、H5 页面返回。
  - `git diff --check` 通过。
- 未做：
  - 未上传小程序体验版。
  - H5 详情、保存、回应包、订阅、发布供需还未迁移。
- 测试环境：
  - 已同步测试后端，未碰生产。
  - 测试端备份：`/home/ubuntu/teamBuy-deploy-backups/20260702-145729-h5-resource-tools-test`。
  - 公网 `https://teambuy.lifelove.top/test-api/h5/resource-tools/` 返回 200。
  - H5 签票、验票、机会列表、供需列表公网链路验证通过。
  - 用户侧展示文案已清理 H5/最小闭环/原生页面等开发字眼，测试环境已同步。
- 追加完成：
  - 机会列表/供需列表支持进入详情。
  - 机会详情支持保存机会、查看联系方式。
  - 供需详情支持申请合作。
  - 以上动作已接真实测试接口；查看联系方式仍由后端资源钱包规则决定是否扣分。
  - 顶部新增“已保存”和“订阅”模块。
  - 已保存模块接真实列表，可生成/打开回应包。
  - 订阅模块接真实读取和保存接口。
  - 回应包基础展示已迁入：展示回应话术、推荐资料、跟进建议，并支持复制回应内容。
  - 发布供需已迁入基础表单，支持保存草稿和提交审核。
  - 我的发布/收到的合作申请/我申请的合作已迁入基础列表。
  - 收到的申请支持通过/拒绝。
  - 回应包反馈雷达已迁入基础显示，可查看打开/复制/联系等事件统计。
  - 发布供需已支持关联资料/合集选择器。
  - 我的发布已支持编辑回填和下架。
  - 回应包复制内容会写入 `copy` 反馈事件。
- 下次继续：
  - 确认小程序后台业务域名。
  - 重新上传/预览包含 `pages/resource-tools-webview` 的小程序前端。
  - 如果测试库为空，需要先录入/审核测试机会和供需数据，才能在 H5 列表中看到可点卡片。
  - 后续主要是 UI 体验打磨、筛选/排序增强、生产环境上线前合并热修基线和切生产配置。

## 2026-07-02 本轮交接：资料详情胶囊、头像交互和缓存

- 用户反馈：
  - 资料详情页各场景胶囊都偏大、间距不舒服。
  - 页面切换读取慢，希望登录后进入用户自己的缓存，非主动更新数据优先读缓存。
  - 个人资料弹窗不要单独“选择头像”按钮，直接点头像换头像，头像放中间。
- 本轮完成：
  - 个人资料弹窗头像居中，点击头像直接触发 `chooseAvatar`。
  - 删除独立“选择头像”按钮。
  - 资料详情页候选、功能、快捷选项、状态、预览和媒体角标胶囊统一调小并加大间距。
  - `api.js` 新增用户级 note 缓存，按环境/API 前缀/用户/查询条件隔离。
  - 资料详情页和旧笔记列表先读缓存再拉后端。
  - 资料库页不再强制绕过 resource-store 缓存，resource-store 可从 storage 直接返回缓存。
- 已验证：
  - 小程序相关 JS `node --check` 通过。
  - 小程序相关 JSON 校验通过。
  - `git diff --check` 通过。
- 未做：
  - 未部署生产。
  - 未上传小程序。
  - 后端缓存未在本轮直接加，后续建议用 ETag/短 TTL/写入失效策略。

## 2026-07-02 本轮交接：商机候选与规则样本池

- 用户反馈：
  - `推广、落地页100起 / 外贸独立站1000起 / 官网商城 / 小程序开发 / 返点 / 联系电话` 这类明显商机服务内容进不了商机。
  - 资料详情页“这条资料像是”和“添加功能”按钮太大、太挤。
  - 希望 PC 端增加“规则样本池”，用户纠错后人工确认，再逐步形成全局规则。
- 本轮完成：
  - 小程序普通笔记兜底候选新增 `service_offer` 商机合作。
  - 建站、推广、落地页、独立站、官网商城、小程序开发、服务器、源码交付、返点、联系电话等信号会出现商机合作候选。
  - 候选按钮和功能胶囊调小，并增加按钮间距。
  - 后端 `confirm-type` 会在类型变化时记录规则学习样本。
  - PC 后台新增“规则样本池”，支持筛选和确认/拒绝样本。
- 已验证：
  - 小程序相关 JS 语法通过。
  - PC 后台内联 JS 语法通过。
  - 后端 compileall 通过。
  - 规则样本池本地存取通过。
  - 相关后端回归 10 passed。
  - `git diff --check` 通过。
- 未做：
  - 未部署生产。
  - 未上传小程序。
  - 尚未做“approved 样本生成全局规则配置并发布”的第二步。

## 2026-07-02 本轮交接：登录页缩略图 WebP 改 JPG

- 用户反馈：登录页左侧房源缩略图真机空白，但开发者工具能显示。
- 判断：该图是包内 `/static/workspace/login-room.webp`，真机体验版对 WebP 或包资源更新可能不稳定。
- 本轮完成：
  - 新增 `/static/workspace/login-room.jpg`，360x270，约 21KB。
  - 登录页改用 JPG。
  - 增加图片加载失败兜底，失败时显示“房”字。
- 注意：
  - 需要重新上传小程序体验版后才能在真机看到。

## 2026-07-02 本轮交接：生产热修基线归档与二期合并计划

- 用户问题：生产环境已有 OCR/房源热修，但二期后端是另一套代码，担心二期上线时覆盖掉当前新增功能。
- 本轮完成：
  - 已从生产服务器拉取线上关键代码文件到 `artifacts/prod-baseline-20260702-ocr-property/`。
  - 已确认生产容器 `backend`、`backend-worker`、`archive-worker`、`postgres` 均正常运行。
  - 已新增合并计划文档：`docs/stage2-docs/37-production-hotfix-merge-plan.md`。
  - 已在 `docs/decisions.md` 记录：二期上线必须先合并生产热修基线。
  - 已在 `docs/pitfalls.md` 记录：禁止二期代码直接覆盖生产热修。
- 当前判断：
  - `docker-compose.yml`、`dependencies.py`、`worker.py`、`sync_task_queue.py`、`background_task_worker.py`、`property_table_ocr_*` 本地与生产一致。
  - `app_service.py`、`repository.py` 是高风险文件，本地比生产多二期商机/资源钱包代码，不能整文件覆盖。
- 二期上线前必须：
  - 用生产基线和二期代码建立统一分支。
  - 人工合并 `app_service.py`、`repository.py`。
  - 跑 OCR、007 表格 OCR、房源识别、会话归档 worker 回归。
  - 只从统一分支部署测试环境，测试通过后再部署生产。

## 2026-07-02 本轮交接：真机反馈修复

- 用户反馈：
  - 我的雷达保存失败。
  - 发布需求页“我能提供 / 我在寻找”胶囊变形。
  - “绑定资料 ID”用户不知道怎么填，应从自己的资料库或合集选择。
  - 供需广场城市和行业不能只靠少量固定标签。
- 本轮完成：
  - 我的雷达保存前校验 `user.id` 和商机 id，失败时展示后端具体原因。
  - 发布供需页类型选择改成稳定胶囊 `view`。
  - 发布供需页改为拉取用户资料库和合集，选择关联资料，不再手填 ID。
  - 供需卡新增 `linkedResourceType/linkedResourceId`，兼容旧 `linkedNoteId`。
  - 发布页和供需广场城市、行业、类型支持自定义输入 + 常用标签。
- 已验证：
  - P1 闭环单测：1 passed。
  - 相关回归：13 passed。
  - 后端 compileall、前端 node --check、小程序 JSON、`git diff --check` 均通过。
- 边界：
  - 未部署生产。
  - 未上传小程序。
  - 若真机仍用旧体验版或旧测试后端，仍需要上传/同步后才能看到修复。

## 2026-07-01 本轮交接：商机雷达 P1 1-5 全部完成

- 用户要求：把 P1 里面 1 到 5 全做了。
- 本轮完成范围：
  - P1-1 订阅雷达真实保存：后端新增订阅模型、仓储、接口；小程序订阅页可保存并回填订阅。
  - P1-2 联系方式解锁：线索详情可调用真实解锁接口，使用资源积分扣减，重复解锁不重复扣分。
  - P1-3 回应包雷达反馈页：新增反馈接口和小程序反馈页，展示打开、联系、保存等事件统计和下一步建议。
  - P1-4 我的发布 / 发布供给：新增供需卡模型、发布页、我的发布页、提交审核流程；供需广场展示审核通过内容。
  - P1-5 供给审核后台：PC 运营后台新增供给审核 tab，支持筛选、通过、驳回。
- 主要涉及文件：
  - 后端：`backend/app/models/domain.py`、`backend/app/services/repository.py`、`backend/app/services/app_service.py`、`backend/app/api/routes_opportunities.py`、`backend/app/api/routes_ops_admin.py`、`backend/app/main.py`。
  - 小程序：`miniprogram/services/api.js`、`miniprogram/pages/opportunity-*`、`miniprogram/pages/response-package-radar/*`、`miniprogram/pages/supply-demand-publish/*`、`miniprogram/pages/supply-demand-my/*`、`miniprogram/app.json`。
  - PC 后台：`backend/app/static/ops-admin/index.html`。
  - 测试：`backend/tests/test_app.py`。
- 已验证：
  - P1 闭环单测：1 passed。
  - 商机/资源钱包/回应包/后台相关回归：13 passed。
  - 后端 compileall、前端 node --check、小程序 JSON 校验、`git diff --check` 均通过。
- 未做/后续：
  - 未部署生产，未上传小程序。
  - 供给发布页的关联资料仍是手动 `linkedNoteId`，后续可做资料选择器。
  - 供需广场里供给卡的进一步合作申请、付费联系和保证金机制仍属于后续 P2，不在本轮。
  - 外部主动推送订阅结果未做；当前订阅只影响小程序“我的机会”展示。

## 2026-07-01 本轮交接：P1 体验补强四项完成

- 用户要求继续补：
  - 已保存跟进台完善。
  - 订阅匹配结果按订阅条件生成“今日推荐机会”。
  - 回应包生成前可勾选/替换资料。
  - 供需广场城市、行业、类型、可联系状态筛选接真实数据。
- 本轮完成：
  - 后端 `/api/opportunity-leads` 支持 `city/industry/demandType/contactStatus`，带 `userId` 时按 active 订阅生成推荐结果。
  - 后端 `/api/opportunity-leads/saved` 支持 `status/keyword/packageStatus`，返回回应包状态、最近跟进和跟进次数。
  - 后端回应包预览和生成支持 `selectedAssetIds`，预览返回 `assetOptions/selectedAssetIds`。
  - 后端 `/api/supply-demand/cards` 支持 `city/industry/demandType/cardType/contactStatus`。
  - 小程序已保存页支持状态筛选、回应包筛选、改状态、设提醒、打开回应包。
  - 小程序回应包页支持资料勾选、替换和按所选资料刷新话术。
  - 小程序供需广场筛选胶囊接真实接口，非商机供需卡暂不误跳商机详情。
- 已验证：
  - P1 增强闭环单测：1 passed。
  - 商机/资源钱包/回应包/后台相关回归：13 passed。
  - 后端 compileall、前端 node --check、小程序 JSON、`git diff --check` 均通过。
- 未做：
  - 未部署生产，未上传小程序。
  - 供需卡独立详情页、合作申请、主动推送已在后一段补齐。

## 2026-07-01 本轮交接：供需详情、合作申请、站内主动推荐完成

- 用户要求继续做：
  - 供需卡独立详情页。
  - 合作申请。
  - 主动推送。
- 本轮完成：
  - 新增后端模型：`SupplyDemandApplication`、`OpportunityPushDigest`。
  - 新增仓储读写和 Postgres 表映射。
  - 新增供需卡详情接口、申请接口、申请列表接口、申请处理接口。
  - 新增站内推荐摘要生成、列表、标记已读接口。
  - 新增小程序供需详情页 `pages/supply-demand-detail/index`。
  - 供需广场非 `opp_*` 卡片进入供需详情页。
  - 我的发布页展示收到的申请，并支持通过/拒绝。
  - 我的机会页新增“主动推荐”站内摘要模块。
- 已验证：
  - P1 闭环单测：1 passed。
  - 商机/资源钱包/回应包/后台相关回归：13 passed。
  - 后端 compileall、前端 node --check、小程序 JSON、`git diff --check` 均通过。
- 重要边界：
  - 当前主动推送只是站内摘要，不外发微信订阅消息/模板消息/企微消息。
  - 本轮未部署生产，未上传小程序。

## 2026-07-01 本轮交接：P1 最后三个尾巴完成

- 用户要求：
  - 我的发布编辑回填。
  - 我申请的合作列表。
  - 站内推荐自动生成接口/后台触发，不外发消息。
- 本轮完成：
  - 供需发布页支持 `id` 参数编辑回填，并保存到原供需卡。
  - 我的发布页新增“编辑”按钮。
  - 我的发布页新增“我申请的合作”列表，展示 applicant 视角申请状态。
  - 供需详情接口允许 owner 查看自己的非公开状态卡片，用于编辑；非 owner 仍只能看 published。
  - 后端新增批量生成站内推荐摘要服务方法。
  - PC 后台供给审核页新增“生成站内推荐摘要”按钮。
- 已验证：
  - P1 闭环单测：1 passed。
  - 商机/资源钱包/回应包/后台相关回归：13 passed。
  - 后端 compileall、前端 node --check、小程序 JSON、`git diff --check` 均通过。
- 边界：
  - 没有生产部署。
  - 没有小程序上传。
  - 没有微信/企微外发，也没有定时任务；后台触发只是批量生成站内摘要。

## 2026-07-01 本轮交接：房源公开联系方式与上游私密备注分离

- 用户确认需求：生成房源卡时自动把用户“我的”资料里的手机号/微信写进公开联系方式，同时继续把识别到的上游联系方式写进私密备注。
- 本轮修改：
  - `backend/app/services/skill_router_service.py`
  - `backend/app/services/app_service.py`
  - `backend/tests/test_app.py`
  - `backend/tests/test_skill_router.py`
- 后端规则：
  - 房源原文中的手机号不再写入公开 `note.phone` 或 `structuredData.contact`。
  - 房源原文中的手机号进入 `visibilityConfig.privateData.upstreamPhones`。
  - 上游相关行进入 `visibilityConfig.privateData.privateRemark`。
  - 创建/确认房源时，自动读取 owner `phone/wechat`，写入公开 `structuredData.phone/contact/contactPhone/wechat/contactWechat`。
  - 公开接口继续过滤 `privateData/privateTags`，并对 property note 替换安全 `body`，防止原文上游电话泄露。
- 部署：
  - 已同步生产容器 `teambuy-backend-1` 和测试容器 `teambuy-test-backend-test-1`。
  - 最新备份：`/home/ubuntu/teamBuy/backend/backups/codex-20260701-141141-owner-contact-empty-field-fix/`。
- 已验证：
  - 生产健康检查正常。
  - 容器内验证生成房源：owner 电话 `13900001111`、微信 `agent-yiyi` 在公开字段；上游电话 `18501775740` 仅在 `privateData.upstreamPhones`；公开 payload 不包含上游电话。
  - 本地 `backend/tests/test_skill_router.py`：14 passed。
  - 本地 `backend/tests/test_app.py -k "owner_contact or upstream_private or property_note_uses"`：2 passed。
- 注意：
  - 本地 `app_service.py` 仍混有其它未上线工作，后续部署继续使用“线上干净版本 + 最小补丁”的方式，避免整文件覆盖。

## 2026-07-01 本轮交接：测试环境旧规则导致房源仍显示普通笔记

- 用户截图显示 13:44 上传 `开福区天健一期H栋1205 / 独门独户 / 底价1500押一付三 / 民水民电 / 不养宠物 / 密码锁` 后仍是普通笔记，并且没有“整理成房源”按钮。
- 排查结论：
  - 生产 `/api/skills/content-to-note/run` 已能识别该文案为 `property_listing`。
  - 测试 `/test-api/skills/content-to-note/run` 起初仍是旧规则，返回 `text_note`、房源分 1、`typeSuggestions=[]`。
  - 因此用户手机体验版大概率仍连测试环境，或该旧笔记是在测试旧规则下创建。
- 本轮修复：
  - `backend/app/services/skill_router_service.py` 已同步到生产容器和测试容器。
  - `miniprogram/pages/note-edit/index.js` 增加前端兜底候选：旧普通笔记命中租房信号时展示“整理成房源”。
  - `backend/app/services/app_service.py` 本地补了确认类型时重新抽字段的逻辑；部署时不能直接使用本地混合版本，已使用线上干净备份做最小补丁部署。
  - `backend/tests/test_app.py` 增加普通笔记确认成房源后抽出租金、押付、水电的回归。
- 生产/测试部署：
  - 最初误传本地混有未上线商机线索依赖的 `app_service.py`，容器短暂启动失败。
  - 已从 `/home/ubuntu/teamBuy/backend/backups/codex-20260701-135443-property-suggestion-test-prod/` 恢复，并重新用干净线上版本打最小补丁部署。
  - 最小补丁备份：`/home/ubuntu/teamBuy/backend/backups/codex-20260701-135708-clean-app-service-confirm-type/app_service.py`。
- 已验证：
  - 生产健康检查正常。
  - 测试 `/test-api/skills/content-to-note/run` 对该文案返回 `property_listing/high`，并抽出 `price=1500`、`paymentMethod=押一付三`、`utilities=民水民电`。
  - 本地 `backend/tests/test_skill_router.py`：14 passed。
  - 本地 `backend/tests/test_app.py -k "quick_capture or confirm_plain_rental_note or manual_draft"`：9 passed。
  - 小程序 `note-edit/index.js` 语法和 JSON 校验通过。
- 注意：
  - 前端兜底按钮需要用户重新上传小程序后才会显示。
  - 当前本地 `app_service.py` 仍处于大工作树混合状态，后续部署必须继续避免直接整文件覆盖生产。

## 2026-07-01 本轮交接：上游备注常驻输入与默认 SCRM

- 用户反馈：房源编辑页仍看不到默认写上游联系方式和备注的地方；另外 SCRM 不管房源还是日常资料都应该默认打开。
- 本轮修改：
  - `miniprogram/pages/note-edit/index.js`
  - `miniprogram/pages/note-edit/index.wxml`
  - `miniprogram/pages/note-edit/index.wxss`
  - `backend/app/services/skill_router_service.py`
  - `backend/tests/test_skill_router.py`
- 前端变化：
  - 房源编辑页“仅自己可见：上游备注”在编辑资料页常驻展示。
  - 可手动填写上游电话、上游微信、上游联系人、中介费、密码锁、看房备注、上游备注。
  - 输入内容保存到 `visibilityConfig.privateData`，不进入客户页公开字段。
  - 小程序默认配置中普通资料、房源、团购、服务卡均默认 `enableLightScrm=true`。
- 后端变化：
  - 普通 `text_note` 默认 `conversionConfig.enableLightScrm=true`。
  - 已热更新到生产，备份：`/home/ubuntu/teamBuy/backend/backups/codex-20260701-134651-scrm-upstream-defaults/skill_router_service.py`。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_skill_router.py -q`：14 passed。
  - `node --check miniprogram/pages/note-edit/index.js`：通过。
  - `node --check miniprogram/app.js`：通过。
  - 小程序 JSON 校验：通过。
  - `git diff --check`：通过。
  - 生产健康检查正常。
  - 生产普通文本卡接口返回 `enableLightScrm=true`。
- 注意：
  - 前端入口要在微信开发者工具重新上传后才会显示。
  - 当前小程序配置仍是生产环境，适合用户本轮重新上传。

## 2026-07-01 本轮交接：生产热更新与小程序生产配置

- 用户要求把本轮房源识别规则上传到后端服务器，并把前端改成生产环境配置，方便重新上传小程序。
- 已完成生产部署：
  - 生产服务器：`81.70.84.35`
  - 项目目录：`/home/ubuntu/teamBuy`
  - 热更新文件：`backend/app/services/skill_router_service.py`
  - 备份文件：`/home/ubuntu/teamBuy/backend/backups/codex-20260701-133204-skill-router/skill_router_service.py`
  - 已执行：`docker cp` 到 `teambuy-backend-1:/app/app/services/skill_router_service.py`
  - 已重启：`teambuy-backend-1`
- 生产验证：
  - `https://teambuy.lifelove.top/health` 返回正常。
  - 生产 `POST /api/skills/content-to-note/run` 用 `珠江好世界B5栋5083 / 租金1980 / 面积40 / 押一付三 / 民水民电 / 密码锁` 验证，返回 `property_listing` 且 `recognitionConfidence.level=high`。
- 前端配置：
  - `miniprogram/app.js` 已切到生产：`apiBaseUrl=https://teambuy.lifelove.top`、`apiRoutePrefix=""`、`mediaRoutePrefix=""`、`environmentName="production"`。
- 已验证：
  - `node --check miniprogram/app.js`：通过。
  - 小程序 JSON 校验：通过。
  - `git diff --check`：通过。
- 注意：
  - 小程序代码已切生产，但还需要用户在微信开发者工具中重新上传体验版/正式版。
  - 当前工作树仍有商机线索、积分账本等既有未提交改动，提交或继续部署时必须区分范围。

## 2026-07-01 本轮交接：单条微信笔记房源识别与编辑单位输入

- 用户指出 `开福区天健一期H栋1205 / 底价1500 / 押一付三 / 民水民电 / 不养宠物 / 密码锁` 被识别成普通笔记且房源分只有 1，规则明显不符合真实租房场景。
- 本轮修复：
  - `backend/app/services/skill_router_service.py` 增强房源识别。
  - `backend/tests/test_skill_router.py` 增加单条微信笔记房源回归。
  - `miniprogram/pages/note-edit/index.js/wxml/wxss` 调整租金和面积输入。
- 后端规则变化：
  - 支持 `【租金】1980`、`【面积】40` 等括号字段。
  - 新增租房口语推断：押付、民水民电、商水商电、独门独户、禁宠、不养宠物、密码锁、底价、租客、空置/搬空、看房、房屋配置。
  - 单条微信笔记命中小区/楼栋/位置 + 租金 + 强租房信号时，可高置信生成 `property_listing`。
  - 房源结构化 `price/area` 统一为数字字符串。
- 前端变化：
  - 房源编辑页“租金”输入框右侧固定 `元/月`。
  - 房源编辑页“面积”输入框右侧固定 `㎡`。
  - 已有带单位的值进入编辑时会提取数字显示。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_skill_router.py -q`：14 passed。
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q -k "property or wecom_archive_process"`：18 passed。
  - `.venv312/bin/python -m compileall -q backend/app backend/tests`：通过。
  - `node --check miniprogram/pages/note-edit/index.js`：通过。
  - 小程序 JSON 校验：通过。
  - `git diff --check`：通过。
- 注意：
  - 本地修改不等于生产生效；如需验证企业微信真实归档，需要部署后端到生产，并上传/预览小程序前端。
  - 当前工作树还有商机线索/积分账本等既有未提交改动，提交或部署时要区分范围。

## 2026-07-01 本轮交接：商机雷达 P0 小程序静态 UI

- 用户要求继续 P0 第三片：小程序商机线索静态 UI，先按 34 号文档和参考图做静态验收，再接接口。
- 本轮新增/修改：
  - `miniprogram/app.json`
  - `miniprogram/pages/profile/index.js`
  - `miniprogram/pages/profile/index.wxml`
  - `miniprogram/pages/opportunity-radar/index.*`
  - `miniprogram/pages/opportunity-market/index.*`
  - `miniprogram/pages/opportunity-detail/index.*`
  - `miniprogram/pages/opportunity-saved/index.*`
  - `miniprogram/pages/opportunity-subscription/index.*`
  - `docs/qa/商机线索静态UI_Codex自测报告.md`
  - `docs/dev-log.md`
  - `docs/decisions.md`
  - `docs/pitfalls.md`
  - `docs/handoff-latest.md`
- 当前状态：
  - 页面使用本地 mock 数据。
  - `我的` 页资源工具里的 `商机线索` 已可点击进入。
  - 我的机会页包含雷达图、匹配度、匹配原因、最高匹配机会和行动按钮。
  - 供需广场、线索详情、已保存、订阅雷达页面已完成静态结构。
- 已验证：
  - 新增 5 个页面和 `profile/index.js` 的 `node --check`：通过。
  - 小程序 `app.json` 和新增页面 JSON 解析：通过。
  - `git diff --check`：通过。
- 未完成：
  - 未接真实接口。
  - 未在微信开发者工具中做静态截图验收。
  - 未做真机复验。
- 下一步建议：
  - 用微信开发者工具进入 `我的 -> 资源工具 -> 商机线索` 做静态截图验收。
  - 静态 UI 过关后，再进入 P0 第四片：接 mock 数据和真实接口。

## 2026-07-01 本轮交接：商机雷达 P0 商机线索数据模型和后台录入

- 用户要求继续 P0 第二片：商机线索数据模型和后台录入。
- 本轮新增/修改：
  - `backend/app/models/domain.py`
  - `backend/app/services/repository.py`
  - `backend/app/services/app_service.py`
  - `backend/app/api/routes_ops_admin.py`
  - `backend/app/api/routes_opportunities.py`
  - `backend/app/schemas/ops_admin.py`
  - `backend/app/schemas/opportunities.py`
  - `backend/app/main.py`
  - `backend/app/static/ops-admin/index.html`
  - `backend/tests/test_app.py`
  - `docs/dev-log.md`
  - `docs/decisions.md`
  - `docs/pitfalls.md`
  - `docs/handoff-latest.md`
- 新增数据结构：
  - `OpportunityLead`
  - `OpportunityLeadSource`
  - `OpportunityLeadContact`
  - `OpportunityLeadMatch`
  - `OpportunityLeadSave`
  - `OpportunityLeadFollowup`
- 新增运营接口：
  - `GET /api/ops/opportunity-leads`
  - `GET /api/ops/opportunity-leads/{lead_id}`
  - `POST /api/ops/opportunity-leads`
- 新增小程序预留接口：
  - `GET /api/opportunity-leads`
  - `GET /api/opportunity-leads/{lead_id}`
  - `GET /api/opportunity-leads/saved`
  - `POST /api/opportunity-leads/{lead_id}/save`
  - `POST /api/opportunity-leads/{lead_id}/followups`
- PC 运营后台新增 `商机线索` 标签页，可人工录入线索标题、摘要、城市、行业、内容、来源和联系方式。
- 前台接口默认只展示 `官方收录`，不展示具体第三方来源，不返回完整联系方式。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q -k "opportunity_lead or resource_wallet"`：8 passed。
  - `.venv312/bin/python -m pytest backend/tests -q`：189 passed。
  - `.venv312/bin/python -m compileall -q backend/app`：通过。
  - PC 后台内嵌脚本语法检查：通过。
  - `git diff --check`：通过。
- 注意：
  - 当前工作树里还有 `miniprogram/pages/note-edit/*` 的房源上游备注前端展示改动，以及用户改过的 `docs/stage2-docs/34-opportunity-leads-ui-spec-v1.md`，不要误判为商机线索第二片代码。
  - 下一步建议进入 P0 第三片：小程序商机线索静态 UI，必须对照 34 号 UI 规格和参考图，不要先接支付、保证金、会员充值或自动联系。

## 2026-07-01 本轮交接：房源编辑页上游备注前端展示

- 用户追问生产会话归档生成的房源里，上游电话在哪里能查到。
- 复查确认：后端已把批量房源/归档识别出的上游电话保存到 `visibilityConfig.privateData.upstreamPhones`，但小程序已生成房源的编辑页此前没有展示完整私密字段。
- 本轮只做前端展示，不改后端解析、存储结构、客户公开接口和生成同款规则。
- 本轮已改：
  - `miniprogram/pages/note-edit/index.js`
  - `miniprogram/pages/note-edit/index.wxml`
  - `miniprogram/pages/note-edit/index.wxss`
  - `docs/dev-log.md`
  - `docs/decisions.md`
  - `docs/pitfalls.md`
  - `docs/handoff-latest.md`
- 新增行为：
  - 房源编辑页在 `房源工作台 -> 编辑资料` 下，如果有 `privateData/privateTags`，展示“仅自己可见：上游备注”。
  - 可展示上游电话、上游微信、上游联系人、中介费、密码锁、看房备注、红包备注、租客限制、来源房源和私密标签。
  - 支持复制全部和复制单项。
  - 页面文案明确“客户页不展示”，并说明不会进入客户预览、公开分享页或生成同款公开资料。
- 已验证：
  - `node --check miniprogram/pages/note-edit/index.js`：通过。
  - 小程序 JSON 校验：通过。
  - `git diff --check`：通过。
- 注意：
  - 这是小程序前端改动，需要用户在微信开发者工具中预览/上传体验版后，真机才会看到。
  - 当前工作树中还有本轮前已存在的商机雷达积分账本等未提交改动，提交时注意区分。

## 2026-07-01 本轮交接：商机雷达 P0 后端积分账本底座

- 当前分支：`codex/version-protection-20260629`，本地已有上一次提交 `842ee4b phase1 property batch showcase and test env`，当前还有未提交改动。
- 用户要求继续二期 P0 开发；本轮按 `docs/stage2-docs/36-opportunity-radar-phase2-dev-checklist.md` 顺序，先做“后端积分账本”。
- 本轮新增/修改：
  - `backend/app/models/domain.py`
  - `backend/app/services/repository.py`
  - `backend/app/services/app_service.py`
  - `backend/app/api/routes_resource_wallet.py`
  - `backend/app/schemas/resource_wallet.py`
  - `backend/app/main.py`
  - `backend/tests/test_app.py`
  - `docs/dev-log.md`
  - `docs/decisions.md`
  - `docs/pitfalls.md`
  - `docs/handoff-latest.md`
- 注意：`docs/stage2-docs/34-opportunity-leads-ui-spec-v1.md` 是用户在本轮前修改的 UI 规格和图片引用，本轮没有改它，提交时要把它作为用户改动单独看待。
- 新增后端数据结构：
  - `ResourceWallet`
  - `ResourcePointLedger`
  - `ResourceFreeQuota`
  - `ResourceUnlockRecord`
- 新增接口：
  - `GET /api/resource-wallet/me?ownerUserId=...`
  - `GET /api/resource-wallet/ledger?ownerUserId=...`
  - `POST /api/resource-wallet/consume`
  - `POST /api/ops/resource-wallet/adjust`
- 当前行为：
  - 用户首次查询钱包时自动发放 100 初始积分，并写入 `grant` 流水。
  - 消费接口支持 `pointsCost`、`freeQuotaType/freeQuotaLimit/periodKey`。
  - 同一用户、同一 `actionType/targetType/targetId` 24 小时内重复调用不会重复扣积分。
  - 免费额度优先消耗，超额后再扣积分。
  - 运营调整接口需要 `X-Admin-Token`，调整后写 `adjust` 流水。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q -k "resource_wallet"`：5 passed。
  - `.venv312/bin/python -m pytest backend/tests -q`：186 passed。
  - `.venv312/bin/python -m compileall -q backend/app`：通过。
  - `git diff --check`：通过。
- 下一步建议：
  1. 做 P0 第二片：商机线索数据模型和后台录入。
  2. 再做小程序静态 UI，必须对照 `docs/stage2-docs/34-opportunity-leads-ui-spec-v1.md` 和三张 `ig_*.png` 参考图。
  3. 静态 UI 过验收后再接 mock/真实接口。
  4. 不要提前做支付、保证金、会员充值、双向收费或自动联系。

## 2026-07-01 本轮交接：批量房源自动生成草稿合集 P0

- 用户提供 `/Users/yiyi/Desktop/房源样本.docx`，其中包含 7 个复杂房源样本：6 个文本批量房源样本、1 个图片房源样本。
- 本轮先做 P0：批量房源解析生成多条房源资料后，同时自动生成一个合集，方便用户整体发客户。
- 用户后续确认本次作为 Bug 修复直接在生产环境验证；生产后端已热更新并重启，公网健康检查正常。
- 本轮已改：
  - `backend/app/services/app_service.py`
  - `backend/tests/test_app.py`
  - `miniprogram/pages/resource-create/index.js`
  - `docs/dev-log.md`
  - `docs/decisions.md`
  - `docs/pitfalls.md`
  - `docs/handoff-latest.md`
- 新增后端行为：
  - 手动批量房源创建 `POST /api/notes/property-batch/create` 会同步生成 `ShowcasePage`。
  - 企业微信客服同步批量房源导入会同步生成 `ShowcasePage`。
  - 企业微信会话归档批量房源导入会同步生成 `ShowcasePage`。
  - 自动合集 `templateId=property_batch_collection`，默认 `draft`，不自动发布。
  - 自动合集条目引用本次生成的全部房源 `UserNote`。
  - 自动合集默认不展示电话/微信，避免公开上游电话。
- 前端变化：
  - 资源创建页批量房源生成成功后，提示“已生成 N 张房源卡和合集”。
- P1 已继续完成：
  - 自动房源合集 `displayConfig.propertyFilters` 会生成区域、户型、价格三组筛选。
  - 公开展示页房源摘要新增 `propertyMeta`。
  - 小程序 `showcase-view` 客户页新增筛选胶囊，支持按区域/户型/价格即时过滤。
  - 筛选只影响当前页面展示，不写回资料、不自动发布。
- P2 已继续完成：
  - 图片资料 OCR 识别结果命中批量房源时，会额外生成多套房源和一个草稿合集。
  - 原图片资料继续保留为 `image_ocr`，只更新 OCR 状态和识别文本。
  - OCR 生成的房源使用 `sourceType=ocr_property_batch`，并保存 `ocrSourceNoteId` 追溯原图。
  - OCR 生成的房源继承原图片封面/媒体，合集继续带 P1 筛选能力。
  - 小程序资料编辑页识别成功后提示“已生成 N 套房源和合集”。
- 已验证：
  - 生产容器内确认 `AppService._create_property_batch_showcase` 已加载，OCR 流程已包含 `ocr_property_batch`。
  - 生产公网 `https://teambuy.lifelove.top/health` 返回正常。
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q -k "property_batch"`：3 passed。
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q -k "ocr"`：5 passed。
  - `.venv312/bin/python -m pytest backend/tests -q`：180 passed。
  - `.venv312/bin/python -m compileall -q backend/app backend/tests`：通过。
  - `node --check miniprogram/pages/resource-create/index.js`：通过。
  - `node --check miniprogram/pages/showcase-view/index.js`：通过。
  - `node --check miniprogram/pages/note-edit/index.js`：通过。
- 生产部署注意：
  - 已备份生产文件到 `/home/ubuntu/teamBuy/backend/backups/codex-20260701-102308`。
  - 本次 Docker 镜像构建卡在系统包更新阶段，最终采用容器内热更新；生产源码目录也已同步。
  - 如果后续容器在镜像未重建前被删除并重新创建，需重新构建或再次同步本次 `app_service.py`。
  - 小程序前端代码已本地配置生产域名，但仍需用户通过微信开发者工具预览/上传体验版后，真机才能看到前端筛选 UI 和成功提示。
- 后续建议：
  - 下一步用 `房源样本.docx` 中 001-006 建立复杂文本解析回归测试，补 `1️⃣/🏠/💕/宅/独厨独卫/字段块` 等规则。
  - 样本 007 单独进入图片 OCR 房源解析回归，不和文本 P0 混在一起。
  - 测试通过前不要部署生产；小程序测试环境需要先完成环境开关，避免开发者工具误打生产 API。

## 2026-06-29 本轮交接：真实小程序分享封面与底部类目边界复查

- 用户真机反馈：微信小程序分享卡片底部显示“未发布的小程序 开发版”，不是预期的“营销推广”；同时指出企业群机器人测试卡片不是普通微信里的真实小程序分享卡片。
- 本轮确认：
  - 企业微信群机器人 `template_card/text_notice` 是企业微信机器人卡片，不等同于用户从小程序转发到微信聊天里的原生小程序分享卡片。
  - 原生小程序分享卡片底部类目/开发版文案由微信客户端根据小程序后台服务类目和版本状态渲染，代码不能直接改成“营销推广”。
  - 代码侧只能控制 `onShareAppMessage` 的标题、路径和封面图 `imageUrl`。
- 本轮已改：
  - `miniprogram/pages/business-card-studio/index.js`
  - `miniprogram/pages/business-card-studio/index.wxml`
  - `miniprogram/pages/business-card-studio/index.wxss`
  - `miniprogram/pages/service-offer-studio/index.js`
  - `miniprogram/pages/service-offer-studio/index.wxml`
  - `miniprogram/pages/service-offer-studio/index.wxss`
  - `docs/dev-log.md`
  - `docs/decisions.md`
  - `docs/pitfalls.md`
- 修复点：
  - 电子名片工作台直接分享时，保存后预生成专属横版名片封面，`imageUrl` 优先使用该封面，不再优先退回头像/二维码。
  - 服务方案工作台直接分享时，保存后预生成专属横版服务方案封面，`imageUrl` 优先使用该封面，不再优先退回普通封面/头像。
  - 资料客户预览页、资料库列表、合集列表/预览已复查：已有标题、路径和分享封面兜底。
- 已验证：
  - 小程序相关 JS `node --check`：通过。
  - 小程序 JSON 解析：通过。
  - `.venv312/bin/python -m pytest backend/tests -q`：171 passed。
  - `git diff --check`：通过。
- 后续人工验收：
  - 用户需要在微信开发者工具上传最新体验版或正式版后，用真机分别从资料、合集、电子名片、服务方案入口触发分享。
  - 若仍显示“未发布的小程序 开发版”，优先检查分享来源是否为开发版，以及微信公众平台后台服务类目/发布状态，而不是继续改页面样式。

## 2026-06-29 本轮交接：PC 后台新增群发渠道映射

- 用户决定：暂时不继续测试小程序入群 `config_id`，优先把外部群机器人日报运营链路跑通，尽快上线。
- 本轮已改：
  - `backend/app/schemas/ops_admin.py`
  - `backend/app/services/ops_console_store.py`
  - `backend/app/api/routes_ops_admin.py`
  - `backend/app/api/routes_wecom.py`
  - `backend/app/static/ops-admin/index.html`
  - `backend/tests/test_app.py`
  - `docs/dev-log.md`
  - `docs/decisions.md`
  - `docs/pitfalls.md`
- 新增 PC 后台能力：
  - `/ops` 左侧新增 `群发渠道映射`
  - 可保存自定义 `groupId`、群名称、群机器人 webhook、群类型、人群说明、城市、日报模板、发送时间、负责人、备注、启停状态。
  - 列表展示 webhook 已脱敏；点列表行可回填编辑基础信息，但不会回显真实 webhook。
- 新增/调整接口：
  - `GET /api/ops-admin/group-bot-channels`
  - `POST /api/ops-admin/group-bot-channels`
  - `GET /api/wecom/group-bot/config` 现在同时读取环境变量白名单和 PC 后台映射。
  - `POST /api/wecom/group-bot/broadcast` 现在可直接使用 PC 后台保存的 `groupId` 作为目标群。
- 重要边界：
  - 这里的 `groupId` 是平台自定义群标识，不是企业微信真实客户群 `chat_id`。
  - 第一版日报运营不依赖 `config_id/chat_id`；用 `groupId -> webhook` 即可分别给不同群发不同运营数据。
  - 真实 webhook 只保存在后端状态文件/生产环境，不写入前端代码或 Git。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q -k "group_bot or bot_channel"`：4 passed。
  - `.venv312/bin/python -m compileall -q backend/app backend/tests`：通过。
  - PC 后台内嵌脚本 `node --check`：通过。

## 2026-06-29 本轮交接：PC 后台可生成小程序加群 config_id

- 用户确认：企业微信后台手动创建外部群二维码后，可能找不到小程序插件需要的 `config_id/plugid`，因此需要系统通过 API 生成。
- 本轮已改：
  - `backend/app/services/wecom_client.py`
  - `backend/app/schemas/ops_admin.py`
  - `backend/app/services/ops_console_store.py`
  - `backend/app/api/routes_ops_admin.py`
  - `backend/app/static/ops-admin/index.html`
  - `backend/tests/test_app.py`
  - `docs/dev-log.md`
  - `docs/decisions.md`
  - `docs/pitfalls.md`
- 新增后端能力：
  - `WecomClient.create_group_join_way()`
  - 调用企业微信 `externalcontact/groupchat/add_join_way`
  - 企业微信成功时返回 `config_id`
- 新增 PC 运营后台接口：
  - `GET /api/ops-admin/wecom-group-join-ways`
  - `POST /api/ops-admin/wecom-group-join-ways`
  - 都要求 `X-Admin-Token: <WECOM_ADMIN_TOKEN>`
- 新增 PC 页面：
  - `/ops` 左侧新增 `小程序加群配置`
  - 可填写客户群 `chat_id`、备注、群名规则、群序号、是否群满自动建群、渠道 state。
  - 可点击 `拉取客户群` 从企业微信读取客户群列表，点选后自动填入 `chat_id`。
  - 支持预览配置和生成 `config_id`。
  - 生成历史保存在 `ops-console-state.json` 的 `wecomGroupJoinWays`。
- 重要边界：
  - 以后不是只能系统建群。手动建群、手动二维码仍然可用。
  - 只有“小程序按钮式加入群聊”这条链路，建议走 PC 后台 API 生成 `config_id`。
  - `chat_id` 不需要在企业微信页面手工找，PC 后台现在可以通过企业微信接口拉取客户群列表。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q -k "customer_groups or join_way or group_join"`：4 passed。
  - `.venv312/bin/python -m compileall -q backend/app backend/tests`：通过。
  - PC 后台内嵌脚本 `node --check`：通过。

## 2026-06-29 本轮交接：企业群机器人群发消息 API 已打通

- 用户要求先把群发消息 API 打通，后续试企业群日常运营效果。
- 本轮已改：
  - `backend/app/core/config.py`
  - `backend/.env.example`
  - `backend/app/api/routes_wecom.py`
  - `backend/tests/test_app.py`
  - `docs/dev-log.md`
  - `docs/decisions.md`
  - `docs/pitfalls.md`
- 新增配置：
  - `WECOM_GROUP_BOT_WEBHOOKS`
  - 格式示例：`{"property":"https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx"}`
  - 真实 webhook 只能放后端环境变量或生产 `.env`，不要写入前端或提交 Git。
- 新增接口：
  - `GET /api/wecom/group-bot/config`
  - `POST /api/wecom/group-bot/broadcast`
- 接口规则：
  - 必须带 `X-Admin-Token: <WECOM_ADMIN_TOKEN>`。
  - `broadcast` 支持 `groupId` 或 `groupIds`。
  - `template` 支持 `midday / afternoon / evening / custom`。
  - 默认 `dryRun=true`，只返回预览内容和目标群，不调用 webhook。
  - 只有显式传 `dryRun=false` 才真实发送。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q -k "group_bot"`：3 passed。
  - `.venv312/bin/python -m compileall -q backend/app backend/tests`：通过。
- 重要边界：
  - 这次打通的是“平台自有企业群机器人 webhook 播报”，不是外部客户群自动群发。
  - 外部群仍由运营本人判断和发送；机器人可生成内容和建议，但不作为外部群直接自动运营主链路。

## 2026-06-28 本轮交接：企业资源搜索 V1（天眼查接入）策划

- 用户计划申请天眼查 API Key，并希望在资源库中增加第二类资源能力：企业资源搜索。
- 已新增文档：
  - `docs/stage2-docs/28-tyc-enterprise-resource-search-v1.md`
  - `docs/qa/企业资源搜索V1_测试清单与验收标准.md`
  - `docs/png/enterprise-resource-search-mockup.png`
- 当前口径：
  - 前台入口名为 `企业资源搜索`，放在资源库中，与群资源库并列。
  - 资源页入口建议采用九宫格，方便后续继续扩 `行业黄页`、`商机线索` 等资源能力。
  - 第一版不暴露天眼查 162 个工具，而是收敛为：企业搜索 + 企业卡 + 6 个高频查询功能。
  - 6 个功能为：基本信息、股东结构、司法风险、经营情况、历史变更、知识产权。
  - 搜索候选免费；基本信息低门槛；深度查询按功能扣积分。
  - 查询结果可保存为企业资源卡；“导出企业摘要卡”放后续。
  - Tianyancha API Key 只记录在服务器环境变量或密钥文件中，前端只调用自家后端接口。
  - 同企业同功能 24 小时优先走缓存，避免重复扣分和浪费额度。

## 2026-06-28 本轮交接：服务场景合集空态改为去资料创建

- 用户最新反馈：
  - 服务场景下，名片和方案应该在资料里，不应该在合集页里出现为创建入口。
- 本轮已改：
  - `miniprogram/pages/showcases/index.wxml`
  - `miniprogram/pages/showcases/index.js`
  - `miniprogram/pages/showcases/index.wxss`
- 本轮重点：
  - 合集页服务空态只保留合集创建主动作。
  - 次动作改为 `去资料里做名片/方案`，跳转资料 Tab 并带上服务场景筛选。
  - 统一心智：资料是单个资产，合集是多个资料的打包页。

## 2026-06-28 本轮交接：首页非房源场景补企业微信助手入口

- 用户最新反馈：
  - 除房源外，其他首页场景好像没有写加企业微信入口。
- 本轮已改：
  - `miniprogram/pages/home/index.js`
  - `miniprogram/pages/home/index.wxml`
  - `miniprogram/pages/home/index.wxss`
- 本轮重点：
  - 助手区副标题改为按场景读取，不再硬写房源说明。
  - 日常资料、团购、服务/商机场景补充完整卡片式 `加企业微信助手` 入口。
  - 房源场景继续使用原来的 `添加房源助手` 主按钮，不重复展示。
  - 首页移除 `后续可扩展 / 已预留` 内部规划文案。
  - 雷达页、合集页、资料编辑页、合集编辑页和标签管理页同步去掉 `预留 / 后续` 这类前台表达。
  - 首页邀请提示、我的页帮助描述和资料编辑页能力状态也同步改为用户视角文案。

## 2026-06-28 本轮交接：我的页顶卡和资料页副标题继续精修

- 用户最新反馈：
  - “我的”页顶部编辑提示仍显得长、丑、重复，真机观感和之前差别不够大。
  - 资料页副标题“房源继续保留关键判断信息，其他资料更轻更好发”太硬，不够顺。
- 本轮已改：
  - `miniprogram/pages/profile/index.wxml`
  - `miniprogram/pages/profile/index.wxss`
  - `miniprogram/pages/library/index.wxml`
  - `miniprogram/static/icons/edit-profile.svg`
- 本轮重点：
  - 顶部身份卡移除头像角标，只保留右上角单个 SVG 修改图标，减少重复编辑提示。
  - 我的页副标题改成“你的资料、资源和消息都在这里”，强化页面心智而不是重复产品名。
  - 资料页副标题和 AI 提示标签改得更软一些，继续保留“房源信息可稍重、普通资料更轻”的产品口径。
- 接下来建议：
  - 真机重点看“我的”页顶部是否终于有明显收口感。
  - 如果还嫌重，下一步优先继续压“当前使用场景”整块，而不是再回头调顶部一句话。

## 2026-06-28 本轮交接：资料页 banner 去房源化，状态筛选改蓝色

- 用户最新反馈：
  - 资料页支持场景切换，顶部 banner 不该继续写房源口径。
  - “发客户状态”选中态不要黑色，整页不需要那么多主色。
- 本轮已改：
  - `miniprogram/pages/library/index.wxml`
  - `miniprogram/pages/library/index.wxss`
- 本轮重点：
  - 顶部 banner 改成更短的“资料直接发客户 / 按场景整理，客户一眼看懂”。
  - AI 提示卡首条标签同步改成通用的“重点资料会保留关键信息”。
  - 发客户状态选中态从黑底改成蓝底，和页面主色统一。

## 2026-06-28 本轮交接：我的页资源库入口去掉“预留”字样

- 用户最新反馈：
  - `行业通讯录` 和 `行业资源` 不应再显示 `预留`，可以改成 `近期开放` 这类更自然的口径。
  - 两者语义要区分：一个更像黄页/找人渠道，一个更像行业资料与机会入口。
- 本轮已改：
  - `miniprogram/pages/profile/index.wxml`
- 本轮重点：
  - `行业通讯录` 和 `行业资源` 的标签都统一为 `近期开放`。

## 2026-06-28 本轮交接：专题从资料主流程收掉，资料详情补返回箭头

- 用户最新反馈：
  - 资料页“更多工具”里的 `专题` 和 `合集` 容易混淆。
  - 资料编辑页里的专题字段存在感也不稳定，如果意义不大应先收掉。
  - 资料详情页需要明确的返回箭头。
- 本轮已改：
  - `miniprogram/pages/library/index.wxml`
  - `miniprogram/pages/note-edit/index.wxml`
  - `miniprogram/components/custom-nav/index.wxss`
- 本轮重点：
  - 资料页去掉 `专题管理` 入口与专题筛选。
  - 资料编辑页标签区只保留标签，不再展示专题相关输入。
  - 资料详情页导航开启返回，且返回按钮视觉更明确。

## 2026-06-28 本轮交接：资料列表轻卡在小屏下改为顶部对齐

- 用户最新反馈：
  - 日常资料列表里，左侧轻设计卡与右侧标题区在真机上出现重叠观感。
- 二次修正：
  - 用户明确要求不要改成顶部对齐，应恢复居中，只给左右两个区域增加安全边距。
- 本轮已改：
  - `miniprogram/pages/library/index.wxss`
- 本轮重点：
  - 列表卡恢复垂直居中。
  - 左封面区和右正文区增加左右 margin，把两块视觉拉开。
  - 标题继续保留断词能力，降低长标题顶坏布局的风险。
  - 进一步把左封面区宽高收小、列间距拉大，让右侧正文不再被虚耗空间挤压。
  - 继续按真机反馈把左封面区加宽到 `138rpx`，并收掉额外 margin，避免可用宽度被 margin 吃掉。
  - 最新修正：轻设计卡 `padding` 改为计入固定盒子，左侧文字图居中，封面固定为 `148rpx`，避免实际盒子溢出到右侧文字区域。

## 2026-06-27 本轮交接：收费与会员策略草案

## 2026-06-27 本轮交接：前四个 Tab 视觉收口第一轮已落代码

- 用户最新确认：
  - 四个 Tab 先按最新效果稿统一风格落地。
  - 我的页只先确定风格，里面 UI 内容和功能后续再单独做。
  - PDF 方案书现在只预留入口心智，不进入当前主功能。
- 本轮已改：
  - `miniprogram/pages/home`：hero 改为代码化 banner，保留雷达图和口号；今日机会、房源助手入口、最近有反馈资料保持不删，只收紧文案和层级。
  - `miniprogram/pages/library`：新增资料页说明卡，统一顶部和列表卡的视觉语言；继续保留房源页信息密度，不做过度压缩。
  - `miniprogram/pages/showcases`：新增合集方案包引导卡，强调“打开小程序查看完整合集 / 支持生成同款 / 导出方案书预留”；合集列表卡改为更稳的右侧操作列。
  - `miniprogram/pages/visits`：雷达页头部收为更短的 AI 提示，加入轻量预留入口语义，建议卡层级更清楚。
  - `miniprogram/pages/profile`：只做风格骨架统一，未重排深层功能。
- 已验证：
  - `git diff --check` 已覆盖上述 10 个前端页面文件并通过。
  - `node --check` 已覆盖 home、library、showcases、visits、profile 五个页面 JS 并通过。
- 接下来建议：
  - 先上传体验版做真机截图，对照这轮参考稿看四个 Tab 的字号、间距、长标题和按钮是否需要二次微调。
  - 如果真机观感过关，再进入下一轮更细的“首页 / 资料 / 雷达”局部打磨，不要先扩大到 PDF 或我的页功能。

## 2026-06-28 本轮交接：雷达页已按效果稿方向单独重做首屏

- 用户指出上一版雷达页与效果稿差距仍大，尤其顶部 banner 不像效果稿。
- 已继续改动：
  - `miniprogram/pages/visits/index.wxml`
  - `miniprogram/pages/visits/index.wxss`
- 本轮重点：
  - 顶部改成真正的雷达 hero，而不是普通说明卡。
  - 左侧是“先看信号，再决定怎么跟”的首屏口号；右侧是雷达图和高意向 / 待跟进 / 复活机会三张信号卡。
  - hero 下方增加 AI 跟进建议提示条。
  - 中部 summary 保留“今天优先跟进”结构，并加软标签；队列区标题和右侧 badge 更接近效果稿。
- 已验证：
  - `git diff --check` 已覆盖雷达页两份文件并通过。
- 还需要：
  - 用户重新在开发者工具/真机看一眼雷达页首屏，如果层级和气质到位，再继续做细调；如果仍偏差，就继续只打磨雷达页，不分散到别页。

- 用户讨论未来是否收月费、免费额度是否给 50 个资源/10 个合集、99 元/月是否可行，以及支付/分销上线时机。
- 已新增策略文档：`docs/stage2-docs/25-pricing-membership-strategy-draft.md`。
- 当前口径：
  - 暂不建议马上开发正式支付系统和分销系统。
  - 先验证四工作台、群资源库、公开页传播、生成同款和客户雷达回访。
  - 免费版早期可以给较宽额度：50 个资料资源、10 个合集/公开页、每月 3 个群资源、最近 7 天基础雷达。
  - 专业版可先用 9.9/19.9 元/月内测价验证付费动作，后续正式价可考虑 19.9/29.9 元/月。
  - 99 元/月适合作为后续 `成交雷达 Pro / 经营增长版`，前提是用户已经感受到高意向识别、用户画像、客户轨迹、沉默复活、资料优化和跟进话术的结果价值。
  - 不建议现在把 `去水印` 当核心收费点，改用 `品牌展示增强 / 专属展示页 / 弱化平台标识`。
  - 分销系统等真实付费和自然推荐成立后再做。
- 注意：
  - 这只是收费策略草案，不是当前支付/分销开发任务。

## 2026-06-27 本轮交接：平台运营群分发 SOP 与每日量化动作

- 用户进一步澄清：群运营难点不在聊天，而在大量群和大量内容之间的低效匹配，以及从手机信息到微信群分发的重复劳动。
- 已确认这是平台内部运营需求，不是面向所有用户的前台功能。
- 已新增文档：
  - `docs/stage2-docs/26-semi-auto-group-distribution-sop.md`
  - `docs/stage2-docs/27-daily-growth-operations-playbook.md`
- 当前口径：
  - 不做个人微信无人值守自动群发。
  - 采用半自动方案：系统/Codex 负责群台账、待分发内容池、推荐群列表、发送记录和复盘；RPA 只做打开清单、切群、打开内容和输入框待确认；人工负责最终判断和发送。
  - 平台运营飞轮表达固定为：`用资源找人，用样板打动人，用雷达留住人。`
  - 每日量化动作围绕 3 件事：补资源、做样板、投渠道，并在晚上做复盘。

## 2026-06-27 本轮交接：群资源库 V1 策划交接

- 用户提出群置换需求：用户手里的商业微信群本身是资源，平台希望以轻量自助方式沉淀群资源池，同时方便进入更多精准商业群做小程序推广。
- 已确认产品方向：
  - 做 `群资源库`，放在“我的”或资源入口，不抢首页成交机会主心智。
  - 搜索优先，不做树形资源目录；前期数据少时用搜索、热词、最近可查看和空结果提交需求承接。
  - 用积分交换代替人工撮合：新用户送 100 分，看一个群二维码扣 30 分，贡献一个群加 20 分，成功进群确认继续加分。
  - 查看次数只代表热度，不代表信用；成功进群确认才是有效信号。
  - 二维码 5 天内自然过期不扣分；超过 5 天且多人反馈失效才扣轻罚。
  - 群类型/用途标签必须支持系统预设 + 用户自定义，避免金融、爱好者群、细分行业等长尾场景被分类卡死。
  - 上传页必须遵循“系统能识别就识别、能点选就点选、尽量少手填”的原则；系统识别二维码可读性/疑似微信/重复，城市、类型、用途、人数、活跃度和有效期尽量点选。
  - 每个群必须设置有效期，默认 5 天，可点选 1/3/5/7 天；页面可展示真实的 `今日新增 X 个群 · Y 个确认可进`。
  - 虚假群下架时追回该群发布/确认奖励，再按级别额外扣罚。
  - 举报不立即奖励，必须确认有效后才给少量积分；恶意举报要限制反馈能力。
  - 规则通知采用站内通知兜底，订阅消息用于重要结果提醒，微信客服用于咨询和申诉解释。
  - 第一版不做充值、不做公开二维码墙、不做人工撮合、不做企业通讯录/爬虫。
- 已新增文档：
  - `docs/stage2-docs/24-group-resource-library-v1.md`
  - `docs/qa/群资源库V1_测试清单与验收标准.md`
- 已归档参考图：
  - `docs/png/group-resource-library-search-mockup.png`
  - `docs/png/group-resource-library-points-mockup.png`
- 后续开发 Codex 接手建议：
  - 先读 `docs/stage2-docs/24-group-resource-library-v1.md` 和 QA 清单。
  - 先做搜索、发布、查看扣分、积分明细、反馈和退分/扣分闭环。
  - 不要扩大到充值、企业通讯录、复杂推荐或群交易。

## 2026-06-27 本轮交接：成交辅助系统 V1 客户雷达与机会提醒

- 用户要求实现“成交辅助系统”方向：不只是资料整理，而是通过客户行为识别成交机会、提醒跟进并提供酷的销售助理感。
- 已新增：
  - `docs/stage2-docs/21-conversion-assistant-opportunity-radar.md`
  - `docs/qa/成交辅助系统V1_测试清单与验收标准.md`
- 后端已完成：
  - `ViewEvent` / `ShowcaseEvent` 增加 `sessionId/durationSeconds/maxScrollPercent/focusSections`。
  - 单条资料和展示页访问支持同 `sessionId` 更新，避免回填停留时间时重复增加 PV。
  - `/api/dashboard/business` 返回 `opportunitySummary/opportunityAlerts/radarProfiles/contentInsights/revivalAlerts`。
  - 规则引擎按咨询动作、多次打开、停留、重点板块、复活访问生成高/中/低意向、解释、建议动作和跟进话术。
  - `get_public_note` 已改为公开脱敏 dict，过滤 `privateData/privateTags/analyticsData/opportunityAlerts/radarProfiles`。
- 小程序已完成：
  - `pages/note-preview` 和 `pages/showcase-view` 生成访问 `sessionId`，进入记录打开，离开回填停留、滚动和重点板块；客户页不显示任何后台判断。
  - `pages/home` 房源模式“今日概览”改为“今日成交机会”，接入机会提醒。
  - `pages/business-dashboard` 增加“客户雷达提醒”和“资料优化建议”，支持复制跟进话术。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q`：112 passed。
  - 小程序关键 JS `node --check` 通过。
  - 小程序 JSON 递归解析通过。
  - 本轮关键文件 `git diff --check` 通过。
- 注意：
  - 还未上传体验版，客户雷达卡、首页机会卡和公开页行为上报需要真机确认。
  - 由于 `miniprogram/pages/business-dashboard/` 当前仍是未跟踪目录，普通 `git diff --stat` 不显示该目录内本轮改动；提交或交接时要特别检查。

## 2026-06-27 本轮交接：首页企业微信主入口收口

- 用户明确：暂时不考虑企业微信给用户发小程序；小程序里添加微信客服相关入口先删掉，首页现在太乱。
- 本轮小程序已改：
  - `pages/home` banner 删除微信客服主入口，改为单一“添加房源助手”。
  - banner 文案收口为“加企业微信后置顶，把群里房源转发给它”；不再写“打开微信客服，整理完回小程序”。
  - 首页只保留一个企业微信「联系我」插件入口。
  - 常用入口“添加房源助手”不再嵌第二个插件按钮，只滚动回顶部并提示点击顶部按钮。
  - `pages/home` 移除 `wx.openCustomerServiceChat` 调用。
  - `pages/property-same` 的助手兜底改为复制指令并提示“发给企业微信助手”，不再打开微信客服。
  - 删除 `miniprogram/config/customer-service.js`，并移除 `services/api.js` 里未使用的微信客服配置接口包装。
- 当前产品口径：
  - 主入口：企业微信成员好友。
  - 收消息：会话存档。
  - 看结果：小程序内新导入资料、自动归属和房源合集。
  - 暂不承诺：企业微信成员好友整理完成后主动回小程序卡片。
- 已验证：
  - `node --check` 覆盖 home、property-same、workspace-mode、api。
  - 小程序 JSON 递归解析通过。
  - 首页 WXML 基础标签计数通过。
  - 关键文件 `git diff --check` 通过。
- 待真机：
  - 上传体验版后确认首页没有微信客服授权页/白屏按钮。
  - 点击顶部企业微信插件应进入“联系我/添加企业微信”流程。

## 2026-06-27 本轮交接：WorkBuddy 企业微信单聊回复方案评估

- 用户提供 `/Users/yiyi/WorkBuddy/2026-06-26-18-42-00/单独回复`，要求验证其“企业微信单聊回复发送小程序卡片”是否可满足房源助手需求。
- 已读文件：
  - `企业微信单聊回复_发送小程序卡片.md`
  - `企业微信发送小程序给微信用户_技术实现.md`
  - `wecom-miniprogram-bot` 全部源码；`wecom-miniprogram-bot 2` 与前者内容一致。
- 已测：
  - `python3 -m py_compile` 通过。
  - 运行 `WecomClient._api()` 会报 `NameError: name 'cgi' is not defined`，代码无法真正发送。
- 核心问题：
  - 方案依赖 `/cgi-bin/externalcontact/message/send`，但本轮官方文档核对未确认其为普通企业微信外部联系人单聊实时回复接口。
  - 官方可确认的能力仍是微信客服 `kf/send_msg`、客户联系群发 `externalcontact/add_msg_template`、新客户欢迎语 `externalcontact/send_welcome_msg` 等，各自有限制。
- 当前结论：
  - WorkBuddy 代码不能直接并入，也不能证明“企业微信成员好友单聊可后端自由回小程序卡片”。
  - 房源助手主入口继续按用户最新判断：企业微信成员好友/会话存档收消息是自然入口；微信客服不适合作为推广主入口。
  - 发送结果卡片仍需单独找并验证官方可用通道，不能用未确认接口硬上。

## 2026-06-26 本轮交接：房源助手首页入口与企业微信完成反馈

- 用户要求：按刚才讨论，把首页添加房源助手入口和企业微信完成反馈闭环整理成文档并开发，开发完部署服务器。
- 已新增开发文档：`docs/stage2-docs/20-property-wecom-assistant-entry-feedback.md`。
- 已新增自测报告：`docs/qa/房源助手首页入口与企业微信反馈闭环_Codex自测报告.md`。
- 小程序：
  - 新增 `miniprogram/static/icons/wechat.svg`。
  - `miniprogram/utils/workspace-mode.js` 房源工作台首个常用入口改为“添加房源助手”，描述收口为“群里房源发给助手”。
  - `miniprogram/pages/home` 保留原首页结构，banner 内增加“房源助手已准备好”轻入口；常用入口标题改为“常用入口”；点击助手入口优先打开企业微信客服，失败时复制提示文案。
- 后端：
  - `ImportNotification` 增加结果路径、动作和发送状态字段。
  - 导入状态 `success/claimed` 都视为完成反馈。
  - 企业微信真实 `sync_msg` 导入完成后尝试用微信客服接口发文本反馈；发送失败不阻断资料生成。
- 已验证：
  - 小程序关键 JS 检查通过。
  - `home/index.wxml` 标签计数通过。
  - 后端关键文件 py_compile 通过。
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q`：109 passed。
  - 本轮关键文件 `git diff --check` 通过。
- 待继续：
  - 部署生产后验证 `/health`、`/api/wecom/customer-service-config`、`/api/wecom/notifications`。
  - 需要用户上传小程序体验版后，真机确认微信图标、首页入口和客服打开效果。
- 生产部署已完成：
  - 备份目录：`/home/ubuntu/teamBuy-deploy-backups/20260626-064103-property-wecom-feedback`。
  - 已热更新容器并重启后端。
  - 首次重启因生产缺少此前本地已有的 `PropertyBatch*` schema 短暂 502，已补同步 `backend/app/schemas/notes.py` 和 `backend/app/api/routes_notes.py` 后恢复。
  - 公网 `/health` 200；`/api/wecom/customer-service-config` 200 且 `configured=true`；`/api/wecom/notifications` 200。
  - 还未触发真实企业微信消息回传；下一步需用户发一条房源给助手验证文本完成反馈。

## 2026-06-26 本轮交接：访客身份分层

- 用户确认：中介生成同款后应该被记录，但不能混进租客客户线索。
- 已落地“访客身份分层”底座：客户、疑似中介、疑似上游。
- 后端 `clone_property_same` 成功后，会给原发布者写入一条 `生成同款` 动作，`visitorIdentityType=peer_agent`，`visitorIdentityLabel=疑似中介`。
- 该动作不会投射为 `LeadReminder`，因此不会增加“待联系客户”和优先联系队列。
- `business-dashboard` 最近访客页已新增 `客户 / 同行 / 上游 / 全部` 筛选，默认显示客户；列表、动作流水、访客详情都展示身份标签。
- 设计口径：这是未来可插件化的“访客身份分层”能力，后续团购/服务/名片也可以复用。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q -k "property_same_clone_note_creates_b_owned_note_with_replaced_contact"` 通过。
  - `.venv312/bin/python -m py_compile backend/app/services/app_service.py` 通过。
  - `node --check miniprogram/pages/business-dashboard/index.js` 通过。

更新时间：2026-06-23
工作目录：`/Users/yiyi/Desktop/Desktop/myprojects/teamBuy`
当前分支：`main`
当前最新提交：以 `git log -1 --oneline` 为准。
本地状态：P1 展示页构建器 V1 已实现并完成 AI 测试官验收报告，但尚未提交；仍需排除微信开发者工具自动改动 `miniprogram/project.config.json` 和未跟踪 PDF `企业微信客服服务须知.pdf`。

最新补充：
- 2026-06-25 用户真机截图反馈 `pages/showcase-view` 展示页预览按钮仍变形：清单模板“查看详情”按钮大面积压住内容，底部“生成同款”按钮超出屏幕，同时房源合集混入普通图片资料。已修复：清单行改为三列 grid（封面 / 内容 / 详情按钮）；“查看详情”和“生成同款”改成普通 `view` 轻按钮，避免原生 button 默认样式撑宽；底部联系条单联系方式时改为单列全宽；展示层按 `displayConfig.activeCategory` 过滤，房源合集只展示 `property_listing`，商品/服务合集也按各自类型过滤。验证：`node --check miniprogram/pages/showcase-view/index.js`、WXML `view/button/text` 计数和 `git diff --check` 均通过。仍需用户重新上传体验版后真机确认。
- 2026-06-25 用户补充三点并已处理：`租房对盘工作台` 前台标题改回 `房源工作台`；资料库房源筛选面板默认收起，用户需要时点“展开”；后续开发后端时必须打通“企业微信接收我们自己的小程序房源卡/房源合集”的完整识别链路。具体要求已写入 `docs/stage2-docs/19-property-agent-growth-mvp.md`：收到我们自己的小程序卡不能像贝壳第三方卡一样只拿标题，必须从 `pagePath` 中识别内部 `noteId/showcaseId/sourceNoteId` 等标识并回查完整公开结构，用于生成同款；但仍不能继承原发布者私密保存的真实房东/二房东/渠道联系方式。
- 2026-06-25 已完成“房源中介对盘群增长 MVP”前端第一版。新增开发文档 `docs/stage2-docs/19-property-agent-growth-mvp.md`、测试清单 `docs/qa/房源中介对盘群增长MVP_测试清单与验收标准.md` 和自测报告 `docs/qa/房源中介对盘群增长MVP_Codex自测报告.md`。小程序现在通过 `miniprogram/utils/workspace-mode.js` 的房源增长模式开关默认进入 `property`，首页不再主动弹四工作台选择，也隐藏“切换工作台”入口；其他日常、团购/商品、服务工作台代码和配置均保留未删除。首页文案已收为“资料整理助手 · 房源版 / 租房对盘工作台”，快捷入口为发房源、生成同款、房源合集、客户反馈。
- 2026-06-25 已新增 `miniprogram/pages/property-same` 生成同款确认页。入口来自首页、房源合集公开页和单套房源客户页。页面支持填写微信号、电话和上游联系人，微信号/电话本地记忆；从 A 发布者进入时，B 的上游联系人默认可带 A 的公开联系方式，但 B 可自行修改。主动作是“复制给企业微信助手”，剪贴板内容包含生成类型、来源编号、来源标题、原发布者、我的微信、我的电话、我的上游联系人和隐私规则。注意：本轮是前端半自动引导，不是后端完整克隆接口。
- 2026-06-25 房源合集公开页 `pages/showcase-view` 已增加“我是中介，也想生成这种合集 / 生成同款”入口，并把房源合集文案偏向租房对盘：清单、租房、微信联系、持续更新。单套房源客户页 `pages/note-preview` 已增加“我是中介，也想生成这张房源卡 / 生成同款”入口，仅 `property_listing` 显示。公开客户页仍不展示“隐藏了房东联系方式”等敏感提示。
- 2026-06-25 本轮验证：`node --check` 覆盖 `workspace-mode`、`home`、`showcase-view`、`note-preview`、`property-same` 均通过；`app.json` 和 `property-same/index.json` JSON 解析通过；WXML `view/button/text` 标签计数通过；关键文件 `git diff --check` 通过。未上传体验版，未真机回归，未实现后端克隆接口和媒体资产 hash 去重落库。
- 2026-06-25 已进入“专门营销细节”讨论，并确认首批推广聚焦房源中介/二房东，不再平均推广四个工作台。对外中介版本建议包装为“资料整理助手 · 房源版”，默认展示房源工作台，隐藏日常资料、团购/商品、服务三个主动切换入口，但保留长期架构。首批主打法是利用用户已有十多个房源对盘群，发真实房源卡和房源合集自然种草；重点不是泛讲 AI/SCRM，而是让中介看到“房源卡/合集比普通群消息更专业，且能换成自己的联系方式再发客户”。第一批主推合集模板定为“清单对比”和“精选橱窗”：前者服务对盘群同行快速扫房，后者服务客户和朋友圈视觉展示。房源合集页和单房源页都应有轻量入口“我是中介，也想生成这种合集 / 生成同款房源卡”，点击后进入生成同款引导页。
- 2026-06-25 已确认“生成同款”隐私边界：A 中介发布的房源卡/合集被 B 中介转发给助手或点击生成同款时，B 只复制公开房源内容、图片和展示结构；B 的对外联系方式替换为 B 自己；B 的上游联系人默认可记录为 A 中介，并允许 B 自行编辑。A 私密保存的真实房东/二房东/渠道联系方式默认不继承给 B，除非后续 A 主动开启明确合作共享。公开客户页不展示“隐藏了房东联系方式”；上游联系人私密保存只出现在发布者管理态或生成同款说明中。这个边界非常关键，否则 A 不敢用系统发房源。
- 2026-06-25 已确认媒体资产去重方向：图片和视频应作为独立 MediaAsset，房源卡和合集只保存引用。上传/转存时先算原始 `sha256`，hash 一样就认为是同一个原始媒体，不重复存储；图片统一压缩转 WebP，视频统一转 H.264/AAC MP4 并生成 WebP 封面；同时保存 `originalSha256` 和 `storageSha256`。第一版先做 sha256 完全去重，相似图片/截图/微信压缩后二次识别以后再考虑 perceptual hash。这个能力既省存储，也能支撑营销卖点“同行转来的房源，不用重新传图，一键生成你的版本”。
- 2026-06-25 已修复“快速新增普通笔记后资料库看不到”的后端口径问题。根因是快速笔记保存为 `UserNote` note-only，而资料库读取 `/api/cards`；此前 `/api/cards` 只额外合成 `business_card/service_offer`，普通 `text_note` 未合成。本轮把 owner 场景下无旧 Card 承载、无 `sourceCardId` 的有效 `UserNote` 都合成为 `note_card_{noteId}` 进入资料库，并将普通笔记、链接、图片 OCR 等基础资料分类归一为自然词。房源 `property_listing`、团购商品 `groupbuy_product`、普通笔记 `text_note` 和服务资料 `service_offer` 均已有 `/api/cards` 合成回归测试。普通笔记详情首屏也已收口为“标题 + 内容”，摘要只在扩展为可运营资料后展示。已验证 `.venv312/bin/python -m pytest backend/tests/test_app.py -q` 为 `100 passed`，`node --check miniprogram/pages/note-edit/index.js` 通过。本轮未部署生产，小程序真机要看到新笔记入库仍需部署后端。
- 2026-06-25 产品方向同步确认：日常资料台是资料管理底座，不默认 SCRM 化；普通资料只保留轻量分享反馈。房源、团购/商品、服务工作台继续承载客户动作、访客、留言、预约、接龙、订单和跟进能力。后续首页和底部 Tab 文案应避免让普通资料用户默认看到“客户看板 / SCRM / 高意向”等业务词。
- 2026-06-24 已继续按房源工作台推进逻辑打磨“团购/商品资料库卡片一期”：`groupbuy_product` 资料卡现在不再只是普通资料卡，而是展示商品状态、价格/规格/提货/截止、接龙/下单/待处理/访客信号，并按状态给出下一步提示。商品卡操作区收口为有动态时“处理接龙 + 分享 + 更多”，无动态时“分享 + 更多”；“处理接龙”优先进入单商品 `pages/note-actions` 接龙/下单名单，即使当前为空也显示该商品名单空态。商品详情顶部工作台名称同步为“团购/商品工作台”。本轮验证通过 `node --check`、JSON 解析和关键文件 `git diff --check`；仍需真机确认商品卡小屏布局、按钮居中和各状态文案。
- 2026-06-24 已完成“团购/商品工作台首页一期收口”：前台名称从“团购工作台”调整为“团购/商品工作台”，避免落入普通商品后台或单一接龙名单心智。首页四指标改为“商品 / 待处理 / 今日接龙 / 访客”；商品数和访客数按 `groupbuy_product` 资料过滤，待处理和今日接龙接入现有卖家订单/接龙接口汇总。四指标点击路径已收口：商品进入资料页商品筛选，待处理进入待处理名单，今日接龙进入 `date=today` 今日名单，访客进入团购看板访客页。资料页新增商品入口筛选，卖家侧订单页标题收为“接龙/买家名单”。本轮只做首页入口层和轻筛选，不扩库存、支付、核销或完整电商后台；仍需真机确认首页指标居中、点击路径和今日接龙筛选是否符合直觉。
- 2026-06-24 用户确认合集新建流程应“模板先行”：先选模板帮助用户具象化理解合集，再新建合集、选择生成方式、选择和确认房源、预览、发布。`pages/showcase-edit` 已按该顺序改造；模板卡已改用既有展示页模板参考图，参考图上传到生产服务器 `/media/showcase-templates/*.webp`，单张约 6-8KB，小程序只引用远程 HTTPS 地址，不放主包。第一版真实支持 `从当前筛选生成` 和 `手动选择`，后续能力改为更贴近房源业务的 `按条件筛选`、`按近期反馈推荐`，暂时只提示下一版开放，不做假入口。资料选择区默认 10 条一批，支持“再显示 10 条”。发布 payload 已保存 `displayConfig.generationMethod`。
- 2026-06-24 继续按用户真机反馈修正房源资料库：房源卡现在 `分享` 常驻，有客户动态时显示 `看客户 + 分享 + 更多`，无客户动态时显示 `分享 + 更多`；资料库房源卡和客户页预览前置 `租金 / 户型`，资料库卡片已改成标题下方第二行，不再是普通胶囊标签。针对旧 Card 列表未带完整 structuredData 的情况，前端已从标题/摘要/详情文本兜底识别租金和户型，识别不到时显示待补。微信转发标题改为两行心智：第一行原始标题，第二行 `租金 · 户型`，不融成一个新标题；同时新增房源专属 canvas 分享封面图，图内固定展示原始标题、租金、户型、面积/位置和房源图，降低微信标题渲染不稳定风险，并先放轻品牌署名 `由资料整理助手生成`。客户页预览从站内进入时显示返回箭头；从微信分享单独打开时，因为没有上一页页面栈，底部提供“回到首页”兜底。合集页已读取当前 `workspaceMode`，房源工作台只突出房源合集，团购突出团购合集，服务突出案例合集，日常突出普通资料包；从合集页新建时会把 mode 传到展示页编辑页，编辑页优先按工作台分类选择资料。自动生成合集 / 一句话生成合集尚未实现，应作为下一阶段核心能力单独设计，不要先做假入口。
- 2026-06-24 用户确认客户看板优先级版本符合预期后，继续打磨房源资料库和详情页。已调整：房源卡保留用户原始标题，不清洗标题里的表情；标题下方由系统整理价格/户型/区域/来源和客户信号；房源卡主操作改为有客户动态时“看客户”、无客户动态时“分享”，编辑/合集/复制/删除收进“更多”。房源详情页上半部主动作收成“转发房源 / 客户页预览”，功能组改为“客户功能”并默认折叠开关，`轻 SCRM` 前台改为“客户反馈”，标签专题下沉为“资料归类”。房源微信转发标题优先使用用户原始标题，保持资料库卡、详情页和微信卡片口径一致。仍需体验版真机确认房源卡视觉和微信转发卡片是否不变形。
- 2026-06-23 用户真机确认房源首页四指标、客户看房和房看客数据已经能对应；随后继续改造 `pages/business-dashboard` 首屏：从 `待跟进 / 新访客 / 咨询预约 / 热门房源` 并列，调整为“优先联系队列 + 客户动态”。优先队列只抬预约、留资、明确咨询和高意向访客；客户动态定位为观察与复盘，用来解释为什么优先、今天谁有动静、哪套房被看得多，不作为第二个待办列表。本轮验证通过 `node --check miniprogram/pages/business-dashboard/index.js`、客户看板 JSON 解析和关键文件 `git diff --check`，仍需用户上传体验版真机确认信息是否更清楚。
- 2026-06-23 已完成“房源首页四指标与客户看板一期”小程序前端实现，未提交、未部署、未上传小程序。
- 房源工作台首页四指标已改为“房源 / 打开 / 访客 / 待跟进”，四个指标可点击：房源进入资料 Tab 的房源筛选，打开进入 `pages/business-dashboard/index?mode=property&tab=propertyEffect`，访客进入 `tab=visitors`，待跟进入 `tab=followup`。
- 客户看板 `pages/business-dashboard` 房源模式默认进入“待跟进”，Tab 为“待跟进 / 最近访客 / 房源效果 / 推荐包效果”；旧 `showcases/notes/customers` 参数已做兼容映射，展示页能力保留为“推荐包效果”。
- 资料页 `pages/library` 已支持房源入口筛选；由于资料页是 tabBar 页面，首页先写入一次性本地筛选再 `switchTab`，资料页读取后默认列表模式展示房源资料，并提供“全部”清除筛选。
- 已补充 `pages/visits` 和非房源模式看板路由，避免日常、团购、服务工作台误进入房源客户看板。
- 新增自测和验收报告：`docs/qa/房源首页四指标与客户看板一期_Codex自测报告.md`、`docs/qa/房源首页四指标与客户看板一期_验收报告.md`，验收结论为“需要人工确认”。
- 本轮验证通过：`node --check miniprogram/pages/home/index.js`、`node --check miniprogram/pages/business-dashboard/index.js`、`node --check miniprogram/pages/library/index.js`、`node --check miniprogram/pages/visits/index.js`、`node --check miniprogram/utils/workspace-mode.js`、`node --check miniprogram/services/api.js`、小程序 JSON 递归解析、`git diff --check`。
- 仍需真机确认：四指标点击路径、客户看板首屏是否显示待跟进或明确空态、匿名访客文案、推荐包效果是否能找到原展示页数据、资料页房源筛选和大屏列表可读性。
- 2026-06-23 追加修正：资料页卡片上的“待跟进 / 客户动态”如果来自新资料 `sourceNoteId` 的客户动作，优先跳转 `pages/note-actions/index?id=sourceNoteId`，不再进入旧 `pages/manager` 访问详情，避免“卡片有待跟进，详情无数据”。
- 资料页顶部四个统计“资料总数 / 当前筛选 / 访客 / 客户动态”暂时只作为概览，不做点击跳转；资料页内的处理入口以单张资料卡的客户动态为准。
- 2026-06-23 已新增 `docs/stage2-docs/18-property-home-customer-dashboard-v1.md`，专门沉淀“房源首页四指标与客户看板一期方案”。
- 该方案明确：展示页在房源场景中仍是“多套房源一起发给客户”的推荐包，不是客户看板本身；客户看板默认应服务“谁来了、看了什么、该联系谁”。
- 房源首页四指标建议为“房源 / 打开 / 访客 / 待跟进”，并要求四个指标都可点击：房源进入资料筛选，打开进入房源效果，访客进入最近访客，待跟进入客户看板默认页。
- 客户看板一期 Tab 建议为“待跟进 / 最近访客 / 房源效果 / 推荐包效果”，默认进入“待跟进”；前台避免使用 SCRM，改用客户看板、客户动态、待跟进、推荐包效果等自然词。
- 下一步开发若做客户看板，应先闭环“看得懂、点得通、知道谁该跟进”，不要优先扩展复杂 CRM、BI、客户画像或企业微信自动推送。
- 2026-06-23 已完成“客户痕迹首页与资料卡曝光优化”：解决新资料客户页打开后首页/资料库统计仍为 0、SCRM/留言只能进详情页才明显的问题。
- 后端新增 `POST /api/notes/{note_id}/view`，客户打开 `pages/note-preview` 后会写入浏览事件；有 `sourceCardId` 的资料归入旧 Card 统计，无旧 Card 的资料按 note 自身统计。发布者自己预览不计入客户打开。
- `list_user_notes` 现在返回每条资料的 `stats` 与 `customerSummary`；旧 `list_cards` 在存在 `sourceNoteId` 时补充新资料客户摘要。
- 首页统计口径调整为“资料 / 打开 / 访客 / 客户动态”，客户动态聚合留资、预约、接龙/下单、咨询等动作；首页“客户动态”列表会优先显示有浏览或客户动作的资料。
- 资料库总览改为资料总数、当前筛选、访客、客户动态；资料卡直接显示“打开 / 访客 / 客户动态”，有待跟进或客户动作时红点/高亮并自动靠前。
- 验证通过：后端专项 `2 passed, 95 deselected`，后端 compileall，小程序关键 JS 检查，小程序 JSON 44 个解析，`git diff --check`。
- 注意：该改动包含后端新接口，生产真机验证需要部署后端；小程序首页/资料库视觉变化需要用户重新上传体验版。当前本地工作区有大量未提交后端改动，本轮未擅自整包部署生产。
- 生产部署补充：2026-06-23 16:20 左右已把后端改动部署到生产。备份目录为 `/home/ubuntu/teamBuy-deploy-backups/20260623-162042-note-view-stats`。标准 `docker compose build backend` 卡在 Debian `apt-get update`，未影响线上旧服务；随后采用容器热补丁方式复制 3 个文件到 `teambuy-backend-1:/app/app/...` 并重启容器。公网 `/health` 200，`POST /api/notes/not_exist_deploy_probe/view` 返回业务级“笔记不存在”，`/api/notes?ownerUserId=user_25ec00a0f0` 已带 `stats/customerSummary`，工作台接口 requester 校验生效。
- 2026-06-23 已完成首页工作台视觉与快捷入口小优化，并新增 `docs/qa/首页工作台视觉与快捷入口小优化_Codex自测报告.md` 和 `docs/qa/首页工作台视觉与快捷入口小优化_验收报告.md`。
- 本轮把首页“今日待处理”统计图标从单字改为短词：日常为资料 / 打开 / 分享 / 资料包，房源为房源 / 打开 / 客户 / 预约，团购为商品 / 打开 / 接龙 / 买家，服务为名片 / 打开 / 咨询 / 预约。
- 快捷开始入口已收口：日常为写笔记 / 存图片 / 存链接 / 建资料包；房源为新建房源 / 记需求 / 房源合集 / 我的名片；团购为新建商品 / 记素材 / 团购合集 / 查看接龙；服务为做名片 / 做方案 / 写笔记 / 案例合集。
- 房源、团购、服务均保留普通资料创建入口；顶部右侧已接入 4 张 240×240 本地 PNG 插画，路径为 `miniprogram/static/workspace/`，总大小约 240KB。验收结论为“需要人工确认”，需用户上传体验版后确认两字 / 三字标签居中、不截字、插画不破图和点击路径。
- 2026-06-23 已新增 `docs/qa/首页与Tabbar工作台模式一期_复测与回归报告.md`，结论为“需要人工确认”：P0 代码和自动化证据已闭环，但缺微信开发者工具 / 真机体验版 UI 截图，不能直接判定通过。
- 复测已确认 BUG-01 / BUG-02 在代码与自动化层面闭环；复测命令通过：关键 JS 检查、44 个小程序 JSON 解析、`git diff --check`、权限专项 `7 passed, 89 deselected`、后端主测试 `96 passed`。
- 当前可以进入最终人工确认；真机重点看业务识别提示卡两个按钮是否出现并居中、点击切换后首页 / 工作台是否按新模式展示，以及五个 Tab 是否无白屏。
- 2026-06-23 已按 `docs/qa/首页与Tabbar工作台模式一期_Bug修复任务单.md` 优先修复两个 P0，并新增 `docs/qa/首页与Tabbar工作台模式一期_Bug修复报告.md`。
- BUG-01 已闭环：`pages/resource-create` 的业务识别提示卡新增“继续当前工作台 / 切换到对应工作台”双选；房源映射 `property`，商品/团购映射 `groupbuy`，服务方案/电子名片映射 `service`；切换只保存本地 `workspaceMode`，不改资料 owner、不删除资料。
- BUG-02 已闭环：`GET /api/dashboard/business` 新增 `requesterUserId` 校验，owner 可读，非 owner 返回 403，匿名缺身份返回 401；小程序 `fetchBusinessDashboard` 默认携带当前 owner 作为 requester。
- 权限专项证据已补：工作台总览、展示页效果、单条资料互动均覆盖 owner / 非 owner / 匿名访客；专项测试 `7 passed, 89 deselected`，后端主测试 `96 passed`。
- 本轮验证已通过：小程序全量 JS 检查、JSON 递归解析 44 个、`git diff --check`。
- P1 未本轮展开：最近反馈按类型筛选、资料页按 `workspaceMode` 推荐、合集页动态推荐、`workspaceMode` 后端持久化；已在 Bug 修复报告说明原因。
- 2026-06-23 验收官已输出 `docs/qa/工作台第一期_验收报告.md`，结论为“不通过”；Codex 已重新自测并新增 `docs/qa/工作台第一期_Codex重新自测报告.md`。
- 重新自测报告当时结论同为“不通过”；随后本轮已修复 P0-23 和 P0-27，真机主链路仍需上传体验版后确认。
- 已补充证据：小程序全量 JS 检查通过、JSON 递归解析 44 个通过、`git diff --check` 通过、后端权限相关专项测试 `8 passed, 88 deselected`。
- 原关键风险已修复：`GET /api/dashboard/business` 已新增 `requesterUserId` 校验，不能再只靠前端入口隐藏保护权限。
- 2026-06-23 已根据 `docs/stage2-docs/17-home-tabbar-workspace-mode.md` 新增 `docs/qa/首页与 Tabbar 工作台模式一期_测试清单与验收标准.md`。
- 该清单覆盖 5 个 Tab、首次选择常用工作台、`workspaceMode` 保存和切换、首页按模式变化、工作台按模式变化、客户看板从“我的”迁出、资料库降噪、合集页轻版入口和空态、模式切换不删除资料、普通用户不暴露经营词等 P0。
- 2026-06-23 已完成首页 / Tabbar / 工作台模式一期代码实现：Tabbar 为：首页 / 资料 / 合集 / 工作台 / 我的。
- 新增 `miniprogram/utils/workspace-mode.js`，本地保存 `workspaceMode`，支持日常资料台、房源工作台、团购工作台、服务工作台。
- 首页首次进入显示“你想先整理哪类资料？”，选择后按模式展示今日待处理、快捷开始、最近成果和最近反馈。
- `pages/visits` 已归位为“工作台 / 反馈中心”，普通资料台显示分享效果，业务模式显示客户看板、接龙看板或咨询看板，并复用原访客 / 互动数据。
- “我的”已移除经营区域和经营看板入口，新增常用工作台设置；`pages/showcases` 已作为“合集”Tab，前台叫资料包 / 合集。
- 本轮自测报告：`docs/qa/首页Tabbar工作台模式一期_Codex自测报告.md`。仍需用户重新上传体验版后真机确认 Tabbar 和首次模式选择。
- 2026-06-23 已修正“我的笔记资料详情”底部标签与专题默认房产化问题：非房源资料会过滤“房产 / 房源 / 租房 / 万家丽 / 公寓”等旧默认上下文，标签和专题输入示例改为通用客户资料场景。
- 该修正涉及 `miniprogram/pages/note-edit/index.js`、`miniprogram/pages/note-edit/index.wxml` 和 `miniprogram/utils/note-display.js`；房源资料仍保留房产标签 / 专题建议，用户手动添加的标签通过 `userTags` 保留。
- 验证已通过：`node --check` 针对 `note-edit`、`note-display`、`notes`，旧房产 placeholder 扫描，`note-edit` 核心 `px` 扫描，以及 `git diff --check`。
- 2026-06-23 已新增项目专属运营策划 Skill：`skills/operation-planning/SKILL.md`，并生成 `skills/operation-planning/agents/openai.yaml`。
- 该 Skill 用于后续讨论资料整理助手的运营方向、目标用户、冷启动、私域推广、销售页转化、商业化包装、上线节奏和运营复盘。
- 运营策划的核心飞轮已固定为：资料入库 -> 结构化成资料卡 -> 生成销售页/展示页 -> 发给客户/群 -> 客户打开、咨询、留资、预约、接龙 -> 发布者处理跟进 -> 复用资料、模板和话术再次发出。
- 后续任何运营建议都必须先校准当前产品阶段和真实可用能力；不要把演示数据、mock 链路或未真机验收页面包装成稳定上线能力。
- 本轮未修改后端和小程序业务代码；Skill 校验已通过：`/tmp/teambuy-py312-test/bin/python /Users/yiyi/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/operation-planning`。
- 2026-06-22 用户基于新的服务方案参考图要求“整块重做”，本轮已不再沿旧半成品页面补丁式迭代，而是直接重写 `miniprogram/pages/service-offer-studio/index.js`、`index.wxml`、`index.wxss`。
- 服务方案工作台现在按 3 段主链路组织：模板选择、资料填写、详情页效果确认；模板选择页改成 4 套模板清单 + 当前模板完整首屏预览，确认页改成客户详情页主视角预览。
- `miniprogram/utils/sales-page-templates.js` 已补 4 套服务方案模板的预览元信息，服务方案模板不再只有标题和几条标签。
- `pages/note-preview` 中 `service_offer` 客户页已同步重做：咨询预约 / 服务报价 / 案例背书 / 活动招募四类模板按不同首屏和内容结构展示，并新增底部主动作条（电话、微信、预约、留资）；邮箱和站内消息下沉到联系区。
- 代码侧静态检查已通过：`node --check miniprogram/pages/service-offer-studio/index.js`、`node --check miniprogram/pages/note-preview/index.js`、`node --check miniprogram/utils/sales-page-templates.js`、`git diff --check`。
- 尚未完成：用户重新上传体验版后的真机验收，重点看模板选择页布局、咨询/报价/案例/活动 4 套模板视觉差异、详情页底部动作条、案例图滚动和分享卡片。
- 2026-06-22 已新增服务方案销售页 V1 独立工作台：`miniprogram/pages/service-offer-studio`，流程为选风格、填方案、确认效果、保存分享。
- “添加 -> 服务方案”现在直接进入服务方案工作台；旧模板页里选择服务方案模板也会跳转到新工作台；已有 `service_offer` 在笔记编辑页通过“设置方案样式”进入，不再把模板预览塞进笔记编辑页。
- 服务方案工作台复用现有 4 个模板：咨询预约、服务报价、案例背书、活动招募；同一份服务内容可切换风格，不清空字段。
- 服务方案字段已拆成电话、微信、邮箱、公司网址 / 介绍链接；保存时继续写入 `UserNote.visibilityConfig.structuredData` 和 `conversionConfig`，默认启用电话、微信、留资、预约、轻 SCRM，关闭团购接龙和支付预留。
- `pages/note-preview` 中 `service_offer` 已新增专属客户页结构，突出服务标题、卖点、适合人群、服务内容、流程 / 保障、报价说明、案例图片、联系与预约，不再套普通销售页。
- 服务方案分享卡新增运行时 canvas 封面生成，复用 `business-card-share.js` 的隐藏画布能力，不新增本地图片资源；无上传封面时也能生成模板化服务方案分享图。
- 本轮验证：小程序相关 JS 检查、全量 JS 检查、小程序 JSON 解析、`git diff --check` 通过；后端服务方案 / 电子名片专项测试 `2 passed, 94 deselected`。前端效果仍需用户重新上传体验版后真机确认。
- 2026-06-22 已新增电子名片与服务方案模板库 V1：`miniprogram/utils/sales-page-templates.js` 定义 8 个模板，电子名片包括专业顾问、门店名片、专家介绍、简洁微信风；服务方案包括咨询预约、服务报价、案例背书、活动招募。
- 电子名片和服务方案模板选择已收口到各自独立工作台；“添加”页入口直接创建并进入对应编辑页。
- 用户真机反馈第一版模板选择页太像线框、看不到模板、底部按钮变形；已重做为 8 套放大的中文销售页预览，去掉 `serviceHero` 等内部模块名，按钮宽度和文字居中均使用 `rpx + flex` 控制。
- 用户进一步确认电子名片首屏应像参考图一样精美，模板预览、资料库名片卡、编辑页首屏、客户页首屏应保持一致；已统一为“圆形头像 + 姓名 + 身份胶囊 + 公司/门店 + 服务范围 + 电话/微信”的名片母版。模板选择页显示样板信息，创建后自动换成用户自己的信息。
- 微信聊天里的小程序分享卡片不是客户页 WXML，已单独补电子名片分享封面：客户页加载后用隐藏 canvas 生成横版名片封面图，`onShareAppMessage.imageUrl` 优先使用该封面；编辑页直接分享时标题和头像也按名片信息兜底。
- 使用模板创建时会写入模板默认字段、模板名称、模板场景和模板 tone；编辑页顶部显示所选模板名。
- `note-preview` 客户页已接入模板化销售页：电子名片突出人、身份、服务标签和联系方式；服务方案突出服务标题、卖点、适合人群、流程、报价、案例和行动按钮；两类均不显示商品 SKU、接龙下单或房源地图模块。
- 新增 8 模板总览图：`docs/png/business-card-service-offer-template-library.svg`。
- 生产后端已部署新版，备份路径：`/home/ubuntu/teamBuy-deploy-backups/20260622-055345-business-card-service-offer`；公网 `business_card` / `service_offer` 创建探针均已从“类型不支持”变为业务级“用户不存在”。
- 新增统一验收报告：`docs/qa/电子名片与服务方案统一验收报告.md`。本轮验证：后端全量测试 133 passed，小程序全量 JS 检查、小程序 JSON 检查、`git diff --check` 和公网 `/health` 均通过。
- 2026-06-22 已新增电子名片与服务方案卡 V1：后端支持 `business_card` 和 `service_offer`，小程序添加入口可创建“电子名片 / 服务方案”，资料库可筛选和展示，编辑页和客户页已分型渲染；两类默认复用咨询、留资、预约沟通、站内留言和轻 SCRM，不启用商品 SKU、下单、接龙或支付。
- 本轮新增设计说明 `docs/stage2-docs/16-business-card-service-offer.md`，以及 4 张黑白线框原型 SVG：`docs/png/business-card-edit-wireframe.svg`、`docs/png/business-card-preview-wireframe.svg`、`docs/png/service-offer-edit-wireframe.svg`、`docs/png/service-offer-preview-wireframe.svg`。
- 本轮新增自测报告 `docs/qa/电子名片与服务方案卡V1_Codex自测报告.md`。验证结果：后端全量测试 133 passed，小程序全量 JS 检查、小程序 JSON 检查、后端编译检查和 `git diff --check` 均通过。
- 待用户上传最新小程序体验版后，真机验收创建电子名片/服务方案、客户页分享、电话咨询、微信咨询、留资、预约沟通，以及经营看板/待联系回流。
- 2026-06-22 开始“迁移链路小收口 V1”：不做大型工作台，只在“我的笔记”补顶部“最近迁入”概览和单条资料来源/状态标签；顶部迁移卡新增“处理第一条”；普通笔记无业务候选时显示“普通笔记”且不计入待处理，详情页默认是轻量笔记器；有房源/商品候选时，可直接“整理成房源 / 整理成商品”，复用后端 `confirm-type` 进入对应工作台；新增 `docs/qa/迁移链路小收口V1_测试清单与验收标准.md`。
- 2026-06-22 P1 展示页效果页已优化“分享批次”展示：每个批次显示“第 N 次发给客户”、状态标签“已发出 / 已打开 / 看过资料 / 已有咨询”、打开/看资料/咨询三项指标，并保留批次尾号方便和经营看板对照；单展示页效果页和展示页列表折叠效果面板里的资料点击排行，可直接进入对应资料客户动作页；最近访客可进入客户库自动搜索。
- 2026-06-22 P1 小优化一次性收口：客户库、待联系、订单页支持 URL 带入来源/状态/搜索筛选；客户库“当前筛选”可跳到同来源待联系或订单；展示页编辑保存增加名称、简介、分享标题和联系文案兜底。
- 2026-06-22 用户真机确认经营闭环 P0 通过：头像、经营看板下钻和客户详情信息“都能看到了”。`docs/qa/经营闭环头像与处理链路_验收报告.md` 已改为通过；该 P0 可关闭。
- 2026-06-22 已新增真机验收记录模板：`docs/qa/经营闭环头像与处理链路_真机验收记录模板.md`。下一次用户上传最新体验版后，可直接按模板记录头像、经营看板下钻、客户处理卡、客户库、待联系、订单/接龙的通过/不通过结果。
- 2026-06-22 已补后端经营看板头像字段来源：`latestActions` 返回 `avatarUrl`，客户动作合并到 `visitorProfiles` 时保留 `payload.avatarUrl`，并新增测试断言保证客户动作头像能进入看板。
- 已部署该后端补丁到生产，备份路径：`/home/ubuntu/teamBuy-deploy-backups/20260622-015958-dashboard-avatar-flow`；注意后端代码在 Docker 镜像内，单纯 `scp + restart` 不生效，已执行 `docker compose build backend && docker compose up -d backend` 重建镜像。
- 公网 `/health` 正常；生产容器内文件已确认包含 `latestActions.avatarUrl`；真实账号 `user_25ec00a0f0` 字段体检通过：`latestActions` 全量带 `avatarUrl` key，`visitorProfiles` 全量带头像字段，并含联系方式、展示页、分享来源、资料点击下钻字段。
- 本次补充验证更新：后端全量测试 131 passed，小程序全量 JS 检查、小程序 JSON 解析、后端 compileall、`git diff --check` 均通过。
- 2026-06-22 对经营闭环目标做完成度审计时，补齐经营看板“客户资料”主客户卡真实头像展示：有 `primaryCustomer.avatarUrl` 时显示图片，无头像时显示彩色文字兜底。
- 新增验收报告 `docs/qa/经营闭环头像与处理链路_验收报告.md`，结论为代码侧通过、真机侧需要人工确认。
- 本次补充验证：经营闭环相关后端专项测试 5 passed，`node --check miniprogram/pages/business-dashboard/index.js` 通过，小程序 JSON 解析通过，`git diff --check` 通过。
- 2026-06-22 继续复核经营闭环目标时，后端全量测试发现旧头像断言与当前规则不一致：空头像不再强行写入 `payload.avatarUrl/customerAvatarUrl`，前端统一彩色文字兜底；已更新测试断言和文档。
- 最新验证：经营闭环专项测试 8 passed，后端全量测试 131 passed，小程序全量 JS 检查、小程序 JSON 解析、后端 compileall、`git diff --check` 均通过；生产 `/health` 正常。
- 2026-06-22 已补个人资料设置 V1：后端新增 `PATCH /api/auth/users/{user_id}/profile`，小程序“我的 -> 编辑资料 / 设置中心”可编辑昵称、手机号、头像链接，并支持微信 `chooseAvatar`。
- 该能力用于解决真机登录后头像白块问题：没有真实头像时仍显示彩色首字兜底，保存后同步后端、本地缓存和 `app.globalData.currentUser`。
- 资料设置后端已部署生产，备份路径：`/home/ubuntu/teamBuy-deploy-backups/20260622-001727-profile-settings`；公网 `/health` 200，新接口用不存在用户验证返回业务级 404“用户不存在”。
- 本轮验证：个人资料专项后端测试 4 passed，后端全量测试 129 passed，后端 compileall、小程序全量 JS 检查、小程序 JSON 解析、`git diff --check` 均通过。
- 2026-06-22 已补头像上传托管保护：`chooseAvatar` 返回本机临时路径时，保存资料前先调用 `/api/uploads/asset` 上传成生产 URL；后端拒绝保存 `wxfile/file/blob/tmp` 等非 HTTP 头像路径。生产备份路径：`/home/ubuntu/teamBuy-deploy-backups/20260622-003526-profile-avatar-upload-guard`；公网已验证真实测试用户传 `wxfile://tmp_avatar.jpg` 返回 400“头像地址必须是 HTTPS 地址”。
- 2026-06-22 已进一步收紧头像规则：后端和小程序头像展示均只接受 `https://`，`http://` 会被拒绝或前端兜底为彩色首字头像。生产备份路径：`/home/ubuntu/teamBuy-deploy-backups/20260622-004456-profile-avatar-https-only`；公网已验证真实测试用户传 `http://cdn.example.test/avatar.png` 返回 400。
- 注意：小程序资料编辑弹窗和头像上传托管属于前端变化，仍需用户在微信开发者工具重新上传体验版后才能真机看到。
- 2026-06-22 已补经营看板下钻体验：顶部“打开 / 访客 / 看资料 / 咨询”四个总数现在会切到访客详情并带筛选；“按展示页拆解”和“分享来源”每行点击也会筛出该展示页或该分享批次带来的具体访客。
- 访客详情页新增“当前筛选”提示卡和当前筛选下的小统计，避免用户看不懂当前列表来自哪个来源。该改动为小程序前端，无需后端部署，但需要重新上传体验版。
- 2026-06-22 已补经营看板动作流水跟随筛选：访客详情页的“动作流水”现在使用当前筛选下的相关客户动作，不再在筛选某展示页/分享来源时混入全量动作。
- 2026-06-22 已补客户库和待联系的“当前筛选”提示：客户库显示当前来源/阶段/意向/标签/搜索组合和客户数量，并可一键回全部；待联系显示当前来源/状态/时间组合和线索数量，并可一键回待联系。该改动同样只需重新上传小程序体验版。
- 2026-06-22 已补订单/接龙页“当前筛选”提示：商家订单中心点状态或来源商品后，会显示当前来源/状态和订单数量，列表落实到具体买家；来源商品右侧“全部”可恢复全部订单。该改动为小程序前端，无需后端部署。
- 2026-06-21 已按 `/Users/yiyi/Desktop/Desktop/myprojects/cloud_tencent/cloud_tencent.md` 完成经营看板生产后端部署，`AGENTS.md` 已补腾讯云生产部署约定。
- 生产部署信息：服务器 `ubuntu@81.70.84.35`，目录 `/home/ubuntu/teamBuy`，域名 `https://teambuy.lifelove.top`，SSH key `/Users/yiyi/Desktop/Desktop/vedo-project/vidoekey.pem`。
- 部署前备份：`/home/ubuntu/teamBuy-deploy-backups/20260621-081845-dashboard-closeout`。已完整重建并重启 `teambuy-backend`。
- 公网验证：`/health` 200；`/api/dashboard/business?ownerUserId=user_test` 已变为业务级“用户不存在”，不再是路由级 404；`/api/orders?userId=user_test&role=seller` 返回 200 空列表。
- 已在服务器写入独立演示用户真实数据：`user_836a4a8986`。该数据不是前端 mock，而是通过后端接口写入真实数据库，包含 4 条资料、1 个已发布展示页、5 条展示页事件、留资/预约和 1 条待处理接龙。
- 演示用户经营看板验证结果：打开 2、访客 2、看资料 1、咨询 2、订单 1；展示页 analytics 和订单列表均可返回。
- 用户随后确认可在生产库真实账号写入测试假数据，正式上线前再清理；已向 `user_25ec00a0f0` / openid `oPSh564GCACiIkZxFPV5VWVgdbds` 写入一组经营看板演示数据。
- 真实账号写入后验证：展示页打开 2、访客 2、看资料 1、咨询 2、待联系线索 2、客户资料 4、订单/接龙 3、展示页总数 13，其中已发布 5。
- 用户反馈真机经营看板 UI 与参考图差异明显；已将 `pages/business-dashboard` 从功能型看板改为更接近参考图的四个页面结构，并对测试头像域名做前端兜底，避免显示空白头像。该改动需要重新上传小程序体验版后才能在真机看到。
- 已把本次教训写入项目规则：`AGENTS.md` 新增“UI 参考稿与实现一致性要求”，`docs/pitfalls.md` 新增“不能把高保真参考图做成简化功能版”，`docs/decisions.md` 明确 UI 参考图必须作为验收标准。后续有参考图的页面，不能只交功能版。
- 用户反馈经营看板 owner 视角不应脱敏手机号/微信号，且需要外呼和复制按钮；已在 `pages/business-dashboard` 去掉电话脱敏，并在访客轨迹、笔记数据、客户资料、客户旅程、跟进记录等位置加外呼/复制微信按钮，同时修正“添加跟进 / 备注”按钮文字上下居中。
- 新增复测报告：`docs/qa/客户数据看板_复测与回归报告.md`。原 P0“生产后端未部署经营看板接口”已解决；剩余事项是小程序体验版上传和真机确认。
- 2026-06-21 用户要求把后续计划 1-4 一次收口：上线收口、P0 回归、线索闭环、商品/团购轻订单体验。
- 本轮已完成代码侧收口：经营看板最新客户动作可点击进入线索详情/订单详情/笔记客户动作页，资料点击排行可进入笔记客户动作页；商品/团购订单页新增汇总和状态筛选；商品接龙/下单名单页可直接标记已联系、已完成或取消；订单详情显示“类型”区分下单/接龙。
- 已新增生产部署与真机回归清单：`docs/qa/客户数据看板_上线部署与回归清单.md`。有服务器权限的环境可按该清单部署后端并执行公网/真机回归。
- 已新增服务器端命令模板：`docs/deploy/dashboard-closeout-server-commands.sh`。该脚本不负责从本机同步代码，假设新代码已在服务器 `/home/ubuntu/teamBuy`，然后执行备份、文件检查、重建重启和公网验证。
- 本轮验证通过：`/tmp/teambuy-py312-test/bin/python -m pytest backend/tests/test_app.py -q` 为 76 passed；`/tmp/teambuy-py312-test/bin/python -m pytest backend/tests -q` 为 113 passed；小程序全量 JS 检查、小程序 JSON 解析、后端 compileall、`git diff --check` 均通过。
- 生产部署已完成：公网 `/api/dashboard/business` 不再是路由级 404。真机若仍为空面板，优先确认小程序是否已上传最新体验版，以及当前登录用户是否已有真实展示页/访客/订单数据。
- 2026-06-21 07:20 左右真机“我的数据加载失败”已定位：当时生产 `https://teambuy.lifelove.top/api/dashboard/business?ownerUserId=user_test` 返回路由级 404，说明新增经营看板后端接口尚未部署到生产；旧前端用 `Promise.all` 导致新增接口失败拖垮“我的”页整体加载。该生产接口问题现已通过部署修复。
- 已调整小程序端入口：`pages/profile/index.js` 不再自动加载经营看板，只加载基础资源统计和消息未读；经营看板主要入口放到“访客线索”页。
- 本轮验证通过：小程序全量 JS 检查、小程序 JSON 解析、后端 compileall、`git diff --check`。
- 当前已可按 `AGENTS.md` 的腾讯云生产部署约定登录服务器；经营看板后端已部署。若真机仍异常，优先检查体验版是否已更新。
- 2026-06-21 客户数据关系与经营看板已开发：新增文档 `docs/stage2-docs/14-customer-data-dashboard-architecture.md`，明确 `ShowcaseEvent / CustomerAction / LeadReminder / 商品轻订单` 的关系。
- 新增后端 `GET /api/dashboard/business?ownerUserId=xxx`，统一聚合展示页打开、访客、资料点击、咨询、待联系线索、商品下单/接龙、最近访客、资料点击排行和最近客户动作。
- 新增小程序复用组件 `miniprogram/components/business-dashboard/`，当前保留为后续嵌入式看板备用，不再放在“我的”页头像下方。
- 新增 `miniprogram/pages/business-dashboard/index` 经营看板详情页，包含展示页效果、访客详情、笔记数据、客户资料四个 Tab；底部 Tab 的“访问记录”已调整为“访客线索”，页面顶部进入经营看板，线索、订单/接龙、客户库、展示页管理只作为详情页内处理按钮。
- 看板 UI 不展示“行为强度分层”等内部概念，只展示真实事实；匿名访客只显示匿名，不包装成实名客户。
- 新增 QA 文档：`docs/qa/客户数据看板_测试清单与验收标准.md`、`docs/qa/客户数据看板_Codex自测报告.md`、`docs/qa/客户数据看板_验收报告.md`；验收结论为“需要人工确认”。
- 新增后端测试代码 `test_business_dashboard_aggregates_real_customer_data`，但当前本机无法实际跑 pytest：系统 Python 缺少 pytest，`.venv` Python 3.9 会因 `dataclass(slots=True)` 失败；Codex Python 3.12 缺少 pytest，挂载 `.venv` 依赖会因 `pydantic_core` 二进制不兼容失败。
- 本轮验证：后端 compileall 通过、小程序全量 JS 检查通过、小程序 JSON 解析通过、`git diff --check` 通过。
- 2026-06-21 展示页真实效果追踪已开发：新增 `showcase_events`，记录 `view / note_click / phone_click / wechat_copy / share` 五类真实事件；发布者预览不记录，公开客户页才记录。
- 后端新增 `POST /api/showcases/{id}/events` 和 `GET /api/showcases/{id}/analytics?ownerUserId=xxx`；analytics 只允许 owner 查看，匿名访客只统计数量。
- 展示页列表接口已返回轻量 `analytics`；小程序展示页列表卡片显示 `打开 X · 访客 Y · 咨询 Z`，已发布项的 `更多` 菜单新增 `效果`，展开后可看打开、访客、看资料、咨询、最近访客和资料点击排行。
- 展示页开发文档和 QA 清单已更新：真实效果追踪列入本阶段 P1；四模板多尺寸视觉回归、商品/房源混合资料模板策略列为 P2 暂缓，等真实上线反馈再开发。
- 本轮验证：小程序全量 JS 检查通过、小程序 JSON 解析通过、后端 Python 3.12 编译通过、`git diff --check` 通过；pytest 未执行，当前 Codex runtime 缺少 pytest。
- 2026-06-21 展示页列表卡片操作区已减负：已发布只常驻 `发给客户 + 更多`，草稿/下架只常驻 `编辑 + 更多`，`预览 / 删除` 放入更多菜单，删除仍二次确认；右侧按钮区收窄，避免继续挤压标题和简介。
- 客户展示页已去掉虚假营销数字：不再展示 `328+ 服务客户`、`128+ 成交案例`、`98% 好评率`，改为真实可解释的 `资料数量 / 最近更新 / 咨询方式`。
- 品牌名片模板已移除虚构客户评价卡；清单目录模板移除不可用的搜索/筛选按钮；橱窗和品牌模板的 `更多/查看更多` 改为 `共 X 条`。
- 本轮验证：小程序全量 JS 检查通过、小程序 JSON 解析通过、后端 Python 3.12 编译通过、`git diff --check` 通过。
- 2026-06-20 根据用户最新反馈收口展示页编辑体验：展示方式入口已删除，保存时固定 `displayConfig.groupBy=tag`，前期只给用户默认按标签分组。
- 展示页客户页资料卡标签已改为等分网格，最多展示 4 个标签；1/2/3/4 个标签都会自动铺满卡片宽度并保留间距，避免 4 标签挤压变形。
- 自动生成信息已跟随分类语义：房产/房源生成房源标题和说明，商品/团购/电商/好物生成商品标题和说明，用户手动改过的文案不会被强行覆盖。
- banner 配置区已移除图片地址输入框，只显示图片缩略图和“换图片”按钮。
- 删除展示页新增 `POST /api/showcases/{id}/delete`，小程序删除按钮改用 POST，避免 DELETE 在部分环境报“方法不允许”；后端仍保留 DELETE 兼容。
- 本轮验证：小程序全量 JS 检查通过、小程序 JSON 解析通过、后端 Python 3.12 编译通过、`git diff --check` 通过；pytest 未执行，当前 Codex runtime 缺少 pytest，项目 `.venv` 为 Python 3.9.6 会因 `dataclass(slots=True)` 失败。
- 2026-06-20 修正展示页模板硬编码房源文案：展示页保存 `displayConfig.activeCategory`，客户页按分类/资料类型切换为房源、好物或资料文案；商品/团购页不再显示“精选房源、好房推荐、找到理想的家”等房产词。
- 编辑展示页资料选择区已改名“笔记资料”，并增加“隐藏 / 展示”按钮，避免笔记很多时长滚动影响后续配置。
- 展示页新增删除能力：后端 `DELETE /api/showcases/{id}?ownerUserId=...`，列表页和编辑页均有删除按钮；列表页草稿/下架也显性显示“编辑 / 预览 / 删除”，解决用户看到“有的只有预览功能”的问题。
- 2026-06-20 根据用户截图反馈继续修正展示页：我的笔记和新建展示页资料选择区已增加“列表 / 双列卡片”切换；新建展示页复用 `note-select-card mode=list/grid`。
- 客户展示页已重排为四套真实不同结构，不再只是同一套列表换颜色：精选橱窗=大图+顾问卡+双列主推，朋友圈长页=生活故事流，清单目录=搜索筛选+紧凑清单，品牌名片=深色顾问品牌页+横向案例。
- 后端公开展示项已补 `badge/primaryText/secondaryText/priceText`，用于四套模板展示价格、主信息和标签。
- 2026-06-20 已补四张展示页标准模板参考图，保存到 `docs/png/showcase-template-01-featured-window.png`、`docs/png/showcase-template-02-moments-story.png`、`docs/png/showcase-template-03-catalog-list.png`、`docs/png/showcase-template-04-brand-card.png`；`showcase-template-00-all.png` 为总览，`showcase-template-mockups.html` 为源文件。
- 2026-06-20 已新增 `miniprogram/utils/note-display.js`，把“我的笔记”里的资料类型、标签、摘要、徽标、上传时间、房源/商品主副字段等展示计算抽成共用工具。
- `pages/notes/index.js` 已改为使用 `decorateNoteForList`；`pages/showcase-edit/index.js` 已改为使用 `decorateNoteForShowcasePicker` 和 `decorateSelectedShowcaseItem`，展示页选资料不再维护一套孤立的笔记卡片逻辑。
- 后端 schema/model/service 和开发文档里的展示页默认模板已统一为 `featured_window`，不再使用旧 `classic_grid` 默认值。
- 展示页联系方式默认值继续优先取登录用户；用户昵称/头像缺失时，会从已选笔记的联系人姓名和头像结构字段兜底。
- 本轮验证：小程序全量 JS 检查通过、小程序 JSON 解析通过、后端 Python 3.12 编译通过、`git diff --check` 通过；后续仍需用户在微信开发者工具真机预览四套模板、发布分享和客户打开效果。当前 Codex runtime 缺少 pytest，项目 `.venv` 为 Python 3.9.6，会因 `dataclass(slots=True)` 无法跑后端 pytest。
- 2026-06-20 展示页新建流程已重构为“模板 + 分类 + 默认全选”低操作版本：先选四个标准模板，再选分类，默认优先房产分类，分类内笔记卡片显示 `加入 / 已加入`，新建时默认该分类全部加入展示页。
- 四个标准模板固定为：`精选橱窗 / 朋友圈长页 / 清单目录 / 品牌名片`，模板名称和副标题集中在 `miniprogram/utils/showcase-templates.js`。
- 新建展示页会自动生成名称、分享标题、简介、banner 和电话；banner 默认取当前分类已选资料第一张图，电话优先取当前用户手机号，再从已选笔记电话/联系字段推断。
- 微信号也会从已选笔记结构字段 `wechat/contactWechat/weixin/wx` 自动推断；发布者昵称和头像保存到展示页 `contactConfig`。
- 新增 `miniprogram/components/note-select-card/`，展示页编辑页已用它渲染分类内笔记选择卡片，后续资料选择场景可复用。
- 客户公开展示页已按 `templateId` 呈现不同视觉：橱窗精选、长页叙事、紧凑目录、品牌名片。
- 展示页 `contactConfig` 已支持保存 `ownerName/avatarUrl`，品牌名片可展示发布者昵称和头像；生产后端已同步并重启，备份路径 `/home/ubuntu/teamBuy-deploy-backups/20260620-221657-showcase-template-flow`，旧镜像标签 `teambuy-backend:before-showcase-template-flow-20260620`。
- 本轮验证：小程序相关 JS 静态检查通过、小程序 JSON 解析通过、`git diff --check` 通过、后端 Python 3.12 编译通过、生产 `/health` 正常；`.venv` pytest 因 Python 3.9 不支持 `dataclass(slots=True)` 未跑通。
- 2026-06-20 修复用户反馈的展示页 `no found`：生产 `/api/showcases` 原本返回路由级 `{"detail":"Not Found"}`，说明展示页接口未部署；已同步后端到生产并重启 `teambuy-backend`。
- 生产部署备注：完整 `docker compose build backend` 卡在 `apt-get update`，本轮使用热修镜像方式上线；旧镜像标签为 `teambuy-backend:before-showcases-20260620-1000`，生产备份路径为 `/home/ubuntu/teamBuy-deploy-backups/20260620-100050-showcases`。
- 公网复测：`/health` 200；`/api/showcases?ownerUserId=user_test` 已变为业务级“用户不存在”；`/api/showcases/public/test_showcase_not_exists` 已变为业务级“展示页不存在或未发布”，不再是路由未上线。
- 小程序展示页构建页已优化“展示方式”：四个选项明确为“不分组 / 按资料类型 / 按标签 / 按自定义分组”；选择自定义分组时，单独显示已选资料的分组名称编辑区。
- 已补展示页显性分享入口：列表已发布项、编辑页发布后底部、发布者预览页顶部均有“发给客户”；分享路径为 `/pages/showcase-view/index?id=展示页ID`。
- 本轮验证：小程序 `showcase-edit` JS 检查通过；小程序 JSON 解析通过；`git diff --check` 通过；展示页后端专项测试 `3 passed`。
- 2026-06-20 完成展示页构建器 V1 QA 验收，报告保存为 `docs/qa/当前项目_验收报告m2.md`；结论为“需要人工确认”。
- 本轮 QA 回归通过：后端编译、小程序 JS 静态检查、小程序 JSON 解析、`git diff --check`、后端全量测试 `112 passed`。
- 展示页后端 P0 自动化项已覆盖：创建草稿、越权资料拒绝、空资料发布失败、发布后公开访问、草稿/下架不可公开、公开接口只返回可见资料摘要。
- 仍需人工确认：小程序构建页真实保存发布、客户展示页点击资料进入单条资料页、真机分享、banner 裁切、电话拨号和微信号复制。
- QA 发现 1 个 P2 文档偏差：测试清单写 `pages/note-preview/index?noteId=xxx`，现有实现和目标页使用 `id=xxx`；建议修正文档或兼容参数。
- 2026-06-20 进入 P1 展示页构建器 V1：已先补开发文档 `docs/stage2-docs/13-showcase-builder-v1.md` 和测试清单 `docs/qa/展示页构建器V1_测试清单与验收标准.md`。
- 后端新增 `ShowcasePage/ShowcaseItem` 和 `/api/showcases`：支持展示页列表、创建草稿、owner 详情、更新、发布、下架和公开访问已发布展示页。
- 展示页只保存 `noteId`、排序和配置，不复制资料正文；公开接口实时读取 active `UserNote` 摘要，草稿和下架页不可公开访问。
- 后端已覆盖展示页创建、越权资料拒绝、空资料发布拒绝、发布后公开访问、下架后不可访问、资料更新后读取最新摘要。
- 小程序新增 `pages/showcases/index`、`pages/showcase-edit/index`、`pages/showcase-view/index`，并在“我的”页增加展示页入口；构建页支持 banner 上传、资料排序、隐藏、移除、展示标题和自定义分组标题。
- 本轮验证：后端编译通过，小程序全量 JS 检查通过，小程序 JSON 解析通过，`git diff --check` 通过，后端全量测试 `112 passed`。
- 小程序体验版仍由用户在微信开发者工具中手动上传；真机分享、banner 裁切、电话拨号、复制微信号需要人工确认。
- 2026-06-20 修复 PaddleOCR 识别接口 502：06:33 测试图片保存成功，但识别请求让 Uvicorn 主进程退出，Nginx 返回 502；已改为 PaddleOCR 子进程隔离执行。
- 生产公网 `POST /api/ocr/notes/note_af53dd1a18/recognize` 已复测 200，后端容器未再重启；该笔资料 OCR 已完成，置信度约 0.94，并给出“可能是商品”的中置信提示。
- 2026-06-20 已部署 OCR 两段式接口到生产，并启用 PaddleOCR：生产 `OCR_PROVIDER=paddle`，依赖为 `paddlepaddle==3.3.1`、`paddleocr==2.10.0`。
- 真机“保存图片”此前报 `Not Found` 的原因是生产尚未部署 `/api/ocr/images`；当前公网 `GET /api/ocr/images` 已变为 `405 Method Not Allowed`，上传接口用不存在用户测试返回业务级“用户不存在”，说明路由已上线。
- 生产容器内 PaddleOCR 已验证可识别测试图 `HELLO 123`；首次真实 OCR 时若模型缓存不存在，会有一次模型下载/初始化开销。
- 2026-06-20 OCR 改为两段式：小程序“我的笔记”页先 `POST /api/ocr/images` 保存图片资料，资料编辑页再由用户点“识别图片文字”调用 `POST /api/ocr/notes/{note_id}/recognize`。
- OCR 只作为图片 Input Adapter：识别文字写入 `structuredData.ocr` 后，再统一走 `ContentObject.sourceType=image_ocr -> content-to-note -> UserNote`，并更新原图片资料；兼容保留旧 `POST /api/ocr/image-to-note`。
- OCR provider 可配置为 `auto/paddle/tesseract/mock`；未配置时 `structuredData.ocr.status=not_configured`，图片仍会保存，用户可手动补正文和字段。
- 2026-06-20 新增归档 parser 插件化收口：archive parser 现在有稳定 `name/msg_types`，registry 显式注册并给解析结果 metadata 写入 `archiveParser/archiveMsgType`。
- 2026-06-20 新增类型识别可解释：`visibilityConfig.recognitionExplanation` 记录候选类型、分数、命中字段、可读信号、parser hints 和摘要；`typeSuggestions` 也带 `score/matchedFields/signals/reason`。
- 2026-06-20 新增中置信人工确认接口：`POST /api/notes/{note_id}/confirm-type`，支持确认成房源、商品或普通笔记；确认后清空 `typeSuggestions`，写入 manual 识别记录，并保留原文、图片和 `structuredData.miniapp`。
- 小程序 `note-edit` 中置信按钮已改为调用后端确认接口，并展示识别摘要和命中信号，不再前端本地拼完整类型转换结构。
- 2026-06-20 收口：已完成当前大 diff 复核，未跟踪 PDF 不纳入提交，`miniprogram/project.config.json` 暂不纳入提交。
- 已提交后端能力：`feat: add lightweight orders and messaging backend`，包含订单接口、消息接口、消息模型、归档 parser、schema、测试和 mock 数据。
- 已提交小程序体验：`feat: add miniapp orders and messaging flows`，包含订单页、消息页、消息入口组件、商品 SKU/名单体验、客户页和我的页入口。
- 本轮收口验证：小程序全量 JS 检查通过；小程序 JSON 解析通过；Python 3.12 `compileall` 通过；`pytest backend/tests/test_app.py -q` 为 66 passed；`pytest backend/tests -q` 为 103 passed；`git diff --check` 通过。
- 生产后端已部署：同步前备份到 `/home/ubuntu/teamBuy-deploy-backups/20260620-031227`，已重建并重启 `teambuy-backend`。
- 公网验证通过：`/health` 正常；`/api/orders?userId=user_test&role=buyer` 返回 200 空列表；`/api/messages/threads?userId=user_test` 返回 200 空会话列表。
- 小程序体验版仍由用户在微信开发者工具中手动上传。
- 站内消息详情页已改为微信式左右对话：当前登录用户消息在右侧，绿色气泡，头像在右；对方消息在左侧，白色气泡，头像在左，并展示对方昵称。
- 已修正“我的消息头像在气泡右边但整组仍贴左侧”的问题：消息行保持 `justify-content:flex-end`，只对我的头像/气泡设置排序，不再反转整条 flex 主轴。
- 后端消息线程行已返回 `participants`，包含 owner/buyer 的角色、昵称和头像；消息页不再只能按 senderUserId 猜测展示。
- 本次补充验证：小程序全量 JS 检查通过；小程序 JSON 解析通过；Python 3.12 环境下 `pytest backend/tests/test_app.py -q` 为 66 passed；compileall 通过；`git diff --check` 通过。
- `AGENTS.md` 已新增小程序上传约定：小程序预览、上传体验版和提交审核默认由用户在微信开发者工具中手动完成；Codex 不再默认尝试 CLI 上传，只做代码实现、JS/JSON 检查、后端测试和上传提醒。
- 商品 P1 已补：客户页 SKU 选择有属性组时按分组按钮展示，无属性组时保留组合 SKU 卡片兜底。
- SKU 选择售罄逻辑已优化：选项只要存在任一未售罄组合就可点，点击后会自动切到可买组合；提交时仍由后端校验具体组合是否售罄。
- 后端客户动作配置接口已为已提交 `order-intent / relay-intent` 回传 `submittedPayload`，客户再次进入可恢复已提交 SKU、数量、电话、地址、微信和备注。
- 团长 `note-actions` 商品下单/接龙名单已支持按 SKU 筛选；复制汇总、复制单条和发消息均基于当前筛选结果。
- 本轮验证：小程序全量 JS `node --check` 通过；小程序 JSON 解析通过；`git diff --check` 通过；Python 3.12 环境下 `pytest backend/tests/test_app.py -q` 为 66 passed；`.venv` 是 Python 3.9.6，跑 pytest 会卡在 `dataclass(slots=True)`，后续测试请用 Codex runtime Python 3.12。
- 本轮已实现“商品展示基座 + 团购模式”：`groupbuy_product` 继续兼容旧类型，但小程序前台改为商品展示口径。
- 商品 SKU 配置保存到 `structuredData.skuConfig`，支持属性组、选项、组合 SKU、价格、说明和售罄状态；截止时间选填。
- `conversionConfig.enableGroupRelay` 控制是否开启团购接龙；未开启时客户页只展示商品，开启后客户可选 SKU、数量并提交电话 / 微信 / 备注。
- 团购接龙使用 `customer_actions` 的 `relay-intent` 保存，不投影到 `lead_reminders`，不进入轻 SCRM；同一客户同一商品只允许一条有效接龙。
- 后端会拒绝关闭团购后的新增接龙、售罄 SKU 提交和重复提交；本轮不引入地图、支付、订单、库存扣减、核销、分账接口。
- 团长端 `note-actions` 已针对商品展示为“接龙名单”，支持查看头像、昵称、SKU、数量、联系方式、备注、提交时间，并支持复制汇总、复制单条和电话拨号。
- 我的笔记商品卡已改为展示“接龙 N / 接龙名单”，进入名单后沿用客户动作已读逻辑。
- 本地 mock 已补商品资料 `note_seed_groupbuy_product_001` 和接龙样例 `action_seed_relay_001`；当前运行态可直接测商品工作台、客户页 SKU 接龙和团长接龙名单，且 `lead_reminders=0`。
- 小程序“我的”页的“生成测试数据”现在会给当前登录用户生成 3 条房源 + 1 条商品，解决 seed owner 与设备本地 mock 用户不一致时看不到商品数据的问题。
- 当前小程序 `apiBaseUrl` 已恢复为生产 `https://teambuy.lifelove.top`。企业微信客服接收消息和真机微信登录都应以生产环境为主测入口；本地 mock 只作辅助。
- 商品接龙名单和商品工作台顶部动作已修移动端布局：接龙卡头像/昵称/状态不再挤压，按钮不再占满整行；窄屏下顶部主动作可自然换行。
- 本地 mock 模式下“微信登录”不走 mock 身份；正式微信登录需要线上 HTTPS 后端配置小程序 AppSecret，本地测试请点“本地 mock 登录”。
- 商品工作台底部标签 / 专题输入已修手机宽度溢出；SKU 新增选项现在为空输入 + placeholder，不会要求先删除“选项3”。
- 商品价格规则已调整：不在商品基础字段前置展示单一价格；有 SKU 时以 SKU 价格为准，未设置 SKU 属性时才显示“单一价格”兜底。
- 资料详情底部“删除 / 保存”改为左右分布，避免手机端按钮错位。
- 2026-06-19 02:41 用户转发贝壳房源小程序给企业微信，生产会话存档收到 `msgtype=weapp`，字段只有小程序外壳：`appid=wxcfd8224218167d98`、标题 `三江尊园 全天采光 好楼层 拎包入住`、来源 `贝壳找房丨二手房新房租房装修`、`pagepath`、`houseCode=101137825091`、`cityId=150200`；没有价格、户型、面积、图片、地址和经纬度。
- 已修复 `weapp` 解析：`ContentObjectPayload.metadata` 保存小程序元数据，`ContentObjectAdapter`、`WecomMessageNormalizer`、`MessageAggregator`、`MessageType` 均支持小程序卡片；普通客服同步和会话存档都能入库。
- 小程序卡片前台保存为 `sourceType=miniapp`、`systemCategory=小程序`，正文只展示标题、来源、appid、houseCode，完整 `pagePath` 放在 `visibilityConfig.structuredData.miniapp.pagePath`。
- 贝壳小程序卡片只给“可能是房源信息”的中置信提示，不自动高置信生成房源工作台；`miniapp_card` 不提取手机号，避免 pagepath 数字污染电话字段。
- 已修复生产历史空笔记 `note_4ecff85fca` / `card_336b070ffc` / archive `wecom_archive_msg_04c9699da3`，公网读取确认 `phone=null`、`sourceType=miniapp`、`typeSuggestions` 含房源提示。
- 生产后端已重新部署，`https://teambuy.lifelove.top/health` 通过；本地后端验证 `pytest backend/tests -q` 为 98 passed。
- 小程序 `app.json` 已配置贝壳 appid 到 `navigateToMiniProgramAppIdList`；地图选点只声明 `chooseLocation`。
- 编辑页新增“原小程序房源”块，客户页新增“查看贝壳原房源”动作，使用 `wx.navigateToMiniProgram` 跳贝壳原房源；失败时复制房源编码兜底。
- 后端已为当前贝壳小程序卡生成并保存候选网页 URL：`https://m.ke.com/baotou/ershoufang/101137825091.html`，写入 `visibilityConfig.sourceUrl` 和 `structuredData.miniapp.webUrl`；该 URL 可能触发贝壳验证码，仅作备用打开/复制，不作为稳定爬虫来源。
- 贝壳小程序房源候选默认开启轻 SCRM、留资、预约、微信咨询和分享图入口，不开启联系电话展示。
- 已修复“小程序卡确认成房源字段卡时丢失 miniapp 元数据”的问题；生产 `note_4ecff85fca` 当前为 `property_listing + sourceType=miniapp`，保留 `houseCode=101137825091` 和完整 `pagePath`。
- 后端房源高置信识别已把标题小区名纳入 `community` 信号；新增测试覆盖标题为小区名、正文含户型/价格/位置的高置信房源。
- 编辑页和客户页会记住最近一次房源城市；地址不含城市时会用最近城市补全后再请求腾讯地图地理编码，减少同名小区误匹配。
- 客户页动作文案已改为客户语言：`电话咨询`、`留下电话/微信`、`预约看房`、`微信咨询`。
- 客户页留联系方式支持电话或微信二选一；预约看房支持今天/明天快捷项，并可选择具体日期和时间，精确到分钟。
- 房源/团购列表主动作已从“生成推广/生成海报”改为“转发给好友”，并接入微信原生分享；工作台顶部主动作已调整为一行三列：“分享文案 / 转发给好友 / 客户页预览”，分享图入口已弱化为“保存分享图”。
- `pages/note-edit/index` 已新增浅绿色小尺寸悬浮保存按钮，默认吸附右侧中部，拖动后按左右距离吸附；底部保存按钮仍保留。
- 发布者房源/团购联系方式会本地记忆手机号；客户页留资手机号也会本地记忆，后续默认带入但可修改。
- 客户页预览已补充房源图片横向图库，分享卡片仍用封面图，页面内可查看完整图片；正文里的分享按钮已移除，改为右侧靠下两个固定浮动按钮。
- 客户页地图动作已改为“选择导航App / 微信内置地图 / 复制地址”，导航 App 不支持时回退微信内置地图。
- 客户页地图头部已去掉经纬度数字，只展示“腾讯地图 / 正在匹配默认地址 / 按默认地址定位”。
- 新增后端 `GET /api/location/geocode`，由后端使用 `TENCENT_MAP_KEY` 调腾讯地图地理编码，把房源默认地址转成经纬度；Key 不暴露给小程序。
- 编辑页和客户页在有默认地址但没有坐标时会尝试自动解析地图点，成功后显示小房子 marker；解析失败或未配置 Key 时继续用微信原生选点兜底。
- `backend/.env.example` 已增加 `TENCENT_MAP_KEY` / `TENCENT_MAP_GEOCODER_URL`，上线要在后端环境变量补真实腾讯地图 Key。
- 腾讯地图 Key 已配置到本地和生产后端 `.env`，生产后端已重建；公网 `/api/location/geocode` 已验证可返回坐标。
- 产品方向已从显性的房源/团购 4 态流程，收敛为“自动生成结果 + 轻量整理”的两层工作台。
- 高置信房源/团购：后端直接写入 `cardState=generated`，小程序打开 `pages/note-edit/index` 时直接看到工作台，而不是流程步骤页。
- 中置信资料：保留普通资料卡，写入 `typeSuggestions`，小程序提示“这条资料像是？房源 / 团购 / 普通笔记”。
- 低置信资料：直接作为普通笔记，不打扰用户。
- 小程序 `pages/note-edit/index` 已重构为工作台 UI：房源/商品卡、图片与视频、功能组、轻 SCRM、基础信息、标签与专题；核心板块支持隐藏/恢复。
- 普通笔记可通过 `+ 添加功能` 增加轻 CRM、留资表单、预约、接龙功能组。
- `pages/notes/index` 保留每个资料块的上传时间，新增中置信轻提示和“未整理”轻入口；默认仍按上传/导入时间倒序。
- 后端笔记搜索已支持宽松模糊搜索：结构化字段、标签、专题、上传日期、归一化日期数字都参与搜索，`618` 可命中 `6月18日` / `2026-06-18` 类日期。
- 工作台编辑体验已细化：字段输入区改为更清晰的信息块样式，常见字段增加快捷项，图片素材支持设封面和从当前资料卡删除。
- “客户页”已新增 `pages/note-preview/index` 作为 owner 侧客户可见内容预览；`pages/note-poster/index` 已改为“分享图”辅助页，用于保存静态图片和复制发群文案。
- 分享图页已改浅色背景，提供 5 个强调色可选，并新增“保存海报”到相册；分享图标题限制为最多 3 行，canvas 保存时给价格和详情行保留安全空间；客户页动作按钮已改为动作卡，支持分享、联系、留资、预约、接龙和地图定位。
- 房源编辑页地址字段已支持微信原生腾讯地图选点，保存 `structuredData.mapLocation` 后客户页可调用 `wx.openLocation`。
- 标签和专题已增加推荐快捷项，默认标签直接显示并可删除，减少手动输入。
- 商圈字段已增加快捷点选，并会把识别出的商圈拆成短标签候选；`未整理`、`待跟进` 和过长标签已从前台推荐/展示中过滤。
- 地址字段会先展示默认地址；选过地图点后展示真实小地图预览。
- `app.json` 已声明 `chooseLocation` 私密接口；地图选点成功后会自动保存经纬度，不必再手动保存。
- 客户页和编辑页地图 marker 已增加 `🏠` label / callout；客户页会展示经纬度，没经纬度时提示先在编辑页选择小区位置。
- 价格识别已优化为优先读取价格关键词行，并避开服务费/面积/房号数字抢占。
- 本轮验证：后端编译通过，`pytest backend/tests -q` 93 passed，小程序 JS/JSON 静态检查通过。
- 用户确认旧资源详情页先不要删，先隐藏起来并记录好，强制已认领资料走新的 `UserNote` 资料卡链路。
- 后端 `/api/cards` 和 `/api/cards/{card_id}` 已新增 `sourceNoteId`，用于把兼容旧 Card 映射回新 `UserNote`。
- 小程序新增 `miniprogram/utils/resource-navigation.js`，统一处理资源跳转：有 `sourceNoteId` 时打开 `/pages/note-edit/index`，没有时才回退旧 `card-view/card-edit`。
- 已接入统一跳转的入口包括：资源库、首页热门资源、访问记录、客户资料库、待联系列表、线索详情、管理页打开/编辑资源。
- 旧 `card-view` 和 `card-edit` 文件仍保留；拥有者直接打开带 `sourceNoteId` 的旧页面时会自动重定向到新笔记编辑页。
- 客户分享访问旧 `card-view` 暂不强制拦截，避免新客户展示页完成前影响外部查看。
- 本轮验证：后端编译通过，`pytest backend/tests -q` 91 项通过，小程序 JS/JSON 静态检查通过。

最新完成：
- 新增 `docs/stage2-docs/12-typed-content-card-architecture.md`。
- 固定产品原则：统一流程是“收藏 -> 编辑 -> 整理 -> 生成”，但数据结构必须分型。
- 后端 `content-to-note` 已支持规则识别 `property_listing` 房源字段卡、`groupbuy_product` 团购商品卡、`text_note` 文本卡、`link` 链接卡。
- 第一版 typed card 数据继续放在 `UserNote.visibilityConfig.cardType/cardState/structuredData/typeSuggestions`，不新建房源/团购表。
- 房源字段第一版：小区、户型、价格、水电物业、商圈、地址、服务费、备注、联系方式、图片。
- 团购字段第一版：商品名、价格、规格、截止时间、自提/配送、取货地点、库存备注、联系方式、图片。
- “整理”接口已按 `cardType` 分型：链接转文章/阅读卡口径，房源/团购补摘要和生成建议。
- 房源/团购已新增 `conversionConfig`：控制联系电话展示、轻 SCRM、线索收集、预约看房、私聊咨询、团购接龙、分享图入口和下单按钮预留。
- 新增 `/api/notes/{note_id}/generate`：当前生成 `generated` 状态和启用动作清单，正式海报/场景页渲染后续由场景生成 Skill 接管。
- 搜索已覆盖 `structuredData`，可按小区、商圈、商品规格等字段命中。
- 小程序“我的笔记”列表已分型展示链接卡、房源卡、团购卡、普通文本卡。
- 小程序笔记编辑页已增加房源和团购字段表单，同时保留来源类型、弱分类、标签和专题编辑；房源/团购还新增“功能配置”和“生成场景页”动作。
- 静态测试 `test_import_flow_uses_single_import_artifact_transaction` 已校准为检查真实成功保存入口 `_process_import_batch`。

最新验证：
- `python -m compileall backend/app backend/tests`：通过。
- `pytest backend/tests -q`：91 passed。
- 小程序所有 `.js` `node --check`：通过。
- 小程序所有 `.json` 解析：通过。
- 生产 backend 已部署到 `https://teambuy.lifelove.top`，健康检查通过。
- 生产 generate 路由已验证：假 note 请求返回“笔记不存在”，说明接口已挂载。

当前注意：
- 本轮为规则版，不调用大模型。
- 低置信内容会保留为 `text_note` 并写入 `typeSuggestions`，但小程序“手动转换类型”还没做。
- 当前 `generate` 只是生成态配置结果，不是真正的场景生成 Skill。客户页链接是主分享路径；分享图只是可保存到相册的辅助素材，后续可再增强为更精美的推广图。
- 失败导入路径仍因没有 Card artifact 而走分散保存，后续如要完全事务化，需要先调整仓储接口；已写入 `docs/pitfalls.md`。

上一轮已提交修复：
- `relay-list` 组件已增加接龙时间和跟进状态兜底格式化，资源详情页不再直接显示 ISO 时间或 `pending`。
- 资源详情页已绑定已接龙名单的 `标记已跟进` 和 `删除无效` 操作。
- 卡片编辑页素材上移/下移已修复旧 `sortOrder` 导致排序回弹的问题。
- 小程序内“发给客服”入口已改为“添加 / 快速入库”：中间 tab 指向手动添加资源页，`pages/imports/index` 仅作为外部导入后的“待认领导入”页。
- 小程序可见页面已移除“发给客服 / 立即发给客服 / 去发给客服”和可见 mock 导入按钮。
- 已验证：小程序 JS 静态检查通过，小程序 JSON 解析通过，`pytest backend\tests\test_app.py -q` 34 项通过。

本轮改动：
- 后端上传接口和企微媒体转存已接入媒体处理服务，图片通过 Pillow 转 WebP，视频通过 ffmpeg 转 H.264/AAC MP4。
- 上传响应新增 `originalSize`、`storedSize`、`compressed`。
- 小程序新增原生 `resource-store`，承担 Pinia 类似的资源集中管理职责。
- 小程序新增媒体缓存工具，页面展示使用 `coverDisplayUrl` / `media[].displayUrl`，保存仍提交正式 URL。
- 高意向访客待联系 / 已联系 / 备注已从小程序本地 storage 升级到后端 `lead_reminders`。
- 新增统一“待联系”页面 `pages/leads/index`，可跨资源处理待联系线索。
- 资源详情页发布者入口已改成更明显的“线索管理”提示条。
- 待联系页筛选项已改成胶囊样式，线索卡片会展示来源资料，并分别提供“资源详情”和“线索管理”入口。
- 线索第三阶段已新增个人跟进记录和下次跟进日期：后端保存 `followUpLogs` / `nextFollowUpAt`，待联系页可追加跟进记录。
- 线索第四阶段已新增时间筛选和跟进优先级排序：全部时间、今日、逾期、未来、未设置；卡片展示最近 3 条跟进记录。
- 线索第五阶段已新增页内提醒看板：今日待跟进、已逾期、一键只看未处理；暂不接微信通知。
- 线索第六阶段已新增归档结论：无效、暂不跟进、已完成；保存归档原因和归档时间。
- 线索第七阶段已新增线索详情页：列表页只保留摘要和关键动作，详情页承载备注、跟进记录、归档原因和状态操作。
- 线索第八阶段已新增发布者私有客户资料：手机号、微信号、预算、意向等级。
- 线索第九阶段已新增客户资料库 `pages/customers/index`：按意向等级筛选，入口位于“我的”和“待联系”。
- 客户资料库第二阶段已新增搜索和快捷复制：可搜昵称、手机号、微信号、预算、来源资料；手机号/微信号可一键复制。
- 客户资料库第三阶段已新增排序和快捷筛选：高意向优先、最近更新、有电话、有微信、有预算。
- 客户资料库第四阶段已新增“复制客户摘要”：复制当前筛选结果为表格文本。
- 客户资料库第五阶段已强化客户详情页：客户资料区前移，并支持复制单个客户完整档案。
- 已验证：`python -m compileall backend\app backend\tests` 通过，`pytest backend\tests -q` 60 项通过，小程序 JS/JSON 检查通过。

## 1. 项目背景与目标

teamBuy 是一个面向微信私域场景的小程序工具。当前产品名和 UI 方向为“资料整理助手”。

项目核心目标不是做团购交易系统，也不是做支付、订单、库存、分账或完整 CRM，而是验证一条“微信内容资源助理”主链路：

```text
企业微信客服收到用户转发的微信笔记 / 链接 / 图片 / 视频 / 位置等素材
  -> 后端通过企业微信客服回调与 sync_msg 拉取消息
  -> 聚合消息并生成资源卡片草稿
  -> 小程序端认领、编辑、保存、发布
  -> 分享给客户查看
  -> 客户浏览、电话直拨、复制字段、实名接龙
  -> 发布者查看访问统计、接龙名单、跟进状态
  -> 资源库搜索、筛选、复用
```

第一优先用户是房产中介，第二优先用户是团购团长。当前 v0.1 重点是把“素材归档 -> 资源卡片 -> 分享查看 -> 浏览/接龙/跟进 -> 资源库复用”跑通。

需要特别注意：企业微信真实 `sync_msg` 主链路目前仍因企业微信认证/权限配置问题阻塞，不能把手动添加资源或 mock 链路当作最终上线通过。

## 2. 当前阶段目标

当前阶段处于 v0.1 小程序产品化与本地可验收链路补齐阶段。

阶段目标：

- 在企业微信真实权限暂时无法继续推进时，先把小程序端资源管理、发布、分享、接龙、线索跟进体验打磨完整。
- 参考 `docs/png/` 里的页面图，尽量复刻页面功能与体验，但 `docs/png/` 仅作为参考图，不纳入资源入库。
- 保持真实企业微信导入为最终主链路；手动添加资源只是临时可用入口和本地验收入口。
- 所有小程序页面使用自定义导航 `navigationStyle: "custom"`。
- 阶段完成后需要在微信开发者工具里人工验收，自动化测试不能替代真实小程序运行环境验收。

## 3. 已完成的功能

### 3.1 后端基础能力

- FastAPI 后端骨架。
- 本地 JSON/mock 持久化与 PostgreSQL 目标仓储适配。
- `/health` 健康检查。
- 企业微信客服回调 GET/POST 骨架。
- `sync_msg` 客户端、cursor、任务锁、任务日志、媒体转存抽象。
- mock 企业微信导入、消息聚合、卡片草稿生成。
- 卡片创建、更新、发布、复制复用。
- 浏览统计、匿名浏览隔离、登录访客统计。
- 实名接龙、删除无效接龙、标记已跟进。
- 资源分类标签接口。
- 卡片 `media` 字段，支持图片/视频结构化保存。
- 手动上传资源文件接口 `POST /api/uploads/asset`，当前用于小程序本地上传图片/视频。
- 手动上传图片/视频会先压缩再存储，图片为 WebP，视频为 H.264/AAC MP4，默认不保存原始大文件。
- 删除资源时同步清理该资源的访问记录和接龙线索。
- 删除资源时同步清理该资源的访问记录、接龙线索和待联系提醒。
- 登录访客统计增强：
  - 同一登录用户重复访问聚合为一条记录。
  - 返回 `viewCount`。
  - `viewedAt` 使用最新访问时间。
- 接龙状态增强：
  - 同一用户对同一资源只允许一条 active 接龙。
  - stats 返回 `currentUserRelay`。
  - 发布者标记已跟进后，客户侧能读取到 `followUpStatus=followed`。

### 3.2 小程序基础页面

- 登录页。
- 首页。
- 资源库页。
- 添加资源页。
- 待认领导入页。
- 手动添加资源页。
- 标签管理页。
- 资源编辑页。
- 资源详情 / 分享查看页。
- 管理页 / 访问详情页。
- 访问记录页。
- 我的页。
- 自定义导航组件 `custom-nav`。
- tabBar 图标已接入 `miniprogram/static/tab`。

### 3.3 资源库与标签

- 资源库支持真实搜索。
- 资源库支持分类筛选与标签筛选。
- 第二排标签只展示真实自定义标签，不再混入“客服接收 / 手动添加 / 可接龙 / 带链接”等伪标签。
- 标签管理支持新增、删除。
- 卡片可绑定 `categoryIds`。
- 删除标签时会从用户卡片中移除对应绑定。
- 资源库支持删除资源。

### 3.4 手动添加资源与素材

- 手动添加资源页支持上传图片、视频、文件。
- 图片/视频上传到后端后会压缩，资料库保存压缩后的展示 URL。
- 图片/视频写入卡片结构化 `media`。
- 附件类文件继续补充到详情文本。
- 首张图片默认作为封面。
- 多图上传时明确首图为封面，其余图片/视频进入详情。
- 手动添加资源可选择自定义标签。
- 可保存到资源库、进入编辑页。
- 可发布并预览。

### 3.5 资源编辑页

- 编辑页改成接近最终发布页的“所见即所得”结构。
- 顶部封面区可直接编辑标题、项目名、位置。
- 不再显示“封面图片链接”技术字段。
- 详情素材在编辑页内按正式展示形态呈现。
- 点击图片可设为封面。
- 支持删除素材、上移、下移。
- 支持发布后继续上传图片/视频。
- 新上传素材写入当前卡片 `media`，保存修改后持久化。
- 编辑页按钮文案已改为“保存修改 / 发布并查看”，避免“保存草稿”误解。

### 3.6 资源详情 / 客户分享页

- 资源详情页展示封面、标题、项目、位置、详情文本、字段复制、详情素材。
- 详情素材支持多图预览和视频播放。
- 电话直拨。
- 复制信息。
- 复制来源链接。
- 分享资源使用小程序原生 `open-type="share"` 调起微信分享面板。
- 客户视角不展示 PV/UV/接龙数统计。
- 客户视角不展示接龙名单。
- “访问详情”仅发布者可见。
- 客户提交接龙后：
  - 页面切换为“已提交接龙”状态。
  - 输入框和提交按钮隐藏。
  - 刷新后通过 `currentUserRelay` 恢复已提交状态。
- 发布者标记已跟进后：
  - 客户重新打开资源页显示“发布者已跟进”。
  - “已提交”和“已跟进”有不同状态卡样式。

### 3.7 管理页 / 访问详情页

- 发布者可查看 PV、UV、匿名 PV、接龙数。
- 发布者可查看登录访客列表。
- 发布者可查看接龙名单。
- 接龙名单按状态分组：
  - 待跟进
  - 已跟进
  - 全部
- 默认展示待跟进线索。
- 接龙线索支持：
  - 电话直拨
  - 复制电话
  - 复制地址
  - 标记已跟进
  - 删除无效
- 待跟进线索高亮。
- 登录访客区支持：
  - 高意向
  - 最近
  - 全部
- 当前高意向规则：
  - `viewCount >= 2`
  - 且该用户尚未接龙
- 访客标记：
  - 重复访问但未接龙：高意向
  - 已提交接龙：已接龙
  - 其他：普通访问
- 高意向访客支持：
  - 复制昵称
  - 加入待联系
- 待联系提醒支持：
  - 标记已联系
  - 取消待联系
  - 已联系后清除记录
- 待联系提醒已升级为后端持久化：
  - 数据模型为 `LeadReminder` / `lead_reminders`
  - 支持 `pending` / `contacted`
  - 支持备注 `note`
  - 同一资源同一访客只保留一条提醒
  - 用户换手机后仍可读取自己的待联系线索

### 3.9 统一待联系页

- 新增小程序 `pages/leads/index`。
- 我的页新增“待联系线索”入口。
- 页面支持待联系、已联系、全部筛选。
- 筛选项使用胶囊样式。
- 每条线索展示来源资料名称。
- 支持保存备注、标记已联系、恢复待联系、清除线索。
- 支持选择下次跟进日期、追加跟进记录，并展示最近一条跟进记录。
- 支持按跟进时间筛选，并按逾期、今日、未来、未设置、已完成排序。
- 跟进记录区展示最近 3 条记录。
- 顶部提醒看板支持点击今日 / 逾期 / 未处理快速筛选。
- 支持已归档筛选、无效 / 暂不跟进 / 已完成结论动作，以及归档原因记录。
- 线索详情页支持完整编辑备注、追加跟进、选择下次跟进、归档结论和恢复待联系。
- 线索详情页支持维护发布者私有客户资料：手机号、微信号、预算、意向等级。
- 客户资料库集中展示已沉淀客户资料的线索，并支持按意向等级筛选。
- 客户资料库支持搜索和手机号 / 微信号复制。
- 客户资料库支持高意向优先 / 最近更新排序，以及有电话 / 有微信 / 有预算筛选。
- 客户资料库支持复制当前筛选结果的客户摘要。
- 线索详情页支持复制单个客户完整档案。
- 支持从线索打开对应资源详情页和资源管理页。

### 3.8 已通过的自动化检查

最近一次相关检查已通过：

```text
小程序所有 .js：node --check 通过
小程序所有 .json：JSON 解析通过
python -m compileall backend\app backend\tests：通过
pytest backend\tests -q：60 passed
```

注意：这些只代表本地逻辑和静态检查通过，不等于微信开发者工具或真实企业微信链路验收通过。

## 4. 已修改 / 新增的文件

### 4.1 主要后端文件

- `backend/app/services/app_service.py`
- `backend/app/services/repository.py`
- `backend/app/api/routes_cards.py`
- `backend/app/schemas/cards.py`
- `backend/app/models/domain.py`
- `backend/tests/test_app.py`
- `backend/tests/test_media_processing_service.py`
- `backend/tests/test_media_storage_service.py`
- `backend/core/schema.sql` 相关迁移/表结构文件已在历史提交中维护

### 4.2 主要小程序文件

- `miniprogram/app.js`
- `miniprogram/app.json`
- `miniprogram/app.wxss`
- `miniprogram/services/api.js`
- `miniprogram/utils/request.js`
- `miniprogram/utils/dashboard.js`
- `miniprogram/utils/nav.js`
- `miniprogram/components/custom-nav/*`
- `miniprogram/components/relay-list/*`
- `miniprogram/components/field-copy-row/*`
- `miniprogram/components/card-preview/*`
- `miniprogram/pages/home/*`
- `miniprogram/pages/library/*`
- `miniprogram/pages/imports/*`
- `miniprogram/pages/resource-create/*`
- `miniprogram/pages/tag-manage/*`
- `miniprogram/pages/card-edit/*`
- `miniprogram/pages/card-view/*`
- `miniprogram/pages/manager/*`
- `miniprogram/pages/leads/*`
- `miniprogram/pages/visits/*`
- `miniprogram/pages/profile/*`
- `miniprogram/pages/login/*`
- `miniprogram/static/tab/*`

### 4.3 文档文件

- `AGENTS.md`
- `docs/project-memory.md`
- `docs/decisions.md`
- `docs/pitfalls.md`
- `docs/dev-log.md`
- `docs/handoff-latest.md`
- `docs/prompts/codex-start.md`
- `docs/prompts/codex-handoff.md`
- `docs/stage2-docs/*`
- `docs/qa/*`

### 4.4 当前不要纳入提交的文件/目录

- `docs/png/`
  - 这是页面参考图目录，只用于参考，不加入资源入库，不应随便提交。
- `backend/mock/runtime-state.json`
  - 当前为本地运行态数据，已被多轮手动测试污染，除非明确要固化 mock 状态，否则不要提交。
- `miniprogram/project.config.json`
- `miniprogram/project.private.config.json`
  - 微信开发者工具本地配置，当前为未跟踪文件，不要默认提交。
- `docs/qa/当前项目_验收报告m1.md`
  - 当前为未跟踪文件，未确认是否应纳入提交。

## 5. 当前代码状态

当前 `git status --short --branch`：

```text
## main...origin/main [ahead 27]
 M backend/mock/runtime-state.json
?? docs/png/
?? docs/qa/当前项目_验收报告m1.md
?? miniprogram/project.config.json
?? miniprogram/project.private.config.json
```

最新提交：

```text
feat: persist lead reminders and webp media
```

本地 `main` 已领先远端 `origin/main` 27 个提交，尚未推送。本轮未纳入 `backend/mock/runtime-state.json`、`docs/png/`、`docs/qa/当前项目_验收报告m1.md`、微信开发者工具本地配置。

最近关键提交包括：

```text
35c6a5f feat: manage visitor follow-up reminder states
feat: manage visitor follow-up reminder states
43e51cf feat: convert visitors into follow-up reminders
ae218b3 feat: highlight manager visitor intent
a0617c8 feat: filter manager relay leads
44016de feat: add relay lead quick actions
9753b99 feat: show followed relay status to customers
ece6e60 feat: close relay submission follow-up loop
3725ff0 feat: hide customer-facing private resource stats
d0d9f4a feat: support edit page media upload and sharing guard
548a65e feat: make card edit mirror published page
943640d feat: manage card edit media assets
```

当前没有需要继续提交的业务代码改动，工作区剩余改动主要是运行态数据和未跟踪本地/参考文件。

## 6. 已知问题和风险

### 6.0 2026-06-19 房产场景体验补强已完成

本轮继续围绕房源工作台优化了 5 类能力：

- 我的笔记房源卡片显示推广状态；客户信息入口文案更贴近业务，显示“待跟进 N / 客户 N”。
- 资料详情新增“复制客户话术”和“房源状态”：推广中 / 已租 / 暂停推广。
- 客户页遇到已租 / 暂停推广时，关闭新增电话咨询、留资、预约、私聊、接龙动作，只保留原房源 / 地图等信息入口。
- 图片与视频素材支持上移 / 下移排序，并立即保存排序结果。
- 客户动作页按“新线索 / 待跟进、预约看房、已联系 / 已归档、全部客户动作”分层；拨号成功后可提示标记已联系并写入跟进记录。

本轮验证：

- 小程序全量 JS `node --check`：通过。
- 相关页面 JSON 解析：通过。
- `git diff --check`：通过。
- Python 3.12 环境后端 `compileall`：通过。
- `pytest backend/tests -q`：100 passed。

### 6.1 P0：真实企业微信 `sync_msg` 仍未跑通

真实企业微信客服主链路仍卡在：

```text
errcode=48002
errmsg=api forbidden
from ip=81.70.84.35
```

当前判断更像企业微信后台权限、Secret、API 管理、客服账号权限、可信 IP 或认证状态问题，不应盲目大改代码。

企业微信认证目前用户侧也有问题，需要和官方沟通后才能继续真实权限配置。

### 6.2 手动添加资源不是最终主链路

手动添加资源已经可用于本地验收，但它不能替代：

- 企业微信客服真实接收微信笔记/链接。
- `sync_msg` 拉取真实消息。
- 图片/视频 `media_id` 及时下载与转存。
- 导入成功/失败通知。
- 用户认领真实导入内容。

### 6.3 小程序仍需微信开发者工具人工验收

自动化检查不能替代微信环境验收。尤其需要人工确认：

- 登录。
- 上传图片/视频预览。
- 保存、发布、查看。
- 微信原生分享面板。
- 电话直拨。
- 复制字段。
- 接龙提交。
- 发布者和普通客户身份切换后的权限差异。

### 6.4 待联系提醒仍不是完整团队协作能力

高意向访客“加入待联系 / 标记已联系 / 取消待联系 / 备注”已升级为后端持久化，但当前仍是发布者个人待办：

- 暂不支持多人分配。
- 暂不支持跟进提醒通知。
- 暂不支持跟进历史时间线。
- 暂不支持客户手机号自动沉淀为 CRM 客户档案。

如果后续要做团队协作，需要在当前 `lead_reminders` 之上继续增加负责人、提醒时间、跟进记录和权限模型。

### 6.5 隐私与权限风险

必须继续保持：

- 普通客户看不到统计卡片。
- 普通客户看不到接龙名单。
- 普通客户看不到电话、地址、快捷动作。
- `relay-list` 只有 `isOwner=true` 时渲染电话、地址和快捷动作。
- 后端非发布者 stats/list relays 必须继续脱敏，电话和地址置空。

### 6.6 工作区风险

当前存在未提交运行态数据和未跟踪参考文件。新会话不要批量删除或随便提交。

项目规则明确禁止批量删除文件或目录，不要使用：

```text
del /s
rd /s
rmdir /s
Remove-Item -Recurse
rm -rf
```

## 7. 用户已经确认过的产品 / 技术决策

- v0.1 不做交易系统，不做支付、订单、库存、核销、分账。
- 第一批优先用户是房产中介，其次是团购团长。
- 企业微信客服接收微信笔记/链接并自动归档，是最终核心主链路。
- 企业微信客服导入发生在小程序外部会话；小程序内不提供“发给客服”入口，中间加号是“添加 / 快速入库”。
- `pages/imports/index` 只作为外部导入后的待认领页，不暴露 mock 导入按钮。
- 企业微信权限认证问题暂时无法推进时，先继续小程序产品化开发。
- `docs/png/` 是页面参考图，不加入资源入库。
- 小程序所有页面使用 `navigationStyle: "custom"`。
- 原生小程序不直接使用 Pinia；资源状态集中到 `miniprogram/stores/resource-store.js`。
- 图片/视频会缓存到用户手机，本地展示路径和后端正式 URL 分离。
- 自定义导航标题不要用 WXML 属性实体串传递，避免显示 `&#x...`。
- tabBar 图标使用 `miniprogram/static/tab`，`-a` 未选中，`-b` 选中。
- 手动添加资源是临时入口，不替代企业微信导入。
- 手动上传的图片/视频作为结构化 `media` 保存，不塞进纯文本详情。
- 编辑页尽量接近用户最终看到的发布页，减少学习成本。
- 不向用户暴露“封面图片链接”这类技术字段。
- 资源进入编辑页后已经在资料库中，按钮使用“保存修改 / 发布并查看”，不再叫“保存草稿”。
- 资源详情页分享使用小程序原生 `open-type="share"`。
- 访问详情、统计、接龙名单属于发布者能力，普通客户不应看到。
- 同一用户对同一资源只允许一条有效接龙。
- 发布者标记已跟进后，客户侧应看到“发布者已跟进”。
- 管理页线索优先处理待跟进，支持按状态筛选。
- 高意向访客规则当前为“重复访问且未接龙”。
- 高意向访客待联系 / 已联系 / 备注已做后端持久化。
- 待联系提醒需要支持取消和标记已联系，避免本地提醒越积越多。

## 8. 下一步建议执行顺序

建议新会话按以下顺序继续：

1. 先读取项目规则和记忆文档：
   - `AGENTS.md`
   - `docs/project-memory.md`
   - `docs/decisions.md`
   - `docs/pitfalls.md`
   - `docs/dev-log.md`
   - `docs/handoff-latest.md`

2. 检查当前工作区：
   - `git status --short --branch`
   - `git diff --stat`
   - 确认不要提交 `backend/mock/runtime-state.json`、`docs/png/`、微信开发者工具本地配置。

3. 在微信开发者工具里人工验收最近补齐的小程序链路：
   - 手动添加资源。
   - 编辑页继续上传素材。
   - 保存修改、发布并查看。
   - 客户视角资源页隐私。
   - 客户提交接龙后已提交状态。
   - 发布者标记已跟进后客户侧已跟进状态。
   - 管理页接龙筛选、快捷动作。
   - 管理页访客高意向筛选与待联系提醒。
   - 待联系提醒的加入、标记已联系、取消待联系、清除记录。

4. 如果继续开发产品体验，优先做：
   - 给待联系线索增加跟进时间线和下次提醒时间。
   - 给线索增加手机号/微信号等客户字段，但注意普通客户侧隐私。
   - 客户资料库已支持私有客户标签、来源资料筛选、活跃/沉睡筛选、卡片快捷跟进和复制当前筛选跟进清单；后续如扩展标签，注意不要混用资源分类标签。
   - 继续补资源库/管理页的真实小程序人工验收问题。

5. 如果回到核心 P0 主链路，优先做：
   - 等用户解决企业微信认证/权限问题后，继续排查 `sync_msg 48002 api forbidden`。
   - 核对 `WECOM_SECRET` 是否为微信客服 Secret。
   - 核对企业微信后台是否允许 API 管理微信客服账号。
   - 核对 `WECOM_OPEN_KFID` 对应客服账号权限。
   - 核对可信 IP 是否包含 `81.70.84.35`。
   - 跑真实企业微信客服导入验收。

6. 每轮开发结束必须：
   - 运行小程序 JS 静态检查。
   - 运行小程序 JSON 解析检查。
   - 涉及后端时运行 `pytest backend\tests\test_app.py -q`。
   - 更新 `docs/dev-log.md`、`docs/decisions.md`、`docs/pitfalls.md`、`docs/handoff-latest.md`。
   - 合理 commit。

## 9. 新 Codex 会话接手时的第一条提示词

可直接复制给新 Codex：

```text
请先读取：

- AGENTS.md
- docs/project-memory.md
- docs/decisions.md
- docs/pitfalls.md
- docs/dev-log.md
- docs/handoff-latest.md

然后执行：

- git status --short --branch
- git diff --stat

请先不要改代码。请输出：

1. 你理解的项目目标
2. 当前代码状态
3. 已确认的重要决策
4. 当前风险
5. 下一步建议执行顺序

注意：

- docs/png/ 是页面参考图，不要提交或入库。
- backend/mock/runtime-state.json 是本地运行态数据，默认不要提交。
- miniprogram/project.config.json 和 project.private.config.json 是本地微信开发者工具配置，默认不要提交。
- 当前真实企业微信 sync_msg 仍卡在 48002 api forbidden，手动添加资源不能替代最终主链路。
- 客户资料库常用视图目前是小程序本地 storage 偏好，不是后端持久客户数据。
- 客户资料库卡片已按客户资料、跟进状态、来源和操作分区，后续新增字段时不要再平铺堆到卡片主区域。
- 禁止批量删除文件或目录，严格遵守 AGENTS.md。
```
## 2026-06-10 补充：企业微信客服回调地址已拆分

- teamBuy 当前企业微信客服回调地址已从 `/api/wecom/callback` 调整为 `/api/wecom/kf/teamBuy/callback`。
- 企业微信后台请填写：`https://teambuy.lifelove.top/api/wecom/kf/teamBuy/callback`。
- 后端 `GET` 验证、`POST` 事件接收和 `/api/wecom/config-check` 的推荐回调地址已同步新路径。
- README、企业微信客服配置清单、真实联调记录、MVP 测试清单和腾讯云部署文档已同步新路径。
- 已验证：`python -m compileall backend\app backend\tests` 通过；`pytest backend\tests\test_app.py -q -k "wecom_callback or wecom_config_check"` 4 项通过。
- 注意：整份 `pytest backend\tests\test_app.py -q` 当前仍有 1 个与本次无关的环境断言失败，原因是本机 `DATABASE_BACKEND` 读取为 `postgresql`，测试期望 `postgres`。
- 生产补充：已 SSH 登录 `ubuntu@81.70.84.35`，同步生产后端路由文件并重建/重启 `backend` 容器。公网新地址已返回 `"hello-teamBuy"`，`config-check` 已返回新 callbackUrl。生产 `WECOM_CALLBACK_TOKEN` 已同步为企业微信页面当前 Token，原 `.env` 已备份到服务器 `backend/.env.callback-backup-20260610-1616`。若后台保存仍失败，优先核对完整 43 位 `WECOM_ENCODING_AES_KEY`。
## 2026-06-10 补充：企业微信回调新地址已保存成功

- 当前企业微信后台 `API接收消息` 已保存为：`https://teambuy.lifelove.top/api/wecom/kf/teamBuy/callback`。
- 本次失败根因不是新路径不可达，而是 FastAPI GET 验证接口直接返回字符串时被编码为 JSON 字符串；企业微信要求纯文本原样返回 `echostr`。
- 已修复 `backend/app/api/routes_wecom.py`：`GET /api/wecom/kf/teamBuy/callback` 使用 `PlainTextResponse` 返回验证明文。
- 已部署生产并重启 backend 容器；公网验证返回 `200 text/plain`，正文为 `hello-teamBuy`。
- 企业微信后台页面已由 Codex 操作点击保存，页面提示“保存成功”；生产日志确认企业微信请求命中新路径并返回 200。
- 本地验证：`python -m compileall backend\app backend\tests` 通过；`pytest backend\tests\test_app.py -q -k "wecom_callback or wecom_config_check"` 4 项通过。

## 2026-06-15 补充：提交前整理规则

- 当前正式产品名已按用户修正为“资料整理助手”；小程序分享兜底标题也应使用“资料整理助手资源”。
- 本轮可提交范围是企业微信客服回调新路径、`text/plain` 验证响应、测试和配套文档。
- 默认不要提交 `backend/mock/runtime-state.json`、`docs/png/`、`miniprogram/project.config.json`、`miniprogram/project.private.config.json`、未确认验收报告草稿。
- `docs/悦享互动宝 MVP 产品开发文档.md` 当前存在疑似换行符扰动，除非专门处理品牌文档，否则不要混入回调修复提交。
- 本轮验证需注意：当前 shell 没有 `python` / `pytest` 命令；系统 `python3` 为 3.9，不适合跑本项目 pytest。使用 Python 3.12 临时环境验证通过。
- `backend/requirements.txt` 已将不可安装的 `Pillow==12.2.0` 调整为当前可安装的 `Pillow==11.3.0`。
- 最近验证：`python -m compileall backend/app backend/tests` 通过；`pytest backend/tests/test_app.py -q -k "wecom_callback or wecom_config_check"` 4 项通过；小程序 `.js` `node --check` 通过。
- 后续每次遇到错误、失败验证或规避规则，都要同步写入长期记忆文档，避免只留在聊天记录里。

## 2026-06-15 补充：企业微信资料归档接口前的媒体容错准备

- 已补强真实 `sync_msg` 收档链路：图片/视频 `media_id` 下载失败时，写入 `media_retry_jobs`，但不再阻断同批文本、链接等内容生成待认领草稿。
- 已关闭真实链路的 mock 媒体 fallback，避免下载失败时生成假的 `/mock-media/...` URL。
- mock 链路仍保留 fallback，方便本地演示。
- 图片压缩已改为 Pillow 转 WebP，视频继续使用 ffmpeg；避免本地或部署环境缺少 ffmpeg 图片编码能力时回退原图。
- 最近验证：`python -m compileall backend/app backend/tests` 通过；`pytest backend/tests/test_app.py backend/tests/test_media_processing_service.py backend/tests/test_media_storage_service.py -q` 41 项通过。
- 明天拿到企业微信资料归档接口权限后，优先用真实转发笔记验证：回调触发任务、`sync_msg` 拉取、文本成草稿、图片/视频转存、失败时补偿队列可见。

## 2026-06-17 补充：插件化架构 Phase 1 已落地

- 当前正式架构方向已固定为“企业微信稳定基座 + 混合驱动 Skill + 小程序笔记与展示页”。
- 完整架构文档已新增：`docs/stage2-docs/08-plugin-architecture.md`。
- 后端已新增第一版无状态 `skill-router`：
  - `GET /api/skills/commands`：返回快捷指令注册表。
  - `POST /api/skills/route`：快捷指令优先、规则匹配其次，未知输入返回确认菜单。
  - `POST /api/skills/content-to-note/run`：将 `ContentObject` 转为规则版 `UserNoteDraft`，暂不持久化。
- 已确认文字类来源统一进入 `content-to-note`：微信笔记、聊天记录、链接文章、手动文字和后续 OCR 都由 Adapter 转成 `ContentObject`。
- 已确认 `note-to-comic-image` 作为独立渲染型 Skill 保留；`showcase-builder` 是小程序可视化配置工具，不是 AI 自动全权生成。
- 本轮补丁遇到一次依赖装配文件上下文不匹配，原因是 `backend/app/api/dependencies.py` 已采用新服务装配结构；已按当前结构修正。
- 最近验证：
  - `/tmp/teambuy-pytest-venv312/bin/python -m compileall backend/app backend/tests` 通过。
  - `/tmp/teambuy-pytest-venv312/bin/python -m pytest backend/tests/test_skill_router.py -q`：6 项通过。
  - `/tmp/teambuy-pytest-venv312/bin/python -m pytest backend/tests/test_skill_router.py backend/tests/test_app.py backend/tests/test_media_processing_service.py backend/tests/test_media_storage_service.py -q`：47 项通过。
- 当前仍不要提交：
  - `backend/mock/runtime-state.json`
  - `docs/png/`
  - `docs/qa/当前项目_验收报告m1.md`
  - `miniprogram/project.config.json`
  - `miniprogram/project.private.config.json`
  - `docs/悦享互动宝 MVP 产品开发文档.md` 的疑似换行符扰动
- 下一步建议：
  1. 将现有企业微信 `sync_msg` 聚合结果接入 `ContentObject`，让真实导入和 Skill Router 共用同一条 `content-to-note` 入口。
  2. 增加 `SkillRun` 持久化和失败日志。
  3. 小程序新增“我的笔记”基础管理，再逐步承接展示页构建器。
  4. 等企业微信资料归档接口权限到位后，继续做 `wecom-archive-core` 的会话内容存档接入。

## 2026-06-17 补充：企业微信导入已接入 content-to-note

- `AGENTS.md` 已新增长期架构总纲，后续会话应优先遵守：
  - 完整架构文档入口：`docs/stage2-docs/08-plugin-architecture.md`。
  - 企业微信入口混合驱动：快捷指令优先、规则其次、AI 兜底。
  - 文字类来源统一进 `ContentObject -> content-to-note`。
  - 漫画图和展示页保持独立 Skill 边界。
- 已新增 `backend/app/services/content_object_adapter.py`，负责把企业微信 `RawMessage` 批次转为 `ContentObject`。
- `AppService.import_synced_messages()` 已改为先跑 `ContentObject -> content-to-note -> UserNoteDraft`，再兼容生成当前小程序依赖的 `Card` 草稿。
- 当前仍保留 `generatedCard`，不是最终笔记库模型；这是为了不打断现有待认领、编辑、发布和分享链路。
- 本轮发现并修复一个兼容问题：`link_article` 封面必须优先取链接 `coverUrl`，不能被企业微信媒体附件覆盖。
- 最近验证：
  - `/tmp/teambuy-pytest-venv312/bin/python -m compileall backend/app backend/tests` 通过。
  - `/tmp/teambuy-pytest-venv312/bin/python -m pytest backend/tests/test_skill_router.py backend/tests/test_app.py backend/tests/test_media_processing_service.py backend/tests/test_media_storage_service.py -q`：48 项通过。
- 下一步建议：
  1. 为 `SkillRun` 做持久化，记录导入时的 skillId、inputSnapshot、输出引用和失败原因。
  2. 为 `UserNote` / 笔记库建正式模型和接口。
  3. 小程序新增“我的笔记”基础管理页。
  4. 再把 `generatedCard` 逐步替换成 `UserNote` 到展示页/漫画图的后续流转。

## 2026-06-17 补充：P0/P1/P2 路线图已归档

- P0/P1/P2 实施路线图已新增：`docs/stage2-docs/09-p0-p2-roadmap.md`。
- 当前正式启动 P0 第一阶段：先把企业微信客服 `sync_msg` 作为过渡入口跑稳，不等待会话内容存档权限空转。
- 会话内容存档开通后进入 P0 第三阶段，并行新增 `wecom-archive-core`，不替换企业微信客服入口。
- 下一步优先做 `SkillRun` 持久化和导入失败日志，让 `content-to-note` 的每次执行可追踪、可排错、可计费。

## 2026-06-17 补充：工作区清理规则已更新

- 用户确认后续每次提交后尽量保持工作区干净。
- `docs/png/`、`docs/qa/当前项目_验收报告m1.md`、`miniprogram/project.config.json` 作为项目资料/配置纳入版本库。
- `miniprogram/project.private.config.json` 已加入 `.gitignore`，作为个人开发者工具配置保留本地。
- `backend/mock/runtime-state.json` 本地运行态改动已恢复，不提交测试运行数据。
- `docs/悦享互动宝 MVP 产品开发文档.md` 换行符扰动已恢复，不再污染后续 diff。

## 2026-06-17 补充：SkillRun 持久化和导入失败日志已完成

- 已新增 `SkillRun` 领域模型和仓储持久化，JSON / PostgreSQL 都支持。
- 企业微信导入成功时会保存 `content-to-note` 的成功 SkillRun，`outputRef` 指向当前兼容生成的 `Card`。
- 企业微信导入失败时会保存 failed SkillRun、失败导入批次和失败通知，失败可查询。
- 新增接口：
  - `GET /api/skills/runs`
  - `GET /api/wecom/import-failures`
- 最近验证：
  - `/tmp/teambuy-pytest-venv312/bin/python -m compileall backend/app backend/tests` 通过。
  - `/tmp/teambuy-pytest-venv312/bin/python -m pytest backend/tests/test_skill_router.py backend/tests/test_app.py backend/tests/test_media_processing_service.py backend/tests/test_media_storage_service.py -q`：50 项通过。
- 下一步建议：进入 P0 第二阶段前，先补强导入成功/失败通知口径和后台重试可视化；也可以直接开始正式 `UserNote` 模型和“我的笔记”基础接口。

## 2026-06-17 补充：导入通知口径和后台重试可视化已完成

- 导入通知文案已补强：
  - 成功：提示已整理完成，可去小程序认领、编辑和分类。
  - 成功但媒体未转存：提示媒体进入后台重试队列。
  - 失败：包含具体失败原因。
- 真实 `sync_msg` 导入通知 channel 使用 `wecom`，mock 导入继续使用 `mock`。
- 新增后台重试看板接口：`GET /api/wecom/retry-dashboard`。
- 新增失败导入重试接口：`POST /api/wecom/import-failures/retry?importBatchId=...`，需要 admin token。
- 失败导入重试按 `importBatchId` 重跑内容整理；媒体失败仍走 `media-retries/retry`，两类失败不要混用。
- 最近验证：
  - `/tmp/teambuy-pytest-venv312/bin/python -m compileall backend/app backend/tests` 通过。
  - `/tmp/teambuy-pytest-venv312/bin/python -m pytest backend/tests/test_skill_router.py backend/tests/test_app.py backend/tests/test_media_processing_service.py backend/tests/test_media_storage_service.py -q`：50 项通过。
- P0 第一阶段后端基础已基本收尾。下一步可以进入 P0 第二阶段：正式 `UserNote` 模型和“我的笔记”基础接口。

## 2026-06-17 补充：UserNote 模型和“我的笔记”基础接口已完成

- 已新增正式 `UserNote` 模型和仓储持久化，JSON / PostgreSQL 都支持。
- 企业微信导入成功后会双写：
  - `UserNote`：正式笔记库对象。
  - `Card`：当前小程序兼容草稿。
- `ImportBatch` 新增 `generatedNoteId`。
- `SkillRun.outputRef` 现在指向正式 `UserNote` ID。
- 认领导入时，note owner 会同步改为认领用户，状态从 `draft` 改为 `active`。
- 新增接口：
  - `GET /api/notes`
  - `GET /api/notes/{noteId}`
  - `PUT /api/notes/{noteId}`
  - `DELETE /api/notes/{noteId}`
- 笔记删除为软删除，不删除原始消息、导入批次或兼容卡片。
- 最近验证：
  - `/tmp/teambuy-pytest-venv312/bin/python -m compileall backend/app backend/tests` 通过。
  - `/tmp/teambuy-pytest-venv312/bin/python -m pytest backend/tests/test_skill_router.py backend/tests/test_app.py backend/tests/test_media_processing_service.py backend/tests/test_media_storage_service.py -q`：51 项通过。
- 下一步建议：小程序新增“我的笔记”基础页面，接 `/api/notes` 完成列表、搜索、详情、编辑和删除。

## 2026-06-17 补充：小程序“我的笔记”基础页面已完成

- 新增页面：
  - `miniprogram/pages/notes/index`
  - `miniprogram/pages/note-edit/index`
- 已接入 `/api/notes`：
  - 列表和搜索。
  - 详情查看。
  - 编辑保存。
  - 软删除。
- “我的”页和资源库快捷区已增加“我的笔记”入口。
- `services/api.js` 已支持 `fetchNotes`、`fetchNote`、`updateNote`、`deleteNote`，并归一化 note / generatedNote 媒体 URL。
- 最近验证：
  - 小程序所有 `.js` `node --check` 通过。
  - 小程序所有 `.json` 解析通过。
- 仍需微信开发者工具或真机人工验收页面渲染、输入、保存、删除和返回刷新。

## 2026-06-17 补充：会话内容存档配置与 wecom-archive-core 骨架已完成

- 用户已开通企业微信会话内容存档，后台页面为 `https://work.weixin.qq.com/wework_admin/frame#financial/corpEncryptData`。
- 本轮已生成 RSA 密钥对：
  - 私钥：`backend/secrets/wecom_archive_private.pem`
  - 公钥：`backend/secrets/wecom_archive_public.pem`
  - `*.pem` 被 `.gitignore` 忽略，不进入 Git。
- 配置文档已新增：`docs/stage2-docs/10-wecom-archive-config.md`。
- 后端新增会话存档配置项：
  - `WECOM_ARCHIVE_ENABLED`
  - `WECOM_ARCHIVE_SECRET`
  - `WECOM_ARCHIVE_PRIVATE_KEY_PATH`
  - `WECOM_ARCHIVE_PUBLIC_KEY_PATH`
  - `WECOM_ARCHIVE_SDK_LIB_PATH`
- 后端新增会话存档基础模型和仓储：
  - `WecomArchiveCursor`
  - `WecomArchiveMessage`
  - `wecom_archive_cursors`
  - `wecom_archive_messages`
- 新增接口：
  - `GET /api/wecom/archive/callback`
  - `POST /api/wecom/archive/callback`
  - `GET /api/wecom/archive/config-check`
  - `GET /api/wecom/archive/cursor`
  - `GET /api/wecom/archive/messages`
  - `POST /api/wecom/archive/mock-messages`
- 会话存档事件服务器当前默认复用 `WECOM_CALLBACK_TOKEN` 和 `WECOM_ENCODING_AES_KEY`；如后续拆独立配置，使用 `WECOM_ARCHIVE_CALLBACK_TOKEN` 和 `WECOM_ARCHIVE_ENCODING_AES_KEY`。
- 原始会话存档消息查询和样例写入需要 admin token。
- Codex 内置浏览器读取企业微信后台页面时，DOM/截图连续超时；本轮没有自动点击保存后台配置。后续应按配置文档人工粘贴公钥并保存，保存前确认页面是“会话内容存档”而不是“微信客服 API 接收消息”。
- 最近验证：
  - `/tmp/teambuy-pytest-venv312/bin/python -m compileall backend/app backend/tests` 通过。
  - `/tmp/teambuy-pytest-venv312/bin/python -m pytest backend/tests/test_app.py -q -k "wecom_archive or wecom_config_check"`：7 项通过。
  - `/tmp/teambuy-pytest-venv312/bin/python -m pytest backend/tests/test_skill_router.py backend/tests/test_app.py backend/tests/test_media_processing_service.py backend/tests/test_media_storage_service.py -q`：57 项通过。
- 下一步建议：
  1. 生产已部署完成。公网 `/api/wecom/archive/config-check` 返回 `success=true`、`missing=[]`，公网 `/api/wecom/archive/callback?token=...&echostr=hello-archive` 返回 `hello-archive`。
  2. 企业微信后台“设置接收事件服务器”已保存成功。当前生产 archive 专用 Token / EncodingAESKey 已写入 `WECOM_ARCHIVE_CALLBACK_TOKEN` / `WECOM_ARCHIVE_ENCODING_AES_KEY`。
  3. 在企业微信后台会话内容存档页粘贴 `docs/stage2-docs/10-wecom-archive-config.md` 里的公钥并保存。
  4. 不要把真实 `WECOM_ARCHIVE_SECRET` 写入任何 Git 文档；真实值只留在生产 `backend/.env`。
  5. 接官方会话内容存档 SDK，拉取加密消息、解密、写入 `wecom_archive_messages` 并推进 `wecom_archive_cursors.seq`。
  6. 把解密消息 Adapter 到 `ContentObject -> content-to-note -> UserNote`。

## 2026-06-17 补充：会话存档回调已部署生产

- 用户提供生产 SSH key：`/Users/yiyi/Desktop/Desktop/vedo-project/vidoekey.pem`。
- 已用 `rsync` 同步后端代码到服务器，原因是服务器 `git fetch origin` 长时间卡住。
- 同步前已备份服务器 `backend/app/api/routes_wecom.py` 本地 diff 到 `/home/ubuntu/teamBuy-deploy-backups/`。
- 已同步会话存档 RSA 密钥到服务器 `backend/secrets/`，并重建/重启 backend 容器。
- 生产 Docker 环境的会话存档密钥路径已修正为容器内绝对路径：
  - `/app/secrets/wecom_archive_private.pem`
  - `/app/secrets/wecom_archive_public.pem`
- 生产验证：
  - `/api/wecom/archive/config-check`：`success=true`，`missing=[]`。
  - `/api/wecom/archive/callback`：用生产 token 验证返回 `hello-archive`。
- 用户企业微信后台截图里填写的是本地 `backend/.env` 的 Token/AESKey；生产原 `WECOM_CALLBACK_TOKEN` / `WECOM_ENCODING_AES_KEY` 与本地不同。
- 为避免影响已跑通的微信客服回调，已将本地这组值写入生产 archive 专用配置：
  - `WECOM_ARCHIVE_CALLBACK_TOKEN`
  - `WECOM_ARCHIVE_ENCODING_AES_KEY`
- 重启后公网验证 `/api/wecom/archive/callback` 使用 archive 专用 token 返回 `archive-token-ok`。
- 用户确认企业微信后台“接收事件服务器”已保存成功。

## 2026-06-17 补充：P0 会话存档拉取与转笔记链路已实现

- 已新增真实会话存档拉取入口：
  - `POST /api/wecom/archive/pull`
  - 需要 admin token。
  - 从 `wecom_archive_cursors.seq` 继续拉取企业微信会话存档数据。
  - 拉取成功后写入 `wecom_archive_messages` 并推进游标。
  - SDK 或配置缺失时返回 502，并记录 failed 游标。
- 已新增归档消息处理入口：
  - `POST /api/wecom/archive/process`
  - 需要 admin token。
  - 将已解密归档消息转换为 `ContentObject -> content-to-note -> UserNote`。
  - 成功后在归档消息上记录 `generatedNoteId`、`generatedCardId`、`processedAt`，重复调用不会重复生成笔记。
  - 处理失败会写入 `processError`。
- 已新增官方 SDK 封装：
  - `backend/app/services/wecom_archive_client.py`
  - 调用 `GetChatData`、RSA 解密 `encrypt_random_key`、调用 `DecryptData`。
- 已补充配置项：
  - `WECOM_ARCHIVE_SDK_LIB_PATH`
  - `WECOM_ARCHIVE_PULL_LIMIT`
  - `WECOM_ARCHIVE_SDK_TIMEOUT_SECONDS`
  - `WECOM_ARCHIVE_PROXY`
  - `WECOM_ARCHIVE_PROXY_PASSWORD`
- 最近验证：
  - `/tmp/teambuy-pytest-venv312/bin/python -m compileall backend/app backend/tests` 通过。
  - `/tmp/teambuy-pytest-venv312/bin/python -m pytest backend/tests/test_app.py -q -k "wecom_archive"`：9 项通过。
  - `/tmp/teambuy-pytest-venv312/bin/python -m pytest backend/tests/test_skill_router.py backend/tests/test_app.py backend/tests/test_media_processing_service.py backend/tests/test_media_storage_service.py -q`：60 项通过。
- 仍需人工/生产验证：
  1. 将企业微信官方会话存档 Linux SDK 动态库放到生产容器可读路径。
  2. 设置生产 `WECOM_ARCHIVE_SDK_LIB_PATH` 为容器内绝对路径。
  3. 重启 backend 后确认 `/api/wecom/archive/config-check` 返回 `sdkConfigured=true`。
  4. 企业微信真实发一条测试消息。
  5. 调用 `/api/wecom/archive/pull`，确认 `savedCount>0` 或明确看到无新消息。
  6. 调用 `/api/wecom/archive/process`，确认生成 `UserNote`。
  7. 在小程序“我的笔记”中查看、编辑、删除该笔记。

## 2026-06-17 补充：P0 已部署生产，等待官方 SDK 动态库

- 生产已部署 commit：`5e104f0 feat: complete p0 wecom archive import`。
- 生产 `WECOM_ADMIN_TOKEN` 已补齐，值未写入文档。
- 生产公网验证：
  - `/api/wecom/archive/config-check`：`missing=[]`、`privateKeyReadable=true`、`sdkConfigured=false`。
  - `/api/wecom/archive/pull`：返回 502，错误明确为缺少 `WECOM_ARCHIVE_SDK_LIB_PATH`，并写入 failed cursor。
  - `/api/wecom/archive/process`：返回 200，当前 `processedCount=0`。
- 结论：
  - P0 后端链路已经部署。
  - 事件服务器已保存成功。
  - 真实会话内容拉取还不能人工通过，唯一阻塞是生产未安装/未配置企业微信官方会话存档 SDK 动态库。
- 下一步最自然顺序：
  1. 下载企业微信官方 Linux 会话存档 SDK 动态库。
  2. 放到服务器并让 Docker 容器可读。
  3. 设置 `WECOM_ARCHIVE_SDK_LIB_PATH=/app/secrets/<sdk动态库文件名>`。
  4. 重启 backend。
  5. 确认 `sdkConfigured=true`。
  6. 发真实企业微信测试消息。
  7. 调用 `pull -> process -> 小程序我的笔记验收`。

## 2026-06-17 补充：官方 SDK 已部署，真实拉取接口已跑通

- 用户下载了官方 Linux x86 v3.0 SDK：`sdk_x86_v3_20250205.tgz`。
- 已上传 `C_sdk/libWeWorkFinanceSdk_C.so` 到生产服务器 `backend/secrets/`。
- 已配置生产：
  - `WECOM_ARCHIVE_SDK_LIB_PATH=/app/secrets/libWeWorkFinanceSdk_C.so`
- 已修正 `docker-compose.yml`，让 backend 容器只读挂载 `./backend/secrets:/app/secrets:ro`。
- 生产公网验证：
  - `/api/wecom/archive/config-check`：`sdkConfigured=true`。
  - `/api/wecom/archive/pull`：返回 200，当前 `rawCount=0`、`savedCount=0`，说明 SDK 调用成功但没有新消息。
  - `/api/wecom/archive/process`：返回 200，当前 `processedCount=0`。
- 下一步人工验证只剩真实数据：
  1. 用已开启会话存档的成员和外部联系人产生一条新会话。
  2. 再调用 `/api/wecom/archive/pull`，预期 `rawCount` 或 `savedCount` 大于 0。
  3. 调用 `/api/wecom/archive/process`，预期生成 `UserNote`。
  4. 小程序“我的笔记”查看、编辑、删除该笔记。

## 2026-06-17 补充：21:57 测试消息暂未拉到

- 用户 21:57 发送“你好啊”后，已在 21:58 和 21:59 两次调用生产 `/api/wecom/archive/pull`。
- 两次均返回 200，但 `rawCount=0`、`savedCount=0`。
- `/api/wecom/archive/messages?limit=20` 仍为空数组。
- 后端日志无 SDK 报错，说明当前不是接口异常。
- 后续排查顺序：
  1. 确认发送该消息的企业微信成员正好在会话存档“开启范围”内。
  2. 确认这条消息是企业微信成员与微信外部联系人的会话，而不是普通内部会话、自己给自己发、或未纳入服务版范围的场景。
  3. 等待企业微信归档延迟后再拉取。
  4. 若持续为 0，再查看企业微信后台是否有“待同意/未授权/未生效”的范围提示。

## 2026-06-17 补充：客服通道目前被企业微信拒绝

- 生产客服配置检查通过：
  - `useMock=false`
  - `missing=[]`
  - `configured=true`
  - callback URL：`https://teambuy.lifelove.top/api/wecom/kf/teamBuy/callback`
- 手动触发生产 `/api/wecom/real-sync` 时，企业微信返回：
  - `errcode=48002`
  - `errmsg=api forbidden`
  - 来源 IP：`81.70.84.35`
- 最近后端日志没有看到企业微信访问 `/api/wecom/kf/teamBuy/callback`。
- 当前结论：
  - 后端客服通道配置已具备。
  - 企业微信后台客服 API 权限、可信 IP 或接收消息服务器配置仍需处理。
  - 现在还不能确认用户测试消息是否走了客服通道，因为客服 `sync_msg` 被企业微信拒绝。

## 2026-06-17 补充：会话存档真实链路已跑通

- 用户 23:41 发送测试消息后，企业微信会话存档开始返回真实数据。
- 本轮修复：
  - 修正 C SDK `DecryptData` ctypes 签名，多传 `sdk` 指针会导致 `10008`。
  - 增加归档 `msgtime` 毫秒时间戳归一化，避免 Pydantic 字符串校验失败。
- 生产验证结果：
  - `/api/wecom/archive/pull`：`rawCount=1`、`savedCount=1`、cursor `seq=1`。
  - 实际收到文本：`归档测试2218资料管理助手`。
  - `/api/wecom/archive/process`：`processedCount=1`、`failedCount=0`。
  - 生成笔记：`note_fc9f58783e`。
  - 生成兼容卡片：`card_ec1e041dde`。
- 当前结论：
  - P0 主链路已跑通：企业微信外部联系人消息 -> 会话存档 SDK -> 原始归档消息 -> `ContentObject -> content-to-note -> UserNote`。
- 下一步建议：
  1. 小程序“我的笔记”人工查看 `note_fc9f58783e` 是否可见。
  2. 验证编辑、删除、搜索。
  3. 再发一条更接近业务场景的客户资料长文本，验证摘要质量。

## 2026-06-18 补充：自动归档 worker 与新导入页已完成

- 后端自动归档 worker 已实现并部署生产。
- 生产配置：
  - `WECOM_ARCHIVE_WORKER_ENABLED=true`
  - `WECOM_ARCHIVE_WORKER_INTERVAL_SECONDS=60`
- 生产公网 `/api/wecom/archive/config-check` 已确认：
  - `sdkConfigured=true`
  - `workerEnabled=true`
  - `workerIntervalSeconds=60`
  - `missing=[]`
- 当前企业微信新消息会由 worker 自动执行 `pull -> process`，不再需要 Codex 手动调用。
- 小程序“待认领”页已改为“新导入资料”：
  - 默认展示标题和内容。
  - 支持通用 / 中介 / 团购模板按钮。
  - 认领后优先进入笔记编辑页。
  - 笔记编辑页显示模板字段提示。
- 仍然保留待认领原因：
  - `identity-core` 尚未实现。
  - 企业微信外部联系人和小程序用户还不能自动绑定。
  - 上线前应把“待认领”改成弱入口或异常入口。

## 2026-06-18 补充：微信笔记 note 解析与 5 秒聚合已部署

- 用户 03:39 发送房产类型微信笔记，企业微信归档为 `msgType=note`。
- 已支持解析 `note.info.items`：
  - 文本进入正文。
  - 位置进入 `locationText` 和正文。
  - 多张图片进入 media 引用。
- 已支持普通多消息 5 秒聚合：
  - 同一会话。
  - 同一发送人。
  - 相邻消息不超过 5 秒。
  - 非 `note` 类型。
- 生产验证：
  - 用 03:39 note 的 mock 副本生成 `note_da48e67e5e`。
  - 正文包含房源小区、户型、价格、商圈、备注、位置。
  - `mediaCount=5`。
- 注意：
  - 已经生成过的 `note_03cdd38ad5` 不会自动重处理。
  - 验证副本 `note_da48e67e5e` 会暂时出现在待认领列表。
  - 下一步仍建议做会话存档媒体下载转存，让图片真实显示。

## 2026-06-18 补充：图片不显示的原因与下一步收口

- 用户确认当前产品后续继续按“真实企业微信链路小范围生产联调，重要经验沉淀到文档”的思路推进。
- 当前图片不显示不是用户测试错误：
  - 企业微信归档图片已经作为 `sdkfileid` 引用进入 `UserNote.media`。
  - 但官方 SDK `GetMediaData` 下载媒体本体和服务端转存还未实现。
  - 因此小程序暂时没有可预览的 `media.url`。
- 下一步最自然开发：
  1. 实现会话存档媒体下载服务，支持通过 `sdkfileid` 调官方 SDK `GetMediaData`。
  2. 下载后进入现有媒体处理链路，图片转 WebP，先保存到服务器媒体目录，后续再换 COS/CDN。
  3. 将持久化 URL 写回 `UserNote.media.url` 和兼容 `Card.media.url`。
  4. 增加失败记录和后台重试，避免媒体失败阻断文字笔记。
  5. 用用户重新发送的一组“图片 + 文字 + 微信笔记”做人工验收。
- 已确认原则：
  - 小程序本地缓存只是展示优化，不能作为资料库正式图片存储。
  - 会话存档不负责回复用户；处理完成通知要后续单独接企业微信应用消息、微信客服消息或小程序订阅消息。
  - P0 阶段可继续在生产小范围联调，P1/P2 前建议拆 staging/test 环境。

## 2026-06-18 补充：会话存档媒体下载转存已实现

- 已实现会话存档媒体本体下载：
  - `backend/app/services/wecom_archive_client.py` 新增 `download_media()`。
  - 官方 SDK `GetMediaData` 按 `outindexbuf` 分片循环下载，直到 `is_finish`。
  - 二进制数据通过 `GetDataLen/GetData` 长度读取，避免字符串截断。
- 已接入归档处理：
  - `process_wecom_archive_messages(limit, archive_client)` 会在生成 `content-to-note` 前补齐 media URL。
  - 成功下载的图片进入现有媒体处理链路，转 WebP 后保存到 `/media`。
  - `UserNote.media.url`、兼容 `Card.coverUrl` 和 `Card.media.url` 会拿到正式媒体地址。
  - 下载失败不阻断文字笔记，会记录 `media_retry_jobs`，结果里返回 `media.failedCount`。
- worker 和手动接口都已传 archive client：
  - 自动 worker 每轮 `pull -> process` 会尝试下载媒体。
  - `POST /api/wecom/archive/process` 手动触发也会尝试下载媒体。
- 已验证：
  - compileall 通过。
  - `pytest backend/tests/test_app.py -q -k "wecom_archive"`：14 passed。
  - 相关后端测试：65 passed。
- 生产状态：
  - 已部署到 `https://teambuy.lifelove.top`。
  - `/api/wecom/archive/config-check` 确认 `sdkConfigured=true`、`workerEnabled=true`、`missing=[]`。
  - 手动 `/api/wecom/archive/process?limit=20` 返回 `processedCount=0`，当前没有未处理的新归档消息。
- 下一步建议：
  1. 用用户新发的“图片 + 文字 + 微信笔记”真实验证 `media.downloadedCount` 和小程序图片展示。
  2. 对已经生成但没有 URL 的旧图片笔记，后续可以补一个“历史媒体补下载/回填”脚本或后台按钮。

## 2026-06-18 补充：历史媒体补下载/回填已实现

- 新增后台接口：
  - `POST /api/wecom/archive/media-backfill`
  - admin token 保护。
  - `limit` 控制本次最多处理的缺失媒体数量。
- 回填范围：
  - 已有 `UserNote.media[]` 里 `mediaId` 存在、`url` 为空的媒体。
  - 不处理已经有 URL 的媒体，不覆盖用户已编辑内容。
- 回填结果：
  - 成功下载后写回 `UserNote.media.url`。
  - 若对应 `sourceCardId` 存在，会补齐 `Card.coverUrl` 和 `Card.media`，保证当前小程序旧卡片展示链路也能看到图片。
  - 下载失败写入 `media_retry_jobs`，继续处理下一条。
- 已验证：
  - archive 测试 15 passed。
  - 相关后端测试 66 passed。
- 下一步生产部署后可手动调用一次回填接口，补旧的 `note_f6cfe62264`、`note_866ce69346`、`note_da48e67e5e` 等历史无 URL 图片笔记。
- 生产首次回填已执行：
  - 成功回填 `note_da48e67e5e` 的 5 张图片，兼容卡片 `card_9af73ff8e0` 已更新。
  - 另有 2 个旧图片失败，原因是超长 `sdkfileid` 造成本地文件名过长。
  - 已修复媒体文件名规则：超长 media ID 截断并追加 hash，等待重新部署后再跑一次回填。
- 生产二次回填已执行：
  - 成功回填 `note_f6cfe62264` 和 `note_866ce69346`。
  - `downloadedCount=2`、`failedCount=0`。
  - 当前已知历史无 URL 图片笔记已补齐。

## 2026-06-18 补充：identity-core 第一版已实现

- 已新增“认领后绑定”：
  - 用户第一次认领导入后，保存 `wecom_external_user + externalUserId -> ownerUserId`。
  - 后续同来源企业微信客服导入和会话存档导入，会自动归属到该小程序用户。
  - 自动归属的 `ImportBatch.status=claimed`，不再进入“新导入资料/待认领”列表。
  - `UserNote.ownerUserId` 和兼容 `Card.ownerUserId` 会直接写成绑定用户。
- 新增模型/表：
  - `WecomIdentityBinding`
  - `wecom_identity_bindings`
- 已验证：
  - 认领后绑定。
  - 企业微信客服 mock 后续导入自动归属。
  - 会话存档后续导入自动归属。
  - 相关后端测试 68 passed。
- 当前边界：
  - 仍然是 mock 登录用户 ID。
  - 不是正式微信 code/openid/unionid 绑定。
  - 上线前仍需补正式微信登录和更清晰的绑定管理/解绑能力。
- 生产状态：
  - 已部署生产。
  - `/health` 正常。
  - PostgreSQL `wecom_identity_bindings` 表已确认存在。
  - 下一次用户从小程序认领某个来源后，同来源后续新消息应自动进入其笔记库。

## 2026-06-18 补充：URL 默认轻收藏已实现

- 产品口径：
  - 普通文章 URL 默认是轻收藏，但轻收藏不是通用笔记模板，而是文章收藏卡。
  - 第一层只展示原始链接、标题、封面、来源、收藏时间、基础标签、基础分类和一句话摘要。
  - 用户点击轻收藏卡片默认打开原文；公众号文章优先尝试小程序官方能力，普通网页降级复制链接。
  - 用户明确点企业微信快捷指令或输入 `整理链接` 等指令时，才做深度整理。
  - 小程序内可以从轻收藏手动升级为深度笔记。
- 已实现：
  - Skill Router 新增 `link_bookmark` 意图。
  - 普通 URL 规则路由到 `link-bookmark`。
  - `整理链接` 精确指令仍路由到 `content-to-note`。
  - 企业微信客服导入和会话存档导入已走统一判断。
  - 轻收藏后端字段已包含 `category/sourceName/sourceLabel/openAction/sourceUrl`。
  - 小程序“我的笔记”列表轻收藏已按文章卡片展示。
  - 小程序轻收藏详情页已从通用模板改成文章卡 + 基础信息 + 整理动作。
- 已验证：
  - compileall 通过。
  - 相关后端测试 70 passed。
  - 小程序 JS 静态检查通过。
  - 生产 `/health` 正常。
  - 生产路由验证：普通 URL 返回 `link_bookmark`，`整理链接` 返回 `content_to_note`。
  - 生产容器内 `run_link_bookmark()` 验证轻收藏字段已包含 `category/sourceName/sourceLabel/openAction/sourceUrl`。
- 当前边界：
  - 轻收藏第一版不抓取全文，只保存来源链接、标题、封面和简短描述。
  - 非公众号普通外链在微信小程序内不能保证直接打开，当前降级为复制链接。
  - “整理为笔记”第一版主要完成状态升级和编辑入口，后续可接入真正文章抓取、正文提取和大模型结构化摘要。
- 未提交/不应提交的本地文件仍需注意：
  - `miniprogram/project.config.json` 是微信开发者工具本地配置扰动，除非明确需要，不纳入功能提交。
  - `企业微信客服服务须知.pdf` 是本地参考文件，不纳入提交。

## 2026-06-18 补充：强标签、弱分类、专题聚合第一版已实现

- 架构文档：
  - `docs/stage2-docs/11-tag-topic-search-architecture.md`
- 产品口径：
  - 不做强制三级分类。
  - 来源类型对齐微信收藏基础类型。
  - `systemCategory` 是系统弱分类。
  - `tags/userTags/tagLevels` 是核心组织结构。
  - `topics/topicIds` 替代多级文件夹，承载场景集合。
- 已实现：
  - 轻收藏规则标签入库。
  - 标签建议接口。
  - 专题列表、创建、加入资料、移出资料接口。
  - 笔记列表按来源类型、标签、专题、排序筛选。
  - 小程序“我的笔记”筛选条。
  - 小程序轻收藏编辑页支持来源类型、弱分类、用户标签、专题。
  - 小程序新增“专题”页面和“我的”页入口。
- 已验证：
  - compileall 通过。
  - 相关后端测试 70 passed。
  - 小程序 JS 检查通过。
  - 小程序 JSON 解析通过。
  - 生产 `/health` 正常。
  - 生产 PostgreSQL 已确认存在 `topics` 表。
  - 生产专题接口路由与用户校验生效。
- 当前边界：
  - 暂未接 L2 轻模型和 L3 大模型。
  - 暂未做标签重命名、合并、批量打标。
  - 专题关系暂存在 `UserNote.visibilityConfig.topicIds`，不是最终表结构。

## 2026-06-19 补充：资料详情支持用户补传图片/视频

- 背景：
  - 用户确认贝壳等第三方小程序房源不应强依赖自动抓取完整图片/价格/字段。
  - 产品主链路应允许“保存原小程序入口 + 用户自己补字段/图片/视频 + 使用 SCRM/留资/预约/客户页”。
- 已实现：
  - `miniprogram/pages/note-edit/` 的“图片与视频”板块新增“添加”入口。
  - 支持添加图片和视频，复用现有 `api.uploadAsset()` / `POST /api/uploads/asset`。
  - 上传成功后自动保存当前资料，首张图片自动设为封面。
  - 媒体列表避免封面和同一张图片重复显示；删除封面媒体时会自动换下一张图片或清空封面。
  - 编辑页视频素材可直接播放。
  - `miniprogram/pages/note-preview/` 客户页预览新增“房源视频”区，补传视频会展示给客户。
- 已验证：
  - 小程序 JS 静态检查通过。
  - 小程序 JSON 解析检查通过。
  - `git diff --check` 通过。
- 当前边界：
  - 上传接口已存在，本次未改后端。
  - 视频第一版只做展示/播放，不做封面截帧、转码、压缩进度条或排序。
  - 需要用户在微信开发者工具重新编译预览后实测真机上传权限、视频播放和客户页展示。

## 2026-06-19 补充：房源轻 SCRM 支持按资料查看客户动作

- 背景：
  - `customer-action-plugin` 第一版已让客户页留资、预约持久化到 `customer_actions`，并投影到 `lead_reminders`。
  - 用户确认发布者更希望在房源资料详情里查看这套房源的客户动作，而不是只去全局线索列表搜索。
- 已实现：
  - 后端新增 `GET /api/notes/{note_id}/customer-actions?ownerUserId=...`，按 noteId 返回动作汇总、动作明细和已投影线索。
  - `miniprogram/pages/note-edit/` 的“轻 SCRM”板块显示客户动作、留资和待跟进数量。
  - “轻 SCRM”板块新增“查看客户动作 / 查看线索”入口，跳转 `pages/note-actions/index`。
  - 有待跟进线索时，轻 SCRM 标题和入口显示红点；红点绑定 `pending` 线索。
  - 新增 `miniprogram/pages/note-actions/`，展示当前资料的客户动作时间线和线索列表，可继续进入线索详情。
- 已验证：
  - 目标后端测试通过。
  - 新增小程序页面 JS 静态检查通过。
  - 小程序 JSON 解析检查通过。
- 当前边界：
  - 红点目前不是已读/未读事件表，而是待跟进线索提醒；线索状态改为已联系或归档后应消失。
  - 咨询点击等动作后续接入 `customer-action-plugin` 后，可继续复用当前 note 级入口展示。
  - 仍需微信开发者工具/真机验证页面跳转、红点视觉和线索处理后的刷新效果。

## 2026-06-19 补充：客户动作接口已部署生产并修复多端按钮适配

- 问题：
  - 用户在手机/iPad 上测试客户页留资和房源轻 SCRM 动作页时遇到 `Not Found`。
  - 资料详情顶部动作按钮和客户预览页浮动分享按钮在不同设备上样式不稳定。
- 根因：
  - 小程序 `apiBaseUrl` 指向生产域名，但生产后端还没有部署 `customer_actions` 新接口。
  - 部分按钮使用固定 `line-height`，不同屏宽渲染容易变形或过小。
- 已处理：
  - 已用 rsync 同步后端 `backend/app/` 和 `backend/mock/customer-actions.json` 到生产服务器。
  - 已重建并重启 `teambuy-backend` 容器。
  - 公网 `/health` 正常。
  - 公网 `/api/notes/note_not_exists/customer-actions?...`、`/customer-actions/config`、`POST /customer-actions/lead-contact` 已不再返回路由级 `Not Found`，而是业务级“笔记不存在”。
  - 小程序资料详情顶部三按钮、保存分享图、客户预览页好友/朋友圈和提交按钮已改为 rpx + flex 适配。
- 已验证：
  - 后端全量测试 98 passed。
  - 小程序 JS 静态检查通过。
  - 小程序 JSON 解析通过。
  - `git diff --check` 通过。
- 下一步：
  - 需要在微信开发者工具重新编译，并上传/预览新版小程序前端，手机和 iPad 才能看到样式修复。

## 2026-06-19 补充：真机身份隔离、SCRM 红点已读和电话拨号

- 问题：
  - 两个不同微信真机测试看到同样数据。
  - 轻 SCRM 红点点开后不消失。
  - 待联系和 SCRM 线索页手机号旁缺少直接拨号入口。
- 根因：
  - 登录页仍走固定“本地测试用户”mock 身份，两个微信默认 nickname 一样时后端 openid 一样。
  - 红点绑定 pending 数量，不是“未读动作”。
- 已实现：
  - 后端新增 `POST /api/auth/wechat-login`，支持通过微信 code 换 openid。
  - 生产后端已部署该路由；当前服务器未配置 `WECHAT_MINIAPP_SECRET`，所以公网返回明确配置提示，不是 404。
  - 小程序登录优先走微信登录；正式登录未配置时使用设备级唯一 mock openid 兜底，避免串用户。
  - 小程序启动时清理旧 `openid_本地测试用户` 缓存。
  - 轻 SCRM 红点改成本机已读：点开“查看客户动作 / 查看线索”后红点立即消失。
  - 待联系列表、线索详情、房源客户动作页手机号旁增加“拨号”入口。
- 已验证：
  - 后端全量测试 99 passed。
  - 小程序 JS 静态检查通过。
  - 小程序 JSON 解析通过。
  - `git diff --check` 通过。
- 下一步：
  - 生产服务器 `.env` 需要补 `WECHAT_MINIAPP_APPID=wxf43f7bc098d9858b` 和真实 `WECHAT_MINIAPP_SECRET`，再重启 backend，才是正式 openid 登录。
  - 目前设备级 mock 只用于未配置 AppSecret 的真机测试隔离，不等于正式登录体系。

## 2026-06-19 补充：已支持当前用户生成房源测试数据

- 背景：
  - 用户需要几组假数据在真机上测试房源详情、轻 SCRM、客户动作、红点和拨号。
- 已实现：
  - 后端新增 `POST /api/notes/demo-data?ownerUserId=...`。
  - 小程序“我的”页新增“生成测试房源数据”。
  - 每次会给当前登录用户生成 3 条房源资料、2 条线索、3 条客户动作。
  - 数据属于当前用户，可用于验证两个微信账号是否隔离。
  - 生产后端已部署并验证成功。
- 测试路径：
  - 重新编译/预览小程序。
  - 登录后进入“我的”。
  - 点击“生成测试房源数据”。
  - 到“我的笔记”看 3 条测试房源。
  - 进测试房源 A，看轻 SCRM 红点和“查看客户动作 / 查看线索”。
  - 到“待联系线索”测试手机号拨号。
- 已验证：
  - 后端全量测试 100 passed。
  - 小程序 JS/JSON 检查通过。
  - `git diff --check` 通过。

## 2026-06-19 补充：我的笔记卡片前置 SCRM 入口

- 用户反馈：
  - “我的笔记”搜索按钮太长太黑。
  - 房源 SCRM 入口藏在详情页下方太深，应该在房源笔记卡片直接进入。
  - 卡片有未读客户动作时要显示红点，点进 SCRM 后红点消失。
  - 资料详情顶部三按钮文字不居中，浮动“存”太小。
- 已处理：
  - 搜索按钮改短并使用主题蓝色。
  - 房源/团购笔记卡片加载后补取 note 级客户动作汇总。
  - 卡片右上显示未读红点，底部新增 `SCRM` 胶囊入口。
  - 点击卡片 `SCRM` 会写入本机已读时间并跳转客户动作页，红点立即消失。
  - 资料详情顶部三按钮改为专用 class + 内部 text + flex 居中。
  - 浮动保存按钮放大到 84rpx。
- 已验证：
  - 小程序 JS 静态检查通过。
  - 小程序 JSON 解析通过。
  - `git diff --check` 通过。
- 注意：
  - 这是小程序前端改动，需要重新编译/预览/上传小程序才能在真机看到。

## 2026-06-19 补充：生产登录、归属和 chatrecord 解析已修复

- 生产状态：
  - `WECHAT_MINIAPP_APPID` / `WECHAT_MINIAPP_SECRET` 已配置到生产 `backend/.env`，backend 已重建重启。
  - 真机微信登录已成功创建真实用户 `user_25ec00a0f0`。
  - 企业微信外部联系人 `wmCSe7EwAAwsQd1LH-iNoA78ey4i0cXg` 已绑定到 `user_25ec00a0f0`。
  - 2026-06-19 当天 4 条误归属导入已迁移到该真实用户。
- 解析状态：
  - 新增注册式归档 parser：`backend/app/services/archive_message_parsers.py`。
  - `chatrecord` 已能解析 `ChatRecordText` 正文并识别鸡蛋团购商品。
  - 生产中两条旧 `chatrecord` 笔记已原地修复为“白凤乌鸡蛋 / groupbuy_product / 团购”。
- 前端状态：
  - 商品工作台重排为“商品信息 / 图片与视频 / 规格与价格 / 自提配送 / 团购接龙”。
  - 客户页商品卡先展示规格与价格；开启团购接龙后才显示提交入口。
  - 图片缩略图保持普通图片原样 `aspectFill`，封面角标只红字，不再额外铺背景。
- 验证：
  - 生产 `/health` 正常。
  - 生产当前用户笔记接口已返回“白凤乌鸡蛋 / groupbuy_product / 团购”。
  - 本地后端全量测试 103 passed。

## 2026-06-19 补充：商品下单 / 接龙名单出口

- 本轮结论：
  - 商品客户预览页默认必须有 SKU 点选和下单按钮。
  - 接龙开关只决定提交后叫“下单名单”还是“接龙名单”，不决定是否落库。
- 已实现：
  - 后端新增 `customer_actions.order-intent`。
  - `enableGroupRelay=false`：客户点“下单”写 `order-intent`。
  - `enableGroupRelay=true`：客户点“下单并接龙”写 `relay-intent`。
  - 两者都不投影到 `lead_reminders`，不进入 SCRM。
  - 客户预览页提交后显示已下单/已接龙的 SKU 和数量。
  - 团长资料详情页入口改为“查看下单 / 接龙名单”。
  - 我的笔记商品卡入口改为“下单 N / 下单名单”。
  - 客户动作页商品资料展示“商品下单名单 / 商品接龙名单”，支持复制汇总、复制单条、复制电话/微信和拨号。
- 生产确认：
  - 生产后端已同步并重启，`order-intent` 已生效。
  - 生产商品笔记 `enableGroupRelay=false` 时，客户动作配置返回 `order-intent / 下单`。
  - 生产商品笔记 `enableGroupRelay=true` 时，客户动作配置返回 `relay-intent / 下单并接龙`。
  - 客户预览页下单区域已移动到底部动作区后方。
- 已验证：
  - 使用 Codex Python 3.12 运行 `backend/tests/test_app.py`：66 passed。
  - 小程序 `note-preview`、`note-actions`、`note-edit`、`notes` JS 语法检查通过。
  - 小程序 JSON 解析检查通过。
  - `git diff --check` 通过。

## 2026-06-19 补充：轻订单中心和站内消息

- 本轮目标：
  - 商品轻订单补齐地址、电话、备注等履约信息。
  - 买家和商家都有订单中心。
  - 商品/房源/订单支持小程序内异步留言。
  - 我的页从单列表重构为业务分区。
- 已实现：
  - 后端新增订单接口：`GET /api/orders`、`GET /api/orders/{orderId}`、`PATCH /api/orders/{orderId}/status`。
  - 后端新增消息接口：线程列表、创建线程、消息列表、发送消息、标记已读。
  - 后端新增 `message_threads` / `message_records` 存储，mock JSON 和 Postgres schema 已同步。
  - 下单 payload 扩展 `receiverName / phone / address / wechat / remark`，电话和地址必填。
  - 小程序新增订单列表、订单详情、消息列表、消息会话页面。
  - 客户预览页商品下单区补齐地址/电话等字段，并新增“发消息”入口。
  - 商品下单/接龙名单每条记录新增“发消息”。
  - 资料详情和我的笔记卡片增加消息入口。
  - 我的页分为会员服务、笔记区域、线索/订单、消息专区、开发测试。
- 已验证：
  - 后端全量测试：66 passed。
  - 小程序相关 JS 语法检查通过。
  - 小程序 JSON 解析检查通过。
  - `git diff --check` 通过。
- 注意：
  - 本轮没有做支付、库存扣减、物流、退款、核销。
  - 站内消息第一版是异步文本留言，没有 WebSocket、图片、语音。
  - 需要重新编译/预览/上传小程序，真机才能看到新增订单和消息页面。

## 2026-06-19 补充：消息入口已前端插件化

- 已实现：
  - `miniprogram/plugins/message-plugin/index.js`：统一封装打开会话、打开消息中心、获取未读数。
  - `miniprogram/components/message-entry`：统一入口组件，支持 thread / center 两种模式和 card / row / pill / text 样式。
  - 订单详情、商品名单、资料详情、我的笔记、我的页消息专区已接入组件。
  - 客户预览页动态“发消息”动作已改为调用 `messagePlugin.openMessageThread`。
- 后续约定：
  - 新增房源、商品、活动、课程等场景时，不在页面里直接调用 `api.createMessageThread`。
  - 有可见入口时优先使用 `message-entry`；只有动态动作列表这类场景才直接调用 `messagePlugin` 方法。
- 已验证：
  - 小程序相关 JS 语法检查通过。
  - 小程序 JSON 解析检查通过。
  - 后端全量测试：66 passed。
  - `git diff --check` 通过。

## 2026-06-20 补充：商品下单弹层和我的页宫格

- 已实现：
  - 商品客户预览页不再直接展开地址/电话/备注表单。
  - 点击“下单 / 下单并接龙”后打开底部弹层填写下单信息。
  - 我的页会员服务、笔记区域、线索/订单、开发测试改为 4 列图标宫格。
  - 订单中心空态区分“暂无购买订单 / 暂无收到的商品订单”。
  - 如果生产后端尚未部署 `/api/orders`，前端会显示中文提示，不再裸露英文 not found。
- 已验证：
  - 小程序相关 JS 语法检查通过。
  - 小程序 JSON 解析检查通过。
  - 后端全量测试：66 passed。
  - `git diff --check` 通过。

## 2026-06-20 补充：生产后端已部署订单和消息接口

- 已完成：
  - 生产后端已同步并重建 `teambuy-backend`。
  - `/api/orders`、`/api/orders/{orderId}`、`/api/messages/threads` 已在公网可用。
  - 订单中心的路由级 `Not Found` 已解决。
- 公网冒烟：
  - `/health` 正常。
  - `/api/orders?userId=user_test&role=buyer` 返回 200 空列表。
  - `/api/messages/threads?userId=user_test` 返回 200 空列表。
  - 真实用户 `user_25ec00a0f0` 可看到商品订单列表和订单详情。
- 未完成：
  - 小程序体验版上传未能由 Codex CLI 完成。本机微信开发者工具提示服务端口关闭，CLI 自动开启端口超时。
  - 需要人工在微信开发者工具打开“设置 -> 安全设置 -> 服务端口”，然后重新预览/上传小程序。

## 2026-06-20 补充：企业微信纯图片已接入 OCR 两段式

- 已完成：
  - 统一导入层会识别“无正文、无链接、仅图片且图片已转存”的企业微信内容。
  - 企业微信客服同步纯图片、会话归档纯图片现在先生成 `image_ocr` 图片资料，OCR 状态为 `pending`。
  - 用户仍需在小程序编辑页点击“识别图片文字”才会调用 OCR；未配置 OCR 时仍可手动补文字和字段。
  - 图文混合资料仍走原 `content-to-note`；媒体下载失败仍走媒体重试，不生成无图的 OCR 资料。
- 已验证：
  - 新增 `test_real_sync_pure_image_saves_pending_ocr_note`。
  - 新增 `test_wecom_archive_pure_image_saves_pending_ocr_note`。
  - 后端全量测试：108 passed。
  - `compileall backend/app backend/tests` 通过。
  - `git diff --check` 通过。
- 下一步建议：
  - 用企业微信真实发一张纯图片到生产，确认小程序资料库出现“图片资料 / 待识别”。
  - 在真机小程序点“识别图片文字”，确认 PaddleOCR 识别结果能回写同一条资料。

## 2026-06-20 补充：identity-core P0 收窄为 openid 唯一身份

- 用户确认：
  - 小程序微信 `openid` 是多途径来源进入系统后的唯一身份锚点。
  - 企业微信 `external_userid` 只做系统内部来源映射，不做用户侧绑定管理、解绑或改绑。
- 已完成：
  - `AGENTS.md` 已写入 openid 身份总规则。
  - `WecomIdentityBinding` 增加 `ownerOpenid`。
  - 认领导入时写入 `external_userid -> ownerOpenid/ownerUserId`。
  - 企业微信后续导入归属优先按 `ownerOpenid` 找用户，旧映射继续按 `ownerUserId` 兜底。
- 已验证：
  - 新增 openid 优先归属测试。
  - 身份相关回归：3 passed。
  - 后端全量测试：109 passed。
- 注意：
  - P0 不新增解绑/改绑接口和小程序页面。
  - 测试期如发生误认领，先由后台数据修正，不作为正式用户功能。

## 2026-06-20 补充：identity + OCR 纯图片导入已部署生产

- 已完成：
  - 生产同步前备份：`/home/ubuntu/teamBuy-deploy-backups/20260620-072737`。
  - 已同步后端 app/tests/requirements/Dockerfile，并重建重启 `teambuy-backend`。
  - 生产数据库已补 `wecom_identity_bindings.owner_openid` 列。
- 已验证：
  - 公网 `/health` 正常。
  - 公网 `/api/ocr/images` 已上线，GET 返回 405，不再是路由级 404。
  - 公网 OCR 识别路由对不存在笔记返回业务级“笔记不存在”。
  - 生产容器内 `ownerOpenid` 字段、纯图片导入分流、PaddleOCR worker 均可导入。
  - 生产容器内 PaddleOCR 测试图识别为 `HELLO 123`。
  - 容器重启次数为 0。
- 企业微信真实消息状态：
  - 已手动触发会话存档 `pull -> process`。
  - 当前 `rawCount=0`、`processedCount=0`，说明生产没有新的真实归档消息可处理。
  - 下一步需要用户从企业微信真实发送一张纯图片，然后再次触发 `pull -> process` 验证图片资料 pending OCR 闭环。

## 2026-06-20 补充：企业微信真实图片 OCR 已闭环

- 用户 07:36 左右通过企业微信发送图片。
- 生产已处理该真实图片：
  - 归档消息 `seq=28`，`msgType=image`。
  - 生成资料 `note_f01130a526`。
  - 图片已转存为 `/media/...webp`。
- OCR 已由小程序端触发并成功：
  - `POST /api/ocr/notes/note_f01130a526/recognize` 返回 200。
  - `structuredData.ocr.status=done`。
  - `provider=paddle`，`confidence≈0.948`。
  - 识别文本为聊天截图中的时间、群名、联系人和聊天内容。
- 结论：
  - 企业微信真实图片归档、转存、图片资料生成、用户主动 OCR、结果回写同一条资料均已跑通。
  - 普通照片如果没有可见文字，OCR 不会做“看图识物”，可能返回空或很少文字。

## 2026-06-20 补充：开发期 Docker 改为可挂载模式

- 背景：
  - 当前 Docker 主要用于开发联调期，反复 `docker compose build backend` 会堆积 build cache。
  - 生产正式上线前可以另写生产专用 Dockerfile/镜像发布流程。
- 已新增：
  - `backend/Dockerfile.dev`：只装系统库和 Python 依赖，不复制源码。
  - `docker-compose.dev.yml`：挂载 `backend/app`、`backend/tests`、`backend/mock`、`backend/secrets`，并启用 `uvicorn --reload`。
- 开发期启动：
  - `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build backend`
- 安全清理建议：
  - 每天可清 Docker build cache：`docker builder prune -af --filter "until=24h"`。
  - 可顺手清悬空镜像：`docker image prune -f`。
  - 不要日常使用带 `--volumes` 的全量清理，避免误删 Postgres 或媒体数据卷。
- 验证：
  - 本机无 Docker CLI，未执行 `docker compose config`。
  - 已用 Ruby YAML 解析校验 `docker-compose.dev.yml`。
  - `git diff --check` 通过。

## 2026-06-21 补充：经营看板 / 展示页数据闭环已统一收口

- 本轮已完成：
  - 经营看板改为独立入口，保留展示页效果、访客详情、笔记数据、客户资料四个视图。
  - 访客和客户资料中的手机号/微信不脱敏，并补齐外呼、复制微信操作。
  - 访客详情底部“添加跟进 / 备注”按钮已修正垂直居中，并接入跟进记录更新。
  - 展示页列表新增单个展示页“效果”入口，进入后查看该展示页打开、访客、看资料、咨询、最近访客、资料点击排行。
  - 后端客户行为按业务强度排序，但小程序不展示“行为强度分层”字样。
  - 增加演示数据清理能力：后端清理接口 + 小程序“我的 -> 开发/测试 -> 清理测试”按钮。
- 生产状态：
  - 生产后端已部署，备份目录：`/home/ubuntu/teamBuy-deploy-backups/20260621-094814-dashboard-scrm-closeout`。
  - 生产已设置 `ALLOW_MOCK_LOGIN=false`，公网 mock 登录返回 403。
  - 真实测试账号 `openid=oPSh564GCACiIkZxFPV5VWVgdbds` 当前保留演示数据，方便继续真机看板测试；上线前可一键清理。
- 已验证：
  - 后端核心测试：76 passed。
  - 后端全量测试：113 passed。
  - `compileall backend/app backend/tests` 通过。
  - 小程序全量 JS `node --check` 通过。
  - 小程序 JSON 解析检查通过。
  - `git diff --check` 通过。
  - 公网 `/health` 正常，生产容器 `teambuy-backend-1` 正常运行。
- 下一步：
  - 需要用户在微信开发者工具手动上传/预览新版小程序，才能在真机看到最新前端页面。
  - 真机重点回归：经营看板四个页签、手机号外呼、微信复制、添加跟进、备注、单展示页效果页、清理测试按钮。
  - 上线前确认是否执行“清理测试”，删除生产库中的演示数据。

## 2026-06-21 补充：上线闭环与真实分享追踪 V1 已完成 P0 第一刀

- 新增文档：
  - `docs/stage2-docs/15-launch-closed-loop-share-tracking-v1.md`
  - `docs/qa/上线闭环与真实分享追踪V1_测试清单与验收标准.md`
  - `docs/qa/上线闭环与真实分享追踪V1_Codex自测报告.md`
- 已完成代码：
  - `ShowcaseEvent` 增加 `shareId/shareFromUserId/scene/referrer`。
  - `POST /api/showcases/{id}/events` 接收并保存分享来源。
  - analytics 增加 `shareSourceCount/topShares`。
  - 展示页列表和展示页预览/公开页分享路径携带 `sid/from/scene/ref`。
  - 客户打开展示页、点击资料、电话咨询、复制微信都携带分享来源。
  - 单展示页效果页展示“分享批次”。
- 已验证：
  - 后端核心测试：76 passed。
  - 后端全量测试：113 passed。
  - Postgres 仓储字段/index 测试：3 passed。
  - Python 编译、小程序 JS、小程序 JSON、`git diff --check` 均通过。
- 生产部署：
  - 已部署生产后端。
  - 部署前备份目录：`/home/ubuntu/teamBuy-deploy-backups/20260621-104718-launch-share-tracking-v1`。
  - 公网 `/health` 正常，生产 mock 登录返回 403。
  - 已写入一条生产冒烟事件 `share_prod_smoke_20260621`，analytics 可返回 `shareSourceCount/topShares`。
- 未完成：
  - 小程序需要用户手动上传新版后才能真机验收分享路径。
- 下一步建议：
  - 部署生产后端，验证新字段自动补列和事件接口。
  - 用户上传小程序体验版后，真机验证分享批次链路。

## 2026-06-21 补充：展示页好友打开“页面不存在”已修预览误分享

- 用户反馈：
  - 自己点展示页正常，转发给微信好友后好友点击显示页面不存在。
- 排查：
  - 生产已发布展示页公开接口可正常访问。
  - 预览态/草稿态会让 owner 自己能看，但客户公开访问返回不存在。
  - 如果小程序仍是体验版，未加入体验成员的好友也可能打不开。
- 已修：
  - `pages/showcase-view` 未发布预览隐藏分享按钮和右上角分享菜单。
  - `pages/showcase-edit` 发布态分享统一携带 `sid/from/scene` 并记录 `share` 事件。
- 已验证：
  - 相关小程序 JS 检查通过。
  - 小程序 JSON 解析通过。
  - 展示页后端相关测试通过。
  - `git diff --check` 通过。
- 仍需人工确认：
  - 重新上传/预览新版小程序。
  - 确认用于测试的微信好友是体验成员；如果不是体验成员，需要发布正式版或先加入体验成员。

## 2026-06-21 补充：UI 居中规则已写入文档，展示页分享增加 id 兜底

- 已写入：
  - `AGENTS.md` 新增“UI 文本居中与按钮排版硬规则”。
  - `miniprogram/app.wxss` 新增全局交互控件基线。
  - `docs/pitfalls.md` 记录原生 button 不能靠 `line-height` 假居中。
- 已修展示页列表：
  - 按钮统一 flex 居中。
  - 状态标签不换行，避免“已发布”拆行。
  - 分享按钮 `bindtap=prepareShare`，分享前锁定展示页 id/title/banner，`onShareAppMessage` 再兜底读取。
- 仍需用户上传新版小程序后真机验证。

## 2026-06-21 补充：指定演示展示页公开接口正常，已修分享路径空白兜底

- 用户指定展示页：
  - `演示展示页：房源和好物精选`
  - ID：`showcase_627fc56634`
- 后台确认：
  - 状态为已发布。
  - 公开接口能返回 4 条资料。
  - 事件接口可写入。
- 已修：
  - 分享路径同时带 `id/showcaseId`。
  - 展示页公开页缺 id 或接口失败时显示明确错误，不再空白。
  - 分享来源查询参数改用 `src`，继续兼容旧 `scene`。
  - 展示页列表右侧操作改紧凑横排，避免“发给客户 / 更多”占半张卡。
- 已验证：
  - 相关 JS 检查通过。
  - 小程序 JSON 检查通过。
  - `git diff --check` 通过。
- 仍需：
  - 用户重新上传/预览新版小程序后，再用管理员账号和好友账号测试同一条展示页分享。

## 2026-06-21 补充：展示页分享已改为首页中转

- 背景：
  - 指定展示页 `showcase_627fc56634` 后台和公开接口正常，但好友打开分享仍显示页面不存在。
  - 判断问题发生在微信打开分享路径阶段。
- 已改：
  - 展示页相关分享统一使用 `pages/home/index?shareTarget=showcase&showcaseId=...`。
  - 首页识别参数后，在登录判断前跳转 `/pages/showcase-view/index?...`。
  - 客户未登录也能通过首页中转打开公开展示页。
- 验证：
  - 小程序 JS 全量检查通过。
  - 小程序 JSON 检查通过。
  - `git diff --check` 通过。
- 真机验证：
  - 必须重新上传新版小程序后再测试。
  - 测试路径：展示页列表第二条 `演示展示页：房源和好物精选` -> 发给客户 -> 管理员账号/好友账号打开。

## 2026-06-21 补充：首页中转失败，已改为专用展示页分享落地页

- 用户反馈：
  - 12:40 另一台手机打开分享进入首页并显示“首页数据加载失败”。
- 已修：
  - 新增 `pages/showcase-share/index`。
  - 展示页列表、编辑页、公开展示页的分享路径全部改为 `pages/showcase-share/index?showcaseId=...`。
  - 落地页不登录、不加载首页数据，只跳转 `pages/showcase-view/index`。
  - 移除首页 `shareTarget=showcase` 中转逻辑。
- 已验证：
  - 小程序 JS 全量检查通过。
  - 小程序 JSON 检查通过。
  - 分享路径静态检查通过。
  - `git diff --check` 通过。
- 真机验证：
  - 必须重新上传新版小程序，因为新增了页面 `pages/showcase-share/index`。

## 2026-06-21 补充：分享落地最终改用已有 showcases 页面

- 再次调整：
  - 不再使用新增 `pages/showcase-share/index` 作为分享入口。
  - 分享路径统一改为已有 `pages/showcases/index?shareTarget=showcase&showcaseId=...`。
  - `pages/showcases/index` 在登录检查前识别分享参数并跳转公开展示页。
  - `app.json` 已移除 `pages/showcase-share/index` 注册，降低“新增页面未进体验版”风险。
- 已验证：
  - 小程序 JS 全量检查通过。
  - 小程序 JSON 检查通过。
  - `git diff --check` 通过。
## 2026-06-21 补充：展示页公开页已改为发布快照缓存

- 背景：
  - 用户明确要求展示页公开访问必须加缓存，不能让每次客户打开都重新拉资料拼页面。
- 当前实现：
  - 展示页发布时写入 `publicSnapshot`。
  - 公开接口 `/api/showcases/public/{id}` 优先返回快照。
  - 老的已发布页没有快照时，首次公开访问自动补快照。
  - 重新发布刷新快照版本。
  - 删除资料时同步修剪相关展示页快照。
- 验证：
  - `/tmp/teambuy-py312-test/bin/python -m pytest backend/tests/test_app.py -q -k "showcase"`：3 passed。
  - `/tmp/teambuy-py312-test/bin/python -m pytest backend/tests -q`：113 passed。
  - `python3 -m compileall backend/app -q`：通过。
  - `find miniprogram -name '*.js' -print0 | xargs -0 -n 1 node --check`：通过。
  - 小程序 JSON 解析检查：通过。
  - 已部署线上后端。
  - 线上 `/health` 返回正常。
  - 线上 `showcase_627fc56634` 连续两次公开访问均返回 `snapshotVersion=1`，且 `snapshotCreatedAt=2026-06-21T14:50:04.334312+08:00` 未变化。
- 后续注意：
  - 客户公开页不要再改回实时逐条读取笔记拼装。
  - 展示页内容变更后，用户需要重新发布才刷新客户看到的快照。

## 2026-06-21 补充：上线闭环 1-4 已统一收口

- 本次核准范围：
  - 1. 扩展 ShowcaseEvent 数据字段和接口请求。
  - 2. 修改展示页分享路径，生成并携带 `shareId`。
  - 3. 公开展示页记录事件时带上分享来源。
  - 4. analytics 和经营看板聚合分享来源。
- 当前实现：
  - 后端 `ShowcaseEventRequest/ShowcaseEvent` 支持 `shareId/shareFromUserId/scene/referrer`。
  - 小程序展示页列表、编辑页、公开页分享都会生成 `shareId`，并通过 `pages/showcases/index` 中转携带 `sid/from/src/ref`。
  - 公开展示页打开、点击资料、电话、复制微信都会记录事件并携带分享来源。
  - 单展示页 analytics 返回 `shareCount/shareSourceCount/topShares/recentEvents.shareId`。
  - 经营看板返回 `summary.shareCount/summary.shareSourceCount/topShares`，详情页和复用组件已展示“分享来源”。
- 验证：
  - 后端全量测试：113 passed。
  - 小程序 JS 全量语法检查：通过。
  - 小程序 JSON 解析检查：通过。
  - 后端编译检查：通过。
  - `git diff --check`：通过。
  - 已部署线上后端，线上 `/health` 正常。
  - 线上经营看板 `user_25ec00a0f0` 已返回 `shareSourceCount=11`、`topSharesLength=6`。
- 后续注意：
  - 小程序端变更需要用户重新上传体验版/正式版才能在真机看到“分享来源”模块和最新分享路径。

## 2026-06-21 补充：分享追踪 V1 代码侧 P0 边界加固

- 用户反馈真机分享回归测试多次未成功，本轮按用户要求不再继续卡住真机分享，转为向下推进代码侧收口。
- 已补测试：
  - `test_mock_login_can_be_disabled`：`ALLOW_MOCK_LOGIN=false` 时 mock 登录返回 403。
  - `test_showcase_builder_create_publish_public_and_archive`：草稿和下架展示页调用事件接口均返回“展示页不存在或未发布”。
  - `test_create_note_demo_data_for_owner`：同账号真实资料和真实展示页在清理演示数据后仍保留。
- 验证：
  - `/tmp/teambuy-py312-test/bin/python -m pytest backend/tests/test_app.py -q -k 'create_note_demo_data_for_owner or mock_login_can_be_disabled or showcase_builder_create_publish_public_and_archive'`：3 passed。
  - `python3 -m compileall backend/app backend/tests -q`：通过。
  - `git diff --check`：通过。
- 本轮没有继续改小程序分享路径，也没有调用生产清理接口。

## 2026-06-21 补充：手动新建房源/商品快速向导 V1 已实现

- 范围：
  - 底部 Tab 现有 `pages/resource-create/index` 已从旧资源创建页改为“添加资料”快速向导。
  - 房源、商品团购、普通笔记统一通过 `POST /api/notes/manual-draft` 创建 `UserNote` 草稿。
  - 创建后跳转 `/pages/note-edit/index?id=...`，不新增房源表或商品表。
  - 图片资料继续复用现有 OCR 图片保存接口，成功后进入 `note-edit`。
- 后端规则：
  - `cardType` 只允许 `property_listing/groupbuy_product/text_note`。
  - `inputMode` 只允许 `paste_text/blank`。
  - 粘贴文案走 `ContentObject(sourceType=manual_text)` 和 `content-to-note` 规则，再按用户选择类型人工确认。
  - 空白房源/商品生成结构化壳和默认转化配置；普通笔记不启用转化能力。
- 验证：
  - 后端全量测试：118 passed。
  - 小程序 JS 全量语法检查：通过。
  - 小程序 JSON 解析检查：通过。
  - 后端编译检查：通过。
  - `git diff --check`：通过。
- 后续真机验收：
  - 底部“添加”进入新向导。
  - 粘贴房源文案后进入房源工作台。
  - 粘贴商品文案后进入商品展示工作台。
  - 空白创建能进入详情页继续补字段。
  - 图片资料入口仍能保存图片并进入 OCR 资料页。

## 2026-06-21 补充：添加页方案 A 极简随手记入口已实现

- 用户最终选择：
  - 使用方案 A，不再保留三步类型选择页作为主形态。
  - 底部中间“添加”打开后直接进入“放进笔记库”输入器。
- 当前实现：
  - 新增 `POST /api/notes/quick-capture`。
  - 普通内容保存为普通笔记，保存后停留当前页，并显示“已保存 / 查看详情”。
  - 高置信房源/团购直接保存成对应业务草稿，再用方案 B 页面内业务提示层引导“完善房源/完善商品”。
  - 用户点“先放笔记库”时，仍保留业务草稿类型，不降级成普通笔记。
  - 图片按钮继续走 OCR 图片资料入口。
  - `...` 更多入口保留空白房源、空白商品、图片资料。
- 已验证：
  - 后端全量测试：122 passed。
  - 小程序 JS 全量语法检查：通过。
  - 小程序 JSON 解析检查：通过。
  - 后端编译检查：通过。
  - `git diff --check`：通过。
  - 已部署生产后端，线上 `/health` 返回 200。
  - 线上 `POST /api/notes/quick-capture` 已进入业务层；不存在用户验证返回 `404 用户不存在`。
- 后续注意：
  - 小程序端需要用户重新上传体验版/正式版才能看到新添加页。
  - 小程序上传后即可调用线上 `quick-capture`；Codex 不负责自动上传小程序。

## 2026-06-21 补充：22:53 分享打不开日志结论

- 用户测试：
  - 笔记：`note_f114f85595`
  - 展示页：`showcase_627fc56634`，分享 id `share_showcase_627fc56634_1782053566523_88269`
- 生产日志：
  - 笔记在 22:53 仍请求旧的登录/本人接口：`/api/notes/note_f114f85595?ownerUserId=user_25ec00a0f0`，没有走新版匿名公开接口 `/api/notes/public/note_f114f85595`。
  - 展示页在 22:53:42 已请求 `/api/showcases/public/showcase_627fc56634`，并记录访问事件，均 200。
  - 手动请求生产公开接口确认：笔记公开接口 200，展示页公开接口 200。
- 下一步优先级：
  - 先让用户重新上传/切换到最新小程序体验版，再复测笔记分享；新版应出现 `/api/notes/public/{id}` 请求。
  - 展示页如果仍无法显示，重点看真机/开发者工具页面报错和渲染状态；后端公开接口不是当前阻断点。
  - 不要因为本次 22:53 反馈优先乱改后端展示页公开接口。

## 2026-06-21 补充：23:02 分享复测与笔记客户页收口

- 用户 23:02 再测：
  - 笔记 `note_f114f85595`
  - 展示页 `showcase_21cb92837c`
- 日志结论：
  - 笔记仍走 `/api/notes/note_f114f85595?ownerUserId=user_25ec00a0f0`，没有走公开接口。
  - 展示页已走 `/api/showcases/public/showcase_21cb92837c` 并记录事件，后端返回 200。
- 新修复：
  - `miniprogram/pages/note-preview/index.js` 固定用 `api.fetchPublicNote(noteId)` 加载客户预览页。
  - 这能避免客户手机有登录态时被误导到 owner 私有接口。
- 验证：
  - 小程序 JS 检查、JSON 检查、`git diff --check` 均通过。
- 待用户操作：
  - 重新上传最新小程序体验版。
  - 用另一个已加入体验成员的微信号打开分享卡片；打开笔记时后台应看到 `/api/notes/public/note_f114f85595`。

## 2026-06-21 补充：经营看板递进处理台第一版

- 用户确认旧体验版问题后，继续反馈：
  - 经营看板只有“打开、访客、看资料、咨询”总数，不知道属于哪个展示页，也无法点击处理。
  - 分享来源、访客详情、客户库、待联系都需要从总数递进到具体来源、具体客户和具体成交/问询/下单动作。
  - 用户头像登录后仍白色。
- 本轮已完成：
  - 后端 `/api/dashboard/business` 新增 `showcaseBreakdown`、`visitorProfiles`，并给 `topShares` 增加访客预览字段。
  - 经营看板展示页效果页改为“全部展示页汇总 -> 按展示页拆解 -> 分享来源 -> 最近客户”。
  - 访客详情页改为优先显示可处理客户列表，客户卡片可跳线索详情、订单详情或资料动作页。
  - “我的”页没有头像时显示彩色首字兜底；经营看板继续过滤无效示例头像并兜底。
- 生产状态：
  - 已备份生产 `backend/app/services/app_service.py` 到 `backups/20260621-235421-dashboard-drilldown/`。
  - 已同步后端并重建启动 `teambuy-backend-1`。
  - 公网验证 `user_25ec00a0f0` 返回 `showcaseBreakdown=14`、`visitorProfiles=20`、`topShares=6`。
- 已验证：
  - 后端全量测试：127 passed。
  - 小程序全量 JS 检查、JSON 检查、`git diff --check` 通过。
- 还未完成：
  - 需要用户重新上传小程序体验版后真机看新经营看板。
  - 真实头像需要后续做设置中心/资料设置；微信登录不会自动返回头像。
  - 客户库和待联系还需要继续按“总数 -> 来源 -> 具体客户 -> 处理动作”重构。

## 2026-06-21 补充：客户库 / 待联系递进处理视图

- 本轮继续补齐用户提出的同一类问题：
  - 客户库和待联系也要能从总数递进到来源、具体客户和处理动作。
- 已完成：
  - 客户库新增处理阶段分组、来源资料分组，客户卡片显示头像兜底、电话外呼、微信复制、下一步动作、来源资料、跟进入口。
  - 客户库筛选新增 `activeStageFilter`，保存常用视图时会带上处理阶段。
  - 待联系新增优先处理区和按来源资料拆解区，线索卡片显示头像兜底、电话、微信、来源和处理入口。
- 已验证：
  - 客户库/待联系 JS 语法检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 解析检查通过。
  - `git diff --check` 通过。
- 待用户验收：
  - 上传小程序体验版后检查客户库和待联系页面是否符合“总览 -> 分来源/分状态 -> 具体人 -> 处理动作”的心智。
  - 真机确认外呼、复制微信、头像兜底、按钮居中和长标题不挤压。

## 2026-06-22 补充：订单/接龙落实到具体下单人

- 本轮继续补“成交的人、下单的人”：
  - 客户库加载商家订单，新增“下单 / 成交”面板，并在客户卡片显示最新订单状态。
  - 商家订单中心新增状态分组、来源商品分组，订单列表改成买家处理视角。
  - 订单详情新增复制微信。
- 验证：
  - 订单页、订单详情、客户库 JS 检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - `git diff --check` 通过。
- 待用户验收：
  - 上传体验版后检查订单中心是否能从总数进入状态/来源，再到具体买家。
  - 检查客户库下单/成交面板是否能进入订单详情。
  - 真机确认外呼、复制微信、复制地址。

## 2026-06-22 补充：资料点击排行可下钻到访客

- 本轮继续收口经营看板：
  - 后端 `visitorProfiles` 增加 `noteIds`，记录访客点过哪些资料。
  - 小程序经营看板点击“资料点击排行”单条资料时，切到访客详情并只看点过该资料的人。
  - 当前筛选下的动作流水同步过滤，不再混入其他资料动作。
- 生产状态：
  - 已备份生产 `backend/app/services/app_service.py` 到 `/home/ubuntu/teamBuy-deploy-backups/20260622-dashboard-note-drilldown/`。
  - 已同步后端并重建启动 `teambuy-backend-1`。
  - 公网验证 `/health` 200，`/api/dashboard/business?ownerUserId=user_25ec00a0f0` 返回的 `visitorProfiles` 已包含 `noteIds`。
- 验证：
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - 后端 Python 3.12 编译检查通过。
  - `git diff --check` 通过。
  - 本机后端 pytest 因本地虚拟环境 Python 3.9 不支持项目当前 `dataclass(slots=True)` 未跑。
- 待用户验收：
  - 上传体验版后，在经营看板点“资料点击排行”某条资料，确认下面只出现看过这条资料的访客。

## 2026-06-22 补充：经营看板访客详情处理卡

- 本轮继续改善“看得云里雾里”的问题：
  - 访客列表点击后先打开页内客户详情处理卡，不再直接跳走。
  - 详情卡展示头像、来源、打开/看资料/咨询次数、电话、微信、来源展示页、看过资料和分享批次。
  - 详情卡内可直接外呼、复制微信、回到列表，或进入线索/订单/资料动作处理。
- 验证：
  - 经营看板 JS 检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - `git diff --check` 通过。
- 待用户验收：
  - 上传体验版后，在经营看板点任意客户行，确认先出现客户详情处理卡。
  - 真机确认弹层不挡底部按钮、外呼/复制微信按钮居中并可点击。

## 2026-06-22 补充：客户库客户详情处理卡

- 本轮继续把客户库从“卡片列表”收口到“具体人处理”：
  - 点击客户头像/姓名区域先打开页内客户详情处理卡。
  - 详情卡展示阶段、意向、电话、微信、来源资料、最近查看、最近跟进、订单状态和客户标签。
  - 原有外呼、复制、订单、跟进等快操作保留。
- 验证：
  - 客户库 JS 检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - `git diff --check` 通过。
- 待用户验收：
  - 上传体验版后，在客户库点客户头像/姓名区域，确认先看到客户详情处理卡。
  - 真机确认“下一步处理”会按订单/电话/微信/客户详情正确执行。

## 2026-06-22 补充：待联系线索详情处理卡

- 本轮继续统一“先看人，再处理”：
  - 待联系页点击线索头像/姓名区域后，先打开页内线索详情处理卡。
  - 详情卡展示状态、跟进时间、电话、微信、来源资料、查看次数、最近查看、备注、最近跟进和归档原因。
  - 原有拨号、复制微信、查看线索、资源详情、标记已联系等快操作保留。
- 验证：
  - 待联系 JS 检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - `git diff --check` 通过。
- 待用户验收：
  - 上传体验版后，在待联系页点线索头像/姓名区域，确认先看到线索详情处理卡。
  - 真机确认“立即处理”会优先拨号，其次复制微信，没有联系方式时进入线索详情。

## 2026-06-22 补充：订单/接龙买家处理卡

# 2026-06-22 补充：电子名片微信转发封面独立生成器

- 本轮按用户反馈修正电子名片分享：
  - 新增 `miniprogram/utils/business-card-share.js`，作为电子名片专用微信转发封面生成器。
  - 封面独立绘制，不复用普通资料分享图，也不依赖客户页截图。
  - 封面包含圆形头像、姓名、身份、公司/门店、服务范围、电话/微信和打开名片行动提示。
  - `pages/note-preview` 客户预览页分享已改为调用专用生成器。
  - `pages/note-edit` 编辑页新增隐藏 canvas，电子名片直接转发时优先使用专用封面。
  - `pages/notes` 资料库列表新增隐藏 canvas，电子名片列表分享也优先使用专用封面。
- 真机反馈后继续修复：
  - 用户截图显示微信卡片已经用上名片封面，但右半边被裁切，只露出左侧头像和局部文字。
  - 已修复生成器的画布尺寸逻辑：按真机 `windowWidth` 缩放绘制，再导出 750×600 分享图，避免半张图裁切。
- 验证：
  - 小程序相关 JS 检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - 新增样式未发现独立 `px`，使用 `rpx`。
  - `git diff --check` 通过。
- 待用户验收：
  - 重新上传体验版后，从电子名片客户预览页、编辑页、资料库列表分别转发给微信好友。
  - 微信聊天里应看到精美横版名片封面，而不是普通小程序二维码卡片。
  - 如果头像没有显示，先确认头像是否为 HTTPS 且在小程序下载域名白名单内；否则应显示文字头像兜底。

# 2026-06-22 补充：电子名片详情页独立重做

- 本轮按用户参考图继续修正电子名片详情页：
  - `pages/note-preview` 中 `business_card` 已从通用客户预览 / 销售页结构中拆出。
  - 新详情页使用绿色名片风格首屏：圆形头像、姓名、身份胶囊、公司/门店、服务范围、电话/微信。
  - 首屏下方四个圆形动作：电话咨询、微信咨询、留下电话/微信、预约沟通。
  - 下方内容改为服务介绍、三列服务范围、联系与二维码/占位、保存名片。
  - `service_offer` 仍使用服务销售页结构；房源、商品、普通资料不受影响。
- 验证：
  - 小程序相关 JS 检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - 相关 WXSS 未发现独立 `px`。
  - `git diff --check` 通过。
- 待用户验收：
  - 重新上传体验版后打开电子名片客户详情页，确认不再是蓝色普通客户预览页。
  - 对照参考图检查：名片首屏、动作区、服务介绍、服务范围、联系与二维码是否都在。

# 2026-06-22 补充：电子名片图片字段与模板差异

- 本轮继续按用户反馈修正电子名片：
  - 编辑页名片字段去掉头像 URL 和二维码 URL 输入。
  - 新增头像/二维码图片区，直接显示图片。
  - 素材区图片新增“设头像 / 设二维码”，上传二维码后需要点“设二维码”明确指定。
  - 新增公司网址选填字段，详情页展示后点击复制。
  - 详情页电话咨询会清理空格后拨号；微信咨询复制微信号。
  - 保存名片按钮复制完整名片信息。
  - 二维码显示兼容多种历史字段名，并从图片素材兜底。
  - 4 款电子名片详情页已按模板 ID 拉开视觉差异。
  - 微信转发封面已按模板 ID 使用不同色板。
  - 新增 4 模板差异图：`docs/png/business-card-4-template-detail-comparison.svg`。
- 验证：
  - 小程序相关 JS 检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - 相关 WXSS 未发现独立 `px`。
  - `git diff --check` 通过。
- 待用户验收：
  - 重新上传体验版。
  - 在电子名片编辑页上传二维码图片后，点素材区“设二维码”，保存并打开客户详情页确认二维码出现。
  - 分别创建/切换 4 款电子名片模板，确认卡片封面和详情页风格有差异。

# 2026-06-22 补充：电子名片风格切换与动态联系方式

- 本轮继续修正电子名片核心体验：
  - 编辑页新增 4 款“名片风格”切换区。
  - 用户填完一张名片后，可直接切换专业顾问、门店名片、专家介绍、简洁微信风，不需要重新填写。
  - 切换风格只保存模板元信息，不覆盖已填内容。
  - 电子名片新增邮箱字段。
  - 电子名片详情页移除“预约沟通”，动作区改为按字段动态显示电话、微信、邮箱、留下电话/微信。
  - 电话点击后拨号；微信点击复制微信；邮箱点击复制邮箱。
  - 点击联系方式后，页面出现联系方式提示卡，明确当前点击的是哪个联系方式。
  - 电子名片编辑页功能组不再显示预约开关；服务方案仍保留预约。
- 验证：
  - 小程序相关 JS 检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - 相关 WXSS 未发现独立 `px`。
  - `git diff --check` 通过。
- 待用户验收：
  - 重新上传体验版后，先填写一张电子名片，再在编辑页切换 4 款风格，确认内容不丢。
  - 分别只填电话/电话+微信/电话+微信+邮箱，确认详情页动作区自动变为 2/3/4 个并居中。
  - 点击电话、微信、邮箱，确认能拨号或复制，并在页面看到对应联系方式提示。

- 本轮继续补“下单的具体人”：
  - 订单/接龙列表点击订单卡后，先打开页内买家订单处理卡。
  - 处理卡展示买家、状态、下单时间、来源商品、规格/数量、订单类型、备注、地址和联系方式。
  - 商家侧可直接外呼、复制微信、查看订单或立即处理。
- 验证：
  - 订单页 JS 检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - `git diff --check` 通过。
- 待用户验收：
  - 上传体验版后，在商家订单中心点任意订单卡，确认先看到买家订单处理卡。
  - 真机确认“立即处理”优先外呼，其次复制微信，没有联系方式时进入订单详情。

## 2026-06-22 补充：头像白块兜底扩大覆盖

- 本轮继续处理用户反馈的白头像问题：
  - 小程序统一头像清洗：非 HTTPS、`example.com`、`avatar-default`、`wxfile/file/blob`、`/tmp` 都不渲染图片。
  - 首页访客、访客线索、资源管理页访客、接龙组件、资料动作页、展示页公开页、展示页列表、展示页统计和站内消息补头像兜底或清洗。
  - 登录页默认头像改为空，不再写入 `example.com/avatar-local.png`。
  - 后端登录默认头像改为空，资料更新拒绝无效头像地址。
- 生产状态：
  - 已备份生产文件到 `/home/ubuntu/teamBuy-deploy-backups/20260622-avatar-sanitize-wide/`。
  - 已同步并重建启动后端。
  - 公网 `/health` 正常；PATCH `https://example.com/avatar-default.png` 返回 400。
- 验证：
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - 后端 Python 3.12 编译检查通过。
  - `git diff --check` 通过。
- 待用户验收：
  - 重新上传体验版后，真机重点检查首页访客、访客线索、经营看板、客户库、待联系、订单中心、展示页列表和资料动作页头像区域。

## 2026-06-22 补充：完成度审计补漏

- 本轮做了一次代码侧审计：
  - 经营看板、客户库、待联系、订单/接龙的下钻路径已覆盖到具体人处理卡。
  - 头像直渲染剩余风险主要在旧组件和旧资源统计链路。
- 已补：
  - 旧 `components/business-dashboard` 组件内部清洗 `recentVisitors.avatarUrl`，并生成 `avatarText`。
  - 组件模板改读 `displayDashboard`，避免未来复用时把无效头像直接渲染成白块。
- 验证：
  - 旧经营看板组件 JS 检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - 后端编译检查通过。
  - `git diff --check` 通过。
- 剩余：
  - 代码侧可验证项已收口；是否完全解决需要用户上传最新体验版后真机回归。

## 2026-06-22 补充：真机回归清单

- 已新增回归清单：
  - `docs/qa/经营闭环头像与处理链路_真机回归清单.md`
- 覆盖：
  - 头像兜底。
  - 经营看板总数/展示页/分享来源/资料排行到具体访客。
  - 客户库来源/状态到具体客户。
  - 待联系来源/状态到具体线索。
  - 订单/接龙来源商品/状态到具体买家。
- 下一步：
  - 用户上传最新体验版后，按清单真机回归。
  - 若仍出现白头像，优先记录页面名、用户/客户名称、是否真实头像、是否从旧分享进入，再查对应接口返回。

## 2026-06-22 补充：Codex 自测报告

- 已新增自测报告：
  - `docs/qa/经营闭环头像与处理链路_Codex自测报告.md`
- 结论：
  - 代码侧自测通过。
  - 生产后端已部署头像入库保护。
  - 真机拨号、复制微信、体验版是否最新、头像域名白名单和另一个微信打开分享后的新记录仍需人工确认。
- 本轮最终检查：
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 检查通过。
  - 后端 Python 3.12 编译检查通过。
  - `git diff --check` 通过。
  - 生产 `/health` 正常。

## 2026-06-22 补充：我的笔记/我的页小优化

- 本轮按用户反馈做了 UI 收口：
  - “我的笔记”快捷分类新增“普通笔记”，并把快捷项调整为“全部 / 普通笔记 / 房源 / 商品团购”。
  - 普通笔记筛选为本地过滤：`text_note` 且没有房源/团购候选，不新增后端参数。
  - “最近迁入”待处理筛选增加整卡绿色选中态，避免只看到列表条数变化。
  - 删除“我的笔记”顶部“保存图片”按钮；图片保存不再从列表页进入。
  - 笔记数量蓝色胶囊显式使用 `rpx` 字号和 flex 居中。
  - “我的”页编辑资料和退出登录移到头像昵称下方；底部退出按钮删除；编辑资料弹窗去掉“头像链接”输入。
  - “我的”页去掉访客线索、待联系、客户库主入口，保留经营看板和订单入口。
- 判断：
  - 客户库暂不物理删除。它仍可作为客户档案沉淀页，并且经营看板/历史链路可能还会依赖。
  - 待联系底层页面也暂不物理删除。若后续确认废弃，需要单独扫描看板下钻、客户库跳转、历史路径和 tab 配置再清理。
- 项目规则：
  - `AGENTS.md` 新增小程序尺寸单位硬规则，要求核心尺寸、统计数字、头像、按钮和宫格默认使用 `rpx`。
- 验证：
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 解析通过。
  - `git diff --check` 通过。
- 待用户验收：
  - 上传体验版后检查“我的笔记”普通笔记筛选、迁入筛选绿色态、顶部保存图片按钮是否消失。
  - 真机检查“我的”页头部编辑资料/退出登录按钮是否不变形，编辑资料里是否不再显示头像链接。

## 2026-06-22 补充：电子名片独立工作台

- 用户最新确认：
  - 按其提供的流程图和 4 款风格图开发。
  - 电子名片单独开页面，不放在“我的笔记”或输入笔记器里。
  - 资料库增加“电子名片”按钮。
  - 4 款风格尽量复原参考图，差异必须明显。
- 本轮实现：
  - 新增 `miniprogram/pages/business-card-studio/`：
    - 第一步：4 款名片风格预览。
    - 第二步：填写名片信息，含头像、二维码、姓名、职位、公司/门店、电话、微信、邮箱、网址、城市、地址、服务范围、服务介绍。
    - 第三步：同时预览微信转发卡片和客户详情页。
  - `miniprogram/app.json` 注册新页面。
  - `miniprogram/pages/library/` 新增“电子名片”快捷入口。
  - `miniprogram/pages/resource-create/index.js` 旧“电子名片”入口改跳新工作台。
  - 保存仍创建/更新 `business_card` 类型 `UserNote`，不新增后端系统。
- 验证：
  - `node --check miniprogram/pages/business-card-studio/index.js` 通过。
  - `miniprogram/app.json` 和新页面 JSON 解析通过。
  - 新页面和资料库触达 WXSS 未发现核心独立 `px`。
  - 本轮触达文件 `git diff --check` 通过。
- 后续建议：
  - 用户上传体验版后真机检查资料库入口、4 款模板视觉差异、资料切换不丢、头像/二维码显示。
  - 若用户认可独立工作台体验，再把已有电子名片的“编辑”入口也逐步迁到该工作台的编辑模式。

## 2026-06-22 补充：电子名片模板选择体验修正

- 用户反馈：
  - 模板头像不能只是一个字，4 款模板要两男两女。
  - 双列在手机上会把横向名片挤压变形，需要列表/双列选择，默认列表。
  - 需要核对制作预览、微信转发卡片、点击后的详情页是否一致。
- 本轮处理：
  - `miniprogram/utils/sales-page-templates.js`：4 款电子名片模板补完整默认人物信息，并标记头像样式。
  - `miniprogram/pages/business-card-studio/`：模板选择页新增列表/双列切换，默认 `list`；模板头像改为两男两女职业头像样板。
  - `miniprogram/utils/business-card-share.js`：转发封面调色对齐 4 款模板，CTA 去掉“预约沟通”。
- 核对结论：
  - 制作页保存 `displayTemplate`、`displayTemplateName`、`displayTemplateTone`、`structuredData`。
  - 客户详情页通过 `displayTemplate` 解析模板并渲染对应详情样式。
  - 微信转发封面通过同一套 `businessCardHero` 数据生成图片。
  - 编辑页保留 SCRM/转化配置是合理的；新制作工作台隐藏复杂配置，但保存时仍保留轻 SCRM 与留资能力。

## 2026-06-22 补充：电子名片模板写真头像

- 用户反馈：
  - 模板头像希望换成超现实写真美女和男生头像。
- 本轮处理：
  - 生成一组 2 男 2 女写真头像。
  - 曾短暂切分到 `miniprogram/static/business-card/`；后因主包超过 2MB，已改为服务器 WebP。
  - 4 款电子名片模板的 `preview.avatarUrl` 当前指向服务器 WebP。
  - 模板选择页优先渲染头像图片，图片不可用时才回落到 CSS 样板/首字兜底。

## 2026-06-22 补充：头像资源迁移到服务器 WebP

- 用户反馈：
  - 真机调试报错 `source size 2200KB exceed max limit 2MB`。
  - 要求图片不要再放前端包，放入服务器并改成 WebP。
- 本轮处理：
  - 4 张模板头像通过线上上传接口转为服务器 WebP。
  - `miniprogram/utils/sales-page-templates.js` 改为引用 HTTPS WebP 地址。
  - 删除前端包内的写真头像 PNG/JPG 文件，小程序目录约 1.5MB，低于 2MB 主包限制。
  - `backend/app/main.py` 增加 `mimetypes.add_type("image/webp", ".webp")`，并已部署生产后端。
- 线上头像 URL：
  - `https://teambuy.lifelove.top/media/media_a535beaccd-manual_asset_0afb19f5db.webp`
  - `https://teambuy.lifelove.top/media/media_35b3a047fc-manual_asset_25ae3bb5b2.webp`
  - `https://teambuy.lifelove.top/media/media_c8b9458757-manual_asset_b208951151.webp`
  - `https://teambuy.lifelove.top/media/media_94ec97ee72-manual_asset_744c2c96ca.webp`
- 生产验证：
  - `https://teambuy.lifelove.top/health` 正常。
  - 4 个 WebP 均返回 `content-type: image/webp`。

## 2026-06-22 补充：电子名片头像裁切修正

- 用户反馈：
  - 真机中部分头像显示为 4 人拼图或裁切错误。
- 本轮处理：
  - 从原始 2x2 头像图重新按像素坐标切出 4 张单人头像。
  - 重新上传服务器 WebP，替换 `sales-page-templates.js` 中 4 个模板头像 URL。
- 当前正确头像 URL：
  - 专业顾问男：`https://teambuy.lifelove.top/media/media_a535beaccd-manual_asset_0afb19f5db.webp`
  - 门店名片女：`https://teambuy.lifelove.top/media/media_35b3a047fc-manual_asset_25ae3bb5b2.webp`
  - 专家品牌女：`https://teambuy.lifelove.top/media/media_c8b9458757-manual_asset_b208951151.webp`
  - 简洁微信男：`https://teambuy.lifelove.top/media/media_94ec97ee72-manual_asset_744c2c96ca.webp`

## 2026-06-22 补充：已有名片进入电子名片工作台

- 用户反馈：
  - 电子名片工作台前面需要有“已做好的名片”区域。
  - 已有名片应从“我的笔记/编辑名片”进入工作台，在工作台实时切换风格，看卡片预览和详情预览，再保存和分享。
  - “我的笔记”的编辑名片页不要再放风格选择网格。
- 本轮处理：
  - `pages/business-card-studio/index` 支持 `?id=noteId` 加载已有名片。
  - 工作台顶部新增“已做好的名片”提示区。
  - 确认页增加“卡片预览 / 详情预览”切换和 4 款模板缩略图。
  - 切换风格后标记未保存，保存后才显示“预览 / 分享”，避免分享旧版本。
  - 保存时合并保留已有 `conversionConfig`，不清掉编辑页配置过的 SCRM 能力。
  - `pages/note-edit/index` 电子名片区移除原“名片风格”网格，新增“设置名片风格”按钮跳转工作台。

## 2026-06-22 补充：服务方案工作台白屏修复

- 用户反馈：
  - 服务方案页面打开白屏。
- 本轮处理：
  - 修复 `pages/service-offer-studio/index` 默认表单引用未定义变量的问题。
  - 修复服务方案预览构建时图片列表没有兜底的问题。
  - 为页面补充默认模板、默认表单和默认预览，避免首屏初始化阶段空数据触发运行时异常。
  - 增加页面可见错误态：未登录、模板加载失败、读取已有方案失败时显示提示和“重试/去登录”，不再纯白屏。
- 验证：
  - 服务方案页面 JS 语法检查通过。
  - 小程序全量 JS 检查通过。
  - 小程序 JSON 解析检查通过。
  - 模拟有用户加载：4 套服务方案模板正常出现。
  - 模拟无用户加载：显示登录提示，不白屏。
- 待用户验收：
  - 需要重新上传小程序体验版后，在真机从资料库“服务方案”入口打开工作台确认。

## 2026-06-23 补充：服务方案工作台第二轮重做已继续推进

- 用户新增要求：
  - 工作台不要再纵向拖太长，顶部“选模板 / 填资料 / 确认效果”改成横向。
  - 模板方案区也改为横向排列，点击一个模板，下方预览立即跟着变化。
  - “服务报价 / 案例背书”需要真实默认图，不要白块占位。
  - 资源库里在“电子名片”旁边新增“服务方案”独立入口，原笔记器/快速入库入口保留。
- 本轮已做：
  - `pages/service-offer-studio/index` 步骤条与模板条改成横向滑动结构。
  - `sales-page-templates.js` 为报价型与案例型补入服务器 WebP 默认参考图。
  - `pages/note-preview/index` 与 `utils/business-card-share.js` 支持服务方案模板服务器图片兜底，避免详情页和分享图掉图。
  - `pages/library/index` 新增“服务方案”独立入口，位置与“电子名片”并列。
- 已验证：
  - 服务方案工作台、客户详情页、资源库入口相关 JS 语法检查通过。
  - `git diff --check` 通过。
- 下一步建议：
  - 用户重新上传体验版后，真机重点看服务方案工作台首屏长度、模板横滑手感、案例图展示和资源库入口位置是否符合参考图预期。

## 2026-06-23 续更：服务方案工作台真机变形已继续修

- 用户最新反馈：
  - 服务方案工作台和“我的笔记 / 电子名片”几乎同类，应直接参考电子名片稳定样式。
  - 当前截图中标题、步骤、模板卡和预览图片在手机/iPad 上都有撑宽或变形。
  - 副标题一行放不下必须自动换行；核心尺寸必须继续用 `rpx`。
- 本轮已做：
  - `pages/service-offer-studio/index.wxml` 去掉步骤区横向滚动结构，改成三列固定步骤。
  - `pages/service-offer-studio/index.wxss` 增加页面宽度约束、文本换行、模板卡纯文字布局、预览图固定 rpx 高度和图片铺满规则。
  - `utils/sales-page-templates.js` 切换到 v2 高分辨率源图转存后的服务器 WebP 资源。
  - 当前后端图片：
    - `https://teambuy.lifelove.top/media/media_aa4adc7919-manual_asset_5d2328325f.webp`
    - `https://teambuy.lifelove.top/media/media_3463380142-manual_asset_1dd9a72074.webp`
    - `https://teambuy.lifelove.top/media/media_9ff631d3e1-manual_asset_6a708c50d6.webp`
  - 已删除小程序前端 `miniprogram/static/service-offer` 图片文件，避免默认图进入 2MB 代码包。
  - 选择模板卡去掉缩略图，只展示模板名称、标签和适合场景；横向滚动区增加 `2rpx` 安全内边距和宽度约束。
- 已验证：
  - 服务方案 JS 与模板配置 JS 语法检查通过。
  - 服务方案工作台相关 WXSS/WXML 未发现非 `rpx` 核心 `px` 尺寸。
  - 前端不再存在 `/static/service-offer` 代码引用；后端 WebP 图片公网返回 `200`。
  - `git diff --check` 通过。
- 待真机验收：
  - iPhone 普通屏、小屏和 iPad 上确认页面不再横向撑开，副标题能换行，模板选择区不再出现缩略图，预览图铺满且不变形。

## 2026-06-23 再续：服务方案三步页面手机溢出补修

- 用户最新反馈：
  - “选模板 / 填资料 / 确认效果”顶部三步和模板横滑区基本正常。
  - 4 个类型的样板卡片、下方预览、填写资料页底部按钮、确认效果页在手机端仍有溢出。
  - 要求页面宽度 100%，但保留 rpx 留白。
- 本轮已做：
  - `pages/service-offer-studio/index.wxss` 对阶段卡、样板预览、表单卡、确认页预览补齐 `width/max-width/min-width` 约束。
  - 关键内层卡片改为 `calc(100% - 4rpx)`，左右保留 `2rpx` 安全留白。
  - 缩小手机端容易挤压的详情预览头像、英雄区边距和标题字号。
  - 确认页 4 个客户动作由一行 4 列改成 2 列，减少文字挤压。
  - 底部操作条由固定 grid 改为 flex，按钮可收缩；“下一步：确认效果”文案缩短为“确认效果”。
- 已验证：
  - `node --check miniprogram/pages/service-offer-studio/index.js` 通过。
  - 服务方案工作台相关 WXSS/WXML 未发现非 `rpx` 的核心 `px` 尺寸。
  - 前端未重新引入 `/static/service-offer` 图片引用。
  - `miniprogram/static` 未发现超过 200KB 的静态图片文件。
- 待真机验收：
  - 重新上传体验版后，用普通手机重点看三个步骤页是否还有横向滚动：模板预览区、填写资料底部按钮、确认效果客户预览页。

## 2026-06-23 三续：服务方案底部遮挡与微信转发封面

- 用户最新反馈：
  - 工作台三步页面底部“返回 / 使用这个模板”等按钮会挡住模板和上方预览内容，需要留白并向下。
  - 服务方案微信转发不能使用默认小程序卡片，应像电子名片一样保持模板、转发卡片和“我的笔记”列表的完整一致。
- 本轮已做：
  - `pages/service-offer-studio` 增加底部 spacer，sticky 操作条更贴近安全区，减少遮挡。
  - `utils/note-display.js` 增加 `serviceOfferPreview`，从服务方案结构化数据和模板默认图构建列表/分享预览。
  - `pages/notes/index` 将服务方案纳入列表分享图预生成，调用 `generateServiceOfferShareImage`。
  - “我的笔记”服务方案列表从普通左图右文卡改为专属方案预览卡，展示模板名、服务标题、卖点、标签和封面。
  - 列表页分享服务方案时使用专属横版模板封面和服务方案标题，不再优先退回默认小程序卡片。
- 已验证：
  - `node --check` 已覆盖 `pages/notes/index.js`、`pages/note-preview/index.js`、`utils/note-display.js`、`utils/business-card-share.js`、`pages/service-offer-studio/index.js`。
  - 工作台与我的笔记相关 WXML/WXSS 没发现核心布局使用 `px`。
  - `miniprogram/static` 仍约 `88K`，没有新增前端大图。
  - `git diff --check` 通过。
- 待真机验收：
  - 重新上传体验版后，验证工作台底部按钮是否还遮挡最后一屏内容。
  - 从“我的笔记”服务方案点击“发方案”，看微信聊天里的卡片是否显示完整服务方案横版封面。

## 2026-06-23 四续：P0/P1 代码侧统一收口

- 用户确认当前测试没有问题，要求 Codex 统一处理剩余 P0/P1 开发操作，体验版上传由用户处理。
- 本轮已做：
  - 服务方案双列卡片新增专属迷你方案预览，避免双列模式退回普通封面卡。
  - 电子名片 / 服务方案分享按钮新增“封面准备中”状态，封面生成完成或失败后恢复“发名片 / 发方案”。
  - `note-display` 的 `serviceOfferPreview` 继续作为服务方案列表、双列卡片、分享封面的统一数据源。
  - 新增自测报告：`docs/qa/电子名片与服务方案P0P1收口_Codex自测报告.md`。
  - 确认生产 mock 登录关闭能力已有 `ALLOW_MOCK_LOGIN` 开关和自动化测试。
- 已验证：
  - `node --check` 覆盖 `pages/notes`、`pages/note-preview`、`utils/note-display`、`utils/business-card-share`、`pages/service-offer-studio`。
  - 工作台 / 我的笔记相关 WXML/WXSS 未发现核心布局 `px`。
  - 小程序前端密钥关键词扫描只命中登录页提示文案，未发现真实密钥。
  - `miniprogram/static` 约 `88K`，没有新增前端大图。
  - `git diff --check` 通过。
  - 后端 mock 登录关闭已有用例，但本机 Python/pytest 环境不匹配，本轮未实际执行。
- 待用户体验版确认：
  - 分享按钮封面准备状态是否自然。
  - 双列卡片服务方案是否符合模板感。
  - 微信聊天卡片是否稳定显示完整横版封面。

## 2026-06-23 五续：首页与 Tabbar 工作台模式一期验收

- 用户要求：
  - 基于 `docs/qa/首页Tabbar工作台模式一期_Codex自测报告.md` 和 `docs/qa/首页与 Tabbar 工作台模式一期_测试清单与验收标准.md` 输出验收报告。
- 本轮已做：
  - 新增 `docs/qa/工作台第一期_验收报告.md`。
  - 验收结论为“不通过”。
  - 主要原因：P0 未全部闭环，P0-23 业务识别提示仅部分覆盖，P0-27 权限和隐私缺专项回归证据，真机主链路仍待确认。
  - 报告已包含未覆盖/异常项、开发 Bug 单、P0/P1 回归清单和上线前检查事项。
- 新会话接手建议：
  - 如果继续开发，优先修复 P0-23：识别房源/商品/服务资料后，提示用户“切换到对应工作台”或“继续当前工作台”。
  - 同步补 P0-27 权限回归证据：owner、非 owner、匿名访客三类身份访问工作台/看板/展示页效果。
  - 修复后重新输出 Codex 修复报告，再让 AI 测试官做复测与回归。

## 2026-06-23 六续：资料库展示与客户入口修正

- 用户最新反馈：
  - 15:34 左右测试纯文字、微信笔记、链接均能进入资料库。
  - 资料库列表区域只显示左半边，疑似双列卡片布局没有铺满。
  - SCRM 和留言入口不明显，需要恢复/露出。
- 本轮已做：
  - `miniprogram/pages/library/index` 增加 `viewMode`，资料库默认“列表”展示，并提供“双列”切换。
  - 资料库页面新增页面级 `.card-grid.list-mode` / `.card-grid.grid-mode`，覆盖全局 `.resource-card { width: calc(50% - 10rpx) }`，修复半屏卡片。
  - 每张资料卡新增“客户/SCRM”和“留言”入口；客户入口跳 `manager`，留言入口打开消息中心。
- 已验证：
  - `node --check miniprogram/pages/library/index.js` 通过。
  - 小程序 JSON 解析通过。
  - 资料库相关文件 `git diff --check` 通过。
- 待用户体验版确认：
  - 上传体验版后查看资料库默认列表是否铺满宽度。
  - 切换“双列”后是否能左右两列正常展示。
  - 点击“客户/SCRM”和“留言”是否符合预期。

## 2026-06-23 七续：房源客户看板专属口径与 Python 3.12

- 用户最新反馈：
  - 现在还没上线，客户痕迹/待跟进是核心付费价值，不能因为一期想简单就退让。
  - Python 版本问题不能每次影响后端验证，要求统一升级到 3.12。
- 本轮已做：
  - 创建 `.venv312`，当前 Python 为 `3.12.13`，已安装后端完整依赖。
  - `.gitignore` 增加 `.venv312/`。
  - 后端 `GET /api/dashboard/business` 支持 `mode` 参数。
  - 新增 `mode=property` 专属房源客户看板聚合：房源数、打开、访客、待跟进、房源效果、推荐包效果、最近访客、客户动作和访客画像。
  - 房源模式过滤非房源客户动作和非房源 `note_click`，避免服务/普通资料污染房源看板。
  - 首页房源四指标优先使用后端房源看板汇总。
  - 资料库卡片如有 note 级客户动作，客户入口优先进入 `pages/note-actions/index?id=...`。
  - 新增后端回归用例：`test_property_business_dashboard_only_counts_property_customer_data`。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q`：98 passed。
  - `.venv312/bin/python -m compileall backend/app`：通过。
  - `node --check miniprogram/pages/home/index.js`：通过。
  - `node --check miniprogram/services/api.js`：通过。
  - `node --check miniprogram/pages/business-dashboard/index.js`：通过。
  - `node --check miniprogram/pages/library/index.js`：通过。
  - 关键文件 `git diff --check`：通过。
- 新会话接手建议：
  - 真机/开发者工具上传体验版后，优先验证房源首页四指标是否和客户看板数字一致。
  - 用企业微信发一条真实房源，打开/预约/留资后检查：首页 `待跟进`、客户看板 `待跟进`、单条资料客户动作页是否三处一致。
  - 如果继续优化 UI，先优化客户看板首屏“待跟进客户”列表，不要先扩展复杂 CRM。

## 2026-06-23 八续：房源首页今日/累计切换

- 用户最新反馈：
  - 当前首页数字应是历史累计，却写成“今日概览”，口径不对。
  - 希望首页能同时显示今日和历史累计，并且点击今日访客后能看到今天具体谁来了、看了哪些、如何联系。
- 本轮已做：
  - 后端房源看板返回 `summary`（累计）和 `todaySummary`（今日）。
  - 今日口径覆盖：今日新增房源、今日打开、今日访客、今日新增待跟进。
  - 访客画像、最近访客、客户动作增加今日标记。
  - 单条房源浏览合并进 `visitorProfiles`，今日访客不再只依赖推荐包。
  - 首页房源概览新增“今日 / 累计”切换，默认今日。
  - 首页进入客户看板时，今日口径携带 `range=today`；客户看板筛选今日访客和今日动作。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q`：98 passed。
  - `.venv312/bin/python -m compileall backend/app`：通过。
  - `node --check miniprogram/pages/home/index.js`：通过。
  - `node --check miniprogram/pages/business-dashboard/index.js`：通过。
  - `node --check miniprogram/services/api.js`：通过。
  - 关键文件 `git diff --check`：通过。
- 新会话接手建议：
  - 真机验证首页“今日/累计”切换是否清楚，特别是今日新房源为 0 时用户是否能理解。
  - 点击今日访客进入客户看板后，确认只出现今天的访客；历史访客只能在累计入口或清空筛选后看到。

## 2026-06-23 九续：待跟进数字与列表不一致修复

- 用户最新反馈：
  - 首页待跟进显示 1，但点击进入客户看板没有看到待跟进记录。
- 本轮已做：
  - 核对确认为 `LeadReminder` 和 `CustomerAction` 两套来源不一致。
  - 房源客户看板 `latestActions` 现在会合并 pending `LeadReminder`。
  - 无客户动作的旧线索会生成 `lead-followup` 行，点击后可进入线索详情。
  - 房源排行的待跟进数也按 `lead.cardId` 归因，不再只看 action projection。
  - 测试补充旧线索场景，防止再次出现“有数无列表”。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q`：98 passed。
  - `.venv312/bin/python -m compileall backend/app`：通过。
  - 关键文件 `git diff --check`：通过。
## 2026-06-23 19:30 房源客户看板线上修正

- 线上后端已准备部署：补 propertyBreakdown；待跟进动作用 LeadReminder 反填姓名/联系方式；旧动作名为空时按 CUSTOMER_ACTION_LABELS 兜底。
- 本地验证：backend/tests/test_app.py 98 passed；backend/app compileall 通过。
- 2026-06-23 19:40 线上已部署并复验：房源客户看板今日待跟进 1 可下钻到具体客户，高先生 / 预约看房 / 新世界广场B-938；后端容器 teambuy-backend-1 正常运行。

## 2026-06-23 19:50 阶段性交接归档

- 已新增 `docs/handoff-策划运营.md`。
- 文档覆盖：项目背景与目标、当前阶段目标、已完成功能、已修改/新增文件、当前代码状态、已知问题和风险、用户已确认的产品/技术决策、下一步建议执行顺序、新 Codex 会话第一条提示词。
- 新会话如果继续做运营策划，必须同时读取 `skills/operation-planning/SKILL.md` 和 `docs/handoff-策划运营.md`。

## 2026-06-24 一续：合集缓存、红点口径与资料库工具收口

- 用户最新反馈：
  - 合集页每次点击都显示正在读取，希望缓存到用户手机，降低服务器压力。
  - 房源资料库卡片红点在客户状态更新后仍存在。
  - 资料库工具区中服务方案、电子名片等入口和当前资料库场景不清晰。
  - 合集新建和分享层级偏深，希望优化。
- 本轮已做：
  - `miniprogram/pages/showcases/index.js` 增加 `userId + mode` 维度本地缓存，缓存 5 分钟；有缓存时先展示，过期再后台同步。
  - 合集首页增加 `latestPublished` 和“分享最近”，降低已发布合集再次转发的层级。
  - 合集方向卡可直接点击进入新建，减少用户必须点右上按钮的路径。
  - `miniprogram/utils/dashboard.js` 将 `hasHotCustomerSignal` 调整为只看未读和待跟进，历史客户动态不再触发红点。
  - `miniprogram/pages/library/index.wxml` 从“更多工具”移除电子名片和服务方案，只保留待认领、管理标签、我的笔记。
- 已验证：
  - `node --check miniprogram/pages/showcases/index.js`：通过。
  - `node --check miniprogram/utils/dashboard.js`：通过。
- 待真机验证：
  - 第一次进入合集无缓存时仍会加载；第二次进入应优先显示缓存。
  - 处理客户状态后红点应消失，但客户动态和看客户入口仍保留。
  - 分享最近按钮在有已发布合集时出现，并能正常触发微信分享。

## 2026-06-24 二续：标签解释与客户功能两列设置

- 用户最新反馈：
  - 不清楚“管理标签”对普通用户有什么用、如何让用户知道并使用。
  - 房源资料详情页“客户功能”展开后过长，希望直接两列显示，避免 6 行拉长页面。
- 本轮已做：
  - 资料库入口“管理标签”改为“标签设置”。
  - 标签管理页增加业务说明：快速筛选、自动归类、生成合集；输入示例改为房源场景。
  - 资料详情页客户功能展开区改为两列卡片式开关，约 5-6 个功能压缩到 3 行左右。
- 已验证：
  - `node --check miniprogram/pages/note-edit/index.js`：通过。
  - `node --check miniprogram/pages/tag-manage/index.js`：通过。
  - 本轮相关文件 `git diff --check`：通过。
- 待真机验证：
  - 客户功能两列在手机端是否文字不挤、不溢出。
  - 标签设置页说明是否能让用户理解标签和筛选/合集的关系。

## 2026-06-24 三续：房源筛选增强与详情页分层

- 用户最新反馈：
  - 房源库筛选需要加强，增加价格、户型、地铁、电梯、状态等专门筛选；价格需要两个区间输入。
  - 房源详情页继续分层，先按 Codex 判断做一版。
  - 标签设置后如何使用仍不清晰，且标签按钮样式偏窄。
- 本轮已做：
  - 房源资料库在房源模式下新增“房源筛选”面板。
  - 支持最低价/最高价输入；提供不限、1300 以下、1300-1800、1800-2500、2500 以上快捷按钮。
  - 支持户型、地铁/近地铁、电梯/楼梯、状态筛选。
  - 房源详情页分层调整：顶部发客户动作后，先展示客户反馈，再展示房源卡，低频客户功能设置继续折叠和两列展示。
  - 标签管理页按钮加宽加高。
- 已验证：
  - `node --check miniprogram/pages/library/index.js`：通过。
  - `node --check miniprogram/pages/note-edit/index.js`：通过。
  - `node --check miniprogram/pages/tag-manage/index.js`：通过。
  - 本轮相关文件 `git diff --check`：通过。
- 待真机验证：
  - 房源筛选面板在手机端是否高度可接受。
  - 价格上下限筛选是否符合用户预期。
  - 详情页先客户反馈、再房源卡、再低频设置的顺序是否更顺。

## 2026-06-24 四续：房源筛选入口前置与合集条件筛选开放

- 用户最新反馈：
  - 资料库页面根本没看到“分类筛选”，无法理解房源筛选在哪里。
  - 希望合集里的“按条件筛选”也可以打开。
- 本轮已做：
  - 资料库中只要存在房源资料，就在“新增资料 / 更多工具”下方直接显示“房源筛选”面板。
  - 默认不影响普通资料；当用户设置价格、户型、地铁、电梯、状态后，列表会按房源条件收窄。
  - 合集编辑页启用“按条件筛选”，可按价格、户型、地铁、电梯/楼梯、状态自动筛出房源并加入合集。
- 已验证：
  - `node --check miniprogram/pages/library/index.js`：通过。
  - `node --check miniprogram/pages/showcase-edit/index.js`：通过。
  - 本轮相关文件 `git diff --check`：通过。
- 待真机验证：
  - 资料库顶部是否能直接看到“房源筛选”面板。
  - 合集里点“按条件筛选”后，条件变化是否会自动更新候选和已加入房源。

## 2026-06-24 五续：房源工作台小闭环集中打磨

- 用户最新要求：
  - 将房源卡状态、筛选生成合集、详情页分层、客户反馈处理和合集效果复盘这些小优化一起完成，再统一验收打磨。
- 本轮已做：
  - `utils/dashboard.js`：房源卡新增 `propertyFollowStatus`，支持待处理、已跟进、有浏览、待分享。
  - `pages/note-actions`：待跟进线索增加已联系、暂不合适、已完成、重点跟进四个快捷处理按钮。
  - `pages/library`：房源筛选面板新增“用当前筛选生成合集”，通过本地缓存把条件传给合集编辑页。
  - `pages/showcase-edit`：启用“按条件筛选”，支持价格、户型、地铁、电梯/楼梯、状态，并自动加入符合条件的房源。
  - `pages/showcase-analytics`：新增“客户看房轨迹”，展示客户、动作、房源和时间，可下钻到房源客户反馈。
  - `pages/note-edit`：房源详情页增加“发客户 / 编辑资料”切换，默认发客户视角。
- 已验证：
  - `node --check` 覆盖本轮关键 JS：通过。
  - 本轮相关文件 `git diff --check`：通过。
- 待真机验证：
  - 房源卡状态是否准确、不过度打扰。
  - 客户反馈快捷处理后，待处理红点/待跟进数字是否刷新。
  - 当前筛选生成合集是否能复用条件并自动选中房源。
  - 合集效果页轨迹是否能看出客户浏览路径。
  - 房源详情“发客户 / 编辑资料”是否降低长页面压力。

## 2026-06-24 六续：标签设置使用链路补齐与系统校验

- 用户最新反馈：
  - 标签设置里添加标签后，不知道如何在每个房源上体现。
  - 要求系统再跑一轮校验。
- 本轮已做：
  - `utils/dashboard.js`：资料库房源卡标签来源从仅 `categoryIds` 扩展为 `categoryIds + visibilityConfig.userTags/tags`。
  - `pages/note-edit`：房源详情“编辑资料 -> 资料归类”新增“常用标签”，展示标签设置页创建的标签，可一键应用到当前房源。
  - 常用标签按钮样式加宽加高。
- 标签使用路径：
  - `资料库 -> 更多工具 -> 标签设置` 创建常用标签。
  - 进入某套房源详情，切到 `编辑资料`。
  - 在 `资料归类` 中点 `常用标签`，保存。
  - 返回资料库，该房源卡片会显示标签，并可在标签筛选里使用。
- 已验证：
  - 小程序关键脚本 `node --check`：通过。
  - 本轮关键文件 `git diff --check`：通过。
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q`：98 passed。

## 2026-06-24 七续：房源系统标签自动补齐

- 用户最新确认：
  - 系统标签应根据房源信息默认补齐，不让用户点选；后续按标签选房和生成合集依赖这些标签。
  - 第一批重要标签包括电梯/楼梯、租金区间、公寓、地铁口等。
- 本轮已做：
  - `pages/note-edit` 保存房源时自动生成 `systemTags`。
  - 自动标签覆盖：租金区间、户型/公寓、地铁口/地铁、电梯房/楼梯房、状态、待确认。
  - `systemTags` 合并进 `tags` 参与筛选；`userTags` 保留用户个人判断，不被覆盖。
- 已验证：
  - `node --check miniprogram/pages/note-edit/index.js`：通过。
  - `node --check miniprogram/utils/dashboard.js`：通过。
  - 本轮关键文件 `git diff --check`：通过。

## 2026-06-24 八续：房源工作台发客户前打磨

- 用户最新要求：
  - 先把客户视角预览、房源卡跟进闭环、合集发送前检查、筛选和系统标签继续补强一起做完。
- 本轮已做：
  - `pages/note-edit`：房源详情“发客户”页签新增客户视角预览卡和单套房源发布前检查。
  - `pages/note-edit`：房源字段新增面积、楼层/电梯、押付方式、入住时间。
  - `pages/note-edit`：系统标签补强面积区间、小户型、押一付一/押一付三、随时入住/本周可住。
  - `pages/library`：房源筛选新增面积、押付、入住条件；筛选状态更新后再刷新列表。
  - `pages/showcase-edit`：合集条件筛选同步新增面积、押付、入住；发布前新增检查项，存在缺项时弹窗确认。
  - `utils/dashboard`：房源卡状态新增下一步提示。
  - `utils/note-display`：合集已选行展示房源主信息，发布检查可判断租金完整度。
- 已验证：
  - `node --check` 覆盖资料详情、资料库、合集编辑、dashboard、note-display：通过。
  - 本轮关键文件 `git diff --check`：通过。
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q`：98 passed。
- 待真机验证：
  - 房源详情客户视角预览是否比直接跳客户页更直观。
  - 单套房源检查项文案是否会造成过度焦虑。
  - 资料库/合集新增筛选条件是否好找、好用。
  - 合集发布前弹窗是否只提醒、不打断工作。

## 2026-06-24 九续：团购/商品工作台与单品详情页打磨

- 用户最新方向：
  - 房源工作台基本处理完后，按房源推进逻辑继续打磨团购/商品工作台。
  - 主入口名称确定为“团购/商品工作台”，不要退化成普通商品后台。
  - 单商品详情页要像房源详情一样分成“发群 / 编辑商品”，并加客户视角预览和发群前检查。
- 本轮已做：
  - 首页团购/商品模式改名并收口四指标：商品、待处理、今日接龙、访客。
  - 首页指标点击路径分别进入商品资料、待处理名单、今日接龙名单和访客看板。
  - 资料库商品卡增强价格/规格/取货/截止时间、接龙/下单信号、待处理状态和“处理接龙”入口。
  - 单商品详情页新增“发群 / 编辑商品”双页签。
  - “发群”页签新增客户视角预览、发群前检查和接龙/买家反馈。
  - “编辑商品”页签保留商品信息、图片/视频、SKU、取货与下单、资料归类。
  - 商品标题、电话、快捷字段、SKU、素材和下单开关变化后，会同步刷新发群预览和检查项。
- 已验证：
  - `node --check` 覆盖首页、资料库、订单页、工作台模式、dashboard、商品详情页：通过。
  - 相关页面 JSON 解析：通过。
  - 本轮关键文件 `git diff --check`：通过。
- 待真机验证：
  - 团购/商品首页四指标是否好理解、点击路径是否顺手。
  - 商品资料卡小屏是否拥挤，按钮是否居中。
  - 商品详情“发群 / 编辑商品”是否降低长页面压力。
  - 发群前检查是否提示准确、不过度焦虑。
  - 接龙/买家反馈入口是否符合团长处理名单的直觉。

## 2026-06-24 十续：商品合集发群前链路

- 用户最新指令：
  - 继续下一阶段开发。
  - 按房源工作台推进逻辑继续打磨团购/商品工作台。
- 本轮已做：
  - 首页团购/商品工作台“商品合集”快捷入口直接进入 `pages/showcase-edit/index?mode=groupbuy`。
  - 资料库商品卡“加入合集”进入商品合集编辑页。
  - 合集编辑页按当前分类生成场景文案：商品分类下使用发群、商品合集、商品条件、已选商品等表达。
  - 商品合集新增条件筛选：价格区间、取货方式、截止时间。
  - 商品合集发群前检查覆盖合集名称、已选商品、合集封面、联系入口、价格完整度、取货信息。
  - 发布按钮仍只发布；发布后通过“发到群里”转发，避免误导为自动发群。
- 已验证：
  - `node --check` 覆盖 `showcase-edit`、首页、资料库、`note-display`：通过。
  - 本轮关键文件 `git diff --check`：通过。
- 待真机验证：
  - 商品合集入口路径是否顺手。
  - 商品条件筛选是否符合团长快速组团的直觉。
  - 商品合集页面是否仍有不该出现的房源文案。
  - 发群前检查是否准确、不过度打断。

## 2026-06-24 十一续：团购/商品工作台 P0 代码侧补齐

- 用户最新要求：
  - 把团购/商品工作台 P0 全部补上。
  - 涉及人工测试的内容后置。
- 本轮已做：
  - 后端 `/api/orders` 增加 `noteId` 过滤参数。
  - 小程序 `fetchOrders` 支持 `noteId`。
  - 卖家“接龙/买家名单”页用 `noteId` 做来源分组和过滤，展示名仍用商品名。
  - 单商品范围内清除筛选时不跳出当前商品。
  - 后端新增同名商品订单过滤测试，避免标题相同导致串单。
  - 商品合集价格识别收紧，避免规格数字误判为价格。
  - 已复核客户接龙/下单自动化覆盖：售罄 SKU、重复提交、团长可见、买家不可改状态、消息线程、订单状态更新。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q`：98 passed。
  - 后端订单相关文件 Python 编译：通过。
  - 小程序相关 JS `node --check`：通过。
  - 本轮关键文件 `git diff --check`：通过。
- 剩余后置：
  - 微信开发者工具/真机人工验收。
  - 体验版上传、真实微信群转发打开验证。
  - 页面视觉细节：按钮居中、文案是否拥挤、商品合集是否残留房源语境。

## 2026-06-24 十二续：团购/商品工作台 P1 代码侧补齐

- 用户最新要求：
  - P1 也直接全部开发，之后统一测试。
- 本轮已做：
  - 商品合集客户侧展示增强，商品卡展示规格、取货方式、取货地点、截止时间和“查看详情/接龙”提示。
  - 后端发布快照同步输出商品 meta，确保正式分享页和预览一致。
  - 资料库新增商品筛选：价格、取货方式、截止时间、有接龙、有访客、待补价格、待补取货。
  - 资料库“用当前筛选生成商品合集”会把商品筛选条件带入合集编辑页。
  - 商品保存自动生成系统标签：自提/配送/快递、今日/本周截止、有 SKU、已售罄、待补价格、待补取货。
  - 接龙/买家名单新增“今日新增”切换。
  - 商品资料支持“复用成新商品”，只复制内容，不复制订单/接龙/统计/消息。
  - 后端新增 `/api/notes/{note_id}/duplicate`。
- 已验证：
  - 后端完整测试 98 passed。
  - 后端相关文件编译通过。
  - 小程序相关 JS 检查通过。
  - 页面 JSON 解析通过。
  - 本轮关键文件 `git diff --check` 通过。
- 剩余后置：
  - 真机看商品筛选面板高度、商品合集客户卡片是否拥挤。
  - 真机验证“复用成新商品”后的字段和图片是否符合预期。
  - 真实数据下看“今日新增”切换是否清楚。

## 2026-06-24 十三续：工作台模式资料隔离修正

- 用户最新反馈：
  - 工作台之间已经可以切换，但资料信息没有随工作台切换隔离；房源仍会在团购/商品工作台里显示。
- 本轮已做：
  - `miniprogram/pages/library/index.js`：资料库 Tab 直接进入时读取当前工作台模式，房源模式默认只看房源，团购/商品模式默认只看商品；统计、分类、标签也按过滤后的资料计算。
  - `miniprogram/pages/visits/index.js`：底部工作台页按当前模式过滤资料后再生成概览和反馈列表。
  - `backend/app/services/app_service.py`：团购看板按团购/商品资料过滤笔记、相关合集、客户动作和线索，避免后端返回房源排行。
- 已验证：
  - `node -c miniprogram/pages/library/index.js`：通过。
  - `node -c miniprogram/pages/visits/index.js`：通过。
  - `python3 -m py_compile backend/app/services/app_service.py`：通过。
- 未验证：
  - 当前本机系统 Python 没有安装 `pytest`，`python3 -m pytest backend/tests/test_app.py -q` 无法运行。
  - 需要微信开发者工具/真机确认：首页切到团购/商品 -> 点底部“资料”和“工作台” -> 不再显示房源。

## 2026-06-24 十四续：16:14 企业微信团购笔记未进工作台

- 用户最新反馈：
  - 16:14 左右通过企业微信发送团购微信笔记，但没有进入团购/商品工作台。
  - 房源筛选标签只应在房源工作台显示，不应出现在其他工作台。
- 排查结论：
  - 线上 Nginx 记录显示 2026-06-24 16:13:51 收到 `/api/wecom/archive/callback`。
  - 数据库中 `wecom_archive_messages` 新增 seq=37，`import_batches` 新增 `import_6a739cb1df`。
  - 对应资料 `note_ec9fc09893` 已生成并归属 `user_25ec00a0f0`，类型是 `groupbuy_product / 团购`，标题“白凤乌鸡蛋”。
  - 问题原因：`/api/cards` 返回的 card 没有携带来源 note 的类型，前端按标题关键词判断时，“白凤乌鸡蛋”没有团购关键词，导致被团购工作台过滤掉。
- 本轮已做：
  - `backend/app/services/app_service.py`：`list_cards` 返回来源 note 的 `cardType/systemCategory/visibilityConfig`。
  - `miniprogram/pages/library/index.wxml`：房源筛选面板改为只在 `showPropertyFilters` 时显示；商品筛选面板只在 `showGroupbuyFilters` 时显示。
- 已验证：
  - `node -c miniprogram/pages/library/index.js`：通过。
  - `python3 -m py_compile backend/app/services/app_service.py`：通过。
  - 本轮关键文件 `git diff --check`：通过。
  - 本地模拟：补出 `cardType=groupbuy_product` 后，“白凤乌鸡蛋”会被 `isGroupbuyCard` 识别。
- 未验证/待办：
  - 本机系统 Python 缺少 `pytest`，完整后端测试未运行。
  - 当前修正尚未部署到生产；部署后端并上传/预览小程序后，需要真机验证该商品进入团购/商品工作台。

## 2026-06-24 十五续：后端已临时部署到生产

- 用户最新要求：
  - 先部署后端，便于测试前端。
- 本轮已执行：
  - 检查生产磁盘和 Docker 状态：根分区约 60%，空间足够；后端和 Postgres 容器正常。
  - 备份生产 `/home/ubuntu/teamBuy/backend/app` 到 `/home/ubuntu/teamBuy_deploy_backups/backend_app_20260624_162457.tar.gz`。
  - 同步本地 `backend/app` 到生产服务器。
  - 尝试重建后端镜像，但 `docker compose build backend` 卡在 `apt-get update`；未中断旧服务。
  - 停止卡住的构建后，将生产服务器 `backend/app` 复制进运行中的 `teambuy-backend-1:/app/app` 并重启容器。
- 已验证：
  - `https://teambuy.lifelove.top/health` 返回 200，数据库正常。
  - 生产 `/api/cards?ownerUserId=user_25ec00a0f0` 已返回“白凤乌鸡蛋”的 `cardType=groupbuy_product`、`systemCategory=团购`、`sourceNoteId=note_ec9fc09893`。
  - 生产团购看板接口 `/api/dashboard/business?...&mode=groupbuy` 返回 200。
- 注意：
  - 当前后端修复已在线上容器生效，但镜像本身未重建成功；如果后续容器被强制重建，需要重新构建或再次确认代码已进入镜像。
  - 前端筛选面板逻辑仍需要微信开发者工具预览/上传小程序后才会生效。

## 2026-06-24 十六续：团购样式堆叠与封面不显示

- 用户最新反馈：
  - 团购资料页真机上按钮和内容堆叠，商品卡片“更多”按钮横向溢出。
  - 编辑展示页“已选商品顺序”区域封面、正文和排序/隐藏/删除按钮挤在一起。
  - 设置封面后，微信转发可显示，但资料页团购卡片仍显示占位。
- 本轮已做：
  - `miniprogram/pages/library/index.wxss`：团购商品卡片动作区改为两列布局；有客户/接龙时主操作独占一行，分享和更多下一行平分；按钮补齐 flex 居中、最小宽度和不换行规则。
  - `miniprogram/pages/showcase-edit/index.wxss`：已选商品行改为上方封面+正文、下方四个操作按钮，底部固定操作按钮补齐 flex 居中和不换行。
  - `backend/app/services/app_service.py`：`list_cards` 在 card 无封面时兜底返回来源 note 的封面或第一张图片。
- 已部署：
  - 已同步 `backend/app` 到生产服务器，并复制进运行中的 `teambuy-backend-1` 容器后重启。
- 已验证：
  - `node -c miniprogram/pages/library/index.js`：通过。
  - `node -c miniprogram/pages/showcase-edit/index.js`：通过。
  - `python3 -m py_compile backend/app/services/app_service.py`：通过。
  - 本轮关键文件 `git diff --check`：通过。
  - 生产健康接口正常。
  - 生产 `/api/cards?ownerUserId=user_25ec00a0f0` 中“白凤乌鸡蛋”已返回 `coverUrl=https://teambuy.lifelove.top/media/media_44020dedaf-manual_asset_0c176188a5.webp`。
- 待用户验证：
  - 在微信开发者工具重新预览/上传小程序后，真机检查团购资料列表和展示页编辑页是否还堆叠。
  - 刷新资料页确认团购商品卡片显示封面，不再显示“资料”占位。

## 2026-06-24 十七续：资料卡片按钮背景过长

- 用户最新反馈：
  - 真机截图中房源和团购资料卡片底部按钮背景仍然过长，没有必要占满整行。
- 本轮已做：
  - `miniprogram/pages/library/index.wxss`：资料卡片操作区从等分网格改为可换行 flex 胶囊布局。
  - “看客 / 分享 / 更多 / 处理接龙”等按钮改为按内容宽度显示，只保留最小可点宽度，不再拉伸成整条背景。
- 已验证：
  - `node -c miniprogram/pages/library/index.js`：通过。
  - `git diff --check -- miniprogram/pages/library/index.wxss`：通过。
- 待用户验证：
  - 微信开发者工具重新预览/上传后，真机确认房源和团购资料卡片底部按钮不再横向过长。

## 2026-06-24 十八续：客户资料提交文案改为留言

- 用户最新反馈：
  - 原客户资料提交术语很多人看不懂，希望统一改成“留言”。
  - 同时询问底部第四个“工作台”与首页、我的里切换工作台的区别和意义。
- 本轮已做：
  - 小程序可见文案统一改为“留言”，覆盖资料详情、客户动作页、客户看板、工作台配置、电子名片/服务方案、分享图和演示数据。
  - 后端演示文案同步改为“留言”。
  - 内部字段名、action key 和统计结构保持不变。
- 已验证：
  - `rg -n "留资" miniprogram backend/app backend/mock`：无结果。
  - `node -c` 覆盖 `dashboard.js`、`workspace-mode.js`、`note-edit`、`note-actions`、`business-dashboard`：通过。
  - `python3 -m py_compile backend/app/services/app_service.py`：通过。
  - 本轮关键文件 `git diff --check`：通过。
- 产品判断：
  - 首页里的工作台切换更像“选择当前业务场景 + 快速开始”。
  - 我的里的工作台切换更像“账号默认设置”。
  - 底部第四个 Tab 当前实际是经营反馈页，展示访客、客户动态、高意向和待处理事项；它不应主要承担切换工作台的心智。
  - 后续更建议把第四个 Tab 命名和定位收敛为“看板”或“反馈”，工作台切换只保留为顶部筛选或入口辅助。

## 2026-06-24 十九续：客户看板命名与卡片创建时间

- 用户最新反馈：
  - 第四个 Tab 可以叫“客户看板”。
  - 希望每个资料库卡片和合集卡片下面都显示创建时间。
- 本轮已做：
  - `miniprogram/app.json`：第四个 Tab 文案改为“客户看板”。
  - `miniprogram/pages/visits/index.wxml`：页面标题改为“客户看板”。
  - 首页和我的页跳转入口改为“去客户看板”。
  - `miniprogram/utils/dashboard.js`：资料卡片增加 `createdText`。
  - `miniprogram/pages/library/index.wxml/.wxss`：资料卡片下方展示创建时间。
  - `miniprogram/pages/showcases/index.js/.wxml/.wxss`：合集卡片下方展示创建时间。
- 已验证：
  - `node -c miniprogram/utils/dashboard.js`：通过。
  - `node -c miniprogram/pages/showcases/index.js`：通过。
  - `node -c miniprogram/pages/library/index.js`：通过。
  - `miniprogram/app.json` JSON 解析：通过。
  - 本轮关键文件 `git diff --check`：通过。
- 待用户验证：
  - 微信开发者工具重新预览/上传后，确认底部 Tab 显示“客户看板”。
  - 真机确认资料库卡片、合集卡片创建时间位置不挤压按钮。

## 2026-06-24 二十续：团购/商品 P1 体验收口

- 用户最新问题：
  - P0 企业微信团购笔记识别的“高置信”逻辑是什么，是否还是只看标题。
  - P1 中客户看板团购化、商品复用体验、空态引导可以优先处理。
- 识别逻辑确认：
  - 后端 `skill_router_service.py` 高置信团购识别基于全文和结构化字段，不是只看标题。
  - 高置信条件包含：团购分数 `score >= 5`、团购分数高于房源分数、命中商品信号、命中价格或解析器 hint、命中取货/规格/截止/接龙等交付信号，并且字段数量足够。
  - 不满足高置信时会降为普通资料或给人工确认建议，不直接强行进团购。
- 本轮已做：
  - `miniprogram/pages/business-dashboard/`：团购模式下 Tab 改为“待处理 / 买家/访客 / 商品效果 / 发群效果”，首屏和空态切到接龙、下单、买家、商品点击语境。
  - `miniprogram/pages/library/index.js`：商品复用前增加确认，说明不会复制旧接龙、订单、访客和统计。
  - `miniprogram/pages/library/index.wxml/.wxss`：团购资料空态增加“新建商品 / 商品合集”入口。
  - `miniprogram/pages/showcases/index.js/.wxml/.wxss`：团购合集空态增加“先建一个商品”入口。
  - `miniprogram/pages/home/index.wxml`：团购模式空态提示新建商品、企业微信导入和发群后接龙反馈。
- 已验证：
  - `node -c miniprogram/pages/business-dashboard/index.js`：通过。
  - `node -c miniprogram/pages/library/index.js`：通过。
  - `node -c miniprogram/pages/showcases/index.js`：通过。
  - `node -c miniprogram/pages/home/index.js`：通过。
  - 本轮关键文件 `git diff --check`：通过。
- 待用户验证：
  - 真机检查团购客户看板文案是否更像商品/接龙场景。
  - 复用商品弹窗文案是否足够清楚。
  - 团购资料库和商品合集空态入口是否顺手。

## 2026-06-24 二十一续：电子名片/服务工作台一期优化启动

- 用户最新要求：
  - 开始做电子名片/服务工作台，并基于房源和团购工作台经验看首页如何优化。
- 当前理解：
  - 这个工作台的核心不是“资料整理”，而是让服务型用户先做名片、服务方案，再看咨询客户和方案效果。
  - 首页已经有服务工作台视觉，但数据范围和看板语境还需要独立。
- 本轮已做：
  - `miniprogram/pages/home/index.js`：服务工作台首页统计只按 `business_card/service_offer` 计算。
  - `miniprogram/pages/home/index.js`：服务模式“看资料”和统计卡跳转资料库时写入 `service_workspace` 入口过滤。
  - `miniprogram/pages/home/index.wxml`：服务模式空态提示先做名片、再补服务介绍页；咨询反馈空态改为名片/方案发出后回流。
  - `miniprogram/pages/library/index.js/.wxml`：资料库支持服务工作台入口过滤，只看名片/服务方案，并展示对应说明。
  - `miniprogram/pages/business-dashboard/index.js/.wxml`：服务模式客户看板 Tab 改为“待咨询 / 访客 / 方案效果 / 案例合集”，首屏与效果页文案切到咨询、方案和案例合集语境。
- 已验证：
  - `node -c miniprogram/pages/home/index.js`：通过。
  - `node -c miniprogram/pages/library/index.js`：通过。
  - `node -c miniprogram/pages/business-dashboard/index.js`：通过。
  - 本轮关键文件 `git diff --check`：通过。
- 下一步建议：
  - 继续打磨服务工作台 P0/P1：名片详情页、服务方案详情页、客户视角预览、发布前检查、咨询/预约回流。
  - 真机检查首页服务模式的统计、快捷入口、资料库隔离和客户看板文案。

## 2026-06-24 二十二续：电子名片/服务工作台 P0 闭环补强

- 用户最新要求：
  - “可以。先做 p0。”
- 本轮已做：
  - `miniprogram/pages/home/index.js`：服务工作台资源数改为电子名片 + 服务方案；咨询数只取真实客户互动，不再用服务方案数量兜底。
  - `backend/app/services/app_service.py`：客户看板 `mode=service` 只聚合电子名片和服务方案；合集、点击事件、客户动作和线索同步按服务资料范围收窄。
  - `backend/tests/test_app.py`：新增服务看板隔离用例，验证名片/服务方案/团购商品混合时，服务看板不统计团购订单和团购资料。
  - `miniprogram/pages/business-card-studio/`：新增发给客户前检查；未保存或有未保存改动时隐藏分享菜单；保存后分享路径指向客户页；底部主动作改为“保存并预览”。
  - `miniprogram/pages/service-offer-studio/`：新增服务方案发前检查；未保存或有未保存改动时隐藏分享菜单；保存后分享路径指向客户页；底部主动作改为“保存并预览”。
  - `miniprogram/pages/business-dashboard/index.js`：服务模式下待咨询、访客、方案效果等兜底文案不再出现“预约看房 / 看房源 / 房源资料”。
- 已验证：
  - `node --check miniprogram/pages/home/index.js`：通过。
  - `node --check miniprogram/pages/business-card-studio/index.js`：通过。
  - `node --check miniprogram/pages/service-offer-studio/index.js`：通过。
  - `node --check miniprogram/pages/business-dashboard/index.js`：通过。
  - `python3 -m py_compile backend/app/services/app_service.py backend/tests/test_app.py`：通过。
  - 本轮关键文件 `git diff --check`：通过。
- 未跑通：
  - `python3 -m pytest backend/tests/test_app.py::test_service_business_dashboard_only_counts_service_customer_data ...` 未执行成功，原因是当前系统 Python 缺少 `pytest`。
- 待用户验证：
  - 微信开发者工具重新预览/上传体验版后，真机确认名片/方案编辑页未保存时不显示分享、保存并预览能打开客户页。
  - 客户页留言/预约提交后，服务客户看板只显示服务咨询，不混入房源和团购。
- 下一步建议：
  - 如果 P0 真机通过，再做 P1：服务案例合集空态/模板、名片与服务方案复用、服务工作台首页最近咨询卡片更精细化。

## 2026-06-24 二十三续：电子名片/服务工作台 P1 收口与后端部署

- 用户最新要求：
  - “p1 也一起做了，然后在部署后端，我测试前端。”
- 本轮已做：
  - `miniprogram/pages/library/index.js/.wxml`：服务资料卡新增复用入口；复用电子名片进入名片工作台，复用服务方案进入服务方案工作台；复用说明明确不复制访客、留言、预约和统计。
  - `miniprogram/utils/resource-navigation.js`：电子名片和服务方案点击编辑时进入对应专属工作台，不再回通用编辑页。
  - `miniprogram/pages/library/index.wxml`：服务资料库空态增加“做名片 / 做方案”。
  - `miniprogram/pages/showcases/index.js/.wxml/.wxss`：案例合集空态增加“先做名片 / 先做方案”。
  - `miniprogram/pages/business-dashboard/index.wxml`：服务客户看板空态继续服务化，覆盖咨询动态、服务分享来源、咨询客户明细和方案效果。
- 已验证：
  - `node --check` 覆盖首页、资料库、合集、名片工作台、服务方案工作台、客户看板和资源跳转工具：通过。
  - `python3 -m py_compile backend/app/services/app_service.py backend/tests/test_app.py`：通过。
  - `.venv312/bin/python -m pytest backend/tests/test_app.py::test_service_business_dashboard_only_counts_service_customer_data backend/tests/test_app.py::test_property_business_dashboard_only_counts_property_customer_data -q`：2 passed。
  - 本轮关键文件 `git diff --check`：通过。
- 后端部署结果：
  - 部署前备份：`/home/ubuntu/teamBuy/backups/backend-code-before-service-p1-20260624233925.tgz`。
  - 已同步本地后端代码到服务器，未覆盖 `.env`、`backend/secrets/`、媒体目录或运行态 mock 数据。
  - 已执行 `docker compose build backend && docker compose up -d backend`。
  - `teambuy-backend-1` 当前运行正常。
  - `http://127.0.0.1:8002/health`：200 OK。
  - `https://teambuy.lifelove.top/health`：200 OK。
  - 容器内确认存在 `_is_service_note` 和 `mode == "service"` 看板过滤逻辑。
  - 部署后服务器根分区约 64%，剩余约 21G。
- 用户测试前注意：
  - 后端已生效。
  - 小程序前端仍需要在微信开发者工具重新预览或上传体验版，才能看到 P1 前端改动。
- 建议测试顺序：
  - 服务工作台首页：确认统计、空态和入口。
  - 资料库服务视图：确认编辑进入专属工作台、复用生成新名片/方案、加入案例合集。
  - 名片/方案工作台：确认保存并预览、未保存不分享、保存后可分享。
  - 客户页：提交留言/预约。
  - 咨询看板：确认只出现服务咨询，不混房源/团购。

## 2026-06-25 零点续：电子名片/服务方案资料库和模板一致性修复

- 用户最新反馈：
  - 点击/转发“方案”和“电子名片”后，回到工作台资料库看不到对应资料。
  - 服务方案“展示模板”和“确认详情页效果”未编辑也显示不同内容，其他服务模板也类似。
- 根因确认：
  - 名片/方案保存为 `user_notes`，资料库读 `/api/cards`，旧接口未合并没有 backing card 的服务 note。
  - 服务方案模板小预览读 `template.preview` 示例内容，确认效果读表单默认内容；默认表单非空导致模板 defaults 未覆盖。
- 本轮已做：
  - `backend/app/services/app_service.py`：`list_cards` 合并 note-only 的 `business_card/service_offer` 资料，返回 `note_card_{noteId}`、`sourceNoteId`、`cardType`、`categoryName`、统计和客户摘要。
  - `backend/tests/test_app.py`：新增 `test_service_note_resources_are_listed_as_library_cards`，覆盖服务方案保存后能进入资料库列表。
  - `miniprogram/utils/resource-navigation.js`：资料库查看服务资料打开客户预览页，编辑才进入专属工作台。
  - `miniprogram/pages/library/index.js`：note-only 服务资料删除改为删除来源 note。
  - `miniprogram/pages/service-offer-studio/index.js`：模板 defaults 在未手动改写字段时生效；小预览的服务内容和统计项改为使用实际表单内容。
  - `miniprogram/pages/business-card-studio/index.js`：名片模板 defaults 同步采用同样规则。
- 已验证：
  - `node --check miniprogram/pages/service-offer-studio/index.js`：通过。
  - `node --check miniprogram/pages/business-card-studio/index.js`：通过。
  - `node --check miniprogram/pages/library/index.js`：通过。
  - `node --check miniprogram/utils/resource-navigation.js`：通过。
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q`：100 passed。
- 待继续：
  - 后端已部署，线上 `/api/cards` 已包含服务 note-only 资料合并逻辑。
  - 用户需重新上传/预览小程序体验版，才能验证前端模板一致性、资料库查看/编辑/删除路径。
- 部署结果：
  - 部署前备份：`/home/ubuntu/teamBuy/backups/backend-code-before-service-library-template-202606250026.tgz`。
  - 已同步后端代码，未覆盖生产 `.env`、`backend/secrets/`、媒体目录和运行态 mock 数据。
  - 已执行 `docker compose build backend && docker compose up -d backend`。
  - `teambuy-backend-1` 已重建并启动。
  - 内网 `http://127.0.0.1:8002/health`：200 OK。
  - 公网 `https://teambuy.lifelove.top/health`：200 OK。
  - 容器内确认存在 `_service_note_card_rows`。
  - 部署前服务器根分区约 65%，剩余约 21G。

## 2026-06-25 续：电子名片/服务方案确认页点选编辑

- 用户最新要求：
  - 参考 Codex 浏览器网页上的备注修改方式，在模板/效果页上点击内容进行修改，保存后替换成用户自己的内容。
- 本轮方案：
  - 先做“半所见即所得”：不推翻当前三步流程，在确认效果页增加点选编辑。
  - 点选编辑只更新当前页面表单和预览，最终仍通过“保存并预览”写入后端。
- 本轮已做：
  - `miniprogram/pages/service-offer-studio/index.js/.wxml/.wxss`：
    - 确认效果页支持点击服务名称、一句话卖点、适合人群、服务内容、流程/报价/案例、联系方式、预约说明。
    - 点击后底部弹出编辑面板，保存修改后即时刷新预览，并标记为未保存。
  - `miniprogram/pages/business-card-studio/index.js/.wxml/.wxss`：
    - 名片卡片预览和详情预览支持点击姓名、身份、公司/门店、一句话介绍、服务介绍、服务范围、联系方式。
    - 逻辑同样只回写当前表单，最终由“保存并预览”持久化。
- 已验证：
  - `node --check miniprogram/pages/service-offer-studio/index.js`：通过。
  - `node --check miniprogram/pages/business-card-studio/index.js`：通过。
  - WXML view 标签配对：服务方案 258/258；电子名片 166/166。
  - 本轮关键文件 `git diff --check`：通过。
- 待用户验证：
  - 微信开发者工具重新预览/上传体验版。
  - 服务方案：点击标题、人群、服务内容、流程/报价/案例、联系方式，确认底部编辑面板和即时刷新。
  - 电子名片：在卡片预览和详情预览分别点击内容修改，确认预览刷新。
  - 修改后未保存时分享仍不可用；点击“保存并预览”后客户页展示修改后的内容。

## 2026-06-25 续二：电子名片浅色模板编辑角标修复

- 用户最新反馈：
  - 电子名片第二个模板背景偏浅，点选编辑角标使用白色导致不清晰。
- 本轮已做：
  - `miniprogram/pages/business-card-studio/index.wxss`：为 `store_sales_card` 模板单独设置点选编辑边框、背景和角标颜色，浅色背景下改为绿色角标。
- 已验证：
  - `node --check miniprogram/pages/business-card-studio/index.js`：通过。
  - `node --check miniprogram/pages/service-offer-studio/index.js`：通过。
  - 本轮关键文件 `git diff --check`：通过。
- 待用户验证：
  - 重新上传体验版后，确认第二个名片模板卡片预览和详情预览的“编辑”角标可读。

## 2026-06-25 续三：服务工作台 P1 统一收口

- 用户最新要求：
  - 服务工作台整体测试无大问题，把剩余 P1 统一补上。
- 本轮已做：
  - 服务方案工作台：
    - 确认页点封面、头像占位、案例图可直接替换图片。
    - 联系方式缺失时显示“补电话/补微信/补邮箱/补网址”。
    - 底部电话/微信按钮文案可点选编辑，并写入 `structuredData.primaryAction/secondaryAction`。
    - 切模板时若已有未保存改动，弹出“保留我的内容 / 套用模板文案”。
  - 电子名片工作台：
    - 卡片预览/详情预览点头像和二维码可直接替换。
    - 联系方式缺失时可直接补齐。
    - 切模板时同样提供“保留我的内容 / 套用模板文案”。
  - 客户页：
    - 服务方案电话/微信咨询按钮使用编辑器里保存的按钮文案。
    - 服务方案留言/预约表单占位文案切成咨询问题、预算、期望服务方式语境。
- 已验证：
  - `node --check miniprogram/pages/service-offer-studio/index.js`：通过。
  - `node --check miniprogram/pages/business-card-studio/index.js`：通过。
  - `node --check miniprogram/pages/note-preview/index.js`：通过。
  - WXML view 标签配对：服务方案 263/263；电子名片 172/172；客户页 176/176。
  - 本轮关键文件 `git diff --check`：通过。
- 待用户验证：
  - 重新上传体验版后，验证点图替换、补联系方式、切模板选择、按钮文案保存后客户页展示。

## 2026-06-25 续四：团购/商品首页访客改订单

- 用户最新要求：
  - 团购/商品工作台首页“今日待处理”第四项不再放访客，改成订单；普通商品更应该展示订单详情。
- 本轮已做：
  - 首页团购/商品四格改为“商品 / 待处理 / 今日接龙 / 订单”。
  - “订单”统计使用订单/接龙总数，点击进入 `/pages/orders/index?role=seller`。
  - “待处理”继续进入待处理订单，“今日接龙”继续进入今日过滤订单。
  - 商品资料卡主按钮：有接龙显示“处理接龙”，只有普通下单显示“处理订单”，只有访客不再显示处理按钮。
  - 团购资料库筛选从“有访客”改为“有订单”。
- 已验证：
  - `node --check miniprogram/pages/home/index.js`：通过。
  - `node --check miniprogram/pages/library/index.js`：通过。
  - `node --check miniprogram/pages/orders/index.js`：通过。
  - `node --check miniprogram/utils/dashboard.js`：通过。
  - `node --check miniprogram/utils/workspace-mode.js`：通过。
  - WXML view 标签配对：首页 72/72；资料库 104/104。
  - 本轮关键文件 `git diff --check`：通过。
- 待用户验证：
  - 重新上传体验版后，检查团购/商品首页第四格、点击跳转、资料库商品卡按钮文案和筛选项。

## 2026-06-25 续五：日常资料台反馈命名、真实待整理与普通资料整理

- 用户最新要求：
  - 继续改首页和底部 Tab 的“客户看板”命名。
  - 做待整理任务真实化。
  - 做普通资料的一键整理和资料包增强。
- 本轮已做：
  - 底部 Tab `pages/visits` 改名为“反馈”，反馈页标题同步改为“反馈”。
  - 首页反馈面板按模式切文案：日常资料台为“分享反馈 / 看反馈”，团购为买家动态，服务为咨询动态，房源仍保留客户看板。
  - 首页日常资料台“待整理任务”改为真实任务卡：待认领、待整理、待识别图片、未完成资料包。
  - `pages/notes` 支持首页任务跳转筛选：`sourceType=ocr`、`migrationPending=1`、`plain=1`、`systemCategory`。
  - 普通笔记详情增加“一键整理”“加入资料包”“添加能力”三类入口；添加能力仍保留为后续插件方向。
  - 资料包编辑页支持 `noteId` 直达预选，普通资料包不再套房源合集文案和租金校验。
  - 后端普通资料 `organize` 增加轻量 `organizeResult`，用于资料包、分享摘要、标签归类。
- 已验证：
  - `node --check miniprogram/pages/home/index.js`
  - `node --check miniprogram/pages/notes/index.js`
  - `node --check miniprogram/pages/visits/index.js`
  - `node --check miniprogram/pages/note-edit/index.js`
  - `node --check miniprogram/pages/showcase-edit/index.js`
  - `miniprogram/app.json` JSON 解析通过。
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q`：100 passed。
  - `git diff --check`：通过。
- 未做：
  - 按用户要求未部署、未上传小程序体验版。
- 待用户验证：
  - 重新预览后检查：首页待整理任务数字和跳转是否符合实际数据；普通笔记详情按钮是否不挤、不误导；普通资料加入资料包后是否只预选当前资料。

## 2026-06-25 续六：资料库和反馈页按当前工作台隔离

- 用户最新反馈：
  - 3:02 左右点击资料库，日常资料台疑似仍看到其他工作台资料。
  - “反馈”Tab 内仍显示四个工作台切换，日常用户看到房源/服务会突兀。
- 本轮已做：
  - 资料库新增 `notes_workspace` 范围：当前工作台是日常资料台时，默认排除房源、团购、名片/服务方案，只显示普通资料、链接、图片、笔记等日常资料。
  - 反馈页数据过滤补齐：日常只看日常资料，房源只看房源，团购只看商品，服务只看名片/服务方案。
  - 反馈页移除四工作台切换 Tab，避免底部一级页自动暴露所有业务场景。
  - 日常反馈页统计图标/筛选项改为中性表达，“进入/分享记录”改去资料包，不再跳业务看板。
- 已验证：
  - `node --check miniprogram/pages/library/index.js`：通过。
  - `node --check miniprogram/pages/visits/index.js`：通过。
  - `git diff --check`：通过。
- 待用户验证：
  - 重新预览后在日常资料台点“资料”，确认看不到房源/团购/服务资料。
  - 点“反馈”，确认没有四个工作台切换，列表只显示当前工作台资料反馈。

## 2026-06-25 续七：资料库专题筛选

- 用户最新要求：
  - 先做资料库专题筛选。
- 本轮已做：
  - 资料库加载时同步读取用户专题。
  - 专题列表按当前工作台范围内的资料统计，只展示真实有关联资料的专题。
  - 资料库新增“专题筛选”胶囊，可与分类、标签、关键词组合筛选。
  - 关键词搜索纳入专题名，用户可直接搜专题相关资料。
  - 文档确认：专题是内部组织和检索维度，资料包是外部分享集合。
- 已验证：
  - `node --check miniprogram/pages/library/index.js`：通过。
  - `git diff --check -- miniprogram/pages/library/index.js miniprogram/pages/library/index.wxml miniprogram/pages/library/index.wxss`：通过。
- 未做：
  - 本轮未部署、未上传小程序体验版。
- 待用户验证：
  - 在任一工作台下进入资料库，确认有专题的资料会出现“专题筛选”。
  - 点击专题后，列表只剩该专题资料；切回“全部”恢复当前工作台资料范围。

## 2026-06-25 续八：资料工作台 P0 收口

- 用户最新要求：
  - 确认“专题=内部整理检索，资料包=外部分享集合”后，要求把剩余 P0 全部开发。
- 本轮已做：
  - 资料库选中专题后出现“建资料包”入口。
  - 入口文案明确：专题用于内部整理和检索；资料包用于整理成可分享页面。
  - 资料包编辑页支持 `topicId/topicName`，从专题进入时只读取该专题资料并默认加入。
  - 资料包编辑页展示“来自专题”说明，继续强化专题/资料包心智。
  - 普通资料卡点“合集”时直接进入资料包编辑页并预选当前资料。
  - 普通资料包空封面提示改为资料语境，不再残留房源图提示。
- 已验证：
  - `node --check miniprogram/pages/library/index.js`：通过。
  - `node --check miniprogram/pages/showcase-edit/index.js`：通过。
  - WXML view 标签配对：资料库 110/110；资料包编辑 86/86。
  - 本轮关键文件 `git diff --check`：通过。
- 未做：
  - 本轮未部署、未上传小程序体验版。
- 待用户验证：
  - 资料库选专题后点“建资料包”，确认资料包里只自动加入该专题资料。
  - 资料库普通资料卡点“合集”，确认资料包里只预选当前资料。
  - 普通资料包页面不再出现房源化文案。

## 2026-06-25 续九：专题 / 合集命名统一

- 用户最新要求：
  - 不再创造过多名词。内部统一叫“专题”；外部统一叫“合集”。
  - 四个工作台外部集合命名固定为：日常合集、房源合集、商品合集、案例合集。
- 本轮已做：
  - 小程序前端去掉“资料包 / 团购合集 / 专辑”等旧口径。
  - 资料库专题筛选后的入口改为“建合集”，说明“专题用于内部整理和检索，合集用于分享”。
  - 普通资料详情按钮改为“加入合集”。
  - 日常工作台、首页任务、反馈页、业务看板旧“资料包”改为“日常合集”。
  - 合集新建页按场景显示：日常合集、房源合集、商品合集、案例合集。
- 已验证：
  - 前端 `rg "资料包|团购合集|服务资料包|普通资料包|建资料包|加入资料包|资料包效果|专辑"`：无结果。
  - `node --check`：showcase-edit、showcases、library、home、workspace-mode 通过。
  - WXML view 标签配对通过。
  - 本轮关键文件 `git diff --check`：通过。
- 未做：
  - 未部署、未上传小程序体验版。

## 2026-06-25 续十：资料工作台 P1 统一收口

- 用户最新要求：
  - 把剩余 P1 全部做一下，并统一测试。
- 本轮已做：
  - 专题页：
    - 增加专题/合集心智说明。
    - 支持从专题直接建日常合集。
    - 支持删除专题，删除只移除资料上的专题关联，不删除资料。
  - 日常合集编辑页：
    - 日常/服务场景不再沿用房源推荐包、租金、户型等文案。
    - 日常“按条件筛选”改为后续“按专题/标签生成”能力，不误展示房源筛选面板。
  - 普通资料详情：
    - “添加能力”改为插件占位面板，留言、咨询、接龙均作为后续插件能力展示。
    - 不再一键打开运营配置，保留“先补摘要、标签和专题”的轻量整理入口。
  - 标签设置页：
    - 文案从房源示例改为通用资料、专题、合集语境。
  - 后端：
    - 普通资料一键整理生成选项改为“日常合集 / 分享摘要 / 标签归类”。
    - 新增删除专题接口，并回归删除后资料专题关联清理。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q`：101 passed。
  - `python3 -m compileall -q backend/app`：通过。
  - 小程序关键 JS `node --check`：通过。
  - 小程序 JSON 递归解析：通过。
  - WXML view 标签配对通过。
  - 旧词扫描无结果。
  - 本轮关键文件 `git diff --check`：通过。
- 后端部署：
  - 生产后端已部署删除专题接口。
  - 部署前备份目录：`/home/ubuntu/teamBuy/backups/backend-topic-delete-20260625-043627`。
  - 标准镜像构建长时间无输出，已改用容器热补丁方式复制 `app_service.py`、`routes_notes.py` 并重启 `teambuy-backend-1`。
  - 公网 `/health` 200；线上创建后删除探针专题验证通过，探针专题已清理。
- 小程序上传：
  - 本机已确认微信开发者工具 CLI 存在，但用户明确小程序体验版自行上传，本轮不再代传。
- 下一步真机回归：
  - 新增普通笔记 -> 资料库可见。
  - 资料库专题筛选。
  - 专题筛选后建日常合集。
  - 普通资料加入合集。
  - 日常合集发布/分享。
  - 反馈页数据按当前工作台隔离正常。

## 2026-06-25 续十一：专题管理入口与工作台入口出口检查

- 用户最新要求：
  - 补充完专题管理入口。
  - 检查四个工作台每一项的逻辑、入口和出口。
  - 没问题后开始讨论专门营销细节。
- 本轮已做：
  - `pages/library`：
    - “更多工具”新增“专题管理”，可直达专题页。
    - 专题筛选标题右侧新增“管理专题”。
    - “新增资料”按工作台进入对应新建路径：日常、房源、商品、服务不再混用。
    - 卡片“更多”菜单区分普通资料与房源资料，日常资料不再显示“编辑房源”。
  - `pages/home`：
    - 首页最近成果、反馈列表改为按当前工作台过滤，不再用全量资料导致四个工作台看起来相同。
    - 团购/商品“商品合集”入口改为进入合集列表，与日常、房源、服务保持一致。
  - `pages/showcases`：
    - 合集列表按当前工作台过滤：日常合集、房源合集、商品合集、案例合集分开展示。
    - 缓存读取路径也做过滤，避免旧缓存继续串台。
  - `pages/showcase-edit`：
    - 日常合集候选资料改为真实“日常资料”范围，不再用“全部”把房源、商品、服务资料带进去。
    - 分类统计对“资料/房源/商品/服务”等场景词做去重，避免计数虚高。
- 四工作台检查结论：
  - 日常资料台：入口和出口围绕资料入库、专题、日常合集、分享反馈。
  - 房源工作台：入口和出口围绕房源创建、房源资料、房源合集、客户看板。
  - 团购/商品工作台：入口和出口围绕商品创建、商品资料、商品合集、订单/接龙。
  - 服务工作台：入口和出口围绕名片、服务方案、案例合集、咨询反馈。
- 已验证：
  - `node --check miniprogram/pages/library/index.js`
  - `node --check miniprogram/pages/home/index.js`
  - `node --check miniprogram/pages/showcases/index.js`
  - `node --check miniprogram/pages/showcase-edit/index.js`
  - `node --check miniprogram/pages/visits/index.js`
  - `node --check miniprogram/utils/workspace-mode.js`
  - 小程序 JSON 递归解析通过。
  - 旧词扫描无结果。
  - 本轮关键文件 `git diff --check` 通过。
- 待用户操作：
  - 本轮是小程序前端改动，需用户上传体验版后真机查看。
  - 真机重点看：资料库“更多工具 -> 专题管理”、专题筛选右侧“管理专题”、四个工作台首页/资料/合集/反馈是否不再串台。

## 2026-06-25 续十二：第四期工作台优化阶段性交接归档

- 用户最新要求：
  - 进入“专门营销细节”前，先做一次阶段性交接归档。
  - 生成 `docs/第四期工作台优化.md`，适合直接给新 Codex 窗口继续执行。
- 本轮已做：
  - 新增 `docs/第四期工作台优化.md`。
  - 文档覆盖项目背景与目标、当前阶段目标、已完成功能、已修改/新增文件、当前代码状态、已知问题和风险、已确认产品/技术决策、下一步建议执行顺序、新会话第一条提示词。
  - 明确区分已完成、已部署、未上传体验版、仍需真机确认和下一阶段营销建议。
- 已验证：
  - `git diff --check -- docs/第四期工作台优化.md`：通过。
  - 文档尾部提示词代码块完整。
- 下一步：
  - 不建议继续大规模开发工作台功能。
  - 建议开始讨论第一批用户、首个打穿场景、产品一句话定位、销售路径和演示脚本。

## 2026-06-25 续十三：房源合集展示排列与微信助手入口

- 用户最新反馈：
  - 所有模板和展示页都应支持一列/双列，默认一列。
  - 按钮样式问题反复出现，应把按钮基线抽到主样式文件。
  - 手机点击“微信助手”只显示复制，想知道为什么不能自动添加微信客服。
- 本轮已做：
  - `pages/showcase-edit` 增加“一列 / 双列”展示排列选择，默认一列，保存到 `displayConfig.layoutMode`。
  - `pages/showcase-view` 四类模板按 `layoutMode` 渲染，一列默认，双列作为用户主动选择。
  - `miniprogram/app.wxss` 补原生 `button` 全局 reset 和常用按钮/标签居中基线。
  - 后端新增 `/api/wecom/customer-service-config`，复用现有整理助手企业微信客服配置，按 `WECOM_CORP_ID / WECOM_OPEN_KFID` 生成小程序打开客服所需参数。
  - `pages/property-same` 主按钮改为“打开微信助手”，优先请求后端配置并调用 `wx.openCustomerServiceChat`，失败或后端未部署时复制整理指令兜底。
  - `miniprogram/config/customer-service.js` 只保留离线兜底，不放密钥，也不是主配置来源。
- 重要说明：
  - 小程序不能凭微信号直接自动添加客服，必须走微信官方企业微信客服能力。
  - 当前项目已有归纳整理用企业微信客服，主路径应复用后端 env，不需要前端另配一套。
- 待验证：
  - 用户重新上传体验版后，检查展示页默认一列是否稳定，切双列是否不挤压按钮。
  - 后端部署新接口后验证“打开微信助手”是否直达归纳整理助手的企业微信客服会话。

## 2026-06-25 续十四：后端承接企业微信助手与自有小程序卡

- 本轮已做：
  - 后端新增 `/api/wecom/customer-service-config`，复用现有归纳整理助手 `WECOM_CORP_ID / WECOM_OPEN_KFID`，不要求前端另配客服。
  - 企业微信 `weapp` 小程序卡解析补充 `noteId/showcaseId/sourceNoteId/id` 识别。
  - 自有小程序房源卡/房源合集进入企业微信后，后端会回查公开结构，写入生成草稿的 `visibilityConfig.structuredData.internalMiniapp`。
  - 公开结构过滤私密上游/房东/渠道/电话/微信/rawText 等字段，避免 B 中介生成同款时继承 A 的私密联系人。
  - 后端展示页配置保留 `layoutMode=list/grid`。
- 已验证：
  - 后端全量测试 104 passed。
  - 后端编译通过。
- 已部署生产：
  - 备份目录：`/home/ubuntu/teamBuy/backups/backend-property-agent-20260625-073636`。
  - 已同步并热补丁后端文件到 `teambuy-backend-1`，容器已重启。
  - `https://teambuy.lifelove.top/health` 返回 200。
  - `https://teambuy.lifelove.top/api/wecom/customer-service-config` 返回 `configured=true`。
  - 容器内后端编译通过。
- 仍需用户侧：
  - 小程序体验版更新后，“打开微信助手”优先直达归纳整理助手；失败才复制兜底。

## 2026-06-25 续十五：生成同款克隆接口与媒体资产 hash 去重底座

- 用户最新要求：
  - 优先补齐两个基础底座：
    - 完整后端克隆接口：A 的公开房源卡/合集一键生成 B 名下正式房源卡/合集，并替换联系方式。
    - 媒体资产 hash 去重：图片 WebP hash、视频 hash、MediaAsset 引用表落库。
- 本轮已做：
  - 后端新增 `MediaAsset` / `MediaAssetRef` 模型。
  - 后端新增 `media_assets`、`media_asset_refs` 两张表及索引/唯一约束。
  - `process_and_store_media` 改为：
    - 计算原始 hash。
    - 图片转 WebP / 视频转 MP4。
    - 计算处理后 hash。
    - 命中已有资产时复用 URL，只新增引用。
  - OCR 图片上传也接入 hash 去重，仍保留真实本地落文件行为。
  - 新增 `POST /api/notes/property-same/clone`：
    - `sourceType=note`：生成 B 名下新房源卡。
    - `sourceType=showcase`：逐条复制公开房源为 B 名下新房源卡，再生成 B 名下新合集。
  - 克隆隐私边界：
    - 公开字段过滤 `contact/phone/wechat/landlord/upstream/channel/rawText` 等敏感字段。
    - B 的电话/微信替换进公开联系方式。
    - B 的上游联系人写入 `visibilityConfig.privateData.upstreamContact`，默认取 A 的公开联系方式或身份。
    - 不继承 A 的 `privateData`。
- 已验证：
  - 本地后端全量测试：144 passed。
  - 生产 `/health` 200。
  - 生产新接口已命中业务逻辑：
    - 缺用户返回 `用户不存在`。
    - 真实用户 + 缺源返回 `公开房源卡不存在`。
  - 生产 PostgreSQL 已存在 `media_assets`、`media_asset_refs`。
- 已部署生产：
  - 备份目录：`/home/ubuntu/teamBuy/backups/backend-clone-media-20260625-075259`。
  - 注意：本项目生产后端热补丁必须同时同步宿主机文件和 `docker cp` 到 `teambuy-backend-1:/app/app/...`；本轮已完成并重启容器。
- 下一步建议：
  - 小程序 `pages/property-same` 从“复制给企业微信助手”升级为优先调用 `POST /api/notes/property-same/clone`。（2026-06-25 续十六已完成）
  - 调用成功后根据返回 `type=note/showcase` 跳转到新房源卡或新合集编辑/预览页。（2026-06-25 续十六已完成）
  - 保留企业微信助手作为接口失败或用户想半自动整理时的兜底。
  - 后续单独做历史媒体 hash 回填任务。

## 2026-06-25 续十六：生成同款页接入后端克隆接口

- 用户最新要求：
  - 做小程序前端接入：`pages/property-same` 优先直接调用后端克隆接口，成功后跳到 B 的新房源卡/新合集；企业微信助手保留失败兜底。
- 本轮已做：
  - `miniprogram/services/api.js` 新增 `clonePropertySame(payload)`。
  - `miniprogram/pages/property-same/index.js`：
    - 新增 `handleGenerateSame`。
    - 有来源 ID 时调用 `POST /api/notes/property-same/clone`。
    - 缺来源或接口失败时自动走原有 `handleOpenAssistant`，复制整理指令并打开企业微信助手。
    - 成功生成 `note` 后跳 `/pages/note-edit/index?id=...`。
    - 成功生成 `showcase` 后跳 `/pages/showcase-edit/index?id=...&mode=property`。
  - `miniprogram/pages/property-same/index.wxml`：
    - 主按钮改为“生成同款”。
    - 次按钮保留“打开助手”。
    - 增加失败兜底提示。
  - `miniprogram/pages/property-same/index.wxss`：
    - 增加兜底提示样式和主按钮 disabled 状态。
- 已验证：
  - `node --check miniprogram/pages/property-same/index.js`：通过。
  - `node --check miniprogram/services/api.js`：通过。
  - 小程序 JSON 递归解析：通过。
  - 关键文件 `git diff --check`：通过。
- 待用户操作：
  - 需要在微信开发者工具上传体验版后真机测试。
  - 重点路径：房源卡公开页 -> 生成同款 -> 新房源卡编辑页；房源合集公开页 -> 生成同款 -> 新合集编辑页。

## 2026-06-26 续十七：生成同款首次登录与预览落点优化

- 用户最新反馈：
  - 生成同款已经能一键生成，问题不大。
  - 首次点击会进入登录页，旧登录页视觉粗糙，键盘顶起页面。
  - 生成成功后进入操作/编辑页，观感不像“一键生成”，还像需要继续点选操作。
- 本轮已做：
  - `pages/property-same` 未登录跳转登录页时带 `returnUrl`，保留原 `sourceType/sourceId/sourceTitle/publisherName/upstreamContact`。
  - 登录成功后回到原生成同款页面，并通过 `autoGenerate=1` 自动继续生成。
  - 生成成功后不再跳编辑页：
    - 房源卡跳 `/pages/note-preview/index?id=...`。
    - 房源合集跳 `/pages/showcase-view/index?id=...`。
  - 前端克隆合集时传 `publishShowcase=true`，保证生成后可以直接打开客户可见合集。
  - 登录页重做：
    - 删除昵称输入框，避免键盘首屏干扰。
    - 强化“房源工作台 / 一键生成你的房源卡”首屏。
    - 增加房源卡预览感模块和三步价值点。
    - 主按钮为“微信一键登录”，本地 mock 登录仅本地后端显示。
- 已验证：
  - `node --check miniprogram/pages/login/index.js`：通过。
  - `node --check miniprogram/pages/property-same/index.js`：通过。
  - 小程序 JSON 递归解析：通过。
- 待真机：
  - 未登录用户从房源卡/合集点“生成同款” -> 登录 -> 自动回跳并生成 -> 直接进入新预览页。

## 2026-06-26 续十八：登录页文案图片与 iPad 底部按钮修复

- 用户最新反馈：
  - 登录页 `openid 隔离` 改为“微信官方隔离”。
  - 登录页一键登录区域要有真实房源图片，使用用户给的第二张图片。
  - iPad 点击房源详情、合集详情后，底部按钮仍有变形/裁切。
  - 继续思考登录页是否应该获取微信昵称头像。
  - 登录说明“不展示给其他中介”容易让中介产生误会，文案需要更短。
- 本轮已做：
  - 新增压缩图片 `miniprogram/static/workspace/login-room.jpg`，约 93KB。
  - `pages/login` 预览卡使用真实房源图，隔离标签改为“微信官方隔离”。
  - `pages/login` 登录区改为“登录后保存到你的账号 / 用于生成同款、查看线索，下次打开还能继续管理。”，减少隐私说明带来的反向提醒。
  - `pages/note-preview`：
    - 生成同款卡片增加最大宽度居中。
    - iPad 宽屏下分享浮层跟随内容区域右侧。
    - 窄屏下生成同款卡片自动上下布局。
  - `pages/showcase-view`：
    - 底部联系按钮最大宽度居中。
    - 固定分享按钮最大宽度居中。
    - 生成同款卡片最大宽度居中，窄屏自动上下布局。
- 登录策略当前建议：
  - 当下继续使用“微信一键登录”，只确认身份和归属，降低首次生成同款阻力。
  - 不在登录第一步强制头像昵称；头像昵称更适合放到“我的资料/名片/发布资料前补全”。
  - 原因：登录用户不一定是中介，也可能是租客/客户；强制填头像昵称会影响他们查看房源。
- 已验证：
  - `node --check miniprogram/pages/login/index.js`：通过。
  - `node --check miniprogram/pages/property-same/index.js`：通过。
  - 小程序 JSON 递归解析：通过。

## 2026-06-26 续十九：合集模板发布态与朋友圈长页修复

- 用户最新反馈：
  - 换了合集模板后，转发给客户仍看到上一个模板。
  - 四个模板需要再检查，当前“朋友圈长页”在 iPad 上仍然变形。
- 本轮已做：
  - `pages/showcase-edit` 新增 `unpublishedChanges`：
    - 已发布合集修改模板、排列、标题、封面、联系方式、筛选和已选资料后，标记为新版未发布。
    - 顶部和底部分享按钮在新版未发布时改成“发布新版”。
    - `onShareAppMessage` 遇到新版未发布时提示“先发布新版再分享”，避免误发旧版客户页。
  - `pages/showcase-view`：
    - 四个模板主体统一 `max-width: 720rpx` 居中。
    - “朋友圈长页”首屏、故事卡、列表行、服务条和标签使用可收缩列，降低 iPad 宽屏/分栏下横向撑开风险。
- 已验证：
  - `node --check miniprogram/pages/showcase-edit/index.js`：通过。
  - `showcase-edit/showcase-view` JSON 解析通过。
  - `showcase-edit/showcase-view` WXML 标签配对通过。
  - 本轮关键文件 `git diff --check`：通过。
- 待真机：
  - 已发布合集 -> 切换模板 -> 底部应显示“发布新版”，发布后再分享客户应看到新模板。
  - 逐个切换 `精选橱窗 / 朋友圈长页 / 清单目录 / 品牌名片`，在 iPad 和手机确认主体不再横向拉满或按钮裁切。

## 2026-06-26 续二十：品牌名片、微信优先和发布排序

- 用户最新反馈：
  - 第四个“品牌名片”模板仍然变形。
  - 房源合集不应电话优先，微信联系更重要，几个模板都需要改。
  - 重新保存并发布新版后，应当算最近更新，合集列表应靠前。
- 本轮已做：
  - `pages/showcase-view`：
    - 精选橱窗、清单目录、品牌名片的联系按钮改为微信优先。
    - 统计位只要有微信就显示“微信咨询”，不再优先显示“电话咨询”。
    - 品牌名片模板头部去掉负 margin，列表卡片加固定列、两行标题、单行摘要、两列标签和无图兜底。
  - `pages/showcase-edit`：
    - 发布新版成功后清理 `teambuy_showcases_{userId}_{mode}` 本地缓存。
  - `pages/showcases`：
    - 缓存和接口数据都按 `updatedAt/createdAt` 倒序展示，配合后端发布时刷新 `updatedAt`，让新版合集靠前。
- 已验证：
  - `node --check miniprogram/pages/showcase-edit/index.js`
  - `node --check miniprogram/pages/showcase-view/index.js`
  - `node --check miniprogram/pages/showcases/index.js`
  - 三个页面 JSON 解析和 WXML 标签配对通过。
  - 本轮关键文件 `git diff --check`：通过。
- 待真机：
  - 品牌名片模板在 iPad 上确认头部、统计卡、联系区、房源列表、固定分享按钮不再裁切。
  - 有微信号的合集确认各模板先展示“微信联系”；没有微信号时才只展示电话。
  - 发布新版后返回合集列表，确认该合集靠前。

## 2026-06-26 续二十一：品牌名片房源卡结构修复

- 用户最新反馈：
  - 第四模板按钮仍然变形，缩略图也变形。
  - 房源布局整体缺少验收感，不能给中介“很酷、想用”的冲动。
- 本轮已做：
  - `pages/showcase-view` 品牌名片模板的房源卡新增 `brand-case-body`，让封面和内容区成为明确两列结构。
  - 封面图/无图兜底固定 `188rpx` 正方形，不再被标题、标签或价格高度挤压。
  - 内容区内部固定层级：标题两行、摘要一行、标签两列、价格底部。
  - `.sticky-share` 改为 flex 居中，房源模板分享按钮文案缩短为“发给客户”，避免按钮在 iPad 分栏下裁切。
- 已验证：
  - `node --check miniprogram/pages/showcase-view/index.js`：通过。
  - `showcase-view` JSON 解析和 WXML 标签配对通过。
  - `showcase-view` 关键文件 `git diff --check`：通过。
- 仍需真机确认：
  - 第四模板在 iPad 上的封面比例、右侧内容、标签和底部分享按钮是否稳定。
  - 如果还有“酷感不足”，下一步应做模板视觉重设计，而不是继续只修错位。

## 2026-06-26 续二十二：登录页小地图导航卖点合并

- 用户最新反馈：
  - 登录页“生成同款后”区域可加小地图，但应和房源预览卡合并一行。
  - 删除“图片已复用”，只保留“近地铁 / 可带看”。
- 本轮已做：
  - `pages/login` 预览卡改为三列布局：房源图、房源信息、小地图导航。
  - 小地图用 WXSS 画道路、定位点和“导航”胶囊，不增加图片资源。
  - 能力点从 3 个带数字卡改为 4 个短标签：房源卡、房源合集、查看客户线索、位置导航。
  - 更新 `docs/png/login-map-navigation-mockup.svg` 为合并版本。
- 已验证：
  - `node --check miniprogram/pages/login/index.js`：通过。
  - `login` JSON 解析和 WXML 标签配对通过。
  - 登录页和效果图 `git diff --check`：通过。
- 待真机：
  - 上传体验版后确认小地图在普通手机/iPad 分栏下不挤压房源标题和微信联系文案。
## 2026-06-26 本轮交接：房东长文本批量拆房源

- 用户确认：房东群发信息里 `禁宠` 属于公开标签，可以展示给客户；但上游电话、微信、中介费、密码锁、红包、朋友圈照片视频和带看协作限制必须只给中介自己看。
- 后端已新增：
  - `POST /api/notes/property-batch/parse`
  - `POST /api/notes/property-batch/create`
- 解析规则已支持一条房东长文本拆多套房源；无图房源也能生成 `property_listing`，后续可补图。
- 生成的房源卡公开结构包含：标题、小区/楼室、单元/房间、户型、租金、公开标签和公开卖点。
- 私密结构写入 `visibilityConfig.privateData/privateTags`，包含上游电话、微信、中介费、密码锁、红包、朋友圈照片视频和带看限制；客户公开页不得展示。
- 小程序 `pages/resource-create` 已接入：粘贴多套房源时先出现“房源批量识别”确认卡，可勾选生成多张房源卡，也可“按普通资料保存”。
- 已补测试 `test_property_batch_parse_and_create_keeps_upstream_private`，覆盖 `禁宠` 公开和上游信息私密隔离。
- 本地已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py -q -k "property_batch_parse_and_create_keeps_upstream_private or manual_note_draft_creates_property_from_pasted_text"` 通过。
  - `.venv312/bin/python -m py_compile backend/app/services/app_service.py backend/app/schemas/notes.py backend/app/api/routes_notes.py` 通过。
  - `node --check miniprogram/services/api.js` 和 `node --check miniprogram/pages/resource-create/index.js` 通过。

## 2026-06-26 本轮交接：房源助手入口改为企业微信「联系我」

- 背景：
  - 用户确认微信客服会话不能置顶，不是目标路径；目标是添加企业微信成员，形成可置顶的长期会话。
- 本轮已改：
  - `miniprogram/app.json` 新增插件：
    - provider：`wx104a1a20c3f81ec2`
    - alias：`contactPlugin`
  - `miniprogram/pages/home/index.json` 新增组件：
    - `cell: plugin://contactPlugin/cell`
  - `miniprogram/pages/home/index.js` 新增配置 ID：
    - `propertyContactPluginId = 3bf7435f594f0d6ca83a9a185ea201e5`
  - 首页 banner 房源助手入口改为官方按钮模式 `<cell plugid="{{propertyContactPluginId}}" styleType="{{2}}" blockStyle="button" />`。
  - 常用入口里的 `openAssistant` 不再调用 `wx.openCustomerServiceChat`，改为滚动到顶部并提示点官方联系入口。
  - 真机反馈按钮被裁切后，banner 助手卡已改为上下结构，官方插件按钮独占一行；常用入口按钮区域也加高居中。
- 已验证：
  - `node --check miniprogram/pages/home/index.js` 通过。
  - `miniprogram/app.json`、`miniprogram/pages/home/index.json` JSON 解析通过。
- 用户/真机待做：
  - 在小程序后台 `设置 -> 第三方服务 -> 添加插件` 搜索并添加插件 ID `wx104a1a20c3f81ec2`。
  - 上传体验版后确认点击首页插件入口进入企业微信成员添加流程，而不是微信客服会话。
  - 如果开发者工具提示插件不可用，先确认小程序后台已添加插件 `wx104a1a20c3f81ec2`，再确认 `cell` 组件路径和插件版本 `1.4.7`。

## 2026-06-26 本轮交接：外部联系人自动回消息测试

- 用户已在企业微信后台添加生产服务器可信 IP：`81.70.84.35`。
- 已用 2026-06-26 08:40 左右个人微信发给企业微信成员的真实会话记录定位到外部联系人 `external_userid`。
- 测试结果：
  - `kf/send_msg` 微信客服发送文本：返回 `48002 api forbidden`，成员好友会话不能复用微信客服发送接口。
  - `media/upload` 上传测试图片：成功返回 `media_id`，说明服务器 IP 可信配置已生效。
  - `externalcontact/add_msg_template` 文本、图片、小程序卡片：均返回 `48002 api forbidden`。
  - `externalcontact/get` 获取客户详情：返回 `48002 api forbidden`。
- 当前结论：
  - 用户已在企业微信后台补充客户联系 API 权限后，`externalcontact/get` 成功，文本发送任务创建成功。
  - `media/uploadimg` 成功并可用于 `externalcontact/add_msg_template` 的图片附件，图片发送任务创建成功。
  - 小程序卡片目前失败：普通临时素材 `media/upload` 返回的 `media_id` 会被 `add_msg_template` 判为 `40007 invalid media_id`；无封面卡片返回 `41006 media_id missing`；永久素材 `material/add_material` 当前返回 `48002 api forbidden`。
- 新结论：
  - 外部联系人客户联系链路已能创建文本和图片发送任务。
  - 小程序卡片还需要补齐可用于 `pic_media_id` 的素材权限或换用可产生合法卡片封面的上传方式。

## 2026-06-26 本轮交接：房源助手 external_userid 绑定

- 当前方案：
  - 首次使用房源助手时，企业微信/会话存档消息先生成“新导入资料”。
  - 用户打开小程序并登录，用“新导入资料”页点击“认领并绑定”。
  - 后端在 `wecom_identity_bindings` 保存 `externalUserId -> ownerUserId/ownerOpenid`。
  - 之后同一个企业微信外部联系人发来的资料，会自动进入该小程序账号，不再走待认领。
- 已有后端链路：
  - `AppService.claim_import` 会调用 `_save_wecom_identity_binding`。
  - `_resolve_owner_user_id_for_external` 会按 `wecom_external_user + external_userid` 查绑定，并优先用 `ownerOpenid` 找用户。
  - 会话存档处理和 mock sync 已有测试覆盖自动归属。
- 本轮小程序优化：
  - `miniprogram/pages/imports/index.wxml` 文案改为“第一次认领会绑定房源助手，后续发来的资料自动进你的账号”。
  - 认领按钮改为“认领并绑定”。
  - 未登录时点击认领跳转登录页。
  - 认领成功提示“已绑定房源助手”后进入编辑页。
  - `miniprogram/pages/imports/index.wxss` 的模板按钮和认领按钮改为 flex 居中。
- 用户真机流程：
  - 个人微信先添加企业微信成员“高士腾/房源助手”。
  - 给这个企微发一条房源文本或图片。
  - 等后台会话存档处理生成导入后，打开小程序首页进入“资料/待认领/新导入资料”。
  - 点击该条资料的“认领并绑定”。
  - 之后再发给同一个企微成员的房源，会自动归属到当前小程序账号。
- 验证：
  - `node --check miniprogram/pages/imports/index.js` 通过。
  - `python3 -m py_compile backend/app/services/app_service.py backend/tests/test_app.py` 通过。
  - 当前本机缺少 `pytest`，未能运行完整目标 pytest；后续可在有依赖的 `.venv312` 或服务器测试环境补跑。

## 2026-06-26 本轮交接：点击小程序链接自动绑定

- 用户反馈：
  - 竞品体验是：用户把链接/视频发给企业微信，企业微信处理完成后自动回小程序链接，用户点击即可进入下载/结果页；不是让用户去待认领列表找。
- 本轮新增主链路：
  - 后端可调用 `AppService.build_import_claim_link(import_id)` 生成：
    - `token`
    - `pagePath = pages/import-claim/index?token=...`
    - `title`
  - 小程序新增 `pages/import-claim/index`。
  - 用户点击企业微信发回的小程序链接后：
    - 未登录：跳登录页。
    - 登录后：自动调用 `POST /api/imports/claim-by-token`。
    - 后端：认领导入、写入 `external_userid -> ownerUserId/ownerOpenid` 绑定。
    - 小程序：进入资料编辑页。
- 安全规则：
  - token 使用 HMAC 签名并带 7 天过期时间。
  - 已被其他账号认领的导入不能被再次抢绑。
  - 待认领仍保留为兜底，不作为第一推荐用户路径。
- 仍待接入：
  - 企业微信处理完成后的“回小程序卡片/链接”发送动作还需要接到通知发送服务上。
  - 发送内容应使用 `pagePath`，而不是让用户自己进入“新导入资料”。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_app.py::test_claim_import_and_publish_flow backend/tests/test_app.py::test_claim_import_by_token_binds_external_user backend/tests/test_app.py::test_wecom_archive_process_auto_assigns_bound_external_user backend/tests/test_app.py::test_wecom_identity_mapping_resolves_owner_by_openid`：4 passed。
  - `node --check miniprogram/pages/import-claim/index.js`、`node --check miniprogram/pages/imports/index.js`、`node --check miniprogram/services/api.js`：通过。

## 2026-06-26 本轮交接：会话存档完成通知补齐与发送阻塞

- 用户 10:19 左右给企业微信成员发送微信笔记后，没有收到小程序回传链接。
- 线上排查确认：
  - 会话存档已收到 `note` 消息。
  - 已生成导入批次 `import_0437e0a14e`。
  - 已生成资料 `note_de667374ee`。
  - 当时没有生成 `ImportNotification`，所以不会发送完成提示。
- 本轮已补：
  - archive 处理成功后自动创建完成通知。
  - 完成通知的结果入口使用 `pages/import-claim/index?token=...`，用户点击后可登录、认领、绑定并进入结果。
  - archive worker 支持处理后立即调用通知发送器。
  - 新增后台补发接口 `POST /api/wecom/notifications/send-pending`。
  - 已热更新生产后端。
- 生产验证结果：
  - 对 10:19 历史导入手动创建通知后，调用补发接口可进入发送流程。
  - 企业微信返回 `48002 api forbidden`，错误内容为 `send customer service text failed`。
- 当前结论：
  - 资料生成和通知生成已经补齐。
  - 主动回消息仍阻塞在发送通道：当前代码使用微信客服 `kf/send_msg`，不能给企业微信成员外部联系人会话直接发消息。
- 下一步建议：
  - 不再把 `kf/send_msg` 当成企业微信成员好友私聊通道。
  - 继续验证客户联系可用触达方式：欢迎语、客户群发、群发任务、客户群触达，或改为小程序内消息中心/首页待认领作为稳定反馈。
  - 如果要继续做“整理完成自动私聊回卡片”，必须先找到企业微信允许服务端对该外部联系人发送的正式接口，并确认文本、图片、小程序卡片三类能力。

## 2026-06-26 本轮交接：微信客服 API 权限已通，仍需用户从客服入口发消息

- 用户补齐微信客服「可调用接口的应用」后，生产验证结果：
  - `kf/account/list` 成功。
  - 返回可用客服账号 `wkCSe7EwAAtY1p65p2bXVj3gTbWWzcKg`。
  - 生产 `backend/.env` 已更新 `WECOM_OPEN_KFID` 为该值。
  - 后端容器已 force recreate 以刷新环境变量，公网 `/api/wecom/customer-service-config` 返回新客服号。
  - `POST /api/wecom/real-sync` 成功，不再 `48002`。
- 重要踩坑：
  - force recreate 后旧镜像丢失了之前容器内热补丁，本轮已重新 `docker cp` 同步 `routes_wecom.py`、`routes_imports.py`、`dependencies.py`、`schemas/imports.py`、`services/app_service.py`、`services/wecom_archive_worker.py`、`services/wecom_client.py`、`models/domain.py`、`services/repository.py`、`core/schema.sql`、`services/import_notification_service.py`、`schemas/notes.py`、`api/routes_notes.py` 等文件，后端已恢复。
  - 后续应尽快做正式镜像构建，避免再次因重建容器丢热补丁。
- 16:38 用户测试：
  - 用户说 16:38 发了一段文字房源。
  - `real-sync` 没有拉到新客服消息。
  - 日志显示收到的是 `/api/wecom/archive/callback`。
  - archive 表最新消息 `wecom_archive_msg_d463844610`，`msgType=text`，已生成 `note_23816dbffa`。
  - 对该外部联系人调用 `kf/send_msg` 返回 `95018 session status invalid`。
- 当前结论：
  - 微信客服官方链路权限已经打通。
  - 16:38 这条不是从微信客服入口发的，而是发给了企业微信成员好友，所以进入会话存档。
  - 下一次必须从小程序的 `wx.openCustomerServiceChat` 入口打开微信客服会话后发送房源，才能测试 `sync_msg -> 整理 -> kf/send_msg 回小程序卡片`。

## 2026-06-26 本轮交接：首页入口改为微信客服主链路

- 用户确认产品逻辑：
  - 前端交互看起来像“发给企业微信助手”。
  - 底层收发消息必须走微信客服 API。
  - 会话里再引导添加企业微信好友，形成长期联系。
- 小程序已改：
  - 首页 banner 主卡标题从“添加房源助手”改为“发房源给助手”。
  - 主按钮“立即发送房源”调用 `openPropertyAssistant`，也就是 `wx.openCustomerServiceChat`。
  - banner 内「联系我」插件降级为二级入口，文案“长期联系可再添加企业微信”。
  - 常用入口的“发房源给助手”点击卡片本身直接打开微信客服。
  - 常用入口内保留“加企业微信”插件按钮，作为转化入口。
- 验证：
  - `node --check miniprogram/pages/home/index.js` 通过。
  - `node --check miniprogram/utils/workspace-mode.js` 通过。
  - JSON 解析和 WXML 标签计数通过。
- 下一步：
  - 用户上传体验版。
  - 真机点“立即发送房源”，确认打开的是微信客服会话。
  - 在该会话发房源后，再触发 `POST /api/wecom/real-sync` 验证 `kf/sync_msg` 是否收到。

## 2026-06-27 本轮交接：成交雷达真机数据与手机按钮遮挡

- 用户反馈：
  - 6:44 左右用两个微信测试客户访问，但客户看板没有看到雷达生成。
  - 手机端房源详情页“好友/朋友圈”浮层遮挡“生成同款”区域，质疑全局样式为何没有一次解决。
- 线上只读排查：
  - 生产服务运行在 `teambuy-backend-1`，PostgreSQL 正常。
  - 生产数据库已记录北京时间 6:45-6:46 的事件：
    - `showcase_3f537b64ed` 被 `user_5fd8d56c26` 打开。
    - 同一用户点击了多套房源，包括 `note_d00ca2b3bd`、`note_730305fd2e`、`note_ea2607e9d8`。
  - 生产 `/api/dashboard/business?ownerUserId=user_25ec00a0f0&requesterUserId=user_25ec00a0f0&mode=property` 仍返回旧版 `data.summary/recentVisitors/topNotes/visitorProfiles` 结构。
  - 生产接口没有返回 `opportunitySummary`、`opportunityAlerts`、`radarProfiles`、`contentInsights`、`revivalAlerts`。
- 结论：
  - 这次真机测试数据进后台了，但生产后端未部署本地新增的成交雷达规则，因此没有生成雷达提醒。
  - 线上事件也没有 `durationSeconds/maxScrollPercent/focusSections`，说明新版小程序行为上报尚未在真机体验版生效。
- 本轮本地修复：
  - `miniprogram/pages/note-preview/index.wxss`：手机端默认把分享按钮改成行内，不再固定悬浮遮挡业务卡片。
  - `property-same-card` 手机默认单列，大屏再变双列；`生成同款`按钮继续 flex 居中。
- 已验证：
  - `node --check miniprogram/pages/note-preview/index.js` 通过。
  - `git diff --check -- miniprogram/pages/note-preview/index.wxss` 通过。
- 下一步建议：
  - 先正式部署后端成交雷达代码。
  - 再上传小程序体验版。
  - 真机用非 owner 微信打开推荐包，停留、滚动、查看价格/联系方式并触发一次咨询/预约，回 owner 客户看板验证 `客户雷达提醒 + 复制话术 + 资料优化建议`。

## 2026-06-27 本轮交接：首页与客户雷达 UI 收敛

- 用户反馈：
  - 首页做了多轮后功能都有，但视觉不好，客户相关展示位置太多，用户不知道看哪个。
  - 希望先按效果图方向重构首页和雷达页。
- 新增文档：
  - `docs/stage2-docs/22-home-radar-ui-consolidation.md`
- 本轮已改：
  - `miniprogram/pages/home/index`：
    - 首页改为“今日成交机会”优先。
    - 保留“把房源发给助手”工作入口。
    - 新增“客户雷达”统一入口。
    - 移除旧首页分散的客户动态/反馈模块。
  - `miniprogram/pages/visits/index`：
    - 从旧“反馈”页重构为“客户雷达”页。
    - 三个 tab：待跟进、访客、资料优化。
    - 待跟进卡展示意向、原因、标签、建议动作、复制话术、标记已联系。
    - 资料优化页展示建议，并预留生成对比建议。
  - `miniprogram/app.json`：
    - 底部 tab 文案从“反馈”改为“雷达”。
- 重要产品决策：
  - 客户相关主入口以后统一叫“雷达”。
  - 首页不再堆多个客户入口，只展示今日机会摘要和雷达入口。
  - 旧客户看板 `pages/business-dashboard` 继续作为明细/兼容页，不再作为首页主心智。
- 已验证：
  - `node --check miniprogram/pages/home/index.js && node --check miniprogram/pages/visits/index.js && node --check miniprogram/pages/business-dashboard/index.js` 通过。
  - `miniprogram/app.json`、首页/雷达页 JSON 解析通过。
  - 首页、雷达页、客户看板 WXML 标签检查通过。
  - 本轮关键文件 `git diff --check` 通过。
- 待真机：
  - 微信开发者工具重新编译后检查首页首屏是否不再拥挤。
  - 检查底部 tab 是否显示“雷达”。
  - 手机和 iPad 检查首页机会卡、雷达入口、雷达客户卡按钮是否居中且不挤压。

## 2026-06-27 本轮交接：其他工作台释放与 UI 文案切换

- 用户判断：
  - 产品对外先做房源，但销售场景很多，用户的客户未来也可能成为用户。
  - 需要把服务、团购、日常资料三个工作台逻辑梳理清楚，符合基座 + 插件架构。
- 本轮已改：
  - `miniprogram/utils/workspace-mode.js`
    - 取消 `PROPERTY_GROWTH_MODE_ENABLED` 强制锁房源。
    - 默认工作台仍为 `property`。
  - `miniprogram/pages/home/index.js/wxml`
    - 新增 `HOME_UI_BY_MODE`。
    - 首页 banner、主动作、空状态和最热指标按 `property/service/groupbuy/notes` 切换。
    - banner 增加轻量工作台切换 chip。
    - 服务/团购也尝试拉取对应 `business-dashboard`。
  - `miniprogram/pages/visits/index.js/wxml`
    - 雷达页按工作台切换资料名词、空状态和对比建议。
    - 房源显示房源对比合集；团购显示商品对比；服务显示服务方案对比；日常显示资料合集。
- 当前代码可继续优化点：
  - `isPropertyCard/isGroupbuyCard/isServiceCard` 在首页、雷达、资料库等页面重复，建议抽到统一 `workspace-mode` 或 `note-display` 工具。
  - 首页与雷达页已有工作台文案配置，但 `business-dashboard` 明细页仍有较多房源文案，需要继续统一。
  - 服务/团购的雷达规则可复用后端机会引擎，但后续应补场景化标签：服务看案例/报价/保障，团购看规格/价格/取货/接龙。
- 已验证：
  - `node --check miniprogram/pages/home/index.js && node --check miniprogram/pages/visits/index.js && node --check miniprogram/utils/workspace-mode.js` 通过。
  - 首页和雷达页 WXML 标签检查通过。
  - 本轮关键文件 `git diff --check` 通过。

## 2026-06-27 本轮交接：首页白屏排查

- 用户反馈：
  - 首页打不开、白屏，怀疑是否有些请求走服务器、有些走本地后端。
- 排查结果：
  - 当前小程序接口基址统一为 `https://teambuy.lifelove.top`，未发现首页链路混入 `localhost/127.0.0.1`。
  - 线上接口可达，不存在用户返回 404，说明不是网关不可达。
  - 首页 JS 可模拟加载；主要风险在 WXML 复杂表达式和旧本地 mock 登录态。
- 本轮已改：
  - `miniprogram/pages/home/index.wxml`：移除 `||`、三元和相等判断表达式。
  - `miniprogram/pages/home/index.js`：新增 `modeSwitchLabel`，机会卡和统计卡 class 由 JS 预处理。
  - `miniprogram/utils/workspace-mode.js`：工作台选项增加 `activeClass`。
  - 首页未登录跳转登录页时携带 `returnUrl`。
- 已验证：
  - `node --check miniprogram/pages/home/index.js && node --check miniprogram/utils/workspace-mode.js && node --check miniprogram/pages/login/index.js && node --check miniprogram/app.js` 通过。
  - 首页、登录页、雷达页 WXML 标签检查通过。
  - 首页 WXML 表达式检查通过。
- 真机建议：
  - 微信开发者工具先清缓存并重新编译。
  - 如果之前用本地 mock 登录，线上模式会清掉该登录态，需要重新微信登录。
  - 若仍白屏，优先看开发者工具 Console 第一条报错。

## 2026-06-27 本轮交接：商机/合作信息能力

- 用户输入：
  - 提供真实群消息样例：保险出单、海参工厂批发、城市群管理员招募、进口清关代理。
- 产品判断：
  - 这些不是普通服务方案，而是“商机/合作信息”。
  - 暂不新增第五个工作台，归入服务工作台。
- 本轮已改：
  - `docs/stage2-docs/23-business-opportunity-service-card.md`：新增开发文档。
  - `miniprogram/utils/sales-page-templates.js`：新增 `service_business_opportunity` 模板。
  - `miniprogram/pages/service-offer-studio/index.js/wxss`：商机模板指标和主题色。
  - `miniprogram/pages/home/index.js`：服务模式入口改为“做服务/商机页”，并可跳转商机模板。
  - `miniprogram/utils/workspace-mode.js`：服务工作台新增“商机合作”快捷入口。
  - `backend/app/services/skill_router_service.py`：新增商机/合作规则识别，高置信自动生成 `service_offer + service_business_opportunity`。
- 样例验证：
  - 保险出单 -> `service_offer / service_business_opportunity`。
  - 海参工厂批发 -> `service_offer / service_business_opportunity`。
  - 城市群管理员招募 -> `service_offer / service_business_opportunity`。
  - 进口清关代理 -> `service_offer / service_business_opportunity`。
- 已验证：
  - 前端相关 JS `node --check` 通过。
  - 后端 `skill_router_service.py` 编译通过。
  - 服务方案页和首页 WXML 标签检查通过。
  - 本轮关键文件 `git diff --check` 通过。
- 待真机：
  - 服务工作台点击“商机合作”，检查模板卡和预览是否出现。
  - 把四条样例通过导入/手动粘贴走一遍，确认进入服务工作台并能打开客户页。

## 2026-06-27 本轮交接：我的页工作台文案

- 用户反馈：
  - “我的页 / 常用工作台”只看到服务，没有看到商机合作。
  - “只影响首页和工作台展示，不会删除资料”应简化。
  - “工作台”是否指雷达不清晰。
- 本轮已改：
  - `miniprogram/utils/workspace-mode.js`：服务工作台显示为“服务/商机工作台”，短名“服务商机”，图标“商机”。
  - `miniprogram/pages/profile/index.wxml`：说明改为“只影响首页和雷达展示。”。
  - `miniprogram/pages/profile/index.wxml`：按钮改为“去雷达”。
  - 我的页选中态改用 `activeClass`，减少 WXML 判断。
- 已验证：
  - `node --check miniprogram/utils/workspace-mode.js && node --check miniprogram/pages/profile/index.js` 通过。
  - 我的页和首页 WXML 标签检查通过。

## 2026-06-27 本轮交接：首页 V2 资料机会雷达

- 用户确认：
  - 产品主心智升级为“资料发出去，机会看得见”。
  - 不希望产品被房源完全框死，但房源仍作为默认推广尖刀。
  - 四工作台不能做成首页四个大按钮，避免退回功能超市。
  - 雷达 banner 是强心智资产，需要继续放在首页首屏。
- 新增文档：
  - `docs/stage2-docs/24-home-opportunity-radar-generalized.md`
- 本轮已改：
  - `miniprogram/pages/home/index.wxml`
    - banner 改为“资料机会雷达 / 资料发出去，机会看得见”。
    - 场景切换收敛为“当前：房源场景”轻胶囊。
    - 今日机会独立成数据面板。
    - 移除单独“客户雷达”大卡。
    - “最近成果”改为“最近有反馈的资料”。
  - `miniprogram/pages/home/index.js`
    - 今日机会指标统一为高意向、新打开、待跟进、待处理。
    - 待处理/待跟进按场景进入对应雷达或订单处理入口。
  - `miniprogram/pages/home/index.wxss`
    - 新增今日机会面板样式。
    - 优化场景胶囊、banner 文案和底部弱切换入口。
- 已验证：
  - 首页 JS 语法检查通过。
  - 首页 WXML 标签检查通过。
  - 首页 WXML 复杂表达式检查通过。
  - 首页模拟加载通过，默认房源场景和四个今日机会指标正常。
  - 本轮关键文件 `git diff --check` 通过。
- 待真机确认：
  - 微信开发者工具重新编译首页，确认 banner 雷达图、今日机会卡、房源助手第一动作与参考方向一致。
  - 在 iPhone 和 iPad 检查“当前：房源场景”胶囊、今日机会数字和底部弱入口不挤压。

## 2026-06-27 本轮交接：首页 banner 视觉资产化

- 用户反馈：
  - 效果图很漂亮，但前端 DOM 版本差点意思。
  - 询问 banner 是否为 DOM，并建议实在不行直接使用效果图。
- 本轮已改：
  - `miniprogram/static/workspace/home-opportunity-radar-banner.png`
    - 从用户确认的整页效果图中裁出首页 banner 卡片。
  - `miniprogram/pages/home/index.wxml`
    - 首页 banner 改为本地图片资产。
    - 保留场景切换点击热区。
  - `miniprogram/pages/home/index.wxss`
    - 移除 banner 外层重复边框、背景和阴影。
    - 图片宽度自适应首页内容区。
- 注意：
  - banner 内的“当前：房源场景”是图片文案，不再随工作台动态变化。
  - 这是为了优先拿到高质感首屏；若后续要动态支持商品/服务/资料，需要补多张 banner 或重做高保真 DOM。
- 已验证：
  - 首页 JS 语法检查通过。
  - 首页 WXML 标签检查通过。
  - 首页 WXML 复杂表达式检查通过。
  - banner 图片资源存在，关键文件 `git diff --check` 通过。

## 2026-06-27 本轮交接：资料、合集、雷达销售助理闭环 V1

- 用户确认：
  - 底部 tab 继续叫“资料”。
  - 资料和合集都要能发客户，不是所有资料都必须先做合集。
  - 主按钮更适合叫“发客户”，而不是普通“分享”。
  - 发客户后的状态追踪可以用浅色条和文字呈现。
- 新增文档：
  - `docs/stage2-docs/25-material-collection-radar-sales-assistant-loop.md`
- 本轮已改：
  - `miniprogram/utils/dashboard.js`
    - `enrichCard` 新增 `deliveryStatus`。
    - 根据打开、访客、客户动作输出等待客户打开、客户已打开、客户重复查看、建议跟进。
  - `miniprogram/pages/library/index.wxml/wxss`
    - 顶部定位改为“管理单条资料，直接发客户”。
    - 资料卡新增状态追踪条。
    - 主按钮改为“发客户”，客户入口改为“去雷达”。
  - `miniprogram/pages/showcases/index.js/wxml/wxss`
    - 顶部定位改为“把多条资料打包发客户”。
    - 合集卡新增状态追踪条。
    - 已发布合集主按钮改为“发客户”。
  - `miniprogram/pages/visits/index.wxml/wxss`
    - 顶部新增“看客户反馈和跟进建议”的定位卡。
- 已验证：
  - 相关 JS 语法检查通过。
  - 资料、合集、雷达 WXML 标签检查通过。
  - 状态规则模拟通过。
  - 本轮关键文件 `git diff --check` 通过。
- 待真机确认：
  - 资料页不同卡片状态条是否换行自然。
  - 合集页“发客户/更多”按钮是否在手机和 iPad 上不挤压。
  - 雷达页新增定位卡是否不会把首屏内容推得太低。

## 2026-06-27 本轮交接：资料/合集/雷达 P1 酷功能规则版

- 用户要求：
  - 先把 P1 收口，全部实现。
- 本轮已改：
  - `miniprogram/utils/dashboard.js`
    - `enrichCard` 新增 `salesCheck`。
    - 发前体检规则：缺联系方式、价格不清、缺图片、标题偏短、体检通过。
  - `miniprogram/pages/library/index.js/wxml/wxss`
    - 新增发客户状态筛选：全部、待整理、已发客户、有反馈。
    - 资料卡显示发前体检提示。
  - `miniprogram/pages/showcases/index.js/wxml/wxss`
    - 新增合集状态筛选：全部、草稿、已发布、有反馈。
  - `miniprogram/pages/visits/index.js/wxml`
    - 雷达画像标签增强。
    - 下一句话建议按标签生成。
    - 客户卡新增“打开来源”。
    - 资料优化建议可跳回来源资料。
    - 生成对比合集建议跳到合集创建页。
  - `miniprogram/pages/showcase-edit/index.js/wxml/wxss`
    - 支持 `method=radar_compare`。
    - 新增“来自雷达建议”提示卡。
  - `docs/stage2-docs/25-material-collection-radar-sales-assistant-loop.md`
    - 补充 P1 已落地清单和未完成的 P0 真实发送闭环。
- 已验证：
  - 相关 JS 语法检查通过。
  - 资料、合集、雷达、合集创建页 WXML 标签检查通过。
  - 发前体检和状态规则模拟通过。
  - 本轮关键文件 `git diff --check` 通过。
- 重要边界：
  - 这是规则版 P1，可见体验已收口。
  - 真实 P0 发送闭环已在下一段补齐；如果继续增强，可再做独立发送记录表和按 shareId 下钻。

## 2026-06-27 本轮交接：P0 发客户真实链路

- 用户要求：
  - P0 也要全部实现，继续收口。
- 本轮已改：
  - `backend/app/schemas/cards.py`
    - `RecordViewRequest` 增加 `eventType/shareId/shareFromUserId/scene/referrer`。
  - `backend/app/models/domain.py`
    - `ViewType` 增加 `share`。
    - `ViewEvent` 增加分享归因字段。
  - `backend/app/core/schema.sql`
    - `view_events` 增加 `share_id/share_from_user_id/scene/referrer`。
    - 新增 `idx_view_events_share`。
  - `backend/app/services/repository.py`
    - Postgres 映射和索引补充 view_events 分享字段。
  - `backend/app/services/app_service.py`
    - `record_note_view/record_view` 支持 `eventType=share`。
    - share 事件不计入 PV/UV，只计 `shareCount/latestShareAt/topShareId`。
    - owner 自己打开资料不入库。
    - owner 自己打开合集事件返回 `recorded:false`。
  - `miniprogram/pages/library/index.js/wxml/wxss`
    - 资料卡“发客户”变成真实 `open-type=share`。
    - 分享时生成 `shareId`，记录 share 事件，并带归因参数。
  - `miniprogram/pages/note-preview/index.js`
    - 接收 `sid/from/src/ref`。
    - 记录客户打开时回传分享归因。
    - 预览页再次分享也生成新 `shareId` 并记录。
  - `miniprogram/utils/dashboard.js`
    - `normalizeStats` 保留 `shareCount/latestShareAt/topShareId`。
    - `deliveryStatus` 支持“已发出，等待打开”。
- 已验证：
  - `.venv312/bin/python -m pytest backend/tests/test_postgres_repository_schema.py backend/tests/test_app.py::test_note_preview_view_updates_note_list_stats -q` 通过。
  - `.venv312/bin/python -m pytest backend/tests/test_app.py::test_showcase_builder_create_publish_public_and_archive -q` 通过。
  - 后端关键文件编译通过。
  - 前端相关 JS 检查通过。
  - 资料页、资料预览页、合集页、合集公开页 WXML 标签检查通过。
- 待真机：
  - 在资料页点“发客户”，确认直接弹微信分享。
  - 客户微信打开后，资料状态从“已发出，等待打开”变为“客户已打开”。
  - 发布者自己打开分享链接不应让 PV/UV 增加。
  - 合集 owner 自己打开不应进入访客统计。

## 2026-06-27 本轮交接：P0/P1 分享体验与裂变补强

- 用户要求：
  - P0/P1 一起补强。
  - 每个资源或合集下面的转发卡片下方加入“生成同款”等营销语句，方便裂变。
- 本轮已改：
  - `miniprogram/pages/library/index.js/wxml/wxss`
    - 点“发客户”后即时把本地卡片状态改为“已发出，等待打开”。
    - 卡片下方新增裂变提示。
    - “发前体检”提示可点击直达编辑。
    - 分享标题改为“xxx｜点开查看完整资料”。
  - `miniprogram/pages/showcases/index.js/wxml/wxss`
    - 点“发客户”后即时把本地合集状态改为“已发出，等待打开”。
    - 合集卡下方新增裂变提示。
    - 分享标题改为“xxx｜点开查看完整资料”。
  - `miniprogram/pages/note-preview/index.js`
    - 资料公开页二次转发标题改为客户友好口径。
  - `miniprogram/pages/showcase-view/index.js`
    - 合集公开页二次转发标题改为客户友好口径。
  - `docs/stage2-docs/25-material-collection-radar-sales-assistant-loop.md`
    - P0/P1 补强完成项已更新。
- 已验证：
  - 相关 JS 语法检查通过。
  - 资料页、合集页 WXML 标签检查通过。
  - 本轮关键文件 `git diff --check` 通过。
- 待真机：
  - 点资料/合集“发客户”后，确认微信分享面板出现，同时列表状态即时变化。
  - 检查裂变提示在手机和 iPad 上不挤压主按钮。
  - 客户打开公开页后，继续确认底部“生成同款”入口可见但不干扰看资料。

## 2026-06-27 本轮交接：小程序图片转 WebP 降包体

- 用户要求：
  - 前端图片改成 WebP，因为小程序前端超过 2MB，当前测试不了。
- 本轮已改：
  - `miniprogram/pages/home/index.wxml`
    - 首页雷达 banner 改用 `/static/workspace/home-opportunity-radar-banner.webp`。
  - `miniprogram/pages/login/index.wxml`
    - 登录页图片改用 `/static/workspace/login-room.webp`。
  - `miniprogram/utils/workspace-mode.js`
    - 四个工作台场景图改为 WebP。
  - `miniprogram/static/workspace/`
    - 新增 6 张 WebP。
    - 删除已替换的 PNG/JPG 原图。
  - 清理：
    - 删除 `miniprogram/.DS_Store`。
    - 删除 `miniprogram/static/.DS_Store`。
- 体积结果：
  - `miniprogram` 真实文件字节约 `1,697,712 bytes`。
  - 图片真实字节约 `149,917 bytes`。
  - `du` 显示约 `2172 KB block usage`，这是磁盘块占用，不等同微信上传体积。
- 已验证：
  - 旧 PNG/JPG 路径引用检查为空。
  - `node --check miniprogram/utils/workspace-mode.js` 通过。
  - 首页、登录页 WXML 标签检查通过。
- 待真机：
  - 首页 WebP banner 是否正常显示。
  - 登录页图片是否正常显示。
  - 四个场景图是否正常显示。

## 2026-06-27 本轮交接：前四个 tab 闭环感补强

- 用户要求：
  - 继续先打磨，不部署。
  - 先把前 4 个 tab 中“更稳、更酷”的点加上。
- 本轮已改：
  - `miniprogram/pages/home/index.js`
    - 新增雷达目标 tab 缓存。
    - 日常资料场景下，今日机会数字可进入雷达对应 tab。
  - `miniprogram/pages/visits/index.js`
    - 读取首页带来的雷达目标 tab。
    - 客户卡新增 `nextStep` 规则。
  - `miniprogram/pages/visits/index.wxml/wxss`
    - 客户卡展示“下一步动作”。
  - `miniprogram/utils/dashboard.js`
    - `enrichCard` 新增 `materialStage`。
  - `miniprogram/pages/library/index.wxml/wxss`
    - 资料卡新增阶段提示：待补强、可发送、已发出、已打开、建议跟进。
  - `miniprogram/pages/showcases/index.js/wxml/wxss`
    - 合集卡新增用途标签：推荐包、对比包、商品包、方案包、资料包、复访包。
- 已验证：
  - 相关 JS 语法检查通过。
  - 首页、资料、合集、雷达 WXML 标签检查通过。
  - 本轮关键文件 `git diff --check` 通过。
- 待真机：
  - 资料卡新增阶段条是否挤压内容。
  - 合集用途标签在小屏是否换行自然。
  - 雷达客户卡“下一步动作”是否让首屏过长。

## 2026-06-27 本轮交接：资料/合集卡片高度与雷达动作位置修正

- 用户反馈：
  - 资料和合集卡片不是字太长，而是卡片太高、不美观。
  - 用户追问“下一步动作”最合理应该在哪个页面。
- 本轮已改：
  - `miniprogram/pages/library/index.wxml/wxss`
    - 列表封面降为 `132rpx`，整体间距和按钮高度收紧。
    - 无客户动态时不再展示空胶囊。
    - 三条状态说明合并成一行状态胶囊。
    - 裂变提示缩短为“可追踪反馈 · 支持生成同款”。
  - `miniprogram/pages/showcases/index.wxml/wxss`
    - 封面降为 `96rpx`，右侧操作区收窄。
    - 用途标签和发客户状态合并成一行状态胶囊。
    - 裂变提示单行省略。
  - `miniprogram/pages/visits/index.wxml/wxss`
    - 撤回资料优化 tab 的“下一步动作”模块。
    - 产品判断：客户“下一步动作”属于待跟进页；资料优化页只保留资料修补和生成对比合集建议。
- 已验证：
  - 相关 JS 语法检查通过。
  - 资料、合集、雷达 WXML 标签检查通过。
  - 本轮关键文件 `git diff --check` 通过。
- 待真机：
  - 资料卡、合集卡高度是否更协调。
  - 待跟进客户卡里的“下一步动作”是否清晰。

## 2026-06-27 本轮交接：资料卡二次压缩、合集顶部紧凑化、待跟进动作按钮化

- 用户反馈：
  - 要坚持产品判断，不要只是顺着用户说。
  - “下一步动作”应该在雷达待跟进页，需要补得更明显。
  - 资料卡仍偏高。
  - 合集顶部 banner 过高，需要重新设计。
- 本轮已改：
  - `miniprogram/pages/library/index.wxml/wxss`
    - 封面降到 `112rpx`。
    - 创建时间并入统计行。
    - 状态胶囊减少为阶段和发前问题。
    - 裂变提示仅在已发出、已打开或有客户动态时显示。
    - 按钮高度继续收紧。
  - `miniprogram/pages/showcases/index.wxss`
    - 顶部大 banner 改为紧凑工具卡。
    - 方向入口改成一行紧凑卡。
    - 顶部按钮和说明文字尺寸收紧。
  - `miniprogram/pages/visits/index.js/wxml/wxss`
    - 待跟进客户卡里的“下一步动作”改为可点击按钮。
    - 对比/合集动作跳生成对比合集，其余动作复制话术。
- 产品判断：
  - 客户“下一步动作”只放在雷达待跟进页。
  - 资料优化页不叫下一步动作，只放资料优化和生成对比合集建议。
- 已验证：
  - 相关 JS 语法检查通过。
  - 资料、合集、雷达 WXML 标签检查通过。
  - 本轮关键文件 `git diff --check` 通过。

## 2026-06-27 本轮交接：资料缩略图权重恢复与合集无效 banner 删除

- 用户反馈：
  - 资料缩略图太小、太靠上靠左，需要居中。
  - 资料卡右侧按钮区有多余空白。
  - 合集顶部 banner 没有作用就不要保留。
- 本轮已改：
  - `miniprogram/pages/library/index.wxss`
    - 列表缩略图调整为 `144rpx`。
    - 列表卡改为垂直居中。
    - 无客户动态时操作按钮改为两列，消除右侧空白。
  - `miniprogram/pages/showcases/index.wxml/wxss`
    - 删除顶部大 banner。
    - 删除方向卡。
    - 改为轻量操作栏：场景标签、合集类型、新建按钮。
- 产品判断：
  - 合集页是工具页，不应该用无效 banner 占首屏。
  - 资料缩略图需要保留识别价值，不能为了压高度压得过小。
- 已验证：
  - 相关 JS 语法检查通过。
  - 资料、合集 WXML 标签检查通过。
  - 本轮关键文件 `git diff --check` 通过。

## 2026-06-27 本轮交接：资料搜索按钮收窄与合集 Hero 样式统一

- 用户反馈：
  - 资料搜索按钮背景太长。
  - 合集顶部如果没有更好方案，就跟资料页 banner 一样。
- 本轮已改：
  - `miniprogram/pages/library/index.wxss`
    - 搜索按钮改为自适应宽度，保留 `min-width` 和左右内边距。
  - `miniprogram/pages/showcases/index.wxml/wxss`
    - 合集顶部改为和资料页一致的 hero 样式。
    - 下方保留轻操作栏：场景标签和新建按钮。
- 已验证：
  - 相关 JS 语法检查通过。
  - 资料、合集 WXML 标签检查通过。
  - 本轮关键文件 `git diff --check` 通过。

## 2026-06-27 本轮交接：合集页按参考效果图重做比例

- 用户反馈：
  - 之前生成的合集页效果图比例比当前前端好很多。
  - 可以按效果图重新做合集页。
- 本轮已改：
  - `miniprogram/pages/showcases/index.wxml`
    - 在自定义导航下新增页面内“合集”视觉锚点。
    - 合集卡状态改成一行胶囊：发布状态、资料数量、用途、发客户状态。
  - `miniprogram/pages/showcases/index.wxss`
    - 页面外边距、Hero 高度、标题、右侧“合”视觉块和主按钮比例按参考图重排。
    - 场景标签和新建合集按钮组成清晰主动作区。
    - 筛选胶囊、列表卡、封面、按钮和标签整体收紧，避免卡片继续显高。
- 产品判断：
  - 合集页可以有任务型 Hero，但必须服务“把多条资料打包发客户”。
  - 客户画像和下一步动作仍留在雷达，不进入合集页。
- 已验证：
  - `node --check miniprogram/pages/showcases/index.js` 通过。
  - 合集 WXML 小程序模板适配检查通过。
  - 本轮关键文件 `git diff --check` 通过。

## 2026-06-27 本轮交接：修正合集页重复标题和空白过大

- 用户反馈：
  - 真机截图中合集页出现多个“合集”，顶部空白过大，视觉上完全不像参考图。
- 本轮已改：
  - `miniprogram/pages/showcases/index.wxml`
    - 删除额外的 `body-title`。
  - `miniprogram/pages/showcases/index.wxss`
    - 收紧 Hero 顶部留白和最小高度。
    - 让文案和右侧“合”视觉块回到首屏主位置。
- 产品判断：
  - 参考图要复刻视觉重心，不应机械复刻大段留白。
  - 自定义导航已有页面标题时，正文不要再重复放同名标题。
- 已验证：
  - `node --check miniprogram/pages/showcases/index.js` 通过。
  - 合集 WXML 小程序模板适配检查通过。
  - 本轮关键文件 `git diff --check` 通过。

## 2026-06-27 本轮交接：修复合集页双导航标题

- 用户反馈：
  - 删除一个“合集”后，真机仍然还有两个“合集”。
- 根因：
  - 合集页 WXML 使用了 `<custom-nav title="合集" />`。
  - 但 `miniprogram/pages/showcases/index.json` 仍未设置 `navigationStyle: "custom"`。
  - 微信原生导航标题和自定义导航标题同时出现。
- 本轮已改：
  - `miniprogram/pages/showcases/index.json` 增加 `navigationStyle: "custom"`。
  - `docs/pitfalls.md` 记录：custom-nav 页面必须同步关闭原生导航。
- 已验证：
  - 合集 JS、JSON、WXML 检查通过。
  - 本轮关键文件 `git diff --check` 通过。

## 2026-06-27 本轮交接：资料与合集卡片风格统一

- 用户要求：
  - 如果合集页比例通过，继续把资料页和合集页卡片风格统一，让两个原材料页面像同一个产品体系。
- 本轮已改：
  - `miniprogram/pages/library/index.wxml`
    - 普通资料卡外露操作收口为“发客户 / 编辑 / 更多”。
    - 更多入口复用已有 `handleMoreCardActions`，保留加入合集、复制文案、删除等低频功能。
  - `miniprogram/pages/library/index.wxss`
    - 资料卡改为与合集卡一致的白底、细边框、`8rpx` 圆角和轻阴影。
    - 资料卡标题、状态胶囊、裂变提示和按钮高度向合集卡靠齐。
    - 保留资料缩略图较大尺寸，避免降低单条资料识别度。
- 产品判断：
  - 统一卡片语言，不强行统一所有尺寸。
  - 资料页重点是识别单条资料，合集页重点是打包发客户。
- 已验证：
  - 资料、合集 JS 检查通过。
  - 资料、合集 WXML/JSON 检查通过。
  - 本轮关键文件 `git diff --check` 通过。

## 2026-06-27 本轮交接：雷达页销售助理 5 项收口

- 用户要求：
  - 直接把 5 个点都做了：待跟进客户卡、下一步动作、客户画像标签、资料/合集联动、助理式空状态。
- 本轮已改：
  - `miniprogram/pages/visits/index.js`
    - 新增来源筛选状态 `teambuy:radarSourceFilter`。
    - 读取资料/合集入口状态后，雷达只展示当前来源相关反馈。
    - 客户标签扩展到价格敏感、位置优先、反复看联系方式、关注保障、需要信任、沉默复活、正在比较、疑似同行、疑似上游等。
    - 待跟进主池过滤疑似同行和疑似上游。
    - 来源筛选时顶部统计按当前来源重算。
  - `miniprogram/pages/visits/index.wxml/wxss`
    - 新增来源筛选条。
    - 待跟进卡改为销售助理结构：看过什么、为什么值得跟、画像标签、下一步动作、复制话术、生成对比、打开来源、标记已联系。
    - 4 个动作按钮使用两列布局，避免挤压变形。
    - 空状态改为助理口吻，并提供生成对比合集入口。
  - `miniprogram/pages/library/index.js`
    - 有客户反馈的资料点击“去雷达”时写入来源筛选并切到雷达。
  - `miniprogram/pages/showcases/index.js`
    - 已发布合集“更多”菜单新增“雷达”入口，带合集来源进入雷达。
- 产品判断：
  - 资料/合集只负责“发出去”和“带来源进雷达”；客户动作统一在雷达处理。
  - 同行和上游不进入待跟进主池，但保留在访客画像里观察。
- 已验证：
  - 资料、合集、雷达 JS 检查通过。
  - 资料、合集、雷达 WXML/JSON 检查通过。
  - 本轮关键文件 `git diff --check` 通过。
## 2026-06-27 本轮交接：前四个 Tab 二次收口

- 用户要求：
  - 首页、资料、合集、雷达整体方向可用，但要继续收口，减少蓝白单调和说明文字，增强 AI 助理感。
- 本轮已改：
  - `miniprogram/pages/home/index.wxml`
    - 首页“今日机会”和“最近有反馈的资料”副文案改短，转成结果导向。
  - `miniprogram/utils/dashboard.js`
    - 未分享资料默认状态从“等待客户打开”改成“待发送”。
  - `miniprogram/pages/library/index.wxml/.wxss`
    - “发客户状态”前置为默认主筛选。
    - 分类、专题、标签收进展开工具区，减轻首屏后台感。
    - 资料卡首屏状态改为优先显示 `deliveryStatus`。
  - `miniprogram/pages/showcases/index.wxml/.wxss`
    - 合集卡把 `purpose` 提前为主信息块，先告诉用户这是推荐包、对比包、商品包、方案包还是资料包。
  - `miniprogram/pages/visits/index.wxml/.wxss/.js`
    - 雷达顶部说明压缩为一句短提示。
    - 客户卡标签改为“AI判断”。
    - 客户卡动作从 4 个并列按钮收口为“复制话术 + 更多”。
    - 雷达统计卡加入暖橙、绿、淡紫等轻状态色，降低蓝白单调感。
- 当前产品口径：
  - 首页：今天先做什么。
  - 资料：单条资料现在处于哪个发送阶段。
  - 合集：这份包适合发给谁。
  - 雷达：谁值得跟、为什么跟、下一句怎么说。
- 已验证：
  - `node --check miniprogram/pages/home/index.js`
  - `node --check miniprogram/pages/library/index.js`
  - `node --check miniprogram/pages/showcases/index.js`
  - `node --check miniprogram/pages/visits/index.js`
  - `node --check miniprogram/utils/dashboard.js`
  - 本轮关键文件 `git diff --check` 通过。
- 待真机确认：
  - 资料页高级筛选折叠后，首屏节奏是否更舒服。
  - 合集用途块是否比之前更容易让用户理解“这个包适合谁”。
  - 雷达统计卡新颜色在手机和 iPad 上是否足够清楚，但不过度花。
  - 雷达卡按钮收口后，用户是否更容易先执行主动作。

## 2026-06-27 本轮交接：无图资料标题封面卡

- 用户问题：
  - 许多微信群转发、展示页和资料卡没有图片，当前只显示“资料 / 房源 / 合集”这类占位字，不够好看也不够好认。
- 本轮已改：
  - 新增 `miniprogram/utils/title-cover.js`
    - 从标题中提取不超过 8 个字的重点内容，生成两行标题封面数据和轻色调。
  - `miniprogram/utils/dashboard.js`
    - 资料卡统一补 `titleCover`，供首页、资料页等无图卡片复用。
  - `miniprogram/pages/library/index.wxml/.wxss`
    - 无图资料卡改为“标签 + 标题重点词”封面卡。
  - `miniprogram/pages/home/index.wxml/.wxss`
    - 首页最近反馈无图卡同步改成标题封面卡。
  - `miniprogram/pages/showcases/index.js/.wxml/.wxss`
    - 无图合集卡改成标题封面卡。
  - `miniprogram/pages/showcase-view/index.js/.wxml/.wxss`
    - 展示页内无图 Hero、无图卡片同步改成标题封面卡。
    - 新增 `showcaseShareCanvas`，无 banner / 无首图时可生成分享封面。
  - `miniprogram/utils/business-card-share.js`
    - 新增 `generateTitleShareImage` 通用标题封面图。
  - `miniprogram/pages/note-preview/index.js`
    - 普通资料无图分享时，优先生成标题封面图。
- 当前边界：
  - 资料库/合集列表直接点“发客户”的分享 `imageUrl` 仍优先走已有图片。
  - 但无图资料和无图合集现在会在点击分享时预生成标题封面图，再作为分享兜底图使用。
- 已验证：
  - `node --check miniprogram/utils/title-cover.js`
  - `node --check miniprogram/utils/dashboard.js`
  - `node --check miniprogram/utils/business-card-share.js`
  - `node --check miniprogram/pages/note-preview/index.js`
  - `node --check miniprogram/pages/showcase-view/index.js`
  - `node --check miniprogram/pages/showcases/index.js`
  - 本轮关键文件 `git diff --check` 通过。

## 2026-06-27 本轮交接：无图分享卡 CTA 再收口

- 用户补充：
  - “我也想做同款”最好只是一行字，字体更小，不要占主要视觉。
- 本轮已改：
  - `miniprogram/utils/business-card-share.js`
    - 通用无图分享卡改成双层 CTA：主按钮固定承接“打开小程序查看完整资料/合集”，底部一行小字承接“我也想做同款”。
  - `miniprogram/pages/note-preview/index.js`
    - 普通资料无图分享图改用“打开小程序查看完整资料”主文案。
  - `miniprogram/pages/showcase-view/index.js`
    - 合集无图分享图改用“打开小程序查看完整合集”主文案。
  - `miniprogram/pages/library/index.{js,wxml,wxss}`
    - 无图资料从列表直接点“发客户”时，会先预生成标题分享图。
  - `miniprogram/pages/showcases/index.{js,wxml,wxss}`
    - 无图合集从列表直接点“发客户”时，会先预生成标题分享图。
- 待真机确认：
  - 分享面板弹起前，预生成无图分享图的速度是否足够稳定。
  - 微信实际分享预览里，小字版“我也想做同款”是否清楚但不抢视觉。
## 2026-06-28 前四个 Tab 真机收边补充

当前新增：

- [docs/qa/前四个Tab视觉收口_真机回归清单.md](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/docs/qa/前四个Tab视觉收口_真机回归清单.md)

说明：

- 这是接在“首页 / 资料 / 合集 / 雷达第一轮视觉落地”和“雷达页 banner 二次收口”之后的补充动作。
- 目的不是继续改代码，而是先把真机验收标准钉住，避免下一轮收口时方向发散。
- 清单已经明确当前保留原则：
  - 首页保留 `banner + 机会面板 + 发给助手`
  - 雷达页保留 `同行过滤` 等业务必要模块
  - 房源卡高密度，非房源卡可继续压缩

建议下一步：

1. 用户上传体验版。
2. 按清单截首屏和关键卡片。
3. 只挑阻断体验的问题修第一轮。

## 2026-06-28 前四个 Tab 第一轮收边已落地

本轮已改：

- [miniprogram/pages/home/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/home/index.wxml)
- [miniprogram/pages/home/index.wxss](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/home/index.wxss)
- [miniprogram/pages/library/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/library/index.wxml)
- [miniprogram/pages/library/index.wxss](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/library/index.wxss)
- [miniprogram/pages/showcases/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/showcases/index.wxml)
- [miniprogram/pages/showcases/index.wxss](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/showcases/index.wxss)

已完成内容：

- 首页首屏文案压短，减少 banner 和助手区的重复说明。
- 资料页顶部说明压缩，并把一段长解释改成两个短胶囊。
- 资料页无图卡改成更明显的“轻设计卡”呈现。
- 资料卡底部重新补强“打开小程序 / 输入同款”传播引导。
- 合集页首屏说明压缩，无图合集卡同步切到轻设计风格。
- 合集卡底部保留“打开小程序看完整合集 / 输入同款继续问 / 导出方案书预留”一行引导。

仍未完成：

- 未做微信开发者工具截图验证。
- 未做真机验收，因此这轮只能算“第一轮视觉落地”，还不是最终收边完成版。

## 2026-06-28 前四个 Tab 统一收边完成到当前阶段

继续补的内容：

- [miniprogram/pages/visits/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/visits/index.wxml)
- [miniprogram/pages/visits/index.wxss](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/visits/index.wxss)
- [miniprogram/pages/home/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/home/index.wxml)
- [miniprogram/pages/home/index.wxss](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/home/index.wxss)

本轮补齐后，前四个 Tab 当前状态：

- 首页、资料、合集、雷达都已统一到同一套轻量工作台风格。
- 首页最近反馈卡、资料卡、合集卡都已有无图轻封面兜底。
- 雷达页 hero、摘要区、跟进卡、建议卡、时间线卡已收成同一层次体系。
- “同行过滤”已在雷达摘要区显式保留。

当前建议：

- 前四个 Tab 可以先视为“收边到可继续推进其他模块”的状态。
- 后续如果还有 UI 微调，优先走真机截图驱动的小修，不再回到大范围重做。

## 2026-06-28 说明字删除补充

- 首页删除了 hero 底部 `AI会按打开、停留、咨询和重点查看自动整理机会`。
- 雷达页删除了 hero 底部 `AI跟进建议 / 会按打开、停留、咨询和重点查看自动更新`。
- 这一轮是纯减法，目标是避免用户看到“系统解释文字”产生不适感。

## 2026-06-28 我的页已按收口版落地

已改文件：

- [miniprogram/pages/profile/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/profile/index.wxml)
- [miniprogram/pages/profile/index.wxss](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/profile/index.wxss)
- [miniprogram/pages/profile/index.js](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/profile/index.js)
- [backend/app/models/domain.py](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/backend/app/models/domain.py)
- [backend/app/schemas/auth.py](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/backend/app/schemas/auth.py)
- [backend/app/services/app_service.py](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/backend/app/services/app_service.py)

当前结构：

- 顶部身份卡：默认头像/昵称、可编辑、微信号/手机号状态
- 资源库：群资源库主入口，行业通讯录与行业资源预留
- 当前使用场景：唯一场景切换入口，绑定“完善资料”
- 我的内容：资料 / 合集 / 消息
- 设置与帮助：个人资料 / 帮助与反馈 / 退出登录

额外说明：

- 个人资料编辑层已补上 `微信号`，不再只有手机号。
- 群资源库和帮助反馈当前先用轻提示占位，后续功能落地时可直接接真实页面。

## 2026-06-28 我的页顶部小收边

- 顶部右侧长条 `编辑` 已改成更小的圆形修改提示。
- 资源库右上角已明确展示 `100 积分`。

## 2026-06-28 五个 Tab 心智与名片入口收口

本轮已改文件：

- [miniprogram/pages/home/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/home/index.wxml)
- [miniprogram/pages/home/index.js](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/home/index.js)
- [miniprogram/pages/library/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/library/index.wxml)
- [miniprogram/pages/library/index.js](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/library/index.js)
- [miniprogram/pages/profile/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/profile/index.wxml)
- [miniprogram/pages/profile/index.js](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/profile/index.js)
- [miniprogram/pages/profile/index.wxss](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/profile/index.wxss)
- [miniprogram/pages/showcases/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/showcases/index.wxml)
- [miniprogram/pages/visits/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/visits/index.wxml)

当前状态：

- 五个 Tab 顶部心智已分别收成：首页 `今天先做什么`、资料 `适合发客户的资料`、合集 `多条资料打包成一页`、雷达 `谁值得跟进`、我的 `资料、资源、消息和个人资料入口`。
- 服务场景下，`我的名片` 已从合集创建逻辑里抽出来：我的页显示独立名片入口，首页服务场景次动作进入名片筛选，资料页支持只看名片和只看服务方案。
- 本轮没有扩会员、PDF、专题、行业通讯录、行业资源。

已验证：

- `node --check miniprogram/pages/home/index.js`
- `node --check miniprogram/pages/library/index.js`
- `node --check miniprogram/pages/profile/index.js`
- `node --check miniprogram/pages/showcases/index.js`
- `node --check miniprogram/pages/visits/index.js`
- `git diff --check` 覆盖本轮修改文件。

仍需人工真机看：

- iPhone 小屏下五个 Tab 首屏是否不堵。
- 日常资料、名片、服务方案三类无图卡是否左侧文字图居中且不压右侧标题。
- 底部 Tabbar 是否挡住列表最后一张卡。

## 2026-06-28 群资源库添加微信群前端 MVP

本轮已改文件：

- [miniprogram/app.json](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/app.json)
- [miniprogram/components/custom-nav/index.js](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/components/custom-nav/index.js)
- [miniprogram/pages/profile/index.js](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/profile/index.js)
- [miniprogram/pages/group-resource-library/index.json](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/group-resource-library/index.json)
- [miniprogram/pages/group-resource-library/index.wxml](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/group-resource-library/index.wxml)
- [miniprogram/pages/group-resource-library/index.js](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/group-resource-library/index.js)
- [miniprogram/pages/group-resource-library/index.wxss](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/group-resource-library/index.wxss)

当前能力：

- 我的页 `群资源库` 已跳转到新页面。
- 新页面支持搜索、热词、积分胶囊、添加微信群、上传二维码、填写城市和标签、发布奖励积分、查看二维码扣积分。
- 本轮是前端本地 MVP，数据存本机 storage。

未完成：

- 未接后端持久化。
- 未做二维码内容识别、重复校验、真实微信群判断。
- 未做积分明细、举报反馈、审核下架、退分扣分。
- 未做跨用户资源共享。

已验证：

- `node --check miniprogram/pages/group-resource-library/index.js`
- `node --check miniprogram/pages/profile/index.js`
- `node --check miniprogram/components/custom-nav/index.js`
- `app.json` 和页面 `index.json` JSON 解析通过。
- `git diff --check` 覆盖本轮修改文件。

补充小修：

- 搜索按钮已收进搜索卡片内，避免右侧溢出。
- 添加微信群按钮已改短。
- 已新增 `积分规则` 入口和弹层。

再次补充：

- 群资源库 `添加微信群` 已从内联表单重做为四步发布流程：
  - 上传识别
  - 点选信息
  - 有效期确认
  - 发布成功
- 搜索框已改为胶囊内部按钮布局，避免按钮和背景溢出屏幕。
- 效果稿中的新增/确认数字没有写死，当前用本地真实数据展示。

规则修正：

- 发布群不再直接到账积分，改为冻结 20 积分；2 人确认可进或后台确认后才应转可用积分。
- 当前前端本地 MVP 只展示冻结状态，不提供用户自己确认到账的按钮，避免自刷。
- 自己发布的群资源卡片新增 `删除`。
- 积分规则弹层已补充退分和惩罚机制。
- 城市选择已改为微信小程序原生地区选择器，保留 `全国` 快捷选项；当前 MVP 默认城市位为 `长沙市`，真实自动定位城市后续需接定位反查服务。
- 最近可查看群卡片底部已拆成统计行和按钮行，避免 `查看 / 确认可进 / 冻结积分` 被按钮挤压成竖排。

## 2026-06-28 部署前文字减负收口

本轮已完成：

- 首页、资料、合集、雷达、我的页首屏文案进一步压短。
- 资料页采用 B 方案：大块 AI 提示卡改为轻提示条。
- 企业资源搜索页工具语气收口，重复积分说明移出主页面。
- 帮助与反馈页保留奖励主心智，奖励卡副文案压为 `被采纳，就给奖励`。

已验证：

- `node --check` 覆盖首页、合集、雷达、我的、企业资源搜索、帮助与反馈 JS。
- `git diff --check` 覆盖本轮修改文件。

仍需人工真机看：

- 小屏首屏文字是否明显减负。
- 资料页轻提示是否足够解释“为什么列表会这样排序”。
- 企业资源搜索积分规则弹层里的规则是否和后端正式积分账本一致。

## 2026-06-28 部署前自测结果

本轮额外收口：

- 清理小程序前端展示文案里的开发态表达，避免用户看到 `后端 / 部署 / API Key / 本地测试 / 后续按钮` 这类不成熟措辞。
- 企业资源搜索保存页移除未实现的摘要卡按钮。
- 登录页本地调试入口在本地环境仍可用，但展示文案改为 `便捷登录`。

已验证：

- 小程序全量 JS `node --check`：通过。
- 小程序 49 个 JSON 文件解析：通过。
- `git diff --check`：通过。
- 后端编译：`.venv312/bin/python -m compileall backend/app backend/tests` 通过。
- 后端主测试：`.venv312/bin/python -m pytest backend/tests/test_app.py -q`，112 passed。
- 后端完整测试：`.venv312/bin/python -m pytest backend/tests -q`，149 passed。

部署前仍建议人工确认：

- 微信开发者工具真机预览五个 Tab 首屏。
- 资料无图卡、群资源库卡片、我的页资源工具四宫格在小屏手机上不挤压。
- 生产后端部署前备份 `.env`、`secrets/`、媒体目录和运行态数据。

## 2026-06-28 生产后端已部署

部署方式：

- 未使用服务器 `git pull`。
- 采用本地已验证代码定向 `rsync` 到生产。
- 排除生产 `.env`、`secrets/`、媒体目录、Docker volume 和运行态数据。

备份与回滚：

- 生产备份目录：`/home/ubuntu/teamBuy/backups/pre-deploy-20260628-062157`
- 旧后端镜像回滚标签：`teambuy-backend:before-deploy-20260628-062238`

已验证：

- `docker compose build backend` 成功。
- `docker compose up -d backend` 成功。
- 服务器本地 `/health` 正常。
- 公网 `https://teambuy.lifelove.top/health` 正常。
- 公网 `/api/wecom/config-check` 正常。
- 公网 `/api/wecom/customer-service-config` 正常。
- 服务器本地 `/api/wecom/archive/config-check` 正常。
- 管理接口错误 token 返回 403。
- 重启后等待一个 worker 周期，容器稳定，日志无异常堆栈。

待人工继续：

- 在微信开发者工具上传小程序体验版。
- 真机扫五个 Tab、资料分享页、企业微信助手入口。
- 后续版本稳定后整理 GitHub release，避免本地和服务器都出问题时无法恢复。

## 2026-06-28 最新交接：PC 运营后台 V1

本轮完成：

- 新增 PC 运营后台文档：
  - `docs/stage2-docs/29-pc-ops-console-v1.md`
  - `docs/qa/PC运营后台V1_测试清单与验收标准.md`
- 新增后台页面：
  - `GET /ops`
- 新增后台接口：
  - 总览 / 用户排行 / 内容排行 / 系统待处理
  - 群二维码批量上传预览、保存、列表
  - 反馈工单创建、列表、更新
- 新增后台轻量存储：
  - `backend/app/services/ops_console_store.py`
- 新增静态后台页面：
  - `backend/app/static/ops-admin/index.html`

已确认现状：

- 这版 PC 后台已经能做：
  - 每日运营总览
  - 用户活跃排行
  - 合集 / 资料排行
  - 导入 / 通知 / 媒体 / 同步异常聚合
  - 群二维码批量录入批次
  - 反馈工单处理
- 这版 PC 后台还不能做：
  - 真实全局资源积分余额调整
  - 群资源库 / 企业资源搜索的全局积分统计
  - 小程序帮助反馈前台自动汇入后台

为什么不能直接做积分管理：

- 群资源库积分、企业资源搜索积分、帮助反馈，目前主要仍在小程序本地 storage。
- 如果现在硬加“改别人积分”按钮，会出现后台已改、前台不生效的错觉。
- 因此本轮只在总览页明确标记这些模块“待后端化”。

测试结果：

- `python3 -m compileall backend/app`：通过
- `./.venv312/bin/python -m pytest backend/tests/test_app.py -k "ops_admin" -q`：4 passed

下个 Codex 接手建议顺序：

1. 先把资源积分和帮助反馈迁到后端真实存储。
2. 再补“用户积分调整、冻结积分、积分流水、排行”。
3. 再把群二维码批次正式接入群资源库前台审核 / 发布链路。

## 2026-06-28 新增二维码服务器上传提示词文档

已新增：

- `docs/stage2-docs/30-group-qr-server-upload-handoff.md`
- `docs/prompts/group-qr-upload-codex-prompt.md`

用途：

- 给后续新会话的 Codex 一套稳定提示词，用于处理：
  - 读取本地微信群二维码图片
  - 上传到生产服务器 media 目录
  - 生成公网图片 URL
  - 整理成 CSV / XLSX 模板

重要口径：

- 批量模板里的 `二维码链接` 是图片 URL，不是扫码后的微信内部数据。

## 2026-06-28 最新交接：小程序真机 UI 细节收口

本轮完成：

- 群资源库空态 `去添加` 改成短胶囊并居中。
- 群资源库顶部移除重复的 `100 积分 / 积分规则`。
- 企业资源搜索顶部和详情页移除重复积分展示，只保留资源工具里的总积分入口。
- 企业资源搜索本地扣分 key 改为复用群资源库同一个资源积分 key。
- 雷达客户卡删掉底部重复 `复制话术 / 更多` 操作层。
- 首页机会卡删掉黑色 `复制话术` 按钮。
- 首页 `待发现` 空态去掉重复文案，压成一条提示。

已验证：

- 相关小程序页面 JS 语法检查通过。
- 本轮小程序文件 `git diff --check` 通过。

待人工继续：

- 微信开发者工具重新上传体验版。
- 真机重点复看：
  - 群资源库空态按钮是否居中、是否足够短。
  - 雷达客户卡 `下一步动作` 按钮是否不挤压文案。
  - 首页 `待发现` 是否只显示一条 `先发一份资料。`。
  - 我的页资源工具是否成为唯一积分总入口。

## 2026-06-28 最新交接：雷达动作与企业搜索真实接入

本轮完成：

- 雷达页客户卡默认动作改为 `看详情`，点击进入客户看板访客页。
- 删除“普通客户下一步默认复制话术”的心智，复制话术不再作为雷达主动作。
- 我的页资源积分规则补充冻结积分说明。
- 新增后端企业资源搜索接口：
  - `GET /api/enterprise-resources/search`
- 小程序企业搜索页优先调用后端接口。
- 生产后端已部署并验证：
  - `/health` 正常。
  - `/api/enterprise-resources/search?keyword=长沙装饰&page_size=2` 返回真实天眼查 MCP 候选。

重要注意：

- 当前天眼查 key 是 MCP 权限，REST OpenAPI `searchV2` 返回“无权限访问此api”。
- 后端已走 MCP `search_companies`，不要再切回 REST 搜索接口。
- 小程序前端改动仍需微信开发者工具上传体验版后才能在手机看到。
- 企业搜索“深度查询”当前仍主要是本地模拟结果；本轮先把企业候选搜索接到真实天眼查，后续再把风险/股东/变更等功能逐个接 MCP 工具。

## 2026-06-28 最新交接：企业资源搜索积分减负

本轮根据真机测试反馈调整：

- 企业候选搜索免费。
- 企业基本信息和深度查询统一改为 `5 分/项`。
- 查询卡片上的 `10分 / 20分` 已同步改为 `5分`。
- 我的页资源工具积分规则已同步为 `企业查询：-5/项`。
- 24 小时缓存仍然不重复扣分。

注意：

- 当前这是小程序本地资源积分策略调整，测试号此前已扣掉的本地积分不会自动恢复。
- 正式版资源积分仍需后端化，必须补余额、冻结、流水、缓存、退分、补偿和异常申诉。

验证：

- `node --check miniprogram/pages/enterprise-resource-search/index.js`：通过。
- `git diff --check` 针对企业资源搜索页和我的页规则文件：通过。

## 2026-06-29 最新交接：企业微信智能机器人权限网关 MVP

本轮完成：

- 新增后端路由 `backend/app/api/routes_robot.py`。
- `backend/app/main.py` 已挂载机器人路由。
- `backend/app/core/config.py` 新增 `robot_gateway_token`。
- `backend/.env.example` 新增 `ROBOT_GATEWAY_TOKEN` 说明。
- 新增接口：
  - `POST /api/robot/query`
- 调用要求：
  - Header: `Authorization: Bearer <ROBOT_GATEWAY_TOKEN>`
- 请求核心字段：
  - `chatType`: `private` / `group`
  - `externalUserId` 或 `fromUserId`；WorkBuddy API 插件也可不传，后端会读取请求头 `userid`
  - `roomId`
  - `text`
  - `limit`
- 返回核心字段：
  - 顶层 `result/text/answer/content` 都是机器人回复内容，优先给 WorkBuddy 输出参数映射 `result`。
  - `data` 内保留完整结构，供后端或小程序后续继续使用。
- 权限模型：
  - `public`：天气、帮助等公开问题。
  - `self`：查用户自己的资料/合集，必须通过 `wecom_identity_bindings` 映射到 `ownerUserId`。
  - `room`：群日报/广告识别，需要 `roomId`。
- 已实现：
  - 未配置或 token 错误时拒绝访问。
  - 未绑定企微身份时返回 `bind_required`。
  - 绑定后只查当前绑定用户自己的资料。
  - 群里问个人数据时返回 `private_required`。
  - 资料结果返回 `/pages/note-preview/index?id=...`。
  - 合集结果返回 `/pages/showcase-view/index?id=...`。

验证：

- `python3 -m compileall backend/app/api/routes_robot.py backend/app/main.py backend/app/core/config.py`：通过。
- `./.venv312/bin/python -m pytest backend/tests/test_app.py -k "robot_gateway" -q`：3 passed。

待接 WorkBuddy 前确认：

- WorkBuddy API 插件参数建议只配置 `text/chatType/roomId/limit`，发送人身份优先从请求头 `userid` 读取。
- WorkBuddy 输出参数如果调试面板空白，必须显式配置输出字段 `result`，来源路径填顶层 `result`。
- 第一版机器人只调用网关，不直接连接数据库。
## 2026-06-29 企业群日常运营方案已补

已新增文档：

- `docs/stage2-docs/31-enterprise-group-daily-operations-v1.md`

后续如要继续推进，建议按这份文档往下拆：

- 企业群机器人消息模板配置
- 企业群消息对应的小程序承接页
- 不同群类型的模板差异化

当前已确定的企业群口径：

- 不是继续分群场
- 是持续更新和内容承接场
- 固定 3 类消息：
  - 中午更新
  - 下午入口
  - 晚间总结
## 2026-06-29 企业群机器人消息模板已补

已新增文档：

- `docs/stage2-docs/32-enterprise-group-bot-message-templates-v1.md`

这份文档适合下一步继续拆给：

- 开发配置群机器人模板
- 运营配置不同 `groupId` 的文案
- 产品确认不同群类型的小程序承接页

## 2026-06-29 最新交接：电子名片分享封面修正

本轮完成：

- 电子名片分享到微信时，不再把头像/封面图铺满。
- `miniprogram/utils/business-card-share.js` 已给电子名片单独绘制横版模板卡片：
  - 专业顾问：深蓝金色。
  - 门店名片：浅绿。
  - 专家介绍：黑金。
  - 简洁微信风：白底轻量。
- 名片封面底部保留：`由资料整理助手生成 · 点击生成同款`。
- `miniprogram/pages/note-preview/index.wxss` 已强化客户页 4 种名片模板的视觉差异。

验证：

- 小程序全部 JS `node --check`：通过。
- 小程序 JSON 解析：通过。
- `python3 -m compileall backend`：通过。
- `./.venv312/bin/python -m pytest backend/tests/test_app.py -q`：134 passed。

待用户真机确认：

- 在微信开发者工具重新编译/上传后，重新从电子名片“发客户/分享”测试。
- 确认聊天里的名片卡片封面显示为模板卡片，而不是头像大图。
- 分别点开 4 种名片模板客户页，确认视觉差异符合预期。

## 2026-06-29 最新补充：资料库名片发客户入口已改

本轮补漏：

- 用户反馈“分享还是大图”，原因是资料库 / 名片列表的发客户入口仍走通用资料封面生成。
- 已改为：
  - 名片资料：名片专属模板封面。
  - 服务方案：服务方案专属封面。
  - 普通资料：通用资料封面。
- 资料库名片 / 服务卡片的“发客户 / 更多”按钮已复用房源卡片按钮结构，避免一个过长一个过短。

测试提示：

- 重新编译小程序后再测。
- 如果当前页面已经打开过，建议返回后重新进入资料库，避免页面内旧的 `shareImages` 仍留着上一版封面。

## 2026-06-29 最新交接：生产后端与 PC 运营后台已部署

本轮生产部署完成：

- 服务器：`81.70.84.35`
- 项目目录：`/home/ubuntu/teamBuy`
- 容器：
  - `teambuy-backend-1`
  - `teambuy-postgres-1`
- 备份：
  - 代码备份：`/home/ubuntu/teamBuy-backups/teamBuy-code-20260629-215329.tar.gz`
  - Nginx 备份：`/etc/nginx/conf.d/teambuy.conf.bak-20260629-220842`

本轮部署方式：

- 本地同步 `backend/` 和 `docker-compose.yml` 到服务器。
- 未覆盖生产 `backend/.env`、`backend/secrets/`、媒体目录和数据库卷。
- 服务器执行 `docker compose build backend` 和 `docker compose up -d backend`。
- Nginx 已增加 `/ops` 和 `/ops/` 代理到后端 `127.0.0.1:8002`。

线上验证：

- `https://teambuy.lifelove.top/health`：正常。
- `https://teambuy.lifelove.top/ops`：PC 运营后台可打开。
- `https://teambuy.lifelove.top/api/ops-admin/overview` 不带口令返回 403，说明 API 到达后端且鉴权生效。

小程序下一步：

- 用户可在微信开发者工具重新编译、上传体验版，并提交审核。
- 小程序端需要使用线上域名 `https://teambuy.lifelove.top`。

## 2026-06-30 最新交接：测试环境已搭建

本轮完成生产 / 测试环境隔离。

生产环境保持不变：

- 域名：`https://teambuy.lifelove.top`
- API：`/api/`
- PC 后台：`/ops`
- 健康检查：`/health`
- Docker 容器：
  - `teambuy-backend-1`
  - `teambuy-postgres-1`

新增测试环境：

- 与生产共用服务器，但容器、数据库、媒体卷、端口隔离。
- Compose 文件：`docker-compose.test.yml`
- 测试环境配置：服务器 `/home/ubuntu/teamBuy/backend/.env.test`，不提交 Git。
- Docker project：`teambuy-test`
- 容器：
  - `teambuy-test-backend-test-1`
  - `teambuy-test-postgres-test-1`
- 测试入口：
  - API：`https://teambuy.lifelove.top/test-api/`
  - PC 后台：`https://teambuy.lifelove.top/test-ops`
  - 健康检查：`https://teambuy.lifelove.top/test-health`
  - 媒体：`https://teambuy.lifelove.top/test-media/`

重要安全点：

- `/test-ops` 页面通过 Nginx `sub_filter` 把页面里的 `/api/...` 替换为 `/test-api/...`，避免测试后台误操作生产 API。
- 测试环境默认：
  - `WECOM_USE_MOCK=true`
  - `WECOM_ARCHIVE_WORKER_ENABLED=false`
  - `WECOM_GROUP_BOT_WEBHOOKS={}`
- 测试群机器人 webhook 只在测试后台单独配置，不能复用正式群 webhook。

验证结果：

- `https://teambuy.lifelove.top/health`：正常。
- `https://teambuy.lifelove.top/test-health`：正常。
- `https://teambuy.lifelove.top/test-api/ops-admin/overview` 不带口令返回 403，说明测试 API 路由和鉴权生效。
- `curl https://teambuy.lifelove.top/test-ops | rg '/test-api|/api/'` 已确认测试后台请求走 `/test-api/...`。

相关文档：

- `docs/deploy-env-isolation.md`

后续规则：

- 小程序审核和真实用户只走生产环境。
- 企业群机器人运营卡片生成器、每日运营内容、测试群 webhook、群二维码挂载、资源上架实验、外部群二维码/黄页爬取、支付分销和推广内容，先走测试环境。
- 分享封面生成器和机器人运营卡片生成器只复用底层绘图能力，上层模板必须隔离，避免再次破坏正式小程序分享卡片。

## 2026-06-30 最新交接：企业微信助手首次导入自动绑定

本轮修复用户 12:12 房源资料进入 `unclaimed` 的根因方向。

关键结论：

- 小程序 `openid` 和企业微信会话归档 `external_userid` 不是同一个 ID，不能直接相等匹配。
- 新增“添加企业微信助手前创建绑定意图”的流程：
  - 小程序首页企业微信助手入口触发 `handleContactStart`。
  - 前端调用 `POST /api/auth/wecom-bind-intent`。
  - 后端为当前小程序用户保存短期绑定意图。
  - 下一条未绑定企业微信消息进入导入链路时，如果只有一个有效绑定意图，自动建立 `external_userid -> ownerUserId/openid` 绑定。
  - 资料直接归属到该用户，不再进入待认领。
- 如果多个用户同时有有效绑定意图，系统不自动绑定，继续进入待认领，防止串用户。
- 保留待认领和认领链接作为兜底。

改动文件：

- `backend/app/core/config.py`
- `backend/.env.example`
- `backend/app/schemas/auth.py`
- `backend/app/api/routes_auth.py`
- `backend/app/services/app_service.py`
- `backend/tests/test_app.py`
- `miniprogram/services/api.js`
- `miniprogram/pages/home/index.js`
- `docs/dev-log.md`
- `docs/decisions.md`
- `docs/pitfalls.md`

新增配置：

- `WECOM_BIND_INTENT_TTL_SECONDS=3600`
- `WECOM_UNCLAIMED_DEFAULT_OWNER_USER_ID=`

验证：

- 小程序全部 JS `node --check`：通过。
- 小程序 JSON 解析：通过。
- `python3 -m compileall backend/app`：通过。
- `./.venv312/bin/python -m pytest backend/tests/test_app.py -q`：136 passed。

注意：

- 这次代码能解决“后续用户从小程序点击添加助手后，第一条资料自动入库”的问题。
- 2026-06-30 12:12 已经进入 `unclaimed` 的历史资料不会因为这次代码自动迁移，仍需通过认领或生产数据修复归属。
- 小程序端改了首页逻辑，用户需要重新上传体验版 / 审核版后，前端自动创建绑定意图才会生效。
## 2026-06-30 资源工具与商机雷达方案已补

已新增文档：

- `docs/stage2-docs/33-resource-tools-opportunity-radar-v1.md`

核心口径：

- 资源工具是机会雷达入口，不是单纯工具集合。
- 商机线索模块拆为：
  - 需求广场
  - 我的机会
  - 订阅
  - 已保存
- 线索来源前台弱化，后台完整保留。
- 与四个工作台打通的含义是“用户回应机会时生成自己的资料页/合集”，不是把第三方线索直接做成合集。

后续建议：

- 继续讨论商机线索页面效果图和交互。
- 再进入开发前，需要补测试清单与数据结构细化。
## 2026-06-30 商机线索 UI 规格已补

已新增文档：

- `docs/stage2-docs/34-opportunity-leads-ui-spec-v1.md`

关键要求：

- 我的机会页必须保留雷达图和匹配度。
- 线索详情页必须突出“生成回应资料”。
- 已保存页必须是轻量跟进台，不是收藏夹。
- 开发必须先静态 UI 验收，再接 mock 数据和真实接口。
- 交付必须提供真机截图，并说明是否为参考图版本。
## 2026-06-30 供需广场发布供给卡已补

已补充到：

- `docs/stage2-docs/33-resource-tools-opportunity-radar-v1.md`
- `docs/stage2-docs/34-opportunity-leads-ui-spec-v1.md`

核心新增点：

- 需求广场可升级为供需广场。
- 用户可以把自己整理好的资料卡、服务页、房源合集、商品合集、电子名片发布为供给卡。
- 供给卡审核通过后进入广场，被别人查看、保存、申请联系。
- 我的发布页需要展示展示中、待审核、已下架、被查看、被保存和雷达反馈。

后续页面讨论可围绕：

- 供需广场
- 发布供给
- 供给预览
- 我的发布

## 2026-06-30 回应包与商业化方案已补

已新增文档：

- `docs/stage2-docs/35-response-package-monetization-v1.md`

已同步补充：

- `docs/stage2-docs/33-resource-tools-opportunity-radar-v1.md`
- `docs/stage2-docs/34-opportunity-leads-ui-spec-v1.md`

核心口径：

- 回应包是把机会变成行动的成交资料包，不是普通文案。
- 回应包应从用户已有资料中推荐服务页、合集、名片、案例等内容，并生成首次话术和追踪链接。
- 第一版必须用户确认后发送，不做自动骚扰。
- 商业化节奏建议为：免费回应包 -> 积分消耗 -> 会员额度 -> 供给卡曝光付费 -> 保证金认证 -> 双向收费。
- 保证金是可信认证和违规约束，不是平台担保成交。

## 2026-06-30 商机线索二期开发清单已补

已新增文档：

- `docs/stage2-docs/36-opportunity-radar-phase2-dev-checklist.md`

给开发 Codex 的重点：

- 不要一次做完整商业化，先做 P0 闭环。
- 开发顺序建议为：
  1. 后端积分账本
  2. 商机线索数据模型和后台录入
  3. 小程序静态 UI
  4. 接 mock 数据和真实接口
  5. 回应包 P0
  6. PC 后台增强
- 保证金、会员、充值、双向收费、自动爬虫和自动联系全部后置。
- 二期验收以“用户能生成回应包并跟进”和“运营能录入线索、看积分、处理反馈”为准。

## 2026-06-30 企业微信助手绑定码已实现

本轮修复目标：

- 避免多人同时点击添加助手时，企业微信 `external_userid` 被错误绑定到其他小程序用户。
- 避免微信插件出现“需要允许微信身份后才能添加”时阻断首次绑定。

实现方式：

- 小程序点击企业微信助手入口时，后端生成 `TB-XXXXXX` 绑定码。
- 小程序自动复制话术：`绑定资料助手 TB-XXXXXX`。
- 用户把话术发给企业微信助手，后端从客服同步消息或会话归档消息识别绑定码。
- 绑定码消息只用于绑定，不生成资料。
- 绑定成功后，后续资料按 `external_userid -> ownerUserId/ownerOpenid` 自动归属。

已覆盖测试：

- 客服同步消息绑定码只绑定不入库。
- 绑定后再发资料自动归属用户。
- 会话归档先发绑定码、再发资料自动归属用户。

待人工真机验证：

- 上传新版小程序体验版。
- 登录后点“添加房源助手”或“加企业微信助手”。
- 确认剪贴板里是 `绑定资料助手 TB-XXXXXX`。
- 把该话术发给企业微信助手。
- 再发一条房源资料，确认进入当前小程序账号。

## 2026-06-30 批量挂牌房源导入已修

用户反馈样本：

```text
挂牌
1）松涛路200弄朝南次卧3000
2）松涛路200弄50号301次卧3100
3）汤臣四期次卧一室户3100
4)玉兰4期122号1401次卧2500
5)万科500号410一室户3500
6）申源苑4号701主卧3380
汤臣四期大厅一室户3700
电话15201882219。
```

处理结果：

- 该文本实际包含 7 条带价格房源行。
- 当前批量解析器已能拆出 7 条。
- 会话归档导入已绑定用户时，会自动生成 7 条房源资料。
- 未绑定用户仍走待认领，不自动批量创建到错误账号。

后续同类反馈迭代方法：

- 先把真实失败样本加入测试。
- 补解析规则。
- 跑批量解析测试和企业微信导入回归。
- 再部署测试/生产。

## 2026-06-30 添加资料助手引导层已补

当前状态：

- 首页添加助手入口仍走后端绑定码接口。
- 绑定话术复制成功后，会展示自定义引导层，不再只弹系统提示。
- 引导层包含绑定话术、三步说明、重新复制和“我去发送”按钮。
- 当前不自动打开微信客服，也不尝试代用户发送消息，避免混淆企业微信成员好友主链路。

待人工真机验证：

- 上传新版小程序体验版。
- 点首页“添加房源助手 / 加企业微信助手”。
- 确认弹层可见、绑定话术已复制、按钮文字居中。
- 按引导把绑定话术发给企业微信资料整理助手，再发资料确认自动入库。

## 2026-07-01 绑定码复用与已绑定状态

当前状态：

- 首页添加资料助手弹层已接入真实企业微信资料助手二维码。
- 后端 `POST /api/auth/wecom-bind-intent` 已改为：
  - 已绑定用户返回 `status=bound`，不再给绑定话术。
  - 未绑定且有效期内已有绑定码时复用原绑定码。
  - 未绑定且无有效绑定码时才生成新码。
- 小程序收到已绑定状态后，提示可以直接转发资料，不再展示二维码/绑定码。

已验证：

- 相关绑定码测试 5 条通过。
- 后端完整测试 142 条通过。
- 小程序首页 JS 静态检查通过。

待上线动作：

- 后端需要部署到生产，接口行为才会生效。
- 小程序需要上传新版体验版，已绑定提示和二维码弹层才会在真机生效。

## 2026-07-01 资料列表发客户按钮已改为不等分享图

当前状态：

- `miniprogram/pages/library/index.js` 已调整分享按钮逻辑。
- 有 `sourceNoteId` 的资料直接显示“发客户”，不再因缺少 canvas 生成图显示“生成中”并禁用。
- 有首图时直接把首图作为分享图；没有首图时也允许分享客户页，不传 `imageUrl`。
- 缺少 `sourceNoteId` 的资料显示“先编辑”，避免把不可分享伪装成生成中。

验证：

- `node --check miniprogram/pages/library/index.js` 通过。

待上线动作：

- 这是小程序前端改动，不需要后端部署。
- 需要用微信开发者工具重新上传体验版，真机确认资料列表房源卡可直接“发客户”。

## 2026-07-01 房源编辑页封面、复制、商圈候选已修

当前状态：

- `miniprogram/pages/note-edit/index.js` 已让切换封面同步到 `structuredData.coverUrl/images`。
- `miniprogram/services/api.js` 已让资料列表卡片在缺少 `coverUrl` 时用第一张图片媒体兜底显示缩略图。
- 上游备注“复制全部”改为读取页面状态，单项复制和复制全部都有按压变深反馈。
- 商圈/区域候选改为从原文、标题、备注、地址和识别字段中提取；固定兜底只剩“人民广场 / 五一广场”。
- `miniprogram/utils/note-display.js` 同步移除旧本地商圈上下文词。

验证：

- `node --check miniprogram/pages/note-edit/index.js` 通过。
- `node --check miniprogram/services/api.js` 通过。
- `node --check miniprogram/utils/note-display.js` 通过。
- `git diff --check` 通过。

待上线动作：

- 这是小程序前端改动，不需要后端部署。
- 需要重新上传体验版，真机确认切换封面、复制全部和商圈候选。

## 2026-07-01 当前前端环境：测试环境

当前状态：

- `miniprogram/app.js` 已切回测试环境，方便二期阶段测试。
- 当前配置：
  - `apiBaseUrl=https://teambuy.lifelove.top`
  - `apiRoutePrefix=/test-api`
  - `mediaRoutePrefix=/test-media`
  - `environmentName=test`
- 小程序内 `/api/...` 请求会被转换到 `/test-api/...`。
- 媒体相对路径 `/media/...` 会转换到 `/test-media/...`。

验证：

- `node --check miniprogram/app.js` 通过。
- `GET https://teambuy.lifelove.top/test-api/cards` 返回 200。

注意：

- 测试环境和生产数据隔离；生产企业微信会话归档生成的资料不会出现在测试前端。

## 2026-07-01 表格式多套房源拆分已本地修复

当前状态：

- 后端批量房源解析已支持 keycap emoji 数字归一化。
- 后端批量房源入口已支持“区域标题 / 楼栋 / 房号价格”的表格式公寓房源表。
- 用户 15:39 提供的“湘聚公寓最新房源表”样式，本地测试可拆出 8 套房源。
- “电话13268058758微信同号”会写入上游电话和上游微信私密字段。
- 已重新读取 `/Users/yiyi/Desktop/房源样本.docx`，文字样本 001-006 全部对齐预期拆分数量，并且归档入口判断均为 true：
  - 001：13/13
  - 002：12/12
  - 003：6/6
  - 004：4/4
  - 005：5/5
  - 006：8/8
- 已补充覆盖字段块房源、`元/月包物业` 清单、地址行后接 `203室980`、价格区间和 `1680空` 等格式。

验证：

- `.venv312/bin/python -m pytest backend/tests/test_app.py -q -k "property_batch"` 通过，7 passed。
- 原始 docx 文字样本复测 `ALL_OK=True`。

待上线动作：

- 已上线测试后端和生产后端。会话归档文字房源表自动分类和拆分已在后端生效。
- 小程序前端不需要为这条规则单独修改；前端只展示后端生成的房源合集和房源卡。
- 本地 `backend/app/services/app_service.py` 当前仍混有其他未提交功能改动。部署生产/测试时不要直接整文件覆盖，建议从服务器当前版本做最小补丁并备份后热更新。

部署记录：

- 服务器备份：`/home/ubuntu/teamBuy/backend/backups/codex-20260701-162139-property-batch-text/app_service.py`。
- 已同步容器：
  - `teambuy-test-backend-test-1`
  - `teambuy-backend-1`
- 容器内验证 15:39 样本 + docx 001-006 全部 `ok=True`。
- 公网健康检查和 `/api/cards`、`/test-api/cards` 均返回 200。

下一步：

- 图片样本 007 已单独走真实 OCR 验收。当前 Paddle OCR 对长表小字识别质量不足，只识别 26 行，解析器只能误拆 4 条。
- 已补 OCR 防误拆保护并同步测试/生产后端：表格式房源图 OCR 低产出时，不自动创建错误房源合集。
- 后续 007 需要做 OCR 表格专项优化，方向是异步切块、表格行重建、价格列/户型列纠错，不能用当前同步 OCR 直接上线自动拆。

007 专项实验进展：

- OpenCV 表格线检测可稳定识别列边界：区域、商圈、房源地址、户型、月租。
- 横线可切出约 163 个行区间。
- 使用“单行裁剪 + 4 倍放大 + 单进程 PaddleOCR”后，全表约 51 秒完成 OCR。
- 得到 159 行有效文本，其中 156 行已可用规则结构化为房源候选。
- 仍需补的纠错：
  - 区域/商圈顺序错位，如 `金盆岭 天心区 ...`。
  - `星沙` 非“区”结尾区域。
  - 少数价格漏识别或地址边界误切。
- 下一步建议新增独立 `property-table-ocr` worker，不要复用当前整图 OCR 同步接口直接生成房源。

## 2026-07-01 OCR 后台任务架构改造

当前状态：

- 本地已完成 OCR 后台任务化架构改造，尚未部署服务器。
- 图片上传、OCR 识别、图片截图保存接口现在会先保存图片资料，再创建 `ocr-recognize-note` 队列任务，并把 OCR 状态写成 `queued`。
- 前端应展示“识别中”，后续通过刷新资料详情/列表拿到识别完成后的资料。
- 新增后台 worker：
  - `backend-worker`：处理 OCR 队列任务，默认并发 1。
  - `archive-worker`：单独处理企业微信会话归档。
  - `backend`：只处理小程序 API 请求。
- `property-table-ocr` 任务名已预留，但 007 表格图片的专项算法还没接入正式 worker。

涉及文件：

- `backend/app/core/config.py`
- `backend/app/services/sync_task_queue.py`
- `backend/app/services/background_task_worker.py`
- `backend/app/worker.py`
- `backend/app/api/dependencies.py`
- `backend/app/main.py`
- `backend/app/api/routes_ocr.py`
- `backend/app/api/routes_notes.py`
- `backend/app/services/app_service.py`
- `backend/tests/conftest.py`
- `backend/tests/test_app.py`
- `docker-compose.yml`

验证：

- `.venv312/bin/python -m compileall -q backend/app backend/tests` 通过。
- `.venv312/bin/python -m pytest backend/tests/test_app.py backend/tests/test_sync_task_queue.py -q -k "ocr or image_capture or property_batch or archive_worker or sync_task"`：19 passed。
- `git diff --check` 通过。

注意：

- OCR worker 架构已部署到生产服务器。
- 备份目录：`/home/ubuntu/teamBuy/backend/backups/codex-20260701-203506-ocr-worker-arch`。
- 当前生产为三进程：
  - `teambuy-backend-1`：API。
  - `teambuy-backend-worker-1`：OCR worker。
  - `teambuy-archive-worker-1`：企业微信归档 worker。
- 生产 compose 设置了 `DATABASE_REQUIRE_POSTGRES=true`，避免 Postgres 异常时 fallback 到 JSON。
- repository schema 初始化已加 advisory lock，避免三进程同时启动 deadlock。
- 线上已验证 `/api/ocr/images` 上传立即返回 `queued`，OCR worker 消费后任务状态 `success`。
- 当前工作区还有较多此前阶段的未提交改动，后续部署不要直接整包覆盖生产，尤其不要覆盖 `backend/.env`、`backend/secrets/`、媒体目录和运行态数据。
- 007 专项 worker 已接入生产：
  - 表格检测：真实 007 `tableLike=true`，识别到 6 条竖线和约 167 行区间。
  - 完整 OCR：真实 007 得到 163 行有效文本，confidence 约 0.989。
  - 结构化：真实 OCR 文本解析出 159 套房源候选。
- 注意：为避免污染生产账号，本轮没有通过公网 API 直接上传 007 生成 159 条房源；真机测试时请使用专用测试账号。

## 2026-07-01 商机线索 P0 第 4/5 片交接

当前状态：

- 已完成小程序商机页面接真实接口：
  - `miniprogram/pages/opportunity-radar/index.js`
  - `miniprogram/pages/opportunity-market/index.js`
  - `miniprogram/pages/opportunity-detail/index.js`
  - `miniprogram/pages/opportunity-saved/index.js`
  - `miniprogram/services/api.js`
- 已新增回应包页面：
  - `miniprogram/pages/response-package/index.js`
  - `miniprogram/pages/response-package/index.wxml`
  - `miniprogram/pages/response-package/index.wxss`
  - `miniprogram/pages/response-package/index.json`
- 已注册页面：
  - `miniprogram/app.json`
- 已完成后端回应包 P0：
  - `backend/app/models/domain.py`
  - `backend/app/services/repository.py`
  - `backend/app/services/app_service.py`
  - `backend/app/api/routes_opportunities.py`
  - `backend/app/main.py`
  - `backend/app/schemas/opportunities.py`
- 已补测试：
  - `backend/tests/test_app.py`

核心行为：

- 预览回应包不扣积分。
- 正式生成回应包先使用每月免费额度，之后每次 20 积分。
- 同一用户同一商机重复生成不会重复扣积分。
- 生成回应包会保存商机为“跟进中”，并写入跟进记录。
- 回应包不自动联系对方，只生成推荐资料、开场话术、追踪路径和跟进建议。
- 小程序真实接口失败或无数据时继续使用 mock 兜底。

验证：

- `node --check miniprogram/services/api.js && node --check miniprogram/pages/opportunity-radar/index.js && node --check miniprogram/pages/opportunity-detail/index.js && node --check miniprogram/pages/opportunity-market/index.js && node --check miniprogram/pages/opportunity-saved/index.js && node --check miniprogram/pages/opportunity-subscription/index.js && node --check miniprogram/pages/response-package/index.js` 通过。
- `python3 -m json.tool` 校验 `miniprogram/app.json` 和商机/回应包页面 JSON 通过。
- `DATABASE_BACKEND=json DATABASE_URL= DATA_FILE=/tmp/teambuy-test-response-package.json .venv312/bin/python -m pytest backend/tests/test_app.py -q -k "response_package or opportunity_lead or resource_wallet"`：11 passed。
- `DATABASE_BACKEND=json DATABASE_URL= DATA_FILE=/tmp/teambuy-test-response-package.json .venv312/bin/python -m compileall -q backend/app` 通过。
- `git diff --check` 通过。
- 全量后端 JSON 模式测试：198 passed、1 failed；失败是健康检查用例固定断言 `postgres`，与本次代码无关。

下一步建议：

1. 部署测试后端，让小程序真实接口能访问回应包 P0。
2. 用户在微信开发者工具预览/真机测试商机页、详情页、回应包页。
3. P0 静态验收通过后，再进入 P1：订阅保存接口、联系解锁策略、回应包可选资料、多线索筛选。

## 2026-07-01 PC 后台增强交接

当前状态：

- 已补 PC 后台二期 P0 增强：
  - 商机线索看板。
  - 商机线索快捷下架。
  - 积分账本 Tab。
  - 回应包记录 Tab。
- 新增运营接口：
  - `GET /api/ops/opportunity-dashboard`
  - `POST /api/ops/opportunity-leads/{lead_id}/offline`
  - `GET /api/ops/resource-wallet/users`
  - `POST /api/ops/resource-wallet/users/{user_id}/adjust`
  - `GET /api/ops/response-packages`
- 所有新增接口需要 `X-Admin-Token`。
- 后台下架会把线索状态改为 `archived`，小程序公开接口不再返回该线索。
- 积分调整复用统一积分流水，不单独造运营记录。

涉及文件：

- `backend/app/api/routes_ops_admin.py`
- `backend/app/services/app_service.py`
- `backend/app/static/ops-admin/index.html`
- `backend/tests/test_app.py`

验证：

- `DATABASE_BACKEND=json DATABASE_URL= DATA_FILE=/tmp/teambuy-test-ops-pc.json .venv312/bin/python -m pytest backend/tests/test_app.py -q -k "ops_opportunity_dashboard or response_package or opportunity_lead or resource_wallet"`：12 passed。
- `DATABASE_BACKEND=json DATABASE_URL= DATA_FILE=/tmp/teambuy-test-ops-pc.json .venv312/bin/python -m compileall -q backend/app` 通过。
- PC 后台 HTML 内联脚本提取后 Node 语法检查通过。

注意：

- 供给卡审核仍未实现独立审核流；当前 P0 先覆盖商机、积分、回应包和反馈。
- 部署测试后端后，需要打开 `/ops` 用管理员 Token 验证表格加载和积分调整/下架按钮。

## 2026-07-02 当前阻塞：测试后端未同步商机 P1 路由

用户真机反馈：

- “订阅雷达”读取失败，保存失败。
- “发布供需”保存/提交显示 `Not Found`。

已确认：

- 小程序当前配置仍是测试环境：
  - `apiBaseUrl=https://teambuy.lifelove.top`
  - `apiRoutePrefix=/test-api`
- 本地后端已经注册商机 P1 路由：
  - `/api/opportunity-subscriptions/me`
  - `/api/opportunity-subscriptions`
  - `/api/supply-demand/cards`
  - `/api/supply-demand/cards/{card_id}/submit`
- 公网测试入口仍未同步：
  - `/test-api/opportunity-subscriptions/me?userId=route_check` 返回 404。
  - `/test-api/supply-demand/cards` 返回 404。

已处理：

- `miniprogram/pages/opportunity-subscription/index.js`：读取/保存遇到 404 时提示“测试后端未同步接口”。
- `miniprogram/pages/supply-demand-publish/index.js`：读取/保存/提交遇到 404 时提示“测试后端未同步接口”。

验证：

- `node --check miniprogram/pages/opportunity-subscription/index.js && node --check miniprogram/pages/supply-demand-publish/index.js` 通过。
- `PYTHONPATH=backend DATABASE_BACKEND=json DATABASE_URL= DATA_FILE=/tmp/teambuy-route-check-tests.json .venv312/bin/python -m pytest backend/tests/test_app.py -q -k "p1_subscription or supply_demand"`：1 passed。

下一步：

1. 只部署测试后端，不部署生产。
2. 部署后验证：
   - `curl https://teambuy.lifelove.top/test-api/opportunity-subscriptions/me?userId=route_check`
   - `curl https://teambuy.lifelove.top/test-api/supply-demand/cards`
3. 真机回归订阅保存、供需草稿保存、提交审核、我的发布编辑回填。

## 2026-07-02 分享封面勾子处理

用户确认的产品规则：

- 所有小程序转发尽量都返回自定义 `imageUrl`。
- 分享图底部统一小字：`由资料整理助手生成 · 点击生成同款`。
- 有首图/封面时优先使用首图按比例裁切作为分享图主体。
- 无图时才生成标题图。

已处理：

- `miniprogram/pages/library/index.js`：
  - 资料库不再直接使用原封面作为分享 `imageUrl`。
  - 可分享资料统一等待生成带底部勾子的分享图。
  - 分享图未准备好时按钮显示“封面准备中”，避免发出无勾子的卡片。

已有基础：

- `miniprogram/utils/business-card-share.js` 的 `generateNativeShareImage` 已支持有图时裁切首图、无图时生成标题封面，并统一绘制底部勾子。
- 合集页 `showcases` / `showcase-view` 已走该工具。

待补：

- 二期新增的商机、回应包、供需详情页面仍未统一接入 `onShareAppMessage + imageUrl` 分享图。

## 2026-07-02 二期页面分享层已补

已处理：

- 新增 `miniprogram/utils/universal-share.js`，统一生成带底部勾子的分享图。
- 新增全局 `.universal-share-canvas` 样式。
- 以下页面均已接 `onShareAppMessage` 和 `universalShareCanvas`：
  - `pages/opportunity-radar/index`
  - `pages/opportunity-market/index`
  - `pages/opportunity-detail/index`
  - `pages/opportunity-saved/index`
  - `pages/opportunity-subscription/index`
  - `pages/response-package/index`
  - `pages/response-package-radar/index`
  - `pages/supply-demand-detail/index`
  - `pages/supply-demand-my/index`
  - `pages/supply-demand-publish/index`

验证：

- `node --check` 覆盖上述 10 个页面和 `utils/universal-share.js`，通过。
- `rg -c 'onShareAppMessage'` 确认 10 个页面均有分享处理。
- `rg -c 'universalShareCanvas'` 确认 10 个 WXML 均有 canvas。
- `git diff --check` 通过。

资源工具剩余判断：

- P0/P1 代码主闭环基本完成。
- 真正阻塞真机测试的是测试后端未同步最新路由，以及小程序未上传最新体验版。
- 后续新增方向可进入 PC 端批量上传资源、支付/会员/分销/保证金方案拆解，但支付和保证金仍建议先走测试环境和灰度，不直接上生产。

## 2026-07-02 测试后端 P1 路由已同步

已处理：

- 只同步测试后端，未碰生产。
- 服务器备份：`/home/ubuntu/teamBuy-deploy-backups/20260702-103933-p1-test-routes`。
- 标准 `docker compose -f docker-compose.test.yml --env-file backend/.env.test -p teambuy-test up -d --build backend-test` 在 apt 阶段长时间无输出，已中止。
- 后改用测试容器热同步：
  - 同步本地 `backend/app` 到服务器 `/home/ubuntu/teamBuy/backend/app`。
  - `docker cp backend/app/. teambuy-test-backend-test-1:/app/app/`
  - `docker restart teambuy-test-backend-test-1`

公网验证：

- `/test-health`：200。
- `/test-api/opportunity-subscriptions/me?userId=user_a732f38f21`：200。
- `/test-api/opportunity-subscriptions` POST：200。
- `/test-api/supply-demand/cards` GET/POST：200。
- `/test-api/opportunity-leads?userId=user_a732f38f21`：200。

清理：

- 测试订阅 `opp_sub_e863a027c7` 已删除。
- 测试供需卡 `sd_17ca6283ba` 已归档。

后续注意：

- 标准测试镜像仍未完整重建成功；当前测试环境靠容器热同步生效。下一次重建镜像前要确保 `backend/app` 已在服务器宿主机保持最新。
- 真机仍需用户重新预览/上传小程序，才能看到最新前端分享层。

## 2026-07-02 本轮交接：个人资料弹层

已处理：

- `miniprogram/pages/profile/index.wxml`：
  - 头像本身作为 `chooseAvatar` 入口。
  - 微信号标题行新增“手机微信同号”快捷按钮。
- `miniprogram/pages/profile/index.wxss`：
  - 关闭 X 固定右上角，透明背景。
  - 头像按钮去掉浅色背景。
- `miniprogram/pages/profile/index.js`：
  - 新增 `handleUsePhoneAsWechat`，把手机号填入微信号。

验证：

- `node --check miniprogram/pages/profile/index.js` 通过。
- `python3 -m json.tool miniprogram/pages/profile/index.json >/dev/null` 通过。

状态：

- 未上传小程序，未部署后端。

## 2026-07-02 本轮交接：规则样本池测试后端部署

已处理：

- 已将本地 `backend/app` 同步到测试服务器并热拷进 `teambuy-test-backend-test-1`。
- 已重启测试后端容器，测试后端健康检查正常。
- 服务器备份目录：`/home/ubuntu/teamBuy-deploy-backups/20260702-140814-rule-learning-test`。

公网验证：

- `https://teambuy.lifelove.top/test-health`：200。
- `/test-api/ops-admin/rule-learning-samples` 无 Token：403，证明路由存在且权限生效。
- `/test-api/ops-admin/rule-learning-samples?cardType=service_offer` 带测试管理员 Token：200。
- 临时笔记确认类型生成规则样本链路已跑通：
  - 临时笔记：`note_8a82e792af`，已删除。
  - 验证样本：`rule_sample_7582ffaf54`，已标记为 `rejected`。

仍未完成：

- PC 样本池“审核通过”还不会自动变成全局规则。
- 后续要做规则发布/生效/回归，才能让服务、商机、日常等高置信判断持续自我迭代。

## 2026-07-02 本轮交接：资料库统一入口

已处理：

- `miniprogram/pages/library/index.*`：
  - 底部 Tab“资料”默认显示全部资料。
  - 去掉按 `workspaceMode` 自动套房源/商品/服务/日常入口过滤。
  - 新增 `libraryTypeFilters`：全部、房源、商机、服务、商品、日常。
  - 新增本地类型筛选 `activeLibraryType`，切换不重新请求后端。
- `miniprogram/pages/notes/index.*`：
  - 文案改为统一资料库。
  - 新增本地类型筛选和 `allNotes` 缓存。
  - 类型筛选不再每次打后端。

验证：

- `node --check miniprogram/pages/library/index.js` 通过。
- `node --check miniprogram/pages/notes/index.js` 通过。
- `python3 -m json.tool miniprogram/pages/library/index.json >/dev/null` 通过。
- `python3 -m json.tool miniprogram/pages/notes/index.json >/dev/null` 通过。
- `git diff --check` 通过。

状态：

- 只改前端，未上传小程序，未部署后端。
- 首页和雷达仍保留工作场景；目前先看“资料库统一入口”的实际体验。

补充：

- `miniprogram/pages/home/index.*` 已同步改成统一入口：
  - 首页不再展示“四场景选择”弹窗。
  - 首页顶部不再显示“当前场景 / 切换场景”。
  - 首屏主入口为添加资料、查看资料库、客户雷达。
  - 房源、商机、服务、商品、合集作为快捷工具。
  - 首页数据按全部资料展示，不再按工作场景过滤。

补充验证：

- `node --check miniprogram/pages/home/index.js` 通过。
- `python3 -m json.tool miniprogram/pages/home/index.json >/dev/null` 通过。

## 2026-07-02 本轮交接：资料库按钮与登录页收口

已处理：

- `miniprogram/pages/library/index.js`：
  - 修复 `handleManualAdd` 残留调用 `readWorkspaceMode` 导致“新增资料”点击无反应。
  - 现在按当前筛选类型跳转：商品到商品创建，房源到房源创建，服务/商机到服务方案，默认到普通资料。
- `miniprogram/pages/library/index.wxss`：
  - 商品/服务空态按钮改小胶囊并居中。
  - 生成合集、生成商品合集、重置筛选按钮改为小号限宽居中。
- `miniprogram/pages/login/index.wxml`：
  - 登录页文案从房源-only 改为 SCRM 获客口径。
  - 首屏标题为“把资料变成你的客户 SCRM”，不再主讲多类型资料归档。
  - 功能点为资料获客、客户雷达、线索跟进、成交复盘。

验证：

- `node --check miniprogram/pages/library/index.js && node --check miniprogram/pages/home/index.js && node --check miniprogram/pages/login/index.js` 通过。
- `miniprogram/pages/library/index.json`、`home/index.json`、`login/index.json` JSON 解析通过。
- `git diff --check -- miniprogram/pages/library/index.js miniprogram/pages/library/index.wxss miniprogram/pages/login/index.wxml miniprogram/pages/login/index.js` 通过。

状态：

- 本轮只改小程序前端，未部署后端。
- 用户截图仍看到旧登录页时，需要重新上传/预览小程序前端体验版。

## 2026-07-02 本轮交接：小程序前端切回生产环境

已处理：

- `miniprogram/app.js` 已从测试环境切回生产环境：
  - `apiRoutePrefix: ""`
  - `mediaRoutePrefix: ""`
  - `environmentName: "production"`
  - `apiBaseUrl` 保持 `https://teambuy.lifelove.top`

验证：

- `node --check miniprogram/app.js && node --check miniprogram/utils/request.js && node --check miniprogram/services/api.js` 通过。
- `buildApiUrl('/api/health')` 本地输出 `https://teambuy.lifelove.top/api/health`。
- `buildApiUrl('/media/example.jpg')` 本地输出 `https://teambuy.lifelove.top/media/example.jpg`。

状态：

- 只改前端配置，未部署后端。
- 用户可在微信开发者工具上传生产体验版/提交前端。

## 2026-07-02 本轮交接：生产后端不部署

检查结论：

- 本轮前端已经切生产配置，但后端不建议部署。
- 原因是本地后端混有二期商机、供需、资源钱包、H5、规则样本池等未统一验收改动，直接部署会有覆盖生产热修的风险。
- 生产当前健康：
  - `/health` 返回 `status=ok`，数据库为 Postgres。
  - `teambuy-backend-1`、`teambuy-backend-worker-1`、`teambuy-archive-worker-1`、`teambuy-postgres-1` 均运行中。

下一步：

- 用户先上传小程序前端。
- 后端二期上线前继续按 `docs/stage2-docs/37-production-hotfix-merge-plan.md` 合并统一分支并走测试环境回归。

## 2026-07-02 本轮交接：前端配置已切回测试

已处理：

- 用户已提交生产前端后，`miniprogram/app.js` 已切回二期开发测试环境：
  - `apiRoutePrefix: "/test-api"`
  - `mediaRoutePrefix: "/test-media"`
  - `environmentName: "test"`
  - `apiBaseUrl` 仍为 `https://teambuy.lifelove.top`

验证：

- `node --check miniprogram/app.js && node --check miniprogram/utils/request.js && node --check miniprogram/services/api.js` 通过。
- `buildApiUrl('/api/health')` 本地输出 `https://teambuy.lifelove.top/test-api/health`。

状态：

- 当前工作区小程序配置适合继续二期测试，不适合直接提交生产前端。

## 2026-07-03 本轮交接：协议与隐私政策入口

已处理：

- 新增：
  - `miniprogram/pages/legal/terms/index.*`
  - `miniprogram/pages/legal/privacy/index.*`
- `miniprogram/app.json` 已注册两个协议页。
- 登录页新增协议勾选，不同意不能登录。
- 个人资料弹窗新增协议勾选，不同意不能保存昵称、头像、手机号、微信号。
- 协议主体：湖南悦享互动科技有限公司。

验证：

- 相关 JS 语法检查通过。
- `app.json`、登录页、我的页和协议页 JSON 解析通过。
- 协议相关改动 `git diff --check` 通过。

状态：

- 当前工作区仍为测试环境配置。
- 若要提交审核/生产前端，需要先把 `miniprogram/app.js` 切回生产配置。

## 2026-07-02 本轮交接：生产配置与登录/头像点击反馈

已处理：

- 用户反馈生产前端登录头像和按钮仍像点不了，已重新切回生产配置：
  - `apiRoutePrefix: ""`
  - `mediaRoutePrefix: ""`
  - `environmentName: "production"`
- `pages/login/index.js`：
  - 微信登录点击后立即显示 `登录中` loading，接口成功或失败都会关闭。
- `pages/profile/index.*`：
  - 头像原生 `chooseAvatar` 按钮增加点击能力检测兜底。
  - 头像按钮点击热区明确化，头像图片不再吞点击。

验证：

- JS 语法检查通过：`app.js`、`login/index.js`、`profile/index.js`、`utils/request.js`、`services/api.js`。
- 登录/资料页 JSON 解析通过。
- 生产登录接口 `/api/auth/wechat-login` 路由存在，无效 code 返回 400 而不是 404。

状态：

- 当前工作区小程序配置是生产前端配置。
- 本轮未部署后端。

## 2026-07-03 H5 资源工具交接：mine 子页路径化

本轮已完成：

- 文件：`backend/app/static/h5/resource-tools/index.html`
- 新增 mine 子路径：
  - `?page=mine`
  - `?page=mine-received`
  - `?page=my-applications`
  - `?page=mine-history`
- `renderMine()` 已按当前子路径只渲染一个主体模块，不再把“收到申请 / 我申请的合作 / 历史”全部堆在同页底部。
- 顶部二级入口已改成明显的数字胶囊，可直接切页。
- `mine-history` 已从旧 detail 渲染方式改成正式列表视图。

本轮验证：

- 内联脚本解析通过。
- `git diff --check -- backend/app/static/h5/resource-tools/index.html` 通过。

下一步建议：

- 同步测试环境后，用以下路径逐个真机确认可见性与切换反馈：
  - `.../index.html?page=mine`
  - `.../index.html?page=mine-received`
  - `.../index.html?page=my-applications`
  - `.../index.html?page=mine-history`
- 如果二级 tab 视觉仍不够强，下一轮优先补 active 态颜色、下划线或分段导航条。

## 2026-07-12 本轮交接：企业微信会话归档 / 机器人 / 建群资料总索引

本轮已完成：

- 新增文档：`docs/wecom-archive-robot-group-reference.md`
- 已把以下内容集中整理：
  - 企业微信客服回调地址与相关配置
  - 会话内容存档 callback / pull / process / worker / SDK 配置
  - 智能机器人 `POST /api/robot/query` 的鉴权与能力边界
  - 企业微信群机器人 `GET/POST /api/wecom/group-bot/*`
  - PC 后台 `add_join_way` / `group-bot-channels`
  - 群二维码上传交接信息
  - 生产部署地址、项目目录、迁移建议、常见混淆点

推荐后续查阅顺序：

1. `docs/wecom-archive-robot-group-reference.md`
2. `backend/.env.example`
3. `backend/app/core/config.py`
4. `backend/app/api/routes_wecom.py`
5. `backend/app/api/routes_robot.py`
6. `backend/app/api/routes_ops_admin.py`

本轮状态：

- 未修改业务逻辑。
- 未部署测试或生产。
- 适合作为后续新项目迁移这些能力时的第一份交接材料。
# 最新交接补充：资料页 UI 重构（2026-08-13）

- 已完成资料页有资料态和零资料态前端重构，文件：
  - `miniprogram/pages/library/index.js`
  - `miniprogram/pages/library/index.wxml`
  - `miniprogram/pages/library/index.wxss`
  - `miniprogram/components/custom-nav/index.js`（显式标题优先，修复 Tab 保活时标题串页）
- 已做对抗式审查，结论与用例见 `docs/qa/资料页UI重构_对抗式审查与自测报告.md`。
- 未修改合集、新建资料二级页；未部署任何环境。
- 当前测试账号无资料，微信开发者工具已验收零数据态；下一步需要有真实资料的账号验证有数据卡片的封面、标签长度、发客户按钮和信号入口。
- 当前工作区仍有此前五阶段 SCRM 等大量未提交改动，不要回滚或覆盖。
# 最新交接补充：合集一级页（2026-08-13）

- 已重构：
  - `miniprogram/pages/showcases/index.js`
  - `miniprogram/pages/showcases/index.wxml`
  - `miniprogram/pages/showcases/index.wxss`
- 合集一级页现在与资料页使用同一信息架构和视觉语言；旧的数据、发布、下架、分享追踪与会员边界继续复用。
- 新建合集、编辑合集和合集预览页未修改，下一阶段应先讨论这几个二级流程再开发。
- 当前账号无合集，零数据态已在微信开发者工具验收；有数据态已完成逻辑测试，仍需真实合集账号补视觉截图。
- 自测报告：`docs/qa/合集一级页重构_数据逻辑自测报告.md`。

# 最新交接补充：内容编辑器产品结论（2026-08-13）

- 已完成但尚未开发：通用编辑器、文字/图片/文章链接展示、生成同款曝光规则和房源深度编辑器产品设计。
- 详细结论已保存到 `docs/stage2-docs/39-content-editor-product-design-decisions.md`，后续不得只依据聊天记录实现。
- 房源出租/出售单选且默认只显示当前值；腾讯地图用于客户导航至公开小区位置；具体楼栋、房号、密码及精确位置必须私密。
- 第一版取消作者选择“复制公开内容 / 仅复用样式”，避免扩大系统复杂度。
- 本阶段未改业务代码、未部署；下一步从商品/团购深度编辑器继续讨论。

# 最新交接补充：商品 / 团购深度编辑器（2026-08-13）

- 商品/团购的产品边界和高保真结构已获用户认可，结论已补入 `docs/stage2-docs/39-content-editor-product-design-decisions.md`。
- 一个编辑器通过单一销售方式切换咨询购买或团购接龙；第一版只做简单规格，不做商品支付和复杂电商后台。
- 配送、自提腾讯地图、接龙设置和私密进货管理渐进展开；接龙必须关联客户雷达。
- 尚未开发或部署；下一步讨论服务方案深度编辑器。

# 最新交接补充：服务 / 合作与电子名片（2026-08-13）

- 服务/合作已收敛为跨行业轻编辑器；此前复杂专业服务效果稿已废弃，以名称、介绍、图片/PDF/链接和附件 SCRM 为准。
- 电子名片以个人身份为核心，第一版每个用户一张主名片，最多关联 3 项精选资料。
- 已确认 4 个免费轻样式：商务蓝、简洁白、暖灰金、清新绿；样式只改视觉，不能修改内容、统计或创建新资料。
- 现有代码已有 4 个带行业默认内容的模板，开发时必须迁移为纯视觉样式，重点防止切换模板覆盖用户资料。
- 详细结论在 `docs/stage2-docs/39-content-editor-product-design-decisions.md`；本阶段未开发或部署。

# 最新交接补充：五类编辑器下一窗口开发资料（2026-08-13）

- 新窗口开发必须读取：
  - `docs/stage2-docs/39-content-editor-product-design-decisions.md`
  - `docs/stage2-docs/40-five-editor-data-route-attachment-spec.md`
  - `docs/qa/五类内容编辑器_测试清单与验收标准.md`
  - `docs/prompts/five-editors-development-codex-prompt.md`
- 已认可的7张高保真参考图位于 `docs/png/editor-design/`，必须作为UI验收标准。
- 当前只完成产品、数据、路由、附件和测试规格，尚未修改五类编辑器业务代码。
- 已知首要P0：真实PDF上传/预览、附件事件、统一路由、同一noteId、公开净化/生成同款共用策略、名片单一身份源。
- 用户要求五类一次开发完成后统一进行对抗式审查；不得完成一类就宣称整个阶段结束。

# 最新交接：五类内容编辑器已完成（2026-08-13）

- 五类编辑器及共同基础能力已经完成，不再是“尚未开发”状态。
- 核心新文件：
  - `miniprogram/utils/structured-editor.js`
  - `miniprogram/templates/structured-editor.wxml`
  - `miniprogram/templates/structured-editor.wxss`
  - `miniprogram/pages/property-editor/`
  - `miniprogram/pages/product-editor/`
- 服务和名片入口仍使用原路由目录，但内部已替换为轻量编辑器；通用文字/图片/链接继续使用 `pages/note-edit`。
- 后端已具备真实 PDF 校验/存储、统一附件模型、稳定附件 ID、公共配置全树净化、个人销售资料、互动事件和同款隔离。
- 唯一路由在 `miniprogram/utils/resource-navigation.js`；新增入口不得再自行拼接编辑页路径。
- 对抗式审查已统一完成，报告在 `docs/qa/五类内容编辑器_对抗式审查与修复报告.md`；自测报告在 `docs/qa/五类内容编辑器_Codex自测报告.md`。
- 最终验证：`backend/tests/test_app.py + backend/tests/test_sales_scrm.py` 175 passed；小程序全部 JS/JSON 静态检查、后端编译和 `git diff --check` 通过。
- 未部署生产、未上传体验版。下一步只做微信开发者工具/真机 UI 验收与合法域名确认，不要先部署。
- 当前工作区包含本轮之前的大量用户未提交改动；继续工作时必须保留，不要回滚或覆盖。

# 最新交接补充：真机有数据态 UI 回归修正（2026-08-13）

- 已修复资料/合集卡片、筛选弹层、结构化编辑器底栏和新建资料识别卡在窄屏下的按钮越界。
- 结构化编辑器现在按核心/更多/私密渐进展示；客户页和编辑预览已处理旧数据重复文案、空封面和本人不适用动作。
- 通用编辑器文案为“存”的可拖动保存浮球已移除；截图里的黑色“已连接/展开”和右侧绿色“…”仍是微信真机调试覆盖层，不属于仓库代码。
- 静态针对性验证通过；下一步必须在微信开发者工具重新编译，用用户截图中的旧房源与合集数据复查有数据态，不能沿用旧缓存截图判断。
- 未部署生产或测试服务器，未上传小程序。

# 最新交接补充：新建资料第一屏（2026-08-13）

- `pages/resource-create` 已按用户提供的第一步效果图收口：单一输入框、添加图片/链接、整理主按钮、房源/商品/服务方案/普通资料四个空白入口。
- 普通资料是通用兜底；PDF 和电子名片已从此页一级入口移除，但其底层能力未删除。
- 图片现在暂存到当前新建上下文，可与文字一起保存为同一份资料，不再点击后立即另建并跳走。
- 下一步应继续逐张对照第二步“确认识别”，不要重新扩展第一屏。
- 第一屏附件交互已补齐：图片调用相册/相机并暂存缩略图；链接通过底部输入面板加入当前资料，要求 HTTPS，支持标题与删除。

# 最新交接补充：新建资料入口与资料页细节（2026-08-13）

- `pages/resource-create` 已从旧版“放进笔记库 / 发送”改为新版统一创建入口：文字、图片、文章链接、PDF 四种来源，主动作统一为“继续整理”，并提供房源、商品/团购、服务/合作、电子名片四类空白起步。
- 识别结果只确认资料类型并进入对应深度编辑器，不再要求用户切换工作台；创建入口本身不承担发客户动作。
- PDF 路径为先上传真实附件、再创建单条 `UserNote` 并更新其 media；上传失败不会留下空草稿。
- 资料页固定“新建资料 / 资料助手”已改等宽，卡片更多按钮已下移并加重。
- 静态检查通过；下一步必须让用户在微信开发者工具重新编译，分别截图新建资料首屏、识别结果和资料有数据态。不得用旧缓存截图宣称未生效，也不得据此部署生产。

# 最新交接补充：房源编辑器高保真重构（2026-08-13）

- `pages/property-editor` 已从共享 `structured-editor` 中拆出，当前首屏顺序是客户效果预览、房源图片、核心字段，结构与用户最新效果图对齐。
- 租售使用 `structuredData.listingMode` 单选和唯一 `structuredData.price`；保存会清理旧 `rentPrice/salePrice/dealType`，预览和资料卡按租售状态生成正确标签及单位。
- 房源图片第一张是唯一封面真源，长按缩略图会移到第一张；PDF/链接仍在“更多房源信息”内按需展开。
- 资料列表卡已改为右上三个点、右下发客户/完善。
- 本轮仅修改本地小程序代码和文档，未部署生产、未上传体验版。下一步必须重新编译后用一个有多图的出租房源和一个出售房源分别真机验收，不要继续使用旧编译包截图。

# 最新交接补充：资料卡交互与房源租售互斥（2026-08-13）

- 资料库整卡主点击已统一为进入编辑，包括 `article`；“发客户 / 更多 / 雷达”是拦截冒泡的次级动作。
- 客户页按钮文案已缩为“好友 / 朋友圈”。
- 房源核心信息只渲染当前“出租”或“出售”标签和一个价格输入框，点击当前标签才弹出切换选项。
- 资料卡显示最近有效动作时间：最近编辑或最近发送，同时保留发送/打开次数。
- 本轮静态验证通过，未部署、未上传；真机验收应重点测试整卡、三个点、发客户三个点击区域不会串动作。

# 最新交接补充：URL 链接资料页恢复原产品约定（2026-08-13）

- 已回读用户指定的历史任务，链接资料不再使用截图中的旧 `note-edit` 插件页。
- 未整理链接进入 `link-confirm`；确认生成后和资料库内已整理 article 都进入新的 `pages/link-editor`。
- `link-editor` 只编辑封面、标题、来源、原文 URL、销售推荐语和内容简介；禁止显示轻 CRM、留言、预约、接龙和通用附件管理。
- 客户 `note-preview` 的 article 分支以“查看原文”为主动作，并记录独立 `source_open` 信号；作者本人预览不计入。
- 定向后端回归 2 项通过，JS/JSON、DOM 正反向断言、后端编译和 diff 检查通过。未部署、未上传。

## 2026-08-13 编辑器路由与旧页退役审计

- 通用 `note-edit` 尚承接文字、图片 OCR/图文和 PDF，因此当前不能整页删除。
- 路由已改为显式白名单：只有 `text_note/image_ocr/mixed_content/pdf_document` 能进 `note-edit`；URL、房源、商品、服务、名片进独立页；未知类型进确认页。
- `note-edit` 加了同样的运行时白名单守卫，避免绕过路由表直接打开旧 DOM。
- 已从普通资料 WXML 移除“添加功能/能力插件/留言/咨询/接龙”入口，回归内容整理和合集归档。
- 审计发现商品、服务、名片虽然路由独立，但仍共用 `templates/structured-editor.wxml`，不能视为已通过三张独立效果图的 UI 验收。
- 整页删除 `note-edit` 的门槛：新通用编辑器已承接上述四种模式；全库无路由/消息/旧卡片引用；旧数据回归通过；从 `app.json` 移除后小程序编译通过。

## 2026-08-13 资料库分享封面与分页缓存

- 资料库分享 `imageUrl` 现在固定按“营销 canvas HTTPS 图 -> 资料原图 -> `/static/workspace/workspace-notes.webp`”兜底，首次点击不再返回空图。
- 列表首页及每次触底均为 10 条，分享图仅为当前可见批次预生成。
- 前端列表优先使用 `resourceStore` 内存/本地缓存；后端合并列表有 20 秒进程内缓存，写操作主动失效。
- 未部署测试或生产；真机微信分享卡图仍需重新编译体验版后验收。
# 2026-08-14 交接：商品编辑器专属重构完成

- `miniprogram/pages/product-editor/` 已是商品专属页面，不再 include 旧共享编辑器模板。
- 新商品真源：`salesMode`、`variants[].priceFen`、`fulfillment`、`relayConfig`；历史商品字段继续只读兼容。
- `miniprogram/pages/note-preview/index.js` 已兼容新商品价格、履约、自提位置和销售方式。
- 分享首次点击空图问题已覆盖资料库、旧笔记列表、详情页和旧编辑页；三级兜底为生成图、原封面、默认封面。
- 需要用户在微信开发者工具上传体验版后真机确认：图片相册、地图选点、PDF、商品页面布局、咨询/接龙切换、首次发客户。
- 后端 pytest 尚未跑通，阻断为本机 Python 3.9 不支持项目使用的 `dataclass(slots=True)`；需用项目测试容器或 Python 3.10+ 环境运行。
- 当前工作区仍有大量本轮之前的未提交改动，后续不得回滚或覆盖。
# 2026-08-14 最新交接：资料库筛选面板与入口视觉已按新模型重做

- 筛选面板已改为“资料类型 / 客户进度 / 标签”三层；旧“分类”DOM 已移除。
- 标签支持搜索、热门标签和“查看全部标签”；底部按钮改为对称的“重置 / 完成（数量）”。
- 资料类型与客户进度显示当前数据数量，链接、电子名片、普通资料也纳入筛选类型。
- 资料库 tab 图标使用放大裁切资源，搜索旁筛选入口变为更明显的图标 + “筛选”文字。
- JS、JSON 和 diff 检查已通过；未部署、未上传，需微信开发者工具重新编译后确认真机滚动、长标签和 tabBar 图标视觉。

## 2026-08-14 最新交接：资料入库后端三条硬约束已落地

- 生命周期：新建、导入、识别、复制资料默认 `private`，通过 `POST /api/notes/{note_id}/publish` 才进入公开态；公开资料被编辑后自动退回私有草稿。
- 幂等：手动、快速、链接、图片、房源批量等创建请求支持 `intakeId + idempotencyKey`，重复请求返回原资料或原合集；PostgreSQL 有条件唯一索引，删除资料占用的键不可复用。
- 版本与边界：`revision` + `expectedRevision` 防止旧页面静默覆盖；公开接口、客户动作、浏览和互动都拒绝未发布资料；发布前检查公开投影并剥离私有字段/敏感联系方式。
- 展示页发布会同步发布有效资料并固化公开快照；名片精选资料仅包含同一拥有者、未删除且已发布的资料。
- 验证：`./.venv312/bin/pytest -q backend/tests` → 220 passed；Python compileall 和 diff 检查通过。
- 下一步：小程序创建/编辑流程接入幂等键、版本号和显式发布按钮，补保存失败重试、409 刷新提示；重新编译体验版后人工验收。未部署生产。
- 对抗式复查已补齐旁路编辑：整理、类型确认、生成场景、OCR、附件回填、认领、专题归档都会递增 revision；已发布资料发生这些变更时自动回到 private。

## 2026-08-14 最新交接：合集场景映射、合集选择与资料卡动作收口

- 后端 `ShowcasePage.sceneType` 与 `showcase_templates.py` 负责模板白名单；房源禁止 `brand_card`，场景不匹配的资料返回 400，公开快照再次归一化模板。
- `property_batch_collection` 仅保留为历史房源批量标识，前端和公开页按 `featured_window` 处理。
- `pages/showcase-edit` 已改成四段式合集编辑；`pages/showcases` 支持 `select=1&noteId=...&scene=...` 选择已有合集或新建合集。
- `pages/library` 三个点统一接入编辑、加入合集、复制、停止分享/重新发布、删除；卡片分享状态来自后端 `shareState`，撤回显示灰色“已停止分享”。
- 新增 `POST /api/notes/{note_id}/revoke`；撤回资料会清空其关联已发布合集快照，避免继续公开。
- 验证：后端 224 passed；小程序核心 JS 语法通过；旧模板选择页无代码引用。未部署生产，需用户在微信开发者工具重新编译体验版后统一人工验收。

## 2026-08-14 最新交接：合集编辑默认选择与客户页布局

- 新建合集默认 `activeGenerationMethod=manual`，无预选资料时 `selectedItems=[]`；显式 `method=filter`、条件筛选、专题预选和资料卡指定加入仍按原意工作。
- 合集编辑模板按 `sceneType` 白名单展示，并渲染模板预览图；远端图片失败时显示可见的场景色回退卡，不再把“模板丢失”误判为空模板。
- 模板预览改用小程序本地压缩静态设计稿（`/static/showcase/*.jpg`，220px 宽）；原 `teambuy.lifelove.top/media/showcase-templates/*.webp` 目前返回 404，不再作为首选资源。
- “资料排列”使用 `128rpx + 等宽双按钮` 网格，客户页列表/目录/故事/名片的标题与摘要限制行数，并缩小列表封面和查看按钮，避免文字撑高布局。
- 本轮仅改小程序合集编辑与客户展示样式及新建默认选择；未部署、未上传体验版。全部开发完成后需统一验收：新建空合集、显式批量生成、房源两模板预览、列表/双列、超长标题、图片失败回退。

## 2026-08-14 最新交接：体验版源码包体积优化

- 解决 `80051 source size 2956KB exceed max limit 2MB`：四张合集模板预览由大 PNG 改为 220px 宽 JPEG，实际代码仍使用 `/static/showcase/*.jpg`。
- 移出未引用的重复资料 tab 图标和旧结构化模板文件；未改动运行时路由或业务逻辑。
- 本地统计：非 Git 小程序源码约 2.19MB，zip 约 0.8MB；需微信开发者工具重新编译上传确认最终源码包低于 2MB。
- 2026-08-14 资料卡分享状态回归：`pages/library` 右侧主按钮和三个点菜单共用 `getCardShareState()`，分享封面异步完成不会改变私有/撤回状态；停止分享、重新发布调用 `loadCards({ forceRefresh: true })`；`resourceStore` 缓存升到 v2，避免旧版缺字段卡片继续复现。若真机仍显示“完善”，需区分资料确实为 `private`（编辑后必须重新发布）或旧 Card 无 `sourceNoteId`（仍是兼容旧卡片），不能用发送次数推断发布态。已通过 `node --check`、后端 224 tests、`git diff --check`；未部署、未上传体验版。

## 2026-08-14 最新交接：保存后资料误显示“完善”

- 根因已修复：保存后的私有资料不再被误判为半成品。资料库动作状态现在为：内容不完整“完善”、内容完成但私有“发布”、公开资料“发客户”、撤回资料“已停止分享”。
- 资料库主按钮和三个点菜单均可执行显式发布；发布成功后强制刷新首批资料，随后出现“发客户”。
- 链接、商品、房源、服务/合作、电子名片和普通资料编辑器主操作统一为“保存并发布”；顶部预览仍为私有预览，不会绕过发布安全检查。
- `api.publishNote` 发布后同步写入资料项缓存并失效资料列表缓存。
- 验证：前端相关 JS `node --check`、`git diff --check`、后端 224 tests 通过。未部署生产、未上传体验版；需重新编译后真机验证“微信文章选择 -> 保存并发布 -> 资料库显示发客户 -> 原生分享”。

## 2026-08-14 最新交接：发客户报错与五类分享链路复查

- 已定位并修复：导入/企业微信资料保存后仍为 `status=draft`，`POST /api/notes/{id}/publish` 因只接受 `active` 而报“资料尚未完成”。保存、整理、确认类型等统一内容编辑会把遗留 draft 推进为 active；发布接口也兼容已有内容的遗留 draft，并在版本校验和公开安全检查通过后才改变状态。
- 资料库后端列表新增 `sourceNoteStatus` 仅用于诊断和迁移；前端真实完成度只看真实标题 + 正文/封面，不再把 `status=draft` 或 `cardState=draft` 当成“完善”。五类编辑器底部主操作和资料卡主操作统一显示“发客户”，不再把内部“发布”暴露给用户。
- 分享边界保持不变：资料仍先私有保存，客户公开读取必须是 `shareState=published`；私有资料的“发客户”第一次点击仅完成发布准备，原生分享只在已发布态开放，避免异步发布与微信分享回调竞态。
- 对抗式检查结果：链接、商品、房源、服务/合作、电子名片和普通资料均走“编辑保存 → publishNote → 资料库强制刷新 → 原生分享”同一链路；撤回后主按钮置灰，重新发布走同一安全门禁；分享图缺失时有生成图/原图/默认图三级兜底；快速切换卡片不会复用上一张待分享封面。
- 验证：后端 225 tests 通过；相关 7 个小程序 JS `node --check` 通过；`git diff --check` 通过。未部署生产、未上传体验版，仍需微信开发者工具重新编译后人工验收五类资料各一条。

## 2026-08-14 最新补充：旧资料列表分享旁路已收口

- `pages/notes` 仍可从待处理/专题入口打开，原先所有行直接使用 `open-type=share`；现在只有已发布资料才显示原生“发客户”。私有完成资料先请求发布，内容不完整显示“完善”并进入对应编辑器。
- `notes.onShareAppMessage` 增加状态校验；资料库、旧资料列表、五类编辑器的发客户语义一致。
- 完成度判断不再依赖 `status=draft`，避免完整微信文章/链接因导入生命周期残留再次显示“完善”。
- 回归：后端 `226 passed`；相关 JS `node --check`、`git diff --check` 通过；未部署、未上传体验版。
- 历史 `card-view` 也只对 `Card.status=published` 显示发客户；新资料主链路不再被旧卡片分享按钮旁路。

## 2026-08-14 最新交接：资料卡“发客户”与三个点菜单

- 当前资料卡三个点不再显示重复的“发客户”；主按钮是唯一发送入口，菜单只承担编辑、合集、复制、停止/重新分享和删除。
- `pages/library.onShareAppMessage` 只使用当前卡片的 `sourceNoteId`，并再次检查 `shareState=published`；旧 dataset 或快速切卡不再允许构造错误的客户页链接。
- 远端测试环境核查结果：小程序当前配置是 `https://teambuy.lifelove.top/test-api`，该环境对 `/notes/{id}/publish` 返回 `Not Found`。本地代码已有该接口，但未部署到测试环境；因此重新编译体验版前不能据此判断发客户链路已通。
- 未部署生产，未上传体验版；待后端同步到 test-api 后，人工验收“私有完成资料首点准备、第二次发客户、停止分享、重新发布、三个点菜单”五条路径。

## 2026-08-14 最新交接：合集模板、客户页留白与底部双按钮

- 修复 `showcase-view` 目录模板的大段留白：移除内部模板的整屏最小高度，并让双列 Grid 从顶部收缩排列。
- 客户页按合集 `sceneType` 归一化模板；异常旧模板按场景默认值渲染，避免跨场景错误模板。模板缩略图仍有静态资源和失败回退两层保障。
- 合集三个点已收口：已发布为“客户反馈 / 编辑 / 停止分享 / 删除”，草稿和下架分别提供继续编辑/预览与重新发布；发客户不放进菜单。
- 筛选底栏与客户页双联系按钮已接入全局等宽双按钮规则。
- 静态验证通过；未部署生产、未上传体验版。人工验收需重新编译后检查：模板选择、目录页无空白、筛选底栏等宽、生命周期菜单和客户页打开。

## 2026-08-14 最新交接：展示页首屏与模板选择说明

- `miniprogram/pages/showcase-view/index.wxml` 已移除重复的 `custom-nav`，`index.json` 不再声明该组件；客户页沿用微信原生导航标题“展示页”，首屏内容不再为“资料整理助手”预留空间。
- 合集编辑页模板卡继续显示且可点选；模板卡是作者选择器，不是客户内容。编辑页新增“点选样式，预览客户页查看真实内容”提示；完整预览统一查看带真实资料的客户页。
- 未部署生产、未上传体验版；需重新编译后人工检查：客户页首屏是否紧贴内容、编辑页模板选择是否清晰、预览客户页是否按当前场景渲染。

## 2026-08-14 最新交接：客户雷达第一性原理重构

- 雷达页面已改成行动优先结构：今日摘要 → 待跟进优先队列 → 访客事实 → 资料优化；不再把装饰图、时间线、导出和未持久化的“已联系”混在主路径里。
- 队列卡片展示匿名/实名身份、来源资料、可验证行为证据、排序原因和明确动作；“查看资料 / 复制首句”与“生成对比”均为真实绑定动作，两个操作等宽。
- 加载中、接口失败、会员锁定分别渲染；接口失败提供“重新读取”，不会错误引导会员购买。会员锁定保留免费汇总和匿名信号预览。
- `onShow` 不再强制刷新资料库；同一场景和资料筛选 30 秒内复用已加载结果，资料库只作同步缓存回退。
- 验证：`node --check miniprogram/pages/visits/index.js`、`node --check miniprogram/stores/resource-store.js`、相关 `git diff --check` 通过。未部署生产、未上传体验版；下一步由用户在开发者工具重新编译，人工验证会员态、失败重试、真实信号、匿名访客、来源跳转和小屏按钮布局。

## 2026-08-14 最新交接：会员页第二版与推广规则说明

- `pages/membership` 已按第二版效果稿重构：客户行为信号主卡、雷达视觉、四项权益、推广收益卡、脱敏访客预览和数据保留说明。
- 会员说明与推广奖励规则均为可展开内容，避免首屏过载；推广卡和“进入推广中心”均可进入 `pages/referral-center`。
- `pages/referral-center` 已补完整用户文案：邀请码用途、一级直接邀请、有效订单实付金额 50%、不做二级自动奖励、退款/异常交易处理、提现手续费 0。
- 当前仍未部署、未上传体验版；`node --check`、JSON 解析和 `git diff --check` 已通过。人工验收需检查会员页首次加载、已开通状态、规则展开、推广中心跳转和真机按钮/字体布局。

## 2026-08-14 最新交接：分享链接自动归因与会员资格分佣

- 当前分享主链路不再展示或要求邀请码。资料库、旧资料列表、编辑页、`note-preview`、`showcase-view`、历史 `card-view` 分享链接都会携带 `from`，登录用户打开后异步调用 `/api/scrm/referrals/share-bind` 建立首次触达关系。
- 后端新增幂等分享归因接口；已存在其他推广人的关系不会覆盖，也不会阻塞打开。邀请码绑定接口仍保留给旧数据/兼容调用，但当前推广中心不再使用。
- 会员订单确认时校验推广人是否为有效付费会员：有效时按好友实付金额 50% 生成一级奖励；推广人到期期间不产生奖励，重新付费后只对后续好友订单生效，不追补错过月份。
- 推广中心和会员页文案已经同步说明以上规则。
- 验证：`./.venv312/bin/pytest -q backend/tests`（227 passed）；后端编译、相关小程序 JS `node --check`、`git diff --check` 通过。未部署生产、未上传体验版；需开发者工具重新编译后人工测试真实分享打开、登录补归因、有效会员/到期会员两种分佣结果。

## 2026-08-14 最新交接：客户雷达指标快捷筛选

- 四个摘要指标已成为快捷筛选：待跟进/高意向进入待跟进队列，访客/有动作进入访客列表；高意向和有动作会实际过滤卡片。
- 快捷筛选有明确的当前状态和清除入口；手动切换 Tab 会恢复完整数据集。
- 资料优化卡片改为“编辑资料”，按资料类型进入对应编辑器；客户详情/轨迹页尚未接入，等待下一轮字段方案确认。
- 验证：`node --check miniprogram/pages/visits/index.js`、相关 `git diff --check` 通过。未部署、未上传体验版。

## 2026-08-14 最新交接：客户详情页（已完成初版）

- 新增页面：`miniprogram/pages/customer-detail/index.*`；路由已注册到 `miniprogram/app.json`。
- 雷达待跟进/访客卡片的身份标题可进入详情页，传递稳定 `customerId`、可选 `leadId` 和当前场景；无稳定 ID 的卡片不会出现空跳转。
- 详情页包含身份卡、下一步建议、相关资料、标签、动态和固定底部操作。相关资料可打开原资料；复制话术、生成对比、记录跟进、联系客户均有独立处理。会员锁定跳会员页，受限身份仅允许继续观察。
- 已移除详情页未定义动作的“更多”文案，避免出现不可点击 DOM。
- 人工验收顺序：会员用户打开实名雷达卡 → 检查身份/来源/证据/下一步 → 打开相关资料 → 复制话术 → 生成对比 → 有 leadId 时记录跟进；再验证同行/匿名、缺少 customerId、非会员锁定和网络失败。
- 验证结果：详情页/雷达 JS `node --check`、后端 `python3 -m py_compile`、JSON 解析、相关 `git diff --check` 通过；`.venv312/bin/pytest -q backend/tests/test_sales_scrm.py` 10 项通过，后端运行时导入通过。未部署生产、未上传体验版。
- 对抗式审查剩余风险（非 P0）：详情页暂复用全量客户智能接口；相关资料封面暂用类型占位；无 leadId 的资料只能提示从资料页建立跟进档案。后续若需要规模化和更高保真，再拆分 customerId 详情接口、补封面字段和新建跟进动作。

## 2026-08-14 最新交接：雷达卡片点击与合集编辑页留白修复

- `showcase-edit` 已移除重复的自定义导航，页面保留微信原生标题“编辑展示页”，顶部不再叠加“编辑合集”和额外安全区。
- 雷达待跟进/访客卡片已整卡绑定客户详情；资料来源与按钮使用 `catchtap`，不会误触客户详情。高意向卡片有黄色矩形边框和浅黄色高亮。
- 人工验收：合集编辑页首屏不应再出现重复标题和大块空白；雷达点击卡片头像、姓名、证据、标签、下一步均进入客户详情；点击查看资料、复制话术、生成对比仍只执行自身动作；高意向卡片明显为黄色高亮。
- 静态验证：雷达/合集编辑 JS `node --check`、JSON 解析、重复导航搜索、相关 `git diff --check` 通过。未部署、未上传体验版。

## 2026-08-15 最新交接：客户详情缓存与会员显示门禁

- 新增 `miniprogram/stores/customer-intelligence-store.js`，雷达与客户详情共享 60 秒进程内缓存，并合并同一用户/场景的并发请求。
- `miniprogram/pages/customer-detail/index.js` 打开详情时优先复用雷达刚读过的客户智能结果；只有缓存失效或冷启动才重新读取。
- 雷达锁定态不再直接渲染会员权益卡：没有客户信号时显示中性空态；检测到真实信号后，只有点击顶部指标才弹出“查看权益”提示。
- 个人页“解锁客户信息链”入口使用同一门禁；无客户信号时隐藏，有信号时点击弹出提示，已付费用户仍可查看会员页。
- 相关风险：冷启动首次打开仍需一次客户智能网络请求；客户详情暂无按 `customerId` 的窄接口。两者属于后续性能优化，不改变当前权限边界。
- 本轮验收：执行 JS/JSON/`git diff --check`，未部署生产、未上传体验版。

## 2026-08-15 最新交接：我的页会员/推广门禁与登录身份收口

- `miniprogram/pages/profile/index.*`：商业入口文案为“会员”“推广收益”，并由客户信号统一控制；无信号时整个销售客户助手区隐藏且不请求推广中心。
- `miniprogram/pages/profile/index.wxml`：设置区增加“隐私说明”，指向现有隐私政策；会员协议没有加入个人页设置项，资料保存仍保留必要的用户服务协议/隐私政策确认。
- `miniprogram/pages/login/index.js`：真实微信登录只发送 `code`，移除随机昵称；本地便捷登录使用固定“测试用户”，避免每次登录变名。
- `backend/app/schemas/auth.py`、`backend/app/services/app_service.py`：微信登录字段改为可选，空/默认资料不会覆盖已有昵称、头像、手机号和微信号；首次登录使用“微信用户”占位。
- `miniprogram/pages/note-preview/index.js`、`miniprogram/pages/showcase-view/index.js`、`miniprogram/utils/dashboard.js`：清理公开事件和工具层的随机昵称回退。
- 验证：JS/JSON/Python 编译、WXML 标签平衡、`git diff --check` 通过；后端全量测试 227 passed。未部署生产、未上传微信体验版。
- 已知边界：真实微信昵称/头像不会由 `wx.login` 自动返回；当前不阻塞登录，用户可在“个人资料”中主动完善。冷启动仍需一次客户智能请求，属于后续性能优化。

## 2026-08-15 最新交接：会员日期与微信头像昵称组件

- `miniprogram/utils/date-format.js` 新增 `formatDateOnly`；个人页和会员页显示 `expiresAtText`，只展示“YYYY年M月D日”，不改变后端完整时间戳。
- `miniprogram/static/icons/membership.svg` 新增会员徽章；个人页会员卡左侧已从“会”字改为徽章图标，并收紧日期、按钮和背景装饰的布局。
- `miniprogram/pages/login/index.*` 新增可选微信头像/昵称填写区；登录页使用 `chooseAvatar` 和 `type="nickname"`，登录成功后头像临时文件会通过既有上传接口转为服务端资源。
- `miniprogram/pages/profile/index.*` 的个人资料头像按钮改为 `chooseAvatar`，删除旧相册选择函数；昵称输入继续使用 `type="nickname"`。
- 已知边界：微信号仍需用户主动填写，不能从 `wx.login` 自动读取；老的本地随机昵称不会凭空变成真实微信昵称，用户需要重新登录或在个人资料中选择/填写一次。
- 验证：JS/JSON/WXML/日期格式检查、旧相册入口搜索、`git diff --check` 和后端 227 项测试通过。未部署生产、未上传微信体验版。

## 2026-08-15 最新交接：登录页去除冗余大 Banner

- `miniprogram/pages/login/index.wxml` 删除“登录后保存到你的账号”及长说明，登录页底部改为轻量头像/昵称行、协议确认和登录按钮。
- `miniprogram/pages/login/index.wxss` 去除登录面板的边框、背景和阴影，保留必要间距，不再形成第二个大白卡。
- 头像昵称可选填写、微信一键登录、本地便捷登录均保留，未改变登录流程或隐私确认。
- 验证：登录页 JS/JSON/WXML 检查、冗余文案搜索、`git diff --check` 和后端 227 项测试通过。未部署生产、未上传微信体验版。

## 2026-08-15 最新交接：登录页纯一键登录与微信同号动作修复

- `miniprogram/pages/login/index.*` 已移除头像昵称填写区，登录页只保留协议确认、微信一键登录和本地便捷登录；用户资料统一在个人页编辑。
- `miniprogram/pages/profile/index.wxml` 的“手机微信同号”改为紧凑“同号”文本动作，`profile/index.js` 使用 `setData` 回调确保手机号同步到微信号输入框。
- 头像、昵称仍可在个人资料页通过 `chooseAvatar` / `type="nickname"` 编辑，不影响登录建立 openid。
- 已知边界：点击“同号”前必须先填写手机号；无手机号时继续提示“请先填写手机号”。

## 2026-08-15 最新交接：我的页按效果稿重排

- `miniprogram/pages/profile/index.wxml` 已移除“当前使用场景”和旧的销售客户助手标题；页面结构收敛为身份卡、我的资产、客户信号/推广收益、设置与帮助。
- 资产面板包含资料、合集、我的名片和消息；设置列表包含个人资料、隐私说明、帮助与反馈、退出登录。未增加“悦享互动宝”等品牌导航。
- `membershipVisible` 在每次进入页面时先重置为 `false`，仍只由真实客户智能信号决定；无信号时会员和推广收益整块隐藏且不请求推广中心。
- 身份副标题由 `salesProfile.jobTitle/city` 组合，没有资料时显示“资料、资源和消息。”；新增 9 个轻量 SVG 图标，均为小程序本地静态资源。
- 静态验收：个人页 JS/JSON/WXML、SVG XML、`git diff --check` 通过；SCRM 定向后端测试 10 passed。待开发者工具编译后对照效果稿检查头像兜底、资产入口点击、客户信号门禁、长昵称和小屏按钮布局；本轮未部署生产、未上传体验版。

## 2026-08-15 最新交接：身份卡按钮与文字布局修正

- 身份卡右侧按钮已从“编辑资料”改为“编辑”，宽度固定为 96rpx；头像固定 128rpx，中间身份区使用剩余空间。
- 昵称和副标题采用单行省略，修复小屏截图中“微信用户”和“资料、资源和消息。”被压成多行的问题。
- 静态检查通过；需开发者工具重新编译后确认真实机型上的按钮宽度与省略效果。

## 2026-08-15 最新交接：身份卡编辑按钮比例二次修正

- “编辑”按钮现在固定为 160rpx × 64rpx，`flex-basis/min-width/max-width` 同步限制，防止原生 button 在真机上变成长条。
- 仍保留单行昵称和副标题省略；需重新编译确认截图中按钮横向更短、纵向更厚，且中间文字恢复可读宽度。
- 验证：登录/个人页 JS/JSON/WXML 检查、旧登录资料控件搜索、`git diff --check` 和后端 227 项测试通过。未部署生产、未上传微信体验版。

## 2026-08-15 最新交接：统一对抗式审核与生产状态

- 后端新增 `backend/app/services/session_token.py` 及生产认证中间件：受保护 API 必须 Bearer token，用户身份字段与 token 不一致时返回 403；登录/资料更新返回新 token。
- 前端 `request.js` 自动注入 token；客户智能、资料、展示页、媒体缓存增加环境/用户隔离、TTL、并发合并和登出清理，首页复用客户智能缓存避免重复请求。
- 全量后端测试 228 passed；Python compileall、相关 JS/JSON/WXML/SVG 和 `git diff --check` 通过。
- 生产已运行在 `https://teambuy.lifelove.top`，teamBuy 后端使用 8004，Nginx 路由已切换；`/health` 200、未授权 401、跨账号 403、自有账号 200 均已实测。部署备份：`/home/ubuntu/teambuy-backups/teambuy-predeploy-20260815-140309/`。
- 生产剩余注意：本次运行时使用已有基础镜像并挂载最新源码，OCR 自动模式在缺少 Paddle 依赖时会降级；核心资料/SCRM API 不受影响，但若首版依赖 OCR，应先补完整 OCR 镜像。
- 小程序配置已切到生产 `/api`、`/media` 和 `environmentName=production`。开发者工具 CLI 上传因登录态失效未完成，当前扫码登录窗口等待用户确认；上传成功后还需在微信公众平台手动提交第一版审核。

## 2026-08-15 最新交接：小程序上传由用户手动完成

- 用户确认由本人使用微信开发者工具上传当前 `miniprogram` 目录，并在微信公众平台完成第一版审核提交。
- Codex 已停止 CLI 扫码/上传尝试；生产后端和代码验证不受影响。

## 2026-08-15 最新交接：资料/合集切换缓存优化

- `pages/library` 现在先渲染 `resourceStore` 的内存/本地快照，再后台刷新资料；专题和合集数量不再阻塞资料首屏。
- `resource-store` 能识别空列表快照，首次空资料用户不会因 `length === 0` 每次重复请求；刷新请求按用户合并且 60 秒内不重复发起。
- `services/api` 为专题和合集列表增加环境/API 前缀/用户隔离的 5 分钟缓存、内存 + storage 双层读取、并发合并和写操作失效；合集页强制重试仍会绕过缓存。
- `AppService.list_showcases` 增加 30 秒用户级进程缓存，合集创建、编辑、发布、下架、删除和关联资料删除会失效；公开事件不主动清空列表缓存。
- 资料/合集列表取消进入页面时批量生成分享封面；“发客户”仍保留按需生成，合集首次点击显示“准备封面”，生成成功后才显示可分享按钮，资料分享继续使用已有封面或默认兜底。
- 验证：相关 JS `node --check`、后端 `py_compile`、`git diff --check`、后端全量 `228 passed`。
- 当前状态：本轮尚未部署后端，也未上传小程序体验版；要验证真实切换速度，需要后端发布本轮代码并由用户在开发者工具重新编译上传。

## 2026-08-15 最新交接：雷达页面前后端缓存复核

- 雷达页仍使用 30 秒页面结果缓存；客户雷达与客户详情共用 60 秒小程序进程缓存，缓存命中时不再显示 loading，缓存只有 32 条上限且不写入本地存储。
- 雷达请求增加请求序号，旧请求晚到时不会覆盖新场景；资料来源筛选使用本次请求快照，避免返回后误套用已变化的筛选条件。
- 后端 `/api/scrm/customer-intelligence` 新增 15 秒、按用户/请求者/场景隔离的进程缓存；命中前复核请求者和会员状态。展示页、浏览/资料互动、客户动作、线索和会员状态写入会清理对应缓存。
- 会员测试开通成功后，会员页会立即清理当前用户的小程序客户智能缓存，回到雷达不会继续显示旧锁定态。
- 验证：`node --check`（雷达页、会员页、客户智能 store）、`python3 -m py_compile backend/app/services/app_service.py`、JSON 解析、`git diff --check` 通过；后端全量测试 228 passed。
- 本轮只修改代码和文档，尚未重新部署生产；后端部署后才会启用服务端缓存，小程序需用户重新编译/上传体验版后才会启用前端修复。

## 2026-08-15 最新交接：雷达缓存生产已发布

- 后端源码已发布到生产 `/home/ubuntu/teamBuy`，生产 Compose 现行 API 容器监听 8004；backend、backend-worker、archive-worker 已重启并运行。
- 部署备份：`/home/ubuntu/teambuy-backups/teambuy-predeploy-20260815-154948/`。服务器 `.env`、密钥、媒体卷和数据库卷未覆盖。
- 公网验证：`/health`=200；SCRM 客户智能路由无 token=401；模板媒体资源=200。
- 小程序代码已切生产：`https://teambuy.lifelove.top`，API `/api`、媒体 `/media`。用户仍需在微信公众平台确认 request/uploadFile/downloadFile 合法域名，并在开发者工具重新编译上传后再提交审核。
- 上传封装已补 Bearer token：`miniprogram/services/api.js` 的资料附件和图片资料上传现在与普通请求使用同一会话头；必须重新编译上传小程序后才能消除生产日志中的 401。

## 2026-08-15 最新交接：我的页名片分享图与读取速度修复

- `miniprogram/pages/profile/index.*`：已发布电子名片现在直接使用 `open-type="share"`；分享回调使用电子名片专用画布生成图，不再使用头像或 `/static/workspace/workspace-notes.webp` 作为名片封面。生成期间显示“准备中”，失败显示“重试分享图”。
- `miniprogram/services/api.js`：新增 `fetchCardsMetadata`，个人页不再等待所有资料媒体缓存；个人页使用用户/环境隔离的进程内 60 秒缓存，陈旧数据最多 10 分钟可先展示并后台刷新。
- `AGENTS.md` 已写入首屏和缓存性能基线，后续必须检查元数据/媒体解耦、缓存隔离、TTL、并发合并和冷/热读取耗时。
- 验证：相关 JS `node --check`、68 个 JSON 解析、`git diff --check`、后端全量测试 228 passed。
- 当前未重新部署生产、未上传微信体验版。用户需在微信开发者工具重新编译并上传体验版，然后人工验证：进入“我的”基础内容快速出现；已发布名片显示“发名片”；分享卡包含完整名片信息；未发布名片仍进入编辑器。

## 2026-08-15 最新交接：个人页轻量摘要、资料名片发送与样式同步

- 个人页现在调用 `/api/notes/business-card-summary`，不再用全量 `/api/cards` 构造首屏；摘要缓存按环境/API/用户隔离，60 秒命中，10 分钟陈旧可读并后台刷新。此前对象摘要被数组命中判断挡住的问题已修复。
- 个人页、资料库、资料列表的名片发送统一生成完整名片分享图，优先读取当前用户销售资料；资料列表名片优先生成，未准备好时显示“准备中/重试分享图”，不再发送头像或通用资料图。
- 名片编辑器选择样式后，实时预览会显示样式名称和明显配色；保存后客户页和分享图均读取同一个 `visibilityConfig.displayConfig.styleId`。
- 验证：相关 JS、68 个 JSON、`git diff --check`、后端全量测试 `229 passed`。未部署生产、未上传小程序体验版。
- 人工验收：重新编译上传体验版后，冷启动/返回“我的”应先显示身份与资产；资料库和资料页名片按钮应先准备完整名片图再打开分享面板；逐个切换四种样式，编辑器、客户页和分享卡应保持同色系差异。
- 兼容说明：若小程序先于后端摘要接口上传，个人页只对 404 使用旧 metadata 兼容回退以保证可用；后端发布新路由后才会获得完整的轻量读取收益。

## 2026-08-15 最新交接：分享静态图快照插件

- 前端插件：`miniprogram/plugins/share-snapshot/index.js`；资料/名片/合集的发送图统一按实体版本复用静态快照。
- 后端接口：`PATCH /api/notes/{noteId}/share-snapshot`、`PATCH /api/showcases/{showcaseId}/share-snapshot`；资料校验 `revision`，合集校验 `snapshotVersion + updatedAt`。
- 旧图保留 30 天后由现有 archive-worker 清理；本地媒体和对象存储均有安全删除入口。新图失败不会删除当前图。
- 已接入资料列表、资料库、资料预览、合集编辑/列表/客户页；已有数据缺少快照时，进入发送入口会按需补生成。
- 旧图从 `retiredAt` 起保留 30 天；外部域名媒体 URL 不会进入删除流程，客户公开资料不返回历史快照元数据。
- 定向回归 6 passed；完整后端测试在 JSON 隔离环境下 230 passed，另有 1 项环境配置断言因 `DATABASE_BACKEND=json` 与其期望的 Postgres 不同而失败。当前未部署生产、未上传微信体验版；后续由用户在开发者工具重新编译上传体验版验证真实发送图和首次生成耗时。

## 2026-08-15 最新交接：无图资料分享卡

- `business-card-share.js` 现在对无封面、图片下载失败和图片解码失败统一生成类型化信息视觉区：房源/商品/服务/普通资料分别使用房屋/商品/服务/文档图形与字段。
- 无图卡仍会上传并保存为分享静态快照；发送入口只读取快照，不退回头像、通用封面或不相关图片。
- 当前未部署生产、未上传微信体验版。人工验收需覆盖：无图房源、无图商品、无图普通资料、失效图片 URL 四种情况，并确认卡片仍可读且不会误导客户。

## 2026-08-15 最新交接：统一缓存策略与分享快照去重

- 前端统一策略文件为 `miniprogram/utils/cache-policy.js`：列表 60 秒、分类/专题 10 分钟、客户智能内存 60 秒、雷达页面 30 秒、媒体 7 天且最多 80 项/32MB。
- 资料库和资料列表使用 metadata-only 读取，首屏不再等待附件下载；媒体缓存只处理封面及每卡最多 3 张图片，PDF/视频不保存为本地文件。
- 分享快照插件现在有同版本跨页面并发锁和 10 分钟/64 项内存复用；旧列表快照不会导致重复 Canvas/上传，后端版本化快照仍是最终真源。
- 空数组缓存、分类 TTL、资源详情按用户隔离和退出登录清理已补齐；客户身份/联系方式仍不写入本地持久化缓存。
- 验证：前端缓存/分享冒烟测试通过，相关 JS `node --check`、JSON 解析、`git diff --check` 通过；当前环境没有 pytest 和 Docker，因此后端全量测试未执行。未部署生产，未上传微信体验版。

## 2026-08-15 最新交接：订阅消息模板配置

- 后端 `Settings` 新增 `wechat_miniapp_subscribe_template_id`，环境变量名为 `WECHAT_MINIAPP_SUBSCRIBE_TEMPLATE_ID`。
- 当前模板为微信公众平台“未读消息提醒”，模板编号 654，模板 ID 已写入本地 `backend/.env` 和 `backend/.env.test`；示例配置使用占位符。
- 本轮没有实现订阅授权、消息发送、一次性额度消费或会员推送开关；后续开关入口建议放在客户雷达的通知设置。
- 未部署生产、未上传小程序体验版；生产服务器环境变量仍需用户按部署流程单独配置后才会生效。

## 2026-08-16 最新交接：客户查看订阅消息闭环

- 已实现：点击授权、服务端授权额度幂等、浏览事件分层、30 分钟去重、后台发送 worker、失败/重试/释放额度和雷达偏好设置。
- 接入入口：资料列表/资料库、资料预览、合集列表/编辑/客户页、电子名片、旧卡片页面的“发客户/分享”按钮。
- 后端新增订阅配置/授权/偏好接口，新增 PostgreSQL/JSON 状态：通知偏好、订阅额度、订阅投递；worker task 名称为 `wechat-subscription-send`。
- 本地与服务器已配置模板 ID、模板字段键和跳转页；服务器原 `.env` 备份在 `/home/ubuntu/teambuy-backups/teambuy-subscription-env-20260816/backend.env`。本轮没有重启或重新部署生产，服务器当前旧容器尚未加载本轮代码。
- 已从线上“未读消息提醒”模板详情确认字段键为 `thing13/thing16/thing14/thing3/time11`；生产 `WECHAT_MINIAPP_SUBSCRIBE_FIELD_KEYS` 已按此映射修正。
- 上线动作由用户完成：先部署后端并重启 backend/backend-worker，再在微信开发者工具重新编译并上传体验版；不要只上传小程序而不部署 worker。
- 验证：后端全量 `233 passed`；相关 Python 编译、JS `node --check`、`git diff --check` 通过。
- 对抗式复核另修正：资料预览的客户留资手机号不再写入 `wx` 持久化存储，仅保留当前进程内存。

## 2026-08-16 最新交接：微信支付 API v3 闭环

- 已实现会员微信小程序支付：服务端统一下单和签名，小程序 `wx.requestPayment`，公开回调验签/解密，订单与权益幂等落库。
- 新增接口：`POST /api/scrm/membership/orders/{order_id}/pay`；微信回调：`POST /api/scrm/wechat-pay/notify`。
- 本地 `backend/.env` 已登记商户号 `1111636477`、API v3 Key、商户证书序列号和密钥路径；`backend/secrets/` 已复制商户私钥、商户 API 证书和微信支付公钥，文件均为 `0600` 且被 Git 忽略。
- 源项目没有找到微信支付公钥 ID/平台证书序列号；`WECHAT_PAY_PLATFORM_CERT_SERIAL_NO` 仍为空，`WECHAT_PAY_ENABLED=false`，不会发起扣款。不能把商户证书序列号当作平台验签 ID。
- 开启真实支付前必须补齐对应的 `WECHAT_PAY_PLATFORM_CERT_SERIAL_NO`（或确认平台证书序列号）、确认小程序 AppID 与商户号已绑定，并复核 `WECHAT_PAY_NOTIFY_URL`、支付权限和生产部署版本。
- 生产上线仍由负责人安排：先备份服务器 `.env`，部署同一版本后重启 API/worker，再在微信后台确认支付权限和回调地址；本轮未部署生产、未重启容器、未上传小程序体验版。
- 验证：OpenSSL 成功读取三份支付 PEM，API v3 Key 长度为 32，商户证书序列号映射一致，三份密钥文件均被 `.gitignore` 忽略；当前可用 Python 3.9 环境缺少项目依赖，支付专项测试未能启动。之前完成的代码测试结果仍为后端全量 `237 passed`、支付专项 `15 passed`。

## 2026-08-16 最新核查：旧项目没有平台验签序列号，teamBuy 已补入

- 已搜索 `/Users/yiyi/Desktop/Desktop/vedo-project` 及其 Desktop 外部副本，没有找到 `WECHAT_PAY_PLATFORM_CERT_SERIAL_NO` 或平台公钥 ID配置。
- 旧项目使用 `myproject/certs/pub_key.pem` 固定验签；`core/certKey.py` 的 `verify_signature()` 不强制匹配回调头 `wechatpay-serial`，所以旧项目的 `serial_no` 不能作为 teamBuy 的平台验签配置。
- teamBuy 当前不降级复用旧项目逻辑；负责人已从微信商户平台取得公钥 ID 并写入本地 `WECHAT_PAY_PLATFORM_CERT_SERIAL_NO`。具体值不写入交接文档。
- 当前代码会先匹配回调头 `Wechatpay-Serial`，再使用本地公钥验签；平台 ID 配置已通过非空检查，公钥 PEM 可读。真实支付开关仍为 `false`，尚未部署生产。

## 2026-08-16 最新交接：支付、订阅消息与静态快照已部署生产

- 生产备份：`/home/ubuntu/teambuy-backups/teambuy-predeploy-20260816-031413-payment-subscription/`。
- 生产已同步后端支付、订阅消息和静态分享快照代码；`backend`、`backend-worker`、`archive-worker` 已重建启动，Postgres 未重建。
- 生产微信支付已开启：`WECHAT_PAY_ENABLED=true`；支付私钥、商户证书、微信支付公钥已放入 `backend/secrets`，容器路径为 `/app/secrets/...`，文件权限为 `0600`。
- 生产订阅模板配置已存在，订阅授权/额度/投递 worker 已同步；资料和合集静态分享快照接口及旧图清理逻辑已同步。
- 公网验证通过：`/health=200`、支付配置 `missing=[]`、无签名支付回调返回 401、支付/订阅/静态快照路由存在、三个服务无启动异常。
- 当前阻塞在小程序端人工验收：必须在微信开发者工具重新编译并上传体验版，才能测试 `wx.requestPayment`、`wx.requestSubscribeMessage` 和真实静态卡片发送。不要重复部署后端。
- 支付人工验收顺序：会员页发起真实订单 → 完成支付 → 等待服务端回调 → 确认会员状态和权益到账；失败时记录订单号和时间，不要重复点击造成多笔订单。

## 2026-08-16 最新交接：多项目共享企业微信会话存档核心

- 平台代码在 `platform/wecom-archive-core/`，运行时应只有一个 Finance SDK worker 和一个全局 `seq` 游标。
- teamBuy/Petlove 的共享消费端已经写入本地，但当前仍未切生产；生产仍需先部署 core PostgreSQL/API/worker，再配置两个项目路由和独立 token。
- 企业微信唯一回调建议使用 `https://api.lifelove.top/wecom/archive/callback`，Nginx 需要把该地址转到 core 8050，旧 Petlove callback 不能继续作为主入口。
- 切换前必须确认真实 `chat_id` 路由；空 `chatIds` 默认不投递。切换期间不得同时开启项目直拉 worker。
- 当前没有部署生产或上传小程序；下一步是 staging/生产分阶段部署、单条真实消息路由验收、媒体代理验收和重复事件回放验收。

## 2026-08-16 最新交接补充：共享核心安全收口

- 核心媒体代理已经加入项目投递归属校验；新项目历史补投使用 `/v1/admin/replay/{projectId}`，不动全局游标。
- Petlove 共享事件在业务入库后立即 ACK，汇总转后台任务；teamBuy 共享入口仍是持久化后 ACK。
- 本地验证为核心 4 项、teamBuy 归档/WeCom 53 项通过；Docker 未安装，Petlove pytest 因 `pydantic-settings` 缺失无法收集。
- 生产仍是旧的三个直连 worker 状态，尚未切换；正式迁移必须先部署核心、配置真实路由/token，再停项目直拉 worker，最后切企业微信唯一回调。
- 生产配置两个项目的 `WECOM_ARCHIVE_CORE_MEDIA_BASE_URL` 时，应使用共享 HTTPS 域名；Nginx 片段已包含 `/v1/projects/` 媒体代理，不要填项目容器内回环地址。

## 2026-08-16 最新交接：媒体哈希去重与共享核心 24 小时清理

- teamBuy 业务图片现统一登记 `originalSha256`、`storageSha256`；手动附件上传已接入统一媒体资产服务，重复内容复用 `MediaAsset` 和物理对象，附件关系仍按业务对象单独保存。
- 共享核心默认 `WECOM_ARCHIVE_CORE_RAW_RETENTION_HOURS=24`。核心 worker 只删除超过 24 小时且所有项目已 `delivered` 的中转事件；pending/失败事件继续重试。
- 验证：teamBuy 后端全量 `238 passed`；共享核心 `4 passed`；相关 Python 编译与 `git diff --check` 通过。Docker/PostgreSQL 实际清理 SQL 尚未在本机执行。
- 当前仍未部署生产、未上传小程序、未切换企业微信唯一回调。生产切换前必须确认项目在 ACK 前已完成业务入库，并在核心 env 使用 `WECOM_ARCHIVE_CORE_RAW_RETENTION_HOURS=24`。

## 2026-08-16 最新交接：共享企业微信会话存档核心已部署生产

- 生产备份：`/home/ubuntu/teambuy-backups/wecom-core-migration-20260816-051547/`。
- 共享核心目录：`/home/ubuntu/wecom-archive-core`；服务为 `wecom-archive-core-archive-core-1`、`wecom-archive-core-archive-core-worker-1`、独立 PostgreSQL，API 监听 `127.0.0.1:8050`。
- 核心状态：健康 200；项目 `teamBuy`/`petlove` 已注册；`rawRetentionHours=24`；迁移时 teamBuy 历史投递 `81 delivered`，无 pending。
- teamBuy/PetLove 生产环境已经是 `WECOM_ARCHIVE_SOURCE=shared`；teamBuy 本地进程只处理共享事件，PetLove 本地直拉 Worker disabled；未上传小程序。
- Nginx 已将 `https://api.lifelove.top/wecom/archive/callback` 指向共享核心，并代理 `/v1/projects/`；公网非法签名 403、未授权媒体 401 已验证。
- 本次路由采用保守隔离：teamBuy 接收明确历史群和一对一消息，PetLove 接收其配置群；PetLove 群需要负责人发送一条真实消息做最终联调。不要启用 `allowAllChats` 作为临时解决方案。
- 回滚：先停止共享核心 Worker，再把目标项目切回 `WECOM_ARCHIVE_SOURCE=direct` 并恢复原直拉配置/游标；不得双 Worker 并行。

## 2026-08-16 最新交接：登录按需触发与未登录会话存档

- 根因：`miniprogram/pages/home/index.js` 的首页 `onShow` 在无用户时直接跳转登录，触发了微信审核的“打开即要求授权”问题。
- 已修复：首页匿名可浏览；资料新建、文字/链接/图片/PDF 录入、房源/商品/服务/名片创建入口才要求登录，并带 `returnUrl` 回到原目标页。
- 已加安全保护：匿名首页清除私有资料/客户/统计状态；异步 dashboard 请求在用户身份变化后丢弃旧结果。
- 会话存档结论：未登录用户仍可向企业微信发送消息，共享核心接收并解密后，teamBuy 业务库保存为 `unclaimed` 待认领资料；后续明确认领才写入用户 openid 绑定。现有后端测试已覆盖生成待认领资料和认领后归属。
- 本轮只改本地小程序和项目文档，未部署生产、未上传微信体验版。负责人需在微信开发者工具重新编译，重点验收：冷启动首页不弹登录、点击添加资料才登录、授权后回到录入页。

## 2026-08-16 最新交接：微信审核登录退出与头像选择整改

- `miniprogram/pages/login/index.wxml/js/wxss`：登录页增加页面栈返回和“暂不登录，先浏览首页”；跳过登录统一进入公开首页，不触发协议校验或受保护页面回跳。
- `miniprogram/pages/profile/index.wxml/js/wxss`：头像入口改为有边框、有文案、整块可点击的 `chooseAvatar` 原生按钮，并增加点击/选择结果反馈。
- 本地验证已通过：两个页面 JS `node --check`、相关 JSON 解析、`git diff --check`。未修改后端，未部署生产，未上传体验版。
- 负责人重新编译体验版后重点验收：登录页点击“暂不登录，先浏览首页”可进入首页；从“我的 → 个人资料”点击头像卡片能打开微信头像选择，选中后头像预览变化并提示保存。

## 2026-08-20 最新交接：客户信息链总开关与 PC 客户经营面板

- 本地已完成，尚未部署生产，未上传小程序体验版。
- 设计边界：不做微信虚拟支付；当前用户订单仍只走普通微信支付。开关只控制客户信息链展示、会员入口、会员新订单/发起支付、分佣、订阅授权和客户查看提醒，不阻断微信支付成功回调落账。
- 后端开关：`GET/PUT /api/ops-admin/customer-info-chain`，由管理员 Token 保护；状态写入运营后台状态文件，首次缺少字段时使用 `CUSTOMER_INFO_CHAIN_DEFAULT_ENABLED`，默认 `false`。
- 后端运营数据：`GET /api/ops-admin/customer-operations`，并扩展 `/api/ops-admin/overview` 摘要。数据来自客户浏览事件、客户动作、LeadReminder、会员订单/权益、订阅投递、归档消息和媒体资产；汇总接口不返回客户手机号/微信号。
- PC `/ops` 新增“客户经营”页：开关状态和操作人、今日/7 日客户信号、待跟进客户、普通微信支付订单/收入、订阅/归档/媒体健康。关闭按钮会先确认，再调用服务端 PUT。
- 小程序 profile 读取会员状态中的 `featureEnabled` 后决定是否请求客户情报；会员页对关闭态显示“客户信息链暂未开放”，不显示支付按钮。
- 重要部署说明：本轮没有改生产 `.env`、没有重启 backend/worker、没有生产验收。后续部署前先备份并确认服务器默认开关为关闭，再由负责人通过 `/ops` 人工开启；小程序需重新编译并由负责人上传体验版。
- 验证：后端原有 `test_sales_scrm.py + test_app.py` 188 项通过；新增开关/面板测试 2 项通过；相关 Python 编译、前端 JS 检查、PC 内联脚本解析、JSON 解析和 `git diff --check` 通过。
- 对抗式复核已补齐：状态请求失败和客户雷达旧缓存均不会继续展示客户信息或支付入口；运营支付指标只统计真实 `wechat_pay` 订单。最终定向回归为 190 项通过。

## 2026-08-21 最新交接：匿名浏览与客户信息链双开关

- 本地已完成，尚未部署生产，未上传小程序体验版。
- 首页匿名浏览逻辑保持有效；匿名打开“资料”不再跳登录，也不访问空 owner 的个人资料接口，只显示安全空态；匿名打开“雷达”不再跳登录，页面不渲染客户运营内容。编辑、创建、保存、发布等动作仍按需登录。
- 运营后台客户信息链现在有两个独立状态：`enabled` 和 `paymentRequired`。`enabled=false` 隐藏客户信息链；`enabled=true,paymentRequired=false` 为免支付查看模式；`enabled=true,paymentRequired=true` 为正式会员支付模式。
- 后端 `GET/PUT /api/ops-admin/customer-info-chain` 返回并接受 `paymentRequired`；会员状态和客户情报返回该字段；免支付模式直接返回完整客户情报且拒绝新建/发起会员支付订单；收费模式仍要求有效会员。
- PC `/ops` 客户经营页增加“隐藏会员支付/开启会员支付”按钮；本轮未部署生产，生产当前状态和生产 `.env` 未修改。
- 关键验收：匿名冷启动不弹登录；匿名打开资料/雷达不弹登录；客户信息链关闭时雷达无提示卡和客户内容；免支付模式可查看客户情报但没有支付入口；收费模式未付费只能看受控摘要；切换后缓存不串状态。
- 验证：后端全量 `240 passed`；相关小程序 JS 检查通过；小程序 `app.json`、`project.config.json`、`project.private.config.json` 解析通过；`git diff --check` 通过。
## 2026-08-21 最新交接：雷达匿名空白页修复

- `miniprogram/pages/visits/index.js/wxml` 已恢复匿名和客户信息链关闭时的雷达页面骨架，避免只剩标题和底部导航。
- 未登录只显示中性说明，不触发登录，不加载任何客户数据；登录且服务端允许后才读取雷达。
- 本轮未改后端，生产后端无需重新部署；负责人需在微信开发者工具重新编译并上传体验版。
- 验证已完成：雷达 JS、JSON、WXML 结构和 `git diff --check` 通过。

## 2026-08-21 最新交接：我的页按需登录修复

- `miniprogram/pages/profile/index.js/wxml` 已移除“进入我的页即登录”的旧逻辑，未登录显示游客浏览状态。
- 资料、合集等入口可以继续浏览；名片制作、个人资料编辑、消息中心等账号动作才触发登录。
- 匿名态会清空会员、客户、推广、未读消息和名片状态，并阻止旧账号异步请求回写。
- 负责人需在微信开发者工具重新编译并上传体验版；本轮未改后端，生产后端无需重新部署。

## 2026-08-21 最新交接：匿名主工作台请求隔离补强

- `miniprogram/pages/showcases/index.js` 已移除合集页 `onShow` 强制登录；匿名进入显示安全空态，新建合集时才带 `returnUrl` 登录。
- `miniprogram/pages/library/index.js`、`showcases/index.js`、`visits/index.js` 使用请求序号 + 当前用户 ID 双重校验，退出/切换账号后旧列表、后台刷新、分页和雷达提醒响应不得回写。
- `profile/index.js` 匿名或切换账号时额外清空会员、积分、编辑草稿、名片分享状态和未读消息。
- 验证已完成：相关 JS `node --check`、小程序 JSON 解析、`git diff --check` 均通过；后端 pytest 因当前环境未安装 pytest 未执行成功。
- 本轮未改后端、未部署生产、未上传微信体验版；负责人仍需在微信开发者工具重新编译并上传体验版，重点验收四个主入口匿名进入、登录动作回跳和账号切换不串数据。

## 2026-08-21 最新交接：私聊归档资料列表刷新修复

- 生产 13:40 私聊测试已确认完整入库：图片/文字归档序号 519/520，资料 `note_bb88091b03`、卡片 `card_bedc5f98ad`，媒体已成功转存；后端列表计算也能返回该卡片。
- 本地修复 `miniprogram/pages/library/index.js`：资料页仍先展示缓存，但每次重新显示时强制后台刷新第一页元数据，避免企业微信独立 worker 新增资料后被客户端 60 秒资源缓存遮蔽。
- 验证：JS `node --check`、`git diff --check`、后端 pytest `240 passed`。
- 本轮未部署后端；小程序必须由负责人在微信开发者工具重新编译并上传体验版后，重新验证“发送私聊图片+文字 → 返回资料页 → 新记录出现在顶部”。

## 2026-08-21 最新交接：私聊图片详情刷新修复

- 生产资料 `note_bb88091b03` 已确认 `coverUrl`、`media[0].url` 存在且媒体 `ready`；资料当前为 `private` 草稿，公共预览接口不适合作为这条资料的详情验证入口。
- 本地修复 `miniprogram/pages/note-edit/index.js`：缓存先展示，随后强制请求最新 note 详情，避免卡片缩略图已更新而编辑内容仍使用无图片旧缓存。
- 本轮不需要部署后端；负责人需在微信开发者工具重新编译并上传体验版后，打开该条资料确认“补充资料”中显示图片，并重新测试图片+文字私聊导入。
- 验证已完成：相关 JS `node --check`、小程序 JSON 解析、`git diff --check`、后端全量测试 `240 passed`。

## 2026-08-21 最新交接：所有 Tab 数据失败的 401 会话修复

- 生产健康检查正常；`/api/cards` 在无有效 Authorization 时返回 401，确认问题是小程序本地登录会话失效，不是后端接口整体不可用。
- 本地修复 `miniprogram/app.js` 和 `miniprogram/utils/request.js`：生产启动清理缺失/过期 token；统一请求遇到 401 清理当前会话与客户情报缓存，避免所有 Tab 持续显示数据失败。
- 本轮未部署后端；负责人需重新编译上传体验版。首次打开如回到游客态，请主动登录一次；登录成功后再检查资料、合集、我的和雷达。
- 验收重点：旧会话不再连续 401；重新登录后多个 Tab 恢复；退出/失效后匿名页不显示旧账号资料、会员、客户和推广数据。

## 2026-08-21 最新交接：私聊图片附件与静态分享卡修复

- 生产 `note_bb88091b03` 已确认图片媒体成功保存；当前问题是编辑页预览未展示附件，以及资料库普通资料在分享图未就绪时错误地触发微信默认页面截图。
- 本地修复 `miniprogram/pages/library/index.js/wxml`：已发布资料后台准备静态分享快照；所有资料在 `shareImageReady` 前不能进入微信分享，只显示“准备分享图”；生成完成后才显示“发客户”。
- 本地修复 `miniprogram/utils/business-card-share.js`：分享图上传失败直接失败，不再把临时 `wxfile://` 路径保存为快照。
- 对抗式审查补强：分享图任务绑定启动时用户和资料页请求序号，账号切换/页面刷新后旧任务不再写入快照；媒体上传 owner 不随当前账号漂移。
- 本地修复 `miniprogram/pages/note-edit/index.wxml/wxss`：普通文字资料的客户效果预览显示已归档图片；数据真源仍是 `media`，正文不嵌入图片二进制。
- 本轮不需要部署后端；负责人必须在微信开发者工具重新编译并上传体验版后验收：打开该资料确认客户效果有图；资料库等待“准备分享图”完成后点击“发客户”；微信聊天卡片显示设计好的资料静态图而不是资料库截图。
- 验证已完成：相关 JS `node --check`、小程序 JSON 解析、`git diff --check`、后端全量测试 `240 passed`。

## 2026-08-21 最新交接：资料与合集分享静态图全入口收口

- 对抗式审查已确认并修复 4 个遗漏：普通资料列表未全面门禁、资料客户预览好友按钮缺图仍可点、旧 `card-view` 使用普通封面、电子名片使用本地临时图。
- 现在资料库/统一资料列表、资料预览、历史资料详情、合集列表/编辑/客户预览和电子名片都只允许已保存的当前版本静态快照进入微信分享；无图时不会再主动返回空图或普通封面。
- 本轮未改后端、不需要后端部署；负责人必须在微信开发者工具重新编译并上传体验版后验收：资料四类（普通资料、图片资料/房源、商品、服务）、电子名片、合集各发送一次，聊天卡片均显示设计好的静态图，不能显示当前资料库页面截图。
- 自动化验证：相关 JS `node --check`、3 个小程序 JSON 解析、`git diff --check`、后端 `245 passed`、共享归档核心 `5 passed`。
- 已安装并使用工作区 Python 的项目依赖以执行测试；未修改仓库依赖文件。

## 2026-08-21 最新交接：Tab 缓存与合集“生成同款”受众修复

- 本地修复 `miniprogram/pages/library/index.js`、`miniprogram/pages/home/index.js`、`miniprogram/pages/notes/index.js` 和缓存策略：有用户隔离快照时先显示基础资料，后台再刷新；资料列表陈旧元数据最多保留 10 分钟，客户/会员/雷达数据不走该陈旧缓存。
- 本地修复 `miniprogram/pages/showcase-view/index.js/wxml`：编辑预览和本人查看自己的合集页隐藏“生成同款”，客户查看公开合集时才显示；后端克隆安全校验未被绕过。
- 对抗式结论：目前仍有后端性能债务——`/api/cards` 先全量计算统计/客户摘要再分页。它需要后续单独的后端查询优化、基准测试和明确部署，不属于本轮小程序缓存修复，生产后端本轮未部署。
- 验证：后端 `245 passed`、共享归档核心 `5 passed`、相关 JS `node --check`、3 个小程序 JSON 解析、Python 编译和 `git diff --check` 通过。
- 下一步人工验收：负责人需在微信开发者工具重新编译并上传体验版；分别冷启动/返回检查首页、资料、合集、雷达、我的的首屏速度，并用“本人编辑预览”和“另一账号/匿名客户打开已发布合集”验证“生成同款”显隐。

## 2026-08-21 最新交接：图片主体正文与静态分享图修复

- 已修复 `miniprogram/pages/note-edit` 和 `miniprogram/pages/note-preview`：图片主体资料进入正文主视觉，图片不再重复放在附件/相关资料；旧生产资料的系统转存提示不会再作为标题和正文展示。
- 已修复 `miniprogram/pages/notes/index.js`、`miniprogram/pages/library/index.js` 和 `miniprogram/utils/business-card-share.js`：图片主体分享图使用 `image_note_v2`，完整比例绘制原图并为 footer 留出空间，旧裁剪快照会在列表后台重新生成。
- 生产只读证据：`note_bb88091b03` 图片媒体和旧分享图均可访问，旧记录为 `text_note`；生产 worker 当前未出现 18:40 新消息。本轮未部署后端、未上传体验版。
- 人工验收：负责人在微信开发者工具重新编译上传后，打开旧图片资料确认正文直接显示完整图片；回到资料库等待“分享图准备完成”，发送一次图片+简单文字资料，检查微信卡片不再出现裁剪图或资料库页面截图。后端归档修复部署前，新测试仍可能再次生成旧系统提示，若要验证新归档分类需先部署后端。
- 自动化验证：相关 JS `node --check`、3 个 JSON 解析、`git diff --check`、图片主体兼容性断言、后端 `245 passed`、共享归档核心 `5 passed`、Python 编译通过。

## 2026-08-21 最新交接：图片 + 文字正文内容块

- 进一步按 Apple 备忘录式体验调整图片资料：编辑页不再给图片单独一套“附件式”编辑入口，而是统一显示标题、正文说明和图片；客户页文字在前、图片随后连续阅读。
- 仅图片资料走该内容块；房源、商品、服务保留各自的主图/画廊，PDF 和链接仍在附件区。
- 本轮仍未部署后端、未上传体验版；需重新编译上传后验收图片资料编辑、客户预览和微信静态分享卡三个路径。

## 2026-08-21 最新交接：列表原图与微信静态图一致性修复

- 已修复旧私聊资料被保存为 `text_note + manual_text + 图片媒体` 时的前端漏判：非结构化图片资料现在进入正文主视觉链路，房源/商品/服务/链接不受影响。
- 已修复 `notes`、`library` 两个发送入口盲信同 revision 旧快照的问题；图片资料只接受 `image_note_v3`，否则自动重画。静态图使用 `media` 原始图片 URL 和 `contain`，footer 不再覆盖图片内容。
- 生产只读证据：`note_275571da57` 和 `note_6d36ec939e` 当前均为旧 `note_v1`/`cover` 快照，这正是本次截图差异的直接证据。
- 本轮未改后端、未部署生产；负责人需在微信开发者工具重新编译上传体验版。上传后先打开“溜了”或“图片不”，等待分享图重新准备，再点击“发客户”；验收微信卡片图片应与列表原图内容一致且底部不被裁切。
- 自动化验证：相关 JS `node --check`、3 个小程序 JSON 解析、兼容性断言、`git diff --check`、后端 `245 passed`。

## 2026-08-21 最新交接：名片快照版本冲突与标签规范核对

- 已修复 `miniprogram/pages/library/index.js`：电子名片分享图生成前强制读取当前 source note，避免资料库缓存 revision 触发保存快照 409；失败状态改为“重试分享图”。
- 已修复 `miniprogram/pages/business-card-studio/index.js`、`miniprogram/pages/note-edit/index.js`：保存资料后清理资料库元数据缓存，返回资料库时不再继续使用旧 revision。
- 后端 409 保留，不需要后端部署；它是拒绝旧分享快照的正确安全保护。本轮未部署生产、未上传微信体验版。
- 标签结论：标签能力本来就被设计为资料库的核心索引；早期决策保留编辑页标签整理，后续五类编辑器规格要求移到资料工作区。现有标签能力本轮不删除，后续若按最新规格调整入口，必须先补齐工作区逐条打标替代路径。
- 验证：相关 JS `node --check`、3 个小程序 JSON 解析、`git diff --check`、后端 `245 passed`。

## 2026-08-21 最新交接：资料工作区标签与分享样式口径澄清

- 用户确认当前资料编辑/整理页属于资料工作区，标签整理入口保留；不要把它误归类为客户 SCRM 后台。
- 名片旧 `clean_white` 快照不需要手工删除，合法样式定义也不能删除；等待当前 revision 的新快照保存成功后由生命周期逻辑退役旧快照。
- 本轮没有新增代码变更；仍需用户上传前端体验版验证名片从“准备分享图”变为“发客户”。

## 2026-08-21 最新交接：分享快照重试循环根因修复

- 已确认循环根因不是 `clean_white` 等旧样式：后端将完整分享指纹截断到 512 字符，前端严格比较完整指纹，导致后端返回 200 后仍被前端判失败。
- 已修复 `miniprogram/plugins/share-snapshot/index.js`：规范化内容改作为固定 128-bit `v2` 摘要的输入，避免超过后端边界；旧截断快照首次会重建一次，之后应直接复用。
- 本轮未改后端、未部署生产、未上传微信体验版。负责人需在微信开发者工具重新编译上传后，清除旧小程序缓存或重新进入资料库，等待名片状态从“准备中/重试分享图”稳定变为“发客户”，再实际发送一次。
- 自动化验证待本轮结束执行：相关 JS `node --check`、长指纹稳定性断言、JSON 解析、`git diff --check`、后端全量 pytest。

## 2026-08-21 最新交接：图片资料分享卡水平居中修复

- 已修复 `miniprogram/utils/business-card-share.js`：图片资料 `contain` 绘制改为显式计算水平中心并使用五参数 `drawImage`，避免微信端 no-op 源裁切造成竖图视觉右偏。
- 已将图片资料分享样式从 `image_note_v3` 升为 `image_note_v4`；历史 ready 快照会在重新进入分享准备流程时重画，不能继续复用旧图。
- 本轮未部署后端、未上传微信体验版。负责人需重新编译上传后，打开图片资料，等待“准备分享图”完成，再发送一张验证：图片左右留白相等、图片完整显示、footer 不覆盖原图。

## 2026-08-21 最新交接：电子名片高保真标准专业分享图

- 已修改 `miniprogram/utils/business-card-share.js`：电子名片所有分享入口统一绘制白底蓝色重点的单层信息卡，显示真实头像、姓名、身份、有值的公司/一句话介绍/联系方式；不再显示旧颜色皮肤、模板名、假二维码、重复查看按钮和静态图内“生成同款”。
- 已同步 `miniprogram/pages/business-card-studio/index.js/wxml/wxss` 编辑预览，样式选择收口为“标准专业”；历史样式 ID 保留兼容，编辑器保存统一为 `business_blue`。
- 分享源带 `business_card_v2` 渲染版本，旧静态图会按当前 revision 重新生成；后端指纹修复仍保留，未改后端。
- 自动化验证：相关 JS `node --check`、`app.json/project.config.json/project.private.config.json` JSON 解析、`git diff --check`、名片数据断言、后端 `245 passed`。
- 本轮未部署生产、未上传微信体验版。下一步由负责人在微信开发者工具重新编译上传，验证：编辑器预览 → 资料库名片缩略图 → 发客户后的微信卡片，三处都使用同一套姓名/身份/联系方式层级；验证成功后再确认是否保留当前视觉方案。

## 2026-08-21 最新交接：资料列表性能与 22:01 归档媒体回填

- 已部署后端资料列表性能修复：`/api/cards`、`/api/notes` 批量加载统计/客户摘要，缓存 TTL 60 秒，缓存 key 按用户和查询条件隔离；未把客户敏感信息写入小程序持久化缓存。
- 已部署共享归档核心嵌套微信笔记媒体解析、精确事件重投、共享媒体依赖注入修复；生产媒体地址改为 Docker 内网归档核心，视频读取超时下限 120 秒。
- 已完成 22:01 事件 `archive_805893c99207a6b0e1398d3d_523` 的受控重投和回填：目标资料 `note_bd77702ded` 的 9 个媒体全部有 URL，失败重试数为 0，来源资料卡封面已同步。
- 生产备份：`/home/ubuntu/teambuy-backups/teambuy-cache-archive-20260821-190500/`。生产健康检查通过；小程序仍需负责人在微信开发者工具重新编译并上传体验版。
- 生产基准：资料卡冷启动约 127ms/命中约 11ms，资料列表冷启动约 168ms/命中约 39ms。数据库级真正分页仍是后续可独立优化项，但本轮首屏 N+1 阻塞已移除。

## 2026-08-22 最新交接：房源图片滑动、地图回退与雷达显示修复

- 已修改 `miniprogram/pages/note-preview/`：资料/房源图片改为 swiper；地图地址增加完整地址失败后的社区级候选回退。
- 已修改 `miniprogram/pages/card-view/`：房源图片改为 swiper，视频单独列出，避免所有媒体挤在同一纵向区域。
- 已修改 `miniprogram/pages/visits/`：移除会员/支付相关可见文案和入口；关闭客户信息链时强制保持关闭态，避免异步配置回写。
- 生产腾讯地图 key 已确认存在；当前空白案例是精确到栋房号的地理编码失败，短地址可以成功返回坐标。生产客户信息链仍为关闭，未因本轮前端修改而开启。
- 本地验证已通过；用户下一步需要在微信开发者工具重新编译并上传体验版。地图和滑动行为需用真机复测；若要开放客户信息链，需另行在生产运营后台确认开关已持久化。

## 2026-08-22 最新交接：房源编辑页横向布局与地图预览

- 已修改未提交的 `miniprogram/pages/property-editor/`：只有图片容器横向滚动，整个编辑页和核心信息表单不再横向溢出。
- 房源编辑页新增地图预览和小区级地址回退；首次读取旧房源时会尝试生成坐标并保存，修改地址/小区后旧坐标不会复用。
- 用户需重新编译并上传小程序体验版后复测：图片容器左右滑动、核心信息左右两列完整显示、地图卡片显示房源标记。
- 本轮没有后端代码变更、没有生产部署；生产客户信息链仍保持关闭。

## 2026-08-22 最新交接：地图导航与分享页固定返回首页

- `note-preview` 和 `property-editor` 的地图现在可点击；点击后可选导航 App、微信内置地图或复制地址。
- 独立分享页增加右侧固定圆形“首页”按钮，仍保留底部“回到首页”按钮。
- 本轮不需要后端部署；用户重新编译并上传体验版后，需真机验证地图点击是否弹出动作菜单，以及分享卡片打开后右侧首页按钮是否始终可见。

## 2026-08-22 本轮接手：人工验收准备完成

- 本轮未修改业务代码、未部署生产、未上传微信体验版；现有未提交改动未被清理或覆盖。
- 已完成只读核对与静态验证：房源编辑页仅图片区横滑、地图回退/导航、客户分享页固定首页入口、雷达关闭态门禁均已有对应实现；目标 JS `node --check`、全部小程序 JSON、`git diff --check` 通过；后端 pytest `245 passed`。
- 待人工验收：微信开发者工具重新编译并上传体验版；按房源图片/核心信息、地图三种动作、分享页固定首页、雷达关闭态四组清单逐项验证。发现异常时先记录观察事实、根因、最小修改范围和验收标准，再决定是否改代码。

## 2026-08-22 最新交接：统一入口直接发布

- 本轮已完成本地开发：统一新建资料入口支持正文/图片/链接/PDF 后直接发布；图片在普通资料正文中可见；类型编辑器降级为可选增强路径。
- 后端新增 `contentBlocks` 的存储、更新、兼容派生和公开过滤；房源公开正文块使用脱敏后的结构化正文，避免私密采集文本泄露。
- 已验证：相关前端 JS `node --check`、全部小程序 JSON 解析、`git diff --check`、后端 pytest `246 passed`。
- 未执行生产部署，未上传微信体验版。下一步由负责人在微信开发者工具重新编译并上传体验版，重点验证：正文粘贴后直接发布、图片在客户正文中可见、PDF/链接仍可打开、旧资料打开不报错，以及房源隐私不泄露。

## 2026-08-22 最新交接：分享图统一插件与发送状态链路

- 已完成本地开发：所有资料分享图生成入口统一经 `miniprogram/plugins/share-snapshot/index.js`，页面不再直接选择具体静态图生成器；插件负责场景归一化、版本指纹、并发合并、上传保存和最终校验。
- 当前状态链路为：未发布/发布 → 分享图缺失或过期 → 生成中 → 上传保存 → 重新读取校验 → `ready` → 才显示微信原生“发客户”。失败显示“重试分享图”，不会发送默认页面截图或上一版本旧图。
- 后端公开资料接口已过滤 revision 不一致的快照；历史 `card-view` 也改用插件严格判断当前 snapshot。旧图片资料分享样式不再被当作当前样式复用。
- 自动验证已通过：插件 stub 冒烟测试、目标 JS `node --check`、68 个小程序 JSON、`git diff --check`、后端 pytest `246 passed`。
- 未部署生产，未上传体验版。负责人需在微信开发者工具重新编译并上传后人工验证：发布后客户页立即可打开；生成中按钮不可发送；生成完成后微信卡片使用静态图；修改资料后旧图立即失效并重新生成；图片资料在正文主视觉展示且不被当作普通附件。

## 2026-08-22 最新交接：统一渲染器实际落地

- 已纠正上一轮遗漏：此前只统一了分享图生命周期，没有统一实际绘制器；本轮所有场景统一走 `ShareCardModel -> generateUnifiedShareCardImage()`。
- 普通资料分享图现在读取真实正文块，图片资料使用正文主图并按 contain 保持比例；旧固定 chips、内容信息卡和“生成同款”不再由新 renderer 自动补写。
- `share_card_v2` 已成为当前统一样式版本；旧版本快照不会被资料列表、资料库、编辑器或客户页继续作为可发送图使用。
- 验证：全部小程序 JS `node --check`、68 个 JSON、`git diff --check`、插件和 Canvas 冒烟测试通过；后端 pytest `246 passed`。
- 未部署生产，未上传微信体验版。下一步人工验收仍需重点检查：文字内容与资料正文一致、图片不变形且不裁主体、生成中不能弹原生发送面板、版本变化后旧图重新生成。

## 2026-08-22 最新交接：名片专用布局回归统一渲染器

- 已确认上一轮的实际问题：统一了分享图插件生命周期，但把 `business_card` 的结构化内容扁平化为普通资料文本块，导致微信卡片显示成通用资料卡，名片原有布局丢失。
- 已在同一 `generateUnifiedShareCardImage()` 内恢复 `layoutId=business_card` 的名片布局；没有恢复页面级第二套生成器。资料库、客户页、编辑器预览和个人页仍通过同一插件/模型链路。
- 已将分享图样式版本升为 `share_card_v3`，旧 `share_card_v2` 快照不再作为当前版本发送图；旧媒体不删除，成功生成新图后由既有生命周期处理。
- 图片策略：名片头像按比例完整绘制在头像框内；普通图片仍不能用 `cover` 解决留白问题，后续真机验收必须确认“主体完整、不变形”，必要时再让媒体区按原图比例自适应高度。
- 本轮未部署生产、未上传微信体验版。用户需重新编译体验版后验证：名片恢复专用样式；名片姓名/身份/公司/联系入口不丢；普通图片不裁主体；分享图生成完成前不允许原生发送。

## 2026-08-22 最新交接：单图资料原图布局

- 已确认固定高度 `contain` 不是单图资料的最终方案：当前体验图把“图片资料”标签、标题、媒体框和底栏重复叠加，导致原图主体变小并产生大面积留白。
- 已新增 `image_single` 布局：单张图片且无正文说明时，直接绘制原图，固定宽度、按原图比例生成高度；不使用 `cover`，不变形，不再套两层“图片资料”结构。
- 带正文说明、多图和业务型资料不进入 `image_single`，继续使用对应内容布局；统一插件、统一 Canvas 导出入口和发布/生成/校验/发送状态链不变。
- 已将相关隐藏 Canvas 容器高度放宽到 1800rpx，避免常见竖图导出被容器裁断。未部署生产、未上传微信体验版。

## 2026-08-22 最新交接：房源摘要卡与其他场景原图直出

- 已实现两种非名片策略：房源使用 property_summary，主图按原比例完整展示，下方显示真实的价格/户型/面积及已有扩展字段；商品、服务、普通资料等有真实主图时使用 original_media，直接复用托管原图。
- original_media 仍进入同一个分享快照插件，发布状态、owner、revision、fingerprint、styleId 和托管媒体 URL 校验不变；只跳过 Canvas 绘制和二次上传。
- 无主图资料继续使用最小信息图 fallback；名片继续使用专用布局。未部署生产、未上传微信体验版，需在体验版验证房源摘要、商品营销图和旧资料版本失效。

## 2026-08-22 最新交接：商品/房源发送标题补齐

- 详情页和资料库发送入口均已补齐商品“价格 + 商品名”、房源“房源名 + 价格/户型”的外层分享标题。
- 资料库按钮的通用 `data-title` 不再覆盖这两个场景的结构化标题；图片仍统一由 `share-snapshot` 插件提供。

## 2026-08-22 最新交接：旧分享图强制失效

- 已将分享图样式版本从 `share_card_v3` 升为 `share_card_v4`，用于强制淘汰普通资料卡旧快照，重新生成房源摘要图、商品/普通资料原图和单图原图布局。
- 未部署生产、未上传体验版；必须重新编译体验版后确认旧图不会继续进入“发客户”。

## 2026-08-22 最新交接：客户信息链改为 PostgreSQL 配置表

- `ops_feature_flags(customer_info_chain)` 已接入 `PostgresRepository.init_schema()` 和 `OpsConsoleStore`；生产不再把客户信息链开关写入容器内 JSON。
- 旧 `ops-console-state.json` 只在配置表缺行时参与一次性迁移；已有数据库行优先，后续旧文件变化不会覆盖线上状态。
- 数据库异常时后端 fail closed，小程序不会暴露客户信息/会员门禁，PC 显示“读取失败”而不是误报“已关闭”。
- 本轮未部署生产。部署前必须先迁移并核对当前生产值 `enabled=true`、`paymentRequired=false`，再做生产接口和 PC 刷新验证。

## 2026-08-22 最新交接：新增分享链路对抗式验收门禁

- 已将本次问题写入 `AGENTS.md`、`docs/project-memory.md`、`docs/decisions.md`、`docs/pitfalls.md` 和 `docs/dev-log.md`。
- 后续不能只验证统一渲染器或快照状态；必须验证最终 `onShareAppMessage` 返回的 `imageUrl` 与 `path`。
- 非名片有原图时，A 方案的验收结果必须是原图 `imageUrl` + 当前资料详情页；无图时不得伪造图片；无 `imageUrl` 却返回资料库/首页路径的情况直接判 P0。
- 交付报告必须明确区分静态检查、插件/回调测试和微信开发者工具人工验收，未覆盖项不能写成“已完成”。

## 2026-08-22 最新交接：A 方案非名片原图直出完成

- 已按最终决策处理：电子名片保留专用布局；房源、商品、服务、普通资料、图片资料和合集均不再进入旧式 Canvas 卡片。
- 有主图时，统一插件直接复用原图并用原生分享标题承载房源价格/户型/面积、商品价格/名称等关键字段；无主图时只分享原生标题和路径，不伪造缩略图。
- `share-snapshot` 仍是唯一入口，样式版本为 `share_card_v5`；非名片原图仍经过 revision、fingerprint、发布状态、归属和托管媒体地址校验，名片仍经过 Canvas 生成和快照保存。
- 资料库生成前会重新读取所有资料的最新版本，不再只为名片刷新 revision；所有发送入口已支持 `direct` ready，避免无图资料被卡在“分享图准备中”。
- 已完成：相关 JS `node --check`、`git diff --check`。未完成：后端 pytest 和微信开发者工具真机验收；本轮未部署生产、未上传体验版。

## 2026-08-22 最新交接：生产旧分享链接 404 与发送门禁修复

- 生产现象已定位：资料编辑后后端将该 revision 设为 `private`，旧分享链接访问公开接口得到 404；支付、会话存档和订阅消息不是本次客户页加载失败的直接原因。
- 已修复：资料库分享准备强制读取最新 owner note；private/revoked 资料清除旧 ready 状态并回到重新发布，异步校验期间不允许旧按钮穿透；客户页对 404 显示“资料已停止分享，请重新发布后再发送”，网络故障保留重试入口。
- 对抗式验证已覆盖：编辑后 private 卡片、校验悬挂窗口、旧公开链接失效态；目标 JS 检查、68 个 JSON、`git diff --check` 通过，相关后端 pytest 3 passed。
- 未部署生产、未上传体验版。用户需要在生产资料上重新发布后重新发送一张新卡片；当前旧卡片按设计不会被恢复为可公开链接。

## 2026-08-22 最新交接：修复非名片有图直出断链

- 已修复“非名片有真实主图但仍显示资料分享图准备中”的根因：统一插件现在把所有非名片标记为 `direct`，有合法主图时直接提供原图 `imageUrl`，不再等待非名片快照生成。
- 资料库、资料列表、客户页、编辑页和公共资料页均使用插件的 `getShareImageUrlFromState()`，避免页面只读 `snapshot.url` 导致直出状态与最终发送对象脱节。
- 对抗式回归结果：房源 + 有图 + 快照缺失时，最终资料库发送回调返回原图 URL 和 `/pages/note-preview/index` 详情路径；名片缺少快照时仍不会被误判为直出。
- 已完成：相关 JS `node --check`、定向 Node 回调反例测试、`git diff --check`。未修改后端，因此未运行后端 pytest；仍需用户在微信开发者工具重新编译并人工验证实际微信发送卡片。本轮未部署生产、未上传体验版。

## 2026-08-22 最新交接：客户信息链配置表已部署生产

- 已部署客户信息链 PostgreSQL 配置表改造的最小后端补丁；生产开关现在以 `ops_feature_flags.customer_info_chain` 为唯一真源，PC 接口和小程序接口契约未变。
- 线上原文件备份：`/home/ubuntu/teamBuy/.deploy-backups/customer-info-chain-20260822191532/`。已重建三个后端相关容器，PostgreSQL 未重启，生产 `.env`、secrets、媒体目录未修改。
- 生产验证通过：本地和公网 `/health` 均正常；运营接口实际读取 `enabled=true`、`paymentRequired=false`、`updatedBy=ops`。本轮没有开启或改变客户信息链。
- 注意：归档 worker 处理已有历史任务时记录了独立的 `SkillRouterService.is_image_primary_import` 缺失错误；不属于本次配置表部署，后续需单独修复和回归。
## 2026-08-22 最新交接：会员支付待支付订单复用与倒计时

- 已完成：30 分钟内取消支付后复用同一业务订单；超时订单惰性关闭，旧订单号不能再次发起支付，下一次创建新订单。
- 会员状态接口新增 pendingOrder.expiresAt / pendingOrder.secondsRemaining；小程序会员页按服务端到期时间显示 mm:ss 倒计时，取消支付后提示可继续支付。
- 已覆盖对抗式反例：重复进入不重复订单、超时后新订单、超时旧订单号支付被拒、晚到真实支付回调不因本地关闭丢失。
- 验证：后端 backend/tests 共 250 passed，会员页及 API node --check、JSON 解析、git diff --check、倒计时 Node 冒烟通过。未部署生产，需用户在微信开发者工具重新编译体验版后人工验证微信支付取消和倒计时。

## 2026-08-22 最新交接：订阅消息 SOP 对抗式修复

- 需要与会员支付待支付订单改动合并部署；当前订阅修复尚未部署生产。
- 生产浏览接口不再信任请求体 `viewerUserId`；有效会话由服务端 Authorization 推导，匿名访问不消耗一次性订阅额度。
- 订阅额度和投递失败已补回收：入列失败、连续 3 次可重试发送失败、客户信息链关闭均释放/失效对应额度；`reserved` 超过 15 分钟自动回收。
- 小程序发送入口会等待后端模板配置；授权记录失败保留同一 requestId/status 重试；拒绝后 24 小时内不重复弹授权。
- 后端专项反例测试已通过；部署前仍需完成后端全量回归、生产只读健康检查和部署后公网路由验证。生产客户信息链保持关闭，不要将部署验证误认为已经开启订阅链路。

## 2026-08-22 最新交接：订阅消息与会员支付改动已合并部署

- 已一次性部署订阅消息 SOP 修复和会员支付 30 分钟待支付订单复用；线上备份位于 `/home/ubuntu/teamBuy/.deploy-backups/subscription-payment-20260822201302/`。
- 已重建 `backend`、`backend-worker`；PostgreSQL、archive-worker、生产 `.env`、secrets 和媒体卷未修改。
- 公网健康检查、容器启动和目标文件 SHA-256 校验通过；生产配置复核为 `enabled=false`、`paymentRequired=true`，客户信息链仍关闭。
- 代码测试结果：后端全量 `254 passed`；相关 Python 编译、订阅/支付 JS `node --check`、68 个小程序 JSON 解析、前端配置/授权失败重试冒烟和 `git diff --check` 通过。
- 仍待人工步骤：重新编译并上传小程序体验版，验证客户分享后订阅授权、发布者查看提醒、连续失败不阻塞浏览，以及会员支付取消后倒计时和原订单复用。生产客户信息链未开启，不能用它验证客户联系方式链路。

## 2026-08-22 最新交接：归档 Worker 已修复，成员好友自动回发仍受通道边界限制

- 已部署最小后端修复：生产 `skill_router_service.py` 补齐 `is_image_primary_import`；线上原文件备份为 `/tmp/teamBuy-skill_router_service.py.before-fix-20260822`。已重启三个后端相关容器，未重启 PostgreSQL，未修改 `.env`、secrets、媒体目录。
- 生产 21:46 测试消息 `seq=538` 已完成归档和资料生成，`generated_note_id=note_87f5a9f411`；Worker 不再因缺少方法失败。
- 自动回复真实结果为失败：`kf/send_msg` 返回 `95018 session status invalid`。该消息来源是企业微信成员好友会话存档，不是微信客服会话；当前小程序成员添加/绑定入口不会触发 `wx.openCustomerServiceChat`。
- 结论：资料整理链路通过，成员好友自动回消息未通过且不是本次 Worker 修复范围内的代码故障。若要验证客服自动回复，需从微信客服入口发送新消息；若保持成员好友主入口，应使用小程序站内/订阅消息或人工发送等独立触达方案。
- 验证：后端全量 `255 passed`；相关 JS、68 个 JSON、`git diff --check` 通过。生产客户信息链状态未修改。
# 2026-08-23 最新交接：联系我插件绑定卡片链路

- 用户添加企业微信后仍需点击一次欢迎小程序卡片；这是获得当前小程序 openid 并完成安全绑定的必要动作。
- 配置 ID 为 `df29f3fd3ddc95bfec70e60cef93730c`。前端已登记 contactPlugin，后端已接入添加客户事件和 `/pages/wecom-bind/index`。
- 生产上线前需要配置 `WECOM_BIND_CARD_PIC_MEDIA_ID`、企业微信回调 Token/AES，并确认回调地址为现有 `/api/wecom/kf/teamBuy/callback`；本轮未部署生产。
- 绑定卡片 token 只保存哈希、短时有效、一次消费；重复事件和失败事件不会再次发送同一个 welcome code，避免重复推送和重复绑定。
- 负责人下一步：完成企业微信后台配置后，再要求部署后端；重新编译上传小程序体验版，人工验证“添加 -> 自动收到卡片 -> 点击卡片 -> 绑定成功 -> 再次点击幂等”。
# 2026-08-23 最新交接：企业微信回调密钥已同步生产

- 正确配置入口是企业微信“资料整理助手”应用详情页底部的“接收消息 -> 设置API接收”，不是开发者中心 API 调试页面。
- 回调 URL：`https://teambuy.lifelove.top/api/wecom/kf/teamBuy/callback`。
- 生产已同步新的 Token/AES 并重建 backend；备份：`/home/ubuntu/teamBuy/.deploy-backups/wecom-callback-20260823005716/backend.env.before`。
- 生产健康检查和公网 callback 路径通过。负责人现在回到企业微信页面点击“保存”；如果仍失败，再检查域名备案主体限制。
- 不要把截图中已暴露的旧 Token/AES 继续使用；欢迎卡片封面 `WECOM_BIND_CARD_PIC_MEDIA_ID` 仍需单独配置。

# 2026-08-23 最新交接：绑定欢迎卡片改为 PC 管理的自动素材生命周期

- 正式方案已在本地实现，未部署生产：PC `/ops` 的“客户经营”页新增“企业微信绑定卡片”面板。
- 操作流程：管理员选择 PNG/JPEG/WebP 封面 -> 后端使用现有企业微信凭证上传 -> 保存源图片和 media_id 有效期 -> 后续发送前自动检查并刷新。
- 生产配置不再要求手工填写新的 `WECOM_BIND_CARD_PIC_MEDIA_ID`；该变量仅作旧配置兼容。第一次正式使用必须在生产 PC 后台上传封面。
- 代码入口：`backend/app/services/wecom_bind_card_asset_service.py`、`backend/app/api/routes_ops_admin.py`、`backend/app/services/wecom_client.py`；添加客户事件仍使用 `backend/app/api/routes_wecom.py` 的同步动态卡片发送。
- 已验证：后端全量 `262 passed`，相关 Python 编译、PC 管理页脚本 `node --check`、JSON 解析、`git diff --check` 通过。
- 部署后验收：确认 PC 状态为“已启用 / 自动刷新已启用”；再用新微信客户测试欢迎卡片点击绑定。不要用旧的固定欢迎小程序卡片替代动态 Token 卡片。

# 2026-08-23 最新交接：绑定卡片正式方案已部署

- 生产已部署并重启 API、普通 worker、归档 worker；新增表 `wecom_bind_card_assets` 已自动创建。
- 生产公网 `/ops` 已出现“企业微信绑定卡片”面板；入口为“客户经营”页内，不是左侧独立菜单，也不在企业微信后台。
- 生产当前 `WECOM_USE_MOCK=false`，企业微信凭据已配置，旧固定 `WECOM_BIND_CARD_PIC_MEDIA_ID` 未配置。
- 下一步：管理员进入 `https://teambuy.lifelove.top/ops`，输入管理员 Token，点击“客户经营”，上传封面并确认“已启用 / 自动刷新已启用”；随后用新客户测试动态绑定卡片。

# 2026-08-23 最新交接：小程序主包分包优化

- 已新增 `miniprogram/subpackages/workbench`，迁移 15 个编辑/发布/展示编辑/经营看板页面；公开详情和分享页保留在主包。
- 主包静态体积估算约 1.25MiB，分包约 719KiB；所有 JS、JSON、页面路径和内部跳转已完成静态检查。
- 用户需要在微信开发者工具重新编译并确认主包低于 1.8MB，再上传体验版。重点检查：新建资料、房源/商品/名片编辑、展示页编辑、经营看板、历史分享卡片。

# 2026-08-23 最新交接：失效会话恢复与游客态修复

- 已在本地修改 `miniprogram/app.js`、`miniprogram/utils/request.js`、`miniprogram/services/api.js`、`miniprogram/pages/login/index.js`、`miniprogram/pages/home/index.js`。
- 带 token 的 401：清理当前用户、用户级资料/专题/展示页/雷达缓存，只发起一次登录恢复并保留原目标页；登录页可“暂不登录”回公开首页。
- 无 token 的 401：只清理可能残留的会话和缓存，不强制登录。游客启动、首页和公开内容浏览保持可用；记录笔记、保存、发布和工作台等受保护动作仍需登录。
- 已防护用户切换时旧 topics/showcases 请求回写缓存的竞态；图片文件缓存仍按用户和环境隔离，不在会话恢复中批量删除附件文件。
- 验证已通过：5 个相关 JS `node --check`、全量小程序 JSON、`git diff --check`、后端 `262 passed`，请求层反例测试通过。
- 本轮没有修改后端、没有部署生产、没有上传体验版。负责人下一步：微信开发者工具重新编译上传，分别验收游客浏览、失效 token 自动恢复/跳过登录、重新登录后的个人资料隔离，以及首页网络失败提示。

# 2026-08-23 最新交接：客户留言订阅消息与雷达入口

- 前后端改动已完成；生产后端已按最小范围部署，小程序体验版仍未上传。
- 后端复用现有订阅模板：客户/买家留言通知资料发布者；同一会话 30 分钟去重；排队中的普通浏览提醒最多让出一条额度；发布者回复不通知；模板预览脱敏手机号、邮箱和微信号。
- 通知点击进入消息会话；发布者可点击“查看客户雷达”。雷达入口只携带 `threadId`，雷达页通过后端会话权限校验获得客户身份，原有会员门禁仍有效。
- 主要代码入口：`backend/app/services/app_service.py`、`backend/app/api/routes_messages.py`、`backend/app/models/domain.py`、`miniprogram/pages/message-thread/index.js`、`miniprogram/pages/customer-detail/index.js`。
- 验证已通过：订阅专项 10 passed；后端全量 266 passed；全量小程序 JS `node --check`、69 个 JSON 解析、Python compileall、`git diff --check` 通过。
- 微信开发者工具人工验收顺序：重新编译并上传体验版 -> 资料发布者在分享入口完成原模板订阅授权 -> 客户留言 -> 点击通知进入对应会话 -> 发布者进入客户雷达 -> 验证会员门禁、跨账号会话不可读、30 分钟去重和发布者回复不产生通知。
- 生产部署备份：`/home/ubuntu/teamBuy/.deploy-backups/message-subscription-20260823130659/`。已重建 `backend`、`backend-worker`、`archive-worker`；生产 `.env`、secrets、PostgreSQL 和媒体卷未修改。内网/公网 `/health` 均通过，消息路由未登录返回 401。

# 2026-08-23 最新交接：分享卡片客户侧加载失败调查

- 已确认页面是公开资料预览页 `miniprogram/pages/note-preview/index.js`，不是客户雷达详情页；分享入口路径仍为 `/pages/note-preview/index?id=...`，且页面保留在主包。
- 可复现根因：前端公共请求层会把本地 `currentUser.authToken` 发送给公开资料 GET；生产对失效 Authorization 返回 401，页面把该 401 显示为“客户页加载失败”。无 token 的同一公开资料请求返回 200。
- 16:28 生产 Nginx 日志没有看到客户侧公开资料请求，只有开发者工具访问发布者接口；因此这次具体点击还不能仅凭日志断言是失效 token，仍需客户端请求日志或分享路径确认。但缓存跨用户污染、发布接口拒绝和公开资料接口不可用均没有被生产证据支持。
- 本轮仅调查，没有修改前端或后端，没有部署。下一步最小修复方向：公开资料/公开合集 GET 不附带失效 token（或公开路由对失效 token 降级匿名），客户动作、浏览上报、客户雷达继续走原有鉴权。
- 回归必须覆盖：无 token、失效 token、有效 token、撤回资料、旧分享路径、跨用户公开读取、客户留言/联系方式动作、客户雷达会员门禁；并确认不能把公开读取修复成私有数据越权。

# 2026-08-23 最新交接：登录页 `globalData` 冷启动崩溃补丁

- 已修改本地前端：`miniprogram/utils/request.js`、`miniprogram/pages/login/index.js`。
- 根因是请求模块顶层读取 `getApp()`，而登录页加载 `services/api` 时可能早于 `App()` 初始化；已改为惰性读取，并在 App/API 尚未就绪时返回 `app_init` 可重试错误。
- 登录页已保护 onLoad、微信登录、保存登录用户和 returnUrl 解码；游客浏览策略不变，不会一打开小程序就强制登录。
- 对抗式验证已通过：全量 JS `node --check`、69 个 JSON 解析、`git diff --check`；Node 反例覆盖冷启动不发请求、正常游客请求、游客 401 不跳转、登录用户 401 恢复、`getApp()` 抛错。
- 本轮没有修改后端、没有部署生产。用户必须在微信开发者工具重新编译并上传包含这两个文件的新体验版；旧体验版/线上版本不会自动获得修复。
- 人工验收顺序：冷启动 -> 微信登录 -> 选择暂不登录 -> 受保护动作触发登录 -> 失效 token 恢复并返回原目标页 -> 重新登录后检查个人资料隔离。若仍显示旧错误，先重启微信开发者工具并清理编译缓存后确认上传版本号。

# 2026-08-23 最新交接：订阅模板字段映射修复并重启生产

- 根因：生产发送接口调用成功到达微信，但微信返回 `47003 data.thing13.value is empty`；线上模板实际字段为 `messageName=thing13`、`customerName=thing16`、`projectName=thing14`、`messageContent=thing3`、`reminderTime=time11`，旧生产配置使用了 `thing1/thing2/thing3/thing4/time5`。
- 已修改生产 `/home/ubuntu/teamBuy/backend/.env`，备份为 `/home/ubuntu/teamBuy/backend/.env.backup-subscribe-fields-20260823-1755`；已用 `docker-compose.production-runtime.yml` 重建 `backend` 和 `backend-worker`，未触碰数据库、媒体、企业微信密钥、客户信息链或支付开关。
- 已同步仓库 `backend/.env.example`、`backend/app/core/config.py` 默认值和订阅测试字段断言，避免未来漏挂 `.env` 时恢复旧映射。
- 验证：backend 与 backend-worker 容器均加载新映射；公网 `/health` 返回 `status=ok` 且数据库正常；配置 JSON 解析和 `git diff --check` 通过。
- 旧的两条 `47003` 失败投递保持 `failed`，不会被重启自动伪装成成功；修复后人工验收应使用新的已登录客户浏览/留言事件，确认微信返回 `errcode=0`、投递状态变为 `sent`。匿名浏览仍不触发订阅消息。

# 2026-08-23 最新交接：留言登录回跳、微信头像与浏览历史

- 本地已完成前后端修改，生产后端未部署，小程序体验版未上传。
- 留言：游客打开公开资料仍可浏览；点击留言会进入登录页并保存原资料路径、分享上下文和 `action=message`，登录成功后返回原资料并自动打开对应消息会话。
- 头像：登录页新增可选的微信头像/昵称入口；个人资料页继续使用 `chooseAvatar` 和 `type="nickname"`。临时头像先上传再保存，上传失败不清空旧头像。默认头像为 `[miniprogram/static/images/avatar-default.png](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/static/images/avatar-default.png)`，只在前端展示层使用。
- 浏览历史：“我的”新增“最近看过”，服务端接口按登录用户读取现有 `view_events`，显示标题和 `YYYY-MM-DD HH:mm`；点击按资料类型返回公开资料页或卡片页。只保留其他用户仍公开的目标，不展示联系方式和雷达数据。
- 验证：后端 `tests/test_app.py` 与 `tests/test_postgres_repository_schema.py` 共 `185 passed`；小程序全量 JS `node --check`、69 个 JSON 解析、`git diff --check` 通过。
- 对抗式审查已覆盖：游客不被首屏强制登录；生产跨用户历史查询 403、无 Token 401；撤回资料不会回退旧卡片；回跳动作只允许受控留言；头像上传失败不会覆盖旧值。
- 人工验收顺序：微信开发者工具重新编译 -> 游客打开卡片 -> 点击留言 -> 登录 -> 自动进入消息会话 -> iOS/Android 选择头像并重新进入小程序 -> 删除/换设备后查看“最近看过” -> 撤回资料后确认历史不再打开。不要部署后端，不要上传微信体验版之外的生产版本。
- 生产开关备注：现有后台截图表示“客户信息链已开启”，同时按钮显示“开启会员支付”，即当前会员支付开关为关闭；本轮没有修改这两个状态。

# 2026-08-23 最新交接：最近看过单条预览与独立历史页

- 已将“我的”页的最近看过改为只展示最新 1 条；多于 1 条时显示“查看全部浏览记录”。
- 新增主包页面 `/pages/view-history/index`，展示服务端返回的公开浏览记录，点击 note/card 分别进入现有公开资料页或卡片页。
- 历史页不会在打开时强制登录；未登录只显示可选登录入口。我的页和历史页都区分加载失败与空记录。
- 关键文件：`miniprogram/pages/profile/index.js`、`miniprogram/pages/profile/index.wxml`、`miniprogram/pages/view-history/index.js`、`miniprogram/pages/view-history/index.wxml`、`miniprogram/app.json`。
- 已验证：全量小程序 JS `node --check`、70 个 JSON、后端 185 passed、`git diff --check`。未部署后端，未上传微信体验版。
- 人工验收：登录用户有 2 条以上记录时“我的”仅显示最新 1 条；点击“查看全部”进入列表；点击记录回公开资料；下架资料不出现；游客不被强制登录；网络失败可重试。

# 2026-08-23 最新交接：主包超限已通过默认头像压缩修复

- 真机调试上传报错原文：`main package source size 3089KB exceed max limit 2048KB`；根因是默认头像原图过大，不是网络故障。
- 已将 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/static/images/avatar-default.png` 从 1254×1254、约 1.5MB 压缩为 256×256、75.7KB；文件名和引用路径未变。
- 当前本地精确字节估算主包约 1851.1KB，低于 2048KB；没有迁移页面，没有改公开分享路径，没有部署后端。
- 已通过全量小程序 JS、70 个 JSON、59 个配置页面路径、头像资源完整性、后端 185 passed、`git diff --check` 和对抗式主包检查。
- 下一步必须在微信开发者工具重新编译并上传体验版，确认工具实际显示主包低于 2MB；若仍超限，再基于工具新报告迁移下一批非首屏页面，不要先动公开分享页。

# 2026-08-23 最新交接：最近看过空状态文案

- “我的”页接口成功但没有记录时显示“暂时没有最近的记录”。
- 接口失败时仍显示“最近看过暂时无法读取，点击重试”，用于区分网络/API/登录问题，不能误判为空记录。
- 已完成相关 JS、JSON、后端专项测试和 `git diff --check`；需随前端重新编译上传体验版后人工确认两种状态。

# 2026-08-23 最新交接：生产浏览记录接口缺失

- 已确认真机显示“暂时无法读取”的直接原因：生产 `/api/view-history` 对已登录用户返回 404；生产 `/health` 正常，未发现缓存跨用户污染。
- 生产容器和服务器代码目录没有浏览记录路由；本地后端已有实现，但尚未部署。因此仅重新上传前端不能解决该问题。
- 最小修复范围：部署 `backend/app/api/routes_cards.py`、`backend/app/services/app_service.py`、`backend/app/services/repository.py` 的本地浏览记录改动；不改客户信息链、支付开关、生产 `.env` 或数据卷。
- 当前没有部署。部署前需用户明确确认；部署后验收有效会话返回 200、空记录返回空数组、已有记录返回公开资料，并验证跨用户仍 403。

# 2026-08-23 最新交接：浏览记录后端已完成最小生产部署

- 用户已明确要求部署；生产已补齐 `backend/app/api/routes_cards.py`、`backend/app/services/app_service.py`、`backend/app/services/repository.py` 的浏览记录实现。
- 线上备份：`/home/ubuntu/teamBuy/.deploy-backups/view-history-20260823203926/`。未改生产 `.env`、secrets、客户信息链、支付开关、数据库数据、媒体卷或 worker。
- 已重启 `teambuy-backend-1`；公网 `/health=200`。`/api/view-history` 已从原来的 404 进入生产鉴权，未登录和伪造 Bearer 均返回 401；数据库确认 `view_events.viewer_user_id` 存在；容器内空历史查询返回 `[]`。
- 对抗式审查已完成：按 viewer 隔离；生产鉴权约束 `userId`；仅保留其他用户仍发布的 note/card；撤回资料不回退旧关联卡片；结果不包含客户联系方式和雷达字段；未触碰公开资料分享、客户雷达、会员门禁、订阅消息和企业微信绑定链路。
- 仍需人工验收：重新打开已登录小程序，确认“我的 → 最近看过”能读取真实记录；确认没有记录时显示“暂时没有最近的记录”；用两个账号确认不能读取对方历史。有效会话的 200/跨用户 403 尚未从生产客户端直接取令牌验证。
- 前端仍由负责人在微信开发者工具手动编译、上传和验收；本次没有上传微信体验版，也没有重新部署生产前端。

# 2026-08-23 最新交接：企业客户服务插件已升级到 1.4.8

- 微信开发者工具确认插件 AppID/provider `wx104a1a20c3f81ec2` 的最新版本为 `1.4.8`；项目已将 `miniprogram/app.json` 中 `contactPlugin.version` 从 `1.4.3` 改为 `1.4.8`。
- `plugin://contactPlugin/cell`、首页/资料页联系我组件、`df29f3fd3ddc95bfec70e60cef93730c` 配置 ID 和后端企业微信动态绑定卡片链路均未修改。
- 已通过首页/资料页 JS 语法检查、相关 JSON 解析、插件配置断言和 `git diff --check`；本轮未部署后端。
- 下一步由负责人在微信开发者工具重新编译并上传体验版，人工确认：首页联系我、资料页联系我、添加企业微信后的动态绑定卡片、旧 iOS/Android 真机兼容性。不要在旧 `1.4.3` 版本上继续提交审核。

# 2026-08-23 最新交接：首页今日雷达与待跟进视觉状态

- 本地已完成：`miniprogram/pages/home/index.js`、`index.wxml`、`index.wxss` 将首页 banner 雷达改为今日已分享 / 今日打开 / 今日待跟进；数据来自已有 `todayBusinessSummary`，未新增后端接口。
- 本地已完成：`backend/app/services/app_service.py` 将资料预览直接分享事件纳入今日/累计分享统计，并从打开访客和资料级今日打开明细中排除分享动作，避免“分享一次同时多算一个访客”。
- 本地已完成：`miniprogram/pages/visits/index.wxml`、`index.wxss` 为待跟进队列卡片增加浅绿色背景和“待跟进”徽标；高意向待跟进仍为浅黄色，普通访客卡片保持白色。
- 加载客户数据时会先清空当前页面实例中的雷达数字，防止切换用户时短暂显示上一用户信号；累计统计逻辑未改。
- 已验证：相关 JS `node --check`、70 个 JSON 解析、`git diff --check`、后端客户信息链专项 2 个测试通过；回归覆盖资料预览分享、展示页打开、今日访客和资料级明细口径。
- 当前未部署后端、未上传微信体验版。负责人需在微信开发者工具重新编译后人工确认：首页三圈数字确实为今日口径；“雷达 → 待跟进”卡片为草绿色/浅黄色；“雷达 → 访客”卡片仍为普通白色；切换账号或退出登录不会残留上一用户雷达数字。

# 2026-08-23 最新交接：首页今日雷达统计补丁已部署生产

- 用户已确认部署；生产只更新 `/home/ubuntu/teamBuy/backend/app/services/app_service.py` 的最小统计补丁，备份位于 `/home/ubuntu/teamBuy/.deploy-backups/radar-share-20260823224129/`。
- 线上统计规则：资料预览 `viewType=share` 计入累计/今日分享；不计入累计/今日打开访客，也不计入资料级今日打开和访客明细。展示页原有分享统计保持不变。
- 已重启 `teambuy-backend-1`；本地端口 8004 和公网 `/health` 均返回 200，数据库健康；没有修改生产 `.env`、secrets、数据、媒体卷、客户信息链开关、会员支付开关或 worker。
- 对抗式审查结果：已覆盖“资料直接分享漏计”“分享被重复算打开/访客”“资料级明细与首页总计不一致”三个反例；生产补丁以精确替换并校验唯一匹配，未同步无关脏改动。
- 前端仍需负责人在微信开发者工具重新编译并上传体验版；人工验收：首页三圈为今日已分享/打开/待跟进，雷达待跟进卡片为草绿色或高意向浅黄色，访客卡片保持普通样式，并确认切换账号不残留旧数字。

# 2026-08-23 最新交接：首页漏斗时间筛选 UI 已按参考图收紧，后端已部署

- 生产已补齐 `rangeSummaries.today/last7/total`；备份位于 `/home/ubuntu/teamBuy/.deploy-backups/radar-range-20260823231400/`，仅重启 API 容器，未修改生产配置、数据、客户信息链、会员支付或 worker。
- 首页反馈区已按最新参考图改为左右同排：左侧“客户反馈”及说明，右侧“今日 / 近7日 / 累计”；下方显示当前周期数据，三列漏斗卡使用“已分享 / 打开访客 / 待跟进”。缓存路径必须同时存在 `today/last7/total` 才显示筛选。

- 已修复此前遗漏：`miniprogram/pages/home/index.wxml` 现在包含“今日 / 近7日 / 累计”筛选，默认累计；数字只作用于“发出去以后”的发出、打开、跟进三项。
- `backend/app/services/app_service.py` 新增 `rangeSummaries`：今日、近 7 日、累计；近 7 日按上海时区自然日计算，跟进为该时间段产生且当前仍待处理的线索。
- 首页雷达三圈仍固定今日，不会被漏斗筛选改变；筛选未改变主行动卡；用户切换或请求失败会清空漏斗数据。
- 为防止新前端先于后端上线时显示假数据，只有收到 `today/last7/total` 三组服务端字段才显示筛选控件；当前生产已返回新增 `rangeSummaries`，前端仍需重新编译上传后才会显示。
- 本地验证：相关 JS `node --check`、70 个 JSON、`git diff --check`、后端客户看板专项 2 个测试通过。
- 下一步：由负责人在微信开发者工具重新编译、上传体验版，并人工验收三个筛选的数字、默认累计、跟进状态、切换账号隔离和真实登录会话业务接口返回。

# 2026-08-24 最新交接：客户信息免支付与收费门禁前端分流

- 本地已完成客户访问状态收口：`miniprogram/utils/customer-access.js` 统一返回 `disabled/payment_required/allowed/unknown`。
- 免支付模式直接放行：雷达、客户详情、客户资料库、线索页和工作台客户入口不再因 `active=false` 跳会员页。
- 收费未会员直接进入 `/pages/membership/index`；雷达锁定状态提供“去支付”，删除原“发现客户信号 / 知道了”无行动弹窗。
- 客户详情已拆分客户信号暂未开放、收费门禁、客户信号已更新返回雷达、真实接口异常重新读取，不再把权限状态伪装成读取失败。
- 客户情报缓存清理会提升代次；收费非会员进入雷达时清理当前用户/模式缓存，旧 in-flight 解锁结果不能回写。
- 验证：相关 JS `node --check`、客户访问/缓存竞态 smoke test、70 个 JSON、`git diff --check`、后端客户信息链与销售 SCRM `15 passed`。
- 本轮没有后端代码部署、没有修改客户信息链开关或会员支付开关、没有上传微信体验版。下一步由负责人在微信开发者工具重新编译上传并人工验证免支付直达详情、收费非会员支付、支付成功后恢复、功能关闭中性空态和跨账号联系方式隔离。

# 2026-08-23 最新交接：接手核验首页漏斗，无需重复实现

- 已复核首页三文件：`miniprogram/pages/home/index.js`、`index.wxml`、`index.wxss` 保留最新参考图要求；本轮未发现明确缺陷，未修改业务代码。
- 对抗式检查结果：范围数据缺任一组时不显示筛选；用户切换与异步返回有身份守卫；反馈切换不改今日雷达；客户信息链仍由会员/后端权限控制；分享、发布和支付代码未被本轮触碰。
- 验证通过：全量小程序 88 个 JS `node --check`、70 个 JSON 解析、`git diff --check`；`.venv312` 下定向 pytest `15 passed`。系统 Python 3.9 和 `.venv` 3.9 不适配 `dataclass(slots=True)`，未修改环境。
- 未部署生产、未上传微信体验版。下一步由负责人重新编译上传后人工确认左右同排、默认累计、三周期数字同步、三列卡片、浅绿色待跟进、雷达今日口径、切换账号隔离和真实登录态业务接口。

# 2026-08-24 最新交接：客户资料门禁已按最终规则简化

- 当前前端唯一付费门禁是 `paymentRequired=true && active=false`；只有这一种状态进入 `/pages/membership/index`。
- 支付关闭时不再判断会员身份，雷达、客户详情、客户资料库、线索列表和线索详情直接读取；支付开启且已是会员也直接读取。
- 客户详情首次进入绕过雷达情报缓存，直接读取最新客户情报；身份匹配仍兼容 `user:`/`anon:` 前缀、画像、告警和时间线投影。
- 已移除详情页“这条客户信号已更新”错误态以及客户雷达中“当前查看模式已开放”等复杂解释；异常统一为可重试态，未根据路由参数伪造客户信息。
- 未修改后端、客户信息链开关、会员支付开关或生产配置；未部署生产，未上传微信体验版。
- 已验证：全量小程序 JS `node --check`、87 个 JSON、`git diff --check`、客户信息链/销售 SCRM pytest `15 passed`、四种会员/支付组合门禁 smoke test。
- 下一步由负责人在微信开发者工具清缓存并重新编译；重点人工验证：支付开+非会员跳支付、支付开+会员直达、支付关+会员/非会员均直达，以及切换账号不残留联系方式。

# 2026-08-24 最新交接：匿名客户身份与定向详情已完成本地修复

- 本地已新增稳定 `visitorIdentityId` 并贯穿 `ViewEvent`、`ShowcaseEvent`、`CustomerAction`、`LeadReminder`、雷达告警/画像和客户详情；匿名浏览页、展示页、卡片页共用本地匿名 ID。
- 客户详情接口现在先执行 owner/会员门禁，再按目标身份扫描原始事件、动作和线索；旧事件无匿名 ID 时按持久化事件 ID 兼容，解决同一批待跟进卡只有部分能打开的问题。
- 详情联系方式统一返回已授权的登录客户电话/微信/邮箱、客户主动提交字段和跟进线索字段；锁定态不返回客户对象，跨 owner 仍 403。
- 新增回归覆盖 3 个匿名 ID、1 个无匿名 ID 的旧事件逐个定向打开详情，以及邮箱透传；销售 SCRM、客户信息链和 `test_app.py` 共 `199 passed`。
- 相关 JS `node --check`、JSON 解析、`git diff --check` 已通过。未部署生产后端，未上传微信体验版；负责人需在微信开发者工具清缓存、重新编译后人工验证支付开/关、会员/非会员和三个待跟进卡详情。

# 2026-08-24 最新交接：客户详情后端已部署并兼容旧请求

- 生产已部署稳定 `visitorIdentityId`、定向客户详情和已授权联系方式汇总；部署目标仅为 `app_service.py`、`domain.py`、`cards.py`，备份位于 `/home/ubuntu/teamBuy/.deploy-backups/customer-identity-20260824033921/`。
- 另发现旧前端详情参数重复 URL 编码导致 `%253A` 身份无法匹配，已追加后端一层残留编码兼容并重启 API；备份位于 `/home/ubuntu/teamBuy/.deploy-backups/customer-detail-encoding-20260824034241/`。
- 生产容器、数据库健康；公网 `/health` 返回 `ok`，详情路由未授权请求仍由鉴权拦截；最近错误日志无 Traceback/ImportError/SyntaxError/CRITICAL。
- 本地验证：相关后端 `199 passed`、全量 JS `node --check`、70 个 JSON、后端 AST、`git diff --check` 均通过；对抗式审查确认 owner 隔离、会员门禁、锁定态不返回联系方式、重复编码兼容不改变授权边界。
- 负责人下一步：在微信开发者工具清缓存、重新编译并上传体验版，使用“支付开/非会员、支付开/会员、支付关/两种会员状态”验证详情；再用三个待跟进卡确认每个客户均能打开，不要只看旧缓存截图。

# 2026-08-24 最新交接：待跟进建档与联系方式区域已在本地完成

- 本地新增 `POST /api/scrm/customer-followups/ensure`，待跟进客户在客户详情点击“开始跟进”后自动创建/复用档案并进入 `/pages/lead-detail/index`；已有档案直接打开，不重复创建。
- 修复展示页总览事件没有 `noteId` 导致的误报：详情接口会从匹配展示页条目回溯真实资料；当前回归已覆盖登录客户、匿名客户和展示页级事件。
- 客户详情已增加电话/微信号/邮箱区域，电话可拨打或复制，微信号/邮箱可逐项复制；联系方式只在服务端解锁后的页面内存中存在，不进入 `wx` 持久化缓存。
- 详情页已取消重复会员状态请求，直接以客户详情接口的门禁结果决定是否跳会员页，减少无效请求；客户情报缓存仍按环境、owner、mode 隔离，建档后只清理当前 owner/mode。
- 本地验证：全量 JS `node --check`、JSON 解析、后端编译、`git diff --check`；SCRM/客户信息链 `17 passed`，主应用 `182 passed`。本轮没有部署新后端接口，没有上传微信体验版。
- 上线前阻塞：生产环境尚未包含本轮 `customer-followups/ensure` 路由及展示页来源回溯补丁；负责人需先明确授权后端定向部署，再在微信开发者工具清缓存、重新编译上传体验版。

# 2026-08-24 最新交接：缓存策略已收敛，后端已部署，前端待上传

- 本地已将资料/合集/卡片元数据前后端缓存调整到 30 分钟，专题/分类 2 小时，客户情报 10 分钟；会员状态和提醒配置为 5 分钟用户隔离内存缓存。
- 雷达 `onShow` 仍负责恢复页面，但不再每次向服务端重复请求会员状态；缓存命中时无会员/情报网络请求，缓存过期或显式重试才访问后端。支付成功轮询使用强制会员请求。
- 普通浏览、分享和附件查看不再清空全量客户情报/资料列表缓存；资料写入、发布、撤销、跟进和高价值联系方式动作仍立即失效对应缓存。
- 客户情报和联系方式仍只允许短时内存保存，账号切换/退出清理用户缓存；客户详情、线索接口和后端会员门禁未放宽。
- 验证已通过：相关 JS `node --check`、全量小程序 JSON 解析、`git diff --check`、SCRM/客户信息链 `17 passed`。由于当前 `.venv` 是 Python 3.9，测试使用 Codex Python 运行时完成。
- 后端缓存补丁已定向部署生产：资料/卡片/合集列表 30 分钟，客户情报 10 分钟；普通浏览、分享和附件查看不再清空全量缓存。备份位于 `/home/ubuntu/teamBuy/.deploy-backups/cache-policy-20260824053318/`。
- 本次未修改客户信息链开关、会员支付开关、生产配置、数据库和媒体数据；API、后台任务、归档任务已重启，公网 `/health` 正常。
- 下一步由负责人在微信开发者工具清缓存、重新编译并上传体验版，人工验证冷启动、页面返回、缓存命中、资料更新立即刷新、支付开关/会员状态和切换账号隔离。不要重复部署后端。

# 2026-08-24 最新交接：开始跟进 404 已修复，后端已部署

- 5:37 左右“开始跟进”报 `not found` 的根因是生产未注册 `POST /api/scrm/customer-followups/ensure`，不是客户详情或前端点击参数问题。
- 已定向部署 `app_service.py`、`routes_scrm.py`、`schemas/scrm.py` 的最小补丁；生产备份位于 `/home/ubuntu/teamBuy/.deploy-backups/customer-followup-20260824054119/`。
- 现在未登录请求该接口返回 401，说明路由已生效；生产公网 `/health` 返回 200。没有用真实客户数据做自动建档。
- 请用当前体验版重新进入“雷达 → 待跟进 → 查看资料 → 开始跟进”复测；预期已有档案直接复用，没有档案自动创建并进入线索详情。若前端仍缓存旧页面，先清缓存并重新编译体验版。

# 2026-08-24 最新交接：客户详情合并跟进，旧线索页已删除

## 当前结果

- 客户详情页是唯一单客户页面，已内嵌跟进记录、下次跟进时间、保存跟进和最近日志。
- 旧 `miniprogram/pages/lead-detail/` 页面已删除，旧路由引用已清理。
- 资料卡“三个点”可进入“线索详情”；合集卡片“三个点”可进入“线索详情”，并保留合集原有“资料运营”。两者都先进入来源过滤后的客户雷达，再进入统一客户详情。
- 资料来源过滤增加当前用户隔离，客户详情切换账号会清理并重载，联系方式仍只在内存中短时使用。

## 已完成检查

- 相关前端 JS `node --check` 通过。
- 相关 JSON 解析通过。
- `git diff --check` 通过。
- `./.venv312/bin/pytest -q backend/tests/test_sales_scrm.py` 通过（13 passed）。
- 已完成一次对抗式审查，并修复菜单超过六项、优先处理列表筛选后无法打开两个问题。

## 下一步人工验收

1. 在微信开发者工具清缓存并重新编译体验版。
2. 资料卡/合集卡片“三个点 → 线索详情 → 待跟进客户 → 客户详情”。
3. 在客户详情直接记录跟进、设置下次跟进时间并保存，确认返回后日志和状态更新。
4. 检查联系方式展示与逐项复制、支付开关/会员门禁、切换账号隔离、旧分享链接和浏览历史。

本轮没有部署生产后端，也没有上传微信体验版。

# 2026-08-24 最新交接：后端已核对，未做无意义重部署

- 生产运行容器 `teambuy-backend-1` 已包含本轮前端依赖的客户详情、客户跟进建档、跟进保存、会员门禁和缓存策略；公网客户详情/建档接口未登录返回 401，健康检查 200。
- 本轮没有整体同步本地后端脏工作区，也没有重启生产服务；原因是当前所需后端方法与生产一致，整文件部署风险高于收益。
- 旧 `lead-detail` 页面和运行时路由已清理完毕。
- 未执行批量清理。`git clean -n` 显示会误删客户详情、会员、缓存、分包和 SCRM 测试等当前功能，后续清理需要明确文件清单。

# 2026-08-24 最新交接：资料运营页已补齐，旧线索详情已移除

## 当前入口

- 资料卡“三个点 → 资料运营 → 资料运营页”。
- 合集卡片“三个点 → 资料运营 → 合集运营页”。
- 资料反馈提示、资料详情页和待联系页的资源级入口也进入资料运营。
- 资料运营页中的“客户详情”直接进入统一客户详情，不经过雷达。

## 已删除

- 旧 `miniprogram/pages/manager/` 资源级线索详情页。
- `app.json` 和导航标题中的 manager 路由。
- 资料/合集菜单中的“线索详情”和对应雷达跳转。

## 验证

- 相关 JS `node --check`：通过。
- 相关 JSON 解析：通过。
- `git diff --check`：通过。
- SCRM 后端 pytest：`13 passed`。
- 对抗式断言：资料/合集菜单无旧线索/雷达入口，资料运营页接入资料和客户动作接口，未写入本地持久化缓存。

前端仍需由负责人在微信开发者工具清缓存、重新编译并上传体验版后人工验证资料卡、合集卡片、资料运营统计和客户详情入口。

# 2026-08-24 最新交接：合集入口与头像相册选择修正

## 本轮已完成

- 合集卡片操作顺序已与资料卡统一：更多菜单在上，发客户/编辑/重新发布在下。
- 合集不再显示“准备封面”“重试封面”；分享图准备过程显示为用户可理解的发送准备状态，未就绪不触发分享。
- 合集列表、预览、编辑的分享回调和异常兜底都直接指向当前合集详情页，避免客户收到管理列表路径。
- 登录页和个人资料编辑页点击头像会打开本地相册；保存时上传临时文件并提交服务端地址。微信头像选择仍可通过旁边的备用按钮使用。

## 验证与人工验收

- 相关 JS、JSON、`git diff --check` 和 SCRM pytest 已通过；对抗式静态断言已通过。
- 前端没有上传体验版。负责人需在微信开发者工具清缓存、重新编译后验证：合集按钮顺序、分享图准备/失败重试、登录页选图、个人资料选图、取消选图不丢头像、保存后重新进入头像仍存在。

# 2026-08-24 最新交接：资料运营页按效果图重做

## 本轮结果

- `miniprogram/subpackages/workbench/resource-analytics/` 已按参考图重做为单资料运营页：摘要卡、三项统计、趋势图切换、高意向访客、来源与动作、接龙状态。
- 旧的绿色运营提示、四项客户动作统计和长客户动作列表不再出现在该页面；资料三个点和页面主按钮仍分别进入查看资料、编辑资料、资料运营。
- 资料运营页在读取统计/客户动作前校验资料所属用户；支付门禁仍复用现有会员状态规则，未修改支付开关和客户信息链。
- 后端本地补充卡片趋势字段，但本轮未部署生产后端；小程序体验版也未上传。

## 验证与人工验收

- 已通过相关 JS `node --check`、页面 JSON 解析、资料运营结构断言、`git diff --check`、卡片统计 pytest `3 passed`、SCRM pytest `13 passed`。
- 人工验收需在微信开发者工具清缓存并重新编译：打开资料三个点查看菜单；检查摘要卡和三项统计；切换近7日/今日；检查高意向访客“查看客户”；确认没有绿色运营提示和长客户动作列表。
- 趋势真实数据需在对应后端版本部署后验收；未部署前不要把兼容降级曲线当作历史统计结论。

# 2026-08-24 最新交接：头像入口与合集资料运营统一

## 本轮结果

- 登录页、个人资料弹窗已经删除微信头像按钮和 `chooseAvatar` 事件，只保留“相册换头像/点击从相册更换”。
- 合集卡片的“发客户”和三个点已按资料卡操作区拉开间距。
- 合集所有资料运营入口统一进入 `/subpackages/workbench/resource-analytics/index?entityType=showcase&showcaseId=...`；资料仍进入同一页面的资料模式。旧合集运营路由仅做兼容重定向。
- 本轮未部署后端，未上传微信体验版。

## 验证与人工验收

- 已通过相关 JS `node --check`、相关 JSON 解析、头像/入口/间距静态断言、`git diff --check`、SCRM pytest `13 passed`、统计隔离 pytest `2 passed`。
- 微信开发者工具需清缓存后人工验证：登录页头像只弹相册；个人资料头像只弹相册且保存后仍存在；合集卡片主按钮和三个点间距；资料与合集进入相同资料运营布局；客户功能关闭时合集类型仍显示正确。

# 2026-08-24 最新交接：合集页底部 Tab 静态验收完成

## 结论

- 合集页自定义底部 Tab 结构完整：加载中、首屏空数据、正常列表、筛选弹窗四种路径均保留 Tab 节点；“资料”默认蓝色选中。
- 首页、资料、雷达、我的四个入口均调用 `wx.switchTab`，并与 `app.json` 原生 `tabBar` 路径一致；资料页未引入合集自定义 Tab。
- 新建合集固定操作条位于 Tab 上方并适配 `env(safe-area-inset-bottom)`；筛选弹窗覆盖 Tab 属于预期 modal 行为，避免背景交互。

## 验证

- 相关小程序 JS `node --check`、目标 JSON 解析、静态路由/状态/安全区断言、`git diff --check` 通过。
- `./.venv312/bin/pytest -q backend/tests/test_showcase_scene_mapping.py`：`4 passed`。
- 本轮没有修改业务代码、没有新增后端接口、没有部署生产、没有上传微信体验版。

## 人工验收

- 负责人需在微信开发者工具清缓存并重新编译：分别检查加载中、空数据、正常列表、打开筛选面板时的底部结构；在 iPhone 刘海屏/手势区和小屏设备确认固定新建按钮不压住 Tab、文字上下左右居中，并验证四个入口实际切换。

# 2026-08-24 最新交接：推广奖励与供需发布功能暂存本地

## 本轮结果

- `referral-center`、`supply-demand-publish`、`supply-demand-my`、`supply-demand-detail` 页面源码仍保留，但已从小程序 `app.json` 路由中移除。
- 个人页和会员页不再展示推广收益/分佣规则入口；商机页和供需页不再展示发布需求/供给入口；商机 Tab 中暂时隐藏供需广场。
- 个人页不再为已产生客户信号请求推广中心数据。
- 本轮未删除本地源码，未修改后端，未部署生产，未上传微信体验版。

## 人工验收

- 微信开发者工具清缓存并重新编译。
- 检查个人页、会员页不再出现推广收益/分佣规则。
- 检查商机页不再出现供需广场 Tab，供需发布入口不可见。
- 确认资料、合集、客户雷达和会员客户信息链入口未被误伤。

# 2026-08-24 最新交接：自动化控制面第一段已完成（未接设备）

## 当前结果

- 建立一台 Android 手机、一个 ESP32 HID、两个串行微信账号的设备/任务模型。
- 新增自动化 API：设备心跳、PC 创建任务、设备领取任务、成功/失败回传、群候选保存与 PC 查询。
- 任务支持 `targetWechatAccountId`、幂等键、单设备运行租约和账号上下文校验；PC 操作令牌与设备令牌已分开。
- 群候选目前只落库群二维码引用、群名、保存时间、加入状态、是否可发送和错误摘要，不保存截图二进制。

## 当前未做

- 没有连接真实安卓手机或 ESP32，没有写小红书搜索、加微信群、扫描微信群或群营销动作。
- 没有部署生产，也没有上传微信体验版。
- 生产前仍需补设备级令牌轮换、限流、审计和账号切换的真实页面证据。

## 验证

- 自动化控制测试：`backend/tests/test_automation_control.py`，`4 passed`。
- Python compileall、`git diff --check` 通过。
- 全量后端测试：`273 passed, 1 failed`；失败为既有 `test_note_preview_view_updates_note_list_stats`，分享事件后的 `shareCount` 仍为 0，本轮未改动该业务链路。

## 下一步最小顺序

1. 配置 PC 操作令牌和设备令牌，不提交真实密钥。
2. 先连接一台 Android 手机和 ESP32，运行 AScript 设备/HID 自检，只验证连接、运行模式和截图/选择器读取，不执行加群或群发。
3. 让 AScript 完成“心跳 → 领取 `xhs.find_group` → 只读搜索 → 回传群候选 → 完成任务”的垂直切片。
4. 再加入微信 1/微信 2 的账号切换与 `wechat.join_group`，最后才接 PC 人工审核后的营销发送。

# 2026-08-24 最新交接：真实设备入口联调完成

## 当前结果

- 一台 Android 手机与 AScript 已连接，运行模式为 HID；手机上的 `wechat_launcher` 工程可以稳定启动自定义九宫格。
- 微信入口实测会出现系统双开选择器；左侧和右侧两个微信实例均已分别进入 `com.tencent.mm`，并读取到微信首页节点。右侧微信测试截图显示了已有群列表。
- 小红书入口也会出现双开选择器；左侧实例进入 `com.xingin.xhs` 后读取到“首页/发现/消息/我”。本产品的小红书找群按一个固定实例设计，但尚未做固定实例配置。
- 本轮只验证打开应用和实例选择，没有搜索、扫码、加群、群聊天、资料卡发送或营销动作；手机当前已恢复到 AScript 工具箱。

## 已确认风险

- 现有 launcher 只按包名打开应用，没有显式的微信 1/微信 2 或小红书固定实例选择；下一版必须把系统双开选择器纳入状态机，并在业务动作前确认账号身份。
- 左右实例与真实微信昵称尚未完成映射；不能把“左侧=微信 1”作为永久事实写入后端。需要负责人分别确认两个实例对应的账号昵称/标识。
- AScript 状态显示蓝牙权限未授予，未检索到 ESP32/Bluetooth API；本轮只能确认 HID 动作导致了手机页面变化，硬件握手仍需设备/手机可观测证据补强。
- 应用刚启动时屏幕截图可能暂时全黑，控件树随后已能读取目标包和页面锚点；后续脚本必须等待并做多证据验收。

## 下一步最小顺序

1. 由负责人确认左/右双开实例分别对应的微信昵称，以及固定小红书采集实例；只记录内部账号标识，不把昵称写入前端任务参数。
2. 修改/同步 AScript launcher：把微信 1、微信 2、小红书采集账号做成显式入口，处理系统双开选择器并在切换后回传目标账号和页面锚点。
3. 先做 `xhs.find_group` 只读垂直切片：PC 下发关键词，AScript 读取小红书结果并回传最小群候选；通过后再做微信 `join_group`。
4. 最后接 PC 审核后的群营销任务；所有发送前仍需目标微信账号、群范围、资料卡和人工审核状态齐全。

## 验证状态

- 本轮未修改 teamBuy 业务代码、未调用自动化后端、未部署生产、未上传微信体验版。
- 入口验证使用 AScript 设备状态、截图、控件树、Selector 唯一性和运行结果；未进行真实加群或群发验收。

# 2026-08-24 最新交接：双开微信身份探针已接入，左侧设备阻塞待处理

## 当前结果

- 新增 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/automation/ascript/wechat_account_identity/__init__.py`：通过一个 ESP32 HID 串行读取两个微信实例的登录昵称，按昵称生成内部账号 ID，可选心跳上报 PC。
- 当前真实设备运行结果为 `degraded`：右侧微信昵称 `leo` 已验证；左侧微信从双开选择器点击后黑屏，等待 45 秒仍未出现微信页面，因此没有写入左侧昵称。
- 账号识别不依赖左右位置作为永久身份；重复昵称会标记为歧义，部分账号或重复账号不会上报 `ready`。
- PC 控制面已有 `GET /api/automation/devices`（操作令牌）读取设备与昵称元数据；设备令牌只负责心跳/任务执行，不可读取设备列表。

## 验证

- AScript `wechat_account_identity` 已实际部署运行，ESP32 `AS_4.8_905DDCBA2010` 连接成功，右侧 `leo` 识别成功，左侧失败状态被保留。
- 左右单独入口探针及左侧 45 秒冷启动探针已完成；测试结束手机已恢复 `wechat_launcher` 启动器。
- `backend/tests/test_automation_control.py`：`5 passed`；backend/app 与 backend/tests compileall 通过；`git diff --check` 通过。
- 对抗式审查发现并已修复 `degraded` 心跳状态未被后端 schema 接受的问题；当前回归仍为 `5 passed`。
- 本轮没有生产部署、没有上传微信体验版、没有向后端发送真实 heartbeat（AScript 的 `BACKEND_URL`/`DEVICE_TOKEN` 仍为空）。

## 对抗式审查与剩余风险

- 已检查：操作令牌/设备令牌分权、账号不完整不得 ready、重复昵称阻断、后端未配置不联网、目标账号上下文仍由 PC 任务控制。
- 当前阻塞：左侧微信实例在本机黑屏，不能进入加群和营销链路；需要负责人先在手机上确认左侧微信能手动正常打开，之后再复测账号昵称。
- 后续生产前仍需设备级令牌轮换、限流、审计、稳定账号标识，以及左侧实例恢复后的双账号回归。

## 下一步最小顺序

1. 人工恢复/确认左侧微信实例可正常启动；重新运行账号探针，记录真实昵称。
2. 配置后端地址和设备令牌（只放 AScript/设备安全配置，不提交仓库），验证 heartbeat 在本地测试环境落库。
3. 先实现只读 `xhs.find_group`：PC 下发关键词，AScript 读取候选，回传最小字段。
4. 再实现微信 `join_group`，最后才接人工审核后的群营销任务。

## 2026-08-24 最新交接：生产新建资料 500 已定位，尚未修复

- 生产 `teambuy-backend-1` 在 21:00:39 至 21:01:47（北京时间）连续 5 次 `POST /api/notes/quick-capture` 返回 500。
- 根因：线上 `backend/app/services/app_service.py` 的 `_build_user_note_from_note_draft()` 读取 `note_draft.contentBlocks`，但线上 `backend/app/schemas/skills.py` 的 `UserNoteDraftPayload` 没有该字段，schema 与 service 版本漂移。
- 线上 `/health` 正常；异常发生在保存资料之前。本轮没有改生产、没有重启容器、没有部署修复。
- 下一步最小顺序：补齐并测试 `contentBlocks` 契约 → 在同构运行容器回归 `POST /api/notes/quick-capture`、更新和发布链路 → 备份后部署生产 → 公网验证 200。部署前不要只执行健康检查。

## 2026-08-24 最新交接：生产新建资料 500 已修复并完成对抗式审查

- 生产修复已完成：`UserNoteDraftPayload` 补齐 `contentBlocks` 默认字段；`_build_user_note_from_note_draft()` 对缺失字段使用兼容读取。
- 生产备份：`/home/ubuntu/teamBuy-backups/quick-capture-schema-fix-20260824-211437/`。API、backend-worker、archive-worker 已重启；未改 `.env`、secrets、数据库和媒体目录。
- 验证：本地资料创建/编辑/发布相关测试 `9 passed`；生产公网 `/health` 200；无会话创建探测为 401；容器内 current-schema/legacy-schema builder dry-run 均通过；重启后无新 5xx/Traceback。
- 对抗式审查：修复 diff 仅包含预期 schema/service 两处；ContentMediaPayload 字段在线上已存在；没有创建生产测试资料；分享回调未改动，微信开发者工具人工分享验收仍不纳入本次完成结论。
- 已知剩余项：完整后端测试仍有 1 个既有 `shareCount` 统计失败；未完成真实登录用户的生产“新建→编辑→发布”人工回归，需要用户重新操作一次确认体验。

## 2026-08-24 最新交接：生产新建资料二段 500 已修复，待用户最终复测

- 用户复测期间发现：第一处修复后 `POST /api/notes/quick-capture` 已为 200，但自动保存 `PUT /api/notes/{noteId}` 仍因 `UserNoteUpdateRequest` 缺少 `contentBlocks` 返回 500。
- 已完成第二处生产修复并备份到 `/home/ubuntu/teamBuy-backups/quick-capture-update-fix-20260824-212459/`；生产 schema 和 service 均已对齐，三类 worker 已重启。
- 重启后日志中的 quick-capture 与对应 PUT 均为 200，错误扫描未发现新的 5xx；本地创建/编辑/发布回归 `9 passed`，`git diff --check` 通过。
- 当前结论：代码和线上运行态已修复，但还没有替用户完成真实登录会话下的最终“新建→自动保存→发布”验收。下一步请用户重新新建一条测试资料；若仍失败，立即按新时间戳抓取新堆栈，不再沿用旧 21:23 的错误。

## 2026-08-24 最新交接：22:10 订阅消息未推送原因已确认

- 22:10 另一台手机的资料打开与浏览接口均为 200，但访问事件记录为 `anonymous`，没有 `viewerUserId`；同一时间段没有对应订阅 delivery 或发送任务。
- 当前实现对匿名访问明确不消耗订阅额度，因此这是业务规则命中，不是微信发送失败。
- 最终验收需使用：发布者已授权订阅 + 另一台手机登录不同小程序账号 + 打开同一资料。若要支持匿名点击推送，需重新评估订阅额度、隐私和频控后再开发。

## 2026-08-24 最新交接：匿名点击订阅消息已上线，待真实送达验收

- 业务规则已调整：匿名浏览也允许触发发布者订阅消息；消息只显示匿名访客和资料标题，不暴露 anonymousId 或联系方式。
- 同一发布者/资料/30 分钟窗口内的匿名访问统一去重；显式关闭普通/匿名提醒的账号仍不会推送；无显式偏好的会员账号默认开启该提醒。
- 生产备份：`/home/ubuntu/teamBuy-backups/anonymous-view-notification-20260824-222723/app_service.py`。API、backend-worker、archive-worker 已重启；AST、`/health` 200、worker 启动日志通过。
- 本地 `backend/tests/test_subscription_notifications.py`：`10 passed`；尚未主动制造生产浏览事件，下一步由用户在另一台手机点击一次，观察发布者是否收到订阅消息。

## 2026-08-24 最新交接：xhs.find_group 只读垂直切片已完成

## 当前结果

- 新增 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/automation/ascript/xhs_find_group/__init__.py`，使用一个固定小红书实例（当前实测右侧槽位）执行“打开 → 搜索 → 读取群名 → 同帧二维码解码”。
- 真机已通过 ESP32 HID 成功运行，搜索词为 `微信群`；实测输出过 `资源共享群`、`后来的我们`，最近一次稳定同帧输出为 `后来的我们` 及其 `weixin.qq.com/g/...` 邀请引用。
- 当前脚本严格只读：不打开笔记、不保存 QR 图片、不加入微信群、不进入聊天、不发营销消息。后端地址/设备令牌留空，因此没有真实任务领取、心跳或候选写入。
- 搜索结果复用必须同时满足顶部关键词和群聊标签；OCR 与 CodeScanner 复用同一屏幕快照，避免结果流刷新造成群名/二维码错配。
- 后端账号无关任务领取已修正：小红书任务传空微信账号时允许领取，完成后保留最近已确认微信昵称，不清空设备身份。
- 测试结束手机已恢复 `wechat_launcher` 九宫格。

## 验证

- AScript `xhs_find_group` 已完整部署运行成功，ESP32 `AS_4.8_905DDCBA2010` 连接成功；本地 `py_compile` 通过。
- `backend/tests/test_automation_control.py`：`6 passed`；backend/app 与 backend/tests `compileall` 通过；`git diff --check` 通过。
- 本轮未部署生产后端，未发送真实 automation heartbeat/task，未上传微信体验版。

## 2026-08-24 最新交接：订阅消息消息页已增加首页回流

- 后端消息订阅深链现在为 `/pages/message-thread/index?id={threadId}&source=subscription&autoHome=1`。
- 小程序消息页新增“回到首页”按钮；订阅入口发送成功后显示“已发送，正在回到首页”，约 750ms 后切换到 `/pages/home/index`，`switchTab` 失败时 `reLaunch` 兜底；普通消息页不自动跳转。
- 未登录时的登录 returnUrl 会保留订阅入口参数，登录后仍能执行自动回首页。
- 生产备份：`/home/ubuntu/teamBuy-backups/subscription-message-home-path-20260824-223908/app_service.py`；生产 AST、`/health` 200、后端源码深链检查通过。
- 待用户在微信开发者工具上传体验版后真机验收：订阅消息打开 → 消息页显示首页按钮 → 发送消息 → 自动回到首页；未发送时点击“回到首页”也应立即回首页。

## 对抗式审查与剩余风险

- 已检查：关键词不一致不复用旧结果、二维码为空不伪造、同帧配对、PC/设备令牌分权、账号无关任务不清空微信身份、脚本不执行加群或营销动作。
- 左侧微信实例仍在本机黑屏，两个微信账号的完整昵称映射尚未完成；因此下一步不能直接做双微信加群。
- 小红书固定槽位依赖当前双开选择器排序，设备系统或用户调整双开顺序后必须重新验证；生产前还需把设备级令牌轮换、限流和任务审计补齐。

## 2026-08-24 最新交接：生成同款性能优化已上线

- 已确认生成同款只是公开资料净化克隆和当前用户配置，不触发 AI、OCR、分享图或媒体转码；旧瓶颈是 PostgreSQL 全量 `AppState` 的两次读取和一次整体保存。
- 已改为按表读写 `same_style_generations` 和 `referral_relations`，并减少源用户重复查询；生产备份为 `/home/ubuntu/teamBuy-backups/same-style-performance-20260824-225504/`。
- 最终生成记录与裂变关系已合并为单事务写入；生产 `repository.py` 的最终回滚备份为 `/home/ubuntu/teamBuy-backups/same-style-performance-atomic-20260824-230235/repository.py`。
- 源用户对象也已在联系方式净化阶段复用，生产 `app_service.py` 的追加回滚备份为 `/home/ubuntu/teamBuy-backups/same-style-profile-read-20260824-230555/app_service.py`；最终幂等请求约 `0.0274s`。
- 部署时曾因本地完整 `repository.py` 与生产旧模型版本不一致导致容器启动失败，已恢复备份并在生产基线文件上补丁式部署；当前 API、worker 稳定，公网 `/health` 200。
- 线上已有幂等请求耗时从约 `0.468s` 降至 `0.031–0.049s`；生产 `same_style_generations=4`、`user_notes=1336`，没有创建测试资料。没有修改小程序前端，因此不需要上传体验版。
- 本地同款回归 `3 passed`，Python compileall、AST、`git diff --check` 通过。剩余人工验收：用户从公开资料页实际点击一次“生成同款”，确认快速进入编辑页且资料内容、联系方式、公开净化规则正确。

## 下一步最小顺序

1. 在 PC 端把本次只读结果接入候选列表/人工审核展示，先不自动写入长期群库，确认二维码引用字段与去重规则。
2. 人工恢复左侧微信并重新跑身份探针，确认两个真实昵称及稳定账号映射。
3. 实现 `wechat.join_group`：只接受 PC 审核通过且二维码引用完整的候选，逐个微信账号串行加入并回传加入成功/失败、保存时间和是否可发送。
4. 加群稳定后再做微信群扫描、营销任务和人工审核后的卡片发送。

## 2026-08-24 最新交接：微信原生群已接入统一 PC 群候选模型

### 当前结果

- 后端已经支持 `wechat_native` 群候选：二维码可空、账号与群名幂等、首次保存时间与最近发现时间分离，并保留人工备注和 `canSend` 审核字段；小红书候选仍必须有二维码。
- 新增 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/automation/ascript/wechat_group_inventory/__init__.py`。它会串行处理同一手机的两个微信实例，按登录昵称识别账号，读取“通讯录 → 群聊”的全部可见群名，并可按统一接口回传 PC 群库。
- 真机读取当前微信原生群列表得到 10 个群，未打开群、未加群、未发消息。当前设备脚本的 `BACKEND_URL` 和 `DEVICE_TOKEN` 为空，所以 PC 实际写入尚未完成验收；本轮没有生产部署和微信体验版上传。

### 验证与对抗式审查

- `backend/tests/test_automation_control.py`：`8 passed`；AScript 文件 `py_compile` 通过；`git diff --check` 通过。
- 已检查：小红书二维码门禁没有被原生群放宽、重复扫描不会覆盖人工备注/可发送审核、相同昵称的双微信槽位会拒绝合并、原生群不会自动加入或发消息。
- 仍有明确边界：设备配置为空时不能声称 PC 已同步；当前列表未提供稳定群 ID，同账号同名群会合并；ESP32 插件的物理 HID 通道仍需单独修复/验收。

### 下一步最小顺序

1. 在设备本地配置 PC 可访问的 API 地址和临时设备令牌（令牌不要提交 Git），运行一次后用 PC 群候选列表确认 `source=wechat_native`、真实微信昵称、群名、`savedAt`、`lastSeenAt` 均正确。
2. 让两个微信槽位分别跑一遍；确认昵称不同、账号映射正确，并确认第二次扫描不会覆盖备注和 `canSend`。
3. 再决定是否做群详情页探针获取稳定群 ID；在此之前不要用坐标或群名之外的猜测值做唯一标识。
4. 原生群库稳定后，再开发“PC 人工审核 → 指定微信串行执行加群/营销”的任务链；本轮不扩展自动聊天或营销发送。

## 2026-08-24 最新交接：原生群扫描已补齐首页会话列表来源

### 当前结果

- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/automation/ascript/wechat_group_inventory/__init__.py` 现在合并两个只读来源：通讯录群聊和微信首页会话列表向下滚动。
- 真机最终回归无错误：`高士腾` 账号通讯录 39 + 会话 7 = 46 个；`leo` 账号通讯录 10 + 会话 110 = 111 个。结果按账号与群名合并，二维码为空，`canSend` 未自动放行。
- 会话列表识别经过一次误识别对抗修正，最终结果不再包含已观察到的“服务号、公众号、微信团队、高先生、泡芙”等明显非群会话。
- 设备回传仍未实际写入 PC：最终运行输出 `preparedCount` 为 46/111，`postedCount=0`，`backendWriteConfirmed=false`，因为 `BACKEND_URL` 和 `DEVICE_TOKEN` 为空。未部署生产后端，未上传微信体验版。

### 已完成验证

- ESP32 官方插件方法已改为 `BleDevice.slide()`；最终真机双账号运行完成。
- 本地 AScript `py_compile` 通过；`backend/tests/test_automation_control.py` 为 `8 passed`；`git diff --check` 通过。
- 对抗式审查已覆盖：通讯录漏群、会话列表加载不完整、非群误识别、ESP32 方法名漂移、未配置 API 被误报为已入库、会话扫描上限不能代表全部群。

### 当前剩余风险

- 会话列表最多执行 180 次滑动并在连续 3 次无变化时停止；这证明了“能向下发现更多群”，但不证明已扫描完微信全部会话。
- 会话列表没有稳定群 ID，当前仍以微信账号 + 标准化群名幂等；同账号同名群可能合并。
- 会话列表候选只具备“疑似群聊”证据，必须在 PC 端人工确认后才能设置 `canSend=true` 或加入营销任务。

### 下一步最小计划

1. 先配置 PC 局域网/API 地址和临时设备令牌（令牌不提交 Git），跑一次真实 HTTP 回传，用 PC 群库核对两个微信昵称、来源、群名、`savedAt`、`lastSeenAt` 和幂等结果。
2. 在 PC 群库把“通讯录发现”和“会话列表发现”展示为来源标签，默认均为待人工确认；人工确认前不进入营销发送队列。
3. 把会话扫描改成可续扫任务：记录账号、滚动断点、最后可见群名和扫描批次，检测到底后再标记完成，避免用无限增大滑动次数代替全量扫描。
4. 全量库存稳定后，再开发 `wechat.join_group`；原生已在群的记录只进入“是否可发送/是否营销”审核，不执行重复加群。

以后每次阶段性交付，交接结尾固定保留“当前结果 / 验证 / 剩余风险 / 下一步最小计划”四段。

## 2026-08-24 最新交接：生成同款复用与排序已上线

### 当前结果

- 后端已支持同一用户复用同一公开资料/合集时直接返回已有有效同款；公开资料页使用稳定幂等键，历史时间戳客户端也由后端语义去重兜底。
- 资料库和合集列表把同款置顶，并显示绿色“同款”标签；普通资料仍按原更新时间/收藏时间排序，不改资料 `updatedAt`。
- 生成成功后已清理服务端及小程序列表缓存，避免返回列表时继续显示旧顺序。

### 验证与剩余动作

- 本地同款回归 `3 passed`，Python 编译、相关 JS 语法检查、`git diff --check` 通过；生产 `/health` 200，线上既有记录只读复用验证成功且资料/生成记录数量未变化。
- 生产备份：`/home/ubuntu/teamBuy-backups/same-style-reuse-sort-20260824-232549/`。
- 小程序代码已更新但尚未上传体验版；下一步在微信开发者工具上传后真机验证：生成同款 → 返回资料库/合集 → 同款置顶且显示“同款” → 再次点击进入已有同款而不新增重复资料。

## 2026-08-24 最新交接：资料助手绑定接口已同步

### 当前结果

- 生产已补齐 `/api/auth/wecom-bind-status` 和 `/api/auth/wecom-bind-card`，解决点击资料助手时的 404。
- 生产备份：`/home/ubuntu/teamBuy-backups/wecom-bind-routes-20260824-233812/`。
- 带签名会话的线上绑定状态接口已返回 `unbound`，说明路由、鉴权和服务方法链路均可用；无效卡片请求按预期返回 400。
- 本地小程序首页和资料库入口已把 401/404/其他错误分开提示，但需要上传体验版后才会看到新文案。

### 下一步

1. 用户重新点击资料库底部“资料助手”，确认出现绑定引导而不再提示登录异常。
2. 如果需要验证完整绑定，再通过企业微信添加助手并点击绑定卡片；不要用伪造 token 做生产绑定测试。

## 2026-08-24 最新交接：PC 群自动化运营页与人工审核已完成

### 当前结果

- 运营后台左侧“群运营”下新增“群自动化运营”标签页：可查看设备心跳、双微信账号、原生微信群/小红书候选、来源、最近发现和审核状态。
- 新增运营令牌保护的群审核 PATCH 接口，PC 可以设置“允许营销/暂不营销”和备注；设备令牌调用会返回 403。
- AScript 继续使用现有群候选入库接口，并保留 PC 已审核的 `canSend/remark`；当前真机脚本仍需在设备端配置 `BACKEND_URL` 和 `DEVICE_TOKEN` 才会发生真实 HTTP 入库。
- 会话列表扫描现在按滑动后“名称 + 垂直位置”是否变化判断到底，连续两次无位移才停止；结果包含 `chatListScanStopReason` 和 `chatListScanScrolls`。

### 验证

- `backend/tests/test_automation_control.py`：`9 passed`。
- 后端自动化文件和 AScript 入口 `py_compile` 通过；PC 页面内嵌 JavaScript `node --check` 通过；`git diff --check` 通过。
- 对抗式审查已检查：设备令牌越权审核、重扫覆盖人工状态、令牌长期留存、滑动上限冒充到底、未配置 API 冒充入库；未发现阻断性问题。
- 本轮未部署生产后端，未上传微信体验版，未执行营销发送。

### 剩余风险

- 设备侧尚未用真实 PC 局域网地址和临时设备令牌完成一次 HTTP 回传验收；页面和接口已具备，但不能把静态测试写成真机入库已验证。
- 会话列表仍以“微信账号 + 标准化群名”幂等，没有稳定群 ID；同账号同名群可能合并。
- 会话列表识别仍是视觉/名称启发式；只有 PC 明确设置 `canSend=true` 的记录才可进入后续营销范围。

### 下一步最小计划

1. 在 PC 本地启动一个手机可访问的开发 API（监听局域网地址），配置临时 `AUTOMATION_DEVICE_TOKEN` 和 AScript 的 `BACKEND_URL/DEVICE_TOKEN`，只跑一次原生群扫描并核对真实入库。
2. 在 PC 页面分别标记一个“允许营销”和一个“暂不营销”，再重扫确认审核状态不被覆盖。
3. 将扫描结果的来源/停止原因纳入可续扫任务记录；确认全量群库稳定后，再开发指定微信账号串行加群或营销任务。

## 2026-08-25 最新交接：运营后台菜单已处理旧 HTML 缓存

### 当前结果

- 当前源码的“群运营”下已经包含“群自动化运营”；用户截图中的旧三项菜单来自未重载的旧 HTML/旧服务，不是源码缺少页面。
- `/ops` 的 HTML 响应已增加 `Cache-Control: no-store, max-age=0`，防止后续继续命中旧菜单。

### 验证与限制

- 源码包含菜单、页面脚本 `node --check`、Python 编译和 `git diff --check` 通过；自动化控制 pytest `9 passed`。
- 组合运行 `test_app.py + test_automation_control.py` 为 `190 passed, 1 failed`；失败项是既有资料分享统计 `shareCount` 断言，与本次运营后台修改无关，未改动。
- 本轮没有启动服务，也没有部署生产；用户需要重载实际访问的后端服务后再刷新 `/ops`。

### 下一步最小计划

1. 重启你当前访问的本地后端服务，打开 `/ops` 并刷新；“群运营”下第一项应为“群自动化运营”。
2. 如果仍看不到，提供浏览器地址栏中的完整地址，我再定位是否访问了另一套服务或生产旧版本。
3. 页面显示后，再配置自动化运营 Token，继续做真实群库 HTTP 入库验收。

## 2026-08-25 最新交接：群自动化运营页面已部署生产

### 当前结果

- 已部署生产运营后台菜单和页面；公网 `https://teambuy.lifelove.top/ops` 已验证包含“群自动化运营”，浏览器不再应看到旧三项菜单。
- 已部署自动化控制面相关后端代码，`/api/automation/*` 路由已加载；未配置令牌时访问 `/api/automation/devices` 返回 403，权限边界正常。
- backend 容器 `teambuy-backend-1` 已重启，`/health` 返回数据库正常；没有重建镜像，没有上传小程序。
- 生产备份：`/home/ubuntu/teamBuy/.deploy-backups/ops-automation-page-20260825-0009`、`/home/ubuntu/teamBuy/.deploy-backups/automation-api-20260825-0013`。

### 剩余风险

- 生产 `.env` 中尚未配置 `AUTOMATION_OPERATOR_TOKEN` 和 `AUTOMATION_DEVICE_TOKEN`，因此 PC 群库读取和 AScript 真机回传暂时仍会被拒绝；本次没有把令牌写入聊天或代码。
- 组合本地回归此前有 1 个与本次无关的资料分享统计失败；自动化控制测试为 `9 passed`。

### 下一步最小计划

1. 生成并通过安全方式配置独立的生产运营令牌和设备令牌，重启 backend；不要复用管理员令牌。
2. 在 PC“群自动化运营”页输入运营令牌，确认设备列表/群库空态可正常读取。
3. 在 AScript 填写生产 HTTPS API 地址和设备令牌，先回传一个账号的一次扫描结果，再核对 PC 群库真实入库和幂等。

## 2026-08-25 最新交接：动态欢迎语后端已部署，等待真实客户验证

### 当前结果

- 后端已接入动态欢迎文字和一次性小程序绑定卡片；生产容器 `teambuy-backend-1` 已重启，`/health` 返回 200。
- 生产备份目录：`/home/ubuntu/teamBuy-backups/wecom-dynamic-welcome-20260825-001935/`。
- 不要在企业微信“配置欢迎语”页面保存固定绑定卡片；动态页面由后端实时生成。

### 用户测试步骤

1. 使用一个从未添加过“资料助手”的企业微信客户，或先删除后重新添加。
2. 重新从小程序点击添加资料助手，等待企业微信会话中出现欢迎文字和“开启我的资料库”卡片。
3. 点击卡片并完成小程序登录，确认绑定成功后进入资料库。
4. 如果仍没有消息，记录添加时间，读取 backend 日志中的“企业微信客户回调解析失败”详情；不要把 Token、AESKey 或 Secret 发到聊天中。

### 验证状态

- 已完成：本地绑定回归 `5 passed`、Python 编译、生产源码同步、生产重启和健康检查。
- 待完成：真实企业微信客户添加事件、欢迎语投递、动态卡片点击和绑定闭环。

## 2026-08-25 最新交接：真实添加事件已到达但回调验签失败

### 当前阻塞

- 用户 12:32 左右重新添加资料助手；生产日志收到客户添加 POST，但在解析阶段报 `企业微信回调签名验证失败` 并返回 400。
- 因为验签失败，后端尚未拿到 `WelcomeCode`，没有执行动态欢迎文字和小程序卡片发送。

### 需要人工配置

在企业微信后台进入“客户联系 → 客户 → API → 接收事件服务器”，核对：

- URL：`https://teambuy.lifelove.top/api/wecom/kf/teamBuy/callback`
- Token：必须与生产后端 `WECOM_CALLBACK_TOKEN` 完全一致
- EncodingAESKey：必须与生产后端 `WECOM_ENCODING_AES_KEY` 完全一致

不要把 Token、AESKey 或 Secret 发到聊天；配置保存通过后，用未添加过的客户重新添加，再读取 backend 日志确认回调进入 `send_welcome_msg`。

## 2026-08-25 最新交接：独立令牌已配置，等待解锁后完成真实群库入库

- 生产页面与自动化控制 API 已部署；生产端已配置独立运营令牌和设备令牌，令牌值不在本交接文档中展示。
- 无令牌访问仍为 403，正确运营令牌可读取 `/api/automation/devices` 与 `/api/automation/group-candidates`；生产 `/health` 为 `ok/postgres`。
- AScript 工程 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/automation/ascript/wechat_group_inventory` 已上传单账号配置，计划先验证一个微信，再恢复双账号；本机令牌文件为 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/tmp/automation-secrets-20260825.txt`，仅本地保存。
- 本次真机运行因手机停在锁屏/面部验证页而主动停止，设备心跳和群候选仍为 0；未执行加群、开群、发消息或营销。

### 解锁后的最小继续顺序

1. 人工解锁手机并停留在可操作桌面。
2. 重新运行已上传的单账号 `wechat_group_inventory`，检查 AScript 结果中的 `backendWriteConfirmed=true` 和 `postedCount`。
3. 用 PC 群自动化运营页确认对应昵称的群记录，再把本地配置恢复为双微信实例并做第二次只读扫描。

## 2026-08-25 最新交接：双微信群库真实入库已完成

## 2026-08-25 最新交接：PC 群库页面已改为直接使用管理员 Token

- 生产 `https://teambuy.lifelove.top/ops` 的“群自动化运营”页已删除重复运营 Token 输入框，直接使用顶部管理员 Token。
- 管理员 Token 读取群库已验证返回 149 条：高士腾 47 条、leo 102 条；刷新页面后即可查看全部记录。
- 旧 operator Token 兼容读取仍可用；设备 Token 读取 PC 群库仍为 403，设备权限边界保持不变。
- 本次生产备份目录：`/home/ubuntu/teamBuy/.deploy-backups/ops-admin-token-unification-20260825-005447/`。

## 2026-08-25 最新交接：群库审核已改为行内二选一

- 生产 `https://teambuy.lifelove.top/ops` 的群自动化运营页已移除右侧“人工审核”表单；群库每行直接点击“可以营销”或“不可以营销”即可保存，按钮会显示当前选中状态。
- 生产页面备份目录：`/home/ubuntu/teamBuy/.deploy-backups/ops-inline-review-20260825-011531/`；本地相关文件为 `backend/app/static/ops-admin/index.html`。
- 本地和线上验证均通过：内联 JS 可解析、`git diff --check` 通过、自动化控制测试 `10 passed`、线上 `/health` 正常、旧审核表单标记已消失。
- 本轮没有新增接口、没有创建营销任务、没有发送微信消息；行内按钮只更新已有群记录的 `canSend` 字段并保留备注。

### 营销卡片关联方案

1. 先审核群：只决定群级总开关“可以/不可以营销”。
2. 再给群打少量性质标签，例如“房源”“宠物”“本地生活”“AI 工具”；标签可人工调整。
3. PC 建立营销策略：选择群标签或指定群，关联一张卡片或一个卡片池，设置早/中/晚窗口、每日上限和冷却时间。
4. 策略生成发送任务，AScript 只领取明确任务并执行；同一群多个卡片按卡片池轮换，特殊群可用单群卡片覆盖默认策略。
5. 发送前必须同时满足 `canSend=true`、策略启用、卡片已发布、分享快照有效、时间窗口和频率限制通过；否则不创建任务。

### 下一步计划

1. 先观察行内审核是否满足实际操作，完成群库“允许/不允许”标记。
2. 开发群性质标签和群级覆盖，不先做复杂 AI 自动分类。
3. 开发卡片/合集与群标签的营销策略配置页，再开发策略生成任务。
4. 最后接入 AScript 领取/执行发送任务，先做单群单卡片人工验收，再扩展早中晚和卡片轮换。

- 手机解锁后已完成单账号与双账号只读验收；账号昵称由微信“我”页识别为“高士腾”和“leo”，分别使用稳定的昵称派生内部账号 ID，未把两个实例串账。
- 单账号验收：高士腾首次回传 47 条，PC API 核对 47 条。
- 双账号验收：AScript 本轮高士腾 46 条、leo 102 条，均 `backendWriteConfirmed=true`、无回传错误；PC API 当前实际库为高士腾 47 条、leo 102 条、合计 149 条。
- 所有记录来源为 `wechat_native`，`canSend` 全部未设置；PC 人工审核前不会进入发送任务。
- 本轮结束后设备 `android-01` 已恢复为 `ready`；当前脚本仍只发送开始阶段的 `busy` 心跳，结束心跳自动化列入下一步。
- 会话列表停止条件已按“连续两次滑动后可见会话名称及底部位置均不变化”为准，固定次数只作异常保护。

### 下一步计划

1. 在 PC“群自动化运营”页查看 149 条群记录，按群性质人工设置“不做营销”或 `canSend=true`。
2. 增加群状态筛选和 `lastSeenAt` 复核，区分当前发现、历史入库和连续未发现，避免误删或误发。
3. 审核规则稳定后，再开发“PC 人工审核后的资料卡发送任务”，继续保持 AScript 只执行明确任务、不自主决策。

## 2026-08-25 最新交接：群画像与发送资格第一版已部署

- 生产群自动化运营页已增加群画像编辑：主题、地域、允许内容类型、成员状态；最近活跃和最近检查时间只展示，不由人工表单伪造。
- `canSend` 已明确为“管理员允许进入策略”，页面显示为“管理员许可”；当前发送资格由服务端返回 `sendEligibility`，可显示待管理员确认、待检查群状态、近3天无活跃、待完善群画像、待配置卡片策略等原因。
- 设备候选上报不能再修改 `canSend`；新群和重扫结果不会绕过管理员审核。生产验证设备 Token PATCH 返回 403，群库接口返回资格和画像字段。
- 本轮没有聊天内容读取、营销任务创建或微信发送；生产备份目录为 `/home/ubuntu/teamBuy/.deploy-backups/group-profile-20260825-013732/`。

### 下一步计划

1. 增加 AScript 只读群状态检查：确认当前账号仍在群内，并读取群最近一条消息时间。
2. 通过独立设备接口回传 `membershipStatus`、`lastActivityAt`、`lastVerifiedAt`，不允许设备修改 `canSend`。
3. 在 PC 增加卡片主题、地域、内容类型字段和营销策略配置。
4. 先用一个上海宠物群或上海房产群做单群单卡片人工验收，再开发批量任务和频率控制。

## 2026-08-25 最新交接：群库昵称展示与布局已优化

- 生产群库顶部账号筛选现在显示“高士腾 / leo”，不再把长内部账号 ID作为表格列；群名称下方显示对应微信昵称。
- 149 条历史记录已补齐昵称：高士腾 47 条、leo 102 条；`canSend`、群画像、备注和发送资格未因补齐昵称而改变。
- 群主题的正式作用是内容路由：例如“宠物 + 上海”匹配上海寻猫/领养，“房产 + 长沙”匹配长沙房源；主题不是装饰性标签。
- 生产备份目录：`/home/ubuntu/teamBuy/.deploy-backups/account-label-layout-20260825-014947/`；元数据容错补丁目录：`/home/ubuntu/teamBuy/.deploy-backups/account-label-guard-20260825-015329/`。

### 下一步计划

1. 把本地 AScript 昵称回传改动上传到自动化设备，验证双账号映射自动维护。
2. 开发只读群状态检查，回传是否仍在群内和最近消息时间。
3. 开发卡片主题、地域、内容类型和营销策略配置。
4. 用一个上海宠物群或上海房产群做单群单卡片人工验收。

## 2026-08-25 最新交接：群库已改为每页 10 条，明确卡片内容链路

- 生产 `https://teambuy.lifelove.top/ops` 的微信群库现在每页显示 10 条，底部显示页码和总数，支持上一页/下一页；账号筛选和刷新会回到第 1 页。
- 分页是 PC 端展示层改动，复用现有最多 500 条接口数据，没有新增接口、数据库表或营销发送动作。生产备份目录：`/home/ubuntu/teamBuy/.deploy-backups/group-pagination-20260825-022812`。
- 卡片内容的落地链路已经确定：资料/合集先编辑并发布为可复用卡片，卡片登记主题、地域、内容类型和当前分享快照；营销策略再选择目标群/群标签、卡片或卡片池、时间窗口、频率和冷却；服务端匹配后生成确定任务；AScript 只领取和执行。
- 群库中的“允许内容”只是白名单，例如宠物群允许“寻猫、领养”，房产群允许“房源”；它不是实际营销正文。上海宠物群不匹配上海房产卡，长沙房产群不匹配上海房源卡，没有精确匹配就不发送。
- 本轮生产健康检查正常；本地内嵌 JS 检查、自动化控制 pytest `11 passed`、`git diff --check` 通过。未上传小程序、未上传本地 AScript 昵称回传改动、未执行微信发送。

### 下一步最小顺序

1. 用户刷新生产运营后台，确认每页 10 条、下一页和账号筛选都正常。
2. 增加卡片的主题、地域、内容类型元数据，并复用现有资料/合集发布和分享快照链路。
3. 开发最小营销策略配置：先支持指定群 + 指定单卡片 + 一个时间窗口，完成单群人工验收后再扩展卡片池和早中晚窗口。
4. 再让 AScript 领取明确的卡片发送任务，先做成功/失败回传，不先做自由聊天或自动选卡。
# 2026-08-25 最新交接：营销路由与单群单卡任务已部署

- 小程序通用资料编辑页已增加“微信群营销匹配”：主题、地域、内容类型；保存时写入 `visibilityConfig.marketingRoute`。小程序体验版尚未上传，需由负责人在微信开发者工具手动编译上传。
- 生产 `https://teambuy.lifelove.top/ops` 的群自动化运营页已增加“单群单卡测试”：选择已发布资料卡、目标微信群和执行设备；页面提示只创建待执行任务，不会直接发消息。
- 生产新增并验证：`GET /api/automation/marketing-cards`、`POST /api/automation/marketing-card-tasks`。卡片目录仅返回安全元数据，任务创建由服务端再次验证群状态、路由匹配、发布状态和分享快照 revision。
- 生产备份目录：`/home/ubuntu/teamBuy/.deploy-backups/marketing-card-route-20260825-025730`；`/health` 正常，`/ops` 已包含新面板，带管理员 Token 的卡片目录返回 200。
- 本地验证：相关 JS `node --check`、Python 编译、JSON 解析、`git diff --check` 通过；自动化/资料发布/分享测试共 `17 passed`。
- 当前明确未完成：AScript 尚未实现 `wechat.send_miniapp_card` 领取后的真实微信发送；本轮没有创建生产任务、没有发送微信消息、没有上传小程序。

### 下一步最小执行顺序

1. 你在微信开发者工具上传包含营销路由字段的小程序体验版，发布一张测试资料卡并生成当前分享图。
2. 在 PC 群自动化运营页选一个已审核、已确认仍在群内且近三天活跃的测试群，补齐主题、地域和允许内容类型；如果现有群还没有状态检查结果，先不能创建真实发送任务。
3. 先开发 AScript `wechat.send_miniapp_card`：只领取明确的 `candidateId + cardId + revision + sharePath` 任务，执行一次并回传成功/失败，不做自动选卡和批量发送。
4. 真实群测试通过后，再增加早/中/晚窗口、每日上限、冷却和卡片池轮换。

## 2026-08-25 最新交接：佛山宠物交流群1 预检未发送

- 本次目标群已精确定位为 `佛山宠物交流群1`，手机实时截图/OCR 已看到同名群页面；没有创建生产任务，也没有发出微信消息。
- 选中的最相关卡片是“刚流浪几个月的蓝猫”（`note_d9743da89b`），但当前卡片目录显示主题为其他、地域为空、内容类型为普通资料，且没有当前分享图；不能直接发入宠物群。
- 设备端 `esp32` 插件导入失败：`bad magic number`。因此 AScript 真实发送器仍未通过硬件通道预检。
- 下一步最小顺序：补卡片路由并上传小程序体验版生成分享图 → 确认 `leo` 当前微信实例和群近三天活跃 → 修复/验证 ESP32 插件 → 创建一次单群单卡任务并回传结果。

## 2026-08-25 最新交接：PC 运营总览三档时间范围已部署

- 生产 `https://teambuy.lifelove.top/ops` 已上线“今日 / 近 7 日 / 近 30 日”切换；统计卡、漏斗、关键动作、趋势和客户运营指标按同一范围刷新。
- 生产接口验证通过：overview 趋势长度分别为 1、7、30；customer-operations 的 period days 分别为 1、7、30；后端 `/health` 和数据库检查正常。
- 生产备份：`/home/ubuntu/teamBuy/.deploy-backups/ops-period-switch-20260825-122445/`。本次没有改生产配置、数据库、媒体或小程序。

### 下一步最小执行顺序

1. 管理员打开生产 `/ops`，点击三档范围确认实际数据口径符合运营习惯。
2. 如果需要，再单独讨论用户排行、内容排行是否也要增加时间筛选；当前总览页已完成，当前状态类指标保持“当前”口径。

## 2026-08-25 最新交接：PC 运营总览支持三档时间范围

- 运营总览顶部已增加“今日 / 近 7 日 / 近 30 日”切换；统计卡、核心漏斗、关键动作、趋势和客户运营指标会按选定范围同步刷新。
- 漏斗顺序为匿名独立访客 → 新增注册用户 → 首次创建内容 → 首次发布 → 分享 → 客户信号，并在下方拆出“首次新建资料 / 首次新建合集”分支。
- 待发送通知、待处理异常、待跟进客户、有效会员、资源健康是当前状态，不随时间切换；页面已使用“当前”文案避免误读。
- 相关本地测试通过；完整后端测试仍有 1 个与本次无关的既有失败：`test_note_preview_view_updates_note_list_stats` 的 `shareCount` 预期为 1、实际为 0。未部署生产，未改小程序。

### 下一步最小执行顺序

1. 在本地或体验环境打开 `/ops`，分别点击三档时间，确认统计卡、漏斗和趋势长度/文案一致。
2. 如果确认口径符合运营习惯，再考虑把用户排行、内容排行等独立分页也增加时间范围；本轮只覆盖总览首页，避免把当前排行榜语义误改成历史聚合。

## 2026-08-25 最新交接：单群单卡仍停在发送前门禁

- `佛山宠物交流群1` 已由当前 `leo` 实例打开并确认仍在群内；生产记录已更新成员状态和核验时间，但近三天活跃仍待证据，发送资格显示“待检查近3天活跃”。
- “刚流浪几个月的蓝猫”尚未完成宠物/佛山/寻猫路由、当前版本分享图和所有者发布复核；当前手机为游客态，不能代替所有者操作。
- ESP32 `esp32` 插件仍因 `bad magic number` 无法导入，`ashid` 未激活；安全发送器未创建任务，也没有发送消息。
- 本轮已完成任务载荷和安全失败边界，下一步不是重试发送，而是用户登录卡片所有者账号完成路由/发布/分享图，设备端刷新 ESP32 兼容插件，然后做近三天活跃核验；四项均通过后才创建一次单群单卡任务。

## 2026-08-25 最新交接：群活跃已确认，卡片仍待授权配置

- 管理员已确认 `佛山宠物交流群1` 刚刚有消息；生产 `lastActivityAt` 已更新，群发送资格现在是“待配置卡片策略”。
- 手机重新进入资料整理助手后，“我的”页仍显示“游客浏览 / 登录”，无法编辑生产卡片；卡片目录确认“刚流浪几个月的蓝猫”仍缺少宠物/佛山/寻猫路由和当前分享图。
- ESP32 官方插件复测仍报 `bad magic number`，不能因为蓝牙连接正常就判定 AScript Python 插件可用。
- 当前没有创建发送任务。下一步唯一外部配合是让手机小程序进入卡片所有者登录态；随后才能设置路由、发布、生成分享图，再继续单群单卡发送。

## 2026-08-25 最新交接：已到微信登录确认页

- 目标群活跃已按管理员刚发消息确认，生产资格已进入 `card_route_required`。
- 手机已进入资料整理助手的登录页；页面要求勾选用户服务协议/隐私政策后点击“微信一键登录”。这是用户授权动作，Codex 未代替勾选。
- 登录成功后继续设置“宠物 / 佛山 / 寻猫”，发布卡片并生成 revision 3 分享图；当前 ESP32 仍需设备端插件包修复，实测导入报 `bad magic number`。

## 2026-08-25 最新交接：ESP32 错误不是 idf.py

- 手机端 Python 为 3.8.16，官方 `esp32` 插件最新版本为 4.7.0；加载后实际插件文件头为 `H8tX`，导入 `BleDevice` 报 `bad magic number`。
- 根因在 AScript 手机插件包/运行时兼容层，尚未进入固件通信阶段；不要先用 `idf.py` 刷板。
- 下一步：更新或重启兼容的 AScript Android 运行时，清理并重新下载官方 ESP32 插件；导入成功后再检查蓝牙权限、HID 连接和发送器。

## 2026-08-25 最新交接：ESP32 尚未进入可烧录状态

- Mac 已发现 `/dev/cu.usbmodem211NTXR6V3782`，但只读 `esptool chip_id` 返回 `No serial data received`；没有执行烧录。
- 需要先让板子进入下载模式：保持数据线连接，按住 BOOT，短按 RESET/EN，松开 BOOT；然后重新执行芯片识别。
- 识别出芯片型号后再选择安卓蓝牙对应固件烧录；这只是硬件链路修复，AScript 的 `bad magic number` 仍需单独修复手机插件运行时。

## 2026-08-25 最新交接：佣金提现商家转账适配器已完成最小测试

- 已确认商家转账场景 ID 为 `1005`；本地 `backend/.env` 和 `backend/.env.example` 已增加 `WECHAT_TRANSFER_ENABLED=false`、`WECHAT_TRANSFER_SCENE_ID=1005`、转账回调地址。生产运行环境未修改、未开启转账。
- 后端已增加创建商家转账单的最小客户端适配器，复用现有商户签名配置，支持 OpenID、分金额、1005 场景报备信息和转账回调地址。
- `backend/tests/test_wechat_pay.py` 定向测试 9 项通过，覆盖请求体、签名、关闭开关和失败不换单；尚未实现提现状态机、PC 审核、小程序 `requestMerchantTransfer` 确认、转账回调查单和真实金额联调。

### 下一步最小执行顺序

1. 接入转账回调和查单，将微信状态映射到提现记录，并保证奖励预占可释放。
2. 增加人工审核后发起转账的后台入口，继续保持自动付款关闭。
3. 小程序接收 `package_info` 并拉起用户确认收款；完成小额人工真实联调后，再由负责人确认是否开启生产转账。

## 2026-08-25 最新交接：佣金提现已完成测试金额和回调结算闭环

- 最低提现金额已分环境实现：开发/测试为 `10` 分（¥0.10），正式环境为 `1000` 分（¥10.00）；本地实际读取到 `APP_ENV=development`、场景 `1005`、转账开关 `false`。
- 已增加管理员提现列表、审核发起接口和微信转账回调；流程为“申请预占 → 管理员审核 → 创建转账单 → 等待用户确认/处理中 → 成功回调 → 奖励 withdrawn”。失败或撤销会释放预占奖励，重复回调不会重复结算。
- 测试覆盖 `25 passed`，并通过 Python 编译、前端 JS 语法和差异检查。生产环境未改配置、未部署、未发生真实付款。
- 当前仍未补齐微信查单兜底、微信撤销接口和正式小程序收款确认页面；因此暂不能把功能描述为正式生产可提现。

### 下一步最小执行顺序

1. 补微信商户单号查单和撤销接口，覆盖回调丢失、用户超时未确认、人工撤销。
2. 小程序接入 `packageInfo` 的收款确认调用，并显示 `waiting_user_confirm / paid / failed` 状态。
3. 生产部署但保持转账开关关闭，先用 ¥0.10 做一次负责人确认的小额真实联调；确认到账、回调、对账后再决定是否启用正式 ¥10.00 门槛。

## 2026-08-25 最新交接：当前串口不是 ESP32

- 现有 `/dev/cu.usbmodem211NTXR6V3782` 已确认属于 LG 显示器 USB 控制，不是开发板；`esptool chip_id` 无响应是因为连错串口。
- 没有执行任何固件写入。请用数据线把 ESP32 直接接入 Mac，必要时避开显示器/扩展坞，再重新进入 BOOT 下载模式。
- 出现新的 ESP32 串口后，我会先识别芯片，再选择 C3/S2/S3/Pico32 对应的安卓蓝牙固件烧录。

## 2026-08-25 最新交接：ESP32-C3 已刷写，手机插件仍未恢复

- 用户已把 ESP32 通过数据线直连 Mac；新串口 `/dev/cu.usbmodem101` 已确认属于 Espressif，芯片识别为 `ESP32-C3`，MAC 为 `10:20:BA:DC:5D:90`。
- 已刷写对应 `安卓蓝牙固件/esp32-c3/firmware` 三段镜像；`esptool.py v4.7.0` 的 bootloader、partitions、firmware 均显示 `Hash of data verified`，并已硬复位。此次没有使用 `idf.py`。
- 刷写后 AScript Android-237 复测仍为 `plug.load=true`、`from esp32 import BleDevice` 失败，错误为 `bad magic number in 'esp32': b'H8tX'`。
- 当前结论：硬件固件链路通过，手机端 AScript 插件/运行时兼容问题未解决；未创建发送任务，未发送微信消息，真实 HID 发送仍被安全门禁阻断。

### 下一步最小执行顺序

1. 清理并重新下载/更新 AScript Android 端官方 `esp32:4.7.0` 插件，复测导入。
2. 导入成功后验证蓝牙权限、`BleDevice` 连接和 HID 激活。
3. 再继续卡片路由/发布/分享图和佛山宠物交流群1的单群单卡测试。

## 2026-08-25 最新交接：提现规则与真实转账确认链路已补齐

- 推广中心现在明确展示：开发/测试最低 ¥0.10、正式最低 ¥10.00、每日最多 1 次、每日全天可申请、先平台审核、到账以微信实际到账时间为准且用户确认后基本秒到、失败/撤销会退回冻结佣金。
- 小程序已接入 `wx.requestMerchantTransfer`；提现记录出现 `waiting_user_confirm` 且有 `packageInfo` 时，用户可点击“确认收款”。拉起成功不代表到账，页面继续依据回调/查单刷新状态。
- 后台已增加商户单号查单、撤销和人工处理接口：`POST /api/ops-admin/referral-withdrawals/{id}/query`、`POST /api/ops-admin/referral-withdrawals/{id}/cancel`。撤销 `CANCELING` 阶段不会释放佣金，最终 `CANCELLED` 才释放。
- 本轮验证：`26 passed`，Python 编译、前端 JS 语法和差异检查通过；生产未部署，`WECHAT_TRANSFER_ENABLED` 仍为 `false`。

### 下一步最小执行顺序

1. 在微信开发者工具/真机检查提现规则卡片、提现记录和 `requestMerchantTransfer` 调起页面。
2. 使用配置完整的体验/测试环境验证一笔 ¥0.10：审核、确认、回调、查单和撤销各一条路径。
3. 生产保持转账关闭部署，完成负责人确认后才打开真实转账；正式环境门槛保持 ¥10.00。

## 2026-08-25 最新交接：提现页已注册，可直接用开发者工具打开

- 页面路径：`pages/referral-center/index`，已加入 `miniprogram/app.json`，可在微信开发者工具“编译模式”中直接打开。
- 本地页面预览不需要后端部署；但当前 `miniprogram/app.js` 的 API 地址仍是 `https://teambuy.lifelove.top`，本轮新增后端查单/撤销接口尚未部署到线上，因此真实提交测试前要么启动本地后端并切换 API 地址，要么先部署后端（转账开关保持关闭）。
- 页面提交需要账号存在可提现奖励；没有 `available` 奖励时只会看到规则和“暂无可提现奖励”，不会创建提现申请。

## 2026-08-25 最新交接：我的页已增加推广奖励入口

- 你截图中的“我的”页现在会在客户信号卡片下方显示“推广奖励｜查看分佣规则、申请提现和提现记录”，点击进入 `pages/referral-center/index`。
- 入口仅对已登录用户显示；如果仍看不到，需在微信开发者工具点击“编译”或重启项目，让 `app.json` 和 profile 页面重新加载。

## 2026-08-25 最新交接：敏锐生意人已准备 ¥0.20 测试佣金

- 线上最近更新的账号 `user_ce567d897f`（昵称“敏锐生意人”）已增加 20 分可提现测试奖励，来源单号 `test_withdrawal_user_ce567d897f_20fen_v1`，状态为 `available`。
- 真实微信转账开关仍关闭。用户点击“申请提现”后预期只创建待审核提现记录，不会自动打款。
- 如果手机仍看不到“推广奖励”，先上传/编译包含最新 `app.json` 和 `profile` 页面代码的小程序体验版；线上旧版本不会自动获得新入口。

## 2026-08-25 最新交接：生产提现已部署，推广中心等待体验版上传

- 生产后端已部署到 `https://teambuy.lifelove.top`，API、OCR worker、会话存档 worker 已重启；PostgreSQL 和媒体卷未改动。生产配置为商家转账开启、场景 `1005`、正式最低 ¥10.00、每日最多 1 次。
- 负责人刚测试到账的历史提现 `withdrawal_bd00988073` 已通过人工核实接口收敛为 `paid`，对应 20 分奖励为 `withdrawn`；页面刷新后应显示“已到账”，并显示到账时间。
- 小程序本地 `pages/referral-center/index` 已重构：余额和最低门槛前置、¥10/¥50/¥200 金额按钮、余额不足置灰、中文年月日时间、放大规则和记录字号。小程序尚未自动部署，需在微信开发者工具上传/编译体验版。
- 本地定向回归 `28 passed`，Python 编译、JS 语法、JSON 和差异检查通过；生产健康检查与正式门槛读取已验证。

### 下一步最小执行顺序

1. 在微信开发者工具上传当前小程序代码并编译 `pages/referral-center/index`，确认页面视觉和日期格式。
2. 以“敏锐生意人”账号重新进入“我的 → 推广奖励”，确认历史 ¥0.20 显示“已到账”、奖励显示“已提现”。
3. 正式大额提现前，运营后台对新提现执行“审核 → 微信确认 → 查单/回调”；异常单不得直接标记到账，必须保留人工核实备注。

## 2026-08-25 最新交接：AScript 已收敛为单一九宫格项目

- 手机端本地小程序列表当前为：`资料整理助手`、`test`、`kuaishou_assistant`。后两个不是 teamBuy 项目，已保留；teamBuy 的旧正式入口和诊断探针已逐个删除。
- `资料整理助手` 内含统一 `__init__.py`、`res/ui/launcher.html`、小红书找群、微信群扫描、微信账号、群营销等内部模块；不再需要用户在手机端选择多个 teamBuy 小程序。
- 当前未完成视觉验收：自动运行时前台仍可能是微信，WebWindow 没有被截图确认。用户需要在 AScript → 本地小程序 → `资料整理助手` → 点击运行，确认九宫格出现后再点每个入口。

### 下一步最小执行顺序

1. 用户手动运行 `资料整理助手`，确认九宫格能显示并截图反馈异常。
2. 修复或补齐九宫格内尚未接入的入口，再逐格验证。
3. 统一入口稳定后，再继续 ESP32 插件修复和佛山宠物群单群单卡发送测试。

## 2026-08-25 最新交接：本地宿主遮挡已定位，下一步转独立 APK

- 事实：手机端脚本显示运行中，但 AScript 宿主仍在前台；官方 `WebWindow`、`Dialog`、`Canvas` 均无法在该启动上下文中显示。直接 Activity 模式和绝对 HTML 路径在临时验证中可以显示九宫格，因此不是九宫格 HTML 本身打不开。
- 当前源码已把入口设为 `mode(0)`、JS 通信改为 `window.airscript.call`，资源路径采用官方路径优先；这些改动服务于后续独立程序，不代表本地项目列表已经完成最终前台验收。
- 当前设备本地项目已清理为 `资料整理助手`、`test`、`kuaishou_assistant`；没有后端部署、微信消息发送或小程序体验版上传。

### 下一步最小执行顺序

1. 在 AScript 开发者后台发布当前统一入口小程序，进入“打包独立程序”流程。
2. 安装独立 APK 后，验证九宫格可见、点击“微信群库”只显示提示、不触发微信动作。
3. 再修复 `esp32:4.7.0` 的 `bad magic number`，最后才做佛山宠物群单群单卡发送。

## 2026-08-26 最新交接：统一九宫格继续按 AScript 本地项目推进

- 用户已明确不要求 APK：最终形态是一个 AScript 本地小程序项目“资料整理助手”，内部用九宫格承载小红书找群、扫描微信群、群营销等功能；`test` 和 `kuaishou_assistant` 仍是其他项目，保留不动。
- 入口代码已使用每次启动的新 `WebWindow` ID，并优先读取设备上已验证存在的 `/storage/emulated/0/airscript/model/资料整理助手/res/ui/launcher.html`。本地源文件为 `automation/ascript/__init__.py`，页面为 `automation/ascript/res/ui/launcher.html`。
- 设备主进程直接启动同一入口代码时，九宫格完整可见；同一 WebWindow 实例用 WebSelector 点击“微信群库”已收到 `feature=wechat_group_library`，说明页面渲染和点击桥都通过。
- AScript 的 `run_project` / 本地列表启动仍可能只显示宿主页面；脚本状态为运行中不能当作前台视觉通过。当前未发送微信消息、未部署后端、未上传微信体验版。
- 已补齐 `pagehide` 关闭回传和设备状态入口的结束生命周期；Python 编译、内嵌 JS 语法、差异检查通过，后端定向 `test_automation_control.py` 在 `.venv313` 下 `12 passed`。

### 下一步最小执行顺序

1. 用户在 AScript“本地小程序”页面手动点击 `资料整理助手` 的绿色运行按钮，确认九宫格是否前台出现；若仍停留列表，提供截图继续查宿主前台切换。
2. 九宫格出现后，先验收“微信群库”安全提示，再核对其余八个入口的显示和返回行为。
3. 入口通过后，继续群状态核验、PC 群库入库和佛山宠物群单群单卡测试；真实发送前保留人工确认。

## 2026-08-26 最新交接：AScript 九宫格模块加载错误已修复

- 统一入口已上传到手机；设备主进程验证九宫格可见，“微信群库”提示关闭后能回到九宫格。
- `xhs_find_group` 已改为按项目子目录加载；点击小红书找群不再报 `No module named xhs_find_group`。ESP32 插件仍未通过 `bad magic number` 验证，因此本轮没有实际搜索或发送。
- 下一步先修复/确认 `esp32:4.7.0` 插件导入，再逐个验收扫描群、PC 入库和佛山宠物群单群单卡；真实微信发送前继续保留人工确认。

## 2026-08-26 最新交接：运行模式与 ESP32 状态已区分，九宫格可显式退出

- AScript 的“HID 控件模式”不是 ESP32 插件状态；蓝牙连接正常也不能替代 `BleDevice` 导入成功。当前无需为九宫格反复切换模式。
- 九宫格入口已增加“退出九宫格”按钮，设备实测点击后能退出 WebWindow；Android 返回键仍作为兼容路径保留。
- 下一步继续使用当前模式完成只读群扫描/PC 入库；小红书实际搜索和群营销发送仍等待 ESP32 插件兼容问题解决。

## 2026-08-26 最新交接：当前主链路已暂停 ESP32

- 负责人已确认暂时不使用 ESP32；当前统一输入模式为 `ascript_native`。小红书找群、双微信账号识别、微信群扫描均直接使用 AScript 原生控件树、OCR 和 `action` API。
- 九宫格“设备状态”只显示当前原生输入模式，不再导入 ESP32 插件；微信群库和账号心跳也不再携带虚假的 ESP32 设备标识。
- 群营销模块现在可以进入任务领取/校验流程，但真实小程序卡片发送仍被独立的 UI 验收门禁阻止；本轮没有发送微信消息、没有后端部署、没有微信体验版上传。
- 本轮修改文件：`automation/ascript/__init__.py`、`automation/ascript/xhs_find_group/__init__.py`、`automation/ascript/wechat_group_inventory/__init__.py`、`automation/ascript/wechat_account_identity/__init__.py`、`automation/ascript/wechat_marketing_sender/__init__.py`。

### 下一步最小执行顺序

1. 上传这 5 个 AScript 模块到现有“资料整理助手”项目，先用“设备状态”和“微信群库”做无副作用回归。
2. 在不加载 ESP32 的前提下执行一次双微信只读扫描，确认两个昵称、群名和 PC 入库结果仍能回传。
3. 再补齐佛山宠物交流群1的原生输入发送 UI 验收；在 UI 验收通过前不创建可真实群发的放行状态。

## 2026-08-26 最新交接：雷达支付/详情入口修复

- 生产开关已确认：客户信息链开启，微信支付要求开启。
- 已修复 `miniprogram/pages/visits/index.js`：点击雷达指标、锁定态操作或客户卡片时强制读取最新状态；收费未开通进入会员支付页，免支付模式刷新后查看客户信息，具体客户卡片进入详情，客户信息链关闭则显示明确状态。
- 已修复 `miniprogram/pages/visits/index.wxml`：锁定态按钮按状态显示“去支付查看/继续查看”，增加“客户信息链暂未开启”空态。
- 本轮未改后端，生产无需重启；前端静态检查通过，相关后端回归 `32 passed`。

### 用户下一步验收

1. 在微信开发者工具上传当前小程序代码，生成体验版并用生产环境打开。
2. 后台保持“客户信息链开启 + 微信支付开启”，未开通会员的账号进入雷达并点击指标或“去支付查看”，应直接进入会员支付页。
3. 关闭后台微信支付后重新进入雷达，应该直接看到客户卡片；点击卡片应进入客户详情，不再进入支付页或“请重试”提示。
4. 如关闭客户信息链，雷达应显示“客户信息链暂未开启”，而不是支付页。

## 2026-08-26 最新交接：运营总览卡片支持时间标签和明细

- 已修改 `backend/app/static/ops-admin/index.html`：总览卡片增加周期/当前标签，点击卡片后在下方展开明细面板；明细包含时间、对象、具体数据和状态，并支持收起。
- 已修改 `backend/app/api/routes_ops_admin.py`：新增 `/api/ops-admin/overview-detail`，支持 `today`、`7d`、`30d`，服务端返回 `startAt`、`endAt`、`rangeLabel`、口径、总数和最多 100 条明细。
- 已修改 `backend/tests/test_app.py`：覆盖近 7 日资料详情、今日匿名访客详情和非法指标。
- 已部署生产：只同步运营路由、后台静态页面和测试文件，未覆盖生产 `.env`、secrets、媒体或数据库；备份位于 `/home/ubuntu/teamBuy-deploy-backups/20260826-122159-ops-overview-details/`。backend 已重启，公网 `/health`、`/ops` 和 `/api/ops-admin/overview-detail` 验证通过。

### 本轮验收方式

1. 打开 PC 运营后台，切换“今日 / 近 7 日 / 近 30 日”，确认卡片左上时间标签和顶部精确区间同步变化。
2. 点击“新增用户”“新增资料”“客户动作”“微信支付收入”等卡片，确认下方明细中每条记录都有具体时间。
3. 点击“待处理异常”“待跟进客户”“有效会员”，确认这些卡片显示“当前”而不是被误算成周期数据。
4. 确认匿名访客详情只显示匿名访客，不出现匿名标识或联系方式。

## 2026-08-26 最新交接：运营明细身份显示已澄清

- 已修改 `backend/app/api/routes_ops_admin.py`：新增用户明细区分主动设置昵称、默认昵称未设置；访客动作和展示页打开区分已登录用户与“匿名访客（未登录）”。
- 生产新增用户明细已验证：`微信用户（未设置昵称）`、`登录身份：已登录 · 用户 ID：...`、`已注册 · 未设置昵称`。
- 已部署并重启生产 backend；备份位于 `/home/ubuntu/teamBuy-deploy-backups/20260826-123458-ops-identity-labels/`。生产 `.env`、secrets、媒体和数据库未改动。

### 用户验收

1. 打开 `https://teambuy.lifelove.top/ops`，切换到“今日”并点击“新增用户”。
2. 对已主动完善昵称的账号，应显示其昵称；仅完成登录的账号显示“微信用户（未设置昵称）”。
3. 对未登录产生的展示页访问，应显示“匿名访客（未登录）”，不显示匿名 ID，也不冒充实名用户。

## 2026-08-26 最新交接：资料编辑已进入独立更新面板

- 根因是原运营卡片“新增资料”按 `createdAt` 统计；资料编辑虽然更新了 `updatedAt`，但旧资料不会因此进入新增明细。
- 已新增“资料更新”卡片和明细接口 `metric=updatedNotes`：只统计 `updatedAt > createdAt` 的编辑记录，并按更新时间倒序，最新编辑的资料显示在第一行。
- “新增资料”保持原有创建口径，不会因编辑旧资料而被误增。
- 已部署生产并验证：今日资料更新接口返回 5 条，第一条为最新编辑记录；备份位于 `/home/ubuntu/teamBuy-deploy-backups/20260826-125950-ops-note-updates/`。

### 用户验收

1. 打开 `https://teambuy.lifelove.top/ops` 并刷新页面。
2. 在总览中点击“资料更新”，确认刚编辑的资料显示在第一行，时间为最近编辑时间，状态为“已更新”。
3. 点击“新增资料”时，仍只显示时间范围内新创建的资料。

## 2026-08-26 最新交接：微信群营销路由已与小程序用户端拆分

- 已移除 `miniprogram/subpackages/workbench/note-edit/` 的“微信群营销匹配”区块及其前端字段处理；小程序用户端不再显示主题、地域、内容类型路由。
- 已隔离用户端接口：`/api/notes` 列表、详情、保存、发布、生成、分享快照、公开资料等响应不返回 `marketingRoute`；用户保存不会覆盖已有 PC 路由。
- 已为 PC 群自动化运营增加管理员专用卡片路由配置：`PATCH /api/automation/marketing-cards/{note_id}/route`，PC 页面可设置主题、地域和内容类型。
- 审查确认小程序没有调用 `/api/automation/*`。保留“资料运营”（用户自己的资料数据）和“微信群资源库”（用户资源发布/浏览），它们不是群营销自动化。
- 本轮未部署生产后端、未上传微信体验版；相关代码需用户按现有流程部署/上传后才会在浏览器和微信开发者工具中生效。

### 本轮验证

- 小程序编辑页 JS、PC 运营后台内嵌 JS、后端 Python、JSON 和 `git diff --check` 通过。
- `backend/tests/test_automation_control.py`：`12 passed`；覆盖 PC 路由保存、用户端不返回路由、公开资料不泄露路由。
- `backend/tests/test_app.py`：`181 passed, 1 failed`；失败为分享统计测试 `test_note_preview_view_updates_note_list_stats` 的 `shareCount` 断言，目前未能证明由本轮运营字段隔离引入，需后续单独处理。

### 下一步最小执行顺序

1. 先在本地/测试环境打开 PC 群自动化运营页，验证选择卡片后能保存主题、地域、内容类型。
2. 用户在微信开发者工具编译，确认资料编辑页已没有“微信群营销匹配”。
3. 单独修复或定位分享统计测试失败后，再决定是否部署 PC 后端；不恢复小程序端群营销字段。

## 2026-08-26 最新交接：统一客户页与跨场景沟通入口已完成

- 已修改 `miniprogram/pages/note-preview/index.js`：统一生成沟通动作，支持电话、微信二维码、站内留言、留下需求、预约沟通/看房、邮箱和场景动作分离；服务数据兼容新旧字段。
- 已修改 `miniprogram/pages/note-preview/index.wxml`、`index.wxss`：服务页使用统一客户页视觉结构；房源、商品、文章、图文、图片和文字资料增加“方便沟通”区，分享入口沿用静态分享图状态。
- 已修改 `backend/app/services/app_service.py`、`backend/app/services/skill_router_service.py`：普通资料默认开放基础沟通配置，商品默认支持微信联系；公开资料读取会规范化稀疏配置。已有显式关闭配置仍按原配置执行。
- 已补充商品编辑器兼容保存：历史商品保存时会补齐电话、轻 CRM、留资和微信咨询；预约动作在客户页只对房源生成“预约看房”。
- 后端已部署生产；小程序前端仍需用户在微信开发者工具重新编译并上传体验版后验收。

### 本轮验证与人工验收

- 已通过：`node --check`（预览页、资料编辑页和商品编辑页）、Python 3.13 编译、WXML 标签嵌套检查、服务/房源/普通资料动作生成检查、`git diff --check`。
- 未完成：后端 pytest。项目现有 `.venv` 是 Python 3.9，启动测试时因 `dataclass(slots=True)` 立即失败；不是本次功能断言失败。
- 人工验收顺序：服务/合作资料先看“服务对象、范围、合作方式、图片资料、方便沟通”；再测房源的电话/微信/留言/预约看房/地图；再测一份文字或图片资料的留言和分享。

## 2026-08-26 最新交接：统一客户页后端已部署生产

- 已部署生产后端文件：`backend/app/services/app_service.py`、`backend/app/services/skill_router_service.py`。
- 已重启 `teambuy-backend-1`、`teambuy-backend-worker-1`、`teambuy-archive-worker-1`；生产 Postgres、`.env`、secrets、媒体和数据库未改动。
- 生产备份：`/home/ubuntu/teamBuy-deploy-backups/20260826-142054-customer-page/`。
- 公网验证：`https://teambuy.lifelove.top/health` 返回正常；生产文件 SHA256 与本地待部署文件一致，worker 日志无错误。

### 用户下一步验收

1. 在微信开发者工具打开并上传 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，重新编译并清除缓存。
2. 用匿名状态或非发布者账号打开已发布资料链接；发布者本人预览会隐藏“方便沟通”区。
3. 服务/合作、房源、商品、文字/图片资料分别确认基础沟通入口；房源额外确认“预约看房”和地图。

## 2026-08-26 最新交接：服务资料手机号校验与显式保存已修复

- 已修复服务/合作资料编辑只更新 `body`、遗留旧 `contentBlocks` 的问题；当前编辑器会提交最新正文块，后端也会兼容旧客户端并按当前正文重建。
- 服务编辑页已增加“保存”按钮。删除手机号后请先点击“保存”，再点击“发客户”；如果当前服务名称、简介或详细说明仍有手机号，后端仍会正确拦截。
- 第二次生产备份位于 `/home/ubuntu/teamBuy-deploy-backups/20260826-143541-service-contentblocks-fix/`；生产三个相关容器已重启，公网健康检查通过。
- 生产接口已确认测试资料 `note_6dee30f9ad` 返回 `cardType=service_offer`、基础电话/微信/留言/留资配置开启，且正文块为当前文本块。

### 用户下一步验收

1. 在微信开发者工具打开项目根目录 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存并重新编译，上传新的体验版。
2. 用非发布者账号或匿名状态打开已发布的服务资料，客户页应看到“方便沟通”区；发布者本人预览会隐藏该区。
3. 进入服务编辑页删除手机号，点击“保存”，重新进入确认内容后再点击“发客户”；应不再因为旧正文块提示手机号。

## 2026-08-26 最新交接：旧资源详情入口已收口到统一客户页

- 该阶段先用 `miniprogram/pages/card-view/index.js` 验证了旧入口转发到 `/pages/note-preview/index`，现已按负责人决定删除旧页面。
- 文字、图文、图片资料当前源码均已存在新客户页分支；历史 Card ID 由后端公开资料接口解析到对应 Note，不再依赖旧页面。

### 用户下一步验收

1. 上传 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存并重新编译体验版。
2. 从“最近看过”、历史记录、资料分享链接分别打开一份文字资料和一份图文/图片资料。
3. 确认顶部进入的是“客户页预览/资料详情”新页面，页面中显示“文字资料”“图文资料”或“图片资料”，并在非发布者身份下显示“方便沟通”。

## 2026-08-26 最新交接：资料页主体点击已改为客户预览

- 已修复 `miniprogram/pages/library/index.js`：资料卡片主体点击从编辑路由改为 `navigateToResourceView`；“完善”按钮继续进入编辑器。
- 已删除 `pages/card-view`，标准资料统一进入 `pages/note-preview/index`；历史 Card ID 由后端公开接口解析。

- 已补齐 `library/index.js` 对 `navigateToResourceView` 的函数引入；此前漏引入会导致资料卡片点击无法跳转。

### 用户下一步验收

1. 重新上传 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram` 的最新体验版并清缓存。
2. 进入“资料”页，直接点击文字/图片资料卡片主体，应进入客户页预览；点击右侧“完善”才进入编辑器。
3. 客户沟通入口需要用非发布者或匿名身份打开分享链接验证；发布者自己的预览会隐藏沟通区。

## 2026-08-26 最新交接：旧 card-view 路由已删除

- 小程序 `app.json` 已移除 `pages/card-view/index`，并删除 `miniprogram/pages/card-view/` 下的页面文件。
- 项目内历史记录、资料运营、旧卡片编辑发布、导入通知和再次分享路径均已改为 `note-preview`。
- 后端公开资料接口支持用历史 Card ID 查找对应 Note；生产部署后需验证一个旧 Card ID 和一个直接 Note ID。

### 用户下一步验收

1. 重新上传 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram` 的最新体验版。
2. 进入“资料”页点击文字/图片资料主体，应直接进入客户页；“完善”仍进入编辑器。
3. 从历史记录、导入通知和旧分享入口打开资料，确认不会出现旧资源详情页。

### 本轮补充

- 已清理通用资源导航的动态旧路由兜底：历史 Card 查看统一进入客户页，编辑统一进入工作台卡片编辑页。
- 生产部署时仍需先验证历史 Card ID 能由后端公开资料接口解析，再让用户上传最新小程序体验版。

## 2026-08-26 最新交接：截图仍为旧 note-preview 体验包

- 用户截图不是 `card-view`，而是旧版本 `note-preview`；“咨询这份资料”在当前源码中不存在，当前版本应显示“方便沟通”。
- 当前本地工程唯一上传目录是 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，其中包含 `project.config.json`；不要上传项目根目录或后端静态目录。
- 后端已部署不能替换小程序页面包；重新编译上传并在体验版管理中选择最新版本后，才能看到本轮 WXML/WXSS。

### 验收标志

1. 用微信开发者工具打开上述 `miniprogram` 目录并清缓存编译。
2. 以匿名或非发布者身份打开文字资料，应看到“方便沟通”及沟通入口，不应看到“咨询这份资料”。
3. 资料页主体应进入客户页；“完善”才进入编辑器；项目源码和入口均不再生成旧详情路由。

## 2026-08-26 最新交接：留下需求表单与两列沟通布局

- `note-preview` 的所有资料类型统一使用两列沟通入口；“立即咨询”已放在沟通卡片最底部。
- 点击“留下需求”会在页面下方展开称呼、电话、微信号和需求备注表单，并自动滚动到表单；提交成功后表单收起并显示提交结果。
- 本轮只修改小程序前端，未改变后端接口；需要重新上传 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram` 体验版。

### 本轮真机验收

1. 用匿名或非发布者身份打开文字资料，确认沟通区为两列，底部显示“立即咨询”。
2. 点击“留下需求”，确认页面自动定位到表单；只填电话或微信其中一项应可提交，均不填应提示补充。
3. 提交后确认出现成功提示、表单收起；重新进入页面不会把上一次敏感联系方式写入持久化缓存。

## 2026-08-26 最新交接：留下需求表单改为浅草绿色视觉

- 客户预览页的“留下你的需求”表单已改为浅草绿色渐变卡片，新增“联系信息”提示条、字段标签、选填说明和隐私提示。
- 输入框使用浅绿色底与绿色边框，提交按钮使用绿色；蓝色“立即咨询”仍保留，便于区分“直接咨询”和“留下需求”。
- 本轮只修改 `miniprogram/pages/note-preview/index.wxml` 与 `index.wxss`，不需要部署后端；必须重新上传 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram` 体验版后才能看到。

### 本轮验收

1. 用匿名或非发布者身份打开公开资料，点击“留下需求”，确认表单自动定位并显示浅草绿色卡片。
2. 检查字段标签和提示文字清晰可读；只填手机号或微信号其中一项应可提交。
3. 检查提交成功后表单收起、成功提示显示，且没有把联系方式写入本地持久化缓存。

## 2026-08-26 最新交接：无主图资料不再截取资料库页面

- 已修复资料库“发客户”卡片显示当前资料库截图的问题：根因是无主图资料返回了空 `imageUrl` 的原生分享对象。
- `share-snapshot` 现在统一处理：有真实主图的非名片继续原图直出；无主图资料由插件生成 `resource_default` 信息卡并保存快照；名片继续使用名片专用模板。
- 分享路径仍统一指向 `/pages/note-preview/index`，页面没有新增场景化 Canvas；分享样式版本已升为 `share_card_v6`。
- 通用无封面分享入口也已改为调用插件生成信息卡。本轮只修改小程序前端和项目文档，未部署后端。

### 本轮验收

1. 从资料库选择一份无封面但已发布的普通资料，先点击“准备分享图”，完成后点击“发客户”。
2. 在微信聊天中确认缩略图是资料信息卡，不是资料库页面截图；点击后进入客户页预览。
3. 分别回归有主图资料、房源、商品、服务和电子名片，确认原图直出或专用模板仍有效。

## 2026-08-26 最新交接：复制内容手机号分拣

- 已新增 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/utils/contact-review.js`，统一识别多个手机号并脱敏展示。
- 服务/合作编辑页和普通文字资料编辑页新增联系方式处理卡片：可选“公开电话”“仅自己保存”“移出正文”。公开电话写入 `note.phone`；仅自己保存写入 `visibilityConfig.privateData.contactPhones`；移出正文会同步清理公开字段。
- 资料库的“发客户”和“重新发布”遇到未确认手机号时，会弹出“去处理”并打开对应编辑器；不再停留在无操作路径的错误提示。
- 未处理手机号仍允许保存为草稿，但发布按钮会阻止发布并定位到处理卡片；后端安全校验继续保留，不能通过前端绕过。

### 用户下一步验收

1. 用微信开发者工具重新编译上传 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram` 体验版并清缓存。
2. 在服务/合作编辑页的详细说明粘贴含两个手机号的内容，确认出现脱敏候选列表；分别测试公开、仅自己保存、移出正文三种操作。
3. 确认保存草稿后，未处理号码仍不能发客户；全部处理后可以发布，客户页不显示私密/移除号码。
4. 从资料库点击同一资料的“发客户”，确认出现“去处理”并进入编辑页，而不是只显示旧的“未确认手机号”提示。

## 2026-08-26 最新交接：名片与合集统一客户沟通组件

- 新增 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/components/customer-communication/`，资料详情页统一复用两列沟通入口和底部主 CTA。
- 名片现在接入电话、微信、邮箱、留言、留下需求和分享；旧名片即使历史配置未开启线索，客户页也会展示名片留资入口。名片编辑保存和后端名片默认配置均已开启 `collectLeads`。
- 合集模板不再各自重复电话/微信按钮，统一在模板内容底部展示沟通区；合集只展示电话、微信、分享，留言和留需求必须从具体资料详情页提交，避免线索归属错误。
- 本地验证：相关 JS/JSON/Python 检查通过，名片与客户动作回归 `5 passed`；新增自测报告：[docs/qa/客户页统一沟通能力_Codex自测报告.md](docs/qa/客户页统一沟通能力_Codex自测报告.md)。
- 本轮尚未部署后端，也尚未上传小程序体验版；生产生效需要部署后端并重新编译上传 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`。

### 用户下一步验收

1. 部署后端，再在微信开发者工具中打开上述 `miniprogram` 目录，清缓存并重新编译上传体验版。
2. 用非发布者身份打开名片，测试电话、微信、邮箱、留言、留下需求和分享。
3. 打开合集，确认统一“方便沟通”区；电话和微信在合集层可用，留言/留需求在具体资料详情页可用。

## 2026-08-26 最新交接：客户详情页重排与暂不跟进

- 已调整 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/customer-detail/index.wxml` 与 `index.wxss`：中段改为“下一步怎么做”，移除重复的联系客户按钮；固定底部为“记录跟进 / 联系客户”两个主动作，下方增加“暂不跟进”辅助动作。
- 已修复客户联系方式窄屏布局：手机号、微信号和邮箱值单行省略，右侧拨打/复制按钮固定宽度并保持居中，不再出现截图中的逐字竖排和横向溢出。
- “暂不跟进”复用已有 `PUT /api/lead-reminders/{id}` 的 `paused` 状态；无跟进记录时先调用现有 ensure 接口建档再暂停。暂停后可在同一位置恢复待跟进，没有新增后端接口，本轮无需后端部署。
- 本地验证：客户详情页 JS、API JS 语法检查通过，页面 JSON 解析通过，`git diff --check` 通过。

### 用户下一步验收

1. 从微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，重新编译并上传体验版，清理旧编译缓存。
2. 打开一个有手机号的客户详情，确认号码横向显示，拨打/复制按钮不溢出；再检查微信号、邮箱较长时仅视觉省略，复制内容仍完整。
3. 分别测试有跟进记录和无跟进记录的客户：点击“暂不跟进”确认后状态变为“暂不跟进”，返回雷达不再出现在待跟进；再次进入详情可点击“恢复待跟进”。

## 2026-08-26 最新交接：直接放弃跟进与雷达移除

- 客户详情固定底部已改为两个等宽按钮：左侧“记录跟进/开始跟进”，右侧浅橙色“放弃跟进”；已移除底部“联系客户”和二级确认提示。
- 点击“放弃跟进”会直接把跟进记录置为 `paused`，写入“放弃跟进”日志，保留客户资料、浏览轨迹和历史记录，成功后返回 `/pages/visits/index`。
- 后端雷达已在画像组装处过滤关闭状态，避免客户仍因历史浏览或高意向事件出现在雷达；客户详情仍可打开该客户，并可通过“恢复跟进”重新放回 `pending`。
- 本轮涉及小程序前端和后端；后端需要部署后才对生产生效，小程序需要从 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram` 重新编译、上传体验版并清理缓存。

### 本轮本地验证

1. `node --check miniprogram/pages/customer-detail/index.js`：通过。
2. `python -m compileall -q backend/app/services/app_service.py`：通过。
3. `backend/tests/test_sales_scrm.py`：16 passed。
4. `backend/tests/test_app.py -k lead_reminder_flow_persists_status_note_and_filters`：1 passed。

### 用户下一步验收

1. 先部署后端，再上传最新小程序体验版；后端和小程序前端是两条独立发布链路。
2. 从雷达进入客户详情，点击浅橙色“放弃跟进”，确认无弹窗、出现成功反馈并返回雷达；该客户不再出现在当前列表。
3. 从历史/客户详情再次打开该客户，确认资料、动态和跟进日志仍保留；点击“恢复跟进”后重新出现在待跟进列表。

## 2026-08-26 最新交接：后端已部署

- 当前客户详情放弃跟进的后端代码已部署到生产；生产实际容器为 `teambuy-backend-1`，端口为 `127.0.0.1:8004`，Nginx 的 `teambuy.lifelove.top/api/` 已转发到该端口。
- API、OCR worker、归档 worker 已重启，生产源码与本地 `backend/app/services/app_service.py` SHA256 一致。
- 公网健康检查已通过：Postgres 配置正常。未携带生产登录令牌的业务接口返回 401，属于正常鉴权结果。
- 部署备份位于 `/home/ubuntu/teamBuy/.deploy-backups/customer-detail-abandon-20260826-213938/backend-app.tar.gz`；生产 `.env`、密钥、数据库和媒体卷未修改。

### 用户现在验收

1. 在当前小程序体验版进入雷达客户详情，点击浅橙色“放弃跟进”。
2. 确认无确认弹窗、出现“已放弃跟进”，随后返回雷达且该客户不再出现。
3. 重新上传包含本轮客户详情按钮的最新小程序体验版；后端部署不会替换小程序页面包。

## 2026-08-26 最新交接：旧“联系客户”来自未更新体验包

- 当前源码和路由已确认：唯一正式客户详情为 `/pages/customer-detail/index`，底部只有“记录跟进/开始跟进”和浅橙色“放弃跟进”；`lead-detail`、`card-view` 已清理，不存在另一套客户详情兜底。
- 21:48 看到“联系客户”不是当前生产后端返回的页面，也不是当前 WXML 的按钮，而是微信端仍在使用旧体验包；本轮已移除客户详情 JS 中未使用的旧联系方法和旧提示文案。
- 不要删除唯一正式客户详情路由。请在微信开发者工具中确认项目目录为 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，重新编译、上传并选择最新体验版本；后端部署不会替换小程序页面。

### 验收标准

1. 进入雷达客户详情，底部显示“记录跟进/开始跟进”和浅橙色“放弃跟进”，不显示“联系客户”。
2. 点击“放弃跟进”不弹窗，成功后返回雷达且该客户不再显示。
3. 若仍显示旧文案，先核对体验版版本号和上传目录，不再通过删除正式路由处理。

## 2026-08-26 最新交接：放弃跟进后直接返回雷达

- 已修复放弃跟进后停留在客户详情的问题：原因为对 TabBar 页面误用 `wx.redirectTo`，现在改为 `wx.switchTab`。
- 放弃成功链路为：更新 `paused` → 清理客户雷达内存缓存 → 直接切换 `/pages/visits/index`；不再延迟停留在详情页。
- 客户详情错误页的“返回客户雷达”也已同步改用 `wx.switchTab`。
- 本轮不需要重新部署后端，只需重新编译上传小程序体验版。

### 用户验收

1. 重新上传最新 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram` 体验版。
2. 从雷达进入客户详情，点击“放弃跟进”。
3. 确认保存后立即回到雷达列表，且该客户不再出现在待跟进列表。

## 2026-08-26 最新交接：开始/放弃跟进统一出队并优化速度

- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/customer-detail/index.js` 的“开始/继续跟进”现在调用统一动作接口，写入 `contacted` 后返回雷达；不再只创建 `pending` 档案。
- “放弃跟进”调用同一个动作接口，写入 `paused` 后返回雷达；无档案客户由后端一次完成建档和状态写入，避免原来的两次请求。
- 后端雷达组装统一过滤 `contacted`、`paused`、`invalid`、`completed`，因此两种处理动作都不会继续出现在待跟进和访客列表；详情、资料和历史不删除。
- 雷达前端不再用本地资料缓存重新拼客户 fallback 卡片，服务端空结果就是工作列表空结果，避免旧缓存把已处理客户“复活”。
- 后端已部署到生产，部署备份为 `/home/ubuntu/teamBuy/.deploy-backups/customer-followup-inbox-20260826-223420/backend-app.tar.gz`；公网健康检查正常。

### 用户下一步验收

1. 在微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存、重新编译并上传体验版。
2. 从待跟进或访客进入客户详情，分别点击“开始/继续跟进”和“放弃跟进”；两者都应快速返回雷达，客户不再出现在两个工作列表。
3. 需要查看历史时，从客户详情/跟进记录入口进入；客户资料和浏览轨迹仍应保留。

## 2026-08-26 最新交接：客户雷达高保真工作台 V3

- 客户雷达已按确认的高保真稿重构为三态工作流：待跟进、活跃访客、已放弃；顶部数字由当前可见列表动态计算，处理完成后即时减一。
- 待跟进和访客卡片使用不同的浅色语义背景；卡片展示客户身份、最近发生、客户看到的内容和动态来源资料；“处理情况”进入客户详情，“放弃跟进”直接出队。
- 已放弃独立作为雷达 Tab，支持“恢复跟进”和“清空删除”；清空采用后端 deleted 墓碑保留历史，避免旧浏览事件再次把客户复活到工作列表。
- 雷达采用缓存优先、后台刷新和操作乐观更新；本地缓存不持久化手机号、微信号或客户身份。
- 新增自测报告：`docs/qa/客户雷达工作台_Codex自测报告.md`。

### 本轮验证与人工验收

1. 已通过小程序 JS 语法检查、后端 Python 编译检查和客户雷达专项测试（16 passed）。
2. 全量后端测试为 290 passed、1 failed；失败项是既有的分享统计测试 `backend/tests/test_app.py::test_note_preview_view_updates_note_list_stats`，本轮未修改分享统计链路。
3. 需要在微信开发者工具清缓存、重新编译并上传体验版，重点检查三态切换、处理后即时出队、来源资料跳转和不同尺寸真机布局；当前未部署生产。

## 2026-08-27 最新交接：客户详情页高保真 V2

- 已按确认的高保真稿重排客户详情页：客户身份、真实浏览证据、跟进判断、联系渠道、同一卡片内的两个折叠行、底部跟进操作。
- “最近动态”和“相关资料”默认折叠，点击整行即可展开/收起；展开内容复用当前已加载的数据，不增加请求。
- 来源证据卡绑定 profile.primaryMaterial，资料标题、查看次数、封面（如有）和跳转均来自当前客户详情数据。
- 底部动作顺序与效果稿一致：左侧浅橙色放弃/恢复，右侧蓝色开始/继续；微信号仅提供复制，不提供拨打。
- 本轮仅修改小程序客户详情页，未部署生产；用户需要在微信开发者工具清缓存、重新编译后上传体验版。

### 本轮人工验收

1. 打开客户详情，确认首屏不再出现完整动态和完整资料列表。
2. 点击“最近动态”和“相关资料”标题行，确认箭头旋转、内容展开且没有重复请求。
3. 确认来源资料标题随客户变化；电话横向显示，微信号按钮显示“复制微信号”。
4. 确认底部左“放弃跟进”、右“开始/继续跟进”，并回归处理后的雷达出队行为。

## 2026-08-27 最新交接：雷达来源资料、详情热加载与跟进入口修复

- 雷达“原资料”和客户详情“客户刚刚看了什么/相关资料”均按 canonical `noteId` 直达 `/pages/note-preview/index`，不再把 note ID 送进旧 Card 查询。
- 新增 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/stores/customer-detail-store.js`，客户详情从雷达进入时优先复用 owner-scoped 内存情报，先展示后后台刷新；TTL 2 分钟，不写本地持久化，不跨环境/用户/场景复用。退出登录由 `app.js` 清理。
- 详情加载有请求序列和卸载保护；跟进、放弃、恢复、保存和清空成功后都会失效相关内存缓存。只有 `leadId` 的入口现在也能执行“开始/继续跟进”，不再静默无反应。
- 雷达回前台在情报缓存已失效时不会先画旧卡片；客户详情固定底部为左“开始/继续跟进”、右浅橙色“放弃/恢复跟进”，与雷达卡片的右侧状态动作一致。

### 本轮验证

1. `node --check`：客户详情、雷达、资源导航、详情缓存、`app.js` 通过。
2. 页面 JSON 解析和 `git diff --check` 通过。
3. `backend/tests/test_sales_scrm.py`：16 passed。
4. 对抗式断言通过：canonical note 路由、缓存环境/用户隔离与 TTL、详情/雷达按钮顺序；未发现详情页读写 `wx` 持久化缓存或旧资源导航引用。

### 用户下一步验收

1. 在微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存、重新编译并上传最新体验版；本轮未部署生产。
2. 雷达分别进入待跟进、访客和已放弃卡片，点击“原资料”，确认进入对应资料而不是资料库或报错。
3. 第一次进入客户详情观察冷启动加载；返回雷达再进入同一客户，确认基础身份/来源/联系方式先显示，且后台刷新不闪屏、不把旧客户覆盖到当前页面。
4. 从只有 `leadId` 的客户详情入口点击“开始/继续跟进”，确认有成功反馈并回到雷达；放弃/恢复后确认列表数字和卡片即时符合状态。

## 2026-08-27 最新交接：全链路缓存审计与生产后端部署

### 本轮完成

1. 完成小程序缓存审计并修复：
   - 资料、合集、专题、资源列表使用 60 秒元数据缓存，分类 10 分钟，内存优先、后台刷新、异步持久化。
   - 客户详情、雷达详情、手机号和微信号不写 wx 持久化存储，只做短时进程内缓存。
   - 媒体缓存按环境/API/用户隔离，仅 HTTPS 图片进入缓存，并增加 TTL、数量、容量和账号切换清理。
   - 退出登录、会话过期和用户切换会清理用户作用域缓存。
2. 完成服务端缓存修复并部署生产：
   - 列表缓存 TTL 60 秒；发布、复制、删除分类、浏览和互动后主动失效。
   - API/健康检查 no-store，媒体响应使用版本化长期缓存。
   - 生产备份：/home/ubuntu/teambuy-backups/cache-audit-20260827-050322。
3. 完成验证：
   - 后端全量：293 passed。
   - 客户端缓存行为脚本：通过。
   - 公网 /health：200，响应头符合 no-store。
   - 生产容器、源码 SHA256 和启动日志：通过。

### 仍需用户操作

1. 在微信开发者工具对 /Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram 清缓存、重新编译并上传体验版；否则手机仍运行旧体验包，不会看到本轮客户端缓存改动。
2. 体验版重点验收冷启动、返回缓存命中、退出登录后的数据隔离、资料/合集列表刷新，以及客户详情不落盘。
3. 当前服务端是单 API 进程的进程内缓存；正式扩容前要补 Redis 或共享失效总线。

## 2026-08-27 最新交接：跟进状态与已放弃列表修复

### 本轮完成

1. 修复生产 500 兼容边界：领域模型和服务状态白名单保留 `deleted`，清空线索仍可作为 tombstone 参与过滤。
2. 修复已放弃投影：只展示与真实 `paused` 线索匹配的客户，不再把无档案历史事件拼成已放弃客户。
3. 实现“开始/继续跟进”：
   - 开始：`pending → following`，记录“已开始跟进”；
   - 继续：保持 `following` 或兼容 `contacted`，记录“已继续跟进”；
   - 雷达新增“跟进中”页签和动态数量，客户从待跟进/访客队列移除；
   - 客户详情原地更新状态和最近动态，不再跳转雷达。
4. 雷达摘要和详情缓存携带最近跟进记录，减少缓存首屏与服务端状态不一致。

### 验证与边界

- `./.venv313/bin/pytest -q backend/tests/test_sales_scrm.py backend/tests/test_customer_info_chain_control.py`：20 passed。
- 相关 Python 编译、4 个小程序页面 `node --check`、`git diff --check` 均通过。
- 本轮只完成本地代码和文档，未部署生产；上线时必须将 `domain.py`、`app_service.py`、`routes_ops_admin.py`、`routes_scrm.py` 与小程序雷达/客户详情页面作为同一版本发布。
- 微信体验版仍需用户清缓存、重新编译并上传后才能验证“跟进中”页面和详情原地更新。

## 2026-08-27 最新交接：跟进状态后端已部署

- 已部署后端文件：`backend/app/models/domain.py`、`backend/app/services/app_service.py`、`backend/app/api/routes_ops_admin.py`、`backend/app/api/routes_scrm.py`。
- 线上备份：`/home/ubuntu/teambuy-backups/follow-up-status-20260827-063235`。
- 线上状态：backend 绑定 `0.0.0.0:8004->8000`，backend-worker、archive-worker、Postgres 均运行；本机和 `https://teambuy.lifelove.top/health` 均返回 200；`/api/scrm/customer-intelligence` 未登录返回 401，说明路由与鉴权链生效。
- 线上数据未迁移、未修改 `.env`、密钥、媒体和数据库卷。
- 小程序前端尚未部署；用户需在微信开发者工具清缓存、重新编译并上传体验版后，验证“跟进中”页签、数量动态、开始/继续跟进记录及详情原地更新。

## 2026-08-27 最新交接：跟进状态与客户详情统一收口

### 已完成

- 服务端跟进动作实现幂等 `operationId`、乐观并发 `expectedVersion`、条件写入和提交后回读；首次无档案动作仍由服务端单请求建档并写入目标状态和动作日志。
- 雷达提醒、跟进中和已放弃投影携带 `leadVersion`/最近跟进日志；雷达放弃/恢复和详情放弃/恢复均发送版本号。
- 雷达缓存实现真正 stale-while-revalidate，缓存旧响应、旧账号、旧客户和晚到请求不能覆盖当前页面；跟进动作成功或缓存不确定时清理相关缓存。
- 客户详情使用短时 owner/environment/mode/customer/lead 隔离的内存缓存，先画后刷；详情热路径只查当前身份最近 50 条事件和动作。
- 客户留言摘要只关联拥有者与已知登录客户的精确会话；匿名客户不会被猜测关联。
- 完成 `docs/qa/客户跟进状态与客户详情_Codex自测报告.md`、`docs/qa/客户跟进状态与客户详情_对抗式审查报告.md`、`docs/qa/客户跟进状态与客户详情_复测与回归报告.md`。

### 验证结果

- 后端全量：293 passed；SCRM 专项：16 passed。
- Python compileall、相关小程序 JS `node --check`、核心 JSON 解析、`git diff --check`：通过。
- 页面对象级回归：缓存旧响应、强制刷新、雷达连续点击、请求超时不复活、详情冷启动、详情缓存优先重进：通过。

### 当前边界与下一步

1. 本轮未部署生产，未上传小程序体验版。
2. 用户需在微信开发者工具清缓存、重新编译并上传最新体验版，再进行真实冷启动、连续点击、断网/超时、返回重进和留言关联验收。
3. 若后续启用多 API 进程/副本，先引入 Redis 或共享失效总线，再扩大服务端缓存范围。

## 2026-08-27 最新交接：统一收口版本已重新部署生产

### 发布结果

- 已将本地统一收口版本的 5 个后端文件同步并打入生产镜像：`backend/app/models/domain.py`、`backend/app/schemas/scrm.py`、`backend/app/services/repository.py`、`backend/app/services/app_service.py`、`backend/app/api/routes_scrm.py`。
- 线上备份：`/home/ubuntu/teambuy-backups/followup-unified-20260827-104106/backend/app/`。
- 已重建并启动 `backend`、`backend-worker`、`archive-worker`；Postgres 未重建，生产 `.env`、密钥、数据库和媒体数据未修改。
- 生产 API 仍为 `0.0.0.0:8004->8000`；本机及公网 `/health` 返回 200；SCRM 情报接口未登录返回 401；容器内关键文件 SHA256 与本地一致。

### 用户验证

- 后端现在已经是最新版本；但微信体验版不会因后端发布自动更新前端。请在微信开发者工具中打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存、重新编译后上传体验版。
- 体验版重点验证：雷达“跟进中/已放弃”数量与列表、开始/继续跟进落库和动作日志、详情留言摘要、客户详情缓存优先、连续点击、网络超时、返回重进和冷启动。

### 运行边界

- 线上仍是单 API 进程的进程内服务端缓存；未来扩容前必须先增加共享缓存/失效机制。
- 真实微信端和生产账号的最终人工验收仍由用户完成；本次公网基础验证已通过。

## 2026-08-27 最新诊断：体验版仍报错

- 生产后端不是当前阻塞点：重建后 `backend`、两个 worker 和 Postgres 均正常；数据库中已有最新跟进状态，动作接口日志有 3 次 200。
- 体验版必须单独更新。请确认微信开发者工具的项目目录为 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存、重新编译并上传一个新版本；只重启后端不会改变体验包内的旧 JS/WXML。
- 上传后首次只测试一个客户：点击“开始跟进”或“放弃跟进”，记录操作时间；随后返回雷达、重新进入详情并刷新。若仍失败，需以该次时间在生产日志中确认是否出现 `POST /api/scrm/customer-followups/action`，再按请求中的客户/线索 ID核对数据库，避免把旧包或不同客户身份误判为未写入。

## 2026-08-27 最新交接：房源雷达状态投影已修复

### 本轮结论

- 11:11 左右的跟进请求已经返回 200，数据库也确实有 `following` 状态；“数字又返回”的直接根因是房源看板用 `note.id` 过滤线索，遗漏了生产资料对应的 `sourceCardId`。
- 已修复房源雷达和客户详情的来源匹配，并补充回归测试；本地 SCRM 专项 17 项通过。

### 生产结果

- 已备份并重建线上 `backend`、`backend-worker`、`archive-worker`，备份路径：`/home/ubuntu/teambuy-backups/property-radar-source-20260827-112556/backend/app/services/app_service.py`。
- 生产 Postgres、环境变量、密钥和媒体数据未修改；本机/公网健康检查 200，未登录业务接口 401，容器内定向编译通过。

### 当前仍需用户确认

1. 在微信开发者工具确认项目目录为 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存、重新编译并上传体验版；后端发布不会自动替换体验包。
2. 上传后只测一个客户：在雷达点“开始跟进”或“放弃跟进”，返回雷达并重新进入，确认状态数字和对应页签不回退。
3. 当前无法从 11:11 的服务器日志直接识别体验包版本，因为 `1.4.8` 是客服插件版本，不是小程序构建号；扫码显示“悦享互动宝”属于微信后台登记名称/二维码 AppID 范围，代码导航栏标题仍是“资料整理助手”。

## 2026-08-27 最新交接：清理待跟进旧接口回退逻辑

### 根因

- 11:53/11:55 截图中的“待跟进 5”并非数据库重新写入了 5 条待办。生产 `/api/scrm/customer-intelligence` 返回 `opportunityAlerts=0`，当前小程序却使用旧逻辑 `alerts.length ? alerts : profiles.slice(0, 5)`，将普通访客画像补成待跟进卡片。

### 已处理

- `miniprogram/pages/visits/index.js` 已改为 `const rawRadarCards = alerts`。
- 待跟进为空时不再显示旧日期、低意向或普通访客卡片；跟进中、已放弃和客户详情仍继续使用同一个新情报接口。
- 没有删除 `/api/scrm/customer-intelligence`。需要删除的是错误的前端回退，不是承载新状态投影的正式接口。

### 验证与人工步骤

- `node --check miniprogram/pages/visits/index.js` 通过；`git diff --check` 通过。
- 这是小程序前端修复，尚未替换微信体验包。请在微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存、重新编译并上传后再测；只重启后端不会改变已上传的前端包。

## 2026-08-27 最新交接：详情跟进表单与回雷达页签

### 本轮完成

- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/customer-detail/index.wxml` 新增跟进记录区：系统标签、跟进标签、备注、下次日期、最近记录和保存按钮。
- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/customer-detail/index.js` 复用已有跟进更新接口保存标签/备注/日期；开始、继续、放弃成功后等待服务端确认，清理缓存并回到进入详情时的雷达页签。
- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/visits/index.wxml` 为待跟进、访客、跟进中入口补来源页签，并让跟进中卡片显示标签；`index.js` 优先使用已解锁情报缓存，减少详情点击前的重复会员请求。

### 验证与人工步骤

- 已通过：客户详情/雷达/接口 JS `node --check`、后端 SCRM 17 项、`git diff --check`。
- 用户需在微信开发者工具清缓存、重新编译并上传体验版；分别从待跟进、访客、跟进中打开详情，验证开始/继续/放弃成功后返回对应页签、卡片数字不回弹，并在跟进中详情保存标签/备注/日期后重进确认。
- 生产后端本轮未变更；本轮是小程序前端，不能通过重启后端替换体验包。

## 2026-08-27 最新交接：生产 500 与访客投影修复

### 本轮结论

- 已定位生产“跟进中客户详情 500”：`view_events.visitor_identity_id` 未在生产数据库落地，详情热路径 SQL 因缺列失败。
- 已补 `backend/app/core/schema.sql`、`backend/app/services/repository.py` 的字段/索引迁移，并补 schema 回归断言。
- 已修正 `miniprogram/pages/visits/index.js`：访客列表只保留无 `leadReminderId` 的画像；跟进或放弃后的客户不会再被访客列表重复显示。

### 验证与发布

- 本地 `test_postgres_repository_schema.py` 与 `test_sales_scrm.py` 共 20 项通过；相关小程序 JS、Python 定向检查和 `git diff --check` 通过。
- 需要部署后端并重启生产 API 让数据库动态迁移执行；生产验证重点是：跟进中详情不再 500、访客数字不再包含已处理客户。
- 小程序前端改动仍需用户在微信开发者工具清缓存、重新编译、上传最新体验版；仅部署后端不会替换体验包。

### 生产执行结果

- 生产 Postgres 已直接完成并核验 `view_events.visitor_identity_id` 字段及 `idx_view_events_visitor_identity` 索引，线上健康检查 200，当前 API 容器保持稳定运行。
- 镜像重建因基础镜像依赖下载长时间无响应而中止，未切换半成品容器；源码备份和同步已完成，待基础镜像网络正常后再按 `APP_PORT=8004` 重建后端镜像。
- 访客列表的前端分流修复尚未进入微信体验包；必须重新清缓存、编译、上传 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram` 后，访客人数互斥规则才会在客户端生效。

## 2026-08-27 最新交接：跟进动作慢与标签单列修复

### 本轮处理

- 已修复详情页、雷达卡片的 lead-based 请求：有 `leadId` 时不再携带冲突的 `customerId`，避免误触发“客户与跟进档案不匹配”和额外慢速对账；无 `leadId` 的首次访客仍使用 `customerId` 建档。
- 已将客户详情跟进标签改为固定两列，保留按钮居中和单行文案。

### 验证与人工步骤

- `node --check miniprogram/pages/customer-detail/index.js`、`node --check miniprogram/pages/visits/index.js` 通过；SCRM/schema 定向测试 20 passed；`git diff --check` 通过。
- 本轮没有后端代码变更，也不需要重新部署后端；请在微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存、重新编译并上传体验版，否则真机仍是旧 JS/WXSS。
- 上传后重点验证：从待跟进、访客、跟进中分别进入详情；点击开始/继续/放弃后只出现一次加载，服务端确认后返回对应页签，数字不回弹；在详情保存标签时显示两列。

## 2026-08-27 最新交接：对抗式审查收口客户身份与两列标签

- 已在 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/services/api.js` 增加统一防线：只要动作或详情读取带 `leadId`，就不会再发送 `customerId`；访客首次无 `leadId` 时仍保留 `customerId` 建档。
- 已清理客户列表、线索列表、资源分析和雷达详情导航中的已建档双 ID 传递，避免未来新增入口再次复发。
- 当前源码标签通过 `flex-basis/width: calc(50% - 6rpx)` 固定两列；若体验机仍显示一列，优先判定为体验包未更新或开发者工具缓存，不能依据旧截图否定当前 WXSS。
- 本轮未变更后端、不需要重新部署后端；仍需清缓存、重新编译并上传体验版后进行真机验证。

## 2026-08-27 最新交接：17:17 放弃访客后人数回弹已修复

### 结论

- 生产数据库已确认 17:18:11 有 paused 线索写入，版本 16，来源是团购资料；不是动作未写库。
- 回弹根因是房源雷达按当前场景组装历史访客时，没有用其他场景已处理线索做全局身份屏蔽；另一个遗漏是顶层 summary.visitorCount 仍统计原始历史事件。

### 已发布

- 已同步并生效：app_service.py、routes_scrm.py、repository.py、schema.sql。
- 生产重启后数据库已确认新增客户详情和跟进动作所需字段；公网 health 200。
- 线上直接重算房源模式结果：活跃访客 4、跟进中 3、已放弃 12；17:18 那条已放弃身份不再出现在活跃雷达投影，客户详情可正常读取。
- 本地 schema/SCRM 回归 21 passed；Python/JS 语法和差异检查通过。

### 仍需用户完成

1. 在微信开发者工具打开 /Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram，清缓存、重新编译并上传体验版；手机当前安装的旧包不会自动替换。
2. 上传后只测试一条访客：放弃跟进 → 返回雷达 → 重新进入“活跃访客”，确认数字和列表均减少且不回弹。
3. Docker 基础镜像依赖下载曾阻塞，当前运行容器已热替换并重启生效；宿主机源码也已同步，后续依赖网络恢复后应重建正式镜像，避免未来 recreate 回到旧镜像。

## 2026-08-27 最新交接：清理雷达页旧渲染残留

- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/visits/index.wxss` 已收敛为当前 `radar-workbench-*` 样式和基础 `.radar-page`，未被当前 WXML 使用的三套历史样式已清理。
- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/visits/index.js` 已删除无调用方的旧摘要/时间线构造函数；当前待跟进、访客、跟进中、已放弃均来自服务端明确投影。
- API 边界仍强制 lead-based 请求不发送旧 `customerId`；首次无线索访客仍保留建档参数。
- 已通过定向 JS 语法检查、身份不变量检查、SCRM/schema 测试与 `git diff --check`；本轮无后端变更。
- 要让手机看到这版：微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram` → 清缓存（全部清除）→ 编译 → 上传新体验版本；仓库无法代替这一步。

## 2026-08-27 18:38：核对 17:17 真机测试与修复生效边界

- 生产 access log 已确认 17:18:11 的 `POST /api/scrm/customer-followups/action` 返回 200；该次操作确实落库，随后读取请求也返回 200。这个测试发生在本轮跨场景屏蔽修复和服务重启之前，不能用它判断修复后的结果。
- 当前三个运行容器均已核对为同一份修复源码，公网 `/health` 返回 200；旧兼容接口 `/api/dashboard/business` 和新接口 `/api/scrm/customer-intelligence` 的房源投影均为活跃访客 4、跟进中 3、已放弃 12，计数与列表同源。
- 如果手机仍显示 7，优先是手机继续运行旧体验包/旧页面内存状态。必须重新进入已上传的新体验版本；仓库修改不会替换手机中的微信包。

## 2026-08-27 最新交接：跟进记录两列与动作性能优化

### 本轮完成

- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/customer-detail/index.wxss`：跟进标签改为兼容小程序的固定两列，文字保持单行并上下左右居中。
- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/customer-detail/index.js`：开始/继续跟进成功后排入雷达页进程内 mutation；未知结果对账成功也排入 mutation，避免回雷达后强制完整重算。
- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/visits/index.js` 与 `customer-intelligence-store.js`：消费 mutation 时跨四个状态集合移除旧卡、放入目标集合，操作后不再阻塞完整雷达读取；清理无效的旧刷新标记。
- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/backend/app/services/app_service.py`、`routes_scrm.py`、`schemas/scrm.py`：首次动作接收紧凑身份/来源投影，直接创建跟进档案并返回持久化线索与四类计数；修正 compact 分支旧变量引用。
- `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/backend/tests/test_sales_scrm.py`：增加测试，禁止首次动作调用完整客户详情，仍验证成功落库。

### 验证与发布边界

- 已通过：`backend/tests/test_sales_scrm.py`、`backend/tests/test_postgres_repository_schema.py` 共 22 项；相关 Python/JS `node --check`/`py_compile`；`git diff --check`。
- 生产后端本轮尚未部署；需按项目生产部署文档备份后重建/发布后端，不能只同步源码文件。
- 小程序体验版本本轮尚未替换；用户需在微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存、重新编译并上传后，才能看到两列标签和快速返回雷达效果。

### 真机验收顺序

1. 从访客卡片进入详情，点击开始跟进：确认卡片立即从访客移到跟进中、计数变化且不出现整页长 loading。
2. 从跟进中进入详情，点击继续跟进或放弃跟进：确认返回原雷达页签，状态不会回弹。
3. 冷启动、重进页面、手动刷新：确认最终四类计数和列表以服务端真实状态为准。
4. 在详情跟进记录中确认标签为两列；保存标签/备注/日期后重新进入确认记录仍在。

## 2026-08-27 最新交接：跟进记录结构化与雷达轻量化

### 本轮完成

- 后端跟进历史记录已结构化：同一条记录包含动作、标签、备注、下次跟进日期和创建时间；只选标签/日期也会保存。
- 雷达投影已移除 `followUpLogs` 和旧 `latestFollowUp`，卡片只消费当前状态、版本与最新下次跟进日期；详情接口继续返回完整历史。
- 详情页缓存快照不再伪造历史，日期选择器与历史时间分别使用适合 UI 的日期格式和完整时间格式。

### 验证边界

- `./.venv312/bin/pytest backend/tests/test_sales_scrm.py -q`：19 passed。
- `node --check miniprogram/pages/customer-detail/index.js`、`node --check miniprogram/pages/visits/index.js`、`git diff --check`：通过。
- 本轮未部署生产，也未替换微信体验包；真机需重新编译上传后验证雷达卡片只显示摘要、详情显示绑定的标签/备注/时间记录。

## 2026-08-27 最新交接：21:53 最近记录标签显示修复

- 设计核对通过：最近记录不是纯文本日志，而是标签、备注、下次日期、动作和创建时间的同条记录快照。
- 当前 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram/pages/customer-detail/index.wxml` 已使用 `record/tag` 显式嵌套循环；`index.js` 已读取并规范化 `log.tags`，后端 `update_lead_reminder` 已持久化 `followUpTags`。
- 客户端构建号为 `20260827-followup-records-1`。本轮仅完成本地源码与回归检查，未部署生产、未替换微信体验包。
- 真机验证必须用新体验包新建一条记录：选择标签并填备注/日期，保存后重新进入客户详情，在“最近记录”同一条中检查标签、备注和时间；旧纯文本记录不具备可恢复的历史标签。

## 2026-08-27 最新交接：清理跟进旧写入与静默兜底

- 当前详情跟进链路已移除 `log.customerTags` 标签兼容读取、页面旧历史响应兜底，以及后端 `customerTags` 跟进写入分支。
- 新表单唯一提交 `followUpTags`；接口返回缺失时不再拿旧记录冒充成功结果。
- 旧历史记录仍保留只读显示，但缺少标签的数据不会被伪造补全。需重新编译上传 `20260827-followup-records-1`，再新建记录验证。

## 2026-08-27 最新交接：22:21 线上跟进标签丢失根因

### 已确认

- 线上 `teambuy-backend-1` 仍运行旧后端源码：更新请求仍使用旧 `customerTags`，更新服务也没有结构化 `followUpTags` 分支。
- 这解释了“手动输入下周能显示、所选标签不显示”：文字字段被旧接口写入，标签字段被忽略；旧历史记录本身不能恢复当时未保存的标签。

### 本地已处理

- 详情页保存成功必须收到本次新记录且包含 `action/tags`；收到旧接口响应时直接提示“跟进接口版本未更新，请重新部署后端并上传最新体验版”，不再静默展示假成功。
- 本地 SCRM 回归 19 passed，Postgres schema 回归 3 passed，客户详情 JS 语法和 `git diff --check` 通过。

### 发布边界

- 本轮未部署生产。要让 22:21 问题真正消失，必须先按生产部署规范发布包含 `followUpTags` 的后端，再重新编译上传体验版。
- 发布后用一条新记录验收：选择“已电话沟通/需求已确认”，输入“下周”，保存并重新进入详情；同一条最近记录应同时显示标签和文字。

## 2026-08-27 最新交接：22:32 后端已重新部署

### 发布结果

- 生产 `teambuy-backend-1`、`teambuy-backend-worker-1`、`teambuy-archive-worker-1` 已切到包含 `followUpTags` 的镜像；旧镜像已保留 `pre-followup-20260827-2221` 标签。
- API 已按 Nginx 约定映射 `8004:8000`，公网 `/health` 返回 200，`/health/db` 返回 PostgreSQL connected=true。
- 常规基础依赖构建曾卡在 apt 下载，本次使用同一生产运行时的最小热修复镜像完成发布；没有修改 `.env`、密钥、数据库和媒体数据。

### 用户验收

1. 微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`。
2. 清除全部缓存，重新编译并上传体验版（本次客户端构建号：`20260827-followup-records-2`）。
3. 新建一条跟进：选择“已电话沟通”和“需求已确认”，输入“下周”，保存。
4. 重新进入客户详情，确认同一条“最近记录”同时出现标签、文字和时间。

### 失败边界

- 如果未上传新体验版，手机仍可能看不到本轮前端契约校验。
- 如果新体验版保存时仍提示“接口版本未更新”，应立即停止重复测试并核对实际请求域名是否为 `https://teambuy.lifelove.top`；生产容器源码已核对包含新字段。

## 2026-08-27 最新交接：待跟进进入跟进中同步修复

### 本轮处理

- 详情页开始/继续跟进成功后，改用服务端返回的 `lead.id`、`lead.visitorIdentityId`、查看者/匿名身份键更新雷达 mutation，不再依赖 `leadId` 路由下为空的旧 `customerId`。
- mutation 会跨四个集合删除同一客户的旧卡，再按动作放入跟进中或已放弃；开始/继续跟进返回跟进中页签，放弃返回已放弃页签。
- mutation 增加当前下次跟进日期，仍只存在进程内；手机号、微信号、客户详情不进入本地持久化缓存。

### 验收重点

1. 从待跟进卡点“处理情况”，在详情点击“开始跟进”：应快速返回“跟进中”，待跟进数字减 1、跟进中数字加 1，卡片不再出现在待跟进。
2. 从访客卡点“处理情况”，点击“开始跟进”：访客数字减 1，跟进中数字加 1；重进页面和手动刷新后保持一致。
3. 从跟进中点击“继续跟进”：仍留在跟进中且新增动作记录；点击放弃：移到已放弃。
4. 新体验包必须重新编译上传；若只刷新后端，手机端仍不会包含本轮 mutation 修复。

客户端构建标识：`20260827-followup-routing-1`。

## 2026-08-28 最新交接：统一跟进动作与雷达摘要首屏

### 已完成

- 客户详情移除旧的页面内保存按钮和旧保存/兜底调用；底部唯一按钮一次提交标签、备注、下次时间及开始/继续跟进，成功后自动回到雷达对应的“跟进中”页签。
- 后端原子动作写入当前线索状态和一条结构化跟进记录；记录绑定动作、标签、备注、下次跟进时间和创建时间，并保留幂等/版本校验。
- 雷达增加轻量摘要接口，首屏状态计数先返回，完整卡片后台补齐；未加载状态显示 `—`；客户雷达使用短时进程内缓存，不把客户敏感数据写入手机持久化缓存。

### 验证结果

- `backend/tests/test_sales_scrm.py`：21 passed。
- 全量后端测试：298 passed。
- Python/Node 语法检查及 `git diff --check`：通过。
- 对抗式检查确认：客户详情无 `ensureCustomerFollowup`、旧保存 handler 或 `updateLeadReminder` 调用；旧后端 helper/兼容路由未被当前详情链路调用。

### 人工验收与发布边界

1. 微信开发者工具清除缓存，重新编译并上传最新体验版；本轮没有部署生产或上传小程序。
2. 冷启动雷达：先见摘要数字或 `—`，不应先显示假 0；随后卡片补齐。
3. 从待跟进、访客分别进入详情，选择标签、输入备注、选择下次日期，点击一次底部按钮：应写入数据库、移入跟进中、计数同步并自动返回雷达。
4. 重进详情核对同一条最近记录同时显示标签、备注、下次时间和创建时间；连续点击、请求超时、返回页面和手动刷新不得恢复旧状态。
## 2026-08-28 最新交接：详情底部第二按钮残留已清理

### 本轮修复

- 本地复核发现客户详情 WXML 仍残留独立的放弃/恢复按钮，和交接摘要中的“底部唯一按钮”不一致。
- 已删除详情页独立状态 handler、状态锁、按钮及相关样式；底部统一提交按钮改为明确 100% 宽度。
- 雷达列表仍保留放弃/恢复入口，继续复用 `POST /api/scrm/customer-followups/action`；详情统一提交继续携带标签、备注、下次跟进时间、`operationId` 和 `expectedVersion`。

### 验证与人工边界

- 定向 SCRM 测试 21 passed，Postgres schema 测试 3 passed，全量后端测试 298 passed；Python 编译、Node 语法、69 个小程序 JSON 和 `git diff --check` 均通过。
- 尚未部署生产，也未上传微信体验版。后续人工验收需在微信开发者工具清缓存、重新编译并上传，确认详情单按钮、雷达首屏未加载显示“—”、一次提交后的状态/标签/备注/下次时间/最近记录，以及快速连续点击和迟到响应不会回滚状态。
## 2026-08-28 最新交接：生产后端已完成客户跟进字段热修复

### 已完成

- 生产 `/home/ubuntu/teamBuy` 已切换 API、`backend-worker`、`archive-worker` 三个热修复镜像；应用源码只覆盖客户跟进动作所需的 schema、route、service 三份文件。
- 生产容器已确认 `CustomerFollowupActionRequest` 包含 `followUpTags`、`logContent`、`nextFollowUpAt`；动作 route 已转发三字段，service 已接收并参与原子跟进写入。
- `http://127.0.0.1:8004/health`、`/health/db` 和 `https://teambuy.lifelove.top/health` 均正常，端口映射为 `8004 -> 8000`。
- 回滚备份：`/home/ubuntu/teambuy-backups/20260828-011239/`；旧 API/worker 镜像保留 `rollback-20260828-011239` 标签。

### 未完成与人工验收

- 常规镜像构建因 `apt-get update` 网络卡住而未采用，已使用现有镜像范围热修复；后续网络恢复后可再安排完整构建，但不影响本次三字段链路部署。
- 无认证 POST 探针仅被登录中间件拦截，未写生产数据；不能替代真实用户验收。
- 需要在微信开发者工具清缓存、重新编译并上传最新体验版，然后登录后新建一条包含标签、备注、下次跟进时间的记录，核对状态、最近记录和雷达计数。

## 2026-08-28 最新交接：详情双操作与雷达四类计数修复

### 本轮修改

- 客户详情底部恢复两个并列操作：主按钮为“保存并开始/继续跟进”，第二按钮为“放弃跟进”；已放弃客户显示“恢复跟进”。两者均调用 `POST /api/scrm/customer-followups/action`，一次提交当前标签、备注、下次跟进时间和对应状态动作。
- 无跟进档案的访客详情也显示跟进表单，主按钮可直接完成首次建档；成功后按动作进入雷达“跟进中/已放弃/待跟进”。
- 雷达下方“待跟进、访客、跟进中、已放弃”四个页签都显示数字；加载中显示 `—`，缓存命中时先由同一份 dashboard 派生数字，摘要请求独立完成后再校准，避免卡片与数字不同步。
- 已按上次部署复盘更新四份运行资料：生产默认先做范围明确的镜像热修复和回滚备份，完整 `docker compose build` 仅在显式 `ALLOW_NETWORK_BUILD=1` 下作为例外；生产切换固定 `APP_PORT=8004`。

### 验证和发布边界

- 待运行：客户详情/雷达 JS 语法检查、相关后端回归、JSON/WXML 静态核对和 `git diff --check`。
- 后端客户跟进字段已在生产热修复镜像中核对；本轮只改小程序和文档，没有再次部署后端。
- 小程序仍未上传体验版。人工验收需清缓存、重新编译上传后确认：详情双按钮、已放弃恢复、首次访客可填写跟进、四个顶部状态数、四个页签数字（含 0）、雷达缓存返回速度，以及标签/备注/下次日期在同一条历史记录中保存。

## 2026-08-28 最新交接：四个主 Tab 性能与稳定性专项

### 本轮处理

- 后端房源客户看板和雷达投影改为批量读取资料事件、客户动作、合集事件；客户画像同一请求内复用访客联系方式查询，避免事件数量放大数据库连接次数。
- 前端首页、资料库先显示卡片元数据；“我的”页用客户摘要判断入口；摘要接口增加进程内短缓存、并发合并和动作后的失效，完整客户画像仍不进入 wx 持久化缓存。
- 浏览历史查询在仓储层按请求上限取最新事件，避免长期账号每次打开“我的”都扫描无界历史。
- 审查本地插件和页面引用后，`message-plugin`、`share-snapshot` 仍在使用；工作台子包及 `app.json` 页面仍有路由/兼容用途，本轮没有安全删除项，未做批量清理。

### 验证与发布边界

- 已完成 Python/Node 语法检查和 `git diff --check`；定向测试先因系统 Python 无 pytest、旧 `.venv` 为 Python 3.6 失败，切换项目 `.venv312` 后继续执行。
- `.venv312` 全量回归：`backend` 298 passed，`platform/wecom-archive-core` 6 passed；所有小程序 JS/JSON 静态检查及 `git diff --check` 通过。
- 本轮未部署生产，未上传微信体验版；提交后仍需在微信开发者工具分别验收冷启动、页面返回、缓存命中和客户雷达真实数据耗时。

## 2026-08-28 最新交接：固定分享模板与雷达 count-only 投影

### 本轮修改

- `share-snapshot` 升为 `share_card_v7`，七种资料按类型选择固定无图模板：文字、图片 OCR、链接、房源、商品、服务、名片；名片保留专用布局，真实 HTTPS 主图继续原图直发，无主图使用固定 Canvas 信息卡并限制标题/摘要两行、字段三项。
- 合集列表和合集编辑页统一识别真实封面；无封面使用 `showcase_info` 快照，修复空 `imageUrl` 仍开放原生分享的风险。合集分享回调现在必须拿到有效图片地址。
- 后端新增 `customer_radar_summaries` count-only 表、owner/mode 唯一索引、dirty 标记和 Json/Postgres 读写；雷达摘要干净时只读聚合行，标脏后才重建原有投影。
- 未删除 `message-plugin`、`share-snapshot` 或历史页面：前两者仍有实际引用，历史页面/子包仍有路由或兼容用途，未发现证据充分的安全删除项。

### 验证与发布边界

- 已通过定向后端测试 `24 passed`（包含 SCRM 和 Postgres schema）；Node 四个分享相关文件语法检查、Python 编译检查、类型化分享计划 smoke test、`git diff --check` 通过。
- 已完成静态分享链路对抗式审查：有图 direct、无图 typed snapshot、合集有/无封面、旧样式快照、准备失败和空 `imageUrl` 回调均已检查；微信开发者工具真机人工验收尚未完成。
- 本轮未部署生产、未上传微信体验版。后续需在开发者工具清缓存并重新编译，逐一验证四个业务场景、七种资料和合集的缩略图、目标路径、首屏计数及雷达冷/热读取耗时。

## 2026-08-28 最新交接：最终对抗式复核与并行读取

### 本轮补充

- 资料列表/资料库分享图未就绪的回调兜底已改为对应资料详情页，避免无 `imageUrl` 时被微信截取列表页面；合集和工作台分享仍要求有效图片后才开放原生分享。
- 雷达权限确认后，count-only 摘要与完整客户卡片并行读取，摘要 dirty 重建不再串行阻塞完整内容。
- 对抗式复核确认 `http://` 图片不会绕过 HTTPS 直发校验，会降级为类型化信息卡；有效 HTTPS/内部媒体地址才进入原图路径。

### 验证与发布边界

- 定向后端 `24 passed`，全量后端 `298 passed`；小程序 JS 语法、JSON、Python 编译、类型化分享冒烟和 `git diff --check` 通过。
- 未删除仍被引用的插件或历史页面；仅移除一个无外部引用的旧内部绘制包装函数。部署手册已将无构建范围热修复写成默认策略。
- 本轮未部署生产、未上传微信体验版。必须在微信开发者工具清缓存、重新编译后，人工验证四个业务场景、七种资料、合集有/无封面、长标题、省略号、原生分享图片和目标路径。

## 2026-08-28 最新补充：资料库按钮误发列表截图修复

- 根因已确认：资料库预热得到的 `shareImages[card.id]` 之前只写入、不被最终分享回调读取；列表精简对象缺少完整 `contentBlocks`，导致 fingerprint 与详情页快照不一致，回调返回 `null` 后被微信截成资料列表图。
- 已修复 `miniprogram/pages/library/index.js`：资料库的过滤显示、点击准备和 `onShareAppMessage` 均优先读取并校验预热 JPG 的资料 ID与 revision；错误版本或无图仍禁止原生分享，不返回列表页路径。
- 自动置顶逻辑已在资料库和合集列表生效：发送成功更新 `latestShareAt` 后重新排序；主要新建、编辑、发布、上传和 OCR 入口已主动失效资料元数据缓存。

### 当前完成边界

- 已完成：资料库分享回调断点修复、发送后本地置顶、主要资料变更缓存失效、旧分享运行时静态引用清理。
- 尚未完成：微信开发者工具清缓存/重新编译后的真机矩阵验收；旧微信消息中的历史截图无法被后端改写；所有历史资料的批量预生成不是当前策略，旧资料仍在首次分享或预热时生成。
- 本轮未部署生产、未上传体验版。下一步应先上传当前小程序代码，再用一条新建图片资料按“详情页直接发客户 -> 返回资料库 -> 发客户”顺序复测，并确认两条消息均为 JPG 且进入 `/pages/note-preview/index`。

## 2026-08-28：分享卡内部重复引导已移除

- 用户最新截图中的重复层来自 JPG 模板正文缺少资料摘要时的 fallback“打开小程序查看完整资料”，不是额外的小程序页面；该 fallback 已从统一绘图层和 `miniprogram/plugins/share-snapshot/index.js` 删除。
- 保留外层分享标题、资料正文（有内容时）、JPG 底部 footer 和详情页跳转契约；无正文时只留空白正文区，不伪造业务内容。
- `SHARE_CARD_TEMPLATE_REVISION` 已升级到 v4，旧 v2/v3 快照会因指纹不一致而重新生成；已发送到微信的历史卡片仍不可改写。

## 2026-08-28：商品分享图字段与裁剪问题已修复

- 商品分享源现在从 `structuredData.variants[].priceFen` 计算价格，兼容旧 `skuConfig.skus[].price`/`data.price`；价格会进入第一个 facts 标签和分享标题。
- 商品 `product_info` 主图改为 `contain`，保留完整商品图片；模板 revision 已升级到 `share_template_business_collection_v4`，会使旧商品 JPG 重新生成。
- 七类资料数值字段审计结果：房源的价格/面积仍使用当前 `property_listing` 字段；商品分享和合集编辑页商品筛选已补上 `variants[].priceFen`，兼容旧 SKU/平铺字段；文字、图片、链接、服务、名片分享模板没有独立数值字段被错误读取。合集 `itemCount` 当前来自合集条目数量，未发现同类旧价格字段。
- 仍需人工验收：新旧商品、单规格/多规格、售罄规格、横图/竖图、资料库发送和详情页发送；后端无需重新部署，必须重新编译上传小程序。
- 当前剩余事项：需微信开发者工具清缓存、重新编译并上传体验版确认视觉效果；旧微信消息中的旧 JPG 不会变更。资料库分享回调的本地快照校验已修复，但仍需真机验证。
- 未完成的长期事项：七种资料/四个场景/合集/名片的完整真机矩阵、历史资料批量预生成 JPG、旧页面和旧子包的完整引用审计与安全清理；这些不能仅凭静态检查宣称完成。

## 2026-08-28 最新交接：全系统性能稳定性专项已实现并完成对抗式审查

### 本轮已完成

- PostgreSQL 主仓储和运维功能开关仓储改为有限连接池；会员状态、客户详情、雷达和资料库读取减少全量扫描、重复建连和 N+1 查询。
- 未筛选资料库支持真实窗口分页；雷达摘要继续使用 count-only 投影；前端请求有 15 秒超时，资料/分类缓存有明确 fresh/stale TTL 和并发/代数保护。
- 上传、OCR、运维文件、企业微信和共享归档媒体均有大小边界；远程媒体改为限量流式读取，阻塞处理移出异步 API 事件循环。
- Compose 增加数据库/进程探活，延迟任务增加去重；对抗式审查覆盖缓存、迟到响应、分页、跨用户隔离、超限上传、远程媒体和连接池关闭场景。

### 验证边界

- `.venv312` 全量后端测试：`303 passed`；Python 编译、Node 语法、Compose YAML 解析、`git diff --check` 通过。
- 本地没有 Docker CLI 和可用 PostgreSQL，因此未验证真实容器启动、`psycopg-pool` 连接建立/回收、worker 探活和远程服务流式响应；这些是部署后的必验项。
- 本轮未部署生产，也未上传微信体验版。新增 `psycopg-pool` 后，生产不能使用旧镜像源码覆盖热修复，必须重新构建包含新依赖的 API、backend-worker、archive-worker 镜像。

### 下一步动作

1. 生产先检查磁盘/Docker 占用、备份 Compose/运行配置和数据库连接信息；用稳定网络重建新依赖镜像，不执行全局 Docker 清理。
2. 切换前验证三个镜像均可 `import psycopg_pool`；切换后检查容器健康、`127.0.0.1:8004/health`、`/health/db` 和公网 `/health`，再观察 worker 最近成功时间与任务积压。
3. 用户在微信开发者工具清缓存、重新编译并上传体验版，分别验收冷启动、页面返回、缓存命中、客户雷达四个数字和资料库首屏。

### 已知剩余风险

- 复杂关键词/分类资料筛选仍走原有完整过滤路径；worker 当前探活是进程级，不是任务循环级；生产 PostgreSQL 连接池和新依赖尚未实机验证。

## 2026-08-28 最新交接：性能专项已部署生产并完成真实启动验证

### 已完成

- 性能专项提交 `3aaaf17` 已部署；最终运行镜像为 `teambuy-backend:deploy-3aaaf17-r2`、`teambuy-backend-worker:deploy-3aaaf17-r2`、`teambuy-archive-worker:deploy-3aaaf17-r2`，三个容器均 healthy。
- 生产 PostgreSQL smoke test、API `/health`、`/health/db`、公网 `/health` 全部通过；两个 worker 已正常启动并产生正常 tick 日志。旧镜像回滚标签为 `rollback-20260828-205848`，备份目录为 `/home/ubuntu/teambuy-backups/20260828-205848-perf-runtime`。
- 本次新增 `psycopg-pool` 已在三个运行镜像中验证为 `3.2.6`；连接池默认 row factory 的真实生产问题已修复并补测。

### 后续人工动作

- 小程序尚未上传体验版；用户需在微信开发者工具清缓存、重新编译并上传后，按冷启动、返回、缓存命中路径验收资料库、雷达四个数字和资料分享卡。
- worker 当前 Compose 探活仍是进程级，后续如要提高稳定性，需要继续观察任务积压、最近成功时间和错误日志；本次部署没有引入新的监控系统。
## 2026-08-29 最新交接：推广奖励/提现页状态优化

### 本轮已完成

- 修改 `miniprogram/pages/referral-center/index.wxml`、`index.js`、`index.wxss`：首屏改为加载/失败/已加载三态，已加载页支持后台刷新提示和失败保留旧数据。
- 当前余额、最低提现额、每日次数、申请时段、手续费均显示服务端数据；移除前端按环境推断最低提现额的旧逻辑。
- 余额字段缺失或非法时显示“余额不可用”，不会把异常响应误报为 ¥0，也不会开放分享/提现操作。
- 未达门槛只显示明确的差额和“去资料库分享赚奖励”；只有规则完整且余额达标才显示金额选项与申请按钮。
- 提现选项自动包含服务端最低金额；提交和刷新均有边界保护，迟到请求不能覆盖新结果，详细规则默认折叠。

### 验证与发布边界

- Node 语法、状态断言、JSON 校验、`git diff --check` 和提现/支付后端相关回归均通过；已完成针对核心失败路径的对抗式审查。
- 本轮没有后端、数据库、Compose 或生产文件变更，不需要部署后端，也没有自动上传小程序。
- 用户下一步需在微信开发者工具清缓存、重新编译并上传体验版，人工检查：首次加载/断网重试、余额 ¥0、低于门槛、刚好达到门槛、¥50 部分金额可用、重复点击和刷新失败保留旧数据。
- 审核仍需用真实达到提现门槛的测试账号验证完整申请路径；若审核要求取消门槛，需另开后端规则变更，不可只改页面。

## 2026-08-29 最新交接：登录页游客入口调整

### 本轮已完成

- `miniprogram/pages/login/index.wxml`：将“游客模式 · 先浏览首页”移动到微信一键登录按钮之前。
- `miniprogram/pages/login/index.wxss`：游客入口改为 88rpx 全宽按钮，浅绿色高可见样式，统一 flex 上下左右居中。
- `handleSkipLogin` 原有行为保持不变：游客直接进入公开首页，不写入登录身份，也不要求登录协议确认。

### 验证与发布边界

- 本轮仅涉及登录页前端展示和入口顺序，无后端代码或接口变更。
- 尚未执行微信开发者工具上传；用户需清缓存、重新编译后检查登录页首个按钮为“游客模式 · 先浏览首页”，点击后可进入首页，微信登录仍能正常弹出授权流程。

## 2026-08-29 最新交接：正式最低提现金额改为 ¥0.10

### 本轮代码变更

- `backend/app/core/config.py`：正式环境最低提现金额默认值由 1000 分改为 10 分。
- `backend/.env.example`：同步示例配置为 `WECHAT_TRANSFER_MIN_AMOUNT_FEN=10`。
- `backend/tests/test_sales_scrm.py`：覆盖正式环境低于 10 分拒绝、恰好 10 分创建成功。
- 前端无需新增逻辑，既有服务端规则读取会显示 ¥0.10，并保留 ¥10、¥50、¥200 选项。

### 发布与人工验收边界

- 后端必须重新部署；部署时只更新 `WECHAT_TRANSFER_MIN_AMOUNT_FEN=10`，不覆盖生产 `backend/.env` 中其他密钥、数据库和支付配置。
- 部署后验证 `/api/scrm/referrals` 返回 `minimumWithdrawalFen: 10`，用余额至少 10 分的测试账号验证恰好 10 分可创建、9 分被拒绝；随后确认每日次数限制仍生效。
- 本轮不需要重新上传小程序，除非需要让用户立即看到前端金额选项的缓存刷新结果。

### 生产部署已完成

- 备份目录：`/home/ubuntu/teambuy-backups/20260829-171728-withdrawal-minimum/`。
- 生产 `backend/.env` 已显式设置 `WECHAT_TRANSFER_MIN_AMOUNT_FEN=10`；API、backend-worker、archive-worker 已用 `--no-build --force-recreate` 读取新配置并恢复 healthy。
- 容器内核对结果：`app_env=production`、`wechat_transfer_min_amount_fen=10`；运营规则接口返回 `minimumWithdrawalFen=10`。
- `http://127.0.0.1:8004/health`、`https://teambuy.lifelove.top/health` 和数据库健康检查均通过。
- 标准镜像构建曾在 `apt-get update` 超过 180 秒无进展而终止；本次变更没有新增运行依赖，线上实际生效点是已备份的生产环境变量。后续若要让镜像内的配置默认值与本地源码完全一致，可在基础网络恢复后再做正常镜像构建，但不影响当前 10 分运行规则。

## 2026-08-30 最新交接修订：用户使用数据独立为数据分析子页面（未部署）

### 本轮完成

- 恢复 PC “总览”和“客户经营”原有页面内容，待跟进客户逐条明细恢复显示。
- “数据分析”导航组新增“用户使用数据”子页面；该页面按需加载既有 user-activity-summary 接口，保留今日/近 7 日/总量、每页 10 行和四个字段升降序排序。
- 未修改后端汇总接口、统计口径或生产配置。

### 验证与发布边界

- 尚未部署生产，生产仍为 deploy-3aaaf17-r2；未上传微信体验版。
- 需要人工确认：总览和客户经营页面恢复原样；点击“数据分析 → 用户使用数据”后可加载表格、切换时间、排序和翻页。

## 2026-08-30 最新交接：PC 总览改为用户使用数据汇总（未部署）

### 本轮完成

- backend/app/api/routes_ops_admin.py 新增 /api/ops-admin/user-activity-summary，按用户聚合资料、分享、匿名访客和注册访客，支持 today、7d、total，每页 10 行和四个数值字段升降序排序。
- backend/app/static/ops-admin/index.html 新增总览“用户使用数据”表，时间切换、排序箭头和上一页/下一页均接入服务端；“待跟进客户”指标点击后定位该汇总表，不再显示待跟进客户逐条记录。
- 客户经营页移除重复的待跟进客户列表，仅保留客户信号、支付配置和运营健康；既有待跟进总数及旧明细接口未删除，避免影响兼容调用。
- 资料数按创建时间，分享数按资料/合集分享事件次数，匿名和注册访客按身份去重；日期窗口由服务端东八区自然日决定。

### 验证与发布边界

- 定向运营接口回归：2 passed；内嵌 PC JavaScript node --check、Python 编译和 git diff --check 通过。
- 本轮尚未部署生产，生产仍为已部署的 deploy-3aaaf17-r2 镜像和 2026-08-29 的 ¥0.10 提现配置；未上传微信体验版。
- 需要人工验收 PC 页面：输入管理员 Token 后检查用户表默认今日、近 7 日/总量切换、四个表头升降序、分页第 2 页，以及点击待跟进指标不再出现客户逐条时间明细。

### 真实风险

- 当前接口每次请求扫描现有状态快照后聚合，分页只减少返回行数，不减少历史事件扫描；当事件量明显增长时需监控运维接口延迟并再考虑数据库聚合投影。
- “注册访客”是访问中出现的登录用户去重数，不是可证明由某个内容拥有者直接带来的新注册；当前归因链不应从这张表反推推广佣金。

## 2026-08-30 最新交接：推广中心暖色双页改版（未部署）

### 本轮完成

- `miniprogram/pages/referral-center/` 已改为暖色调概览页：可提现 + 直接去提现、可点击推广指标、最近动态、资料库分享入口和直接展开的推广规则。
- 新增 `miniprogram/pages/referral-detail/` 并注册到 `miniprogram/app.json`：收益记录和推广好友使用页签切换；提现、提现记录、微信收款确认和提现规则均在收益页内承载，规则直接显示。
- `backend/app/services/app_service.py` 新增脱敏直接推广好友投影和收益明细昵称，奖励计算、归因关系、提现服务端校验保持不变。
- 个人页入口名称更新为“推广中心”。没有新建专用推广分享主页，分享按钮继续进入现有资料库和既有分享链路。

### 验证边界

- 后端销售 SCRM + 微信支付测试：`33 passed`；推广定向测试：`5 passed`。
- 小程序 JS/JSON 静态检查、`git diff --check` 通过；已完成本轮提现与推广状态的对抗式检查。
- 本地工作区仍包含用户此前未提交的配置、登录页、最低提现金额及文档改动，本轮未清理或覆盖；当前本轮新增/修改也尚未提交。
- 生产仍为已部署的 `deploy-3aaaf17-r2` 镜像和 2026-08-29 的 ¥0.10 提现配置，本轮未部署后端，也未上传微信体验版。

### 下一步人工验收

1. 在微信开发者工具清缓存、重新编译，检查推广中心暖色布局、可提现/去提现位置以及三个数字入口。
2. 验收“收益记录 / 推广好友”切换、好友昵称脱敏、一级/二级关系口径、空列表和刷新失败保留旧数据。
3. 用余额为 0、低于 ¥0.10、恰好 ¥0.10、可部分提现和已有提现记录的账号，检查规则直接显示、选项、重复点击和微信收款确认。
4. 通过资料详情、资料库、名片和合集各发一次分享，确认本轮没有改变既有分享卡 `imageUrl`、目标 `path` 和归因链路；分享人工矩阵仍未完成。

## 2026-08-30 最新交接：用户使用数据子页面已部署生产

### 本轮发布

- 生产已部署用户使用数据汇总接口和独立 PC 子页面；总览、客户经营原有页面内容保留。
- 发布备份目录：`/home/ubuntu/teambuy-backups/20260830-072815-user-activity/`。
- 当前 API 镜像：`teambuy-backend:deploy-20260830-072815-user-activity`；两个 Worker 使用同批发布标签但沿用原生产镜像内容。三项服务均已用 `APP_PORT=8004` 切换并 healthy。
- 回滚标签：`teambuy-backend:rollback-20260830-072815-user-activity`、`teambuy-backend-worker:rollback-20260830-072815-user-activity`、`teambuy-archive-worker:rollback-20260830-072815-user-activity`。

### 已验证

- `127.0.0.1:8004/health`、`/health/db` 和公网 `https://teambuy.lifelove.top/health` 通过。
- 新接口无管理员 Token 返回 `403`；使用服务器内部管理员 Token 请求用户使用汇总返回 `200`。
- `/ops` 返回页面同时包含 `data-tab="useractivity"` 和原 `customerFollowUpTable`。

### 未完成与边界

- 未上传微信体验版；本轮不涉及小程序发布。
- 仍需在真实浏览器人工确认运营后台的视觉和交互，尤其是“数据分析 → 用户使用数据”的时间范围、四字段排序、每页 10 行和分页。
- 用户汇总接口仍按请求扫描并聚合现有状态快照；数据量持续增长后需关注运维接口延迟。

## 2026-08-30 最新交接：用户使用数据补充首次注册时间（已部署）

### 当前结论

- 生产当前用户使用数据总量为 38，按系统用户记录统计；不能据此断言全部为真实微信用户，测试/Mock 记录仍可能包含在内。
- `User.createdAt` 已存在，表示用户首次在本产品建立记录的时间，不是微信账号注册时间。真实微信登录按 `openid` 找回用户后，改昵称不会改变 `createdAt` 或内部 `userId`。
- 本地已让接口返回 `createdAt`，页面用户列主显示稳定 `userId`、次显示昵称，并增加“首次注册”列。
- 本次代码尚未重新部署生产；如确认发布，需要重新制作 API 镜像并按既有 `APP_PORT=8004` 发布流程验证。
- 本次已重新部署生产；API 镜像为 `teambuy-backend:deploy-20260830-075602-user-activity-created-at`（`4ac8733c742e`），两个 Worker 沿用当前生产镜像内容并完成 Compose 重建，端口保持 `8004`。
- 发布备份目录：`/home/ubuntu/teambuy-backups/20260830-075602-user-activity-created-at/`；生产 `backend/.env`、`secrets/`、媒体目录和数据库卷未覆盖。
- 线上 `/health`、`/health/db`、`/ops` 页面标记和用户使用汇总接口均验证通过；接口总量为 38，每条当前页记录均返回 `createdAt`。

## 2026-08-30 最新交接：PC 二维码活码工作台（未部署）

### 本轮完成

- 新增独立 PC 页面 `/ops/live-qr`，不改既有运营后台导航和小程序分享链路。
- 活码使用固定入口 `/live-qr/{code}`，可创建、改目标、暂停/恢复、复制入口和下载 PNG；目标更新后旧二维码仍指向新目标。
- 活码记录进入主仓储和 PostgreSQL 动态表，访问次数与最近访问时间持久化；后台 API 使用现有管理员 Token。
- 新增运行依赖 `qrcode==8.2`，生产发布必须重新构建依赖完整的后端/API/Worker 镜像。

### 验证与发布边界

- 全量后端测试 `307 passed`；活码定向测试 `2 passed`；Python 编译、页面 JavaScript 检查、HTML 标记检查和 `git diff --check` 通过。
- 本轮尚未部署生产，未上传微信体验版。生产当前仍为上一批已验证镜像和配置。
- 人工验收：打开 `/ops/live-qr` 输入管理员 Token，创建一条以长期 HTTPS 地址为目标的活码，下载二维码；扫码后修改目标，再次扫码确认入口不变且目标已切换；暂停/恢复确认符合预期。

### 真实风险

- 该版本记录的是活码入口请求次数，不是去重访客；没有按设备、渠道或用户身份建立归因。
- 如果目标链接自身有过期时间，活码无法延长目标的有效期；长期投放必须选择稳定的 H5/落地页入口。

## 2026-08-30 最新交接：群运营二维码活码子页面（未部署）

### 本轮完成

- 活码已纳入现有 PC 运营后台的“群运营”菜单，新增“二维码活码”子页面；不改变既有群自动化和群二维码批量上传页面。
- 活码列表支持服务端分页，每页 10 条；显示外层二维码、固定入口、当前目标、目标有效期、访问次数、最近访问和使用状态。
- 支持创建活码、复制入口、下载外层 PNG、暂停/恢复，以及“更换二维码”更新当前群二维码目标。新建和换目标默认按 7 天有效期处理。
- 目标过期或暂停后固定入口返回 410；外层二维码和固定编号不变。目标必须是完整 http/https 地址，不能填一次性登录 ticket。

### 验证与发布边界

- 全量后端测试 `308 passed`；活码定向测试 `3 passed`；运维/仓储定向测试 `13 passed`。
- Python 编译、运营后台 JavaScript 检查、页面 DOM/视觉检查和 `git diff --check` 通过。
- 本轮尚未部署生产，未上传微信体验版；由于新增 `qrcode==8.2`，后续必须重新构建依赖完整的 API/Worker 镜像，不能只覆盖源码。

### 后续人工验收

1. 在真实浏览器打开生产运营后台，展开“群运营”，进入“二维码活码”。
2. 创建一条长期 HTTPS 目标，下载外层二维码；确认列表分页、到期状态、复制和下载操作。
3. 扫码后修改目标，再次扫码确认外层入口不变但目标已切换；验证过期、暂停和恢复提示。
4. 使用真实手机检查从小红书扫码到目标页面的完整链路。

## 2026-08-30 最新交接：活码投放卡片增加虚拟头像和群名（未部署）

### 本轮完成

- 运营后台“群运营 → 二维码活码”和兼容页面 /ops/live-qr 的活码列表，已增加竖版投放卡片预览。
- 卡片由浏览器自动合成：6 个虚拟头像、展示群名、固定外层二维码和最新群入口提示。
- 卡片不展示群有效期；PC 列表仍展示当前目标的 7 天到期状态并负责提醒。
- 默认操作改为“下载投放卡片”，原始二维码下载入口保留；活码创建、目标更换、固定 code、暂停/恢复和扫描计数未改变。

### 已验证

- 活码定向测试：3 passed。
- 两处 PC 页面内嵌 JavaScript 通过 node --check，git diff --check 通过。
- 本地 FastAPI + JSON 状态下已验证：真实二维码图片加载、Canvas 900×1200 卡片生成、6 个虚拟头像、群名预览和下载按钮。

### 生产与人工验收边界

- 本轮未部署生产，未修改生产配置，未上传微信体验版；生产仍为此前已验证的用户使用数据发布版本。
- 后续部署必须按新增 qrcode 依赖的完整镜像流程执行，不能只覆盖静态页面。
- 真实浏览器需确认下载后的 PNG 在小红书发布后可扫码；真实手机需确认扫码仍到固定活码当前目标。

### 真实风险

- 已发到小红书的旧图片不会自动更新，但固定外层入口仍可按服务端当前目标跳转。
- 浏览器字体和 Emoji 字体不同，群名及星号样式可能有轻微视觉差异。
- 卡片无有效期文字不等于群目标永不过期；到期仍需在 PC 页面更换目标。

## 2026-08-30 最新交接：群运营活码已部署生产

### 已完成

- 生产已切换活码 API、PostgreSQL 活码表结构、qrcode==8.2 依赖、运营后台“群运营 → 二维码活码”页面和 /ops/live-qr 兼容页面。
- 生产 Nginx 已新增 /live-qr/ 到 127.0.0.1:8004/live-qr/ 的代理；此前根路径 404 的公网扫码问题已修复。
- 活码投放卡片包含 6 个虚拟头像、展示群名、固定外层二维码和最新群入口提示，不显示群有效期；PC 页面仍显示目标 7 天有效期和到期提醒。
- 生产备份：/home/ubuntu/teambuy-backups/20260830-182429-group-live-qr；Nginx 备份：/etc/nginx/conf.d/teambuy.conf.bak-20260830-182429-group-live-qr。

### 生产验证

- 发布镜像：teambuy-backend:deploy-20260830-182429-group-live-qr-final2、teambuy-backend-worker:deploy-20260830-182429-group-live-qr-final2、teambuy-archive-worker:deploy-20260830-182429-group-live-qr-final2。
- API、backend-worker、archive-worker、PostgreSQL 均 healthy，端口保持 8004 -> 8000。
- /health、/health/db 和公网 /health 通过；运行容器可导入 qrcode，活码管理 API 未授权返回 403，未知公网活码入口返回 410。
- /ops 已包含 liveqr 与既有 useractivity 页面标记；/ops/live-qr 已包含投放卡片生成逻辑。
- 本轮未上传微信体验版，未改变小程序分享回调。

### 待人工验收

1. 在真实浏览器打开 https://teambuy.lifelove.top/ops，强制刷新并展开“群运营”，确认看到“二维码活码”。
2. 使用管理员 Token 创建一条长期 HTTPS 目标，确认列表分页、目标到期提醒、复制入口和投放卡片下载。
3. 用真实手机扫码确认进入当前目标；更换目标后再次扫码确认外层入口不变，再检查暂停/恢复和过期状态。
4. 将下载的投放卡片发布到小红书前，人工确认群名和二维码清晰可识别；换群后重新下载卡片，旧图片不会自动更新。

### 真实风险

- 当前活码访问次数是入口请求次数，不是去重访客，也不产生推广归因。
- 当前目标 7 天到期仍需要运营人员在 PC 页面更换；活码不能延长一次性或本身会过期的目标链接。
- 生产构建受 Debian 网络影响，本批使用受控依赖镜像发布；后续新增依赖仍需按同一策略验证，不能直接源码热覆盖。

## 2026-08-30 最新交接：活码支持上传解析与到期筛选（本地未部署）

### 本轮完成

- `/ops` 的“群运营 → 二维码活码”和兼容页 `/ops/live-qr` 均新增上传群二维码图片并解析链接；解析成功自动回填当前目标，仍可手动粘贴，不保存上传原图。
- 后端新增 `POST /api/ops-admin/live-qr-decode`，管理员 Token 保护，10MB 上限；`zxing-cpp==3.1.1` 首选，OpenCV 兜底，结果必须是完整 http/https 地址。
- 活码列表 API 新增 `expiryFilter` 和 `expirySummary`；页面顶部显示已过期、剩 1 天、剩 2 天、剩 3 天四个指标卡，点击指标或筛选器可查看对应列表并使用“更换二维码”编辑。列表分页仍为每页 10 条。

### 已验证

- `PYTHONPATH=backend .venv312/bin/pytest -q backend/tests/test_live_qr.py`：5 passed。
- `PYTHONPATH=backend .venv312/bin/pytest -q backend/tests`：310 passed。
- 两处内嵌 JavaScript `node --check`、Python 编译、`git diff --check` 通过。
- 本地真实浏览器已验证 3/2/1 天指标、指标筛选、编辑入口和二维码图片上传回填；实际参考微信群二维码截图成功解码为微信链接。

### 生产与人工验收边界

- 本轮只改本地代码和文档，未部署生产；当前生产仍是此前已部署的活码版本，尚未包含二维码解析、到期汇总和筛选。
- 因新增 `zxing-cpp==3.1.1`，后续生产发布必须按新增依赖完整镜像流程执行，同时保留并验证 Nginx `/live-qr/` 代理；不得只上传两个 HTML 文件。
- 未上传微信体验版，未修改小程序分享链路。

### 下一步建议

1. 真实浏览器用一张实际群二维码图片完成上传、解析、创建和更换目标验收；无法解析时确认手动粘贴兜底。
2. 在生产部署前先检查磁盘、Docker 空间和构建网络，按部署文档备份 Compose、明确源码并制作含 `qrcode` 与 `zxing-cpp` 的 API/Worker 镜像。
3. 部署后验证 API 鉴权、汇总/筛选分页、生产 `/ops` 菜单、`/ops/live-qr`、健康检查和公网固定入口，再用真实手机扫码。

### 真实风险

- 上传图片包含二维码不代表二维码内容本身长期有效；一次性微信群链接过期后仍需重新上传新群码并更新目标。
- 到期指标包含暂停活码，运营上是否需要处理仍需结合状态列判断；无有效期记录不会进入四个数字桶。
- 生产尚未包含本轮新依赖和接口，当前本地验证结果不能视为生产解析验证。

## 2026-08-30 最新交接：活码上传解析与到期筛选已部署生产

### 已完成

- 生产已包含 `/ops` 的“群运营 → 二维码活码”、兼容页 `/ops/live-qr`、上传解析接口和到期汇总/筛选分页接口。
- `zxing-cpp==3.1.1` 已进入 API、backend-worker、archive-worker 三个镜像；生产 Compose 继续使用 `8004 -> 8000`，PostgreSQL、生产 `.env`、secrets、媒体目录和数据库卷未覆盖。
- 生产固定入口的 Nginx `/live-qr/` 代理沿用此前已验证配置；本轮只更新应用镜像和明确的活码源码/页面。

### 生产记录

- 备份目录：`/home/ubuntu/teambuy-backups/20260830-202319-live-qr-decode`。
- 发布镜像：`teambuy-backend:deploy-20260830-202319-live-qr-decode`、`teambuy-backend-worker:deploy-20260830-202319-live-qr-decode`、`teambuy-archive-worker:deploy-20260830-202319-live-qr-decode`。
- 回滚标签：`teambuy-backend:rollback-20260830-202319-live-qr-decode`、`teambuy-backend-worker:rollback-20260830-202319-live-qr-decode`、`teambuy-archive-worker:rollback-20260830-202319-live-qr-decode`。

### 生产验证

- API、backend-worker、archive-worker、PostgreSQL 均为 `healthy`；本机和公网 `/health`、`/health/db` 均通过，端口仍监听 `8004`。
- `/ops/live-qr` 公网页面可访问；未知活码返回 `410`；带文件但无管理员令牌的解析请求返回 `403`。
- 使用本地生成的无敏感测试二维码调用生产解析接口，正确返回 `https://weixin.qq.com/teamBuy-production-smoke`；三个镜像均可导入 `zxingcpp` 和 `qrcode`。

### 待人工验收

1. 在真实浏览器强制刷新生产 `/ops`，展开“群运营”，确认“二维码活码”页面显示上传解析按钮、到期指标、筛选和分页。
2. 上传真实群二维码截图，确认能回填目标链接；如二维码内容是一次性/短期链接，仍需人工判断是否适合作为活码当前目标。
3. 创建或更换一条活码，检查“已过期 / 剩 1 天 / 剩 2 天 / 剩 3 天”指标、筛选列表和“更换二维码”入口。
4. 用真实手机扫码验证公网固定入口进入当前目标；本轮未上传微信体验版。

### 真实风险

- 生产解析成功只证明图片中的内容是完整 http/https 链接，不证明该微信群链接本身长期有效。
- 到期指标仍按目标剩余时长向上取整，临界时刻可能从 2 天进入 1 天桶；已发布小红书图片不会自动更新。
- 本次使用受控镜像发布，后续新增依赖仍须先走相同的 wheel/镜像验证流程，不能只同步源码。

## 2026-08-30 最新交接：活码扫码诊断与真实群头像卡片（本地未部署）

### 本轮完成

- 已核对生产现有活码：外层 `https://teambuy.lifelove.top/live-qr/{code}` 可返回 302，当前保存的微信群目标跟随跳转返回 404。因此现象不是外层活码二维码无法识别，而是当前目标地址本身不可用；后续应上传新的有效群二维码并点击“更换二维码”。
- 活码创建/编辑表单新增“真实群头像”上传；后端新增受管理员 Token 保护的头像媒体接口，复用媒体资产处理、哈希去重和引用清理。
- `/ops` 群运营活码页和 `/ops/live-qr` 兼容页的卡片预览/下载均使用真实上传的微信群头像拼图；未上传或头像加载失败时只显示提示，不绘制虚拟头像。

### 已验证

- `PYTHONPATH=backend .venv312/bin/pytest -q backend/tests/test_live_qr.py`：6 passed。
- `PYTHONPATH=backend .venv312/bin/pytest -q backend/tests`：311 passed。
- 两处页面内嵌 JavaScript 分别通过 `node --check`；Python 编译检查、`git diff --check` 通过。

### 生产与人工验收边界

- 本轮仅修改本地代码和项目文档，未部署生产；生产仍未包含真实群头像上传和卡片渲染改动。
- 部署后需在真实浏览器上传一张微信实际群头像拼图和新的群二维码图片，保存后下载卡片，用手机微信扫码确认固定外层入口进入当前有效目标。
- 需要区分“外层活码入口通过”“目标链接可用”“卡片真实头像显示”“真实手机扫码通过”四个验收层级；不能用页面能打开替代手机微信扫码验收。

### 真实风险

- 系统不能从群二维码内容自动获得微信群头像；头像需要单独上传。
- 目标链接返回 404/过期时，活码无法代替微信重新生成有效群二维码；延长后台有效期也不能恢复目标。

## 2026-08-30 最新交接：真实群头像活码改动已部署生产

### 已完成

- 生产已包含真实群头像上传接口、`groupAvatarUrl` 字段、真实头像投放卡片渲染，以及 `/ops` 群运营子页面和 `/ops/live-qr` 兼容页面。
- 三个应用服务已切换到：`teambuy-backend:deploy-20260830-211240-live-qr-avatar`、`teambuy-backend-worker:deploy-20260830-211240-live-qr-avatar`、`teambuy-archive-worker:deploy-20260830-211240-live-qr-avatar`。
- 生产备份目录：`/home/ubuntu/teambuy-backups/20260830-211240-live-qr-avatar`；三个对应回滚标签已保留。
- PostgreSQL、生产 `.env`、secrets、媒体目录和数据库卷未覆盖；无新增 Python/系统依赖。

### 生产验证

- API、backend-worker、archive-worker、PostgreSQL 均 healthy，8004 端口映射正常。
- 本机和公网 `/health`、`/health/db` 通过；`/ops` 与 `/ops/live-qr` 已出现“真实群头像”；管理员接口未授权返回 403。
- 生产活码列表已返回 `groupAvatarUrl` 字段，固定活码入口返回 302；运行中容器文件哈希与本地版本一致。

### 运营人员下一步

1. 打开生产 `/ops`，进入“群运营 → 二维码活码”，编辑现有活码。
2. 上传图 2 这种真实微信群头像拼图并保存；如当前目标仍打不开，同时上传新的有效微信群二维码图片解析并保存。
3. 下载投放卡片，用真实手机微信扫码确认进入当前有效目标。

### 真实风险

- 生产当前活码目标本身返回 404 的问题不会因本次头像部署自动恢复；需要新的有效群二维码目标。
- 本轮未在生产代替运营人员上传群头像或修改当前群目标，避免改变正在投放的数据。

## 2026-08-30 最新交接：新活码仍打不开，目标链接确认返回 404

### 诊断结论

- 新外层卡片二维码已解码为生产固定入口 `qr_g2mrjltbw5e`，入口返回 302；二维码图案、活码服务、Nginx 公网代理均正常。
- 新旧两个生产活码当前均保存同一个 `c.weixin.com/g/...` 目标，跟随目标访问返回 404；“有效期未到”不能推翻目标实际 404。
- 本次没有继续修改代码或生产数据，因为还缺少原始微信群二维码在微信中的直接扫描对照结果。

### 下一步人工确认

1. 直接在微信中扫描你刚上传的“里面那张微信群二维码”，不经过 teamBuy 外层卡片。
2. 如果直接扫描也打不开：原始群二维码无效/已过期/群状态不允许加入，请在微信群信息里重新生成后再上传。
3. 如果直接扫描能打开、外层卡片不能：把结果反馈给我，再评估将活码改成落地页展示当前群二维码并让微信二次识别，而不是服务端 302 到 `c.weixin.com`。

## 2026-08-30 最新交接：已生成临时二次识别测试入口

### 测试资产

- 临时活码：`qr_fsokeazode`，公网入口：`https://teambuy.lifelove.top/live-qr/qr_fsokeazode`。
- 临时落地页：`https://teambuy.lifelove.top/media/live-qr-test-20260830-214405.html`。
- 外层二维码本机文件：`/tmp/teamBuy-live-qr-outer-20260830-214405.png`。
- 页面展示的是用户刚提供的完整内层群二维码图，页面文案提示“长按二维码，选择识别图中二维码”。

### 已完成验证

- 外层固定入口 HTTP 302，目标为临时落地页。
- 临时落地页 HTTP 200，内层图片 HTTP 200，图片类型为 WebP。
- 没有修改仓库代码、Docker 镜像或小程序，也没有替换原有生产活码。

### 用户人工验证

1. 用手机微信扫描本交接中提供的外层二维码。
2. 确认能打开“群聊：外贸群”临时落地页。
3. 在页面内长按内层二维码，选择“识别图中二维码”。
4. 记录第二次识别后的结果：能否打开微信群入口、是否仍然出现 404 或其他微信提示。

### 风险与清理

- 这是一次性技术验证，不是正式二次识别功能；临时活码和媒体页验证结束后应暂停，避免被误投放。
- 如果二次识别成功，再单独设计正式落地页、内层二维码更新和过期提示；如果失败，问题仍可能来自内层群码本身或微信群入口限制。

## 2026-08-30 最新交接：活码删除与群人数提醒已上线

### 当前生产状态

- 生产已部署活码删除、群人数分级、人数筛选和 PC 每日一次页内提醒。
- 服务镜像：`teambuy-backend:20260830-222226-live-qr-member-reminder`、`teambuy-backend-worker:20260830-222226-live-qr-member-reminder`、`teambuy-archive-worker:20260830-222226-live-qr-member-reminder`。
- 回滚备份：`/home/ubuntu/teambuy-backups/20260830-222226-live-qr-member-reminder`；旧镜像保留 `rollback-20260830-222226-live-qr-member-reminder` 标签。
- API、backend-worker、archive-worker、PostgreSQL 均 healthy；本机 8004 和公网 `/health`、`/health/db` 通过。

### 使用与人工验收

1. 进入“群运营 → 二维码活码”，确认顶部出现 `180–199 人`、`200 人及以上`、`人数未检查` 三个数字。
2. 编辑某个活码，录入最近一次群人数并保存；确认列表状态、筛选、顶部数字和提醒颜色一致。
3. 在某行点击“删除”，确认弹窗后检查该行消失，并确认原固定入口不再可访问；不要用当前正在投放的活码做首次验证。
4. 重新打开页面，分别验证目标有效期筛选和群人数筛选可以叠加分页使用。

### 未完成与真实风险

- 群人数当前不是系统自动从微信群读取，而是人工录入/预留后续回传字段；现有记录若未录入会显示“人数未检查”。
- PC 提醒不是操作系统推送；每天打开后台时才会显示一次。若需要早上无人打开页面也能收到通知，还需单独接入可靠的定时通知渠道。
- 本轮未执行微信开发者工具真机验收，也未上传小程序体验版；本次改动只影响 PC/后端活码管理。

## 2026-08-30 最新交接：活码落地页二次识别、7 天默认有效期和自动群人数检测已部署

### 当前实现

- 正式固定入口 `GET /live-qr/{code}` 返回 200 落地页，不返回 302；落地页显示当前保存的内层群二维码，用户需要在微信中长按并选择“识别图中二维码”。
- PC “群运营 → 二维码活码”与 `/ops/live-qr` 兼容页均支持上传内层二维码图片。上传时后台解码并保存原图；解析出的目标链接仅作诊断/兼容字段，不能用于外层重定向。
- 每次新建时上传成功或更换内层二维码成功，`targetExpiresAt` 自动从服务器当前 `Asia/Shanghai` 时间计算 7 天后；PC 不再展示或要求填写有效期。
- backend-worker 每天 08:00 后按活码/日期创建幂等 `wechat.scan_live_qr_member_count` 任务。AScript `wechat_group_inventory` 领取限定任务，支持通讯录群聊和微信会话列表，打开“聊天信息”读取人数并通过 `/api/automation/live-qr/member-count` 回传。
- PC 人数提醒仍为 180–199 黄色、200+ 红色；来源不是 `wechat_group_info`、没有检测值或设备不新鲜时显示“未检测”，不误报正常。

### 验证状态

- 本地后端全量：`314 passed`；PC 页面 Node 语法、变更 Python/AScript 编译、`git diff --check` 通过。
- 本机 `scan_devices` 未发现 AScript 设备，未完成真实设备 UI/OCR/任务闭环；也未完成真实微信外层扫码、长按识别和二次加入验收。
- 本地工作区在这轮基础上仍包含之前推广中心、用户使用数据、活码和其他已确认的未提交变更，未做清理或 reset；部署时只能按本轮范围同步，不要覆盖生产 env、secrets、媒体目录和数据库卷。

### 生产发布结果

- 已按受控无构建热修复流程同步 API、backend-worker、archive-worker；本轮无新增运行依赖，生产 `backend/.env`、`backend/secrets/`、媒体目录和数据库卷未覆盖。
- 备份目录：`/home/ubuntu/teambuy-backups/20260830-233358-live-qr-auto-member/`；回滚标签为三个服务对应的 `rollback-20260830-233358-live-qr-auto-member`。
- 当前发布标签为 `teambuy-backend:deploy-20260830-233358-live-qr-auto-member`、`teambuy-backend-worker:deploy-20260830-233358-live-qr-auto-member`、`teambuy-archive-worker:deploy-20260830-233358-live-qr-auto-member`；Compose 端口仍为 `8004 -> 8000`。
- API、两个 Worker、PostgreSQL 均 healthy；本机和公网健康检查通过。公网固定入口已验证 `200` 且无 `Location`，未授权活码 API `403`，未知入口 `410`。
- 已知旧活码 `qr_ndejoqudeo8` 尚无 `targetQrImageUrl`，访问落地页会提示运营人员上传当前内层群二维码；这是禁止旧 302 回退的预期结果。

## 2026-08-30 最新交接：未接入 AScript 时暂停人数检测，活码落地页仅显示内层二维码（本地待部署）

### 本轮本地修改

- 群人数自动调度默认关闭：`LIVE_QR_MEMBER_COUNT_AUTOMATION_ENABLED=false`。在真实 AScript 设备完成验收前，后台不创建人数任务，设备也不会领取该类任务；人数显示应保持“未检测”。
- 保留后续 AScript 所需的候选群、任务、回传接口和数据字段，未来只需完成设备验收并显式开启配置，不把未知人数当成 0。
- `/live-qr/{code}` 继续使用固定入口和 200 HTML，不回退 302；有内层二维码时只展示上传的二维码原图，不再展示群头像、群名、群聊标题或操作说明；无内层图时显示未上传提示。
- 已更新配置示例、单元测试、AScript 工具文案及项目决策/风险记录。

### 验证和状态

- 本轮本地测试已完成：活码/自动化定向测试 22 passed，后端全量测试 315 passed，Python 编译、页面内嵌 JavaScript 语法和 `git diff --check` 均通过；本轮尚未部署生产，生产仍是上一版活码落地页及人数调度代码，不能把本地结果当作线上已生效。
- 部署时必须同时切换 API、backend-worker、archive-worker，并按腾讯云部署文档保留生产 `.env`、secrets、媒体目录和数据库卷。
- 真实微信扫码、长按识别和 AScript 设备验收仍未完成；没有 AScript 设备时，“未检测”是预期状态。

## 2026-08-31 最新交接：暂停群人数检测与纯内层二维码落地页已部署生产

### 生产状态

- 已部署 API、backend-worker、archive-worker 三个服务；PostgreSQL 保持原容器和数据卷，生产 `.env`、secrets、媒体目录未覆盖。
- 生产备份：`/home/ubuntu/teambuy-backups/20260831-000348-live-qr-no-ascript/`。
- 当前发布镜像：`teambuy-backend:20260831-000348-live-qr-no-ascript`、`teambuy-backend-worker:20260831-000348-live-qr-no-ascript`、`teambuy-archive-worker:20260831-000348-live-qr-no-ascript`；旧镜像保留对应 `rollback-20260831-000348-live-qr-no-ascript` 标签。
- 三个应用容器和 PostgreSQL 均 healthy，端口为 `8004 -> 8000`；本机和公网 `/health`、本机 `/health/db` 均通过。
- 三个应用容器中的 `LIVE_QR_MEMBER_COUNT_AUTOMATION_ENABLED` 均为 `False`，未接入 AScript 前不会创建或领取活码人数检测任务。

### 公网活码验收

- 当前有效活码 `qr_3zi4ilfiwk` 返回 HTTP 200 且无 `Location`；页面只包含内层二维码图片，没有群名、群头像、标题或操作说明。
- 页面引用的内层二维码图片返回 HTTP 200；未授权活码管理 API 返回 403。
- 本次没有上传小程序体验版，也未完成真实微信“外层扫码 → 长按内层图 → 识别图中二维码”人工验收。

### 后续

1. 用真实手机微信扫描当前外层活码，确认页面只显示二维码图片。
2. 长按该图片并选择“识别图中二维码”，确认能进入有效微信群。
3. 未来接入并验收 AScript 设备后，再显式开启 `LIVE_QR_MEMBER_COUNT_AUTOMATION_ENABLED=true`。

## 2026-08-31 最新交接：互帮互助统一积分架构与视觉评审稿

### 本轮状态

- 本轮是产品架构和视觉讨论，不是业务开发；没有修改后端、小程序、数据库、生产配置，也没有上传微信体验版。
- 新增架构文档：`docs/stage2-docs/44-my-tools-mutual-help-architecture-v1.md`。
- 新增视觉稿：`docs/stage2-docs/visuals/my-tools-mutual-help-v1.svg`。

### 已确认的产品规则

- 互帮互助只使用一种 `mutualPoints`。首次进入赠送 100 分；小程序任务默认发布者消耗 5 分、执行者获得 4 分；普通任务最低奖励 5 分、执行者获得奖励的 0.8 倍。
- 首页默认按发布者当前互助积分从高到低排序，可切换为执行者可获得赏金从高到低排序。这里不新增信誉分，也不把热度、余额之外的第二套积分放进首页。
- 小程序任务支持发布者提供 `shortLink`；返回后的反馈文字和图片均可选。普通任务按标题、内容、交付标准和提交材料类型完成闭环。
- 三名不同执行者有效提交连续被拒绝后，任务暂停，发布者降级/复核；先从冻结额度返还执行者积分，再按规则处罚发布者，所有动作进入积分流水。

### 视觉评审范围

视觉稿包含五个画面：互帮互助首页、短链小程序任务详情、普通任务提交、发布任务、我的工具积分页。首页使用“积分优先 / 赏金最高”切换，继承现有参考图的卡片流和橙色强调，但降低红色营销感。

### 下一步建议

1. 用户先确认五屏视觉方向和“统一互助积分”的文案。
2. 确认后补充对应的接口契约、状态机和 QA 测试清单。
3. 再按最小范围实现小程序页面和后端积分流水，不触碰现有活码、企业微信归档和生产部署链路。

### 未完成与真实风险

- SVG 是可评审的静态方案，不是微信小程序真机截图；rpx、状态栏、安全区、字体和跳转回程仍需后续在开发者工具/真机验收。
- `shortLink` 跳转是否在目标小程序、微信版本和真实账号权限下稳定，需要后续真实小程序验收；当前没有实现。
- 三次拒绝的自动返还/处罚需要在开发时用冻结积分、幂等流水和申诉状态保护，不能直接让前端改余额。

## 2026-08-31 最新交接：互帮互助视觉稿改为模块内顶部 Tab

### 本轮状态

- 根据用户反馈，任务卡片的 `+4 分` 已放到右上角；发布者积分仍单独显示。
- 互帮互助不再使用底部 TabBar，五屏稿统一改为顶部 `首页 / 发布 / 任务 / 我的` Tab；该导航只属于“我的工具”内部模块。
- 架构文档已补充第一性原理优化项：以任务闭环为核心，第一期保留冻结、幂等、超时验收、拒绝理由、补交/申诉和明确终态等必要规则，不增加独立信誉分。

### 当前文件

- [架构文档](docs/stage2-docs/44-my-tools-mutual-help-architecture-v1.md)
- [PNG视觉稿](docs/stage2-docs/visuals/my-tools-mutual-help-v1.png)
- [SVG源稿](docs/stage2-docs/visuals/my-tools-mutual-help-v1.svg)

### 下一步建议

1. 先确认顶部 Tab 和卡片右上角奖励位置。
2. 若视觉方向通过，再补齐接口契约、任务状态机和 QA 清单。
3. 之后才进入最小范围的小程序/后端开发；不触碰现有活码、企业微信归档和生产部署链路。

## 2026-08-31 最新交接：普通任务增加可选任务次数

### 已调整

- 普通任务发布页新增“任务次数（可选）”；填写 3 或 5 表示最多成功验收的份数，留空表示次数不限。
- 达到次数后，任务显示“已完成/已下架”，首页和详情页不再允许点击完成，服务端也必须拒绝超额提交；成功次数按验收结算记录计算。
- 按用户意见，第一期发布任务不预冻结积分；结算时检查发布者余额，余额不足以支付下一笔时任务暂停/下架，余额不允许为负数。
- 第 4 屏效果图已改为普通任务发布状态，展示任务内容、交付标准、任务次数和奖励积分。

### 当前文件

- [架构文档](docs/stage2-docs/44-my-tools-mutual-help-architecture-v1.md)
- [PNG视觉稿](docs/stage2-docs/visuals/my-tools-mutual-help-v1.png)
- [SVG源稿](docs/stage2-docs/visuals/my-tools-mutual-help-v1.svg)

### 待确认

- 任务次数为空时按“次数不限”处理；发布者仍可主动暂停或结束任务。
- 任务次数达到上限后的页面状态和服务端拒绝码，开发阶段需补进 QA 清单。

## 2026-08-31 最新交接：补充任务类型与验收标准

### 已确认

- 普通任务发布时必须填写独立的“验收标准”多行字段；它说明提交什么、什么条件算通过、拒绝后如何补交。
- 标题不能代替任务类型。发布页顶部切换直接作为必填 `taskKind`；普通任务再选择 `taskCategory`，不增加重复的“任务类型”输入框。
- 小程序任务的验收标准是有效打开目标、满足停留条件并返回后提交完成；反馈文字和图片可为空。
- 普通任务的“任务次数（可选）”仍表示成功验收上限；达到上限后禁止继续完成。

### 视觉稿

- 第 4 屏现在是普通任务发布态，展示“分类：文本校对”“任务内容”“验收标准”“任务次数（可选）3 次”和奖励积分。
- 顶部仍是模块内 `首页 / 发布 / 任务 / 我的`，没有底部 TabBar。

### 下一步

如果该字段设计确认，开发阶段优先实现 `taskKind`、`taskCategory`、`acceptanceCriteria` 三个字段及发布/详情/提交闭环，再接积分结算；不先扩展复杂的规则编辑器。

## 2026-08-31 最新交接：任务内容支持图片与我的工具分包规划

### 已确认

- 普通任务的任务内容、验收标准均可发送文字、图片或图文混合；允许只上传图片，但每个字段至少要有一个有效内容块。
- 内容按有序 `text/image` 内容块存储，图片复用现有媒体资产哈希去重和受控访问链路；第一期不引入复杂富文本编辑器。
- 当前小程序已经有 `subpackages/workbench`。后续“我的工具”使用普通分包，互帮互助与未来微信群原则上按业务域拆分，不使用独立分包。

### 当前不做

- 本轮不修改 `app.json`，不迁移现有页面，不上传微信体验版。
- 不在小程序启动时预下载全部“我的工具”；开发时根据真实包体积和冷启动数据决定是否增加预下载。

### 下一步

开发互帮互助时先定义任务内容块、验收标准内容块和图片上传接口，再按普通分包实现页面；完成后用微信开发者工具检查主包/分包大小、依赖边界和真机首屏加载。

## 2026-08-31 最新交接：我的工具与互帮互助页面已实现

### 本轮完成

- “我的”页现在可以进入 `subpackages/my-tools/index/index`；工具入口页已预留互帮互助和未来微信群工具。
- 互帮互助页面位于 `subpackages/my-tools-mutual-help`，包含首页、任务详情/提交、发布、我的四个页面，顶部使用模块内 Tab，不新增底部 TabBar。
- 普通任务发布支持任务内容和验收标准的文字、图片或图文混合；任务次数可选，空白表示不限。
- 页面暂使用本地演示适配器，后端接口、真实媒体资产关联和积分流水尚未接入。

### 已验证

- 新增页面路由文件齐全；JavaScript、JSON、WXML 结构和 `git diff --check` 通过。
- 本轮未上传体验版、未修改生产环境；现有工作区未提交修改均保留。

### 下一步

1. 用户使用微信开发者工具编译预览，确认顶部 Tab、分包加载和表单布局。
2. 用户确认页面方向后，定义互帮互助后端任务/提交/积分流水 API 和媒体内容块接口。
3. 再将本地演示适配器替换为真实接口，并补充并发结算、任务次数和真机小程序跳转验收。

## 2026-08-31 最新交接：互帮互助页面第二轮调整已完成

### 本轮完成

- 首页自定义导航不再显示重叠的“互帮互助 / 我的工具”；发布、任务详情、我的页面也移除了右侧并列标题。
- 本地演示积分改用 v2 命名空间，首次进入显示 100 分；这只处理开发演示缓存，不是生产积分迁移。
- 四个模块页面均支持顶部浅色 / 深色切换，主题在页面间共享；表单输入框、提示区、任务卡、详情卡和自定义返回按钮都有深色样式。
- 任务大厅增加 1x / 2x 布局选择，默认 1x；普通任务分类扩展并在任务卡片上显示实际分类。

### 已验证

- 相关 JavaScript 通过 `node --check`；JSON、WXML 标签栈、路由、主题和布局静态检查通过；`git diff --check` 通过。
- 本轮未部署生产、未修改后端数据库、未上传微信体验版。

### 下一步人工验收

1. 在微信开发者工具编译，确认首页顶栏不再叠加文字，规则按钮宽度紧凑且积分显示 100。
2. 在首页分别点击浅色 / 深色，切换到发布、任务详情、我的，确认主题保持一致，输入框与提示文字清晰分层。
3. 在任务大厅分别点击 1x / 2x，确认 2x 双列不截断标题、收益、剩余次数和“去完成”；再检查分类选择中包含“下载 App”等新选项。
4. 继续按原交接流程验证真实后端任务、积分流水、媒体资产和微信真机跳转；当前页面仍不能当作生产结算链路。

## 2026-08-31 最新交接：发布表单图文顺序与积分显示修正

### 本轮完成

- 发布普通任务时，任务内容和验收标准均改为有序内容段：可添加“文字段”或“图片段”，支持文字、图片、文字的混排；不强制固定步骤，流程任务可自行填写“步骤1/步骤2”。
- 添加图片按钮已改为紧凑横向按钮；任务详情提交材料区域也同步收窄。
- 首页积分卡重新排版，规则按钮缩小；演示积分通过一次性种子版本将旧的 0 分缓存初始化为 100 分。

### 已验证

- 相关 JS、WXML 结构、内容段顺序和演示积分初始化检查通过。
- 未部署生产、未修改后端、未触碰活码链路、未上传微信体验版。

### 下一步人工验收

1. 重新编译小程序，确认首页当前积分显示 100 分，积分卡主信息、排序提示和规则按钮排布正常。
2. 发布普通任务时依次添加文字段、图片段、文字段，确认详情页仍按该顺序展示；删除任一段后再次检查顺序。
3. 检查深色模式下内容段、输入框、图片按钮和删除操作的对比度。
4. 真实上线前接入媒体资产接口、任务接口和积分流水；当前仍是本地演示，不代表跨用户任务或真实结算已完成。

## 2026-08-31 最新交接：任务图片原比例与预览保存修正

### 本轮完成

- 发布页图片段缩略图改为等比完整显示；任务详情页内容图和验收图按原始比例展开，不再固定裁切或压缩高度。
- 点击任务详情图片进入微信原生图片预览，支持放大和左右滑动浏览当前任务全部内容图片。
- 每张详情图片下增加“保存到相册”；远程图片先下载到本地，再调用相册保存 API，并处理权限引导。
- 提交材料图片缩略图同步改为等比显示。

### 已验证

- JavaScript 语法、WXML 结构、预览图库组装和远程图片保存流程模拟检查通过。
- 本轮未部署生产、未修改后端、未触碰活码链路、未上传微信体验版。

### 下一步人工验收

1. 用真实长截图和二维码图片检查发布页缩略图、详情页完整比例和页面滚动。
2. 点击多张内容图，确认预览页可放大、左右滑动且包含任务说明和验收图。
3. 分别测试本地临时图片、HTTPS 图片、相册授权允许/拒绝和保存失败提示。
4. 真实上线前接入媒体资产 URL、下载域名白名单和后端图片权限控制。

## 2026-08-31 最新交接：小程序任务卡片式跳转入口

### 本轮完成

- 任务详情页的小程序任务增加卡片式目标入口；点击卡片或底部按钮均调用同一 `wx.navigateToMiniProgram({ shortLink })` 逻辑。
- 卡片只负责让用户理解“点击进入目标小程序”，不嵌套目标页面；返回当前任务页后仍需提交完成。
- 明确当前项目不能伪造目标小程序的微信原生分享卡片；若分享任务，后续应分享本项目的任务详情页。

### 已验证

- 相关 JavaScript、WXML 结构和卡片入口静态检查通过。
- 未部署生产、未修改后端、未触碰活码链路、未上传微信体验版。

### 下一步人工验收

1. 使用真实有效的微信小程序短链接，在真机点击卡片，确认能进入目标小程序。
2. 返回资料整理助手，确认页面保留当前任务并允许提交完成，不在仅点击卡片时结算积分。
3. 分别测试目标链接失效、任务已结束和用户取消跳转时的提示。
## 2026-08-31 最新交接：互帮互助任务分享入口已接前端样板

### 本轮完成

- 积分卡“规则”按钮固定在右上角，字号调整为 24rpx，按钮尺寸固定为 96rpx × 56rpx，浅色/深色模式均有对应样式。
- 发布任务成功后直接跳转 `/subpackages/my-tools-mutual-help/task-detail/index?id=<taskId>`。
- 任务详情增加“分享给好友”按钮和 `onShareAppMessage`，分享标题带当前任务标题，路径精确指向当前任务详情。
- 目标小程序入口继续使用详情里的本项目卡片；点击卡片后才调用 `wx.navigateToMiniProgram({ shortLink })`。
- 带 ID 的未知任务不再静默回退到第一条演示任务，避免分享深链串任务。

### 当前边界

- 互帮互助当前还是本地演示适配器，任务、提交和积分在本地缓存；另一台手机打开分享路径时还不能从服务端找到发布者任务。
- 当前任务分享没有独立 JPG/PNG 分享快照，正式上线前需要接入后端任务查询、分享 token、媒体资产和任务分享图，再做最终微信聊天卡验收。
- 本轮未部署生产，未上传微信体验版；不涉及活码 200 HTML 链路，也未改变 AScript 人数检测关闭状态。

### 下一步建议

1. 先为 `MutualTask` 建立服务端发布/详情/分享查询接口，好友打开分享路径时按 taskId 或可撤销 share token 读取公开任务。
2. 再把任务内容/验收图片接入现有媒体资产登记和权限链路，并生成固定比例任务分享图。
3. 最后在真机验收“发布 → 详情 → 分享给好友 → 好友打开详情 → 点击目标小程序 → 返回 → 提交”整条链路；通过后再接积分流水和跨用户验收结算。
## 2026-08-31 最新交接：互帮互助任务详情角色拆分视觉稿完成

### 本轮结论

- 用户提供的微信截图不是“没有分享成功”，而是当前 `onShareAppMessage` 未返回任务专用 `imageUrl`，微信显示了任务详情页默认截图。
- 已完成两种角色页面的结构设计：
  - 执行者任务详情：奖励/剩余次数 → 任务说明 → 验收标准 → 目标小程序/提交 → 完成。
  - 发布者任务管理：任务状态 → 已完成/待验收/剩余 → 待验收提交 → 通过结算/补交 → 分享/暂停。
- 分享链接默认指向执行者任务详情；发布者只能从“我的”进入管理页。

### 设计稿

- [角色页面设计方案](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/docs/stage2-docs/45-mutual-help-task-role-pages-v2.md)
- [角色页面视觉稿](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/docs/stage2-docs/visuals/my-tools-mutual-help-role-pages-v2.svg)

### 当前不变项

- 本轮没有修改小程序业务代码，没有部署生产，没有上传体验版。
- 目标小程序仍通过详情页内的本项目卡片调用 `wx.navigateToMiniProgram({ shortLink })`；不伪造目标小程序原生分享卡。
- 活码继续保持固定外层二维码 → 200 HTML → 内层二维码图片；人数检测继续关闭。

### 后续开发顺序

1. 服务端建立 `MutualTask` 详情/分享查询和角色权限。
2. 新增执行者详情路由与发布者管理路由，复用内容块和验收标准展示组件。
3. 接入任务分享 JPG/PNG 快照，`onShareAppMessage` 仅在快照 ready 时开放。
4. 再开发提交、验收、补交/申诉和积分流水，并用真机验收跨设备分享链路。

## 2026-08-31 最新交接：任务管理与执行者页面已按角色落地

### 本轮完成

- 顶部“任务”Tab 已改为发布者任务管理列表；“我的”页的发布任务入口改为进入管理列表，发布成功后直接进入当前任务管理详情。
- 新增发布者管理列表和管理详情页面：展示任务状态、发布积分、已完成/待验收/剩余进度、任务内容和验收标准，支持分享任务、查看执行者只读预览、暂停/恢复发布。
- 执行者任务详情移除模块内四个 Tab；发布者访问执行者详情时隐藏提交操作并禁止目标跳转，避免角色混用。
- 任务大厅过滤当前用户已提交、已结束和自己发布的任务；任务仍保存在本地任务集合中，不做全局删除。
- 公共样式主要卡片、表单字段和内容区分隔线调整为 `2rpx`，新增管理页样式并覆盖深色模式。

### 验证与边界

- 已通过相关 JavaScript `node --check`、JSON 解析、6 个互帮互助 WXML 标签结构检查、路由/角色/发布后跳转/大厅过滤/分隔线静态检查和 `git diff --check`。
- 本轮未部署生产、未修改后端和活码链路、未上传微信体验版；工作区既有未提交修改均保留。
- 当前仍是本地演示适配器。任务专用分享图 `imageUrl`、跨设备任务查询、执行者提交回传、发布者验收/补交、积分流水和并发次数控制仍未接入。

### 下一步建议

1. 先接后端 `MutualTask` 公开详情/分享 token 和媒体内容块接口，解决好友设备打开任务的问题，并生成固定比例任务分享图。
2. 再接执行者提交、发布者验收/补交、任务次数原子扣减和统一积分流水；客户端只展示服务端状态。
3. 最后用微信真机验收“发布 → 管理 → 分享 → 好友执行详情 → 目标小程序 → 返回提交”的完整链路。

## 2026-08-31 最新交接：互帮互助任务专用分享图已接入

### 本轮完成

- 修复任务 `onShareAppMessage` 缺失任务专用 `imageUrl` 的问题；新增共享任务分享源和快照准备逻辑。
- 发布者管理详情、执行者详情均使用隐藏 `shareCardCanvas` 生成 `750×600` JPG，复用统一分享图插件上传并在本地任务保存版本指纹。
- 分享按钮在专用图 ready 前禁用；回调只在 `shareCardReady + https imageUrl + 执行者详情 path` 同时满足时返回分享对象，失败时返回 `null`，不会退回微信默认详情截图。

### 验证与边界

- 已通过相关 JavaScript 语法检查、任务分享源/回调 mock、WXML 结构检查和 `git diff --check`。
- 当前未部署生产、未上传体验版、未做微信聊天真实转发；互帮互助任务仍是本地演示数据，跨设备任务详情、媒体资产权限、提交验收和积分流水仍待后端阶段。

### 下一步建议

1. 建立服务端 `MutualTask` 发布、公开详情和可撤销分享 token，并保存任务专用分享快照。
2. 用真机验收“发布 → 生成图片 → 微信转发 → 好友打开任务详情 → 点击目标小程序 → 返回提交”。
3. 再接提交、验收、任务次数原子扣减和统一积分流水；生产部署前不改变活码 200 HTML 和人数检测关闭配置。
# 2026-08-31 最新交接：任务列表外部分享与删除已完成（本地）

## 本轮完成

- 任务管理 Tab 的任务卡已按任务分享卡重排：类型、奖励角标、品牌标识、图片/小程序图标、标题、摘要、任务次数和验收进度保持同一信息层级。
- 分享按钮已移到每张任务卡外部；详情页和发布者管理详情页不再放日常分享按钮。
- 任务管理列表异步预热任务专用 5:4 分享图；分享按钮按任务 ID返回对应的详情路径和 `imageUrl`，未准备完成时禁用。
- 增加“删除任务”：当前为软删除，任务标记为 `deleted` 后从任务大厅和任务管理列表隐藏，已有记录不改写。
- 目标小程序允许所有用户打开；发布者预览和管理页卡片均可调用原有短链接跳转，发布者仍不能提交自己的任务。

## 已验证

- 互帮互助相关 JavaScript 与分享插件 `node --check` 通过。
- 分享回调模拟通过：多任务按按钮 ID选择、有效 `imageUrl`和任务详情 `path`绑定；缺少快照时阻断；详情页已无分享按钮。
- 删除空数组持久化、WXML 结构和 `git diff --check` 通过。

## 当前边界与人工验收

- 本地任务仍使用微信本地缓存；好友在另一台设备打开分享路径还不能从服务端查询发布者任务。
- 未部署生产、未上传微信体验版。
- 需要在微信真机验收：任务管理列表生成分享图 → 点击卡片外部“分享任务” → 聊天卡显示对应任务图 → 好友打开任务详情 → 点击目标小程序 → 返回；同时验证发布者预览也可以打开目标小程序。

## 2026-08-31 最新交接：任务图片与提交结算规则补齐

### 本轮完成

- 任务管理列表优先展示任务专用 5:4 分享图，不再把原始长截图压缩到矮框；详情页原图仍使用原比例展示，并支持点击放大滑动和保存到相册。
- 首页已移除重复的“我的工具”眉标题，并明确提示大厅只展示其他用户可完成的任务；自己发布的任务统一在“任务”Tab管理。
- 小程序任务在执行者打开目标并明确点击“提交完成”后即时结算；普通任务进入发布者管理详情待验收列表，发布者通过后结算，5×24 小时未处理自动通过。
- 本地演示提交存储已改为任务级可聚合结构，避免发布者页面只有静态待验收数字；结算包含发布者扣分、执行者加分、次数减少和幂等保护。

### 已验证

- 相关 JavaScript `node --check`、JSON 解析、普通人工验收/小程序即时结算/五天自动通过 mock、WXML 结构和 `git diff --check` 通过。
- 未部署生产、未上传微信体验版，未修改活码 200 HTML、AScript 人数检测关闭配置或生产数据。

### 当前真实边界

- 任务、提交、积分仍是本地演示数据；好友在另一台设备打开分享链接、跨用户任务大厅查询、服务端任务次数原子扣减、媒体资产登记和真实积分流水仍未接入。
- 5 天自动通过当前由页面进入时触发同步；正式版应由后端定时任务/队列按截止时间执行，不能依赖用户打开页面。

### 下一步建议

1. 建立 `MutualTask`、`MutualTaskSubmission` 和统一积分流水服务端模型，把发布、提交、验收、自动通过和次数扣减放到事务中。
2. 接入任务内容/提交图片的媒体资产登记和受控 URL，并用微信真机确认长图、放大、保存和分享卡效果。
3. 用两台真实微信设备验收“发布者发布 → 生成任务卡 → 分享 → 执行者打开 → 目标小程序 → 返回提交 → 即时/验收结算”。

## 2026-08-31 接手续作：羊毛任务与评论第一期本地实现

### 当前完成

- miniprogram/subpackages/my-tools-mutual-help/wool.js 提供羊毛策略、一次性解锁、积分流水演示、评论/举报、打赏和退款申请状态。
- shared.js 将羊毛接入任务装饰、发布任务和结算逻辑；羊毛固定奖励为 0 时不产生错误扣分，固定奖励大于 0 时按显式奖励结算。
- 发布页可以创建羊毛任务；执行者详情在未解锁时只显示任务标题、评论和解锁卡，解锁后显示完整内容、验收标准和入口。
- 普通任务与羊毛任务都有评论区；发布者管理详情显示评论及待处理举报/退款数量，后续 PC 端可接同一组业务状态。
- 首页新增羊毛筛选，自己发布的未删除任务保留在任务大厅；任务分享源能生成羊毛专用摘要事实。

### 已验证

- 互帮互助 JavaScript node --check、小程序 JSON 解析、WXML 标签结构、git diff --check 通过。
- mock 验证一次性解锁、重复解锁不重复扣分、评论摘要、重复举报、打赏和退款申请去重。

### 仍未完成

- 以上数据仍是单设备 wx 本地演示，尚未接入 MutualTask、评论、解锁订单、退款审核和统一积分流水后端。
- PC 端尚未提供举报/退款审核操作；当前仅在发布者管理详情展示待处理数量。
- 未执行微信开发者工具真实编译、两台设备好友分享、真实专用图片上传、目标小程序跳转或生产部署。

### 下一步建议

1. 先建立服务端公共任务引擎和积分流水：taskKind、woolPolicy、解锁幂等、任务提交、结算与退款审核统一用事务处理。
2. 增加服务端评论/举报/退款接口和 PC 审核列表，前端再移除本地演示适配器。
3. 接入媒体资产登记与任务分享快照后，用两台微信设备验收付费解锁、评论可见性、分享详情和真实目标入口。

## 2026-08-31 最新交接：羊毛任务改为非验收型流程（本地）

### 本轮完成

- 羊毛发布不再展示或要求“验收标准”，也不再设置固定完成奖励；仍支持免费查看/付积分解锁、评论、举报、失效退款申请和自愿打赏。
- 羊毛执行详情不再出现提交材料、提交任务、发布者验收或 5 天自动通过；解锁后直接查看羊毛说明和使用入口。
- 普通任务仍要求任务说明和验收标准，继续由发布者验收或 5 天未处理自动通过；小程序任务仍由执行者打开入口后明确提交完成并即时结算。
- 发布页增加类型专属的新手说明、标题示例、入口提示、内容提示和分类；普通任务分类移除“下载 App”等容易误解的选项，羊毛使用福利类分类。
- 任务管理页对羊毛显示已解锁数量和评论数量，不显示待验收区；任务分享摘要不再展示固定完成奖励。
- 羊毛可选次数按查看/解锁名额处理；本地首次免费查看或首次付费解锁会消耗一次，重复进入/解锁保持幂等。

### 已验证

- 互帮互助相关 JavaScript node --check 通过；互帮互助目录 JSON 解析通过；git diff --check 通过。
- 本地 mock 验证羊毛策略忽略旧固定奖励字段、羊毛空验收内容可发布、普通任务仍要求验收内容。

### 当前真实边界

- 互帮互助仍是本地演示适配器；任务、解锁、评论、退款、打赏和积分流水尚未接入服务端，跨设备分享详情仍未打通。
- 未执行微信开发者工具真实编译、好友转发、目标小程序跳转或生产部署；未修改活码 200 HTML 链路和 AScript 人数检测关闭配置。

### 下一步建议

1. 先在后端公共任务引擎中固定三种流程：小程序即时完成、普通任务验收、羊毛解锁/使用。
2. 接入羊毛解锁幂等、评论/举报、退款和打赏的服务端事务与 PC 审核。
3. 用两台微信设备验收发布羊毛、付费解锁、评论可见、打开小程序/网页入口和任务分享详情。

## 2026-09-01 最新交接：互助积分充值与 PC 工作台

### 本轮已完成

- 后端新增互助积分账户、积分流水、充值订单、活动统计事件及 PostgreSQL 初始化表。
- 新用户首次建立账户自动得到 100 分；充值支持固定积分套餐、微信 JSAPI 预支付和支付回调幂等入账。
- 小程序增加充值页；充值默认开启且可见，提现默认关闭且隐藏，未来提现保留 300 分的规则已固化为展示文案和服务端配置默认值。
- PC 业务工作台新增“互助积分”子 Tab，展示今日/近 7 日/总量的发布任务、完成任务、充值笔数，并可由现有单管理员认证控制充值/提现的启用与显示状态。
- 未新增管理员 token；未上传微信体验版；活码 200 HTML、内层二维码访问和 AScript 人数检测关闭配置没有改动。

### 已验证

- `./.venv312/bin/pytest -q backend/tests`：317 passed。
- 互助积分专项测试：2 passed；Python 编译、小程序 JS 语法、JSON 解析和 `git diff --check` 通过。

### 真实边界与部署后验收

- 互助任务、评论、羊毛解锁、打赏和发布者/执行者真实结算仍是本地演示引擎；本轮上线的服务端活动统计不是完整任务积分账本。
- 生产部署时必须保留现有 backend `.env`、secrets、媒体目录和数据库卷；不能在生产环境创建测试充值或调用 test-confirm。
- 上线后需由用户在微信开发者工具手动上传体验版，再用真实微信支付配置和两台设备验收充值、任务分享、目标小程序跳转及任务结算；本轮 Codex 不上传体验版。

## 2026-09-01 最新交接：互助积分充值与 PC 工作台已部署生产

### 生产状态

- 生产已部署互助积分服务端账户、充值订单、微信支付回调、活动统计和 PC 业务工作台“互助积分”子 Tab。
- 当前生产策略：充值开启且可见；提现关闭且隐藏；新账户首次建立时 100 分；未来提现保留 300 分以上部分的规则已写入配置与页面说明。
- 生产镜像：`teambuy-backend:20260901-015500-mutual-recharge-api2`、`teambuy-backend-worker:20260901-015500-mutual-recharge`、`teambuy-archive-worker:20260901-015500-mutual-recharge`；Compose 使用的服务标签已同步更新。
- 生产备份：`/home/ubuntu/teambuy-backups/20260901-015500-mutual-recharge/`。旧镜像保留 `rollback-20260901-015500` 标签。
- API、两个 Worker、PostgreSQL 均 healthy；生产端口 8004 -> 8000；本机与公网 `/health`、新管理接口、四张互助表和 PC Tab 静态内容均已验证。
- 活码回归：固定入口仍为 200 HTML、无 `Location`，内层二维码图片可访问；`LIVE_QR_MEMBER_COUNT_AUTOMATION_ENABLED` 未被本轮改动。

### 当前边界

- 小程序充值页面代码已在本地工作区，但按照项目规则未上传微信体验版；用户需自行在微信开发者工具上传，才能在真实小程序中验收入口和微信支付。
- 互助任务、评论、羊毛解锁、打赏以及发布者/执行者正式结算仍是本地演示任务引擎；本轮服务端发布/完成事件只用于运营统计，不能当成完整任务积分账本。
- 生产未创建测试充值订单；真实支付验收必须使用真实微信支付配置和测试金额，并由用户人工确认结果。

## 2026-09-01 最新交接：积分核心已可扩展化，尚未部署

### 本轮完成

- 已先建立 Git 安全检查点：`93688f6 chore: checkpoint before extensible points core`，该提交完整保留了此前工作区的 119 个文件改动；没有清理既有未提交工作，也没有修改生产。
- 新增 `PointsCoreService` 作为统一积分账本入口，支持：按 `accountType` 建立账户、首次赠送、发放、消耗、余额不足拒绝、流水查询、跨用户双边转移和幂等重试。
- 现有互助充值入账已通过积分核心完成，仍兼容既有互助积分表、充值接口和生产数据；状态接口返回最近流水，新增积分明细查询接口。
- 设计上后续“我的工具”功能默认复用 `accountType=mutual_help`，不得再建立第二套积分余额；资源工具旧钱包尚未迁移，避免未经核对混合历史账务。

### 验证结果

- 定向后端测试：4 passed；Python compileall 通过；`git diff --check` 通过。
- 已确认当前代码没有部署生产，没有创建测试充值订单，没有调用生产 test-confirm，没有上传微信体验版。

### 下一步

1. 在服务端公共任务引擎中使用 `grant/consume/transfer` 完成任务发布、羊毛解锁、提交验收、退款和打赏的正式落账，并为每个动作定义稳定幂等键。
2. 增加 PostgreSQL 行锁/事务级并发测试，再把小程序互帮互助从本地适配器切换到服务端 API。
3. 完成两台微信设备验收后，再单独讨论生产部署；部署仍必须保留生产 `.env`、secrets、媒体目录和数据库卷。

### 真实风险

- 当前积分核心虽然已提供双边转移接口，但 AppService 仍以进程锁和仓储保存为主，不能直接宣称已完成多副本并发安全。
- 互帮互助任务、评论、羊毛、打赏及正式结算仍未迁移到服务端；生产已有充值积分，后续任务结算必须先完成事务、幂等和跨用户权限校验。

## 2026-09-01 最新交接：我的工具入口改为宫格直达

### 本轮完成

- “我的”页的“我的工具”已改为三列自适应宫格，当前只展示真实工具项，不强行补满九宫格。
- “互帮互助”点击后直接进入 `/subpackages/my-tools-mutual-help/index/index`；“微信群工具”仍为禁用状态并提示即将上线。
- 旧 `/subpackages/my-tools/index/index` 页面未删除，保留兼容性，但已从个人页标准路径移除。

### 验证与部署状态

- 已完成模板、事件处理函数和样式引用的静态核对；个人页 JavaScript `node --check`、79 个小程序 JSON 解析、旧样式引用检查和 `git diff --check` 均通过。
- 本轮没有后端变更，因此没有服务器部署动作；服务器现有生产版本不受影响。
- 小程序生效仍需用户在微信开发者工具中重新编译、真机预览并手动上传体验版，Codex 不自动上传。

### 后续人工验收

1. 打开“我的”页，确认工具入口以三列宫格显示，两个真实项没有被拉伸成整行大卡。
2. 点击“互帮互助”，确认直接进入互帮互助首页；点击“微信群工具”，确认仅提示“即将上线”。
3. 在微信开发者工具清缓存并重新编译后，再决定是否上传体验版；不需要为本轮前端入口改动部署后端。

## 2026-09-01 最新交接：隐藏已上线状态，订阅消息待补任务模板

### 本轮完成

- “我的”页互帮互助宫格已隐藏“已上线”；旧兼容“我的工具”页面也不再给可用工具显示该状态。
- 已核对生产：积分/充值后端已部署，互帮互助完整任务后端尚未部署；当前生产镜像仍为 `20260901-015500-mutual-recharge` 系列。
- 已核对生产已有客户提醒订阅消息链路，但它不是互帮互助任务通知链路。

### 订阅消息建议

- 不要覆盖现有模板。请在微信公众平台的小程序“订阅消息”中另选/申请任务进度或处理结果类模板。
- 拿到新模板后，需要提供模板 ID、模板详情中的字段键/类型和希望跳转的页面；再实现独立配置、前端授权、服务端任务事件发送和幂等重试。
- 首期范围：发布者收到新提交提醒，执行者收到审核结果提醒；任务过期、评论和打赏提醒暂不纳入第一版。

### 当前部署状态

- 本轮只改小程序显示文案和项目文档，没有服务器部署动作；服务器现有生产版本未受影响。
- 小程序改动需用户在微信开发者工具中重新编译、真机预览并手动上传体验版。

## 2026-09-01 最新交接：互帮互助任务进度模板已接入本地配置

### 本轮完成

- 已配置用户提供的模板“任务进度更新通知”，字段为 `thing1` 任务名称、`phrase2` 任务进度、`time3` 更新时间、`thing4` 执行人、`thing5` 发起人。
- 新模板使用独立环境变量 `WECHAT_MINIAPP_MUTUAL_HELP_SUBSCRIBE_TEMPLATE_ID` 和字段映射，不覆盖原客户查看/留言提醒模板。
- 后端配置接口返回 `mutualHelp`，授权记录接口支持 `purpose=mutual_help_task`；小程序订阅服务支持选择互助模板并隔离并发请求。

### 当前边界

- 互帮互助任务发布、提交、验收和正式积分结算仍未迁移到服务端，因此本轮没有在本地演示按钮中弹订阅授权，也没有接入任务通知发送。
- 模板 ID 已写入本地忽略的 `backend/.env`；生产环境配置未修改，不能宣称订阅消息已在线生效。
- 本轮没有服务器部署、没有生产测试订单、没有调用 `test-confirm`、没有上传微信体验版。

### 验证结果

- 订阅消息专项测试 11 passed；Python 编译、小程序 JavaScript 语法、79 个 JSON 解析和 `git diff --check` 通过。

## 2026-09-01 最新交接：微信群工具三 Tab 已完成本地服务端化重构

### 本轮完成

- 群工具主页面现在是三个同级 Tab：`找群 / 我的发布 / 我的活码`，标准入口从“我的”页直接进入，不再经过重复的中转说明页。
- `找群`通过服务端公共资源查询展示当前有效群，支持城市、行业、用途和关键词筛选；卡片不返回二维码，点击后由服务端扣除 30 个互助积分并返回二维码，同一用户同一资源重复查看幂等不重复扣分。
- `我的发布`通过服务端按用户查询，保留过期资源用于“上传新码”；发布时保存城市、行业、用途等筛选字段，每天最多新建 1 个，累计发布奖励最多 100 分且先处于待确认状态。
- `我的活码`已经不是占位页：上传可识别群二维码后自动生成名称和固定入口；下载活码、更新二维码、删除和所有权校验均已接入。更新保留固定 `code`，刷新当前二维码版本和 7 天有效期。
- 新增 `group_resources` JSON/PostgreSQL 持久化模型、服务端路由、媒体引用关系和小程序 API 封装；现有固定活码入口仍为 `200 HTML` 链路。

### 验证结果

- `./.venv312/bin/python -m pytest -q backend/tests`：323 passed。
- 目标后端文件 `py_compile`、小程序全量 JavaScript `node --check`、JSON 解析、`git diff --check` 已通过；新增测试覆盖公共/私有列表隔离、积分扣除幂等、每日限制、活码固定入口和所有权。

### 当前生产/本地状态

- 本轮只修改本地工作区，没有部署服务器，没有执行生产数据库初始化或迁移，没有修改生产环境变量，没有创建生产充值测试订单，没有调用 `test-confirm`，没有上传微信体验版。
- 当前工作区仍有用户此前的既有未提交修改；未执行 reset、checkout、删除或批量清理。生产仍保持上一版互助积分/充值镜像。

### 还需要人工确认与真实风险

1. 用户在微信开发者工具清缓存、重新编译并真机验证三个 Tab；用两个账号分别发布/找群，确认公共列表不显示他人二维码，查看一次扣 30 分、再次查看不重复扣分。
2. 上传真实微信群二维码验证服务端识别、固定入口下载、更新后旧入口仍可打开；验证过期前提醒目前不会自动发送，因为定时订阅发送事件尚未接入。
3. 部署前按腾讯云文档备份并初始化 `group_resources` 表，保留生产 `.env`、secrets、媒体目录和数据库卷；部署后再做公网健康、表结构和跨用户 API 验证。
4. 发布奖励审核/释放、二维码重复资源识别、PostgreSQL 多副本并发锁仍未完成；当前实现适合本地和体验版验收，不应直接宣称已经完成正式生产积分结算。

## 2026-09-01 最新交接：群工具页面字段收口与活码 NoFund 兼容处理

### 本轮变更

- 群资源库主页面移除“微信群工具”眉标题，避免小屏换行变形；“我的活码”仍保留“免费工具”眉标题。
- 发布表单、群资源卡片和发布成功页移除“人数区间”“活跃度”；后端旧字段保留兼容，但小程序新流程不再提交或显示。
- 图片选择器在 `chooseMedia` 返回 `nofund/not found/not support` 时自动尝试 `chooseImage`；活码上传/更新接口返回 404 或同类错误时提示“活码服务尚未部署，请先部署服务器后再试”。

### 关键判断

- 当前 `miniprogram/app.js` 的 API 地址是生产域名，而新增用户活码接口尚未部署生产，所以用户真机看到的 `NoFund`/`Not Found` 很可能与线上接口未同步有关；选择器降级用于覆盖另一种可能，不能替代部署。
- 这轮没有部署服务器、没有修改生产配置、没有上传体验版。部署时仍需先备份 Compose/源码，初始化新表，保留生产 `.env`、secrets、媒体目录和数据库卷。

### 验证

- 后端完整测试：`./.venv312/bin/python -m pytest -q backend/tests`，323 passed；小程序全量 JS 语法检查、JSON 解析、Python 编译和 `git diff --check` 均通过。
- 仍需真机确认标题不换行、发布表单字段已消失、选择图片和活码上传错误提示可理解。

## 2026-09-01 最新交接：活码入口统一为免费工具并进入生产部署

### 本轮变更

- 活码 Tab、顶部导航和页面主标题统一显示“免费工具”。
- 移除活码页顶部重复的“免费工具”眉标题。
- 当前将部署此前本地新增的群资源服务端、用户活码接口及数据库 schema；不修改生产 `.env`、secrets、媒体目录或数据库卷。

### 部署后人工验收

1. 在微信开发者工具重新编译并手动上传体验版，确认 Tab 和导航显示“免费工具”。
2. 使用真实微信群二维码测试上传、二维码识别、固定入口访问、下载、更新和删除。
3. 确认新活码接口公网可访问，生产 API、backend-worker、archive-worker 和 PostgreSQL 保持 healthy。

### 本次生产结果

- 已部署群资源与用户活码后端热修复镜像；生产备份为 `/home/ubuntu/teambuy-backups/20260901-204149-group-tools-live-qr/`。
- 已确认 `group_resources`、`live_qr_codes` 表存在，OpenAPI 已注册 `/api/group-resources` 和 `/api/live-qr-codes` 路由，公网 `/health` 通过。
- 小程序前端未自动上传体验版；用户仍需手动重新编译、真机预览并上传，之后才能看到“免费工具”新名称并测试活码上传。

## 2026-09-01 最新交接：活码提交后消失的根因已定位，待修复

### 已确认

- 20:52 附近两次活码 POST 都是 200，图片 GET 和列表 GET 也是 200，没有删除请求；但生产数据库没有对应新记录，只剩旧活码。
- 代码路径显示 `get_mutual_help_status()` 在 GET 请求中执行全量 `repo.save(state)`；PostgreSQL 的全量保存会删除并重写所有业务表，旧快照可覆盖活码提交。这是本次数据消失的主因。
- 小程序 `loadLiveQrCodes()` 对任何列表请求错误都设置空数组，是会造成“视觉消失”的次要问题。

### 待处理顺序

1. 先把积分账户初始化/状态查询改成行级保存或事务更新，禁止读接口全量回写 `AppState`。
2. 再让活码列表请求失败时保留最近成功数据并提供重试提示。
3. 修复后用真实二维码做一次人工上传验收；本轮没有创建测试活码、没有改代码、没有重新部署。

## 2026-09-01 最新交接：21:05 再次复现并确认覆盖时间线

### 新增证据

- 用户 21:05 提交对应日志为 UTC 13:05:22：活码 POST 200，随后二维码图片 GET 200；21:05:25 互助积分状态 GET 200，紧接着活码列表 GET 200。
- 生产数据库复查没有该次提交的新活码行，仍只有 8 月 30 日旧记录；该时间段没有 DELETE 请求。
- 结合后端 `get_mutual_help_status()` 在 GET 中执行 `repo.save(state)`，当前结论为：活码提交成功后被旧 `AppState` 全量保存覆盖，随后列表读取空数据，造成“显示后消失”。

### 当前处理状态

- 本轮只完成日志、数据库和代码路径核对，没有改业务代码、没有新建测试活码、没有部署。
- 暂停继续真实二维码验收。修复顺序保持为：后端积分状态查询改行级/事务级保存 → 前端列表失败不清空已有数据 → 数据库持久化回归 → 再做真机上传/更新/下载验收。

## 2026-09-01 最新交接：免费工具“我的活码”子页面设计待确认

### 已确定的产品结构

- 首屏展示当前活码记录：左侧“微信群码”二维码，右侧“固定活码”二维码，中间用箭头表示绑定关系。
- 当前记录同时展示“剩余 2 天 / 有效期至 / 到期前提醒已开启”，主按钮为“更新二维码”，辅助动作是“保存活码”。
- 下方增加“历史记录”，默认只展示最近 3 次替换；历史行只展示旧版本二维码、替换时间和“固定入口不变”，不重复制造新的固定入口视觉。
- 到期订阅消息点击后直接进入对应记录，并让“更新二维码”成为首要动作。

### 当前状态

- 已生成一张移动端高保真效果图，本轮没有修改业务代码、没有修复活码覆盖竞态、没有部署。
- 该设计待用户确认后，才进入后端数据模型/接口和小程序页面实现；实现顺序仍需先修复全量快照覆盖问题，再恢复真实上传验收。

## 2026-09-01 最新交接：活码历史页面与覆盖竞态已修复，已部署后端

### 本轮变更

- 已实现“我的活码”当前/历史子页面：当前卡片展示微信群码、固定活码、7 天有效期、剩余天数、保存、更新、删除和到期提醒；历史记录默认显示最近版本，固定入口不变。
- 已修复互助积分状态查询的全量快照覆盖：首次积分账户和流水改为行级保存，状态读取不再保存整份状态；活码列表失败时前端保留上次成功结果。
- 已接入服务到期提醒模板 `N1kBsCXhW0w4DYMDnNjjrD8mYqx68yj8pRQBzSDY1Kg` 的服务端配置。服务端按活码版本在剩余两天内幂等排队一次，发送后页面仍提供“开启提醒”按钮，用于用户主动授权下一次提醒。

### 验证结果

- `./.venv312/bin/python -m pytest -q backend/tests`：326 passed。
- 小程序相关 JavaScript `node --check`、Python 编译、`git diff --check` 通过。
- 全仓测试仍有既有阻断：`platform/wecom-archive-core/tests/test_engine.py` 无法导入 `app.codec`；不属于本轮改动。

### 部署与人工验收

- 已按腾讯云部署文档备份生产 Compose、明确变更源码和 `.env`，并切换 API、backend-worker、archive-worker 镜像；备份目录为 `/home/ubuntu/teambuy-backups/20260901-215205-live-qr-history/`，回滚标签为 `rollback-20260901-215205-live-qr-history`。未创建生产测试活码、充值订单，未调用 `test-confirm`。
- 小程序前端不自动上传。用户需在微信开发者工具清缓存、重新编译并手动上传体验版，然后用真实二维码验证创建、固定入口、下载、更新、历史、过期提醒和再次授权。
- 真实风险：订阅消息只能在用户授权额度允许时发送；模板字段或微信后台配置不正确时提醒会跳过，但不能影响二维码更新。生产后端上线不等于小程序页面已经更新。

## 2026-09-01 最新交接：活码投放样式切换已部署后端

### 本轮变更

- 小程序“免费工具 / 我的活码”支持在“纯二维码”和“保留原图”之间切换；新建时可选择，已有记录可随时切换。
- 后端新增 `posterMode`、`posterImageUrl` 字段和用户样式切换接口。保留原图模式基于用户上传原图，仅替换可识别的二维码区域，原头像、群名称、文案与排版不重新绘制。
- 更新目标二维码时沿用固定入口和版本历史；若当前为保留原图模式，同步生成新投放图。删除时区分目标原图与派生投放图的媒体引用。
- 保存投放图到相册后弹出“要开启到期提醒吗？”，用户确认才请求订阅；卡片继续保留“开启提醒”小入口。

### 验证

- `./.venv312/bin/python -m pytest -q backend/tests/test_group_tools.py`：4 passed。
- 小程序 JavaScript 语法、Python 编译、`git diff --check` 已通过。
- 后端已部署生产，微信体验版未上传；线上后端已包含本轮投放样式代码，用户端页面仍需手动上传体验版。
- 生产备份：`/home/ubuntu/teambuy-backups/20260901-230846-live-qr-style/`；三类镜像回滚标签：`rollback-20260901-230846-live-qr-style`。
- 部署后 API、backend-worker、archive-worker、PostgreSQL 均 healthy；8004 映射、公网 `/health`、数据库健康检查和样式接口路由均已核对。

### 下一步

1. 在微信开发者工具重新编译，手动上传体验版。
2. 使用真实带群头像、群名称和排版的二维码，验证两种样式的生成图能保存、能扫码且固定入口不变。
3. 人工确认满意后继续做真实验收；不要用生产创建测试活码、创建测试充值订单、调用 `test-confirm` 或自动上传体验版。

## 2026-09-02 最新交接：我的活码改为多记录列表与同页详情

### 本轮本地变更

- `miniprogram/pages/group-resource-library/index.js` 增加 `liveQrView`、`liveQrDetailId`、`liveQrDetail` 状态；列表支持点击打开详情，新增后直接进入新记录详情，订阅消息的 `qrId` 可以深链定位。
- `miniprogram/pages/group-resource-library/index.wxml` 将多个完整活码卡片改为紧凑列表；详情页保留二维码对照、样式、提醒、有效期、保存、更新和删除；用户界面已删除“历史更新”。
- `miniprogram/pages/group-resource-library/index.wxss` 增加列表行、状态标签、详情返回和详情卡样式，继续使用 rpx 与 flex 居中。
- 后端接口、数据库和生产配置本轮没有变更；后端原有多条活码记录能力直接复用。

### 验证结果

- 小程序 JavaScript 语法、WXML 标签平衡、`git diff --check` 通过。
- 活码后端回归：`./.venv312/bin/python -m pytest backend/tests/test_group_tools.py backend/tests/test_live_qr.py -q`，12 passed。
- 系统默认 `.venv` 使用的旧 Python 不支持当前项目的 `dataclass(slots=True)`；已使用项目 `.venv312` 完成验证。

### 当前状态与人工验收

- 本轮未部署服务器，未创建生产测试活码，未上传微信体验版。生产后端继续使用上一轮 `20260901-230846-live-qr-style` 部署结果。
- 用户需在微信开发者工具清缓存并重新编译，手动上传体验版后验证：已有多条活码时列表是否紧凑、点击记录是否展开正确详情、返回列表、新增第二条活码、更新固定入口、删除记录，以及从订阅消息进入指定 `qrId`。
- 真实风险：本轮没有微信真机截图验收；如果开发者工具缓存旧 WXML/WXSS，仍可能看到之前的多张详情卡，需要先清缓存重新编译。

## 2026-09-02 部署核对结果

- 本轮没有重复部署后端：当前后端源码与线上容器哈希一致，线上继续使用活码投放样式部署版本。
- 线上 `teambuy-backend-1`、`teambuy-backend-worker-1`、`teambuy-archive-worker-1`、`teambuy-postgres-1` 均 healthy；`8004` 端口、公网 `/health` 和 `/health/db` 已核对通过。
- 多活码列表/详情属于小程序前端变更，仍需微信开发者工具清缓存、重新编译并手动上传体验版；服务器部署不会替代该步骤。

## 2026-09-02 补充：新增活码按钮布局修正

- `miniprogram/pages/group-resource-library/index.wxss` 已将说明卡改为不换行布局，并用 `button.live-qr-add-btn` 固定按钮为 `160rpx`，避免平台原生 button 宽度撑满。
- 本次仍是小程序前端变更，未修改后端、未重启生产、未上传体验版；重新编译后重点确认左侧说明文字和右侧按钮比例。

## 2026-09-02 最新交接：互帮互助双积分账本已部署

### 本轮完成

- 双积分正式分为 `基础积分` 与 `奖励积分`；基础账本承接首次赠送 100 分和基础任务，奖励账本承接奖励积分档位和奖励任务。总积分只做展示汇总。
- 后端 PointsCoreService、JSON/PostgreSQL 迁移、幂等索引、订单 `point_type`、状态接口、流水筛选和任务侧积分类型传播已完成；群资源查看明确扣基础积分并返回双账本总览。
- 用户端/运营页将可见词统一为“奖励积分/获取奖励积分”，移除中文“充值”显示；提现开关保留但默认关闭。

### 自测与审查

- 后端：`./.venv312/bin/python -m pytest -q backend/tests`，330 passed。
- 小程序 JavaScript、SVG、`git diff --check` 通过；双账本初始化、扣增和旧余额迁移脚本通过。
- 对抗式审查报告：`docs/qa/mutual-points-dual-ledger_对抗式审查.md`。已发现并修复迁移默认值误判和不存在的 `balance_after` 列问题。

### 生产状态

- 已部署 API、backend-worker、archive-worker；备份：`/home/ubuntu/teambuy-backups/20260902-015339-dual-ledger/`。
- 旧镜像回滚标签：`rollback-20260902-015339-dual-ledger`。
- 三类服务、PostgreSQL、端口 8004→8000、本机/公网健康检查均正常；数据库已核对双账本字段和唯一索引。
- 未覆盖生产 `.env`、secrets、媒体目录或数据库卷；未创建生产测试订单，未调用测试支付确认。

### 当前边界与下一步

1. 用户在微信开发者工具清缓存、重新编译并手动上传体验版后，人工验收基础/奖励积分展示、任务选择账本、完成结算和奖励入口开关；本轮禁止自动上传体验版。
2. 当前任务发布/领取/验收/结算仍以小程序本地演示数据为主；若要正式防篡改和跨设备一致，下一阶段必须迁移服务端任务事务，不能把本地余额当生产账本。
3. 全仓默认 pytest 仍被既有 `platform/wecom-archive-core/tests/test_engine.py` 的 `app.codec` 导入路径问题阻断。

## 2026-09-02 最新交接：群资源库返回箭头与积分展示效果稿

### 本轮本地变更

- `miniprogram/pages/group-resource-library/index.wxml` 的主页面自定义导航启用返回箭头；发布流程仍只使用流程内返回。
- `miniprogram/subpackages/my-tools/index/index.wxml` 为工具卡增加业务 key 类名；`index.wxss` 将群资源库入口改为蓝色资源视觉，避免开放功能呈现灰色不可用状态。
- 已生成群资源库积分展示效果稿：总值“可用积分”置于首位，下方区分基础积分和奖励积分，并展示群工具扣增规则的用户语言版本。

### 当前边界

- 本轮没有修改积分账本、群资源扣分、奖励发放和审核规则；效果稿中的 `-15/+10` 仍是讨论稿，不是当前实现。
- 本地静态检查通过：两个相关 JavaScript 文件 `node --check`、两个页面 JSON 解析、`git diff --check`。
- 未部署服务器，未创建生产测试数据，未调用生产测试确认，未上传微信体验版。

### 用户人工验收

1. 在微信开发者工具清缓存并重新编译，进入“群资源库”确认顶部返回箭头可返回上一页。
2. 返回“我的工具”确认群资源库图标和积分标签为蓝色，不再呈灰色不可用状态。
3. 仅查看积分效果稿，不要据此验收 `-15/+10` 规则；待业务规则确认后再单独实现积分展示并回归双账本。

## 2026-09-02 最新交接：群资源库积分口径和二维码保存

### 本轮已完成

- 群资源库查看他人二维码的真实扣分已统一为 15 个基础积分；新发布有效群的待确认奖励金额统一为 10 个积分，累计奖励上限仍是 100 分。
- 群资源库找群/我的发布页增加与互帮互助同源的积分摘要：总值为“可用积分”，基础积分始终显示，奖励积分为 0 时隐藏。
- 二维码查看弹窗主按钮改为“保存”，会下载并保存当前二维码到相册；弹窗和按钮保留 flex 居中。

### 验证与上线边界

- .venv312/bin/python -m pytest backend/tests/test_group_tools.py -q：4 passed；小程序 JS 检查、JSON 解析、git diff --check 通过。
- 本轮未部署服务器、未创建生产测试数据、未上传微信体验版。
- 需用户在微信开发者工具清缓存并重新编译后确认：基础/奖励积分显示、奖励为 0 时隐藏、查看群扣 15、二维码弹窗按钮居中并能保存到相册。
- 发布奖励仍然是待确认状态；管理员审核、投诉和追回积分流程尚未在本轮新增。

## 2026-09-02 最新交接：群资源库入口与积分视觉修正

### 本轮已完成

- 积分卡视觉加深，保留总积分、基础积分和非零奖励积分的层级展示。
- 二维码弹窗“保存”按钮固定宽度并使用 flex 居中。
- “我的”页微信群工具入口改为蓝色可用态。
- 群资源库自定义导航增加 `fallbackUrl`；无页面栈时箭头返回“我的”，有页面栈时正常返回。

### 验证与上线边界

- 自定义导航和群资源库脚本 `node --check`、相关页面 JSON 解析、`git diff --check` 已通过。
- 本轮未部署服务器、未上传微信体验版；需要用户在微信开发者工具清缓存并重新编译，人工确认积分卡深浅、保存按钮居中和返回箭头行为。

## 2026-09-02 最新交接：微信群工具入口登录门槛修复

### 本轮已完成

- “我的工具”图标与标题的垂直间距调整为 `18rpx`。
- 群资源库允许未登录用户先进入并浏览公开“找群”内容，修复与互帮互助首页行为不一致的问题。
- 私有数据请求在未登录时跳过；发布、我的发布、活码和查看二维码等动作会按需跳转登录，并携带返回地址。

### 根因与验证边界

- 路由和 `app.json` 注册没有问题，原因为群资源库 `onShow()` 对未登录状态直接 `reLaunch` 登录页。
- 需在微信开发者工具清缓存并重新编译，人工确认未登录可打开页面、登录后能回到目标动作，以及图标文字间距。

## 2026-09-02 最新交接：群资源状态文案修复

### 本轮已完成

- 群资源后端和小程序用户侧统一显示“审核中”。
- 内部 `rewardState: "pending"` 保持不变，前端兼容旧接口返回的“待确认”。
- 已覆盖找群/我的发布列表、发布流程提示和发布成功提示。

### 对抗式审查结论

- 状态展示链路没有发现遗漏；但投诉达到两人后的自动暂停、查看积分退款、奖励追回和管理员处罚目前仍不是本轮实现内容，不能将“审核中”视为这些功能已上线。
- 需在微信开发者工具清缓存并重新编译确认页面显示；若线上服务端尚未部署，前端兼容层仍会显示正确的新文案。

## 2026-09-02 最新交接：PC 微信群审核子工作台

### 本轮本地变更

- `backend/app/static/ops-admin/index.html` 在“业务工作台”下增加“微信群”子工作台。
- `backend/app/api/routes_ops_admin.py` 增加审核队列、详情、统计和审核动作接口；`backend/app/api/routes_group_resources.py` 增加用户投诉接口。
- `GroupResource` 增加审核状态、二维码版本、投诉、退款幂等和审核日志字段；双账本积分操作统一经过 `PointsCoreService`。
- 增加 `backend/tests/test_group_resource_review_workbench.py` 和对应 QA 清单。

### 当前状态

- 本地验证通过：群工具与审核工作台 9 项、既有 `test_app.py` 185 项；后台脚本解析和 Python 编译通过。
- 本轮不部署服务器。生产环境尚未应用本次审核接口、用户字段和后台页面变更。
- 上线前需要人工确认：管理员 Token、真实二维码详情加载、确认有效只发一笔 +10、两名投诉自动暂停并退款、处罚欠分和发布限制。

### 对抗式审查补充

- 查看动作与投诉/审核动作使用同一积分锁，避免自动暂停与扣分并发穿透。
- 处罚前会初始化发布者的共享双账本，避免发布者尚未进入互助页时初始 100 基础积分未被计入。
- 已永久下架资源的审核、无效处理和处罚接口动作已增加终态保护；重复无效处理保持幂等。

## 2026-09-02 最新交接：微信群审核工作台已部署

### 生产状态

- 已部署到 `81.70.84.35` 的 `/home/ubuntu/teamBuy`，生产域名为 `https://teambuy.lifelove.top`。
- API、backend-worker、archive-worker 和 PostgreSQL 均 healthy；生产端口保持 `8004 -> 8000`。
- 备份目录：`/home/ubuntu/teambuy-backups/20260902-063609`；权限修复二次备份：`/home/ubuntu/teambuy-backups/20260902-064326`。

### 已验证

- `/health` 公网 200；公开找群目录 GET 200。
- 未登录发布、查看二维码和投诉均被 401 拦截；带管理员 Token 的审核队列/统计接口 200。
- `/ops` 页面包含“微信群”，最近 3 分钟三个业务容器日志无错误关键词。

### 剩余人工验收

- PC 端使用真实管理员 Token 点击确认有效、投诉保护、退款、追回和处罚操作。
- 小程序需由用户在微信开发者工具清缓存、重新编译并上传体验版；服务器部署不会自动更新小程序页面。

## 2026-09-02 最新交接补充：我的工具入口间距

- 个人页 `miniprogram/pages/profile/index.wxss` 已将 `.tool-item-title` 的图标标题间距调整为 `28rpx`，覆盖“互帮互助”和“微信群工具”两个入口。
- 本轮未部署服务器；验证通过后由用户在微信开发者工具清缓存、重新编译并查看体验版。

## 2026-09-02 最新交接：商机合作高保真实现

### 本轮已完成

- 新增 `miniprogram/pages/business-opportunity/` 商机合作页和 `miniprogram/pages/business-opportunity-detail/` 客户详情页，并在 `app.json` 注册。
- “我的工具”新增“商机合作”入口；无名片跳转 `business-card-studio`，已有名片进入三 Tab 商机页。
- 商机合作页采用服务端分页和触底懒加载，包含动态合作、合作能力、我的名片、搜索、行业/细分行业筛选；列表与精选资料图片按需懒加载。
- 名片编辑页新增行业、细分行业、行业标签和当前合作意向字段；“我能提供”至少 1 项、最多 6 项，行业标签最多 8 项。
- 新增 `/api/business-opportunities/cards` 列表、详情 GET 和联系方式解锁 POST；详情默认脱敏，联系方式解锁为 1 个基础资源积分，24 小时内同一名片不重复扣分。
- 生产身份边界已加固：匿名可浏览列表/详情；生产接口只信任已验证会话，不信任 `viewerUserId`；解锁接口不允许请求体冒充其他账号。

### 本轮验证

- 商机相关回归集 14 passed。
- 完整 `backend/tests/test_app.py` 185 passed，1 个既有 `DATABASE_BACKEND=json` 与硬编码 `postgres` 健康检查不一致的环境失败。
- JS/JSON/Python 静态检查和 `git diff --check` 通过。
- 已完成一次对抗式审查并修复 P0 联系方式越权风险；报告：`docs/qa/商机合作_对抗式审查报告.md`。

### 当前上线状态与下一步

- 服务器未重新部署；本轮后端变更尚未应用到生产。
- 小程序未上传体验版；必须由负责人在微信开发者工具清缓存、重新编译、上传体验版后人工验收高保真布局、匿名浏览、名片字段保存、懒加载和 1 积分解锁。
- 既有直接分享客户页仍是独立链路，商机市场的联系方式门槛只作用于商机市场详情；上线前确认该产品边界。

## 2026-09-02 最新交接补充：商机合作真机可读性与联系方式说明

- 商机合作第一 Tab 用户可见名称已改为“找合作”，顶部文案改为“找对合作伙伴 / 让合作更简单”。内部接口模式仍为 `intent`。
- Tab 主/副标题、行业及细分筛选、列表和详情能力标签字号已放大；按钮和标签继续保持 `rpx`、flex 居中、nowrap。
- 本人客户页预览现在明确提示：本人视角不显示对外沟通操作；其他用户打开公开客户页可电话咨询/复制微信。商机市场详情完成联系方式解锁后，手机号可拨打或复制。
- 本轮仍未部署服务器、未上传小程序体验版；需在微信开发者工具重新编译后人工验收真机效果。

### 本轮最终验证

- 商机相关回归集 15 passed；未重新部署服务器，未上传体验版。
- 需人工确认：本人预览提示、外部客户拨打/复制、商机详情手机号双按钮，以及放大后的 Tab 和行业标签在真机上不换行。

## 2026-09-02 最新交接补充：商机合作 Banner 参考稿重排

### 已完成

- `miniprogram/pages/business-opportunity/index.wxml` 的 Banner 已按参考稿重组为品牌标识、核心主张、名片使用说明、基础积分和合作风险提示五层信息。
- 当前文案为“让对的人相遇，让合作自然发生。”、“从一张名片开始，看见能力，也表达你的需求。”以及“温馨提示：合作有风险，建议多线下交流、充分了解后再决定，谨慎为上。”。
- `miniprogram/pages/business-opportunity/index.wxss` 将积分从右下角悬浮块改为独立横向信息行，并用分隔线建立阅读节奏；风险提示正文允许自然换行。

### 状态与下一步

- 本地静态检查及商机相关回归集已通过：15 passed。
- 本轮只改小程序 Banner 展示层，未部署服务器、未上传体验版，也未改变积分和联系方式业务规则。
- 需由用户在微信开发者工具重新编译后，人工检查 Banner 在普通手机、较窄屏幕和 iPad 下的文字换行、整体占高及 Tab 首屏位置。

## 2026-09-02 最新交接补充：Banner 提示区与 Tab 颜色微调

- `miniprogram/pages/business-opportunity/index.wxss` 已将 Banner 的“温馨提示”限制为约 `56%` 宽度，提示图标、标题和正文同步轻量化；提示正文仍支持自然换行。
- 三个 Tab 非选中态主标题调整为深灰 `#556579`，副标题调整为 `#778596`；选中态仍保留品牌青绿色。
- 本轮只改展示层，未改动商机合作接口、懒加载、联系方式解锁和积分规则。

### 下一步

- 微信开发者工具重新编译后，检查 375/393 宽度下提示文案可读性、Tab 三列不换行，以及 Banner 高度是否符合预期。

## 2026-09-02 最新交接补充：修正温馨提示区高度

- 已恢复 `miniprogram/pages/business-opportunity/index.wxml` / `index.wxss` 中温馨提示的原有横向宽度，不再使用上一轮的 `max-width:56%`。
- 提示区改为全宽横向布局，标题与风险正文优先同行；第二条分隔线的上下间距收紧，正文行距同步压缩，从纵向减少提示区占高。
- 本轮只改 Banner 展示层，未改动商机合作数据、积分、联系方式解锁和列表懒加载。

### 下一步

- 微信开发者工具重新编译后，确认提示区高度约缩短一半，完整风险文案在普通屏幕同行、窄屏自然换行，且不遮挡后续内容。

## 2026-09-02 最新交接补充：移除 Banner 第三段蓝色背景

- `miniprogram/pages/business-opportunity/index.wxml` 已将温馨提示从 `.opportunity-hero` 中移出，放到独立的 `.hero-tip-panel`；提示区域恢复页面全宽浅色背景，不再占用蓝色 Banner。
- `.opportunity-hero` 已移除 `min-height:500rpx`，蓝色区域只包含核心文案、分隔线和基础积分，随内容自适应高度，积分下方不再保留大块留白。
- 本轮未改动商机合作接口、懒加载、联系方式解锁和积分规则。

### 下一步

- 微信开发者工具重新编译后，确认蓝色 Banner 在积分行后自然结束，温馨提示位于其下方且不出现异常空白或文字遮挡。

## 2026-09-02 最新交接补充：我的工具三功能系统性对抗式审查

### 已确认

- 商机合作详情页解锁后：手机号可拨打和复制，文字微信可复制，微信二维码可查看；微信小程序不能直接拉起个人微信私聊。`我的名片 → 预览客户页`是本人预览，隐藏外部沟通操作；其他用户打开公开客户页后通过客户沟通组件拨打电话、复制微信或查看二维码。
- 商机合作已实现服务端分页和触底懒加载，但存在请求竞争 P1：首屏或翻页请求进行中切换 Tab/筛选/搜索，新的查询会被 loading guard 忽略，可能展示旧列表。
- 互帮互助的任务、提交、验收和结算仍是小程序本地 storage 演示，跨设备/跨用户正式闭环未完成；服务端状态接口不能替代任务实体和结算真源。
- 群资源后端有投诉接口和“两名已付费用户自动暂停并退款”规则，但小程序当前没有用户投诉入口；用户端无法自然触发该保护链路。
- 群二维码更新会增加版本，但当前服务端未明确把已审核资源重置为审核中；换码是否重新审核列为上线前业务决策。
- 商机市场的 1 积分联系人解锁与既有公开客户页免费联系方式展示是两条不同入口；是否允许公开分享绕过积分门槛，必须由负责人确认。

### 本轮验证与状态

- 审查报告：`docs/qa/我的工具三功能_系统性对抗式审查报告.md`。
- 群资源、活码、审核、投诉保护和双账本回归 14 passed；商机相关回归合计 15 passed。
- 相关小程序 JS、`app.json` JSON 解析和 `git diff --check` 通过。
- 本轮没有修改业务代码、没有重置或覆盖工作区已有修改、没有重新部署服务器、没有上传微信体验版。

### 下一步优先级

1. 先修商机筛选竞争、补群资源用户投诉入口，并决定互帮互助是否立即服务端化或暂时降级为明确演示。
2. 明确换码重审规则和商机公开客户页的联系人收费边界。
3. 后续补商机联系结果/举报、群资源分页与错误重试、稳定游标和数据库索引。
4. 小程序重新编译后用两个真实账号验收本人预览、他人拨打/复制、商机解锁、重复解锁和投诉路径。

## 2026-09-02 最新交接：我的工具三功能修复完成，待部署验收

### 已完成

- 互帮互助的任务、提交、验收和结算已经由服务端实体承载；同一微信身份在不同设备登录时读取同一份任务与积分结果。生产接口使用已验证会话身份，不把前端传入的用户 ID 当作授权依据。
- 商机合作已补齐服务端分页、触底懒加载和请求竞态保护；快速切换 Tab、行业、细分行业或搜索不会再用旧响应覆盖新条件。联系方式解锁后可拨打/复制手机号、复制微信、查看微信二维码，重复进入详情会复用有效解锁。
- 商机名片未解锁时，普通公开客户页、直接 URL、正文/摘要/简介/内容块和旧分享快照不会泄露联系方式；已解锁的非本人可提交“联系方式失效”举报，同一名片版本重复提交幂等。
- 微信群公共列表已改为游标分页和触底懒加载，查看二维码弹层提供失效举报；二维码更新会回到审核中，既有“两名不同付费用户投诉后暂停并退回查看积分”规则保持不变。

### 验证结果

- `202 passed, 1 deselected`：跨设备互助、双账本结算、商机积分闸门/重复解锁/公开页脱敏/失效举报、微信群分页，以及原有群资源、审核、活码和应用回归。
- 健康检查 `1 passed, 185 deselected`；Python 编译、全部小程序 JS 语法、JSON 解析和 `git diff --check` 通过。

### 上线前人工确认

- 尚未部署生产后端；生产 Postgres 首次使用互助功能前需执行包含新表、索引和旧唯一索引清理的 schema 迁移，并核对容器/健康接口。
- 小程序代码尚未在微信开发者工具重新编译上传；需用两个真实账号/两台设备验证：互助任务发布-提交-验收跨设备一致、商机解锁后电话拨打/复制和微信复制、公开页不能绕过积分、联系方式失效举报、群查看/投诉触发暂停退款。
- 群资源“我的”列表仍按用户日发布上限读取，公共目录已分页；羊毛任务的评论举报/退款目前保留待处理记录，若后续要做平台处理闭环需补管理员处理入口。

## 2026-09-02 最新交接：统一积分账户优化完成

### 本轮结论

- 已完成三个工具共用一个服务端积分账户的统一：基础积分为初始 100 分和平台积分，充值积分只承载充值/未来提现；总积分对外展示，消费按基础优先、充值其次。
- 已加入历史平台奖励和旧资源钱包的一次性兼容迁移，并用迁移标记避免重复执行；可识别充值积分不会被迁移到基础账本。
- 暂不开发独立“积分账户”页面，不把“我的工具”改成四个入口；互帮互助的“我的”保留，充值入口隐藏但服务端充值数据保留。
- 小程序本地缓存迁移已改为保守策略：旧 v2 混合余额进入基础积分；已有 v3 双账本缓存不猜来源，保留充值桶并以服务端状态覆盖。

### 验证结果

- `PYTHONPATH=. .venv312/bin/pytest -q backend/tests`：`344 passed`。
- Python `compileall`、全部小程序 JS `node --check`、小程序 JSON 解析、`git diff --check` 均通过。
- 定向对抗式检查覆盖：重复读取/消费不重复扣分、基础优先消费、充值积分迁移边界、管理员首次发奖励保留 100 分、三个工具不新增第四入口。

### 上线前动作

- 本轮未部署服务器，未上传微信体验版。生产部署前仍需按项目文档备份、执行数据库 schema 迁移并验证容器和公网健康接口。
- 用户需在微信开发者工具清缓存、重新编译并上传体验版；重点人工确认三个页面的总积分/基础积分展示、充值积分有余额时的显示、跨设备登录后的余额一致性。

## 2026-09-02 最新交接：互助任务分享卡和任务过程记录

### 已完成

- 互助任务分享卡已升级为文字优先的 `mutual_task_share_v3`，内容包括任务价值、完成三步、基础积分奖励、无需加微信和平台记录结算说明；旧样式快照不会被误复用。
- 小程序任务和普通任务都必须填写完成标准；前端新增小程序任务字段，服务端对非羊毛任务做最终校验。
- 发布者管理详情保留所有提交记录，显示执行者昵称、待验收/已验收/已结算状态，以及已参与、已提交、已验收、已结算统计。
- 目标小程序行为事件已支持打开、返回、取消和失败，按任务统计并排除发布者；生产行为接口使用认证身份，行为事件只接受存在的服务端任务。

### 验证结果

- `PYTHONPATH=backend .venv312/bin/pytest -q backend/tests/test_my_tools_cross_device_and_gates.py backend/tests/test_mutual_help_points.py`：`11 passed`。
- 相关小程序 JS `node --check`、Python `compileall`、分享源字段断言和 `git diff --check` 通过。
- 对抗式审查已覆盖分享字段丢失、跨任务统计串线、身份伪造、无效任务事件、已结算记录消失等边界，并完成对应修正。

### 上线前人工确认

- 本轮未部署生产服务器，未上传小程序。用户需在微信开发者工具重新编译后，用两个真实账号检查：分享卡图片和标题、任务完成标准必填、发布者四项统计、执行者状态，以及打开目标小程序后返回/取消/失败事件。
- 当前行为事件只是生命周期证据，不能证明外部小程序内具体动作已完成；若未来对重要目标做自动核验，需要目标小程序提供可靠回调或服务端验证接口。

## 2026-09-03 最新交接：互助任务分享卡与评论功能完成，待真机验收

### 已完成

- 任务分享卡已升级为 `mutual_task_share_v4`：普通/小程序任务突出“+N 分”和“免加微信，选任务就能互助”；羊毛福利使用“选福利就能互助”；有图保留真实图，无图使用信息视觉区，并统一指向具体任务详情。
- 三类任务详情均可先查看评论；列表显示评论数量和二元“值得做”比例；“一般”不进入比例分母。
- 评论可在完成前发布；服务端根据提交、验收或完成状态决定是否显示“已完成体验”，客户端不能伪造；详情不返回评论作者 ID。
- 任务分享和评论相关自测、后端回归及对抗式审查报告已保存为 `docs/qa/互帮互助任务分享与评论_Codex自测及对抗式审查报告.md`。

### 验证结果

- `./.venv312/bin/pytest -q backend/tests/test_my_tools_cross_device_and_gates.py`：4 passed。
- 相关小程序 JS `node --check`、任务分享源 smoke check、评论汇总 smoke check 和 `git diff --check` 通过。
- 本轮未部署生产服务器，未重新编译/上传小程序；旧已发送的微信群卡片不会自动变化，重新生成后新发送的卡片才使用 v4。

### 上线前人工确认

- 用户需在微信开发者工具清缓存并重新编译，用有图/无图任务分别检查卡片渲染和原生分享；确认分享按钮只在静态图就绪后出现，分享 path 打开对应任务详情。
- 用两个真实账号检查三类任务的评论列表、评论数/值得做比例、提交前评论、提交后“已完成体验”、重复评论和举报入口；羊毛未解锁时应可看评论但不能评论。

## 2026-09-03 补充交接：原生分享卡 v5 简洁版

- 任务分享卡已从 v4 升级到 `mutual_task_share_v5`：卖点放在任务名称下方，字号 28；移除绿色宣传条、三步块和顶部重复奖励，只保留一个奖励胶囊。
- 微信原生分享标题、应用名称和系统入口保持原生提供，业务层只生成缩略图内容；旧 v4 分享快照会因版本变化重新生成。
- 本轮未部署服务器，未上传体验版；需在微信开发者工具确认副标题完整显示、有图/无图布局和原生分享回调。

## 2026-09-03 最新交接：互助任务分享卡固定 5:4 有图/无图模板

### 已完成

- 互帮互助自定义分享图统一使用 `750×600` 固定 5:4 画布，结构为“视觉区 + 底部固定卖点条”。
- 有图任务完整收纳真实图片，避免截图、海报和二维码被裁切；无图任务使用包含任务标题和摘要的轻量互助信息区。
- 自定义图不再重复绘制微信原生外壳、应用名称和“点击开始任务”按钮；外层微信原生卡片继续负责标题、应用名称和点击入口。
- 样式版本已升级为 `mutual_task_share_v6`，旧 v5 快照不会复用；普通/小程序任务底部显示“​​不用加微信，选任务就能互助”，羊毛福利显示“​​不用加微信，选福利就能互助”。

### 验证结果

- 全部小程序 JavaScript `node --check` 通过。
- 互助回归：4 passed。
- 普通任务/羊毛福利分享源及固定 5:4 模板 smoke check 通过。
- `git diff --check` 通过。

### 上线前人工验收

- 本轮未部署生产服务器，未重新编译或上传微信体验版。
- 用户需在微信开发者工具清缓存后检查有图/无图任务卡、真实图片完整性、底部卖点可读性、原生分享回调的 `imageUrl` 和任务详情 `path`。
- 固定模板会减少绘制分支，但首次生成仍受图片下载、Canvas 导出和上传影响；静态快照准备完成后重复分享才会快速读取。

## 2026-09-03 最新交接：电子名片自动同步商机草稿，公开状态分开确认

### 已完成

- 名片编辑页的商机区现在明确展示“要不要把你的信息展示到商机合作？”，提供“展示我的合作资源”和“发布当前合作需求”两个独立开关；名片内容保存后自动形成商机合作草稿，不自动公开新用户信息。
- 首次发布名片后显示完成提示，可选择公开资源、公开当前需求或先不公开；选择后保存 `visibilityChoiceVersion`，后续编辑会保留选择。
- 后端按资源/需求两个公开状态筛选列表，详情和普通公开资料接口在需求未公开时不透传当前合作需求；旧 `enabled/discoverable` 名片继续兼容，无商机公开字段的名片不进商机目录。
- 商机 Tab 已改为“合作需求 / 合作资源 / 我的名片”，其中“我能提供”仍保留为名片字段和资源 Tab 辅助说明。

### 验证结果

- 商机独立公开状态回归：`5 passed`。
- 业务卡相关回归：`3 passed`。
- 全部小程序 JavaScript `node --check`、后端 `compileall`、`git diff --check` 通过。

### 上线前人工验收

- 本轮未部署生产后端，未重新编译或上传微信体验版。
- 用户需在微信开发者工具检查新名片首次发客户时的完成提示；分别选择资源公开、需求公开、先不公开，并重新进入编辑页确认状态保留。
- 用两个账号确认资源-only 名片只出现在“合作资源”，需求-only 名片只出现在“合作需求”，两项关闭的名片不出现在商机列表；需求关闭后公开详情不显示当前合作需求。

## 2026-09-03 最新交接补充：名片发送语义与字号对抗式审查

### 已确认

- 名片编辑页底部的“发客户”代码顺序是先保存当前名片，再发布当前 revision，最后进入资料库；当前不是保存完成后自动弹出微信原生分享面板。资料库中真正的 `open-type="share"` 入口仍需要用户再次点击，这是微信原生分享触发边界。
- 不建议把编辑页按钮直接改成异步保存同时 `open-type="share"`，否则分享回调可能读取保存前的旧 revision 或旧静态图。产品语义应明确为“保存并发布 → 进入可发送入口 → 点击发客户”。
- 已对小程序全量 WXSS/WXML 做字号和布局风险扫描：79 个 WXSS、77 个 WXML，筛出 115 个小字号 + 固定宽度/不换行的潜在风险规则。名片编辑页 64rpx“更换/添加”按钮、商机详情页 68rpx 联系操作按钮不能直接套用 30rpx 正文字号。

### 本轮验证

- 全部小程序 JS `node --check`：通过。
- 小程序 JSON 解析：81 个文件通过。
- 后端商机/跨设备回归：5 passed；Python compileall：通过；`git diff --check`：通过。
- 详细报告：`docs/qa/商机合作与小程序字号_对抗式审查报告.md`。

### 下一步

- 暂不做全局机械放大；先按语义调整名片编辑页和商机相关页面，并同步扩大固定窄操作区后再进行真机截图验收。
- 本轮未部署生产服务器，未重新编译或上传微信体验版。用户需在微信开发者工具中检查保存并预览、发布后资料库发客户、商机 Tab、商机详情联系人操作以及小屏/大屏字号表现。

## 2026-09-03 最新交接：名片编辑与我的工具字号已调整

### 已完成

- 名片编辑页已完成局部字号升级，底部按钮文案为“保存并预览 / 发客户”；表单输入、商机合作区和联系方式区同步调整，窄操作按钮已扩容。
- 互帮互助所有应用内文字页面共用字号升级后的 `shared.wxss`，包括任务大厅、详情、发布、我的、管理、评论和羊毛相关界面；充值页单独调整。
- 微信群工具的主页面及活码/发布/弹窗流程已调整；商机合作列表、详情和工具入口已调整。
- 没有改全局字号，也没有改变积分、公开状态、审核、投诉、退款、处罚或互助结算规则。

### 验证结果

- 全部小程序 JavaScript `node --check`、81 个 JSON 解析、后端 `compileall`、`git diff --check`：通过。
- `backend/tests/test_my_tools_cross_device_and_gates.py`：5 passed。
- 7 个目标 WXSS 的结构和大字号固定控件对抗式检查：通过；核心中文控件宽度估算：通过。

### 待人工验收

- 未部署生产后端，未重新编译或上传微信体验版。
- 用户需在微信开发者工具清缓存后检查名片编辑、三个工具入口下各页面的字号、动态中文换行、按钮文字上下左右居中、双列选择器、活码操作、商机联系人操作和大屏/iPad 比例。

## 2026-09-03 最新交接：群工具白屏与积分桶显示已修复

### 已完成

- 修复群资源库主包页面跨包引用互帮互助 `shared.js` 的依赖问题：新增主包公共积分模块 `miniprogram/utils/shared-points.js`，群资源库与互帮互助现在复用同一份实现。
- 修复充值页把兼容总余额 `account.balance` 当作充值积分显示的问题；页面改为显示双账本的充值积分 `balances.reward`，同时说明任务/平台奖励进入基础积分。
- 核对服务端任务结算、群资源发布奖励、羊毛打赏均为基础积分，充值订单为充值积分；相关积分测试 17 个通过。

### 待人工验收

- 未部署生产后端，未重新编译或上传微信体验版。
- 用户需在微信开发者工具清缓存、重新编译后，从“我的 → 微信群工具”确认不再白屏，并检查充值页/互帮互助页/群工具页的基础积分、充值积分和总积分是否一致。
- 若旧设备仍显示充值积分 20，需先成功刷新服务端状态，再查看 `/api/scrm/mutual-help/ledger` 对应流水的 `pointType`；不能仅凭本地显示判断账本归属。

## 2026-09-03 最新交接：底部四个 Tab 字号已调整

### 已完成

- 首页、资料、客户雷达、我的四个底部 Tab 已按语义分层放大页面内容字号，未改业务逻辑、接口、积分账本或全局字号。
- 首页反馈/最近资料/优先客户、资料搜索/筛选/资料卡、雷达指标/客户卡/操作按钮、我的资产/工具/浏览记录/设置均已覆盖。
- 资料列表右侧操作列扩大到 120rpx；首页主操作文字和箭头整体居中；雷达与我的页面保留网格和动态中文的防溢出约束。

### 验证结果

- 全部小程序 JavaScript `node --check`、81 个 JSON 解析、后端 `compileall`、`git diff --check`：通过。
- 四个目标 WXSS 的括号平衡、字号范围和固定控件宽度估算：通过。

### 待人工验收

- 未部署生产后端，未重新编译或上传微信体验版。
- 用户需在微信开发者工具清缓存、重新编译后检查首页、资料、雷达、我的在小屏/普通手机/大屏或 iPad 下的动态中文换行、按钮居中、资料卡操作列宽度和整体页面密度。
- 原生底部 Tab 栏字体由微信控制，本轮只能确认页面内容字号已调整。

## 2026-09-03 最新交接：其余页面字号专项已补齐

### 已完成

- 未覆盖的资料/编辑页面、客户/消息/导入页面、商机扩展页面、订单/推广/账户系统页面和工作台分析页面已加入页面级字号覆盖。
- 采用标题、正文、字段、输入、辅助说明、状态标签、按钮分层的字号策略；按钮操作区保留 flex 居中和最小高度，未修改全局字号、原生 Tab 栏、业务逻辑或后端。
- `resource-tools-webview`、互帮互助 `web-view` 和仅负责跳转的分析占位页没有业务文本可调整；隐私页复用协议样式，互帮互助页面复用共享样式。

### 验证结果

- 109 个小程序 JavaScript `node --check`、81 个 JSON 解析、后端 `compileall`、全量 WXSS 花括号检查和 `git diff --check`：通过。
- 静态对抗检查未发现新增的大字号固定宽度规则或大字号 `nowrap` 无省略规则。

### 待人工验收

- 本轮未部署生产服务器，未重新编译或上传微信体验版；当前工作区既有用户未提交修改均保留。
- 用户需在微信开发者工具清缓存并重新编译，检查资料/编辑、客户、商机、订单、账户和工作台页面在小屏、常规手机、大屏/iPad 下的长标题换行、双列字段、按钮居中、固定底栏和纵向密度。
- 原生微信底部 Tab 栏字体仍由微信控制；若仍觉得偏小，应与页面内容区分开验收。

## 2026-09-03 最新交接：全项目用户体验、性能与对抗式审查

### 本轮状态

- 本轮只做审计和自检，没有修改业务代码、积分规则、审核规则、投诉退款规则、生产配置或部署状态。
- 全量审查报告：`docs/qa/全项目用户体验与性能_对抗式审查报告.md`。

### 已确认的高优先级风险

- 公开资料详情首屏等待图片缓存；互帮互助大厅先同步/迁移再渲染；群资源库混淆加载/失败/空态并在刷新时清空旧列表。
- 旧商机市场、雷达、收藏和详情页在空结果/异常时展示未标识的逼真示例数据，部分示例操作会显示成功；当前正式商机入口虽已更新，旧路由仍登记在 `app.json`，需人工确认是否仍可达。
- 资料、订单、供需和旧商机部分接口没有分页或仍全量扫描，数据增长后会放大服务端和小程序首屏成本。

### 验证结果

- `PYTHONPATH=.:backend ./.venv312/bin/pytest -q backend/tests`：346 passed。
- `PYTHONPATH=platform/wecom-archive-core ./.venv312/bin/pytest -q platform/wecom-archive-core/tests`：6 passed。
- 全量小程序 JS/JSON、后端 compileall、`git diff --check`：通过。
- 根目录直接 pytest 的两个导入路径收集错误已识别，不是业务断言失败；正确设置路径后测试通过。

### 下一步建议

1. 先消除旧商机页面未标识 mock fallback 和示例写操作。
2. 拆开资料详情媒体缓存、互助任务同步、首页待认领媒体处理与首屏渲染。
3. 统一页面 loading/empty/error/retry 状态并保留失败前快照。
4. 为正式导航中的全量接口补分页/摘要后，再用 1,000/10,000 条数据做性能验证。

### 待人工验收

- 本轮未部署生产服务器，未重新编译或上传微信体验版。
- 仍需用户在微信开发者工具清缓存，验证冷启动、返回缓存、弱网失败、不同设备字体、长列表和原生分享回调；真实 FCP/TTI、请求瀑布、线上配置和数据规模目前未知。

## 2026-09-03 最新交接：全项目体验与性能建议已统一收口

### 已完成

- 旧商机市场、雷达、已保存、详情和回应包页面已移除未标识示例数据及示例成功操作。
- 公开资料、首页待认领导入和互帮互助大厅已拆开首屏与媒体/同步任务；互助大厅先显示本地快照，失败时提示同步状态。
- 资料、订单、群资源、供需广场、已保存商机和正式商机页面已补齐加载、空态、错误、重试和旧数据保留；订单和资料库接入触底分页。
- 资料、旧商机、供需、订单后端接口增加分页边界；订单摘要支持 `summaryOnly`，首页使用轻量摘要。
- 新增分页形状回归测试：`backend/tests/test_core_list_pagination.py`。

### 验证与边界

- backend：348 passed；`platform/wecom-archive-core`：6 passed；全量小程序 JS/JSON、后端 compileall、受影响 WXML 标签平衡、`git diff --check`：通过。
- 根目录直接混合运行 pytest 仍会因两个顶层 `app` 包的命名冲突在收集阶段失败；标准命令是分别设置对应 `PYTHONPATH` 执行两组测试。
- 分页目前已经限制网络返回体和小程序渲染规模，但部分内存 repository 服务仍会先扫描再切片；规模化阶段需要继续做数据库级分页和耗时基准。
- 未部署生产服务器，未上传微信体验版。用户下一步在微信开发者工具清缓存并人工验收冷启动、返回缓存、弱网重试、长列表、真机字体、真实图片和原生分享回调。

## 2026-09-03 最新交接：我的工具服务端已部署生产

### 已完成

- 当前后端 `backend/app` 中与三个“我的工具”及其共享能力相关的 22 个变更文件已部署到生产服务器 `/home/ubuntu/teamBuy`。
- API、backend-worker、archive-worker 已切换到带时间戳的 hotfix 镜像；PostgreSQL 保持原数据卷，生产 `.env`、密钥、媒体目录和 Compose 配置未改动。
- 部署备份目录为 `/home/ubuntu/teambuy-backups/20260903174554/`，三个旧服务镜像已保留 rollback 标签。

### 线上验证

- 三个服务和 PostgreSQL 均 healthy，端口为 `8004 -> 8000`。
- 公网 `/health`、`/health/db`、群资源、商机合作、互助任务 GET 接口均通过；互助积分、互助任务、群资源相关表已核对存在。
- 本地部署前后端回归为 348 passed；未执行任何生产写入、充值测试或数据库清理。

### 当前边界

- 小程序前端尚未由 Codex 上传；用户需要在微信开发者工具清缓存、重新编译并上传体验版，之后手机端才会使用本轮“我的工具”页面、字号和分享卡代码。
- PC 运营后台代码已随后端静态资源部署，但真实管理员浏览器点击验收仍留待后续；PC 用户端暂不作为本轮目标。

## 2026-09-03 最新交接：三类小程序内容置顶功能已在本地完成

### 已完成

- 服务端已支持互帮互助任务、商业合作名片、微信群资源的管理员置顶/取消置顶。
- PC 运营后台新增“小程序置顶”页；管理员可按类型和关键词管理，置顶状态只影响小程序列表顺序。
- 小程序公开响应隐藏置顶字段，不显示“置顶”文案；分页在置顶排序之后执行。
- 普通用户接口无管理员 Token 时不能变更置顶；普通资料不能按商业合作名片置顶。

### 验证结果

- backend：350 passed；compileall、PC 内嵌 JavaScript 语法检查、`git diff --check`：通过。

### 下一步

- 本轮尚未部署这次置顶代码到生产；如需线上生效，必须由用户明确确认后按生产部署约定执行。
- 生产部署后，用户需在微信开发者工具清缓存、重新编译并上传体验版；人工验收三类列表的置顶排序、分页连续性和无“置顶”标签。
- PC 端需用真实管理员 Token 点击“小程序置顶”，分别验证三类内容置顶、取消置顶和刷新后的状态保持。

## 2026-09-03 最新交接：三类内容置顶后端已部署生产

### 生产状态

- 已按用户确认部署置顶功能；API、backend-worker、archive-worker 和 PostgreSQL 均 healthy。
- 部署备份：`/home/ubuntu/teambuy-backups/20260903205121/`。
- 回滚镜像：`teambuy-backend:rollback-20260903205121`、`teambuy-backend-worker:rollback-20260903205121`、`teambuy-archive-worker:rollback-20260903205121`。
- 生产验证通过：本机/公网健康检查、管理员 403/200 鉴权、PC `/ops`、三类公开列表和置顶字段隐藏检查。

### 当前待办

- 小程序源码未通过服务器部署；用户需在微信开发者工具清缓存、重新编译并上传体验版。
- 上传后人工验证互帮互助、商业合作、微信群三个列表：置顶项进入前列、分页连续、取消置顶恢复普通排序，且小程序不出现“置顶”文字。
- PC 后台使用真实管理员 Token 打开“小程序置顶”页，按实际运营需求执行置顶/取消置顶；本次部署验证未写入任何生产内容状态。

## 2026-09-03 最新交接：微信群 PC 新增与手机管理员配置已在本地完成

### 已完成

- PC 运营后台微信群审核页右上角新增“新增微信群”，支持上传二维码、填写群信息并提交正式 `GroupResource` 审核。
- PC 新增“手机管理员”配置区，可通过已注册用户 ID 开通/取消 `group_resource_admin` 权限，并查看当前已开通用户。
- 手机“我的发布”仅对服务端已授权用户显示“管理员新增”；管理员新增不占普通用户每日 1 个额度、不发放个人发布奖励，提交后在公开列表标记为“审核中”，审核通过后转为正式发布。
- 管理员资源沿用投诉、暂停、查看积分退款、有效期和下架生命周期；无效处理不产生个人处罚欠分。小程序公开响应隐藏管理员来源字段。

### 验证与下一步

- `backend/tests/test_group_resource_admin.py`、全量 `backend/tests`、compileall、JS 语法检查和 `git diff --check` 已通过；全量后端 353 passed。
- 本轮新代码尚未部署生产。部署前仍需按生产约定检查磁盘、备份并明确确认范围；不得默认把工作区全部修改同步上去。
- 部署后由用户在微信开发者工具清缓存、重新编译并上传体验版；人工验收 PC 新增、开通手机权限、手机管理员新增、审核公开、普通用户每日额度和无个人奖励/处罚。

## 2026-09-03 最新交接：推荐分润与商户转账分析已在本地完成

### 已完成

- 后端新增管理员接口 `/api/ops-admin/referral-analytics`，汇总推荐关系、分润奖励和微信商户转账。
- PC 运营后台“数据分析”下新增“推荐分润”子 Tab，支持今日、近 7 日、总计，展示推荐数量、谁推荐谁、推荐人收益排行、分润明细和转账记录。
- 顶部同时区分产生分润、净分润、提现申请、实际到账和当前可提现余额；关键词筛选不改变顶部范围总量。
- 报表只读，不改变奖励、提现、微信转账或用户数据；管理员 Token 鉴权保持不变。

### 验证与下一步

- 全量后端测试 354 passed；推荐/支付/提现相关 34 passed；compileall、PC JavaScript 语法检查和 `git diff --check` 通过。
- 本轮尚未部署生产。部署时需明确只发布本轮相关后端路由、AppService、PC 静态页和测试/文档范围，不能把整个脏工作区同步到服务器。
- 部署后用真实管理员 Token 打开“数据分析 → 推荐分润”，分别检查今日、近 7 日、总计、关键词筛选和真实商户转账状态；线上金额以数据库和微信转账结果为准。

## 2026-09-03 最新交接：PC 推荐分润提现审核入口已在本地完成

### 已完成

- PC“数据分析 → 推荐分润”新增“提现审核”面板，默认列出待审核申请，支持状态筛选和刷新。
- 已接通既有管理员操作：审核并发起微信商户转账、查微信状态、人工确认到账、撤销提现；所有涉及资金或余额的操作均有二次确认，操作后重新读取服务端状态。
- 未修改用户提现接口、奖励预占/释放逻辑和微信转账状态机。

### 验证与下一步

- PC 内嵌 JavaScript 语法检查和 `git diff --check` 通过；本轮后端业务测试未因仅增加管理端页面而重跑。
- 本轮未部署生产。上线前需按生产约定备份并只部署本轮明确的 PC 静态页（后端提现接口已存在），然后用真实管理员 Token 验证待审核、审核转账、查单、撤销和人工到账边界。

## 2026-09-03 最新交接：近期未部署后端与 PC 功能已部署生产

### 生产状态

- 已按用户明确要求发布当前交接中尚未部署的微信群正式目录/手机管理员、推荐分润分析和 PC 提现审核相关代码。
- 发布方式为当前镜像热修复；API、backend-worker、archive-worker 和 PostgreSQL 均 healthy，端口为 `8004 -> 8000`。
- 备份目录：`/home/ubuntu/teambuy-backups/20260903221424/`；回滚标签：`teambuy-backend:rollback-20260903221424`、`teambuy-backend-worker:rollback-20260903221424`、`teambuy-archive-worker:rollback-20260903221424`。

### 线上验证

- 本机/公网 `/health` 和 `/health/db` 通过；PC `/ops` 已包含“提现审核”。
- 管理员 Token 访问提现列表、推荐分润分析返回 200，未授权返回 403；公开群资源列表返回 200。
- 本轮未将小程序源码发布到服务器；用户仍需在微信开发者工具清缓存、重新编译并上传体验版，才能看到手机端相关改动。

### 待人工确认

- PC 使用真实管理员 Token 验证“数据分析 → 推荐分润 → 提现审核”的待审核、审核发起转账、查单、撤销和人工确认到账。
- 微信开发者工具上传后验证微信群管理员新增、三类置顶排序、互助/商机/微信群页面和字号改动。

## 2026-09-04 最新交接：用户内容安全方案已设计，尚未开发

### 已完成

- 已确认现有项目没有通用违禁词配置能力；当前文字安全工具主要处理非法 Unicode surrogate。
- 已新增 [用户内容安全与违禁词配置方案 v1](/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/docs/stage2-docs/46-content-safety-forbidden-words-v1.md)。
- 方案确定由后端统一服务覆盖输入/导入、保存、发布、公开读取、分享快照和历史复检；规则按版本管理，判定按内容 revision 绑定。
- 首版决策为 `block`、`review`、`warn` 三类动作，后台配置入口放在“运营工作台 → 内容安全”，不放在数据分析。

### 当前边界

- 尚未实现规则表、审核记录、审核队列、后台页面、用户端提示或历史复检 worker。
- 尚未确认首批违禁词来源、类别阈值、人工放行权限、白名单和内容留存期限；开发前需要先确认这些业务策略。
- 本轮未导入任何规则，未改变线上内容，未部署生产，也不需要用户此时上传小程序。

### 推荐下一步

1. 确认首批规则类别、动作阈值、人工放行和白名单边界。
2. 先实现规则/判定/审计数据结构与 `ContentSafetyService`，以报告模式接入高暴露入口。
3. 用现有数据评估误杀率后，再开启新发布门禁，最后做历史内容异步复检。

## 2026-09-04 最新交接：用户内容安全第一版已在本地完成

### 当前代码状态

- 后端已新增统一 `ContentSafetyService`、规则/判定/审计数据结构、管理员 API 和 PC“运营工作台 → 内容安全”页面。
- 资料、合集、名片、商机、供需卡、微信群资源、活码、互助任务/提交/评论和相关公开读取链路已接入服务端安全门禁；审核队列不向普通用户泄露具体命中词。
- 互助评论和提交的复核状态支持管理员放行/拦截；其它发布类对象在放行后仍需按原业务流程重新发布或审核，避免内容审核越权改变业务状态。
- 本地全量后端测试为 358 passed；Python、PC 内嵌 JavaScript 和 diff 检查通过。

### 当前生产状态

- 本轮未部署生产，未修改生产规则、数据库、密钥或媒体内容；小程序未上传体验版。
- 当前没有预置任何违禁词。上线前必须由运营在 PC 配置确认后的首批规则，先用“规则试测”验证误杀，再按业务需要启用 block/review。

### 首批规则范围确认

- 用户已明确：贷款、兼职、代理、招商、返现、引流、代运营、代做任务、薅羊毛、解封等泛化词暂不加入人工审核。
- 后续首批只考虑高确定性风险内容；“加微信、合作、微信群、任务、积分、收益、免费、羊毛福利”等产品正常词不设置单词级拦截。

### 下一步人工验收与后续开发

1. 用户在微信开发者工具重新编译，验收正常内容、阻断内容、人工复核内容，以及互助评论/提交审核后的状态恢复。
2. 管理员使用真实 Token 打开“运营工作台 → 内容安全”，新增一条测试规则，验证试测、队列、放行、拦截和审计记录；验收后再配置正式规则。
3. 之后单独实现图片/二维码/OCR/外链安全、规则批量导入、申诉与误杀统计、历史内容异步复检 worker；不要把这些能力假设成当前文本规则已经覆盖。

## 2026-09-04 最新交接：内容安全第一版已部署生产

### 生产状态

- 已按用户明确要求发布内容安全后端与 PC 管理页面；API、backend-worker、archive-worker、PostgreSQL 均 healthy，端口为 `8004 -> 8000`。
- 生产规则数量为 0，审核记录数量为 0；没有导入任何违禁词，也没有启用此前被用户暂缓的泛化人工审核词。
- 备份目录：`/home/ubuntu/teambuy-backups/20260904055511/`；回滚标签：`rollback-20260904055511`；生产 `.env`、密钥、媒体目录和数据库数据未覆盖。
- 本机/公网健康检查、数据库健康检查、内容安全接口未授权 403、PC `/ops` 内容和运行容器文件哈希均已验证。

### 仍需人工操作

1. 管理员登录 PC“运营工作台 → 内容安全”，先使用规则试测，再决定是否录入高确定性风险规则。
2. 用户在微信开发者工具重新编译并上传体验版；本次只部署服务端和 PC，不会自动更新小程序包。
3. 任何正式规则录入后，先用正常业务词（合作、微信群、任务、积分、收益、免费等）做回归，确认没有误伤再启用。

## 2026-09-04 最新交接：资料分享卡 v11 已在本地修改

### 已完成

- 资料分享卡底部文案已改为“少来回解释，资料一页讲清”，图片型、信息型和电子名片底部字号统一为 30。
- 分享版本已升级为 `share_card_v11` / `share_template_business_collection_v5`，旧分享快照会自动视为过期并重新生成。
- 互助任务分享卡仍使用原有“不用加微信，选任务就能互助”或羊毛福利文案，没有被通用资料 footer 改写。

### 验证与人工验收

- 两个相关小程序 JS 文件 Node 语法检查通过，`git diff --check` 通过。
- 本轮未上传小程序体验版。用户需在微信开发者工具清缓存、重新编译后，分别验收有图房源、无图资料、合集、电子名片和互助任务分享卡的字号、换行、截断及原生分享回调。

## 2026-09-04 最新交接：分享卡核心文案已调整为 32px

### 已完成

- 互助任务卡底部“不用加微信，选任务就能互助”和资料卡底部“少来回解释，资料一页讲清”均已调整为 Canvas 32px。
- 分享版本已升级为 `share_card_v12` / `share_template_business_collection_v6`，旧字号快照会自动重新生成。

### 验证与人工验收

- 两个相关小程序 JS 文件 Node 语法检查通过，`git diff --check` 通过。
- 本轮没有部署生产，也没有上传小程序体验版。用户需在微信开发者工具清缓存、重新编译后，确认两类卡片在微信原生分享缩略图中的字号、换行、截断和整体比例。

## 2026-09-04 最新交接：互助任务提交与积分规则已按用户意见收口

### 本轮已完成

- 小程序任务：提交完成记录后，在正常未复核路径立即发放基础积分；页面显示实际到账金额并引导回任务大厅。
- 普通任务：保留发布者验收、1 天自动通过、提交时预留成本和退回释放预留积分；执行者奖励不得高于发布者每次成本。
- 自己发布的任务：允许本人执行、评论、验收和结算，所有行为按普通用户统计，不增加特殊标记或额外限制。
- 任务详情：移除显眼的任务微信群分享按钮；普通/小程序任务提交前不显示评论编辑器，提交后才能评论；服务端 404/410 会清掉本地失效任务并返回任务大厅。
- 小程序任务、普通任务和评论的服务端状态继续作为最终真源；重复提交使用原提交结果，避免积分重复发放。

### 已验证

- `PYTHONPATH=.:backend ./.venv312/bin/pytest -q backend/tests`：360 passed。
- 互助页面相关 JS `node --check`、后端 `compileall`、`git diff --check` 通过。

### 仍需人工确认

1. 在微信开发者工具重新编译，验证小程序任务提交后基础积分即时到账、弹窗金额与余额一致，并返回任务大厅。
2. 验证普通任务的提交后评论入口、发布者通过/退回、自执行本人路径和 1 天自动通过提示。
3. 验证已删除/暂停任务的旧列表卡片点击后能看到明确提示并回到任务大厅。
4. 本轮未部署生产服务器，未上传微信体验版；需要部署时仍按生产部署约定先确认并备份。

## 2026-09-04 最新交接：互助任务后端已部署生产

### 生产状态

- 已按用户确认部署互助任务后端改动；生产 API、backend-worker、archive-worker 和 PostgreSQL 均 healthy。
- 生产端口保持 `8004 -> 8000`；本机 `/health`、`/health/db` 与公网 `/health` 验证通过。
- 备份目录：`/home/ubuntu/teambuy-backups/20260904130242/`；旧镜像回滚标签：`rollback-20260904130242`；新版本标签：`mutual-help-fixed-20260904130242`。
- 已验证运行容器加载了本地四个目标后端文件，API 启动命令正确，互助退回提交路由存在。

### 未完成事项

1. 小程序前端未随服务器部署，需要用户在微信开发者工具重新编译并上传体验版。
2. 仍需真机人工确认小程序任务即时到账、普通任务验收/退回、自己执行、提交后评论和失效任务提示。
3. 如需回滚，使用本次备份和 `rollback-20260904130242` 标签，先确认后再操作。

## 2026-09-04 最新交接：互助任务参与频次已落地本地

### 已完成

- 发布任务页新增“每位用户可参与”：默认“仅限一次”，可选“每天一次”；羊毛任务隐藏该设置并由服务端固定为一次性查看/解锁逻辑。
- 后端任务新增 `repeatPolicy`，提交记录新增服务端 `participationDay`；`daily` 按北京时间自然日允许每位用户每天一次，跨设备列表返回当天完成状态，`once` 继续保持一次性规则。
- 每日任务当天完成后不会从大厅永久消失，会显示“今日已完成，明日可参与”；详情提交按钮同步禁用，跨天后由服务端状态重新开放。
- 发布页辅助说明、占位文字和频次按钮样式已调整，按钮文字显式居中且不拆行。

### 已验证

- `PYTHONPATH=. ./.venv312/bin/pytest -q backend/tests`：362 passed。
- 相关小程序 JS `node --check`、后端 `compileall`、`git diff --check` 通过。

### 仍需人工确认

1. 在微信开发者工具重新编译，检查发布页默认选中“仅限一次”、切换“每天一次”后的提示和深浅主题颜色。
2. 使用两个设备/会话验证每日任务：当天完成后仍显示“今日已完成”，北京时间跨日后可再次提交且不会重复结算同一次提交。
3. 本轮只修改本地代码，未部署生产、未上传小程序体验版；上线前仍需按生产部署约定单独确认。

## 2026-09-04 最新交接：商机合作独立合作机会发布已在本地完成

### 已完成

- 商机合作页仍为三个 Tab，并新增“发布合作信息”入口和底部选择层：可进入名片编辑，或进入独立合作机会表单。
- 独立合作机会表单已实现方向、标题、具体说明、联系方式和有效期；标题空值占位符居中，输入后左对齐。
- 联系方式优先复用名片电话/微信；没有可用名片联系方式时要求手填电话或微信。服务端负责脱敏、审核状态、有效期和联系方式解锁，解锁成本为 1 基础积分，24 小时内幂等。
- 用户发布的供需卡已合并到商机合作的合作资源/合作需求列表，点击进入供需详情；供需详情已支持匿名浏览、登录申请和登录解锁联系方式。
- 修正两路合作列表分页：一路已经没有游标时不再重复请求第一页；编辑合作机会选择长期有效时会清除旧到期时间。

### 已验证

- 定向后端测试：4 passed。
- 新增页面及相关 API JS 通过 Node 语法检查；后端 compileall、`git diff --check` 和页面 JSON 解析通过。

### 仍需人工确认

1. 在微信开发者工具重新编译，验收底部选择层与发布入口的视觉位置。
2. 分别用有名片联系方式、无名片手填电话、无名片手填微信三种情况发布；确认审核通过后出现在正确 Tab。
3. 验收标题占位符居中、输入后左对齐、7/30 天和长期有效切换，以及待审核内容不公开。
4. 验收合作机会详情的匿名浏览、登录后 1 积分解锁、电话拨打、微信复制、24 小时重复查看不重复扣分。
5. 本轮未部署生产、未上传体验版；如需上线，仍需用户明确确认后按腾讯云部署约定执行。

## 2026-09-04 最新交接：商机合作空态按钮已修正

### 已完成

- 商机合作页所有 Tab 都会读取轻量名片摘要，不再只在“我的名片”Tab 判断是否已有名片。
- 列表为空且名片已存在时，显示“发布合作信息”；只有确实没有名片时，才显示“制作我的名片”。名片状态未返回前显示确认中的加载态。
- 加入用户身份竞态保护，避免旧账号的名片摘要覆盖当前账号。

### 已验证与边界

- 商机合作页 Node 检查、后端 compileall、`git diff --check` 和 `app.json` JSON 校验通过。
- 底部发布选择层仍由用户主动点击“＋ 发布合作信息”打开，不会因为老用户自动弹出；本轮未部署生产，未上传体验版。
- 用户需在微信开发者工具重新编译后，验收已有名片/无名片两种空态及按钮跳转。

## 2026-09-04 最新交接：商机合作后端已部署生产

### 生产状态

- 已按用户确认部署商机合作独立发布及供需卡接入所需的后端改动；只同步四个明确后端文件，没有同步未提交工作区，也没有覆盖生产环境变量、密钥、媒体或数据库卷。
- API、backend-worker、archive-worker 和 PostgreSQL 均 healthy；生产端口保持 `8004 -> 8000`，本机健康、数据库健康和公网 `https://teambuy.lifelove.top/health` 均通过。
- 备份目录：`/home/ubuntu/teambuy-backups/20260904211735/`。
- 回滚标签：`teambuy-rollback-api:20260904211735`、`teambuy-rollback-backend-worker:20260904211735`、`teambuy-rollback-archive-worker:20260904211735`。
- 当前部署标签：`teambuy-deploy-api:20260904211735`、`teambuy-deploy-backend-worker:20260904211735`、`teambuy-deploy-archive-worker:20260904211735`。
- 已验证运行容器内四个目标文件 SHA-256 与本地一致，商机合作列表/详情和供需联系方式解锁路由已加载。

### 仍需人工确认

1. 小程序前端没有随本次服务器部署更新；用户需在微信开发者工具清缓存、重新编译并上传体验版。
2. 重点验收已有名片时空态按钮显示“发布合作信息”、无名片时显示“制作我的名片”、底部选择层、独立合作机会发布、名片/手填联系方式、审核后列表展示和 1 积分解锁。
3. 如需回滚，先确认后使用本次备份和对应时间戳 rollback 标签，不直接删除当前容器或生产数据。

## 2026-09-04 最新交接：PC 商机线索录入页已简化（本地未部署）

### 已完成

- PC 商机录入改为核心内容优先：标题明确为必填，摘要/完整内容/联系方式用占位符说明选填或建议填写。
- 线索 ID 已隐藏并继续保留编辑所需的内部值；脱敏展示由服务端自动生成。
- 城市、行业、标签、来源和状态设置均已折叠；新增“仅保存草稿”和“保存并发布”。
- 空来源原文会自动使用完整内容留档，避免重复粘贴。

### 已验证与下一步

- 内嵌 JS 语法、HTML ID 唯一性、关键控件存在性和 git diff --check 通过。
- 本轮只修改本地 PC 静态页面，未部署生产。需要上线时，按生产部署约定只发布该明确文件并先确认。
- PC 人工验收重点：新建不填写 ID、只填标题即可保存草稿、粘贴原文后保存并发布、点击列表行编辑、发布设置展开后仍能调整状态、下架按钮正常。

## 2026-09-04 最新交接：我的工具 PC 运营工作台 V1 已在本地完成

### 已完成

- PC 导航已按业务职责重新归组；新增“我的工具 · 数据总览”，包含今日、近 7 日、累计切换，以及基础积分/充值积分/消费和互帮互助、微信群、商机三类工具摘要。
- 三类工具明细统一提供关键词、状态、仅看测试数据、分页、当前页全选、标记测试、批量下架和测试数据删除。正式记录默认只能下架；硬删除需要显式测试标记且没有业务关联，微信群归档可通过现有审核恢复。
- 新增“权益与计费”页，统一配置充值积分、商户转账/提现、会员与客户信息链的功能与显示状态；原互助积分配置面板已从主操作区隐藏，避免配置分散。
- 新增 PC 管理员发布互助任务最小表单；不填普通用户时归属内部“平台管理员”，普通/小程序任务仍要填写完成标准，所有任务继续走原有内容安全和积分结算规则。PC 群资源新增和商机线索录入沿用既有入口。
- 新增统一手机管理员权限台账和 API。微信群 `group_resource_admin` 已接入手机端管理员新增；互助/商机普通手机发布本来已开放，统一列表中的对应角色当前用于权限归档和后续扩展，不代表新增了额外业务门槛。

### 关键接口

- `GET /api/ops-admin/tools-dashboard`
- `GET /api/ops-admin/tools-records`
- `POST /api/ops-admin/tools-records/bulk-action`
- `POST /api/ops-admin/mutual-help/tasks`
- `GET/PUT /api/ops-admin/mobile-tool-admins[/{userId}]`

### 已验证

- `PYTHONPATH=.:backend ./.venv312/bin/pytest -q backend/tests`：369 passed。
- 工作台专项测试：4 passed；后端 `compileall`、PC 内嵌 JavaScript `node --check`、PC HTML 字面量 ID 检查和 `git diff --check` 通过。
- 对抗式检查覆盖未授权访问、正式任务硬删除保护、测试任务清理、平台任务发布、业务关联记录保护和重复 PC 导航入口；未发现阻断问题。

### 当前状态与人工验收

- 本轮未部署生产、未覆盖生产环境变量/密钥/媒体/数据库，也未上传微信体验版。
- PC 上线前需用真实管理员 Token 验证：总览三种时间范围、三类明细筛选、标记测试/下架/删除保护、平台任务发布、权益开关和手机管理员配置。
- 微信群手机管理员新增仍需用户在微信开发者工具清缓存、重新编译并上传体验版后验收。涉及互帮互助和商机的手机角色目前没有额外特权行为，后续如需“仅管理员可发布平台内容”再单独定义权限门槛。

### 2026-09-04 最新交接：PC 我的工具工作台 V1 已部署生产

- 生产已切换到本轮工作台版本，部署时间戳为 `20260904231407`；备份目录为 `/home/ubuntu/teambuy-backups/20260904231407/`。
- API、backend-worker、archive-worker 与 PostgreSQL 均 healthy；生产端口仍为 `8004 -> 8000`，本地/公网健康检查和数据库连接检查通过。
- 已验证新增管理路由受 Token 保护、PC 页面包含“我的工具 · 数据总览”和“权益与计费”，容器内五个发布文件与本地哈希一致，应用日志无近期错误。
- 回滚标签：`teambuy-rollback-api:20260904231407`、`teambuy-rollback-worker:20260904231407`、`teambuy-rollback-archive:20260904231407`。本次未部署小程序包，用户仍需在微信开发者工具中重新编译、上传体验版并人工验收。

### 2026-09-05 最新交接：充值积分任务与提现审核已在本地完成

- 当前代码已实现基础积分/充值积分双账本：充值积分任务发布时预留预算，验收后立即发给执行者，拒绝或删除时释放；基础积分保持不可提现。
- 充值积分提现规则为最低 100 分、默认手续费 20%、不等待 7 天，申请后冻结并由 PC 管理员审核；平台管理员发布充值积分任务使用独立的平台充值积分任务预算。
- 小程序新增充值积分说明和提现页面，PC 新增平台预算调整和充值积分提现审核区域；三类任务沿用现有入口，羊毛福利不展示无意义的固定奖励选择。
- 本地验证已通过：双账本/任务/提现相关测试 `20 passed`，Python compileall、PC 内联 JS、小程序相关 JS 和 `git diff --check` 通过。
- 本轮未部署生产，也未上传小程序体验版。下一步应先在微信开发者工具清缓存、重新编译，人工验收充值订单、充值积分任务验收到账、提现申请和 PC 审核；真实微信商户转账回调仍需在具备商户配置的环境验证。

### 2026-09-06 最新交接：我的账户入口与提现规则已补齐

- “我的”页面“我的工具”现为四个入口：互帮互助、微信群工具、商机合作、我的账户；账户页位于 `subpackages/my-tools-mutual-help/account/index`。
- PC“权益与计费”已有充值积分和商户转账/提现的“切换显示”“切换功能”；账户页的两个操作按钮分别服从这两组配置。
- 账户页和提现页已展示完整提现规则：仅充值积分可提现、10 分折合 1 元、最低 100 分、手续费 20%、每日最多 1 笔、提交后冻结、PC 审核、微信处理到账、失败/撤销退回。
- 当前显示策略：PC 关闭“提现显示”时，账户页和提现页均隐藏提现按钮及提现说明；仅关闭“提现功能”时，规则保留但按钮显示“提现暂未开放”。
- 已完成本地验证：互助双账本/预算/提现相关测试 `20 passed`，相关 JS/JSON/Python 检查及 `git diff --check` 通过。
- 本轮未部署生产。下一步需在微信开发者工具重新编译并人工验收四格入口、PC 开关同步、充值页和提现规则显示；真实微信商户转账仍需商户配置环境验证。

## 2026-09-06 最新交接：充值积分与账户页后端/PC 已部署生产

### 生产状态

- 用户已确认部署；后端和 PC 生产已切换到 `20260906002920` 发布版本。备份目录：`/home/ubuntu/teambuy-backups/20260906002920/`。
- API、backend-worker、archive-worker 发布镜像：`teambuy-deploy-api:20260906002920`、`teambuy-deploy-worker:20260906002920`、`teambuy-deploy-archive:20260906002920`；回滚标签分别为 `teambuy-rollback-api:20260906002920`、`teambuy-rollback-worker:20260906002920`、`teambuy-rollback-archive:20260906002920`。
- 三个应用服务和 PostgreSQL 均 healthy；端口保持 `8004 -> 8000`，本机/公网 `/health`、`/health/db` 通过。
- PC `/ops` 已包含“权益与计费”和“提现审核”；提现相关接口的未授权访问受保护，生产数据库已确认双账本和 `mutual_point_withdrawals` 表结构存在。

### 仍需人工验收

- 本轮未发布小程序包。请在微信开发者工具清缓存、重新编译并上传体验版，检查“我的 → 我的账户”的四格入口、充值页、提现显示开关及完整规则文案。
- 没有创建生产充值订单、没有发起真实提现；微信支付和商户转账仍需在具备商户配置的环境中单独验证。

### 2026-09-06 最新交接：充值积分隐藏展示已收口（本地未部署）

- 当 PC 关闭“充值积分显示”时，账户页只保留可用总积分和通用积分流水；充值积分拆分、相关说明、充值/提现入口及提现说明均隐藏。
- “我的工具”、充值页、提现页同步遵守该显示开关；历史路径直达时不会展示充值余额、提现规则，也不能提交充值/提现操作。
- 本轮未部署生产、未上传小程序体验版。请在微信开发者工具清缓存、重新编译后检查显示开关开/关两种状态，以及关闭后重新开启是否恢复。

### 2026-09-06 最新交接：互帮互助三页 UI 已重排（本地未部署）

- 已保留浅色/深色切换；任务大厅默认单列并减少为任务决策信息，任务管理列表不再显示大分享图，发布页将普通任务/羊毛福利参考入口收为选填展开项。
- 任务大厅、任务管理、发布页均按 PC `rechargeVisible` 收口充值积分展示；关闭时不会在任务奖励、余额拆分或发布选项中泄漏充值积分文案。
- 已完成相关 JS 静态检查和 `git diff --check`。未部署生产、未上传小程序体验版。
- 人工验收重点：浅色/深色两种主题、任务大厅单列/双列、普通任务和羊毛福利发布、任务管理去验收/分享/删除、充值显示开/关状态。

### 2026-09-06 最新交接：微信群“我的发布”入口与发布额度已完成（本地未部署）

- “我的发布”标题区现在有固定的“＋发布群”按钮，空状态有“发布第一个群”；找群页仅保留轻量发布引导。
- 发布页显示服务端口径的“今天还能发布 N 个群资源”；删除当天资源不会恢复额度，页面会明确说明原因。
- 新增接口 `GET /api/group-resources/publish-quota`；删除按钮与更新二维码按钮同高同列，删除仍需二次确认。
- 已通过群资源专项测试、JS/WXML/Python 检查和 `git diff --check`；未部署生产，未上传小程序体验版。
- 人工验收：在“微信群工具 → 我的发布”检查发布入口、空列表 CTA、额度读取、发布后额度变为 0、删除后仍为 0，以及普通用户/管理员入口显示。

### 2026-09-06 最新交接：微信登录 loading 已增加客户端兜底（本地未部署）

- 登录页新增 20 秒应用层超时；登录成功后先落地用户并跳转，头像/昵称改为后台补充；登录跳转失败有 `reLaunch` 回退。
- `uploadAsset`、活码上传、分享图上传、图片资料上传统一增加 30 秒上传超时。
- 验证通过：成功登录/永不返回超时模拟、112 个小程序 JS、84 个 JSON、登录测试和后端 372 个测试；`git diff --check` 通过。
- 当前生产仍未包含本次小程序改动。用户需要在微信开发者工具清缓存、重新编译并上传体验版后，在 OpenHarmony 真机验证登录；本轮未部署服务器。

### 2026-09-06 最新交接：网络 complete 回调兼容已补充（本地未部署）

- `miniprogram/utils/request.js` 增加 `complete` 携带 HTTP 响应时的统一处理，兼容 OpenHarmony 微信未触发 `success` 的情况，并避免重复结算。
- complete-only 成功、500 失败、网络失败模拟通过；小程序 JS/JSON 检查、后端登录测试和全量后端测试通过。
- 服务器代理配置已只读核对：生产 `/api/` 使用 HTTP/1.1 反代到 8004，登录请求已返回 200；不需要为本次小程序改动部署服务器。
- 必须重新编译并上传体验版后验收；若仍失败，需要微信开发者工具真机 Console/Network 日志。

### 2026-09-06 最新交接：登录再次复现，等待真机回调证据

- 15:04 左右的最新登录请求已在生产记录为 `200 OK`，Nginx 返回大小 `1120 bytes`；同一设备此前多次请求结果一致。
- 请求后没有出现用户资料、首页或其他后续请求，说明客户端没有完成会话保存/跳转；当前不能归因于服务器未部署。
- 下一步只需在微信开发者工具对 OpenHarmony 真机做一次“真机调试”：查看 `/api/auth/wechat-login` 的 Network 状态、Response 和 Console 首条异常。未获得这两项证据前不继续改服务器或堆叠客户端防护。

### 2026-09-06 最新交接：体验版登录可用，暂不新增鸿蒙兼容补丁

- 用户反馈开发版/真机调试中的鸿蒙登录异常，但上传体验版后同一鸿蒙设备可以登录。
- 当前结论：不部署服务器，不新增独立回调式登录或响应头兼容；现有 20 秒应用层超时仅作为永久 loading 兜底保留。
- 后续以体验版为准做冷启动、清会话、弱网重试三项验收；若体验版稳定通过，则关闭该开发版兼容问题。

### 2026-09-06 最新交接：微信群发布额度后端已部署生产

- 本次只部署 `backend/app/services/app_service.py` 与 `backend/app/api/routes_group_resources.py` 两个后端文件，生产备份位于 `/home/ubuntu/teambuy-backups/20260906153836/`。
- 三个应用服务与 PostgreSQL 均 healthy，生产端口为 `8004 -> 8000`；本机/公网 `/health`、`/health/db` 均通过。
- 生产 `GET /api/group-resources/publish-quota` 已生效，未登录返回 `401`；三个运行容器内两份文件哈希均与本地一致，最近日志无明显错误。
- 本地全量后端测试 `372 passed`。小程序源码未通过服务器部署更新，用户仍需在微信开发者工具清缓存、重新编译并上传体验版，才能看到“我的发布”入口和额度卡。
- 回滚标签：`teambuy-rollback-api:20260906153836`、`teambuy-rollback-worker:20260906153836`、`teambuy-rollback-archive:20260906153836`。

### 2026-09-06 最新交接：微信群体验版问题待修复

- 已确认生产“全国综合群”是旧规则记录：`rewardAmount=20` 且已到账；当前新建规则为 `+10`，不得直接改历史积分流水。
- 当前前端 Bug：已到账记录仍显示“审核中 +20”；删除/更新按钮 CSS 不是等宽；发布表单默认仍为长沙；行业列表没有“互助”第一项，也没有自定义标签功能。
- 相关服务端/前端专项检查通过：`13 passed`、JS `node --check`、`git diff --check`。本轮未修改代码、未重新部署。
- 下一轮建议按顺序修复：奖励状态文案 → 操作按钮等宽 → 全国默认 → “互助”行业与自定义标签端到端字段/筛选；同时补充旧记录和二维码更新后的回归测试。

### 2026-09-06 最新交接：微信群工具问题已收口（本地未部署）

- 已修复奖励状态显示：历史已到账 `+20` 保留账本原值并显示“已到账”，新用户待审核奖励显示“审核中 +10”；达到上限和取消状态不再伪装成待审核。
- 已修复待审核群资源删除：取消未发放奖励并释放累计奖励预留；已到账记录不回写。每日发布额度仍按当天创建历史计算，删除后不会恢复。
- 小程序与 PC 管理员发布表单默认“全国 + 互助”，新增最多 8 个自定义标签；标签已贯通保存、公开搜索、后台搜索和内容安全检查。
- PC/手机管理员新增通过内容安全检查后直接进入找群，不再进入人工审核队列；管理员更新二维码不会被普通用户奖励审核规则误置为审核中。
- 删除群资源和更新二维码操作按钮统一为等高等宽；公开卡片、我的发布卡片和发布成功页展示自定义标签。
- 验证结果：全量后端 `373 passed`，群资源专项 `14 passed`；后端 compileall、相关 JS、PC 内联脚本、WXML 事件绑定和 `git diff --check` 通过。
- 本轮未部署生产、未上传微信体验版。下一步请在微信开发者工具清缓存、重新编译上传后，重点验收：旧 `+20` 记录状态、全国/互助默认、标签搜索、管理员直发、删除后额度仍为 0。

### 2026-09-06 最新交接：微信群工具问题收口已部署生产

- 已将 6 个明确后端/PC 文件通过生产镜像文件层热修部署到 `81.70.84.35`，备份目录为 `/home/ubuntu/teambuy-backups/20260906171337-group-resource-closeout/`。
- API、两个 Worker、PostgreSQL 均 healthy；生产端口 `8004 -> 8000`，本机/公网 `/health`、`/health/db` 和公开群资源接口通过；容器内文件哈希与本地一致。
- 回滚标签为 `teambuy-rollback-api:20260906171337`、`teambuy-rollback-worker:20260906171337`、`teambuy-rollback-archive:20260906171337`。
- 小程序端没有服务器部署能力，仍需在微信开发者工具清缓存、重新编译并上传体验版后验收本轮 UI：奖励状态、全国/互助默认、标签、管理员直发和删除后额度。

### 2026-09-06 最新交接：羊毛卡片动作文案已修正

- 任务大厅卡片已按类型区分动作：羊毛显示“去查看”，普通任务和小程序任务显示“去完成”。
- 已通过前端静态检查；本轮未部署生产，需在下一次微信体验版上传后确认真机显示。

### 2026-09-06 最新交接：互帮互助演示任务已从本地代码清理

- 已删除 `shared.js` 内置的 4 条演示任务；已有设备读取任务缓存时会清理 `demo-*` 记录，服务器同步后不再补回演示任务。
- 生产互帮互助任务公开接口已核对为 0 条，PC 删除的服务器任务不会被小程序继续伪装展示。
- 验证：目标 JS `node --check`、空服务器响应与旧演示缓存 cleanup smoke、`git diff --check` 均通过。
- 本轮未部署生产、未上传微信体验版；需要在微信开发者工具清缓存、重新编译并上传体验版，用户设备才会加载新逻辑。

### 2026-09-06 最新交接：测试数据清理策略已收口（本地未部署）

- PC 工具明细的“清理测试数据”已实现分层处理：无业务流水永久删除；有关联流水的测试记录安全下架并保留记录、积分、查看和投诉审计。
- 批量接口返回 `deleted`、`archived`、`updated`、`skipped` 和 `outcomes`；PC 提示会说明每一类实际数量和跳过原因。
- 群资源、互帮互助任务和商机合作的公开列表按服务端状态过滤；群资源、互帮互助“任务管理/我的工具”不再在接口失败时静默展示旧本地发布数据，改为重试空态。
- 验证：工具工作台与群资源专项测试 `10 passed`；Python 编译、相关小程序 JS 检查、`git diff --check` 通过。
- 本轮未部署生产、未上传微信体验版。上线前需要先在 PC 生产确认筛选“仅看测试数据”后的清理结果，再由用户明确确认生产部署；小程序仍需微信开发者工具重新编译并上传体验版。

### 2026-09-06 最新交接：互帮互助任务发布 422 已修复（本地未部署）

- 根因是普通/小程序任务的 `woolPolicy` 发送 `null`，违反后端 `dict` schema；现已改为发送 `{}`。
- 请求层已把 FastAPI 校验详情转为可读文案，发布失败时不再显示 `[object Object]`。
- 验证通过：互帮互助积分测试 `12 passed`、相关 JS 语法检查和 `git diff --check`。
- 本轮未部署生产、未上传微信体验版；用户需在微信开发者工具重新编译并上传后再测试提交任务。

### 2026-09-06 最新交接：我的工具页面返回路径已补全（本地未部署）

- 互帮互助全套页面已补充父级 `fallbackUrl`，包含首页、我的工具、发布、任务管理、任务详情、账户、充值、提现和网页中转页。
- 商机合作首页、详情、发布页已补充返回兜底；微信群资源库原有返回和发布流程内部返回未改动。
- 正常有页面栈时仍返回上一页；无页面栈时按业务父级页面兜底，不再出现左上角空白且无法返回。
- 验证：相关 JS/JSON 检查和 `git diff --check` 通过。本轮未部署生产、未上传微信体验版。

### 2026-09-06 最新交接：名片预览与分享缩略图已修正（本地未部署）

- 名片编辑器的“完整预览”现在使用 `preview=1` 进入拥有者预览：保存私有草稿后通过 owner-scoped 接口读取，不再出现“资料已停止分享”。
- 私有草稿不会生成客户分享图，需点击“发客户”完成发布后才可分享；已发布名片仍使用服务端快照和版本校验。
- 名片分享图改为方形圆头像缩略图；普通资料分享图保持现有模板。
- 已通过名片预览页、分享快照插件和名片渲染器 `node --check`，以及 `git diff --check`。
- 本轮未部署生产、未上传微信体验版；后续需重新编译上传，分别验收私有草稿完整预览、已发布名片分享和客户点击后的完整页面。

### 2026-09-07 最新交接：商机合作行业字典已扩充（本地未部署）

- 新增 `miniprogram/utils/business-industry.js`，统一提供一级行业和细分行业。
- 名片编辑页与商机合作页已改为共用字典；制造业已补充服装纺织、鞋帽箱包、家电、汽配、五金、食品、建材、新能源等常用方向。
- 旧行业值保留，历史名片不需要迁移；本轮只影响小程序前端选择和筛选展示，不需要后端数据库迁移。
- 已通过相关 `node --check`；尚未部署生产或上传体验版，后续需重新编译后人工验证行业切换、细分行业选择和列表筛选。

### 2026-09-07 最新交接：PC 清理列表与置顶队列已部署生产

- 工具明细默认隐藏 `deleted`、`archived`、`removed`，删除/安全下架后刷新即从默认列表消失；服务端审计记录和显式状态查询不受影响。
- 置顶管理过滤删除/下架的互帮互助和微信群内容，业务名片继续使用非删除资料源。
- PC 后台新增右上角页面内通知，原 `alert()` 调用已替换为 `showOpsNotice`，错误反馈不再阻塞页面。
- 定向回归已通过：置顶过滤与工具明细清理测试共 `7 passed`；PC 后台内嵌脚本语法、Python 编译和 `git diff --check` 通过。
- 已部署到 `https://teambuy.lifelove.top`；三个新后端/worker 容器和数据库均 healthy，公网 `/health` 返回 200。
- 生产备份位于 `/home/ubuntu/teambuy-backups/20260907-035647/`，旧运行镜像已打回滚标签。
- 小程序端仍需微信开发者工具重新编译并上传体验版，服务器部署不会自动更新小程序包。

### 2026-09-07 最新交接：互帮互助执行者详情页已重做（本地未部署）

- 已修复跨设备点击任务卡片误显示“任务结束”的根因：`decorateTask()` 不再用执行者设备的本地发布者积分缓存判断关闭，服务端新增 `taskClosed` 作为权威字段。
- 已重做 `miniprogram/subpackages/my-tools-mutual-help/task-detail/`：顶部事实信息一行化、奖励卡、三步流程、打开入口主卡、完整任务三列摘要、完成标准和提交区分层显示。
- 效果图已生成，作为小程序真机验收的视觉参照；主题切换仍保留在当前页面，没有移动位置。
- 代码检查与测试：`node --check`、Python 编译、`git diff --check` 通过；互帮互助积分相关 `12 passed`，应用测试 `186 passed`。
- 后续上线顺序：部署后端 `taskClosed` 变更 → 微信开发者工具重新编译/上传体验版 → 用发布者和另一执行者两个账号验证分享卡片、打开小程序、返回提交全链路。

### 2026-09-07 最新交接：三类执行者任务详情页已统一改版（本地未部署）

- 继续使用第二版效果图作为统一视觉参考。
- 小程序任务：入口优先，按钮链路为“打开小程序→返回提交”。
- 普通任务：有入口时先打开入口，无入口时直接提示“阅读说明并开始”，底部统一提交文字/图片材料。
- 羊毛任务：锁定时展示“支付 X 分查看”，解锁后展示福利入口和“完整福利”，不显示普通任务的提交按钮。
- 保留原有任务状态拦截、羊毛解锁、评论、打赏、退款、图片反馈和分享逻辑。
- 自动检查：WXML 标签配对、Node 语法、`git diff --check` 通过；后端回归 `198 passed`。
- 尚未部署生产或上传微信体验版，后续需在真机重点验收三种任务的按钮状态、长文本滚动和羊毛解锁/退款路径。

### 2026-09-07 最新交接：商机合作与微信群工具打开链路已收口（本地未部署）

- 商机合作入口已移除名片摘要前置等待；现在先进入页面，名片状态后台读取，无名片时只在发布/我的名片动作引导创建。
- 商机大厅主商机列表和供需列表已隔离处理；供需接口失败不会阻塞主列表，生产白名单已允许供需卡片 GET 列表/详情匿名浏览。
- 群资源库已拆分公开群列表与账户补充接口；公开数据先渲染，账户数据后台补齐，状态请求增加 30 秒短缓存和进行中合并。
- 免费活码入口不再等待群资源库其它接口，并增加请求合并和短缓存。
- 验证：小程序全量 JS `node --check`、`git diff --check`、专项测试 `7 passed`；后端测试排除既有导入路径问题后 `367 passed`。
- 全仓库 pytest 的既有收集问题仍未处理：`backend.tests` 包导入和独立 archive-core 测试路径问题。
- 本轮未部署生产、未上传微信体验版；下一步由微信开发者工具重新编译上传体验版，重点验证：无名片进入商机、供需接口异常时主列表、微信群首屏和活码入口。
### 2026-09-07 最新交接：商机合作与微信群工具打开链路已部署生产

- 后端已部署到 `81.70.84.35:/home/ubuntu/teamBuy`；生产备份位于 `/home/ubuntu/teamBuy/.deploy-backups/20260907-1646-tool-entry/`。
- 当前使用基于旧生产镜像的源码热修复镜像，已验证 `psycopg_pool` 和 `app.main` 可导入；旧 API、worker、archive-worker 镜像均有 `rollback-20260907-1646-tool-entry` 标签。
- API、两个 worker、Postgres 均 healthy，端口为 `8004 -> 8000`；公网 `/health`、`/health/db`、供需公开列表和商机列表返回 200。
- 完整 Docker 依赖构建因远端 apt 网络卡住暂缓，网络恢复后需补做标准构建并再次健康检查。
- 小程序端仍需微信开发者工具重新编译、清缓存并上传体验版；服务器部署不会自动更新小程序包。

### 2026-09-07 最新交接：资料与互帮互助分享卡片已切换后端生成（本地未部署）

- 后端新增 `ShareCardRenderer`，当前生成固定 750×600 JPEG，覆盖电子名片、普通/图片资料，以及互帮互助的小程序任务、普通任务和羊毛任务。
- 资料使用 `POST /api/notes/{id}/share-snapshot/prepare`；任务使用 `GET /api/scrm/mutual-help/tasks/{id}/share-snapshot`。快照保存实体版本、模板版本、指纹、renderer、尺寸和历史，前端不再把用户文本/图片作为绘图输入。
- 小程序资料与任务分享链路已改为“后端准备 → 本地内存复用 → 微信返回静态 URL”；缺少有效快照时禁止原生分享，避免再次出现页面截图、空缩略图或错误详情页。
- 任务分享不再使用 `buildShareCardMessage` 的 Canvas/页面截图兜底；旧合集、商机、供需和回复包暂未迁移，仍使用原 `share_card_v12` Canvas 方案。
- 已通过 Python 编译、相关 JS `node --check`、`git diff --check`、5 类渲染输出尺寸 smoke 和原生分享回调反例检查。当前机器无 pytest，未执行 pytest。
- 本轮未部署生产、未上传微信体验版。上线时先构建/部署后端并验证字体、媒体和两个准备接口，再由用户在微信开发者工具清缓存、重新编译上传，最后人工检查聊天卡片图片与详情页跳转。

### 迁移收口补充

- 分享 fingerprint 已改为后端根据实体内容自行计算，前端不再决定快照指纹；资料/任务相关页面也已清除隐藏 Canvas 调用。
- 最终原生分享反例检查通过；本地代码已完成，仍未部署生产或上传微信体验版。

### 2026-09-07 最新交接：分享卡片后端已部署生产

- 生产地址：`https://teambuy.lifelove.top`；部署备份：`/home/ubuntu/teambuy-backups/20260907-200336/`。
- API、backend-worker、archive-worker、Postgres 均 healthy；端口为 `8004 -> 8000`，本机和公网 `/health`、`/health/db` 均通过。
- 生产采用可回滚热修复镜像，已带入当前后端源码和 Noto Sans SC 字体；容器内后端导入正常，分享图 smoke 为 750×600 JPEG。
- 标准 Dockerfile 因远端 apt 网络卡住暂未完成完整构建，待网络恢复后补做；旧镜像仍保留 `rollback-20260907-200336` 标签。
- 小程序前端尚未上传新包。用户需在微信开发者工具清缓存、重新编译并上传体验版，才能验证新的后端分享图链路。

### 2026-09-07 最新交接：任务分享卡片字号已调整（本地未部署）

- 本地任务分享模板已升级为 `mutual_task_backend_v3`，标题、摘要、标签、无图说明和底部文案字号已按聊天缩略图可读性上调。
- 产品原文案“不用加微信，选任务就可以互助”保持不变；羊毛任务仍使用独立福利文案。
- 已完成本地渲染截图和原生分享回调反例检查；生产服务器当前仍是此前已部署的 `mutual_task_backend_v1`，本轮 v3 尚未部署。
- 若要让线上聊天卡片显示本轮字号，下一步需部署后端 v3，并由用户在微信开发者工具清缓存、重新编译上传体验版；当前没有执行这两步。

### 2026-09-07 最新交接：任务分享卡片 v3 已部署生产

- 生产地址：`https://teambuy.lifelove.top`；部署备份：`/home/ubuntu/teambuy-backups/20260907-213359-task-card-v3/`。
- API、backend-worker、archive-worker、Postgres 均 healthy，端口保持 `8004 -> 8000`；本机和公网 `/health` 均通过。
- 生产容器内已确认后端源码哈希与本地 v3 一致，任务分享默认版本为 `mutual_task_backend_v3`，完整文案为“不用加微信，选任务就可以互助”。
- 本次使用现有生产镜像热修复并强制重建应用容器；未改生产 `.env`、密钥、数据库卷和媒体卷。标准 Dockerfile 完整依赖构建仍待远端 apt 网络恢复后补做。
- 小程序前端尚未上传新包。用户下一步需在微信开发者工具清缓存、重新编译并上传体验版，才能在真实聊天中看到本轮字号。

### 2026-09-07 最新交接：微信群自动化职责与增量同步设计稿（未开发）

- 已新增设计文档：`docs/stage2-docs/48-wechat-group-automation-and-incremental-sync-v1.md`。
- 当前共识：继续使用现有「资料整理助手」AScript 工程；首次系统全量扫描，后续按同步时间和会话指纹增量同步；PC 负责群台账、批次内容、任务汇总，用户负责营销许可和异常处理。
- 当前未实现：首次扫描、增量扫描、群批次配置、AScript 两个执行按钮和批量替换内容；也没有部署、上传或操作手机。
- 下一步只有在用户确认设计稿后，才进入 AScript 首次扫描的最小范围实现和真机耗时测试。

### 2026-09-07 最新交接：分享卡片字号与名片标题已修正（本地未部署）

- 本地资料分享模板为 `share_card_backend_v2`，互帮互助任务模板为 `mutual_task_backend_v4`；任务、图片资料和名片的后端 JPG 字号已按聊天缩略展示上调。
- 名片分享标题已统一为“电子名片｜高先生”这类格式；图片资料不再使用名片/资料通用标题；任务详情 path 和有效快照校验保持不变。
- 已通过相关 JS/Python 语法检查、后端渲染 750×600 JPEG smoke、三类任务渲染检查及最终原生分享回调反例检查。
- 本轮未部署生产，也未上传微信体验版。生产当前仍承载此前已部署的任务 v3/资料旧版本；要让真实聊天显示本轮内容，需要后续部署后端并由用户在微信开发者工具清缓存、重新编译、上传体验版。

### 2026-09-07 最新交接：双微信群扫描核对首轮已实现，真机待恢复

- 已实现并部署：后端原生群扫描核对接口、完整/部分覆盖门禁、按微信账号隔离的缺失清理，以及 PC 群列表微信名展示。
- AScript 资料整理助手已上传 `wechat_group_inventory` 首轮代码：扫描通讯录群聊 + 会话列表，两个微信账号分别回传；未进入发送流程。
- 删除范围：仅对应账号的 `wechat_native` 群候选；小红书记录、发送历史和已完成任务历史保留。活码绑定会解除，未开始任务会取消。
- 本地定向测试 16 项通过；生产 API、两个 worker、数据库、公网健康检查通过。
- 当前阻塞：手机 AScript 9096 在运行入口后不返回截图、控件树、状态或停止请求，无法确认扫描是否开始，也没有取得双账号删除计数。下一步先在手机上强制停止/重新打开 AScript，确认 `资料整理助手` 入口可见后再只点一次“扫描微信群”。
- 未开发：群会话指纹增量同步、群批次配置、下一批/重跑上一批按钮和批量替换内容。

### 2026-09-07 最新交接：分享卡片 v2/v4 已部署生产

- 生产地址：`https://teambuy.lifelove.top`；本次备份：`/home/ubuntu/teambuy-backups/20260907-232022/`。
- 已部署资料分享 `share_card_backend_v2` 和互帮互助任务分享 `mutual_task_backend_v4`；API、backend-worker、archive-worker、PostgreSQL 均 healthy，端口为 `8004 -> 8000`。
- 本次仅更新明确的 `share_card_renderer.py`、`routes_scrm.py`，没有覆盖生产 `.env`、密钥、数据库卷和媒体目录；服务器源码树与三个运行容器内的文件 SHA-256 已核对一致。
- 本机、公网 `/health`、`/health/db` 均通过。小程序前端仍未上传体验版；用户需在微信开发者工具清缓存、重新编译并上传后，才能在真实微信群验收“电子名片｜高先生”和新版字号。

### 2026-09-07 最新交接：资料整理助手入口菜单已恢复

- 已把入口代码同步到手机真实中文工程 `/storage/emulated/0/airscript/model/资料整理助手/__init__.py`，不再依赖此前误生成的 URL 编码副本。
- 真机从“资料整理助手”工程运行后已显示原生功能菜单，其中可见“扫描微信群”；当前没有点击该项，因此没有触发扫描、发送或群数据删除。
- 当前下一步：用户可确认菜单可见后，再单独点击“扫描微信群”进行首轮真机扫描验收；扫描结果仍需检查两个微信账号覆盖状态和后端删除计数。

### 2026-09-07 最新交接补充：九宫格已真机显示

- 入口已改为加载真实工程资源文件 `res/ui/launcher.html`，中文工程“资料整理助手”真机显示完整九宫格。
- 第 3 格“扫描微信群”已可见；本次只验证显示，没有启动扫描、发送消息或删除群数据。
- 下一步仍需用户确认后，单独点击“扫描微信群”进行双微信首轮扫描验收。

### 2026-09-07 最新交接：双微信群扫描已完成真机验收

- 用户已点击第 3 格“扫描微信群”，扫描模块实际运行完成，手机回到资料整理助手九宫格。
- PC/后端已收到两个微信账号的完整结果：高同学 53 个群（新增 0、更新 53、删除 0）；leo 140 条账号内群记录（新增 9、更新 131、缺失 4、删除 4）。
- 两个账号的扫描覆盖均为 `complete`；会话列表以滑到底部稳定结束，通讯录以已报告总数结束。没有未关联实时活码，没有取消中的待执行任务，也没有执行营销发送。
- 下一步不需要再次全量点击扫描；先在 PC 群运营页面刷新核对两个账号的群列表。后续新增群可基于当前台账设计增量同步，避免每天重复全量扫描。

### 2026-09-07 最新交接：分享卡片统一比例方案待确认

- 本轮仅生成视觉讨论稿，展示电子名片、普通资料、图片资料和互帮互助任务四种代表卡片；推荐固定后端图片比例为 5:4，底部营销话术放入图片内部。
- 推荐后端统一返回 `imageUrl`、类型化 `shareTitle` 和详情页 `path`，小程序端保持稳定分享壳；微信外层应用名/开发版提示不属于后端图片内容。
- 尚未修改代码、部署服务器或上传体验版；“普通/图片/无图资料”以及“三类任务”可以共用比例和底部结构，只替换内容模块和营销文案。
- 已确认互帮互助任务底部文案统一为“不用加微信，选任务就可以互助”；后续实现和真机缩略图验收均以此为准。

## 2026-09-08 最新交接：分享卡片 v3 本地已完成，待体验版验收

- 后端 `ShareCardRenderer` 已切换到 `share_card_backend_v3`：名片底部为“点击查看完整名片”，头像使用 aspectFill 圆形裁切；资料卡统一 750×600（5:4），无图统一通用资料插画，电商/房源价格放大加粗。
- 小程序 `share-snapshot` 插件已同步 v3 版本门禁和名片默认文案；旧 v2 快照会在进入发送入口时失效并重新准备，后端生成失败不会静默退回错误图片。
- 已完成 py_compile、Node `--check`、Pillow 渲染 smoke 和预览图检查；系统 Python 缺少 pytest，未执行 pytest。
- 本轮没有部署服务器，也没有上传小程序。下一步由用户在微信开发者工具上传 `miniprogram` 体验版，先发送电子名片、电商/房源有图和无图资料，确认聊天缩略图字号与头像裁切；确认后再单独部署生产后端并做公网验证。

## 2026-09-08 最新交接：任务管理页三个问题已本地修复

- 文件：`miniprogram/subpackages/my-tools-mutual-help/task-manage/index.wxml`、`index.js`、`index.wxss`。
- 状态筛选四项已经是独立的四等分控件，使用 `catchtap` 阻断误触导航，处理函数只接受四个合法筛选值；任务卡列表与筛选区增加间距。
- 每张任务卡底部的管理、分享、删除改为三等宽统一按钮；标题区和发布任务按钮改为左右贴边布局。
- 已通过目标页 JS 语法检查、差异空白检查和静态引用检查。本轮未部署服务器，需用户在微信开发者工具重新上传体验版后真机确认点击筛选、三按钮和不同宽度手机的布局。

## 2026-09-08 最新交接：后端已部署，小程序待上传

- 生产 `https://teambuy.lifelove.top` 已切换资料分享后端 `share_card_backend_v3`；backend、backend-worker、archive-worker 均 healthy，数据库连接正常，公网 `/health` 已验证。
- 生产备份：`/home/ubuntu/teambuy-backups/20260908-012921/`；旧三个运行镜像均保留同时间戳的 rollback 标签。
- 后端分享图已在容器内实际生成并确认 750×600 JPEG、中文字体和源码哈希正确；任务管理页前端改动未通过服务器发布。
- 用户下一步：在微信开发者工具重新编译并上传 `miniprogram` 体验版，验证任务管理页四个筛选项、三等宽按钮，以及新生成的名片/电商/房源分享图。历史聊天里的旧分享图片不会自动更新。

## 2026-09-08 最新交接：增量扫描和 PC 群列表已切换

- 本地和手机真实工程已更新：九宫格第 3 格可选择每日增量或完整核对；增量真机测试已将 leo 账号的 56 个可见群以 partial 覆盖写回，未删除任何群。
- PC 运营页已删除旧的单群单卡测试区和旧群库编辑区，当前只保留新的群列表。线上容器已做临时热更新，`https://teambuy.lifelove.top/ops` 已验证出现“群列表”且不再出现“单群单卡测试”。
- 线上正式镜像构建因基础镜像依赖下载卡住而未完成；当前容器健康，回滚标签为 `teambuy-backend:pre-20260908-004439-wechat-group-list`。网络恢复后应重新构建正式镜像并用 Compose 替换临时热更新容器。
- PC 仍未完成的核心功能：卡片素材群编号目录、每群分批内容配置、复制到第 N 批、批量替换卡片编号和文字、每天 3 次发送策略、批次执行按钮、成功/失败汇总与发送历史、被踢/无法发送人工记录，以及私聊自动发送卡片。当前页面只完成群台账与群画像配置。

## 2026-09-08 最新交接：群列表筛选与批次配置第一版已部署

- PC 群列表现在支持：按群名、微信名、群主题/地域、营销状态、最近发送、下一批次和今日次数筛选；列头右侧拖拽调整列宽，列宽保存在当前浏览器。
- 点击群的“配置”打开批次编辑窗口，可保存群主题、地域、营销状态、允许内容、备注，以及第 1–100 批的卡片编号和文字。一个批次可只发卡片、只发文字或两者同时存在。
- 批次编辑器支持“复制到第 N 批”和删除本批；群列表顶部“批量替换内容”支持按旧卡片编号和/或旧文字匹配，替换为新卡片编号和/或新文字。两项操作都不会直接发消息。
- 后端新增 `batchContents` 字段和 `/api/automation/group-candidates/batch-contents/bulk-replace`；群扫描 upsert 会保留已有批次配置。
- 本地自动化定向测试 3 passed；线上备份 `/home/ubuntu/teambuy-backups/20260908-011804/`，API/两个 worker/PostgreSQL healthy，公网 `/health` 与 `/ops` 页面标记检查通过。
- 下一步建议：实现 AScript “重跑上一批 / 运行下一批”领取接口和真实执行结果回写；再做每日 3 次限制、成功/失败汇总、发送历史和异常人工记录。卡片素材群编号目录与私聊发送仍未开发。

## 2026-09-08 最新交接：卡片目录与每日上限控制面已部署

- PC 群运营页新增“小程序卡片目录”：登记 `act-001` 这类编号、深链接、卡片标题和状态；批次配置继续引用卡片编号。目录不会打开小程序，也不会自动发送。
- 群配置新增每日发送上限，默认 3 次；当前只保存策略，不伪造发送次数，也不创建非官方自动发送任务。
- 后端新增 `automation_card_assets` 表以及 `/api/automation/card-assets` 列表/保存接口；`AutomationGroupCandidate.dailySendLimit` 对旧记录兼容默认值 3。
- 本地自动化定向测试 4 passed；线上备份 `/home/ubuntu/teambuy-backups/20260908-020520/`，三个应用容器 healthy，公网 `/health`、页面标记和未授权 403 检查通过。
- 仍未实现且不纳入非官方绕风控方案：AScript 自动群发/私聊、微信内部卡片抓取、真实发送回执。后续可做人工确认执行清单、结果人工登记或官方消息能力适配。

## 2026-09-08 最新交接：01:47 分享卡旧样式核验

- 生产日志已核对到用户 01:47（服务器日志 UTC 17:47）发送窗口：10 个 note 分享快照准备请求全部 200 OK。
- 这 10 个资料的当前快照均为 `share_card_backend_v3`、`ready`，生成时间为 01:47 左右，revision 对齐；线上名片和房源图片实际尺寸均为 750×600。
- 结论：旧资料可以生成新版，问题不是“旧资料不兼容”。优先排查用户是否查看/转发了历史微信群卡片；重新从当前小程序“发客户”入口发送才会使用新图。
- 另有明确边界：合集/展示页仍保留 `share_card_v12` 的前端绘制分享链路；若用户发送的是合集，需要单独迁移该链路，不能用 note v3 的结果判断。

## 2026-09-08 最新交接补充：资料库发送旧图根因

- 用户确认从当前入口重新发送后，已定位 `miniprogram/pages/library/index.js` 的版本门禁缺失：页面内 `shareImages` 命中时只比较资料 ID 和 revision，不比较 `share_card_backend_v3`；发送回调也再次优先取该旧地址。
- 线上数据库当前快照已是 v3，但历史快照仍保留 v10/v6，说明“同 revision 的旧图片”确实存在，能够解释服务器已生成新图而微信仍收到老款。
- 待修：资料库 `shareImages` 必须记录并校验 `styleId`/fingerprint；命中旧版本时强制丢弃，读取当前 v3 快照或重新 prepare。修复后需重新上传小程序体验版；后端不需要再次部署。

## 2026-09-08 最新交接补充：资料库旧图修复完成

- 已修复 `miniprogram/pages/library/index.js`：页面内分享图缓存现在要求 noteId、sourceRevision、`share_card_backend_v3` 和 fingerprint 同时匹配；旧缓存会自动失效。
- 资料库后台准备成功和用户点击发送时写入/读取的缓存记录均保存模板 styleId，避免同 revision 旧图再次进入微信分享回调。
- `node --check`（资料库页、分享插件）与 `git diff --check` 已通过。
- 下一步：用户在微信开发者工具重新编译并上传体验版，再从“资料库 → 发客户”发送一张旧资料验证；后端无需再次部署。合集/展示页的 `share_card_v12` 迁移仍是独立事项。

## 2026-09-08 最新交接补充：分享卡 v4 字段与隐私修复

- 已完成本地修改：资料分享模板为 `share_card_backend_v4`；普通电子名片静态图补齐实际联系方式，启用商机合作的名片显示“联系方式已保护 · 详情页查看”而不泄露真实联系方式。
- 房源/电商外层分享标题已去掉价格、户型、面积，只保留“类型｜名称”；价格和户型仍由后端固定 750×600 图片主体展示。
- 已核对资料列表和资料库左侧缩略图均为 `aspectFill`，无需重复修改。
- 验证：目标 Python 文件编译、三个小程序 JS `node --check`、分享标题样例、`git diff --check` 通过；本机缺少 pytest/Pillow 运行依赖，后端渲染单测未执行。
- 待上线：部署后端 v4 并公网验证，再由负责人在微信开发者工具清缓存、重新编译上传体验版；旧微信群历史消息不会自动更新。发布前重点验收普通名片联系方式、商机名片保护提示、房源/电商标题去重及资料库左侧 `aspectFill` 缩略图。

## 2026-09-08 最新交接：分享卡 v4 已部署生产

- 后端资料分享渲染器已上线，生产 API 使用 `teambuy-backend:deploy-20260907-185417`，容器内版本为 `share_card_backend_v4`；旧 API、backend-worker、archive-worker 均有回滚标签。
- 线上状态：API、backend-worker、archive-worker、PostgreSQL healthy；`127.0.0.1:8004/health`、数据库健康检查和 `https://teambuy.lifelove.top/health` 返回正常，公网 HTTP 200。
- 本次备份：`/home/ubuntu/teambuy-backups/20260907-185342-share-card-v4/`。未改动生产 `.env`、secrets、数据库和媒体数据。
- 本地验证已切换到 Python 3.13 `.venv313`，后端全量测试 `385 passed`；pytest/Pillow 运行环境已可用。
- 待负责人操作：在微信开发者工具重新编译并上传小程序体验版，再从“资料库 → 发客户”重新发送名片、普通资料、房源/电商和互帮互助卡片进行人工验收。历史微信群卡片不会自动换图；合集/展示页仍是独立前端分享链路。

## 2026-09-08 最新交接补充：普通资料标题修复

- 线上“你好”资料的后端快照已确认是 `share_card_backend_v4`，截图中的无图信息主体属于新版；已定位并修复小程序普通资料外层标题缺少类型前缀的问题。
- 修复后标题为“文字资料｜你好｜点开查看完整资料”；四类标题断言、Node 语法检查和差异检查通过。
- 本次无后端变更，不需要再次部署生产 API。待负责人重新编译上传小程序体验版，再从“资料库 → 发客户”发送普通资料验证。
- “未发布的小程序·体验版”是微信原生尾部，不由服务器控制；正式发布或体验包状态变化后由微信显示对应应用信息。

## 2026-09-08 最新交接：分享卡 v5 图层重做并已部署

- 已按最终规范重做资料卡：图 1 为微信原生外壳，图 2 为后端生成的完整 750×600（5:4）图片；图 2 在图 1 中间图片区域完整展示。
- 已生成并纳入项目正式无图插画：`backend/app/static/share-card-assets/universal-info-artwork.png`。图 2 无真实图片时仅将这张插画放入左侧主视觉位，不能使用整张效果图或旧 Pillow 简笔画。
- 资料图 2 的统一骨架为：顶部类型和“有图/无图”状态、下方左视觉/右信息、底部营销条。普通资料、图片、房源、电商、服务、制造业共用骨架；名片和互助任务保留自己的内容模块但仍使用固定 5:4 图层。
- 当前版本：资料 `share_card_backend_v5`；普通资料外层标题为“普通资料｜名称｜点开查看完整资料”。
- 本地验证：资料渲染定向测试 6 passed；全量测试首次 385 passed、二维码 OpenCV 单项偶发失败，单项重试通过；Node 语法和 `git diff --check` 通过。
- 生产验证：API、backend-worker、archive-worker、PostgreSQL healthy；本机/公网 `/health`、数据库检查和生产无图电商渲染 smoke 均通过。生产备份：`/home/ubuntu/teambuy-backups/20260908-033806-share-card-v5/`。
- 待负责人操作：微信开发者工具重新编译上传小程序体验版，再从“资料库 → 发客户”发送普通资料、无图电商、有图资料和名片验收。服务器不能替代小程序上传，历史微信群卡片不会自动更新。

## 2026-09-08 最新交接：ESP32 与 AScript 运行链路诊断

- 当前工作范围是 Mac 本地 AScript 工程、手机 `资料整理助手`、ESP32-C3；AScript 管理后台/商城与本阶段无关。
- Mac 端串口只读探测正常：`/dev/cu.usbmodem101` 为 ESP32-C3，MAC `10:20:BA:DC:5D:90`。当前固件无需重刷。
- 手机 AScript 版本为 `4.0.01`，运行模式为 `HID控件模式`；当前没有证据表明“数字助理模式”已启用。蓝牙运行权限显示未授权。
- 手机端 `esp32:4.7.0` 和 `esp32:4.1.2` 均能下载/加载返回 True，但导入 `esp32` 报 `bad magic number`，真实 HID 动作尚未验收。
- 下一步需先由负责人确认是否升级手机 AScript 到屏幕提示的 `4.0.12 测试版`，或允许清理并重新下载单个 `esp32` 插件缓存；在此之前不要刷固件，也不要进行微信发送测试。

## 2026-09-08 最新交接：分享标题与名片电话优化

- 已完成本地修改：资料分享标题统一最多 40 个 Unicode 字符，超出追加 `...`；电子名片标题读取名片姓名/昵称；商机名片静态图显示脱敏电话并保留详情解锁边界。
- 资料分享版本已升为 `share_card_backend_v6`，用于强制重新生成旧 v5 快照。
- 已更新后端渲染器、分享插件、名片标题工具和预览页标题入口；尚未部署生产，尚未上传小程序体验版。
- 下一步验证：Python 3.13 执行 `backend/tests/test_share_card_renderer.py`，Node 检查三个相关小程序 JS；通过后再按生产部署约定备份并发布后端，最后由负责人上传小程序体验版。

## 2026-09-08 最新交接补充：名片所有者分享联系方式

- 已修正本地代码：名片所有者从“发客户/发名片”准备分享图时显示完整联系方式；商机合作公开列表和未解锁详情仍脱敏。
- 后端通过 `include_owner_contacts=True` 明确标记所有者分享上下文，避免把商机市场的脱敏规则误套到用户自己的客户分享。
- 定向渲染测试 6 passed，相关 JS 语法、Python 编译和 `git diff --check` 通过。
- 当前 v6 修改尚未部署生产，服务器仍是上一版；部署后还需重新编译上传小程序体验版，并从个人名片入口重新发送验证。

## 2026-09-08 最新交接：分享卡 v6 已部署

- 生产已运行 `share_card_backend_v6`；名片所有者从个人“发客户/发名片”生成的分享图可包含完整电话、微信或邮箱，商机合作公开名片仍按原规则脱敏。
- API 和 backend-worker 已使用新镜像重建并 healthy；生产端口为 `8004`，公网 `/health` 与数据库健康检查通过。
- 生产备份：`/home/ubuntu/teambuy-backups/20260908-041355-share-card-v6/`；回滚镜像标签：`teambuy-backend:rollback-20260908-041355`、`teambuy-backend-worker:rollback-20260908-041355`。
- 待负责人操作：在微信开发者工具重新编译上传小程序体验版，然后从“资料库 → 发客户”重新发送普通资料、有图资料、无图资料和电子名片进行人工验收；服务器部署不能替代小程序上传。

## 2026-09-08 最新交接：分享卡 v7 本地完成

- 已修正长标题和长地址：外层标题最多 20 个 Unicode 字符并用 `...` 截断，后端图片信息区长字段最多保留 20 个字符并显示省略号。
- 已放大后端分享图关键文字，图片级样例确认房源地址不再堆叠、名片姓名和电话可读且没有越出固定卡片区域。
- 已修复“我的 → 发名片”空白根因：摘要接口和个人页都会把快照 URL 转为完整 HTTPS 地址；资料库入口保持原流程。
- 当前分享版本为 `share_card_backend_v7`，本轮尚未部署生产；部署时需更新后端渲染器，且小程序端需重新上传体验版。

## 2026-09-08 最新交接：分享卡 v8 底部文案放大

- 已将资料卡底部“少来回解释，资料一页讲清”和互助任务底部“不用加微信，选任务就可以互助”放大到 38 号字；名片底部操作文案同步为 38 号字。
- 当前分享版本为 `share_card_backend_v8`，用于强制重新生成 v7 旧快照；图像级样例确认底部文案在固定区域内未发生裁剪或溢出。
- Python 3.13 定向渲染测试 6 passed；相关小程序 JS 语法、Python 编译和 `git diff --check` 均通过。
- v8 后端已部署生产，部署批次为 `20260908-051050`；备份位于 `/home/ubuntu/teambuy-backups/20260908-051050-share-card-v8/`。API、两个 worker、数据库和公网健康检查通过；尚未上传微信体验版，负责人需重新编译上传后再发送卡片验证外层标题与后端图 2。

## 2026-09-08 最新交接：电子名片 v8 纵向布局已修复

- 已将电子名片图 2 的职位/服务、联系电话和“我能提供”内容上移，避免联系方式被下方信息色块遮挡。
- 本轮只改后端渲染器和服务端重生成指纹，保留客户端兼容的 `share_card_backend_v8`，不需要修改或重新上传小程序前端。
- 生产部署批次为 `20260908-052803`；备份位于 `/home/ubuntu/teambuy-backups/20260908-052803-share-card-layout-v2/`，API、两个 worker、数据库和公网健康检查通过。
- 验证方式：从原有“发名片”入口重新准备并发送一张名片；不要用历史微信群图片判断布局是否更新。

## 2026-09-08 最新交接：电子名片 v8 旧快照失效保护已部署

- 电子名片图 2 的职位/服务、联系电话和“我能提供”区域保持 `business_card_layout_v2` 坐标；后端摘要、列表和公开配置现在会移除旧修订的 v8 快照，避免原有小程序直接复用旧图。
- 本轮仍只修改后端，不需要修改或重新上传小程序前端；请从原有“发名片”入口重新准备并发送验证。
- 生产批次：`20260908-053640`；备份：`/home/ubuntu/teambuy-backups/20260908-053640-share-card-layout-v3/`；API、backend-worker、archive-worker、PostgreSQL 与公网健康检查均通过。
- 历史微信群中的旧图片不会被替换；只有重新准备并发送的新卡片会使用修复后的布局。

## 2026-09-08 最新交接：电子名片 v8 间距与长内容优化（待部署）

- 本地已将职位/服务和电话进一步上移，并将“我能提供”背景区域下移，避免电话与背景块相互遮挡。
- “我能提供”超过 20 个字符时保留 17 个字符并追加 `...`，固定为单行显示。
- 后端 renderer revision 已更新为 `business_card_layout_v3`，`styleId` 仍为 `share_card_backend_v8`；不需要修改小程序前端。
- Python 3.13 分享卡测试 7 passed，已通过 Python 编译和 `git diff --check`；生产尚未部署，部署前需同步 API、backend-worker、archive-worker 并保留回滚镜像。

## 2026-09-08 最新交接：电子名片 v8 服务行动态排版（待部署）

- 已修正“企业服务”与姓名重叠：职位/公司、服务范围、联系电话现在按非空内容动态顺排；无服务范围时电话不会留下过大间距，有服务范围时电话自动下移一行。
- “我能提供”仍为下方独立背景区，超过 20 字使用 `...` 单行截断。
- 当前后端 renderer revision 为 `business_card_layout_v4`，`styleId` 仍为 `share_card_backend_v8`；小程序前端无需修改。
- Python 3.13 分享卡测试 7 passed，已完成两种“企业服务”字段来源的 750×600 图片级预览；本轮尚未部署生产。

## 2026-09-08 最新交接：电子名片 v8 服务行动态排版已部署

- 生产当前后端 renderer revision 为 `business_card_layout_v4`：职位/公司、服务范围、联系电话按非空内容动态顺排；“我能提供”超过 20 字使用 `...` 单行截断。
- 本轮只更新后端，未修改小程序前端；用户应从原有“发名片”入口重新准备并发送验证。
- 生产批次：`20260908-060255`；备份：`/home/ubuntu/teambuy-backups/20260908-060255-share-card-layout-v4/`；回滚标签：`rollback-20260908-060255`。
- API、backend-worker、archive-worker、PostgreSQL、8004 端口及本机/公网健康检查均通过；历史微信群图片不会自动更新。

## 2026-09-08 最新交接：互帮互助任务入口补齐（待上传体验版）

- 普通任务和羊毛任务的任务入口输入区已改为默认展开；支持添加小程序短链接或 HTTPS 网页链接。
- 执行者/发布者任务详情：目标小程序优先跳转；外部网页显示“复制到浏览器”；小程序跳转失败、网页容器加载失败均提供复制兜底。
- 本轮只修改小程序前端，未修改后端，不需要服务器部署；需在微信开发者工具重新编译上传体验版后验证公众号文章、小红书、淘宝、京东、拼多多等入口。
- 已通过相关 JS 静态检查、JSON 解析、入口解析断言和 `git diff --check`。

## 2026-09-08 最新交接：ESP32 HID 微信单群单条测试未进入微信

- AScript 已连接 Android-237，设备报告 Android 15、AScript 4.0.03、`HID控件模式`；蓝牙权限报告为未授权。
- 设备 `hid-test` 最小探针已启动并停止。官方 `esp32` 4.7.0 与 4.1.2 均无法导入 `BleDevice`，分别报 `bad magic number`；不要把 `plug.load=true` 或运行模式当作插件可用。
- 内置 `action.Hid.click` 的无害点击验证因空主机地址 `:8080/api/click` 失败；没有发生实际 HID 点击，也没有打开微信或发送任何消息。
- 下一步只需先恢复与当前 AScript Python 运行时兼容的 ESP32 插件，或补齐内置 HID 服务主机配置；通过无害点击后再按单群、单条、发送前人工确认的门禁继续。
- 诊断逐步日志：`docs/qa/AScript_ESP32_微信单群单条测试_20260908_诊断报告.md`。

### 交接纠正

- 用户提供的 13:07 IDE 原始日志已确认该次 `hid-test` 中 `esp32` 导入成功、ESP32 连接成功，`ble.click()` 输出 `点击 None`；这是无返回值动作的正常结果，不是失败。
- 之前诊断把 `BleDevice` 与 `action.Hid` 两条路径混淆，已在日志中更正。下一步应沿用 `BleDevice` 路径，验证点击后的真实手机 UI 变化，再进入微信群定位。
