# 上线闭环与真实分享追踪 V1 Codex 自测报告

更新时间：2026-06-21

## 1. 本轮实现范围

- 新增开发文档：`docs/stage2-docs/15-launch-closed-loop-share-tracking-v1.md`。
- 新增测试清单：`docs/qa/上线闭环与真实分享追踪V1_测试清单与验收标准.md`。
- 展示页事件扩展分享来源字段：
  - `shareId`
  - `shareFromUserId`
  - `scene`
  - `referrer`
- 展示页分享路径携带追踪参数：
  - `id`
  - `sid`
  - `from`
  - `scene`
  - `ref`
- 客户公开展示页的打开、点击资料、电话咨询、复制微信都会携带同一组分享来源。
- 展示页 analytics 新增：
  - `shareSourceCount`
  - `topShares`
  - 最近事件中的 `shareId/scene/referrer`
- 单展示页效果页新增“分享批次”模块，展示每次分享带来的打开、看资料和咨询。

## 2. P0 覆盖情况

| 测试项 | 结果 | 说明 |
| --- | --- | --- |
| 分享路径携带追踪参数 | 已覆盖 | 小程序分享路径生成 `sid/from/scene/ref` |
| 客户打开记录 view | 已覆盖 | 后端事件接口和测试覆盖 |
| 客户点击资料记录 note_click | 已覆盖 | 后端事件接口和测试覆盖 |
| 电话咨询记录 phone_click | 已覆盖 | 前端会携带追踪来源；后端统计已有 |
| 复制微信记录 wechat_copy | 已覆盖 | 前端会携带追踪来源；后端统计已有 |
| 事件归属 shareId | 已覆盖 | 自动化测试断言同一 `shareId` 产生打开、点击、咨询 |
| 未发布页不可记录 | 已覆盖原规则 | 后端仍只允许 published 展示页记录事件 |
| analytics owner 权限 | 已覆盖 | 原测试继续验证非 owner 403 |
| 匿名访客不伪装实名 | 已覆盖原规则 | 匿名仍按 anonymousId 统计 |
| 经营看板聚合正确 | 已覆盖原规则 | 展示页事件继续进入经营看板聚合 |

## 3. 自动化验证

- 后端核心测试：76 passed。
- 后端全量测试：113 passed。
- Postgres 仓储字段/index 测试：3 passed。
- Python 编译检查：通过。
- 小程序全量 JS `node --check`：通过。
- 小程序 JSON 解析：通过。
- `git diff --check`：通过。

## 4. 生产部署与公网验证

- 生产后端已部署。
- 部署前备份目录：`/home/ubuntu/teamBuy-deploy-backups/20260621-104718-launch-share-tracking-v1`。
- 公网 `/health` 返回正常，数据库连接正常。
- 生产 mock 登录返回 403：“测试登录已关闭”。
- 使用真实测试账号 `user_25ec00a0f0` 的已发布展示页写入一条带 `shareId=share_prod_smoke_20260621` 的 `view` 冒烟事件。
- 生产 analytics 已返回：
  - `shareSourceCount=1`
  - `topShares[0].shareId=share_prod_smoke_20260621`
  - `openCount=1`

## 5. 未覆盖项

- 微信真机分享卡片标题、封面、路径参数需要用户上传新版小程序后人工确认。
- 客户从微信群打开后的真实匿名/实名表现需要真机确认。

## 6. 回归建议

1. 发布者从展示页列表点击“发给客户”。
2. 客户打开分享页。
3. 客户点击资料。
4. 客户点击电话或复制微信。
5. 发布者打开单展示页效果页，确认“分享批次”出现打开、看资料、咨询。
6. 发布者打开经营看板，确认总打开、访客、看资料、咨询同步增加。

## 7. 当前结论

后端、小程序静态验证和生产公网冒烟均通过，可以进入真机人工确认。

小程序前端需要用户在微信开发者工具上传/预览新版后，才能完成真实分享链路验收。
## 2026-06-21 补充：公开展示页发布快照缓存

### 修复范围

- 展示页发布时生成公开快照 `publicSnapshot`。
- 客户打开公开展示页时优先读取快照，不再每次实时拉取笔记并重新拼装。
- 老的已发布展示页首次公开访问自动补快照。
- 重新发布刷新快照版本。
- 删除资料时同步修剪相关展示页快照。

### 验证结果

- 展示页相关后端测试：3 passed。
- 后端全量测试：113 passed。
- 后端代码编译检查：通过。
- 小程序 JS 全量语法检查：通过。
- 小程序 JSON 解析检查：通过。

### 线上验证

- 已部署到 `https://teambuy.lifelove.top`。
- `/health` 返回正常。
- 指定展示页 `showcase_627fc56634` 连续两次公开访问均返回 `snapshotVersion=1`。
- 两次返回的 `snapshotCreatedAt` 均为 `2026-06-21T14:50:04.334312+08:00`，确认第二次读取缓存快照。

### 剩余人工确认

- 用户重新发布展示页后，确认 `snapshotVersion` 增加，客户页内容随发布快照更新。

## 2026-06-21 补充：1-4 统一收口自测

### 覆盖范围

- 1. 扩展展示页事件字段和接口请求。
- 2. 展示页分享路径生成并携带 `shareId`。
- 3. 公开展示页记录打开、资料点击、电话、复制微信时携带分享来源。
- 4. 单展示页 analytics 和经营看板聚合分享来源。

### 本轮补齐

- 经营看板后端返回 `summary.shareSourceCount` 和 `topShares`。
- 经营看板详情页和复用组件展示“分享来源”。
- 后端测试补充经营看板 `shareId` 聚合断言。
- 开发文档和测试清单同步当前中转分享路径。

### 验证结果

- 后端全量测试：113 passed。
- 小程序 JS 全量语法检查：通过。
- 小程序 JSON 解析检查：通过。
- 后端编译检查：通过。
- `git diff --check`：通过。
- 线上后端已部署，`/health` 正常。
- 线上经营看板 `user_25ec00a0f0` 返回 `shareSourceCount=11`、`topSharesLength=6`。

### 剩余人工确认

- 用户重新上传小程序体验版/正式版后，真机查看经营看板“分享来源”模块是否展示正常。
