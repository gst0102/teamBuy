# 企业微信真实联调记录

## 2026-06-08 首次服务器联调

### 已完成

- 代码已推送到 GitHub `main`。
- 腾讯云服务器 `81.70.84.35` 已克隆仓库到 `/home/ubuntu/teamBuy`。
- Docker Compose 已启动 `postgres` 和 `backend`。
- 后端宿主机端口使用 `8002`，避免占用已有 `8000` 服务。
- Compose 构建确认使用清华 PyPI 源。
- `backend/.env` 未提交 Git，服务器端单独配置。
- PostgreSQL 仅暴露在 Docker 内部网络，避免占用宿主机 `5432`。

### 健康检查

服务器本机检查：

```text
GET http://127.0.0.1:8002/health -> 200
GET http://127.0.0.1:8002/health/db -> connected=true
GET http://127.0.0.1:8002/api/wecom/config-check -> success=true, useMock=false, missing=[]
```

公网 IP 检查：

```text
GET http://81.70.84.35:8002/health -> 200
```

### Nginx 反代

已在服务器宿主机 Nginx 增加 `teambuy.liftlove.top` 的 HTTP 反代：

```text
/api/ -> http://127.0.0.1:8002/api/
/health -> http://127.0.0.1:8002/health
/media/ -> http://127.0.0.1:8002/media/
```

带 Host 头的本机反代验证已通过：

```text
GET http://teambuy.liftlove.top/health --resolve teambuy.liftlove.top:80:127.0.0.1 -> 200
GET http://127.0.0.1/api/wecom/config-check with Host=teambuy.liftlove.top -> 200
```

服务器 `backend/.env` 已更新：

```text
PUBLIC_BASE_URL=https://teambuy.liftlove.top
WECOM_USE_MOCK=false
```

### 当前阻塞

1. DNS 尚未在本机和服务器解析器中生效。

当前查询结果：

```text
nslookup teambuy.liftlove.top 223.5.5.5 -> Non-existent domain
server getaddrinfo(teambuy.liftlove.top) -> Name or service not known
```

需要在域名服务商后台确认 `teambuy.liftlove.top` 的 A 记录为：

```text
81.70.84.35
```

如果存在 AAAA 记录但没有配置 IPv6 到本服务器，建议先删除 AAAA，避免客户端走到错误地址。

2. HTTPS 证书尚未签发。

DNS 生效后，在服务器执行：

```bash
sudo certbot --nginx -d teambuy.liftlove.top
sudo nginx -t
sudo systemctl reload nginx
```

3. 真实 `sync_msg` 返回 `48002 api forbidden`。

手动触发：

```text
POST http://127.0.0.1:8002/api/wecom/real-sync?max_pages=1
```

企业微信返回：

```text
errcode=48002
errmsg=api forbidden
from ip=81.70.84.35
```

初步判断代码已成功请求到企业微信接口，当前更像企业微信后台权限问题。需要重点检查：

- `WECOM_SECRET` 是否为“微信客服”对应 Secret，不是小程序 Secret，也不是其他自建应用 Secret。
- 企业微信后台是否开启“微信客服-通过 API 管理微信客服帐号”。
- `WECOM_OPEN_KFID` 对应客服账号是否允许 API 管理。
- 企业微信后台是否需要配置可信 IP/白名单，加入 `81.70.84.35`。
- 当前企业/应用是否有微信客服相关 API 权限。

### 企业微信 callback URL

DNS 和 HTTPS 完成后，在企业微信后台配置：

```text
https://teambuy.liftlove.top/api/wecom/callback
```

Token 和 EncodingAESKey 必须与服务器 `backend/.env` 一致。

### 下一轮测试顺序

1. 验证 `https://teambuy.liftlove.top/health` 返回 200。
2. 验证 `https://teambuy.liftlove.top/api/wecom/config-check` 返回 success=true。
3. 在企业微信后台保存 callback URL。
4. 从真实客户会话发送文本消息。
5. 查看 `/api/wecom/sync-tasks`、`/api/imports/pending`、`/api/wecom/media-retries`。
6. 继续测试链接、图片、视频、位置和微信笔记。
