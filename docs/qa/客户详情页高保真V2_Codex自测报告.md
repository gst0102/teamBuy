# 客户详情页高保真 V2 Codex 自测报告

## 结论

客户详情页已按确认的高保真稿完成前端重构，当前未部署生产。

## 已实现

- 首屏顺序调整为客户身份、真实浏览证据、下一步判断、可联系渠道。
- “客户刚刚看了什么”绑定当前详情返回的首条资料，标题、查看次数、封面和跳转不再固定为房源。
- “最近动态”和“相关资料”合并到同一个白色容器，默认折叠，点击整行展开或收起。
- 移除首屏跟进表单、重点标签和完整资料列表，避免详情页变成长列表；原有跟进保存逻辑仍保留在脚本中。
- 底部操作与效果稿一致：左侧浅橙色放弃/恢复，右侧蓝色开始/继续。
- 电话提供拨打和复制，微信号只提供“复制微信号”；联系方式操作区固定宽度并禁止换行。

## 自动化检查

- `node --check miniprogram/pages/customer-detail/index.js`：通过。
- `python3 -m json.tool miniprogram/pages/customer-detail/index.json`：通过。
- `git diff --check`：通过。
- `backend/tests/test_sales_scrm.py`：16 passed。

## 待人工验收

1. 在微信开发者工具打开 `/Users/yiyi/Desktop/Desktop/myprojects/teamBuy/miniprogram`，清缓存、重新编译并上传体验版。
2. 打开不同来源的客户详情，确认“客户刚刚看了什么”标题随客户真实来源变化，并能打开对应资料。
3. 点击“最近动态”和“相关资料”两行，确认展开、收起和箭头方向正常，且不会额外触发网络请求。
4. 用有手机号、微信号和邮箱的客户检查横向布局，确认手机号不竖排、微信按钮不显示“拨打”。
5. 检查全面屏底部安全区、无联系人空态、匿名客户、无来源资料和长标题。
