# 工作台第一期_验收报告

更新时间：2026-06-23

验收对象：首页与 Tabbar 工作台模式一期

参考资料：

- `docs/qa/首页Tabbar工作台模式一期_Codex自测报告.md`
- `docs/qa/首页与 Tabbar 工作台模式一期_测试清单与验收标准.md`
- `docs/stage2-docs/17-home-tabbar-workspace-mode.md`

## 1. 验收结论

结论：不通过。

原因：

- P0 测试项未全部闭环，不能给出“通过”。
- P0-23“业务识别提示不强迫切换”仅部分覆盖，缺少“是否切换到对应工作台”的专门提示。
- P0-27“权限和隐私不倒退”未提供专项真机或接口回归证据，只说明本轮未改后端权限逻辑，不能作为验收通过依据。
- 测试清单明确要求小程序 Tabbar、首次引导、模式切换、首页布局和工作台入口必须真机确认；当前自测报告中大量 P0 项仍是“代码通过，待真机确认”。

补充说明：

- 当前不是“主链路完全不可用”的结论，而是“未达到 P0 全部验收通过”的结论。
- 修复 P0-23，并补齐 P0-27 权限回归和真机主链路确认后，可以进入复测。

## 2. P0 覆盖检查

P0 是否全部覆盖：否。

| 范围 | 覆盖状态 | 验收意见 |
| --- | --- | --- |
| P0-01 至 P0-22 | 代码侧已覆盖，待真机确认 | 不能直接判定通过，需要用户用最新体验版在真机确认 |
| P0-23 | 部分覆盖 | 缺少“识别后建议切换对应工作台，同时允许继续当前工作台”的专门交互 |
| P0-24 至 P0-26 | 代码侧已覆盖，待真机确认 | 需要真机确认更多工具、路由、空态无白屏和无页面不存在 |
| P0-27 | 未形成专项回归证据 | 需要验证普通访客或非 owner 不能看到 owner 工作台和看板数据 |

## 3. 已覆盖测试项

以下项目根据 Codex 自测报告，属于代码侧已覆盖，但仍需真机确认：

- Tabbar 已调整为：首页 / 资料 / 合集 / 工作台 / 我的。
- 第四个 Tab 前台文案为“工作台”。
- 首页支持首次选择常用工作台模式。
- `workspaceMode` 本地偏好已支持保存、读取和切换。
- 首页内容会按日常资料台、房源工作台、团购工作台、服务工作台变化。
- 日常资料台默认文案已避开客户、线索、接龙、看房、买家、SCRM 等经营词。
- 房源、团购、服务模式已按各自语义显示快捷入口和工作台内容。
- 客户看板已从“我的”迁出，归位到工作台反馈中心。
- 资料页已做降噪，默认聚焦单条资料管理。
- 合集 Tab 可进入，并提供轻版空态和四类创建方向。
- “我的”页提供常用工作台设置入口。
- 模式切换只改偏好和展示，不删除资料或改变资料归属。
- 更多工具保留非当前模式功能。
- 静态路由扫描未发现已知 Tabbar 跳转阻断。
- 首页、资料、合集、工作台有空态或引导文案。

已执行并通过的自动化检查：

```text
find miniprogram -name '*.js' -print0 | xargs -0 -n 1 node --check
```

```text
递归解析 miniprogram 下所有 JSON，共 44 个
```

```text
git diff --check
```

专项扫描已覆盖：

- 未发现 `switchTab` 到已移出 Tabbar 的 `pages/resource-create/index`。
- 未发现页面内 `navigateTo` 到已成为 Tab 页的 `pages/showcases/index`。
- 首页、工作台、我的、资料、合集和 `workspace-mode` 核心页面未发现新增核心 `px` 布局尺寸。
- 日常资料台相关页面未命中“日常资料台 + 客户”等经营词组合。

## 4. 未覆盖或异常项

| 编号 | 级别 | 问题 | 影响 |
| --- | --- | --- | --- |
| QA-P0-01 | P0 | P0-23 业务识别提示仅部分覆盖 | 用户整理出房源、商品、服务资料后，系统没有明确建议切换到更合适工作台，也没有专门保留“继续当前工作台”的选择链路 |
| QA-P0-02 | P0 | P0-27 权限和隐私缺专项回归证据 | 无法确认普通访客或非 owner 是否会误看到 owner 的工作台、看板、客户数据 |
| QA-P0-03 | P0 | 真机主链路未完成验收 | Tabbar、首次引导、模式切换、首页布局、工作台入口仍可能在真机上出现页面不存在、白屏、错位或缓存问题 |
| QA-P1-01 | P1 | 首页最近反馈未按具体反馈类型进入筛选 | 用户点击浏览、留资、咨询、接龙、预约等反馈后，可能只能进入工作台总页，处理效率不足 |
| QA-P1-02 | P1 | 资料页未按 `workspaceMode` 做默认筛选或推荐 | 房源、团购、服务用户进入资料页后，资料列表仍不够贴合当前工作台 |
| QA-P1-03 | P1 | 合集页未按当前模式动态推荐 | 四类方向固定展示，未突出当前模式最应该创建的合集类型 |
| QA-P1-04 | P1 | `workspaceMode` 仅本地保存 | 换设备、清缓存或重新登录后会重新选择，可能造成首页和工作台体验不一致 |

## 5. 需要开发 Codex 修复的 Bug 单

### BUG-01：业务识别后缺少切换工作台专门提示

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P0 |
| 所属模块 | 首页 / 模式切换 / 资料识别 |
| 对应测试项 | P0-23 |
| 复现步骤 | 1. 在日常资料台创建或整理一条房源、商品或服务资料；2. 查看类型识别后的提示；3. 检查是否出现切换到对应工作台和继续当前工作台两个选择 |
| 实际结果 | 自测报告显示只做到“识别后不强迫转换资料类型”，尚未新增“是否切换到对应工作台”的专门提示 |
| 预期结果 | 系统可以建议切换到更合适的工作台，但必须提供继续当前工作台的选项，不能强制改变用户身份 |
| 可能原因 | 资料识别提示只覆盖资料类型确认，没有串联 `workspaceMode` 切换建议 |
| 建议修复方向 | 在房源、商品、服务识别成功后的提示层增加两个明确动作：切换到对应工作台、继续当前工作台；切换只更新工作台模式，不改变资料归属 |
| 修复后必须回归 | P0-03、P0-04、P0-07、P0-22、P0-23 |
| 验收标准 | 四种模式下都能触发对应提示；点击切换后首页和工作台同步变化；点击继续后资料保留在当前模式流程内 |

### BUG-02：权限和隐私未形成专项回归证据

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P0 |
| 所属模块 | 工作台 / 经营看板 / 展示页效果 / 权限隔离 |
| 对应测试项 | P0-27 |
| 复现步骤 | 1. 使用 owner 账号创建资料、合集或展示页并产生看板数据；2. 使用非 owner 或普通访客访问相关页面；3. 尝试进入工作台、看板、客户数据、展示页效果数据 |
| 实际结果 | 自测报告仅说明本轮未做新后端权限变更，未提供真机或接口回归结果 |
| 预期结果 | 普通访客和非 owner 不能看到 owner 后台统计、客户数据、工作台数据；工作台迁移不破坏原有数据隔离 |
| 可能原因 | 本轮重点是前端信息架构迁移，权限回归未补充证据 |
| 建议修复方向 | 开发 Codex 补充权限回归脚本或接口验证记录；如发现越权，再修复接口鉴权和前端入口拦截 |
| 修复后必须回归 | P0-14、P0-15、P0-16、P0-25、P0-27 |
| 验收标准 | 至少覆盖 owner、非 owner、匿名访客三类身份；越权访问返回拒绝或空态，不返回敏感数据 |

### BUG-03：首页最近反馈未按反馈类型进入对应筛选

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P1 |
| 所属模块 | 首页 / 工作台 / 最近反馈 |
| 对应测试项 | P1-10 |
| 复现步骤 | 1. 首页出现浏览、留资、咨询、接龙、预约等最近反馈；2. 点击某一类反馈；3. 查看进入页面是否自动筛选到对应类型 |
| 实际结果 | 自测报告显示入口可进入工作台，但未按具体反馈类型筛选 |
| 预期结果 | 点击不同反馈后进入工作台对应筛选或详情，减少用户二次查找 |
| 可能原因 | 首页反馈卡只做了统一跳转，没有携带反馈类型参数 |
| 建议修复方向 | 为反馈入口补充 `type` 或 `tab` 参数，工作台页面根据参数定位到对应筛选 |
| 修复后必须回归 | P0-14、P1-09、P1-10 |
| 验收标准 | 不同反馈入口能进入对应工作台分组；无数据时展示对应空态 |

### BUG-04：资料页未按工作台模式做默认筛选或推荐

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P1 |
| 所属模块 | 资料页 / 工作台模式 |
| 对应测试项 | P1-11 |
| 复现步骤 | 1. 切换到房源、团购或服务工作台；2. 打开“资料”Tab；3. 查看资料页默认筛选和推荐 |
| 实际结果 | 自测报告显示资料页暂未按 `workspaceMode` 做默认筛选，只做了资料库降噪 |
| 预期结果 | 当前模式下优先展示或推荐对应类型资料，同时保留查看全部资料的能力 |
| 可能原因 | `workspaceMode` 尚未接入资料页筛选默认值 |
| 建议修复方向 | 资料页读取当前工作台模式，设置默认筛选或推荐提示；提供“全部资料”入口避免隐藏数据 |
| 修复后必须回归 | P0-17、P0-18、P0-22、P1-11 |
| 验收标准 | 四种模式下资料页默认呈现符合当前模式；切换模式不删除、不改归属、不屏蔽全部资料 |

### BUG-05：合集页未按当前模式动态推荐

| 字段 | 内容 |
| --- | --- |
| 严重级别 | P1 |
| 所属模块 | 合集页 / 工作台模式 |
| 对应测试项 | P1-12 |
| 复现步骤 | 1. 分别切换日常、房源、团购、服务四种模式；2. 打开“合集”Tab；3. 查看空态和创建方向排序 |
| 实际结果 | 自测报告显示合集页固定展示四类方向，未按当前模式动态排序或突出推荐 |
| 预期结果 | 当前模式下突出最相关的合集方向，例如房源模式优先房源推荐合集，团购模式优先团购合集 |
| 可能原因 | 合集页一期轻版未接入模式推荐策略 |
| 建议修复方向 | 根据 `workspaceMode` 调整合集创建方向排序、标题或推荐标记 |
| 修复后必须回归 | P0-19、P0-20、P1-12 |
| 验收标准 | 四种模式下合集页可正常打开，推荐方向清晰，不影响其他类型合集入口 |

## 6. 回归测试清单

### 6.1 P0 必回归

1. 清缓存或新用户首次进入，确认出现常用工作台选择。
2. 依次选择日常资料台、房源工作台、团购工作台、服务工作台，确认首页和工作台同步变化。
3. 关闭并重新打开小程序，确认已保存模式不重复打扰。
4. 确认底部 Tabbar 只显示：首页、资料、合集、工作台、我的。
5. 确认第四个 Tab 固定叫“工作台”。
6. 确认日常资料台不默认暴露客户、线索、接龙、看房、预约、买家、SCRM。
7. 确认房源模式展示房源、看房客户、预约看房、房源效果等语义。
8. 确认团购模式展示商品、团购、接龙、买家、待处理订单或接龙等语义。
9. 确认服务模式展示电子名片、服务方案、咨询客户、预约记录等语义。
10. 确认“我的”页不再铺经营看板。
11. 确认工作台可进入对应反馈中心。
12. 确认单个展示页效果入口仍可进入。
13. 确认单条资料互动或效果入口仍可进入。
14. 确认资料页聚焦单条资料管理，核心动作可用且不挤压主要信息。
15. 确认合集 Tab 可打开，有轻版入口或空态。
16. 确认“我的”页可修改常用工作台，修改后首页和工作台同步变化。
17. 确认模式切换不删除资料、不改变资料 owner。
18. 确认业务识别提示允许切换到对应工作台，也允许继续当前工作台。
19. 确认更多工具能进入非当前模式功能。
20. 确认所有 Tabbar 和模式快捷入口无页面不存在、白屏、死循环跳转、首页数据加载失败。
21. 确认新用户无资料、无合集、无客户动作时，首页、资料、合集、工作台均有合理空态。
22. 使用 owner、非 owner、匿名访客分别回归工作台和看板数据权限。

### 6.2 P1 重点回归

1. 模式选择卡片文案是否准确。
2. 首页是否是当前模式工作台，而不是全功能宫格堆叠。
3. 每个模式首页主入口是否控制在 3 到 4 个。
4. 前台是否避免暴露 `workspaceMode`、`Dashboard`、`SCRM`、`Lead` 等内部词。
5. 首页最近成果点击是否进入对应详情。
6. 首页最近反馈点击是否进入对应筛选或详情。
7. 资料页默认筛选或推荐是否随模式变化。
8. 合集页空态或创建入口是否随模式突出推荐。
9. Tabbar、按钮、模式卡片、状态标签、底部按钮文字是否上下左右居中。
10. iPhone 和 iPad 真机下页面是否不重叠、不截断、不过度拉伸。
11. 旧客户看板、旧访客线索入口是否跳到新工作台位置或有清晰过渡。

### 6.3 自动化回归

必须重新执行：

```text
find miniprogram -name '*.js' -print0 | xargs -0 -n 1 node --check
```

必须重新执行 JSON 解析：

```text
递归解析 miniprogram 下所有 .json
```

必须重新执行：

```text
git diff --check
```

建议补充：

- 扫描 `app.json` 的 Tabbar 页面和页面注册是否完整。
- 扫描 `workspaceMode` 是否只出现 `notes/property/groupbuy/service`。
- 扫描日常资料台前台文案是否误出现客户、线索、SCRM、接龙、看房、买家。
- 扫描 Tabbar 页面是否存在错误 `navigateTo` / `switchTab`。
- 扫描核心页面样式是否遵守 `rpx` 和 flex 居中规则。

## 7. 上线前必须检查的事项

- 已修复 P0-23，业务识别后有“切换对应工作台”和“继续当前工作台”两个清晰选择。
- 已补齐 P0-27 权限和隐私回归证据，确认非 owner 和普通访客不能看到 owner 工作台、看板、客户数据。
- 已上传最新体验版或正式版，用户真机测试的版本确实包含本轮改动。
- 已在至少一台 iPhone 和一台 iPad 上确认 Tabbar、首次引导、模式切换、首页布局、工作台入口无阻断。
- 已确认 Tabbar 为：首页 / 资料 / 合集 / 工作台 / 我的。
- 已确认普通用户日常资料台不默认暴露经营入口。
- 已确认客户看板不在“我的”页铺开。
- 已确认工作台按模式切换内容和命名。
- 已确认切换模式不会删除资料或改变资料归属。
- 已确认资料库能继续管理、编辑、分享单条资料。
- 已确认合集页有入口和空态。
- 已确认展示页列表、编辑、预览、发给客户、单页效果入口没有被本次 Tabbar 重构破坏。
- 已确认经营看板、待联系、客户资料、订单或接龙入口仍可从新工作台或更多工具到达。
- 已确认按钮、标签、Tabbar、快捷入口文字上下左右居中，不拆行、不偏上、不挤压主要信息。
- 已确认本次未把 P2 的完整合集中心、完整 CRM、支付、库存、商城、会员权益中心误标为已完成。

## 8. 复测准入建议

建议下一轮先按以下顺序处理：

1. 开发 Codex 修复 BUG-01。
2. 开发 Codex 补充 BUG-02 的权限回归证据；如发现越权，再修复代码。
3. 重新上传体验版。
4. 用户按第 6 节执行真机 P0 回归。
5. AI 测试官基于修复报告和真机结果输出复测与回归报告。

在 P0-23、P0-27 和真机主链路未闭环前，不建议进入上线确认。
