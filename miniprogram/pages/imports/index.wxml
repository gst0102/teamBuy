<view class="page-shell">
  <custom-nav showBack="{{true}}" />
  <view class="imports-hero">
    <view>
      <view class="hero-title">待认领导入</view>
      <view class="hero-subtitle">外部导入生成的资料草稿会出现在这里，认领后可继续编辑和管理。</view>
    </view>
    <view class="imports-visual">认领</view>
  </view>

  <view class="panel">
    <view class="section-head">
      <view class="section-title">导入处理</view>
      <view class="section-link">待认领</view>
    </view>
    <view class="step-row">
      <view class="step-item">
        <view class="step-dot">1</view>
        <view>外部内容导入</view>
      </view>
      <view class="step-item">
        <view class="step-dot">2</view>
        <view>自动生成资源草稿</view>
      </view>
      <view class="step-item">
        <view class="step-dot">3</view>
        <view>认领编辑并分享</view>
      </view>
    </view>
    <button class="ghost-btn" bindtap="handleOpenLibrary">进入我的资源库</button>
  </view>

  <view class="panel" wx:if="{{notifications.length}}">
    <view class="section-title">导入结果</view>
    <block wx:for="{{notifications}}" wx:key="id">
      <view class="notice-row">
        <view class="notice-dot {{item.status}}"></view>
        <view class="muted">{{item.message}}</view>
      </view>
    </block>
  </view>

  <view class="section-head">
    <view class="section-title">待认领导入</view>
    <view class="section-link" bindtap="handleOpenVisits">查看访问 ></view>
  </view>
  <block wx:for="{{imports}}" wx:key="id">
    <view class="import-card">
      <view class="import-main">
        <view class="import-title">{{item.titleCandidate}}</view>
        <view class="muted">状态：{{item.status}} ｜ 来源：{{item.sourceType}}</view>
      </view>
      <button class="ghost-btn" data-id="{{item.id}}" bindtap="handleClaim">认领并编辑</button>
    </view>
  </block>
  <view class="empty-state" wx:if="{{!imports.length && !loading}}">暂无待认领内容。外部导入成功后，资料草稿会出现在这里。</view>
</view>
